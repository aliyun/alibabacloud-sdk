/**
  *
  */
import BaseClientBuilder;
import TeaAsyncHandler;
import TeaRequest;
import AsyncRequestBody;
import RequestBody;
import AsyncResponseHandler;
import ClientConfiguration;
import ClientExecutionParams;
extends BaseClientBuilder;
type @product = string
type @version = string
type @endpointRule = string
type @endpointMap = map[string]string
type @REQUEST = TeaRequest
type @handler = TeaAsyncHandler

init(configuration: ClientConfiguration){
  @handler = new TeaAsyncHandler(configuration);
  @product = 'SasClaw';
  @version = '2026-06-26';
  @endpointRule = 'regional';
  @endpointMap = {
  };
}

function close(): void {
  @handler.close();
}

model ChatUserSecAgentRequest {
  agent?: string(name='Agent', example='sec-ops-agent', position='Body'),
  attachmentStagingId?: string(name='AttachmentStagingId', description='附件暂存 ID', example='stg-6f1d9c8b7a2e4530', position='Body'),
  attachments?: string(name='Attachments', description='附件列表 JSON 字符串', example='["oss/input.txt","oss/raw.txt"]', position='Body'),
  channel?: string(name='Channel', description='逻辑渠道名', example='console', position='Body'),
  executionMode?: string(name='ExecutionMode', description='执行模式: single/team/role', example='single', position='Body'),
  extraParams?: string(name='ExtraParams', description='扩展参数 JSON 字符串，如 execution_mode、target 等', example='{"execution_mode":"team","target":"team:auto"}', position='Body'),
  memory?: boolean(name='Memory', position='Body'),
  model?: string(name='Model', example='qwen-max', position='Body'),
  prompt?: string(name='Prompt', description='用户提问；新会话时必填，恢复/交互时可空', example='帮我梳理最近 24 小时的高危告警并给出处置建议', position='Body'),
  responseLanguage?: string(name='ResponseLanguage', example='zh-CN', position='Body'),
  sessionId?: string(name='SessionId', example='5f2c1b9a8d3e4c7f', position='Body'),
  skill?: string(name='Skill', example='alert-analysis', position='Body'),
  stream?: boolean(name='Stream', position='Body'),
  talkId?: string(name='TalkId', example='9b1e7d2c4a6f8e30', position='Body'),
  target?: string(name='Target', description='执行目标', example='sec-ops-team-01', position='Body'),
  timeZone?: string(name='TimeZone', example='Asia/Shanghai', position='Body'),
  userInputInfo?: string(name='UserInputInfo', description='会话恢复/交互提交信息 JSON 字符串', example='{"sessionId":"session_example","talkId":"talk_example","formId":"interaction_example","formValues":{"q1":{"kind":"selected","optionIds":["q1_o1"]}},"formAction":"submit"}', position='Body'),
}

model ChatUserSecAgentResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: string(name='body'),
}

/**
  * @param request  the request parameters of ChatUserSecAgent  ChatUserSecAgentRequest
  * @return ChatUserSecAgentResponse
 */
async function chatUserSecAgent(request: ChatUserSecAgentRequest): ChatUserSecAgentResponse {
  @handler.validateRequestModel(request);
  return @handler.do('RPC', 'ChatUserSecAgent', 'POST', '/', 'string', true, 'form', request);
}

function chatUserSecAgentWithResponseIterable(request: ChatUserSecAgentRequest): iterator[string] {
  @handler.validateRequestModel(request);
  yield @handler.doSSERequestReturnString('SSE', 'ChatUserSecAgent', 'POST', '/', 'string', true, 'form', request);
}

