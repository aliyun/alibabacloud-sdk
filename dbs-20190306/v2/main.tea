/**
 *
 */
import OpenApi;
import OpenApi.OpenApiUtil;

extends OpenApi;


init(config: OpenApiUtil.Config){
  super(config);
  @endpointRule = 'regional';
  @endpointMap = {
    'cn-qingdao' = 'dbs-api.cn-hangzhou.aliyuncs.com',
    'cn-beijing' = 'dbs-api.cn-hangzhou.aliyuncs.com',
    'cn-chengdu' = 'dbs-api.cn-chengdu.aliyuncs.com',
    'cn-zhangjiakou' = 'dbs-api.cn-hangzhou.aliyuncs.com',
    'cn-huhehaote' = 'dbs-api.cn-huhehaote.aliyuncs.com',
    'cn-hangzhou' = 'dbs-api.cn-hangzhou.aliyuncs.com',
    'cn-shanghai' = 'dbs-api.cn-hangzhou.aliyuncs.com',
    'cn-shenzhen' = 'dbs-api.cn-hangzhou.aliyuncs.com',
    'cn-hongkong' = 'dbs-api.cn-hangzhou.aliyuncs.com',
    'ap-southeast-1' = 'dbs-api.ap-southeast-1.aliyuncs.com',
    'ap-southeast-2' = 'dbs-api.ap-southeast-2.aliyuncs.com',
    'ap-southeast-3' = 'dbs-api.ap-southeast-3.aliyuncs.com',
    'ap-southeast-5' = 'dbs-api.ap-southeast-5.aliyuncs.com',
    'ap-northeast-1' = 'dbs-api.ap-northeast-1.aliyuncs.com',
    'us-west-1' = 'dbs-api.cn-hangzhou.aliyuncs.com',
    'us-east-1' = 'dbs-api.cn-hangzhou.aliyuncs.com',
    'eu-central-1' = 'dbs-api.eu-central-1.aliyuncs.com',
    'cn-hangzhou-finance' = 'dbs-api.cn-hangzhou.aliyuncs.com',
    'cn-shanghai-finance-1' = 'dbs-api.cn-hangzhou.aliyuncs.com',
    'cn-shenzhen-finance-1' = 'dbs-api.cn-hangzhou.aliyuncs.com',
    'ap-south-1' = 'dbs-api.ap-south-1.aliyuncs.com',
    'eu-west-1' = 'dbs-api.eu-west-1.aliyuncs.com',
    'me-east-1' = 'dbs-api.me-east-1.aliyuncs.com',
  };

  checkConfig(config);
  @endpoint = getEndpoint('dbs', @regionId, @endpointRule, @network, @suffix, @endpointMap, @endpoint);
}

function getEndpoint(productId: string, regionId: string, endpointRule: string, network: string, suffix: string, endpointMap: map[string]string, endpoint: string) throws: string{
  if (!$isNull(endpoint)) {
    return endpoint;
  }
  
  if (!$isNull(endpointMap) && !$isNull(endpointMap[regionId])) {
    return endpointMap[regionId];
  }
  return OpenApiUtil.getEndpointRules(productId, regionId, endpointRule, network, suffix);
}

model ConfigureBackupPlanRequest {
  autoStartBackup?: boolean(name='AutoStartBackup', description='Specifies whether to enable the automatic backup feature.

*   **true**: enables the automatic backup feature.
*   **false**: disables the automatic backup feature.', example='false'),
  backupGatewayId?: long(name='BackupGatewayId', description='The backup gateway ID. You can call the [DescribeBackupPlanList](https://help.aliyun.com/document_detail/2869825.html) operation to obtain it.

>  If you set **SourceEndpointInstanceType** to **Agent**, this parameter is required.', example='23313123312'),
  backupLogIntervalSeconds?: int32(name='BackupLogIntervalSeconds', description='The interval at which you want to perform incremental log backups. Unit: seconds.

>  Only physical backup supports this parameter.', example='1000'),
  backupObjects?: string(name='BackupObjects', description='The objects to be backed up. You can call the [DescribeBackupPlanList](https://help.aliyun.com/document_detail/2869825.html) operation to obtain the objects.', example='[ { "DBName":"Name of the database to be backed up", "SchemaName":"Name of the schema to be backed up", "TableIncludes":[{ "TableName":"Name of the table to be backed up" }], "TableExcludes":[{ "TableName":"Name of the table to be excluded during the backup" }] } ]'),
  backupPeriod?: string(name='BackupPeriod', description='The day of each week when the full backup task runs. Valid values:

*   **Monday**
*   **Tuesday**
*   **Wednesday**
*   **Thursday**
*   **Friday**
*   **Saturday**
*   **Sunday**', example='Monday'),
  backupPlanId?: string(name='BackupPlanId', description='The ID of the backup schedule. You can call the [DescribeBackupPlanList](https://help.aliyun.com/document_detail/2869825.html) operation to obtain it.

This parameter is required.', example='dbstooi0*******'),
  backupPlanName?: string(name='BackupPlanName', description='The name of the backup schedule. You can call the [DescribeBackupPlanList](https://help.aliyun.com/document_detail/2869825.html) operation to obtain the name.

This parameter is required.', example='dbstooi0*******'),
  backupRateLimit?: long(name='BackupRateLimit', description='The network bandwidth throttling. Unit: KB/s. DBS allows a maximum bandwidth of 10 GB/s.

> This parameter takes effect only when physical backups for MySQL databases are performed.', example='262144'),
  backupRetentionPeriod?: int32(name='BackupRetentionPeriod', description='The number of days for which the backup data is retained. Valid values: 0 to 1825. Default value: 730.', example='730'),
  backupSpeedLimit?: long(name='BackupSpeedLimit', description='The disk I/O limit. Unit: KB/s.

>  This parameter takes effect only during the physical backup of a MySQL database.', example='262144'),
  backupStartTime?: string(name='BackupStartTime', description='The start time of the full backup. Specify the time in the *HH:mm*Z format. The time must be in UTC. You can call the [DescribeBackupPlanList](https://help.aliyun.com/document_detail/2869825.html) operation to obtain the start time of full backup tasks.', example='14:22'),
  backupStorageType?: string(name='BackupStorageType', description='The storage type. Valid values:

*   Empty: If you do not specify this parameter, the system stores backup data in your OSS bucket.
*   system: The system stores backup data in the built-in OSS bucket of DBS.', example='N/A'),
  backupStrategyType?: string(name='BackupStrategyType', description='The backup method that you want to use for full backups. Valid values:

*   **simple**: scheduled backup. If you specify this value for the BackupStrategyType parameter, you must also specify the BackupPeriod and BackupStartTime parameters.
*   **Manual**: manual backup.

> Default value: **simple**.', example='simple'),
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length.', example='ETnLKlblzczshOTUbOCzxxxxxxx'),
  crossAliyunId?: string(name='CrossAliyunId', description='The unique ID (UID) of the Alibaba Cloud account to which the backup schedule belongs. You can call the [DescribeRestoreTaskList](https://help.aliyun.com/document_detail/2869838.html) operation to obtain the UID.', example='2xxx7778xxxxxxxxxx'),
  crossRoleName?: string(name='CrossRoleName', description='The name of the RAM role that is used to perform backup across Alibaba Cloud accounts. You can call the [DescribeRestoreTaskList](https://help.aliyun.com/document_detail/2869838.html) operation to obtain the RAM role.', example='test123'),
  duplicationArchivePeriod?: int32(name='DuplicationArchivePeriod', description='The number of days after which the storage class of the backup data is changed to Archive. Default value: 365.', example='365'),
  duplicationInfrequentAccessPeriod?: int32(name='DuplicationInfrequentAccessPeriod', description='The number of days after which the storage class of the backup data is changed to Infrequent Access (IA). Default value: 180.', example='180'),
  enableBackupLog?: boolean(name='EnableBackupLog', description='Specifies whether to enable the incremental log backup feature. Valid values:

*   **true**: enables the incremental log backup feature.
*   **false**: disables the incremental log backup feature.', example='true'),
  OSSBucketName?: string(name='OSSBucketName', description='The name of the OSS bucket.

>  By default, the system automatically generates an OSS bucket name.', example='TestOssBucket'),
  ownerId?: string(name='OwnerId'),
  resourceGroupId?: string(name='ResourceGroupId', description='The resource group ID.', example='rg-aekzecovzti****'),
  sourceEndpointDatabaseName?: string(name='SourceEndpointDatabaseName', description='The source database name. You can call the [DescribeBackupPlanList](https://help.aliyun.com/document_detail/2869825.html) operation to obtain it.

>  If the source database runs the **PostgreSQL** database engine or **MongoDB** database engine, this parameter is required.', example='testRDS'),
  sourceEndpointIP?: string(name='SourceEndpointIP', description='The source database endpoint. You can call the [DescribeBackupPlanList](https://help.aliyun.com/document_detail/2869825.html) operation to obtain it.

>  If you set **SourceEndpointInstanceType** to **Express**, **Agent**, or **Other**, this parameter is required.', example='rm-uf6wjk5*******.mysql.rds.aliyuncs.com'),
  sourceEndpointInstanceID?: string(name='SourceEndpointInstanceID', description='The database instance ID. You can call the [DescribeBackupPlanList](https://help.aliyun.com/document_detail/2869825.html) operation to obtain the ID.

>  If you set **SourceEndpoint****InstanceType** to **RDS**, **ECS**, **DDS**, or **Express**, this parameter is required.', example='rm-uf6wjk5*********'),
  sourceEndpointInstanceType?: string(name='SourceEndpointInstanceType', description='The location of the database. You can call the [DescribeBackupPlanList](https://help.aliyun.com/document_detail/2869825.html) operation to obtain the location. Valid values:

*   **RDS**
*   **ECS**
*   **Express**: The database is connected to Database Backup (DBS) via Express Connect, VPN Gateway, or Smart Access Gateway.
*   **Agent**: The database is connected over a DBS backup gateway.
*   **DDS**: The database is an ApsaraDB for MongoDB database.
*   **Other**: The database is connected to DBS by using the IP address and port of the database.

This parameter is required.', example='RDS'),
  sourceEndpointOracleSID?: string(name='SourceEndpointOracleSID', description='The system ID (SID) of the Oracle database.

> This parameter is required if the database is an Oracle database.', example='test'),
  sourceEndpointPassword?: string(name='SourceEndpointPassword', description='The password of the account that is used to connect to the database.

> This parameter is required except that the database is an **SQL Server** database that is connected to DBS over a DBS backup gateway or a **Redis** database.', example='testPassword'),
  sourceEndpointPort?: int32(name='SourceEndpointPort', description='The port that is used to connect to the source database. You can call the [DescribeBackupPlanList](https://help.aliyun.com/document_detail/2869825.html) operation to obtain the port.

>  If you set **SourceEndpoint****InstanceType** to **Express**, **Agent**, **Other**, or **ECS**, this parameter is required.', example='3306'),
  sourceEndpointRegion?: string(name='SourceEndpointRegion', description='The region in which the source database resides. You can call the [DescribeBackupPlanList](https://help.aliyun.com/document_detail/2869825.html) operation to obtain the region.

>  If you set **SourceEndpointInstanceType** to RDS, ECS, DDS, Express, or Agent, this parameter is required.', example='cn-hangzhou'),
  sourceEndpointUserName?: string(name='SourceEndpointUserName', description='The username of the account that is used to connect to the database.

> This parameter is required except that the database is an **SQL Server** database that is connected to DBS over a DBS backup gateway or a **Redis** database.', example='testRDS'),
}

model ConfigureBackupPlanResponseBody = {
  backupPlanId?: string(name='BackupPlanId', description='The ID of the backup schedule.', example='dbstooi01ex****'),
  errCode?: string(name='ErrCode', description='The error code.', example='Param.NotFound'),
  errMessage?: string(name='ErrMessage', description='The error message.', example='The specified parameter %s value is not valid.'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='The ID of the request.', example='F1FB49D4-B504-47F1-9F43-D7EAB33F****'),
  success?: boolean(name='Success', description='Indicates whether the request is successful. Valid values:

*   **true**: The request is successful.
*   **false**: The request fails.', example='true'),
}

model ConfigureBackupPlanResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ConfigureBackupPlanResponseBody(name='body'),
}

/**
 * @summary Configures a DBS backup schedule.
 *
 * @param request ConfigureBackupPlanRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ConfigureBackupPlanResponse
 */
@context("configureBackupPlanWithContext")
async function configureBackupPlanWithOptions(request: ConfigureBackupPlanRequest, runtime: $RuntimeOptions): ConfigureBackupPlanResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.autoStartBackup)) {
    query['AutoStartBackup'] = request.autoStartBackup;
  }
  if (!$isNull(request.backupGatewayId)) {
    query['BackupGatewayId'] = request.backupGatewayId;
  }
  if (!$isNull(request.backupLogIntervalSeconds)) {
    query['BackupLogIntervalSeconds'] = request.backupLogIntervalSeconds;
  }
  if (!$isNull(request.backupObjects)) {
    query['BackupObjects'] = request.backupObjects;
  }
  if (!$isNull(request.backupPeriod)) {
    query['BackupPeriod'] = request.backupPeriod;
  }
  if (!$isNull(request.backupPlanId)) {
    query['BackupPlanId'] = request.backupPlanId;
  }
  if (!$isNull(request.backupPlanName)) {
    query['BackupPlanName'] = request.backupPlanName;
  }
  if (!$isNull(request.backupRateLimit)) {
    query['BackupRateLimit'] = request.backupRateLimit;
  }
  if (!$isNull(request.backupRetentionPeriod)) {
    query['BackupRetentionPeriod'] = request.backupRetentionPeriod;
  }
  if (!$isNull(request.backupSpeedLimit)) {
    query['BackupSpeedLimit'] = request.backupSpeedLimit;
  }
  if (!$isNull(request.backupStartTime)) {
    query['BackupStartTime'] = request.backupStartTime;
  }
  if (!$isNull(request.backupStorageType)) {
    query['BackupStorageType'] = request.backupStorageType;
  }
  if (!$isNull(request.backupStrategyType)) {
    query['BackupStrategyType'] = request.backupStrategyType;
  }
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.crossAliyunId)) {
    query['CrossAliyunId'] = request.crossAliyunId;
  }
  if (!$isNull(request.crossRoleName)) {
    query['CrossRoleName'] = request.crossRoleName;
  }
  if (!$isNull(request.duplicationArchivePeriod)) {
    query['DuplicationArchivePeriod'] = request.duplicationArchivePeriod;
  }
  if (!$isNull(request.duplicationInfrequentAccessPeriod)) {
    query['DuplicationInfrequentAccessPeriod'] = request.duplicationInfrequentAccessPeriod;
  }
  if (!$isNull(request.enableBackupLog)) {
    query['EnableBackupLog'] = request.enableBackupLog;
  }
  if (!$isNull(request.OSSBucketName)) {
    query['OSSBucketName'] = request.OSSBucketName;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  if (!$isNull(request.resourceGroupId)) {
    query['ResourceGroupId'] = request.resourceGroupId;
  }
  if (!$isNull(request.sourceEndpointDatabaseName)) {
    query['SourceEndpointDatabaseName'] = request.sourceEndpointDatabaseName;
  }
  if (!$isNull(request.sourceEndpointIP)) {
    query['SourceEndpointIP'] = request.sourceEndpointIP;
  }
  if (!$isNull(request.sourceEndpointInstanceID)) {
    query['SourceEndpointInstanceID'] = request.sourceEndpointInstanceID;
  }
  if (!$isNull(request.sourceEndpointInstanceType)) {
    query['SourceEndpointInstanceType'] = request.sourceEndpointInstanceType;
  }
  if (!$isNull(request.sourceEndpointOracleSID)) {
    query['SourceEndpointOracleSID'] = request.sourceEndpointOracleSID;
  }
  if (!$isNull(request.sourceEndpointPassword)) {
    query['SourceEndpointPassword'] = request.sourceEndpointPassword;
  }
  if (!$isNull(request.sourceEndpointPort)) {
    query['SourceEndpointPort'] = request.sourceEndpointPort;
  }
  if (!$isNull(request.sourceEndpointRegion)) {
    query['SourceEndpointRegion'] = request.sourceEndpointRegion;
  }
  if (!$isNull(request.sourceEndpointUserName)) {
    query['SourceEndpointUserName'] = request.sourceEndpointUserName;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ConfigureBackupPlan',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Configures a DBS backup schedule.
 *
 * @param request ConfigureBackupPlanRequest
 * @return ConfigureBackupPlanResponse
 */
async function configureBackupPlan(request: ConfigureBackupPlanRequest): ConfigureBackupPlanResponse {
  var runtime = new $RuntimeOptions{};
  return configureBackupPlanWithOptions(request, runtime);
}

model CreateAndStartBackupPlanRequest {
  backupGatewayId?: long(name='BackupGatewayId', description='The backup gateway ID.

> 

*   If **SourceEndpointInstanceType** is set to **Agent**, this parameter is required.****

*   For more information about how to install a backup gateway, see [Install a backup gateway](https://help.aliyun.com/document_detail/93250.html).

*   You can query a list of existing backup gateways by calling the [DescribeBackupGatewayList](https://help.aliyun.com/document_detail/2869840.html) operation.', example='23313123312'),
  backupLogIntervalSeconds?: int32(name='BackupLogIntervalSeconds', description='The interval at which you want to perform incremental log backups. Unit: seconds.

>  This parameter is required only if you set BackupMethod to **physical**.', example='1000'),
  backupMethod?: string(name='BackupMethod', description='The method that is used to generate the backup file. Valid values:

*   **logical**: logical backup
*   **physical**: physical backup
*   **duplication**: dump backup

This parameter is required.', example='logical'),
  backupObjects?: string(name='BackupObjects', description='The object to be backed up.', example='[ { "DBName":"Name of the database to be backed up", "SchemaName":"Name of the schema to be backed up", "TableIncludes":[{ "TableName":"Name of the table to be backed up" }], "TableExcludes":[{ "TableName":"Name of the table that you do not want to back up" }] } ]'),
  backupPeriod?: string(name='BackupPeriod', description='The day of the week on which you want to perform full backup. Valid values:

*   **Monday**
*   **Tuesday**
*   **Wednesday**
*   **Thursday**
*   **Friday**
*   **Saturday**
*   **Sunday**

>  You can specify multiple values. Separate multiple values with commas (,).', example='Monday'),
  backupPlanId?: string(name='BackupPlanId', description='The ID of the backup schedule.', example='dbstooi0*******'),
  backupPlanName?: string(name='BackupPlanName', description='The name of the backup schedule.

This parameter is required.', example='dbstooi0*******'),
  backupRateLimit?: long(name='BackupRateLimit', description='The network bandwidth throttling. Unit: KB/s. DBS allows a maximum bandwidth of 10 GB/s.

>  This parameter takes effect only when physical backups for MySQL databases are performed.', example='262144'),
  backupRetentionPeriod?: int32(name='BackupRetentionPeriod', description='The number of days for which the backup data is retained. Valid values: 0 to 1825. Default value: 730.', example='730'),
  backupSpeedLimit?: long(name='BackupSpeedLimit', description='The I/O limit for the disk. Unit: KB/s.

>  This parameter takes effect only when physical backups for MySQL databases are performed.', example='262144'),
  backupStartTime?: string(name='BackupStartTime', description='The start time of full backup tasks. Specify the value in the *HH:mm* format. The time must be in UTC.', example='14:22'),
  backupStorageType?: string(name='BackupStorageType', description='The storage type. Valid values:

*   Empty: If you do not specify this parameter, the system stores backup data in your OSS bucket.
*   system : The system stores backup data in the built-in OSS bucket of DBS.', example='N/A'),
  backupStrategyType?: string(name='BackupStrategyType', description='The backup method that you want to use for full backups. Valid values:

*   **simple**: scheduled backup. If you specify this value for the BackupStrategyType parameter, you must also specify the BackupPeriod and BackupStartTime parameters.
*   **Manual**: manual backup.

> Default value: **simple**.', example='simple'),
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request.', example='DBS'),
  crossAliyunId?: string(name='CrossAliyunId', description='The unique ID (UID) of the Alibaba Cloud account to which the source database belongs.', example='2xxx7778xxxxxxxxxx'),
  crossRoleName?: string(name='CrossRoleName', description='The name of the RAM role that is used to perform backups across Alibaba Cloud accounts.', example='test123'),
  databaseRegion?: string(name='DatabaseRegion', description='The region in which the database that you want to back up resides.

> This parameter is required if the **PayType** parameter is set to **postpay**.', example='cn-hangzhou'),
  databaseType?: string(name='DatabaseType', description='The type of the source database. Valid values:

*   **MySQL**
*   **MSSQL**
*   **Oracle**
*   **MariaDB**
*   **PostgreSQL**
*   **DRDS**
*   **MongoDB**
*   **Redis**

This parameter is required.', example='MySQL'),
  duplicationArchivePeriod?: int32(name='DuplicationArchivePeriod', description='The number of days after which the storage class of the backup data is changed to Archive. Default value: 365.', example='365'),
  duplicationInfrequentAccessPeriod?: int32(name='DuplicationInfrequentAccessPeriod', description='The number of days after which the storage class of the backup data is changed to Infrequent Access (IA). Default value: 180.', example='180'),
  enableBackupLog?: boolean(name='EnableBackupLog', description='Specifies whether to enable the incremental log backup feature. Valid values:

*   **true**: enables the incremental log backup feature.
*   **false**: disables the incremental log backup feature.', example='true'),
  fromApp?: string(name='FromApp', description='The request source. Default value: OpenApi. You do not need to set this parameter.', example='OpenApi'),
  instanceClass?: string(name='InstanceClass', description='The type of the backup schedule. Valid values:

*   **micro**
*   **small**
*   **medium**
*   **large**
*   **xlarge**

>  A backup schedule type with higher specifications offers higher backup and restoration performance. For more information, see [Select a backup schedule type](https://help.aliyun.com/document_detail/84372.html).

This parameter is required.', example='micro'),
  instanceType?: string(name='InstanceType', description='The type of the source database instance. Valid values:

*   **RDS**: ApsaraDB RDS.
*   **PolarDB**: PolarDB.
*   **DDS**: ApsaraDB for MongoDB.
*   **Kvstore**: ApsaraDB for Redis.
*   **Other**: Database connected by using an IP address and a port number.
*   **dg**: Self-managed database that has no public IP address or port number and is connected over Database Gateway.

>  If **PayType** is set to **postpay**, this parameter is required.', example='RDS'),
  OSSBucketName?: string(name='OSSBucketName', description='The name of the Object Storage Service (OSS) bucket used to store backup files. By default, the system automatically generates a name for the OSS bucket.', example='TestOssBucket'),
  ownerId?: string(name='OwnerId'),
  payType?: string(name='PayType', description='The billing method. Valid values:

*   **postpay**: pay-as-you-go.
*   **prepay**: subscription.

>  The default value is **prepay**. You can set this parameter to **postpay** only if you set **BackupMethod** to **duplication**.', example='Postpaid'),
  period?: string(name='Period', description='Specifies whether to use yearly subscription or monthly subscription for the instance. Valid values:

*   **Year**: yearly subscription
*   **Month**: monthly subscription', example='Year'),
  region?: string(name='Region', description='The ID of the region in which you want to store the backup data. You can query the supported regions of DBS by calling the [DescribeRegions](https://help.aliyun.com/document_detail/2869853.html) operation.', example='cn-hangzhou'),
  resourceGroupId?: string(name='ResourceGroupId', description='The ID of the resource group.', example='rg-aekzecovzti****'),
  sourceEndpointDatabaseName?: string(name='SourceEndpointDatabaseName', description='The name of the database.

> This parameter is required if the DatabaseType parameter is set to **PostgreSQL** or **MongoDB**.', example='testRDS'),
  sourceEndpointIP?: string(name='SourceEndpointIP', description='The endpoint of the database.

> This parameter is required if the **SourceEndpointInstanceType** parameter is set to **Express**, **Agent**, or **Other**.', example='rm-uf6wjk5xxxxxxx.mysql.rds.aliyuncs.com'),
  sourceEndpointInstanceID?: string(name='SourceEndpointInstanceID', description='The ID of the database instance.

> This parameter is required if the **SourceEndpointInstanceType** parameter is set to **RDS**, **ECS**, **DDS**, or **Express**.', example='rm-uf6wjk5xxxxxxxxxx'),
  sourceEndpointInstanceType?: string(name='SourceEndpointInstanceType', description='The location of the source database. Valid values:

*   **RDS**: The database is on an ApsaraDB RDS instance.
*   **ECS**: The database is on an Elastic Compute Service (ECS) instance.
*   **Express**: The database is connected to DBS by using Express Connect, VPN Gateway, or Smart Access Gateway.
*   **Agent**: The database is connected to DBS over a DBS backup gateway.
*   **DDS**: The database is on an ApsaraDB for MongoDB instance.
*   **Other**: The database is connected to DBS by using an IP address and a port number.
*   **dg**: The database is a self-managed database that has no public IP address or port number and is connected to DBS over Database Gateway.

This parameter is required.', example='RDS'),
  sourceEndpointOracleSID?: string(name='SourceEndpointOracleSID', description='The system ID (SID) of the Oracle database. This parameter is required if the source database is an Oracle database.', example='test'),
  sourceEndpointPassword?: string(name='SourceEndpointPassword', description='The password of the account that is used to connect to the database.

> This parameter is required except that the database is an SQL Server database that is connected to DBS over a DBS backup gateway or a Redis database.', example='testPassword'),
  sourceEndpointPort?: int32(name='SourceEndpointPort', description='The port of the database.

> This parameter is required if the **SourceEndpointInstanceType** parameter is set to **Express**, **Agent**, **Other**, or **ECS**.', example='3306'),
  sourceEndpointRegion?: string(name='SourceEndpointRegion', description='The region in which the database that you want to back up resides.

> This parameter is required if the **SourceEndpointInstanceType** parameter is set to **RDS**, **ECS**, **DDS**, **Express**, or **Agent**.', example='cn-hangzhou'),
  sourceEndpointUserName?: string(name='SourceEndpointUserName', description='The username of the account that is used to connect to the database.

> This parameter is required except that the database is an SQL Server database that is connected to DBS over a DBS backup gateway or a Redis database.', example='testRDS'),
  storageRegion?: string(name='StorageRegion', description='The region in which you want to store the backup data.

> This parameter is required if the **PayType** parameter is set to **postpay**.', example='cn-hangzhou'),
  storageType?: string(name='StorageType', description='This parameter is unavailable.', example='N/A'),
  usedTime?: int32(name='UsedTime', description='The subscription duration. Valid values:

*   If **Period** is set to **Year**, the valid values of **UsedTime** range from 1 to 5.
*   If **Period** is set to **Month**, the valid values of **UsedTime** range from 1 to 11.', example='1'),
}

model CreateAndStartBackupPlanResponseBody = {
  backupPlanId?: string(name='BackupPlanId', description='The backup schedule ID.', example='dbs1hvb0wwweusfa'),
  createBackupSet?: boolean(name='CreateBackupSet', description='Indicates whether a backup is performed immediately after the backup schedule is configured. Valid values:

*   **true**
*   **false**', example='true'),
  errCode?: string(name='ErrCode', description='The error code.', example='Param.NotFound'),
  errMessage?: string(name='ErrMessage', description='The error message.', example='The specified parameter %s value is not valid.'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  orderId?: string(name='OrderId', description='The ID of the order.', example='2056157***'),
  requestId?: string(name='RequestId', description='The ID of the request.', example='4F1888AC-1138-4995-B9FE-D2734F61C058'),
  success?: boolean(name='Success', description='Indicates whether the request is successful.', example='true'),
}

model CreateAndStartBackupPlanResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateAndStartBackupPlanResponseBody(name='body'),
}

/**
 * @summary Creates, configures, and starts a backup schedule.
 *
 * @description Before you call this operation, make sure that you fully understand the billing methods and [pricing](https://help.aliyun.com/document_detail/70005.html) of Database Backup (DBS).
 *
 * @param request CreateAndStartBackupPlanRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateAndStartBackupPlanResponse
 */
@context("createAndStartBackupPlanWithContext")
async function createAndStartBackupPlanWithOptions(request: CreateAndStartBackupPlanRequest, runtime: $RuntimeOptions): CreateAndStartBackupPlanResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.backupGatewayId)) {
    query['BackupGatewayId'] = request.backupGatewayId;
  }
  if (!$isNull(request.backupLogIntervalSeconds)) {
    query['BackupLogIntervalSeconds'] = request.backupLogIntervalSeconds;
  }
  if (!$isNull(request.backupMethod)) {
    query['BackupMethod'] = request.backupMethod;
  }
  if (!$isNull(request.backupObjects)) {
    query['BackupObjects'] = request.backupObjects;
  }
  if (!$isNull(request.backupPeriod)) {
    query['BackupPeriod'] = request.backupPeriod;
  }
  if (!$isNull(request.backupPlanId)) {
    query['BackupPlanId'] = request.backupPlanId;
  }
  if (!$isNull(request.backupPlanName)) {
    query['BackupPlanName'] = request.backupPlanName;
  }
  if (!$isNull(request.backupRateLimit)) {
    query['BackupRateLimit'] = request.backupRateLimit;
  }
  if (!$isNull(request.backupRetentionPeriod)) {
    query['BackupRetentionPeriod'] = request.backupRetentionPeriod;
  }
  if (!$isNull(request.backupSpeedLimit)) {
    query['BackupSpeedLimit'] = request.backupSpeedLimit;
  }
  if (!$isNull(request.backupStartTime)) {
    query['BackupStartTime'] = request.backupStartTime;
  }
  if (!$isNull(request.backupStorageType)) {
    query['BackupStorageType'] = request.backupStorageType;
  }
  if (!$isNull(request.backupStrategyType)) {
    query['BackupStrategyType'] = request.backupStrategyType;
  }
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.crossAliyunId)) {
    query['CrossAliyunId'] = request.crossAliyunId;
  }
  if (!$isNull(request.crossRoleName)) {
    query['CrossRoleName'] = request.crossRoleName;
  }
  if (!$isNull(request.databaseRegion)) {
    query['DatabaseRegion'] = request.databaseRegion;
  }
  if (!$isNull(request.databaseType)) {
    query['DatabaseType'] = request.databaseType;
  }
  if (!$isNull(request.duplicationArchivePeriod)) {
    query['DuplicationArchivePeriod'] = request.duplicationArchivePeriod;
  }
  if (!$isNull(request.duplicationInfrequentAccessPeriod)) {
    query['DuplicationInfrequentAccessPeriod'] = request.duplicationInfrequentAccessPeriod;
  }
  if (!$isNull(request.enableBackupLog)) {
    query['EnableBackupLog'] = request.enableBackupLog;
  }
  if (!$isNull(request.fromApp)) {
    query['FromApp'] = request.fromApp;
  }
  if (!$isNull(request.instanceClass)) {
    query['InstanceClass'] = request.instanceClass;
  }
  if (!$isNull(request.instanceType)) {
    query['InstanceType'] = request.instanceType;
  }
  if (!$isNull(request.OSSBucketName)) {
    query['OSSBucketName'] = request.OSSBucketName;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  if (!$isNull(request.payType)) {
    query['PayType'] = request.payType;
  }
  if (!$isNull(request.period)) {
    query['Period'] = request.period;
  }
  if (!$isNull(request.region)) {
    query['Region'] = request.region;
  }
  if (!$isNull(request.resourceGroupId)) {
    query['ResourceGroupId'] = request.resourceGroupId;
  }
  if (!$isNull(request.sourceEndpointDatabaseName)) {
    query['SourceEndpointDatabaseName'] = request.sourceEndpointDatabaseName;
  }
  if (!$isNull(request.sourceEndpointIP)) {
    query['SourceEndpointIP'] = request.sourceEndpointIP;
  }
  if (!$isNull(request.sourceEndpointInstanceID)) {
    query['SourceEndpointInstanceID'] = request.sourceEndpointInstanceID;
  }
  if (!$isNull(request.sourceEndpointInstanceType)) {
    query['SourceEndpointInstanceType'] = request.sourceEndpointInstanceType;
  }
  if (!$isNull(request.sourceEndpointOracleSID)) {
    query['SourceEndpointOracleSID'] = request.sourceEndpointOracleSID;
  }
  if (!$isNull(request.sourceEndpointPassword)) {
    query['SourceEndpointPassword'] = request.sourceEndpointPassword;
  }
  if (!$isNull(request.sourceEndpointPort)) {
    query['SourceEndpointPort'] = request.sourceEndpointPort;
  }
  if (!$isNull(request.sourceEndpointRegion)) {
    query['SourceEndpointRegion'] = request.sourceEndpointRegion;
  }
  if (!$isNull(request.sourceEndpointUserName)) {
    query['SourceEndpointUserName'] = request.sourceEndpointUserName;
  }
  if (!$isNull(request.storageRegion)) {
    query['StorageRegion'] = request.storageRegion;
  }
  if (!$isNull(request.storageType)) {
    query['StorageType'] = request.storageType;
  }
  if (!$isNull(request.usedTime)) {
    query['UsedTime'] = request.usedTime;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateAndStartBackupPlan',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates, configures, and starts a backup schedule.
 *
 * @description Before you call this operation, make sure that you fully understand the billing methods and [pricing](https://help.aliyun.com/document_detail/70005.html) of Database Backup (DBS).
 *
 * @param request CreateAndStartBackupPlanRequest
 * @return CreateAndStartBackupPlanResponse
 */
async function createAndStartBackupPlan(request: CreateAndStartBackupPlanRequest): CreateAndStartBackupPlanResponse {
  var runtime = new $RuntimeOptions{};
  return createAndStartBackupPlanWithOptions(request, runtime);
}

model CreateBackupPlanRequest {
  backupMethod?: string(name='BackupMethod', description='The backup method of the backup schedule. Valid values:

*   **logical**: logical backup
*   **physical**: physical backup
*   **duplication**: dump backup

This parameter is required.', example='logical'),
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request.', example='KJSAHKJFHKJSHFKASHFKJADFHKDXXXX'),
  databaseRegion?: string(name='DatabaseRegion', description='The region in which the database you want to back up resides.

> This parameter is required if the **PayType** parameter is set to **postpay**.', example='cn-hangzhou'),
  databaseType?: string(name='DatabaseType', description='The type of the source database. Valid values:

*   **MySQL**
*   **MSSQL**
*   **Oracle**
*   **MariaDB**
*   **PostgreSQL**
*   **DRDS**
*   **MongoDB**
*   **Redis**

This parameter is required.', example='MySQL'),
  fromApp?: string(name='FromApp', description='The source of the request. The default value is OpenAPI and cannot be changed.', example='OpenAPI'),
  instanceClass?: string(name='InstanceClass', description='The type of the backup schedule. Valid values:

*   **micro**
*   **small**
*   **medium**
*   **large**
*   **xlarge**

>  A backup schedule type with higher specifications offers higher backup and restoration performance. For more information, see [Select a backup schedule type](https://help.aliyun.com/document_detail/84372.html).

This parameter is required.', example='micro'),
  instanceType?: string(name='InstanceType', description='The type of the source database instance. Valid values:

*   **RDS**: ApsaraDB RDS.
*   **PolarDB**: PolarDB.
*   **DDS**: ApsaraDB for MongoDB.
*   **Kvstore**: ApsaraDB for Redis.
*   **Other**: Database connected by using an IP address and a port number.
*   **dg**: Self-managed database that has no public IP address or port number and is connected over Database Gateway.

>  If **PayType** is set to **postpay**, this parameter is required.', example='RDS'),
  ownerId?: string(name='OwnerId'),
  payType?: string(name='PayType', description='The billing method of the backup schedule. Valid values:

*   **postpay**: pay-as-you-go
*   **prepay**: subscription

> The default value is **prepay**. If the **BackupMethod** parameter is set to **duplication**, **postpay** is supported.', example='prepay'),
  period?: string(name='Period', description='The unit of the subscription period. Valid values:

*   **Year**: yearly subscription
*   **Month**: monthly subscription', example='Month'),
  region?: string(name='Region', description='The ID of the region in which you can activate Data Disaster Recovery. You can call the [DescribeRegions](https://help.aliyun.com/document_detail/2869853.html) operation to query the regions supported by Data Disaster Recovery.

>  For more information, see [Endpoints](https://help.aliyun.com/document_detail/2869810.html).', example='cn-hangzhou'),
  resourceGroupId?: string(name='ResourceGroupId', description='The ID of the resource group.', example='rg-aekzecovzti****'),
  storageRegion?: string(name='StorageRegion', description='The region in which you want to store the backup data.

> This parameter is required if the **PayType** parameter is set to **postpay**.', example='cn-hangzhou'),
  storageType?: string(name='StorageType', description='This parameter is unavailable.', example='N/A'),
  usedTime?: int32(name='UsedTime', description='The subscription period. Valid values:

*   If **Period** is set to **Year**, the valid values of **UsedTime** range from 1 to 5.
*   If **Period** is set to **Month**, the valid values of **UsedTime** range from 1 to 11.', example='1'),
}

model CreateBackupPlanResponseBody = {
  backupPlanId?: string(name='BackupPlanId', description='The ID of the backup schedule.', example='dbsrhahrsu2****'),
  errCode?: string(name='ErrCode', description='The error code.', example='Param.NotFound'),
  errMessage?: string(name='ErrMessage', description='The error message.', example='The specified parameter %s value is not valid.'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  orderId?: string(name='OrderId', description='The ID of the order.', example='21437345592****'),
  requestId?: string(name='RequestId', description='The ID of the request.', example='7BCF6D62-885F-5A4A-91A1-679760E7****'),
  success?: boolean(name='Success', description='Indicates whether the request is successful. Valid values:

*   **true**: The request is successful.
*   **false**: The request fails.', example='true'),
}

model CreateBackupPlanResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateBackupPlanResponseBody(name='body'),
}

/**
 * @summary Creates a backup schedule.
 *
 * @description For more information about how to create a backup schedule in the Database Backup (DBS) console, see [Purchase a backup schedule](https://help.aliyun.com/document_detail/65909.html).
 *
 * @param request CreateBackupPlanRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateBackupPlanResponse
 */
@context("createBackupPlanWithContext")
async function createBackupPlanWithOptions(request: CreateBackupPlanRequest, runtime: $RuntimeOptions): CreateBackupPlanResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.backupMethod)) {
    query['BackupMethod'] = request.backupMethod;
  }
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.databaseRegion)) {
    query['DatabaseRegion'] = request.databaseRegion;
  }
  if (!$isNull(request.databaseType)) {
    query['DatabaseType'] = request.databaseType;
  }
  if (!$isNull(request.fromApp)) {
    query['FromApp'] = request.fromApp;
  }
  if (!$isNull(request.instanceClass)) {
    query['InstanceClass'] = request.instanceClass;
  }
  if (!$isNull(request.instanceType)) {
    query['InstanceType'] = request.instanceType;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  if (!$isNull(request.payType)) {
    query['PayType'] = request.payType;
  }
  if (!$isNull(request.period)) {
    query['Period'] = request.period;
  }
  if (!$isNull(request.region)) {
    query['Region'] = request.region;
  }
  if (!$isNull(request.resourceGroupId)) {
    query['ResourceGroupId'] = request.resourceGroupId;
  }
  if (!$isNull(request.storageRegion)) {
    query['StorageRegion'] = request.storageRegion;
  }
  if (!$isNull(request.storageType)) {
    query['StorageType'] = request.storageType;
  }
  if (!$isNull(request.usedTime)) {
    query['UsedTime'] = request.usedTime;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateBackupPlan',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates a backup schedule.
 *
 * @description For more information about how to create a backup schedule in the Database Backup (DBS) console, see [Purchase a backup schedule](https://help.aliyun.com/document_detail/65909.html).
 *
 * @param request CreateBackupPlanRequest
 * @return CreateBackupPlanResponse
 */
async function createBackupPlan(request: CreateBackupPlanRequest): CreateBackupPlanResponse {
  var runtime = new $RuntimeOptions{};
  return createBackupPlanWithOptions(request, runtime);
}

model CreateFullBackupSetDownloadRequest {
  backupSetDataFormat?: string(name='BackupSetDataFormat', description='The format in which the full backup set is downloaded. Valid values:

*   **Native**
*   **SQL**
*   **CSV**(Default value)
*   **JSON**', example='SQL'),
  backupSetId?: string(name='BackupSetId', description='The ID of the full backup set.

This parameter is required.', example='dbs1hv****'),
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request.', example='ETnLKlblzczshOTUbOCz****'),
  ownerId?: string(name='OwnerId'),
}

model CreateFullBackupSetDownloadResponseBody = {
  backupSetDownloadTaskId?: string(name='BackupSetDownloadTaskId', description='The ID of the backup set download task.', example='dbstooi01e****'),
  errCode?: string(name='ErrCode', description='The error code.', example='Param.NotFound'),
  errMessage?: string(name='ErrMessage', description='The error message.', example='The specified parameter %s value is not valid.'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='The ID of the request.', example='D6E068C3-25BC-455A-85FE-45F0B22ECB1F'),
  success?: boolean(name='Success', description='Indicates whether the request is successful.', example='true'),
}

model CreateFullBackupSetDownloadResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateFullBackupSetDownloadResponseBody(name='body'),
}

/**
 * @summary Creates and starts a full backup set download task.
 *
 * @param request CreateFullBackupSetDownloadRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateFullBackupSetDownloadResponse
 */
@context("createFullBackupSetDownloadWithContext")
async function createFullBackupSetDownloadWithOptions(request: CreateFullBackupSetDownloadRequest, runtime: $RuntimeOptions): CreateFullBackupSetDownloadResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.backupSetDataFormat)) {
    query['BackupSetDataFormat'] = request.backupSetDataFormat;
  }
  if (!$isNull(request.backupSetId)) {
    query['BackupSetId'] = request.backupSetId;
  }
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateFullBackupSetDownload',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates and starts a full backup set download task.
 *
 * @param request CreateFullBackupSetDownloadRequest
 * @return CreateFullBackupSetDownloadResponse
 */
async function createFullBackupSetDownload(request: CreateFullBackupSetDownloadRequest): CreateFullBackupSetDownloadResponse {
  var runtime = new $RuntimeOptions{};
  return createFullBackupSetDownloadWithOptions(request, runtime);
}

model CreateGetDBListFromAgentTaskRequest {
  backupGatewayId?: long(name='BackupGatewayId', description='The ID of the backup gateway. You can call the [DescribeBackupPlanList](https://help.aliyun.com/document_detail/2869825.html) operation to query the ID.

>  This parameter is required.', example='160813'),
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request. You can use the client to generate the value, but you must make sure that it is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length.', example='ETnLKlblzczshOTUbOCzxxxxxxxxxx'),
  databaseType?: string(name='DatabaseType', description='The type of the database. Valid values:

*   **MySQL**
*   **MSSQL**
*   **Oracle**
*   **MariaDB**
*   **PostgreSQL**
*   **DRDS**
*   **MongoDB**
*   **Redis**', example='MySQL'),
  ownerId?: string(name='OwnerId'),
  sourceEndpointIP?: string(name='SourceEndpointIP', description='The URL that is used to access the database.', example='123.0.0.1'),
  sourceEndpointPort?: int32(name='SourceEndpointPort', description='The port that is used to connect to the database.', example='3306'),
  sourceEndpointRegion?: string(name='SourceEndpointRegion', description='The region in which the backup gateway resides.', example='cn-hangzhou'),
}

model CreateGetDBListFromAgentTaskResponseBody = {
  errCode?: string(name='ErrCode', description='The error code.', example='InvalidParameter'),
  errMessage?: string(name='ErrMessage', description='The error message.', example='This backupPlan can\\\\"t support this action'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code returned.', example='400'),
  requestId?: string(name='RequestId', description='The request ID.', example='EB4DFD5E-3618-498D-BE35-4DBEA0072122'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
  taskId?: long(name='TaskId', description='The ID of the asynchronous task.', example='123456'),
}

model CreateGetDBListFromAgentTaskResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateGetDBListFromAgentTaskResponseBody(name='body'),
}

/**
 * @summary Creates a task to obtain a database list by using a backup gateway.
 *
 * @description This API operation returns a task ID. You can call the [GetDBListFromAgent](https://help.aliyun.com/document_detail/2869852.html) operation to query the task result based on the task ID.
 *
 * @param request CreateGetDBListFromAgentTaskRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateGetDBListFromAgentTaskResponse
 */
@context("createGetDBListFromAgentTaskWithContext")
async function createGetDBListFromAgentTaskWithOptions(request: CreateGetDBListFromAgentTaskRequest, runtime: $RuntimeOptions): CreateGetDBListFromAgentTaskResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.backupGatewayId)) {
    query['BackupGatewayId'] = request.backupGatewayId;
  }
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.databaseType)) {
    query['DatabaseType'] = request.databaseType;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  if (!$isNull(request.sourceEndpointIP)) {
    query['SourceEndpointIP'] = request.sourceEndpointIP;
  }
  if (!$isNull(request.sourceEndpointPort)) {
    query['SourceEndpointPort'] = request.sourceEndpointPort;
  }
  if (!$isNull(request.sourceEndpointRegion)) {
    query['SourceEndpointRegion'] = request.sourceEndpointRegion;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateGetDBListFromAgentTask',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates a task to obtain a database list by using a backup gateway.
 *
 * @description This API operation returns a task ID. You can call the [GetDBListFromAgent](https://help.aliyun.com/document_detail/2869852.html) operation to query the task result based on the task ID.
 *
 * @param request CreateGetDBListFromAgentTaskRequest
 * @return CreateGetDBListFromAgentTaskResponse
 */
async function createGetDBListFromAgentTask(request: CreateGetDBListFromAgentTaskRequest): CreateGetDBListFromAgentTaskResponse {
  var runtime = new $RuntimeOptions{};
  return createGetDBListFromAgentTaskWithOptions(request, runtime);
}

model CreateIncrementBackupSetDownloadRequest {
  backupSetDataFormat?: string(name='BackupSetDataFormat', description='The format in which the incremental backup set is downloaded. Valid values:

*   **Native**
*   **SQL**
*   **CSV**
*   **JSON**

> Default value: Native.', example='Native'),
  backupSetId?: string(name='BackupSetId', description='The ID of the incremental backup task. To obtain the task ID, you can call the [DescribeIncrementBackupList](https://help.aliyun.com/document_detail/2869833.html) operation and view the value of the **BackupSetJobId** parameter in the response.

This parameter is required.', example='dbstooi01e****'),
  backupSetName?: string(name='BackupSetName', description='The ID of the incremental backup set. To obtain the backup set ID, you can call the [DescribeIncrementBackupList](https://help.aliyun.com/document_detail/2869833.html) operation and view the value of the **BackupSetId** parameter in the response.

This parameter is required.', example='test'),
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request.', example='ETnLKlblzczshOTUbOCz****'),
  ownerId?: string(name='OwnerId'),
}

model CreateIncrementBackupSetDownloadResponseBody = {
  backupSetDownloadTaskId?: string(name='BackupSetDownloadTaskId', description='The ID of the backup set download task.', example='dbstooi01e****'),
  errCode?: string(name='ErrCode', description='The error code.', example='Param.NotFound'),
  errMessage?: string(name='ErrMessage', description='The error message.', example='The specified parameter %s value is not valid.'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='The ID of the request.', example='D6E068C3-25BC-455A-85FE-45F0B22ECB1F'),
  success?: boolean(name='Success', description='Indicates whether the request is successful.', example='true'),
}

model CreateIncrementBackupSetDownloadResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateIncrementBackupSetDownloadResponseBody(name='body'),
}

/**
 * @summary Creates and starts an incremental backup set download task.
 *
 * @param request CreateIncrementBackupSetDownloadRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateIncrementBackupSetDownloadResponse
 */
@context("createIncrementBackupSetDownloadWithContext")
async function createIncrementBackupSetDownloadWithOptions(request: CreateIncrementBackupSetDownloadRequest, runtime: $RuntimeOptions): CreateIncrementBackupSetDownloadResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.backupSetDataFormat)) {
    query['BackupSetDataFormat'] = request.backupSetDataFormat;
  }
  if (!$isNull(request.backupSetId)) {
    query['BackupSetId'] = request.backupSetId;
  }
  if (!$isNull(request.backupSetName)) {
    query['BackupSetName'] = request.backupSetName;
  }
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateIncrementBackupSetDownload',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates and starts an incremental backup set download task.
 *
 * @param request CreateIncrementBackupSetDownloadRequest
 * @return CreateIncrementBackupSetDownloadResponse
 */
async function createIncrementBackupSetDownload(request: CreateIncrementBackupSetDownloadRequest): CreateIncrementBackupSetDownloadResponse {
  var runtime = new $RuntimeOptions{};
  return createIncrementBackupSetDownloadWithOptions(request, runtime);
}

model CreateRestoreTaskRequest {
  backupGatewayId?: long(name='BackupGatewayId', description='The ID of the backup gateway.

> This parameter is required if the DestinationEndpointInstanceType parameter is set to Agent.', example='4312****'),
  backupPlanId?: string(name='BackupPlanId', description='The ID of the backup schedule.

This parameter is required.', example='dbs1hvb0ww****'),
  backupSetId?: string(name='BackupSetId', description='The ID of the full backup set.', example='dbs1hvb0w*****'),
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request.', example='ETnLKlblzczshOTUbOC********'),
  crossAliyunId?: string(name='CrossAliyunId', description='The unique ID (UID) of the Alibaba Cloud account to which the source database belongs.', example='2749528728********'),
  crossRoleName?: string(name='CrossRoleName', description='The name of the RAM role that is used to perform backups across Alibaba Cloud accounts.', example='test123'),
  destinationEndpointDatabaseName?: string(name='DestinationEndpointDatabaseName', description='The name of the database.



> This parameter is required if the database is a PostgreSQL database or a MongoDB database.', example='test'),
  destinationEndpointIP?: string(name='DestinationEndpointIP', description='The endpoint that is used to connect to the database.

> This parameter is required if the DestinationEndpointInstanceType parameter is set to Express, Agent, or Other.', example='rm-bp*****9jv8pxero.mysql.rds.aliyuncs.com'),
  destinationEndpointInstanceID?: string(name='DestinationEndpointInstanceID', description='The ID of the database instance.

> This parameter is required if the DestinationEndpointInstanceType parameter is set to RDS, ECS, DDS, or Express.', example='rm-bp1p8c29*****'),
  destinationEndpointInstanceType?: string(name='DestinationEndpointInstanceType', description='The location of the database. Valid values:

*   **RDS**: The database is deployed on an ApsaraDB RDS instance.
*   **ECS**: The database is deployed on an Elastic Compute Service (ECS) instance.
*   **Express**: The database is connected to Database Backup (DBS) by using Express Connect, VPN Gateway, or Smart Access Gateway.
*   **Agent**: The database is connected over a DBS backup gateway.
*   **DDS**: The database is an ApsaraDB for MongoDB database.
*   **Other**: The database is connected to DBS by using the IP address and port of the database.
*   **dg**: The database is a self-managed database that does not have public IP addresses or port numbers and is connected to DBS over Database Gateway.

This parameter is required.', example='RDS'),
  destinationEndpointOracleSID?: string(name='DestinationEndpointOracleSID', description='The system ID (SID) of the Oracle database.



> This parameter is required if the source database is an Oracle database.', example='test'),
  destinationEndpointPassword?: string(name='DestinationEndpointPassword', description='The password of the account that is used to connect to the source database.



> This parameter is required except that the database is an SQL Server database that is connected to DBS over a DBS backup gateway or a Redis database.', example='Test'),
  destinationEndpointPort?: int32(name='DestinationEndpointPort', description='The port of the database.
> This parameter is required if the DestinationEndpointInstanceType parameter is set to Express, Agent, Other, or ECS.', example='3306'),
  destinationEndpointRegion?: string(name='DestinationEndpointRegion', description='The region ID of the destination database instance.

>  You must specify this parameter if **DestinationEndpointInstanceType** is set to RDS, ECS, DDS, Express, or Agent.', example='cn-hangzhou'),
  destinationEndpointUserName?: string(name='DestinationEndpointUserName', description='The username of the account that is used to connect to the database.


> This parameter is required except that the database is an SQL Server database that is connected to DBS over a DBS backup gateway or a Redis database.', example='test'),
  duplicateConflict?: string(name='DuplicateConflict', description='The method of processing objects with the same name. Valid values:

- failure: The restore task fails if the system detects objects with the same name. This is the default value.
- renamenew: The restore task renames objects with the same name starting from the second occurrence.', example='renamenew'),
  ownerId?: string(name='OwnerId'),
  restoreDir?: string(name='RestoreDir', description='This parameter is required if the DestinationEndpointInstanceType parameter is set to Agent and the backup object of the backup schedule is a MySQL database.', example='test'),
  restoreHome?: string(name='RestoreHome', description='The program directory of the database.', example='test'),
  restoreObjects?: string(name='RestoreObjects', description='The objects to be restored.



> This parameter is required except that the DestinationEndpointInstanceType parameter is set to Agent. For information about the parameter definition, see RestoreObjects.', example='[ { "DBName":"Name of the database to be restored", "NewDBName":"Name of the database to which the objects will be restored", "SchemaName":"Schema name of the database to be restored", "NewSchemaName":"Schema name of the destination database to which the objects will be restored"}]'),
  restoreTaskName?: string(name='RestoreTaskName', description='The name of the restore task.

This parameter is required.', example='test'),
  restoreTime?: long(name='RestoreTime', description='The time to run the restore task, such as 1554560477000.', example='1554560477000'),
}

model CreateRestoreTaskResponseBody = {
  errCode?: string(name='ErrCode', description='The error code.', example='Param.NotFound'),
  errMessage?: string(name='ErrMessage', description='The error message.', example='The specified parameter %s value is not valid.'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='The ID of the request.', example='60AF7C5D-EF4D-4D48-8FD5-C0823FDF28AC'),
  restoreTaskId?: string(name='RestoreTaskId', description='The ID of the restore task.', example='s102h*****'),
  success?: boolean(name='Success', description='Indicates whether the request is successful. Valid values:

*   **true**: The request is successful.
*   **false**: The request fails.', example='true'),
}

model CreateRestoreTaskResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateRestoreTaskResponseBody(name='body'),
}

/**
 * @summary Creates a restoration task.
 *
 * @param request CreateRestoreTaskRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateRestoreTaskResponse
 */
@context("createRestoreTaskWithContext")
async function createRestoreTaskWithOptions(request: CreateRestoreTaskRequest, runtime: $RuntimeOptions): CreateRestoreTaskResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.backupGatewayId)) {
    query['BackupGatewayId'] = request.backupGatewayId;
  }
  if (!$isNull(request.backupPlanId)) {
    query['BackupPlanId'] = request.backupPlanId;
  }
  if (!$isNull(request.backupSetId)) {
    query['BackupSetId'] = request.backupSetId;
  }
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.crossAliyunId)) {
    query['CrossAliyunId'] = request.crossAliyunId;
  }
  if (!$isNull(request.crossRoleName)) {
    query['CrossRoleName'] = request.crossRoleName;
  }
  if (!$isNull(request.destinationEndpointDatabaseName)) {
    query['DestinationEndpointDatabaseName'] = request.destinationEndpointDatabaseName;
  }
  if (!$isNull(request.destinationEndpointIP)) {
    query['DestinationEndpointIP'] = request.destinationEndpointIP;
  }
  if (!$isNull(request.destinationEndpointInstanceID)) {
    query['DestinationEndpointInstanceID'] = request.destinationEndpointInstanceID;
  }
  if (!$isNull(request.destinationEndpointInstanceType)) {
    query['DestinationEndpointInstanceType'] = request.destinationEndpointInstanceType;
  }
  if (!$isNull(request.destinationEndpointOracleSID)) {
    query['DestinationEndpointOracleSID'] = request.destinationEndpointOracleSID;
  }
  if (!$isNull(request.destinationEndpointPassword)) {
    query['DestinationEndpointPassword'] = request.destinationEndpointPassword;
  }
  if (!$isNull(request.destinationEndpointPort)) {
    query['DestinationEndpointPort'] = request.destinationEndpointPort;
  }
  if (!$isNull(request.destinationEndpointRegion)) {
    query['DestinationEndpointRegion'] = request.destinationEndpointRegion;
  }
  if (!$isNull(request.destinationEndpointUserName)) {
    query['DestinationEndpointUserName'] = request.destinationEndpointUserName;
  }
  if (!$isNull(request.duplicateConflict)) {
    query['DuplicateConflict'] = request.duplicateConflict;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  if (!$isNull(request.restoreDir)) {
    query['RestoreDir'] = request.restoreDir;
  }
  if (!$isNull(request.restoreHome)) {
    query['RestoreHome'] = request.restoreHome;
  }
  if (!$isNull(request.restoreObjects)) {
    query['RestoreObjects'] = request.restoreObjects;
  }
  if (!$isNull(request.restoreTaskName)) {
    query['RestoreTaskName'] = request.restoreTaskName;
  }
  if (!$isNull(request.restoreTime)) {
    query['RestoreTime'] = request.restoreTime;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateRestoreTask',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates a restoration task.
 *
 * @param request CreateRestoreTaskRequest
 * @return CreateRestoreTaskResponse
 */
async function createRestoreTask(request: CreateRestoreTaskRequest): CreateRestoreTaskResponse {
  var runtime = new $RuntimeOptions{};
  return createRestoreTaskWithOptions(request, runtime);
}

model DescribeBackupGatewayListRequest {
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request.', example='ETnLKlblzczshOTUbOCzxxxxxxx'),
  identifier?: string(name='Identifier', description='The unique identifier of the backup gateway. You can query multiple backup gateways. Separate multiple identifiers with commas (,).', example='7213527653217'),
  ownerId?: string(name='OwnerId'),
  pageNum?: int32(name='PageNum', description='The number of the page to return. The value must be a positive integer. Default value: 0.', example='1'),
  pageSize?: int32(name='PageSize', description='The number of entries to return on each page. Valid values:

*   **30**
*   **50**
*   **100**

> Default value: 30.', example='30'),
  region?: string(name='Region', description='The region in which Database Backup (DBS) is activated. Valid values:

*   **cn-hangzhou**: China (Hangzhou)
*   **cn-shanghai**: China (Shanghai)
*   **cn-qingdao**: China (Qingdao)
*   **cn-beijing**: China (Beijing)
*   **cn-shenzhen**: China (Shenzhen)
*   **cn-hongkong**: China (Hong Kong)
*   **ap-southeast-1**: Singapore (Singapore)
*   **cn-hangzhou-finance**: China East 1 Finance
*   **cn-shanghai-finance**: China East 2 Finance
*   **cn-shenzhen-finance**: China South 1 Finance

This parameter is required.', example='cn-hangzhou'),
}

model DescribeBackupGatewayListResponseBody = {
  errCode?: string(name='ErrCode', description='The error code.', example='Param.NotFound'),
  errMessage?: string(name='ErrMessage', description='The error message.', example='The specified parameter %s value is not valid.'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  items?: {
    backupGateway?: [ 
    {
      backupGatewayCreateTime?: long(name='BackupGatewayCreateTime', description='The time when the backup gateway was created, such as 1554560477000.', example='1554560477000'),
      backupGatewayId?: string(name='BackupGatewayId', description='The ID of the backup gateway.', example='2321313123'),
      backupGatewayStatus?: string(name='BackupGatewayStatus', description='The status of the backup gateway. Valid values:

*   ONLINE: The backup gateway is online.
*   OFFLINE: The backup gateway is offline.
*   STOPPED: The backup gateway is stopped.
*   UPGRADING: The backup gateway is being upgraded.', example='ONLINE'),
      displayName?: string(name='DisplayName', description='The display name of the backup gateway.', example='test'),
      identifier?: string(name='Identifier', description='The unique identifier of the backup gateway.', example='sgdsajhdgu'),
      lastHeartbeatTime?: long(name='LastHeartbeatTime', description='The last time when a heartbeat message was sent, such as 1554560477000.', example='1554560477000'),
      region?: string(name='Region', description='The ID of the region.', example='cn-hangzhou'),
      sourceEndpointHostname?: string(name='SourceEndpointHostname', description='The name of the host on which the backup gateway is installed.', example='test'),
      sourceEndpointInternetIP?: string(name='SourceEndpointInternetIP', description='The public IP address of the host on which the backup gateway is installed.', example='XX.XX.XX.XX'),
      sourceEndpointIntranetIP?: string(name='SourceEndpointIntranetIP', description='The private IP address of the host on which the backup gateway is installed.', example='XX.XX.XX.XX'),
    }
  ](name='BackupGateway')
  }(name='Items', description='The details of backup gateways.'),
  pageNum?: int32(name='PageNum', description='The page number of the returned page.', example='1'),
  pageSize?: int32(name='PageSize', description='The number of entries returned on each page.', example='30'),
  requestId?: string(name='RequestId', description='The ID of the request.', example='46361705-8531-492F-807E-A97E482DD4A1'),
  success?: boolean(name='Success', description='Indicates whether the request is successful.', example='true'),
  totalElements?: int32(name='TotalElements', description='The total number of backup gateways.', example='0'),
  totalPages?: int32(name='TotalPages', description='The total number of returned pages.', example='1'),
}

model DescribeBackupGatewayListResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeBackupGatewayListResponseBody(name='body'),
}

/**
 * @summary Queries backup gateways.
 *
 * @param request DescribeBackupGatewayListRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeBackupGatewayListResponse
 */
@context("describeBackupGatewayListWithContext")
async function describeBackupGatewayListWithOptions(request: DescribeBackupGatewayListRequest, runtime: $RuntimeOptions): DescribeBackupGatewayListResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.identifier)) {
    query['Identifier'] = request.identifier;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  if (!$isNull(request.pageNum)) {
    query['PageNum'] = request.pageNum;
  }
  if (!$isNull(request.pageSize)) {
    query['PageSize'] = request.pageSize;
  }
  if (!$isNull(request.region)) {
    query['Region'] = request.region;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeBackupGatewayList',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries backup gateways.
 *
 * @param request DescribeBackupGatewayListRequest
 * @return DescribeBackupGatewayListResponse
 */
async function describeBackupGatewayList(request: DescribeBackupGatewayListRequest): DescribeBackupGatewayListResponse {
  var runtime = new $RuntimeOptions{};
  return describeBackupGatewayListWithOptions(request, runtime);
}

model DescribeBackupPlanBillingRequest {
  backupPlanId?: string(name='BackupPlanId', description='The ID of the backup gateway.

This parameter is required.', example='160813'),
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request. You can use the client to generate the value, but you must make sure that it is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length.', example='ETnLKlblzczshOTUbOCzxxxxxxxxxx'),
  ownerId?: string(name='OwnerId'),
  showStorageType?: boolean(name='ShowStorageType', description='Indicates whether the storage type is displayed.', example='true'),
}

model DescribeBackupPlanBillingResponseBody = {
  errCode?: string(name='ErrCode', description='The error code.', example='InvalidParameter'),
  errMessage?: string(name='ErrMessage', description='The error message.', example='This backupPlan can\\\\"t support this action'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='400'),
  item?: {
    buyChargeType?: string(name='BuyChargeType', description='The billing method. Valid values:

*   PREPAY
*   POSTPAY', example='PREPAY'),
    buyCreateTimestamp?: long(name='BuyCreateTimestamp', description='The timestamp that indicates when the instance was purchased.', example='1554560477000'),
    buyExpiredTimestamp?: long(name='BuyExpiredTimestamp', description='The timestamp that indicates when the instance expires.

> This parameter is available only if the value of the BuyChargeType parameter is PREPAY.', example='1554560477000'),
    buySpec?: string(name='BuySpec', description='The specifications of the instance.', example='micro'),
    contStorageSize?: long(name='ContStorageSize', description='The size of the built-in storage for storing incremental backup data.', example='12134'),
    fullStorageSize?: long(name='FullStorageSize', description='The size of the built-in storage for storing full backup data.', example='13123'),
    isExpired?: boolean(name='IsExpired', description='Indicates whether the instance expired.

> This parameter is available only if the value of the BuyChargeType parameter is PREPAY.', example='true'),
    isFreeBytesUnlimited?: boolean(name='IsFreeBytesUnlimited', description='Indicates whether the instance has no backup traffic limit.', example='true'),
    paiedBytes?: long(name='PaiedBytes', description='The total paid backup traffic in the current month.', example='213213'),
    quotaEndTimestamp?: long(name='QuotaEndTimestamp', description='The timestamp that indicates when the billing cycle of free backup traffic ends.', example='1554560477000'),
    quotaStartTimestamp?: long(name='QuotaStartTimestamp', description='The timestamp that indicates when the billing cycle of free backup traffic starts.', example='1554560477000'),
    totalFreeBytes?: long(name='TotalFreeBytes', description='The total free backup traffic in the current month.

> This parameter is available only if the value of the BuyChargeType parameter is PREPAY and the value of the IsFreeBytesUnlimited parameter is false.', example='13123'),
    usedFullBytes?: long(name='UsedFullBytes', description='The paid full backup traffic in the current month.', example='1312313'),
    usedIncrementBytes?: long(name='UsedIncrementBytes', description='The paid incremental backup traffic in the current month.', example='131231'),
  }(name='Item', description='The billing information of the backup schedule.'),
  requestId?: string(name='RequestId', description='The ID of the request.', example='EB4DFD5E-3618-498D-BE35-4DBEA0072122'),
  success?: boolean(name='Success', description='Indicates whether the request is successful.', example='true'),
}

model DescribeBackupPlanBillingResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeBackupPlanBillingResponseBody(name='body'),
}

/**
 * @summary Queries the billing information of a backup schedule.
 *
 * @param request DescribeBackupPlanBillingRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeBackupPlanBillingResponse
 */
@context("describeBackupPlanBillingWithContext")
async function describeBackupPlanBillingWithOptions(request: DescribeBackupPlanBillingRequest, runtime: $RuntimeOptions): DescribeBackupPlanBillingResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.backupPlanId)) {
    query['BackupPlanId'] = request.backupPlanId;
  }
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  if (!$isNull(request.showStorageType)) {
    query['ShowStorageType'] = request.showStorageType;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeBackupPlanBilling',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the billing information of a backup schedule.
 *
 * @param request DescribeBackupPlanBillingRequest
 * @return DescribeBackupPlanBillingResponse
 */
async function describeBackupPlanBilling(request: DescribeBackupPlanBillingRequest): DescribeBackupPlanBillingResponse {
  var runtime = new $RuntimeOptions{};
  return describeBackupPlanBillingWithOptions(request, runtime);
}

model DescribeBackupPlanListRequest {
  backupPlanId?: string(name='BackupPlanId', description='The ID of the backup schedule. You can query multiple backup schedule IDs. Separate multiple IDs with commas (,).', example='dbstooi01exXXXX'),
  backupPlanName?: string(name='BackupPlanName', description='Backup plan name.', example='test123'),
  backupPlanStatus?: string(name='BackupPlanStatus', description='Backup plan status, the values are as follows:

* **wait**: Not configured
* **init**: Not started (pre-check failed)
* **running**: Running
* **stop**: Failed
* **pause**: Paused
* **locked**: Locked
* **check_pass**: Pre-check passed', example='wait'),
  clientToken?: string(name='ClientToken', description='Used to ensure the idempotence of the request, preventing duplicate submissions.', example='ASDASDASDSADASFCZXVZ'),
  ownerId?: string(name='OwnerId'),
  pageNum?: int32(name='PageNum', description='Page number, must be greater than or equal to 0 and not exceed the maximum value of Integer. The default value is 0.', example='1'),
  pageSize?: int32(name='PageSize', description='Number of records per page, the value should be between 1 and 100.

> The default is **30**.', example='30'),
  region?: string(name='Region', description='DBS region, you can view the supported DBS regions by calling the [DescribeRegions](https://help.aliyun.com/document_detail/2869853.html) interface.', example='cn-hangzhou'),
  resourceGroupId?: string(name='ResourceGroupId', description='Resource group ID.', example='rg-aekzecovzti****'),
}

model DescribeBackupPlanListResponseBody = {
  errCode?: string(name='ErrCode', description='Error code.', example='InvalidParameterValid'),
  errMessage?: string(name='ErrMessage', description='Error message.', example='The specified parameter %s value is not valid.'),
  httpStatusCode?: int32(name='HttpStatusCode', description='HTTP status code.', example='200'),
  items?: {
    backupPlanDetail?: [ 
    {
      backupGatewayId?: long(name='BackupGatewayId', description='Backup gateway ID.', example='827362187368736'),
      backupMethod?: string(name='BackupMethod', description='Backup method. The return values are as follows:
- **logical**: Logical backup
- **physical**: Physical backup
- **duplication**: Replication backup', example='logical'),
      backupObjects?: string(name='BackupObjects', description='Backup objects.'),
      backupPeriod?: string(name='BackupPeriod', description='Full backup cycle. The return values are as follows:
- **Monday**: Monday
- **Tuesday**: Tuesday
- **Wednesday**: Wednesday
- **Thursday**: Thursday
- **Friday**: Friday
- **Saturday**: Saturday
- **Sunday**: Sunday', example='Monday'),
      backupPlanCreateTime?: long(name='BackupPlanCreateTime', description='Timestamp of the backup plan creation.', example='1582527713000'),
      backupPlanId?: string(name='BackupPlanId', description='Backup plan ID.', example='dbstooi01eXXXX'),
      backupPlanName?: string(name='BackupPlanName', description='Backup plan name.', example='dbstooi01e****'),
      backupPlanStatus?: string(name='BackupPlanStatus', description='Backup plan status. The return values are as follows:
- **wait**: Not configured
- **init**: Not started (pre-check failed)
- **running**: Running
- **stop**: Failed
- **pause**: Paused
- **locked**: Locked
- **check_pass**: Pre-check passed', example='init'),
      backupRetentionPeriod?: int32(name='BackupRetentionPeriod', description='Backup data retention period, with a value range of 0 to 1825 days.', example='365'),
      backupSetDownloadDir?: string(name='BackupSetDownloadDir', description='Download server directory of the backup set'),
      backupSetDownloadFullDataFormat?: string(name='BackupSetDownloadFullDataFormat', description='Full data format for backup set download:
* **Native**
* **SQL**
* **CSV**
* **JSON**', example='SQL'),
      backupSetDownloadGatewayId?: long(name='BackupSetDownloadGatewayId', description='Backup set download backup gateway ID.', example='123123'),
      backupSetDownloadIncrementDataFormat?: string(name='BackupSetDownloadIncrementDataFormat', description='Backup set download full data format:
* **Native**
* **SQL**
* **CSV**
* **JSON**', example='SQL'),
      backupSetDownloadTargetType?: string(name='BackupSetDownloadTargetType', description='Backup set download target type.

> The only value is: agent, indicating that the backup gateway is installed.', example='agent'),
      backupStartTime?: string(name='BackupStartTime', description='Full backup start time, in the format HH:mm.', example='14:22'),
      backupStorageType?: string(name='BackupStorageType', description='Built-in storage type. The return values are as follows:

- Empty (default): Backup data is stored on the user\\\\"s OSS.
- system: Backup data is stored on the built-in OSS of DBS.', example='system'),
      beginTimestampForRestore?: long(name='BeginTimestampForRestore', description='Start time for the database restore period, with a return value of 1554560477000.', example='1554560477000'),
      crossAliyunId?: string(name='CrossAliyunId', description='UID for cross-Aliyun account backup.', example='2xxx7778xxxxxxxxxx'),
      crossRoleName?: string(name='CrossRoleName', description='RAM role name for cross-Aliyun account backup.', example='test123'),
      databaseType?: string(name='DatabaseType', description='Database type.', example='MySQL'),
      duplicationArchivePeriod?: int32(name='DuplicationArchivePeriod', description='Time (in days) to convert to archive cold backup storage.', example='365'),
      duplicationInfrequentAccessPeriod?: int32(name='DuplicationInfrequentAccessPeriod', description='Time (in days) to convert to infrequent access storage.', example='180'),
      enableBackupLog?: boolean(name='EnableBackupLog', description='Indicates whether incremental log backup is enabled, with return values as follows:
- **true**: Enabled
- **false**: Disabled', example='true'),
      endTimestampForRestore?: long(name='EndTimestampForRestore', description='End time of the database restorable period, in timestamp format.', example='1554560477000'),
      errMessage?: string(name='ErrMessage', description='Pre-check task error message.', example='can not connect to oracle instance orcl with user dbs'),
      instanceClass?: string(name='InstanceClass', description='Instance class, with return values as follows:

- **micro**: Entry-level
- **small**: Low-spec
- **medium**: Medium-spec
- **large**: High-spec
- **xlarge**: High-spec (no traffic limit)', example='micro'),
      OSSBucketName?: string(name='OSSBucketName', description='OSS Bucket name.', example='dbs-backup-1857XXXXX489'),
      OSSBucketRegion?: string(name='OSSBucketRegion', description='OSS Bucket region.', example='cn-hangzhou'),
      openBackupSetAutoDownload?: boolean(name='OpenBackupSetAutoDownload', description='Indicates whether the automatic backup set download feature is enabled.', example='true'),
      resourceGroupId?: string(name='ResourceGroupId', description='Resource group ID.', example='rg-aekzecovzti****'),
      sourceEndpointDatabaseName?: string(name='SourceEndpointDatabaseName', description='Database name.', example='test'),
      sourceEndpointInstanceID?: string(name='SourceEndpointInstanceID', description='Database instance ID.', example='test'),
      sourceEndpointInstanceType?: string(name='SourceEndpointInstanceType', description='Location of the database, the return values are as follows:
- **rds**
- **ecs**
- **express**: Database connected via dedicated line/VPN gateway/smart gateway
- **agent**: Database connected via backup gateway
- **dds**: Cloud MongoDB
- **other**: Database connected directly via IP:Port', example='rds'),
      sourceEndpointIpPort?: string(name='SourceEndpointIpPort', description='Database connection address.', example='100.*.*.10:33204'),
      sourceEndpointOracleSID?: string(name='SourceEndpointOracleSID', description='Oracle SID name.', example='test'),
      sourceEndpointRegion?: string(name='SourceEndpointRegion', description='Database region.', example='cn-hangzhou'),
      sourceEndpointUserName?: string(name='SourceEndpointUserName', description='Database username.', example='test'),
    }
  ](name='BackupPlanDetail')
  }(name='Items', description='Details of the backup plans.'),
  pageNum?: int32(name='PageNum', description='Page number.', example='1'),
  pageSize?: int32(name='PageSize', description='Number of records per page.', example='30'),
  requestId?: string(name='RequestId', description='Request ID.', example='4F1888AC-1138-4995-B9FE-D2734F61C058'),
  success?: boolean(name='Success', description='Whether the operation was successful.', example='true'),
  totalElements?: int32(name='TotalElements', description='Total number of backup plans.', example='100'),
  totalPages?: int32(name='TotalPages', description='Total number of pages.', example='4'),
}

model DescribeBackupPlanListResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeBackupPlanListResponseBody(name='body'),
}

/**
 * @summary Query the list of backup plans
 *
 * @description Before using this interface, please activate the OSS service in advance. For more information, see [Object Storage Service (OSS)](https://help.aliyun.com/document_detail/31817.html).
 *
 * @param request DescribeBackupPlanListRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeBackupPlanListResponse
 */
@context("describeBackupPlanListWithContext")
async function describeBackupPlanListWithOptions(request: DescribeBackupPlanListRequest, runtime: $RuntimeOptions): DescribeBackupPlanListResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.backupPlanId)) {
    query['BackupPlanId'] = request.backupPlanId;
  }
  if (!$isNull(request.backupPlanName)) {
    query['BackupPlanName'] = request.backupPlanName;
  }
  if (!$isNull(request.backupPlanStatus)) {
    query['BackupPlanStatus'] = request.backupPlanStatus;
  }
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  if (!$isNull(request.pageNum)) {
    query['PageNum'] = request.pageNum;
  }
  if (!$isNull(request.pageSize)) {
    query['PageSize'] = request.pageSize;
  }
  if (!$isNull(request.region)) {
    query['Region'] = request.region;
  }
  if (!$isNull(request.resourceGroupId)) {
    query['ResourceGroupId'] = request.resourceGroupId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeBackupPlanList',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Query the list of backup plans
 *
 * @description Before using this interface, please activate the OSS service in advance. For more information, see [Object Storage Service (OSS)](https://help.aliyun.com/document_detail/31817.html).
 *
 * @param request DescribeBackupPlanListRequest
 * @return DescribeBackupPlanListResponse
 */
async function describeBackupPlanList(request: DescribeBackupPlanListRequest): DescribeBackupPlanListResponse {
  var runtime = new $RuntimeOptions{};
  return describeBackupPlanListWithOptions(request, runtime);
}

model DescribeBackupSetDownloadTaskListRequest {
  backupPlanId?: string(name='BackupPlanId', description='The backup schedule ID. You can call the [DescribeBackupPlanList](https://help.aliyun.com/document_detail/2869825.html) operation to obtain the ID.

>  You must configure the **BackupPlanId** or **BackupSetDownloadTaskId** parameter.', example='dbsqhnuhyw3****'),
  backupSetDownloadTaskId?: string(name='BackupSetDownloadTaskId', description='The ID of the backup set download task.

*   Full backup set download task: You can call the [CreateFullBackupSetDownload](https://help.aliyun.com/document_detail/2869842.html) operation to create a full backup set download task and obtain the task ID.
*   Incremental backup set download task: You can call the [CreateIncrementBackupSetDownload](https://help.aliyun.com/document_detail/2869843.html) operation to create an incremental backup set download task and obtain the task ID.', example='urxgrxt7****'),
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request.', example='ETnLKlblzczshOTUbOCzXXXXXX'),
  ownerId?: string(name='OwnerId'),
  pageNum?: int32(name='PageNum', description='The number of the page to return. The value must be a positive integer. Default value: 0.', example='1'),
  pageSize?: int32(name='PageSize', description='The number of entries to return on each page. Valid values: 30, 50, and 100.

> Default value: 30.', example='30'),
}

model DescribeBackupSetDownloadTaskListResponseBody = {
  errCode?: string(name='ErrCode', description='The error code.', example='Param.NotFound'),
  errMessage?: string(name='ErrMessage', description='The error message.', example='The specified parameter %s value is not valid.'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  items?: {
    backupSetDownloadTaskDetail?: [ 
    {
      backupGatewayId?: long(name='BackupGatewayId', description='The backup gateway that is used to download the backup set. This parameter is available only if the value of the BackupSetDownloadWay parameter is auto.', example='N/A'),
      backupPlanId?: string(name='BackupPlanId', description='The ID of the backup schedule.', example='qhnuhyw3****'),
      backupSetCode?: string(name='BackupSetCode', description='The code of the backup set.', example='mysql-bin.00XXXX'),
      backupSetDataFormat?: string(name='BackupSetDataFormat', description='The format in which the backup set is downloaded. Valid values:

*   **Native**
*   **SQL**
*   **CSV**
*   **JSON**', example='Native'),
      backupSetDataSize?: long(name='BackupSetDataSize', description='The size of the data in the backup set.', example='237837'),
      backupSetDbType?: string(name='BackupSetDbType', description='The type of the database.', example='MySQL'),
      backupSetDownloadCreateTime?: long(name='BackupSetDownloadCreateTime', description='The timestamp that indicates when the backup set download task was created.', example='1642044001000'),
      backupSetDownloadDir?: string(name='BackupSetDownloadDir', description='The server directory to which the backup set is downloaded.

> This parameter is available only if the value of the BackupSetDownloadWay parameter is auto.', example='N/A'),
      backupSetDownloadFinishTime?: long(name='BackupSetDownloadFinishTime', description='The timestamp that indicates when the backup set download task is complete.', example='1642044013000'),
      backupSetDownloadInternetUrl?: string(name='BackupSetDownloadInternetUrl', description='The public download URL of the backup set.

> This parameter is available only if the value of the BackupSetDownloadWay parameter is manual and the conversion is complete.', example='"//dbs-137XXXX-cn-hangzhou-1hr5cpbtmXXXX.oss-cn-hangzhou.example"'),
      backupSetDownloadIntranetUrl?: string(name='BackupSetDownloadIntranetUrl', description='The internal download URL of the backup set.

> This parameter is available only if the value of the BackupSetDownloadWay parameter is manual and the conversion is complete.', example='"//dbs-13XXXX-cn-hangzhou-1hr5cpbtmXXXX.oss-cn-hangzhou-internal.example"'),
      backupSetDownloadStatus?: string(name='BackupSetDownloadStatus', description='The status of the backup set download task. Valid values:

*   **checking**: The backup set download task is being prechecked.
*   **init**: The backup set download task is not started and fails to pass the precheck.
*   **check_pass**: The backup set download task passes the precheck.
*   **pause**: The backup set download task is paused.
*   **schedule**: The backup set download task is waiting to be scheduled
*   **running**: The backup set download task is running.
*   **stop**: The backup set download task fails.
*   **finish**: The backup set download task is complete.', example='finish'),
      backupSetDownloadTargetType?: string(name='BackupSetDownloadTargetType', description='The type of the destination server to which the backup set is downloaded.

> This parameter is available only if the value of the BackupSetDownloadWay parameter is auto. A value of agent indicates a server on which a backup gateway is installed.', example='N/A'),
      backupSetDownloadTaskId?: string(name='BackupSetDownloadTaskId', description='The ID of the backup set download task.', example='urxgrxt7****'),
      backupSetDownloadTaskName?: string(name='BackupSetDownloadTaskName', description='The name of the backup set download task.', example='1h7za2yws****-ManualCont'),
      backupSetDownloadWay?: string(name='BackupSetDownloadWay', description='The method in which the backup set is downloaded. Valid values:

*   **manual**: The backup set is manually downloaded.
*   **auto**: The backup set is automatically downloaded.', example='manual'),
      backupSetId?: string(name='BackupSetId', description='The ID of the backup set.', example='1h7za2yws****'),
      backupSetJobType?: string(name='BackupSetJobType', description='The type of the backup set task. Valid values:

*   **cbs_backup_sub_full**: logical full backup set download task
*   **cbs_backup_sub_cont**: logical incremental backup set download task', example='cbs_backup_sub_cont'),
      errMessage?: string(name='ErrMessage', description='The error message.', example='java.lang.IndexOutOfBoundsException.'),
    }
  ](name='BackupSetDownloadTaskDetail')
  }(name='Items', description='The details of the backup schedules.'),
  pageNum?: int32(name='PageNum', description='The page number of the returned page.', example='0'),
  pageSize?: int32(name='PageSize', description='The number of entries returned on each page.', example='30'),
  requestId?: string(name='RequestId', description='The ID of the request.', example='6DC3D286-E0E6-5988-A558-2184984B****'),
  success?: boolean(name='Success', description='Indicates whether the request is successful. Valid values:

*   **true**: The request is successful.
*   **false**: The request fails.', example='true'),
  totalElements?: int32(name='TotalElements', description='The total number of backup set download tasks.', example='1'),
  totalPages?: int32(name='TotalPages', description='The total number of returned pages.', example='1'),
}

model DescribeBackupSetDownloadTaskListResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeBackupSetDownloadTaskListResponseBody(name='body'),
}

/**
 * @summary Queries backup set download tasks.
 *
 * @param request DescribeBackupSetDownloadTaskListRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeBackupSetDownloadTaskListResponse
 */
@context("describeBackupSetDownloadTaskListWithContext")
async function describeBackupSetDownloadTaskListWithOptions(request: DescribeBackupSetDownloadTaskListRequest, runtime: $RuntimeOptions): DescribeBackupSetDownloadTaskListResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.backupPlanId)) {
    query['BackupPlanId'] = request.backupPlanId;
  }
  if (!$isNull(request.backupSetDownloadTaskId)) {
    query['BackupSetDownloadTaskId'] = request.backupSetDownloadTaskId;
  }
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  if (!$isNull(request.pageNum)) {
    query['PageNum'] = request.pageNum;
  }
  if (!$isNull(request.pageSize)) {
    query['PageSize'] = request.pageSize;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeBackupSetDownloadTaskList',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries backup set download tasks.
 *
 * @param request DescribeBackupSetDownloadTaskListRequest
 * @return DescribeBackupSetDownloadTaskListResponse
 */
async function describeBackupSetDownloadTaskList(request: DescribeBackupSetDownloadTaskListRequest): DescribeBackupSetDownloadTaskListResponse {
  var runtime = new $RuntimeOptions{};
  return describeBackupSetDownloadTaskListWithOptions(request, runtime);
}

model DescribeDLAServiceRequest {
  backupPlanId?: string(name='BackupPlanId', description='The ID of the backup schedule. You can call the [DescribeBackupPlanList](https://help.aliyun.com/document_detail/2869825.html) operation to obtain the ID of the backup schedule.

This parameter is required.', example='dbstooi01ex****'),
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request.', example='DBS'),
  ownerId?: string(name='OwnerId'),
}

model DescribeDLAServiceResponseBody = {
  autoAdd?: boolean(name='AutoAdd', description='Specifies whether to enable the feature of automatically adding incremental data to a data lake. If this feature is enabled, DBS adds the backup sets that are newly generated to the data lake that is created for the backup schedule. Valid values:

*   **true**: enables the feature.
*   **false**: disables the feature.', example='true'),
  errCode?: string(name='ErrCode', description='The error code.', example='Param.NotFound'),
  errMessage?: string(name='ErrMessage', description='The error message.', example='The specified parameter %s value is not valid.'),
  haveJobFailed?: boolean(name='HaveJobFailed', description='Indicates whether a failed DLA task exists in the return result. Valid values:

*   **true**: A failed DLA task exists.
*   **false**: No failed DLA task exists.', example='false'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='The ID of the request.', example='4F1888AC-1138-4995-B9FE-D2734F61C058'),
  state?: string(name='State', description='The status of the DLA service for the backup schedule. Valid values:

*   **Running**: DLA is running.
*   **Closing**: DLA is being disabled.
*   **Closed**: DLA is disabled.', example='Running'),
  success?: boolean(name='Success', description='Indicates whether the request is successful.', example='true'),
}

model DescribeDLAServiceResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeDLAServiceResponseBody(name='body'),
}

/**
 * @summary Queries the status of the Data Lake Analytics (DLA) service for a backup schedule.
 *
 * @param request DescribeDLAServiceRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeDLAServiceResponse
 */
@context("describeDLAServiceWithContext")
async function describeDLAServiceWithOptions(request: DescribeDLAServiceRequest, runtime: $RuntimeOptions): DescribeDLAServiceResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.backupPlanId)) {
    query['BackupPlanId'] = request.backupPlanId;
  }
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeDLAService',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the status of the Data Lake Analytics (DLA) service for a backup schedule.
 *
 * @param request DescribeDLAServiceRequest
 * @return DescribeDLAServiceResponse
 */
async function describeDLAService(request: DescribeDLAServiceRequest): DescribeDLAServiceResponse {
  var runtime = new $RuntimeOptions{};
  return describeDLAServiceWithOptions(request, runtime);
}

model DescribeFullBackupListRequest {
  backupPlanId?: string(name='BackupPlanId', description='The error code.

This parameter is required.', example='The total number of full backup tasks.'),
  backupSetId?: string(name='BackupSetId', description='The end time of the backup task, such as 1554560477000.', example='The point in time when the backup set expires, such as 1554560477000.'),
  clientToken?: string(name='ClientToken', description='The number of entries returned on each page.', example='ETnLKlblzczshOTUbOCzxxxxxxx'),
  endTimestamp?: long(name='EndTimestamp'),
  ownerId?: string(name='OwnerId'),
  pageNum?: int32(name='PageNum', description='The client token that is used to ensure the idempotence of the request.', example='1'),
  pageSize?: int32(name='PageSize', description='The error message.', example='30'),
  showStorageType?: boolean(name='ShowStorageType', description='The ID of the request.', example='true'),
  startTimestamp?: long(name='StartTimestamp', description='Queries full backup tasks.'),
}

model DescribeFullBackupListResponseBody = {
  errCode?: string(name='ErrCode', example='Param.NotFound'),
  errMessage?: string(name='ErrMessage', example='The specified parameter %s value is not valid.'),
  httpStatusCode?: int32(name='HttpStatusCode', example='200'),
  items?: {
    fullBackupFile?: [ 
    {
      backupObjects?: string(name='BackupObjects', example='[{\\\\"DBName\\\\":\\\\"test\\\\"}]'),
      backupSetExpiredTime?: long(name='BackupSetExpiredTime'),
      backupSetId?: string(name='BackupSetId'),
      backupSize?: long(name='BackupSize'),
      backupStatus?: string(name='BackupStatus', example='finish'),
      createTime?: long(name='CreateTime'),
      endTime?: long(name='EndTime'),
      errMessage?: string(name='ErrMessage', example='NULL'),
      finishTime?: long(name='FinishTime'),
      sourceEndpointIpPort?: string(name='SourceEndpointIpPort'),
      startTime?: long(name='StartTime'),
      storageMethod?: string(name='StorageMethod', example='Standard'),
    }
  ](name='FullBackupFile')
  }(name='Items'),
  pageNum?: int32(name='PageNum', example='1'),
  pageSize?: int32(name='PageSize', example='10'),
  requestId?: string(name='RequestId', example='636BC118-6080-4119-A6B5-C199CEC1037D'),
  success?: boolean(name='Success', example='true'),
  totalElements?: int32(name='TotalElements', example='1'),
  totalPages?: int32(name='TotalPages', example='1'),
}

model DescribeFullBackupListResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeFullBackupListResponseBody(name='body'),
}

/**
 * @summary cn-hangzhou
 *
 * @param request DescribeFullBackupListRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeFullBackupListResponse
 */
@context("describeFullBackupListWithContext")
async function describeFullBackupListWithOptions(request: DescribeFullBackupListRequest, runtime: $RuntimeOptions): DescribeFullBackupListResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.backupPlanId)) {
    query['BackupPlanId'] = request.backupPlanId;
  }
  if (!$isNull(request.backupSetId)) {
    query['BackupSetId'] = request.backupSetId;
  }
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.endTimestamp)) {
    query['EndTimestamp'] = request.endTimestamp;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  if (!$isNull(request.pageNum)) {
    query['PageNum'] = request.pageNum;
  }
  if (!$isNull(request.pageSize)) {
    query['PageSize'] = request.pageSize;
  }
  if (!$isNull(request.showStorageType)) {
    query['ShowStorageType'] = request.showStorageType;
  }
  if (!$isNull(request.startTimestamp)) {
    query['StartTimestamp'] = request.startTimestamp;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeFullBackupList',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary cn-hangzhou
 *
 * @param request DescribeFullBackupListRequest
 * @return DescribeFullBackupListResponse
 */
async function describeFullBackupList(request: DescribeFullBackupListRequest): DescribeFullBackupListResponse {
  var runtime = new $RuntimeOptions{};
  return describeFullBackupListWithOptions(request, runtime);
}

model DescribeIncrementBackupListRequest {
  backupPlanId?: string(name='BackupPlanId', description='The ID of the backup schedule. You can call the [DescribeBackupPlanList](https://help.aliyun.com/document_detail/2869825.html) operation to obtain the ID.

This parameter is required.', example='dbsqdss5tmh****'),
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request.', example='ETnLKlblzczshOTUbOCzxxxxxxx'),
  endTimestamp?: long(name='EndTimestamp', description='The end of the time range to query.', example='1570701361600'),
  ownerId?: string(name='OwnerId'),
  pageNum?: int32(name='PageNum', description='The number of the page to return. The value must be a positive integer. Default value: 0.', example='1'),
  pageSize?: int32(name='PageSize', description='The number of entries to return on each page. Valid values: 30, 50, and 100.

> Default value: 30.', example='30'),
  showStorageType?: boolean(name='ShowStorageType', description='Specifies whether to return the storage class. Valid values:

*   true
*   false

> Default value: true.', example='true'),
  startTimestamp?: long(name='StartTimestamp', description='The beginning of the time range to query.', example='1570701361528'),
}

model DescribeIncrementBackupListResponseBody = {
  errCode?: string(name='ErrCode', description='The error code.', example='Param.NotFound'),
  errMessage?: string(name='ErrMessage', description='The error message.', example='The specified parameter %s value is not valid.'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  items?: {
    incrementBackupFile?: [ 
    {
      backupSetExpiredTime?: long(name='BackupSetExpiredTime', description='The point in time when the backup set expires.', example='1711506719000'),
      backupSetId?: string(name='BackupSetId', description='The ID of the backup set.', example='mysql-bin.00****'),
      backupSetJobId?: string(name='BackupSetJobId', description='The ID of the incremental backup task.', example='1hv5g9wk4****'),
      backupSize?: long(name='BackupSize', description='The size of the backup set.', example='18535'),
      backupStatus?: string(name='BackupStatus', description='The status of the incremental backup task. Valid values:

*   **INIT**: The incremental backup task is not started.
*   **FILLING**: The incremental backup task is in progress.
*   **COMPLETED**: The incremental backup task is complete.
*   **UNCOMPLETED**: The incremental backup task is not complete.', example='FILLING'),
      endTime?: long(name='EndTime', description='The end time of the incremental backup task.', example='1648434713000'),
      sourceEndpointIpPort?: string(name='SourceEndpointIpPort', description='The endpoint that is used to connect to the database.', example='172.1XX.103.1:4XXX'),
      startTime?: long(name='StartTime', description='The start time of the incremental backup task.', example='1648433764000'),
      storageMethod?: string(name='StorageMethod', description='The storage class of the backup data. Valid values:

*   **Standard**: The storage class is Standard.
*   **IA**: The storage class is Infrequent Access (IA).
*   **Archive**: The storage class is Archive.
*   **UNKNOWN**: The storage class is unknown. This value is returned because the task is not complete.', example='Standard'),
    }
  ](name='IncrementBackupFile')
  }(name='Items', description='The details of incremental backup tasks.'),
  pageNum?: int32(name='PageNum', description='The page number of the returned page.', example='0'),
  pageSize?: int32(name='PageSize', description='The number of entries returned on each page.', example='30'),
  requestId?: string(name='RequestId', description='The ID of the request.', example='A5D52069-E8AA-5056-8C5C-654C3610****'),
  success?: boolean(name='Success', description='Indicates whether the request is successful. Valid values:

*   **true**: The request is successful.
*   **false**: The request fails.', example='true'),
  totalElements?: int32(name='TotalElements', description='The total number of incremental backup tasks.', example='1'),
  totalPages?: int32(name='TotalPages', description='The total number of returned pages.', example='1'),
}

model DescribeIncrementBackupListResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeIncrementBackupListResponseBody(name='body'),
}

/**
 * @summary Queries incremental backup tasks.
 *
 * @param request DescribeIncrementBackupListRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeIncrementBackupListResponse
 */
@context("describeIncrementBackupListWithContext")
async function describeIncrementBackupListWithOptions(request: DescribeIncrementBackupListRequest, runtime: $RuntimeOptions): DescribeIncrementBackupListResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.backupPlanId)) {
    query['BackupPlanId'] = request.backupPlanId;
  }
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.endTimestamp)) {
    query['EndTimestamp'] = request.endTimestamp;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  if (!$isNull(request.pageNum)) {
    query['PageNum'] = request.pageNum;
  }
  if (!$isNull(request.pageSize)) {
    query['PageSize'] = request.pageSize;
  }
  if (!$isNull(request.showStorageType)) {
    query['ShowStorageType'] = request.showStorageType;
  }
  if (!$isNull(request.startTimestamp)) {
    query['StartTimestamp'] = request.startTimestamp;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeIncrementBackupList',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries incremental backup tasks.
 *
 * @param request DescribeIncrementBackupListRequest
 * @return DescribeIncrementBackupListResponse
 */
async function describeIncrementBackupList(request: DescribeIncrementBackupListRequest): DescribeIncrementBackupListResponse {
  var runtime = new $RuntimeOptions{};
  return describeIncrementBackupListWithOptions(request, runtime);
}

model DescribeJobErrorCodeRequest {
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request.', example='ETnLKlblzczshOTUbOCz****'),
  language?: string(name='Language', description='The language of the error message. Valid values:

*   **en** (default): English
*   **cn**: Chinese', example='en'),
  ownerId?: string(name='OwnerId'),
  taskId?: string(name='TaskId', description='The ID of the full backup or restore task.

This parameter is required.', example='dbstooi0****'),
}

model DescribeJobErrorCodeResponseBody = {
  errCode?: string(name='ErrCode', description='The error code.', example='Param.NotFound'),
  errMessage?: string(name='ErrMessage', description='The error message.', example='The specified parameter %s value is not valid.'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  item?: {
    errorCode?: string(name='ErrorCode', description='The error code.', example='failed'),
    errorMessage?: string(name='ErrorMessage', description='The standard error message.', example='Describe preCheck progress failed.'),
    jobId?: string(name='JobId', description='The ID of the full backup or restore task.', example='tooi0****'),
    jobState?: string(name='JobState', description='The status of the task.', example='init'),
    jobType?: string(name='JobType', description='The internal ID of the DBS task type.', example='testId'),
    language?: string(name='Language', description='The language of the error message.', example='en'),
  }(name='Item', description='The error information.'),
  requestId?: string(name='RequestId', description='The ID of the request.', example='1FC2F86D-AFF4-4ED9-BB25-ADDE196CB2B5'),
  success?: boolean(name='Success', description='Indicates whether the request is successful.', example='true'),
}

model DescribeJobErrorCodeResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeJobErrorCodeResponseBody(name='body'),
}

/**
 * @summary Queries the error information of a Database Backup (DBS) task.
 *
 * @param request DescribeJobErrorCodeRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeJobErrorCodeResponse
 */
@context("describeJobErrorCodeWithContext")
async function describeJobErrorCodeWithOptions(request: DescribeJobErrorCodeRequest, runtime: $RuntimeOptions): DescribeJobErrorCodeResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.language)) {
    query['Language'] = request.language;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  if (!$isNull(request.taskId)) {
    query['TaskId'] = request.taskId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeJobErrorCode',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the error information of a Database Backup (DBS) task.
 *
 * @param request DescribeJobErrorCodeRequest
 * @return DescribeJobErrorCodeResponse
 */
async function describeJobErrorCode(request: DescribeJobErrorCodeRequest): DescribeJobErrorCodeResponse {
  var runtime = new $RuntimeOptions{};
  return describeJobErrorCodeWithOptions(request, runtime);
}

model DescribeNodeCidrListRequest {
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request.', example='ETnLKlblzczshOTUbOCzxxxxxxx'),
  ownerId?: string(name='OwnerId'),
  region?: string(name='Region', description='The region in which DBS is activated. Valid values:

*   **cn-hangzhou**: China (Hangzhou)
*   **cn-shanghai**: China (Shanghai)
*   **cn-qingdao**: China (Qingdao)
*   **cn-beijing**: China (Beijing)
*   **cn-shenzhen**: China (Shenzhen)
*   **cn-hongkong**: China (Hong Kong)
*   **ap-southeast-1**: Singapore (Singapore)
*   **cn-hangzhou-finance**: China East 1 Finance
*   **cn-shanghai-finance**: China East 2 Finance
*   **cn-shenzhen-finance**: China South 1 Finance

This parameter is required.', example='cn-hangzhou'),
}

model DescribeNodeCidrListResponseBody = {
  errCode?: string(name='ErrCode', description='The error code.', example='Param.NotFound'),
  errMessage?: string(name='ErrMessage', description='The error message.', example='The specified parameter %s value is not valid.'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  internetIPs?: {
    internetIP?: [ string ](name='InternetIP')
  }(name='InternetIPs', description='The public CIDR blocks.'),
  intranetIPs?: {
    intranetIP?: [ string ](name='IntranetIP')
  }(name='IntranetIPs', description='The internal CIDR blocks.'),
  requestId?: string(name='RequestId', description='The ID of the request.', example='5B352E69-E7B1-4EA7-BB8E-29FFE969C791'),
  success?: boolean(name='Success', description='Indicates whether the request is successful.', example='true'),
}

model DescribeNodeCidrListResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeNodeCidrListResponseBody(name='body'),
}

/**
 * @summary Queries the CIDR blocks of nodes on which Database Backup (DBS) is running.
 *
 * @param request DescribeNodeCidrListRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeNodeCidrListResponse
 */
@context("describeNodeCidrListWithContext")
async function describeNodeCidrListWithOptions(request: DescribeNodeCidrListRequest, runtime: $RuntimeOptions): DescribeNodeCidrListResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  if (!$isNull(request.region)) {
    query['Region'] = request.region;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeNodeCidrList',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the CIDR blocks of nodes on which Database Backup (DBS) is running.
 *
 * @param request DescribeNodeCidrListRequest
 * @return DescribeNodeCidrListResponse
 */
async function describeNodeCidrList(request: DescribeNodeCidrListRequest): DescribeNodeCidrListResponse {
  var runtime = new $RuntimeOptions{};
  return describeNodeCidrListWithOptions(request, runtime);
}

model DescribePreCheckProgressListRequest {
  backupPlanId?: string(name='BackupPlanId', description='The backup schedule ID.

>  You must specify one of BackupPlanId and RestoreTaskId.', example='dbstooi01XXXX'),
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request.', example='ETnLKlblzczshOTUbOCzxxxxxxx'),
  ownerId?: string(name='OwnerId'),
  restoreTaskId?: string(name='RestoreTaskId', description='The restoration task ID.', example='dbasdsaXXXX'),
}

model DescribePreCheckProgressListResponseBody = {
  errCode?: string(name='ErrCode', description='The error code.', example='Param.NotFound'),
  errMessage?: string(name='ErrMessage', description='The error message.', example='The specified parameter %s value is not valid.'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  items?: {
    preCheckProgressDetail?: [ 
    {
      bootTime?: long(name='BootTime', description='The time when the check for the item started. This value is a UNIX timestamp representing the number of milliseconds that have elapsed since January 1, 1970, 00:00:00 UTC.', example='1583734969000'),
      errMsg?: string(name='ErrMsg', description='The error message.', example='java.lang.RuntimeException: Could not find any schema ......'),
      finishTime?: long(name='FinishTime', description='The time when the check for the item was complete. This value is a UNIX timestamp representing the number of milliseconds that have elapsed since January 1, 1970, 00:00:00 UTC.', example='1583734969000'),
      item?: string(name='Item', description='The name of the check item.', example='CHECK_OTHER'),
      jobId?: string(name='JobId', description='The ID of the job for the check item.', example='uvk8f6fxnq5s'),
      names?: string(name='Names', description='The name of the group to which the check item belongs.', example='CHECK_OTHER'),
      orderNum?: string(name='OrderNum', description='The sequence number of the check item.', example='10'),
      state?: string(name='State', description='The state of the check for the item. Valid values:

*   **init**: The check for the item is being initialized.
*   **warning**: A warning is reported.
*   **catched**: An exception occurs.
*   **running**: The check for the item is in progress.
*   **failed**: The check for the item fails.
*   **finish**: The check for the item is completed.', example='warning'),
    }
  ](name='PreCheckProgressDetail')
  }(name='Items', description='The details of check items.'),
  progress?: int32(name='Progress', description='The precheck progress. Valid values: 0 to 100.', example='100'),
  requestId?: string(name='RequestId', description='The ID of the request.', example='C4A45FE1-A903-470D-B113-F12A4DF942AB'),
  status?: string(name='Status', description='The status of the precheck. Valid values:

*   **running**: The precheck is in progress.
*   **failed**: The precheck failed.
*   **finish**: The precheck is complete.', example='failed'),
  success?: boolean(name='Success', description='Indicates whether the request was successful. Valid values:

*   **true**
*   **false**', example='true'),
}

model DescribePreCheckProgressListResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribePreCheckProgressListResponseBody(name='body'),
}

/**
 * @summary Queries the precheck progress of a backup schedule or a restore task.
 *
 * @param request DescribePreCheckProgressListRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribePreCheckProgressListResponse
 */
@context("describePreCheckProgressListWithContext")
async function describePreCheckProgressListWithOptions(request: DescribePreCheckProgressListRequest, runtime: $RuntimeOptions): DescribePreCheckProgressListResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.backupPlanId)) {
    query['BackupPlanId'] = request.backupPlanId;
  }
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  if (!$isNull(request.restoreTaskId)) {
    query['RestoreTaskId'] = request.restoreTaskId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribePreCheckProgressList',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the precheck progress of a backup schedule or a restore task.
 *
 * @param request DescribePreCheckProgressListRequest
 * @return DescribePreCheckProgressListResponse
 */
async function describePreCheckProgressList(request: DescribePreCheckProgressListRequest): DescribePreCheckProgressListResponse {
  var runtime = new $RuntimeOptions{};
  return describePreCheckProgressListWithOptions(request, runtime);
}

model DescribeRegionsRequest {
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request. You can use the client to generate the value, but you must ensure that the value is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length.', example='ETnLKlblzczshOTUbOCzxxxxxxxxxx'),
  ownerId?: string(name='OwnerId'),
}

model DescribeRegionsResponseBody = {
  errCode?: string(name='ErrCode', description='The error code.', example='InvalidParameter'),
  errMessage?: string(name='ErrMessage', description='The error message returned if the request failed.', example='This backupPlan can\\\\"t support this action'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code returned.', example='400'),
  regions?: {
    regionCode?: [ string ](name='RegionCode')
  }(name='Regions', description='The regions that DBS supports.'),
  requestId?: string(name='RequestId', description='The request ID.', example='EB4DFD5E-3618-498D-BE35-4DBEA0072122'),
  success?: boolean(name='Success', description='The status of the request.', example='true'),
}

model DescribeRegionsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeRegionsResponseBody(name='body'),
}

/**
 * @summary Queries the regions that Database Backup (DBS) supports.
 *
 * @param request DescribeRegionsRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeRegionsResponse
 */
@context("describeRegionsWithContext")
async function describeRegionsWithOptions(request: DescribeRegionsRequest, runtime: $RuntimeOptions): DescribeRegionsResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeRegions',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the regions that Database Backup (DBS) supports.
 *
 * @param request DescribeRegionsRequest
 * @return DescribeRegionsResponse
 */
async function describeRegions(request: DescribeRegionsRequest): DescribeRegionsResponse {
  var runtime = new $RuntimeOptions{};
  return describeRegionsWithOptions(request, runtime);
}

model DescribeRestoreRangeInfoRequest {
  backupPlanId?: string(name='BackupPlanId', description='The ID of the backup schedule. You can call the [DescribeBackupPlanList](https://help.aliyun.com/document_detail/2869825.html) operation to obtain it.

This parameter is required.', example='dbssl67c7mx****'),
  beginTimestampForRestore?: long(name='BeginTimestampForRestore', description='The earliest point in time to which you can restore data. You can call the [DescribeBackupPlanList](https://help.aliyun.com/document_detail/2869825.html) operation to obtain the value of the parameter for each backup schedule.

This parameter is required.', example='1646674092000'),
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request.', example='ETnLKlblzczshOTUbOCzxxxxxxx'),
  endTimestampForRestore?: long(name='EndTimestampForRestore', description='The latest point in time to which you can restore data. You can call the [DescribeBackupPlanList](https://help.aliyun.com/document_detail/2869825.html) operation to obtain the value of the parameter for each backup schedule.

This parameter is required.', example='1646846814000'),
  ownerId?: string(name='OwnerId'),
  recentlyRestore?: boolean(name='RecentlyRestore', description='Specifies whether to query the most recent point in time to which you can restore data.', example='true'),
}

model DescribeRestoreRangeInfoResponseBody = {
  errCode?: string(name='ErrCode', description='The error code.', example='Param.NotFound'),
  errMessage?: string(name='ErrMessage', description='The error message.', example='The specified parameter %s value is not valid.'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  items?: {
    DBSRecoverRange?: [ 
    {
      beginTimestampForRestore?: long(name='BeginTimestampForRestore', description='The beginning of the time range to which you can restore data.', example='1646760282000'),
      endTimestampForRestore?: long(name='EndTimestampForRestore', description='The end of the time range to which you can restore data.', example='1646760308000'),
      fullBackupList?: {
        fullBackupDetail?: [ 
        {
          backupSetId?: string(name='BackupSetId', description='The ID of the backup set.', example='qecnsxkd****'),
          endTime?: long(name='EndTime', description='The end time of the full backup task. Example: 1646760308000.', example='1646760308000'),
          startTime?: long(name='StartTime', description='The start time of the full backup task. Example: 1646760282000.', example='1646760282000'),
        }
      ](name='FullBackupDetail')
      }(name='FullBackupList', description='If the value of the RangeType parameter is point, this parameter is returned. The value of this parameter describes information about all backup points in the time range.'),
      rangeType?: string(name='RangeType', description='The type of the time range to which you can restore data.

*   **point**: The time range contains discrete points in time at which full backups were performed.
*   **range**: The time range is a period of time for which continuous backup is performed. You can specify a random point in time in the time range to restore data.', example='point'),
      sourceEndpointInstanceID?: string(name='SourceEndpointInstanceID', description='The ID of the database instance.', example='rm-bp106x9tk2c91****'),
      sourceEndpointInstanceType?: string(name='SourceEndpointInstanceType', description='The location of the database.', example='rds'),
    }
  ](name='DBSRecoverRange')
  }(name='Items', description='The information about the time ranges to which you can restore data.'),
  requestId?: string(name='RequestId', description='The ID of the request.', example='E2BD9DFC-6760-5F49-97C5-DA739E29****'),
  success?: boolean(name='Success', description='Indicates whether the request is successful. Valid values:

*   **true**: The request is successful.
*   **false**: The request fails.', example='true'),
}

model DescribeRestoreRangeInfoResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeRestoreRangeInfoResponseBody(name='body'),
}

/**
 * @summary Queries the range of time to which you can restore data in a backup schedule.
 *
 * @param request DescribeRestoreRangeInfoRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeRestoreRangeInfoResponse
 */
@context("describeRestoreRangeInfoWithContext")
async function describeRestoreRangeInfoWithOptions(request: DescribeRestoreRangeInfoRequest, runtime: $RuntimeOptions): DescribeRestoreRangeInfoResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.backupPlanId)) {
    query['BackupPlanId'] = request.backupPlanId;
  }
  if (!$isNull(request.beginTimestampForRestore)) {
    query['BeginTimestampForRestore'] = request.beginTimestampForRestore;
  }
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.endTimestampForRestore)) {
    query['EndTimestampForRestore'] = request.endTimestampForRestore;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  if (!$isNull(request.recentlyRestore)) {
    query['RecentlyRestore'] = request.recentlyRestore;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeRestoreRangeInfo',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the range of time to which you can restore data in a backup schedule.
 *
 * @param request DescribeRestoreRangeInfoRequest
 * @return DescribeRestoreRangeInfoResponse
 */
async function describeRestoreRangeInfo(request: DescribeRestoreRangeInfoRequest): DescribeRestoreRangeInfoResponse {
  var runtime = new $RuntimeOptions{};
  return describeRestoreRangeInfoWithOptions(request, runtime);
}

model DescribeRestoreTaskListRequest {
  backupPlanId?: string(name='BackupPlanId', description='The ID of the backup schedule.', example='dbs1hvb0wwwXXXXX'),
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request.', example='ETnLKlblzczshOTUbOCzxxxxxxx'),
  endTimestamp?: long(name='EndTimestamp', description='The end of the time range to query.', example='1570701361528'),
  ownerId?: string(name='OwnerId'),
  pageNum?: int32(name='PageNum', description='The number of the page to return. The value must be a positive integer. Default value: 0.', example='1'),
  pageSize?: int32(name='PageSize', description='The number of entries to return on each page. Valid values:

*   30
*   50
*   100

> Default value: 30.', example='30'),
  restoreTaskId?: string(name='RestoreTaskId', description='The restoration task ID. Separate multiple IDs with commas (,). You can call the [CreateRestoreTask](https://help.aliyun.com/document_detail/2869836.html) operation to obtain the ID.

>  Configure the BackupPlanId or RestoreTaskId parameter. If you configure the two parameters, an error is returned.', example='s102h7rf5anq'),
  startTimestamp?: long(name='StartTimestamp', description='The beginning of the time range to query.', example='1570701361528'),
}

model DescribeRestoreTaskListResponseBody = {
  errCode?: string(name='ErrCode', description='The error code.', example='Param.NotFound'),
  errMessage?: string(name='ErrMessage', description='The error message.', example='The specified parameter %s value is not valid.'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  items?: {
    restoreTaskDetail?: [ 
    {
      backupGatewayId?: long(name='BackupGatewayId', description='The ID of the backup gateway.', example='324234332'),
      backupPlanId?: string(name='BackupPlanId', description='The ID of the backup schedule.', example='dbs1hvb0wXXXX'),
      backupSetId?: string(name='BackupSetId', description='The ID of the full backup set that is used in the restore task.', example='1ibh2f5uXXX'),
      continuousRestoreProgress?: int32(name='ContinuousRestoreProgress', description='The restore progress of the incremental log files.', example='100'),
      crossAliyunId?: string(name='CrossAliyunId', description='The unique ID (UID) of the Alibaba Cloud account to which the backup schedule belongs.', example='2xxx7778xxxxxxxxxx'),
      crossRoleName?: string(name='CrossRoleName', description='The name of the RAM role that can be used to perform backups across Alibaba Cloud accounts.', example='test123'),
      destinationEndpointDatabaseName?: string(name='DestinationEndpointDatabaseName', description='The name of the database.', example='test'),
      destinationEndpointInstanceID?: string(name='DestinationEndpointInstanceID', description='The ID of the database instance.', example='rm-bp1p8c2947XXX'),
      destinationEndpointInstanceType?: string(name='DestinationEndpointInstanceType', description='The location of the database. Valid values:

*   RDS
*   ECS
*   Express: The database is connected to DBS by using Express Connect, VPN Gateway, or Smart Access Gateway.
*   Agent: The database is connected to DBS over a DBS backup gateway.
*   DDS: The database is an ApsaraDB for MongoDB database.
*   Other: The database is connected to DBS by using the IP address and port of the database.', example='RDS'),
      destinationEndpointIpPort?: string(name='DestinationEndpointIpPort', description='The endpoint that is used to connect to the database.', example='100.X.X.10:33204'),
      destinationEndpointOracleSID?: string(name='DestinationEndpointOracleSID', description='The SID of the Oracle database.', example='test'),
      destinationEndpointRegion?: string(name='DestinationEndpointRegion', description='The region in which the database is deployed.', example='cn-hangzhou'),
      destinationEndpointUserName?: string(name='DestinationEndpointUserName', description='The username of the account that is used to connect to the database.', example='test'),
      errMessage?: string(name='ErrMessage', description='The error message.', example='NULL'),
      fullDataRestoreProgress?: int32(name='FullDataRestoreProgress', description='The restore progress of the full backup data.', example='100'),
      fullStruAfterRestoreProgress?: int32(name='FullStruAfterRestoreProgress', description='The progress of schema restore after full data restore.', example='100'),
      fullStruforeRestoreProgress?: int32(name='FullStruforeRestoreProgress', description='The progress of schema restore before full data restore.', example='0'),
      restoreDir?: string(name='RestoreDir', description='The directory of the destination database to which the objects were restored.', example='100.X.X.10:33204'),
      restoreObjects?: string(name='RestoreObjects', description='The objects to be restored.', example='[     {         \\\\"DBName\\\\":\\\\"test\\\\"       } ]'),
      restoreStatus?: string(name='RestoreStatus', description='The status of the restore task. Valid values:

*   init: The restore task is not started or does not pass the precheck.
*   running: The restore task is running.
*   stop: The restore task failed.
*   pause: The restore task is stopped.
*   check_pass: The restore task passed the precheck.', example='running'),
      restoreTaskCreateTime?: long(name='RestoreTaskCreateTime', description='The time when the restore task was created, such as 1554560477000.', example='1554560477000'),
      restoreTaskFinishTime?: long(name='RestoreTaskFinishTime', description='The time when the restore task was complete, such as 1554560477000.', example='1554560477000'),
      restoreTaskId?: string(name='RestoreTaskId', description='The ID of the restore task.', example='s1XXXXXX'),
      restoreTaskName?: string(name='RestoreTaskName', description='The name of the restore task.', example='test'),
      restoreTime?: long(name='RestoreTime', description='The time to run the restore task, such as 1554560477000.', example='1554560477000'),
    }
  ](name='RestoreTaskDetail')
  }(name='Items', description='The details of the backup schedule.'),
  pageNum?: int32(name='PageNum', description='The page number of the returned page.', example='1'),
  pageSize?: int32(name='PageSize', description='The number of entries returned on each page.', example='30'),
  requestId?: string(name='RequestId', description='The ID of the request.', example='9C397502-B4F2-4E22-AD97-C81F0049F3F3'),
  success?: boolean(name='Success', description='Indicates whether the request is successful.', example='true'),
  totalElements?: int32(name='TotalElements', description='The total number of restore tasks.', example='1'),
  totalPages?: int32(name='TotalPages', description='The total number of returned pages.', example='1'),
}

model DescribeRestoreTaskListResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeRestoreTaskListResponseBody(name='body'),
}

/**
 * @summary Queries restore tasks.
 *
 * @param request DescribeRestoreTaskListRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeRestoreTaskListResponse
 */
@context("describeRestoreTaskListWithContext")
async function describeRestoreTaskListWithOptions(request: DescribeRestoreTaskListRequest, runtime: $RuntimeOptions): DescribeRestoreTaskListResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.backupPlanId)) {
    query['BackupPlanId'] = request.backupPlanId;
  }
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.endTimestamp)) {
    query['EndTimestamp'] = request.endTimestamp;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  if (!$isNull(request.pageNum)) {
    query['PageNum'] = request.pageNum;
  }
  if (!$isNull(request.pageSize)) {
    query['PageSize'] = request.pageSize;
  }
  if (!$isNull(request.restoreTaskId)) {
    query['RestoreTaskId'] = request.restoreTaskId;
  }
  if (!$isNull(request.startTimestamp)) {
    query['StartTimestamp'] = request.startTimestamp;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeRestoreTaskList',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries restore tasks.
 *
 * @param request DescribeRestoreTaskListRequest
 * @return DescribeRestoreTaskListResponse
 */
async function describeRestoreTaskList(request: DescribeRestoreTaskListRequest): DescribeRestoreTaskListResponse {
  var runtime = new $RuntimeOptions{};
  return describeRestoreTaskListWithOptions(request, runtime);
}

model DisableBackupLogRequest {
  backupPlanId?: string(name='BackupPlanId', description='The backup schedule ID. You can call the [DescribeBackupPlanList](https://help.aliyun.com/document_detail/2869825.html) operation to query the ID of the backup schedule.

This parameter is required.', example='dbstooi01xxxx'),
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request.', example='dbs'),
  ownerId?: string(name='OwnerId'),
}

model DisableBackupLogResponseBody = {
  backupPlanId?: string(name='BackupPlanId', description='The backup schedule ID.', example='dbstooi01xxxx'),
  errCode?: string(name='ErrCode', description='The error code.', example='Param.NotFound'),
  errMessage?: string(name='ErrMessage', description='The error message.', example='findValidDBSJob error'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  needPrecheck?: boolean(name='NeedPrecheck', description='Indicates whether a precheck is triggered. Valid values:

*   true: A precheck is triggered. You must call the [StartBackupPlan](https://help.aliyun.com/document_detail/2869816.html) operation to start the backup schedule.
*   false: No precheck is triggered.', example='false'),
  requestId?: string(name='RequestId', description='The ID of the request.', example='D6E068C3-25BC-455A-85FE-45F0B22ECB1F'),
  success?: boolean(name='Success', description='Indicates whether the request is successful.', example='true'),
}

model DisableBackupLogResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DisableBackupLogResponseBody(name='body'),
}

/**
 * @summary Disables incremental backup for a backup schedule.
 *
 * @description ### Impact
 * After you disable the incremental log backup feature, your backup schedule no longer performs incremental log backups.
 *
 * @param request DisableBackupLogRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DisableBackupLogResponse
 */
@context("disableBackupLogWithContext")
async function disableBackupLogWithOptions(request: DisableBackupLogRequest, runtime: $RuntimeOptions): DisableBackupLogResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.backupPlanId)) {
    query['BackupPlanId'] = request.backupPlanId;
  }
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DisableBackupLog',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Disables incremental backup for a backup schedule.
 *
 * @description ### Impact
 * After you disable the incremental log backup feature, your backup schedule no longer performs incremental log backups.
 *
 * @param request DisableBackupLogRequest
 * @return DisableBackupLogResponse
 */
async function disableBackupLog(request: DisableBackupLogRequest): DisableBackupLogResponse {
  var runtime = new $RuntimeOptions{};
  return disableBackupLogWithOptions(request, runtime);
}

model EnableBackupLogRequest {
  backupPlanId?: string(name='BackupPlanId', description='The backup schedule ID. You can call the [DescribeBackupPlanList](https://help.aliyun.com/document_detail/2869825.html) operation to obtain the ID.

This parameter is required.', example='dbstooi01xxxx'),
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request.', example='dbs'),
  ownerId?: string(name='OwnerId'),
}

model EnableBackupLogResponseBody = {
  backupPlanId?: string(name='BackupPlanId', description='The backup schedule ID.', example='dbstooi01xxxx'),
  errCode?: string(name='ErrCode', description='The error code.', example='Param.NotFound'),
  errMessage?: string(name='ErrMessage', description='The error message.', example='findValidDBSJob error'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  needPrecheck?: boolean(name='NeedPrecheck', description='Indicates whether a precheck is triggered. Valid values:

*   true: A precheck is triggered. You must call the [StartBackupPlan](https://help.aliyun.com/document_detail/2869816.html) operation to start the backup schedule.
*   false: No precheck is triggered.', example='false'),
  requestId?: string(name='RequestId', description='The ID of the request.', example='D6E068C3-25BC-455A-85FE-45F0B22ECB1F'),
  success?: boolean(name='Success', description='Indicates whether the request is successful. Valid values:

*   true: The request is successful.
*   false: The request fails.', example='true'),
}

model EnableBackupLogResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: EnableBackupLogResponseBody(name='body'),
}

/**
 * @summary Enables incremental backup for a backup schedule.
 *
 * @description ## Impact
 * It is free to enable the incremental log backup feature. However, the backup traffic and storage capacity generated by the feature are billed in the same way as the full backup feature, and can be offset by the free quota of backup schedules or storage plans.
 *
 * @param request EnableBackupLogRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return EnableBackupLogResponse
 */
@context("enableBackupLogWithContext")
async function enableBackupLogWithOptions(request: EnableBackupLogRequest, runtime: $RuntimeOptions): EnableBackupLogResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.backupPlanId)) {
    query['BackupPlanId'] = request.backupPlanId;
  }
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'EnableBackupLog',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Enables incremental backup for a backup schedule.
 *
 * @description ## Impact
 * It is free to enable the incremental log backup feature. However, the backup traffic and storage capacity generated by the feature are billed in the same way as the full backup feature, and can be offset by the free quota of backup schedules or storage plans.
 *
 * @param request EnableBackupLogRequest
 * @return EnableBackupLogResponse
 */
async function enableBackupLog(request: EnableBackupLogRequest): EnableBackupLogResponse {
  var runtime = new $RuntimeOptions{};
  return enableBackupLogWithOptions(request, runtime);
}

model GetDBListFromAgentRequest {
  backupGatewayId?: long(name='BackupGatewayId', description='The ID of the backup gateway.', example='160813'),
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request. You can use the client to generate the value, but you must make sure that it is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length.', example='ETnLKlblzczshOTUbOCzxxxxxxxxxx'),
  ownerId?: string(name='OwnerId'),
  sourceEndpointRegion?: string(name='SourceEndpointRegion', description='The region in which the backup gateway resides.', example='cn-hangzhou'),
  taskId?: long(name='TaskId', description='The ID of the asynchronous task.', example='123456'),
}

model GetDBListFromAgentResponseBody = {
  dbList?: {
    dbName?: [ string ](name='dbName')
  }(name='DbList', description='The details of the databases.'),
  errCode?: string(name='ErrCode', description='The error code.', example='InvalidParameter'),
  errMessage?: string(name='ErrMessage', description='The error message.', example='This backupPlan can\\\\"t support this action'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='400'),
  requestId?: string(name='RequestId', description='The ID of the request.', example='EB4DFD5E-3618-498D-BE35-4DBEA0072122'),
  success?: boolean(name='Success', description='Indicates whether the request is successful.', example='true'),
}

model GetDBListFromAgentResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetDBListFromAgentResponseBody(name='body'),
}

/**
 * @summary Queries the result of a task that is used to query a database list by using a backup gateway based on the ID of the task.
 *
 * @param request GetDBListFromAgentRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetDBListFromAgentResponse
 */
@context("getDBListFromAgentWithContext")
async function getDBListFromAgentWithOptions(request: GetDBListFromAgentRequest, runtime: $RuntimeOptions): GetDBListFromAgentResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.backupGatewayId)) {
    query['BackupGatewayId'] = request.backupGatewayId;
  }
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  if (!$isNull(request.sourceEndpointRegion)) {
    query['SourceEndpointRegion'] = request.sourceEndpointRegion;
  }
  if (!$isNull(request.taskId)) {
    query['TaskId'] = request.taskId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetDBListFromAgent',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the result of a task that is used to query a database list by using a backup gateway based on the ID of the task.
 *
 * @param request GetDBListFromAgentRequest
 * @return GetDBListFromAgentResponse
 */
async function getDBListFromAgent(request: GetDBListFromAgentRequest): GetDBListFromAgentResponse {
  var runtime = new $RuntimeOptions{};
  return getDBListFromAgentWithOptions(request, runtime);
}

model InitializeDbsServiceLinkedRoleResponseBody = {
  data?: string(name='Data', description='The value is null.', example='null'),
  errMessage?: string(name='ErrMessage', description='The error message returned if the request failed.', example='EntityAlreadyExists.Role : The role already exists:AliyunServiceRoleForDBS\\\\r\\\\nRequestId : 73******-3B4D-501A-9505-FA8B9******'),
  errorCode?: string(name='ErrorCode', description='The error code returned.', example='EntityAlreadyExists.Role'),
  requestId?: string(name='RequestId', description='The request ID.', example='4F1888AC-1138-4995-B9FE-D2734F61C058'),
  success?: string(name='Success', description='Indicates whether the request was successful. Valid values:

*   **true**
*   **false**', example='true'),
}

model InitializeDbsServiceLinkedRoleResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: InitializeDbsServiceLinkedRoleResponseBody(name='body'),
}

/**
 * @summary Grants the AliyunServiceRoleForDBS role to Database Backup (DBS).
 *
 * @param request InitializeDbsServiceLinkedRoleRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return InitializeDbsServiceLinkedRoleResponse
 */
async function initializeDbsServiceLinkedRoleWithOptions(runtime: $RuntimeOptions): InitializeDbsServiceLinkedRoleResponse {
  var req = new OpenApiUtil.OpenApiRequest{};
  var params = new OpenApiUtil.Params{
    action = 'InitializeDbsServiceLinkedRole',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Grants the AliyunServiceRoleForDBS role to Database Backup (DBS).
 *
 * @return InitializeDbsServiceLinkedRoleResponse
 */
async function initializeDbsServiceLinkedRole(): InitializeDbsServiceLinkedRoleResponse {
  var runtime = new $RuntimeOptions{};
  return initializeDbsServiceLinkedRoleWithOptions(runtime);
}

model ModifyBackupObjectsRequest {
  backupObjects?: string(name='BackupObjects', description='The backup objects that are defined in a JSON string in the following format:

    [
        {
            "DBName":"The name of the database that you want to back up",
            "SchemaName":"The name of the schema that you want to back up",
            "TableIncludes":[{
            	"TableName":"The name of the table that you want to back up"
            }],
            "TableExcludes":[{
                "TableName":"The name of the table that you want to exclude during the backup"
            }]
        }
    ]

*   If you specify only `DBName` and do not specify objects of lower levels, all objects in the database are backed up.

*   If you specify `DBName` and some objects of lower levels, only the specified objects are backed up by default. You can use the following regular expressions to define object names:

    *   A period `.` matches any single character except `\\\\r\\\\n`.
    *   An asterisk `*` matches zero or more occurrences of a preceding subexpression. For example, `h.*llo` matches strings such as `hllo` and `heeeello`.
    *   A question mark `?` matches zero or one occurrence of a preceding subexpression. For example, `h.?llo` matches strings such as `hllo` and `hello`, but not `haello`.
    *   Character set `[Characters]` matches a character included in the brackets ([ ]). For example, `h[ae]llo` matches `hallo` and `hello`.
    *   Negative character set `[^Characters]` does not match a character in the brackets ([ ]). For example, `h[^ae]llo` matches `hcllo` and `hdllo`, but not `hallo` or `hello`.
    *   Character range `[character1-character2]` matches any character included in the range from `character1 to character2`, such as `[0-9]` and `[a-z]`.

>  `SchemaName` and `NewSchemaName` apply only to SQL Server databases. Use `DBName` and `NewDBName` to specify the names of other databases.

This parameter is required.', example='[     {         "DBName":"17xxx92xxxx374",         "SchemaName":"gy9xnj8xxxxxx",         "TableIncludes":[{         	"TableName":"test1"         }],         "TableExcludes":[{             "TableName":"test2"         }]     } ]'),
  backupPlanId?: string(name='BackupPlanId', description='The ID of the backup schedule.

This parameter is required.', example='dbs1h****usfa'),
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request.', example='ETnLKlblzczshOTUbOCzxxxxxxx'),
  ownerId?: string(name='OwnerId'),
}

model ModifyBackupObjectsResponseBody = {
  backupPlanId?: string(name='BackupPlanId', description='The ID of the backup schedule.', example='dbs1h****usfa'),
  errCode?: string(name='ErrCode', description='The error code returned if the request failed.', example='Param.NotFound'),
  errMessage?: string(name='ErrMessage', description='The error message.', example='The specified parameter %s value is not valid.'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  needPrecheck?: boolean(name='NeedPrecheck', description='Indicates whether a precheck is triggered. If true is returned, you must call the [StartBackupPlan](https://help.aliyun.com/document_detail/2869816.html) operation to start the backup schedule.', example='true'),
  requestId?: string(name='RequestId', description='The ID of the request.', example='D6E068C3-25BC-455A-85FE-45F0B22ECB1F'),
  success?: boolean(name='Success', description='Indicates whether the request was successful. Valid values:

*   **true**
*   **false**', example='true'),
}

model ModifyBackupObjectsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ModifyBackupObjectsResponseBody(name='body'),
}

/**
 * @summary Modifies backup objects of a backup schedule in Database Backup (DBS).
 *
 * @param request ModifyBackupObjectsRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ModifyBackupObjectsResponse
 */
@context("modifyBackupObjectsWithContext")
async function modifyBackupObjectsWithOptions(request: ModifyBackupObjectsRequest, runtime: $RuntimeOptions): ModifyBackupObjectsResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.backupObjects)) {
    query['BackupObjects'] = request.backupObjects;
  }
  if (!$isNull(request.backupPlanId)) {
    query['BackupPlanId'] = request.backupPlanId;
  }
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ModifyBackupObjects',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Modifies backup objects of a backup schedule in Database Backup (DBS).
 *
 * @param request ModifyBackupObjectsRequest
 * @return ModifyBackupObjectsResponse
 */
async function modifyBackupObjects(request: ModifyBackupObjectsRequest): ModifyBackupObjectsResponse {
  var runtime = new $RuntimeOptions{};
  return modifyBackupObjectsWithOptions(request, runtime);
}

model ModifyBackupPlanNameRequest {
  backupPlanId?: string(name='BackupPlanId', description='The ID of the backup schedule.

This parameter is required.', example='dbstooi0XXXX'),
  backupPlanName?: string(name='BackupPlanName', description='The name of the backup schedule.

This parameter is required.', example='dbstooi0XXXX'),
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request.', example='ETnLKlblzczshOTUbOCzxxxxxxx'),
  ownerId?: string(name='OwnerId'),
}

model ModifyBackupPlanNameResponseBody = {
  backupPlanId?: string(name='BackupPlanId', description='The ID of the backup schedule.', example='dbstooi0XXXX'),
  errCode?: string(name='ErrCode', description='The error code.', example='Param.NotFound'),
  errMessage?: string(name='ErrMessage', description='The error message.', example='The specified parameter %s value is not valid.'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='The ID of the request.', example='D6E068C3-25BC-455A-85FE-45F0B22ECB1F'),
  success?: boolean(name='Success', description='Indicates whether the request is successful.', example='true'),
}

model ModifyBackupPlanNameResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ModifyBackupPlanNameResponseBody(name='body'),
}

/**
 * @summary Changes the name of a backup schedule.
 *
 * @param request ModifyBackupPlanNameRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ModifyBackupPlanNameResponse
 */
@context("modifyBackupPlanNameWithContext")
async function modifyBackupPlanNameWithOptions(request: ModifyBackupPlanNameRequest, runtime: $RuntimeOptions): ModifyBackupPlanNameResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.backupPlanId)) {
    query['BackupPlanId'] = request.backupPlanId;
  }
  if (!$isNull(request.backupPlanName)) {
    query['BackupPlanName'] = request.backupPlanName;
  }
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ModifyBackupPlanName',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Changes the name of a backup schedule.
 *
 * @param request ModifyBackupPlanNameRequest
 * @return ModifyBackupPlanNameResponse
 */
async function modifyBackupPlanName(request: ModifyBackupPlanNameRequest): ModifyBackupPlanNameResponse {
  var runtime = new $RuntimeOptions{};
  return modifyBackupPlanNameWithOptions(request, runtime);
}

model ModifyBackupSetDownloadRulesRequest {
  backupGatewayId?: long(name='BackupGatewayId', description='The ID of the backup gateway that is used to download the backup set.', example='2331****'),
  backupPlanId?: string(name='BackupPlanId', description='The ID of the backup schedule.

This parameter is required.', example='dbstooi01****'),
  backupSetDownloadDir?: string(name='BackupSetDownloadDir', description='The server directory to which the backup set is downloaded.', example='test'),
  backupSetDownloadTargetType?: string(name='BackupSetDownloadTargetType', description='The type of the destination server to which the backup set is downloaded.

> Set the value to agent, which indicates a server on which a backup gateway is installed.', example='agent'),
  backupSetDownloadTargetTypeLocation?: string(name='BackupSetDownloadTargetTypeLocation', description='The type of the destination directory to which the backup set is downloaded. This parameter is required if the automatic download feature is enabled. Valid values:

*   local
*   nas
*   ftp
*   minio

> Default value: local.', example='local'),
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request.', example='ETnLKlblzczshOTUbOCzx****'),
  fullDataFormat?: string(name='FullDataFormat', description='The format in which the full backup set is downloaded. Valid values:

*   Native
*   SQL
*   CSV
*   JSON

> Default value: CSV.', example='CSV'),
  incrementDataFormat?: string(name='IncrementDataFormat', description='The format in which the incremental backup set is downloaded. Valid values:

*   Native
*   SQL
*   CSV
*   JSON

> Default value: Native.', example='Native'),
  openAutoDownload?: boolean(name='OpenAutoDownload', description='Specifies whether to enable the automatic download feature. Default value: false.', example='false'),
  ownerId?: string(name='OwnerId'),
}

model ModifyBackupSetDownloadRulesResponseBody = {
  backupPlanId?: string(name='BackupPlanId', description='The ID of the backup schedule.', example='dbstooi01e****'),
  errCode?: string(name='ErrCode', description='The error code.', example='Param.NotFound'),
  errMessage?: string(name='ErrMessage', description='The error message.', example='The specified parameter %s value is not valid.'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='The ID of the request.', example='D6E068C3-25BC-455A-85FE-45F0B22ECB1F'),
  success?: boolean(name='Success', description='Indicates whether the request is successful.', example='true'),
}

model ModifyBackupSetDownloadRulesResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ModifyBackupSetDownloadRulesResponseBody(name='body'),
}

/**
 * @summary Enables, configures, or disables the automatic download feature.
 *
 * @param request ModifyBackupSetDownloadRulesRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ModifyBackupSetDownloadRulesResponse
 */
@context("modifyBackupSetDownloadRulesWithContext")
async function modifyBackupSetDownloadRulesWithOptions(request: ModifyBackupSetDownloadRulesRequest, runtime: $RuntimeOptions): ModifyBackupSetDownloadRulesResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.backupGatewayId)) {
    query['BackupGatewayId'] = request.backupGatewayId;
  }
  if (!$isNull(request.backupPlanId)) {
    query['BackupPlanId'] = request.backupPlanId;
  }
  if (!$isNull(request.backupSetDownloadDir)) {
    query['BackupSetDownloadDir'] = request.backupSetDownloadDir;
  }
  if (!$isNull(request.backupSetDownloadTargetType)) {
    query['BackupSetDownloadTargetType'] = request.backupSetDownloadTargetType;
  }
  if (!$isNull(request.backupSetDownloadTargetTypeLocation)) {
    query['BackupSetDownloadTargetTypeLocation'] = request.backupSetDownloadTargetTypeLocation;
  }
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.fullDataFormat)) {
    query['FullDataFormat'] = request.fullDataFormat;
  }
  if (!$isNull(request.incrementDataFormat)) {
    query['IncrementDataFormat'] = request.incrementDataFormat;
  }
  if (!$isNull(request.openAutoDownload)) {
    query['OpenAutoDownload'] = request.openAutoDownload;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ModifyBackupSetDownloadRules',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Enables, configures, or disables the automatic download feature.
 *
 * @param request ModifyBackupSetDownloadRulesRequest
 * @return ModifyBackupSetDownloadRulesResponse
 */
async function modifyBackupSetDownloadRules(request: ModifyBackupSetDownloadRulesRequest): ModifyBackupSetDownloadRulesResponse {
  var runtime = new $RuntimeOptions{};
  return modifyBackupSetDownloadRulesWithOptions(request, runtime);
}

model ModifyBackupSourceEndpointRequest {
  backupGatewayId?: long(name='BackupGatewayId', description='The backup gateway ID. You can call the [DescribeBackupPlanList](https://help.aliyun.com/document_detail/2869825.html) operation to obtain the ID.

>  If you set **SourceEndpointInstanceType** to Agent, this parameter is required.', example='21321323213'),
  backupObjects?: string(name='BackupObjects', description='The backup object. If you set SourceEndpointInstanceType to Agent, this parameter is optional. Otherwise, it is required. You can call the [DescribeBackupPlanList](https://help.aliyun.com/document_detail/2869825.html) operation to obtain the backup object.', example='[{ "DBName":"Name of the database to be backed up", "SchemaName":"Name of the schema to be backed up", "TableIncludes":[{ "TableName":"Name of the table to be backed up" }], "TableExcludes":[{"TableName":"Name of the table to be excluded during the backup" }] } ]'),
  backupPlanId?: string(name='BackupPlanId', description='The ID of the backup schedule. You can call the [DescribeBackupPlanList](https://help.aliyun.com/document_detail/2869825.html) operation to obtain it.

This parameter is required.', example='dbs1h****usfa'),
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request.', example='ETnLKlblzczshOTUbOCzxxxxxxx'),
  crossAliyunId?: string(name='CrossAliyunId', description='The unique ID (UID) of the Alibaba Cloud account to which the backup schedule belongs. You can call the [DescribeRestoreTaskList](https://help.aliyun.com/document_detail/2869838.html) operation to obtain the UID.', example='2xxx7778xxxxxxxxxx'),
  crossRoleName?: string(name='CrossRoleName', description='The name of the RAM role that is used to perform backups across Alibaba Cloud accounts. You can call the [DescribeRestoreTaskList](https://help.aliyun.com/document_detail/2869838.html) operation to obtain the RAM role.', example='test123'),
  ownerId?: string(name='OwnerId'),
  sourceEndpointDatabaseName?: string(name='SourceEndpointDatabaseName', description='The name of the database.

*   This parameter is required if the database is a PostgreSQL or MongoDB database.
*   This parameter is required if the database is an SQL Server database that is connected to DBS over a DBS backup gateway.', example='test'),
  sourceEndpointIP?: string(name='SourceEndpointIP', description='The endpoint of the database. You can call the [DescribeBackupPlanList](https://help.aliyun.com/document_detail/2869825.html) operation to obtain the endpoint.

>  If you set **SourceEndpointInstanceType** to Express, Agent, or Other, this parameter is required.', example='100.*.*.10:3306'),
  sourceEndpointInstanceID?: string(name='SourceEndpointInstanceID', description='The database instance ID. You can call the [DescribeBackupPlanList](https://help.aliyun.com/document_detail/2869825.html) operation to obtain the ID.

>  If you set **SourceEndpointInstanceType** to RDS, ECS, DDS, or Express, this parameter is required.', example='rm-bp1p8c29479jv****'),
  sourceEndpointInstanceType?: string(name='SourceEndpointInstanceType', description='The location of the database. You can call the [DescribeBackupPlanList](https://help.aliyun.com/document_detail/2869825.html) operation to obtain it. Valid values:

*   **RDS**
*   **ECS**
*   **Express**: The database is connected to DBS via Express Connect, VPN Gateway, or Smart Access Gateway.
*   **Agent**: The database is connected to DBS over a DBS backup gateway.
*   **DDS**: The database is an ApsaraDB for MongoDB database.
*   **Other**: The database is connected to DBS using the IP address and port of the database.

This parameter is required.', example='RDS'),
  sourceEndpointOracleSID?: string(name='SourceEndpointOracleSID', description='The SID of the Oracle source database. If the database is an Oracle database, this parameter is required.', example='test'),
  sourceEndpointPassword?: string(name='SourceEndpointPassword', description='The password of the account that is used to connect to the database.

This parameter is required except that the database is an SQL Server database that is connected to DBS over a DBS backup gateway or a Redis database.', example='test'),
  sourceEndpointPort?: int32(name='SourceEndpointPort', description='The port that is used to connect to the database. You can call the [DescribeBackupPlanList](https://help.aliyun.com/document_detail/2869825.html) operation to obtain the port.

>  If you set **SourceEndpointInstanceType** to Express, Agent, Other, or ECS, this parameter is required.', example='3306'),
  sourceEndpointRegion?: string(name='SourceEndpointRegion', description='The region in which the database you want to back up resides. You can call the [DescribeBackupPlanList](https://help.aliyun.com/document_detail/2869825.html) operation to obtain the region.

>  If you set **SourceEndpointInstanceType** to RDS, ECS, DDS, Express, or Agent, this parameter is required.', example='cn-hangzhou'),
  sourceEndpointUserName?: string(name='SourceEndpointUserName', description='The account that is used to log on to the database. You can call the [DescribeBackupPlanList](https://help.aliyun.com/document_detail/2869825.html) operation to obtain the account.

If the database is an SQL Server database connected to DBS over a DBS backup gateway or a Redis database, this parameter is optional. Otherwise, it is required.', example='test'),
}

model ModifyBackupSourceEndpointResponseBody = {
  backupPlanId?: string(name='BackupPlanId', description='The ID of the backup schedule.', example='dbs1h****usfa'),
  errCode?: string(name='ErrCode', description='The error code.', example='Param.NotFound'),
  errMessage?: string(name='ErrMessage', description='The error message.', example='The specified parameter %s value is not valid.'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  needPrecheck?: boolean(name='NeedPrecheck', description='Indicates whether a precheck is triggered. If the value of this parameter is true, you must start the backup schedule by calling the StartBackupPlan operation.', example='false'),
  requestId?: string(name='RequestId', description='The ID of the request.', example='D6E068C3-25BC-455A-85FE-45F0B22E****'),
  success?: boolean(name='Success', description='Indicates whether the request is successful. Valid values:

*   **true**: The request is successful.
*   **false**: The request fails.', example='true'),
}

model ModifyBackupSourceEndpointResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ModifyBackupSourceEndpointResponseBody(name='body'),
}

/**
 * @summary Modifies the data source of a backup schedule.
 *
 * @param request ModifyBackupSourceEndpointRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ModifyBackupSourceEndpointResponse
 */
@context("modifyBackupSourceEndpointWithContext")
async function modifyBackupSourceEndpointWithOptions(request: ModifyBackupSourceEndpointRequest, runtime: $RuntimeOptions): ModifyBackupSourceEndpointResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.backupGatewayId)) {
    query['BackupGatewayId'] = request.backupGatewayId;
  }
  if (!$isNull(request.backupObjects)) {
    query['BackupObjects'] = request.backupObjects;
  }
  if (!$isNull(request.backupPlanId)) {
    query['BackupPlanId'] = request.backupPlanId;
  }
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.crossAliyunId)) {
    query['CrossAliyunId'] = request.crossAliyunId;
  }
  if (!$isNull(request.crossRoleName)) {
    query['CrossRoleName'] = request.crossRoleName;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  if (!$isNull(request.sourceEndpointDatabaseName)) {
    query['SourceEndpointDatabaseName'] = request.sourceEndpointDatabaseName;
  }
  if (!$isNull(request.sourceEndpointIP)) {
    query['SourceEndpointIP'] = request.sourceEndpointIP;
  }
  if (!$isNull(request.sourceEndpointInstanceID)) {
    query['SourceEndpointInstanceID'] = request.sourceEndpointInstanceID;
  }
  if (!$isNull(request.sourceEndpointInstanceType)) {
    query['SourceEndpointInstanceType'] = request.sourceEndpointInstanceType;
  }
  if (!$isNull(request.sourceEndpointOracleSID)) {
    query['SourceEndpointOracleSID'] = request.sourceEndpointOracleSID;
  }
  if (!$isNull(request.sourceEndpointPassword)) {
    query['SourceEndpointPassword'] = request.sourceEndpointPassword;
  }
  if (!$isNull(request.sourceEndpointPort)) {
    query['SourceEndpointPort'] = request.sourceEndpointPort;
  }
  if (!$isNull(request.sourceEndpointRegion)) {
    query['SourceEndpointRegion'] = request.sourceEndpointRegion;
  }
  if (!$isNull(request.sourceEndpointUserName)) {
    query['SourceEndpointUserName'] = request.sourceEndpointUserName;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ModifyBackupSourceEndpoint',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Modifies the data source of a backup schedule.
 *
 * @param request ModifyBackupSourceEndpointRequest
 * @return ModifyBackupSourceEndpointResponse
 */
async function modifyBackupSourceEndpoint(request: ModifyBackupSourceEndpointRequest): ModifyBackupSourceEndpointResponse {
  var runtime = new $RuntimeOptions{};
  return modifyBackupSourceEndpointWithOptions(request, runtime);
}

model ModifyBackupStrategyRequest {
  backupLogIntervalSeconds?: int32(name='BackupLogIntervalSeconds', description='The interval at which you want to perform incremental log backups. Unit: seconds.

> This parameter takes effect only when physical backups are performed.', example='1000'),
  backupPeriod?: string(name='BackupPeriod', description='The day of each week when the full backup task runs. Valid values:

*   Monday
*   Tuesday
*   Wednesday
*   Thursday
*   Friday
*   Saturday
*   Sunday

This parameter is required.', example='Monday'),
  backupPlanId?: string(name='BackupPlanId', description='The ID of the backup schedule.

This parameter is required.', example='dbstooi01XXXX'),
  backupStartTime?: string(name='BackupStartTime', description='The start time of the full backup task. Specify the time in the HH:mm format.', example='14:22'),
  backupStrategyType?: string(name='BackupStrategyType', description='The backup method that you want to use for full backups. Valid values:

*   **simple**: scheduled backup. If you specify this value for the BackupStrategyType parameter, you must also specify the BackupPeriod and BackupStartTime parameters.
*   **Manual**: manual backup.

> Default value: **simple**.', example='simple'),
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request.', example='ETnLKlblzczshOTUbOCzxxxxxxx'),
  ownerId?: string(name='OwnerId'),
}

model ModifyBackupStrategyResponseBody = {
  backupPlanId?: string(name='BackupPlanId', description='The ID of the backup schedule.', example='dbstooi01XXXX'),
  errCode?: string(name='ErrCode', description='The error code.', example='Param.NotFound'),
  errMessage?: string(name='ErrMessage', description='The error message.', example='The specified parameter %s value is not valid.'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  needPrecheck?: boolean(name='NeedPrecheck', description='Indicates whether a precheck is triggered. If the value of this parameter is true, you must start the backup schedule by calling the StartBackupPlan operation.', example='false'),
  requestId?: string(name='RequestId', description='The ID of the request.', example='D6E068C3-25BC-455A-85FE-45F0B22ECB1F'),
  success?: boolean(name='Success', description='Indicates whether the request is successful.', example='true'),
}

model ModifyBackupStrategyResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ModifyBackupStrategyResponseBody(name='body'),
}

/**
 * @summary Modifies the backup time of a backup schedule.
 *
 * @param request ModifyBackupStrategyRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ModifyBackupStrategyResponse
 */
@context("modifyBackupStrategyWithContext")
async function modifyBackupStrategyWithOptions(request: ModifyBackupStrategyRequest, runtime: $RuntimeOptions): ModifyBackupStrategyResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.backupLogIntervalSeconds)) {
    query['BackupLogIntervalSeconds'] = request.backupLogIntervalSeconds;
  }
  if (!$isNull(request.backupPeriod)) {
    query['BackupPeriod'] = request.backupPeriod;
  }
  if (!$isNull(request.backupPlanId)) {
    query['BackupPlanId'] = request.backupPlanId;
  }
  if (!$isNull(request.backupStartTime)) {
    query['BackupStartTime'] = request.backupStartTime;
  }
  if (!$isNull(request.backupStrategyType)) {
    query['BackupStrategyType'] = request.backupStrategyType;
  }
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ModifyBackupStrategy',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Modifies the backup time of a backup schedule.
 *
 * @param request ModifyBackupStrategyRequest
 * @return ModifyBackupStrategyResponse
 */
async function modifyBackupStrategy(request: ModifyBackupStrategyRequest): ModifyBackupStrategyResponse {
  var runtime = new $RuntimeOptions{};
  return modifyBackupStrategyWithOptions(request, runtime);
}

model ModifyStorageStrategyRequest {
  backupPlanId?: string(name='BackupPlanId', description='The ID of the backup schedule. You can call the [DescribeBackupPlanList](https://help.aliyun.com/document_detail/2869825.html) operation to obtain the ID.

This parameter is required.', example='dbsqdss5tmh****'),
  backupRetentionPeriod?: int32(name='BackupRetentionPeriod', description='The number of days for which the backup data is retained. Valid values: 0 to 1825.

> Default value: 730.

This parameter is required.', example='730'),
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request.', example='dbstest'),
  duplicationArchivePeriod?: int32(name='DuplicationArchivePeriod', description='The number of days after which the storage class of the backup data is changed to Archive. The value of this parameter must be smaller than the value of the BackupRetentionPeriod parameter. For more information about the Archive storage class, see [Storage class overview](https://help.aliyun.com/document_detail/51374.html).

> Default value: 365.

This parameter is required.', example='366'),
  duplicationInfrequentAccessPeriod?: int32(name='DuplicationInfrequentAccessPeriod', description='The number of days after which the storage class of the backup data is changed to Infrequent Access (IA). The value of this parameter must be smaller than the value of the DuplicationArchivePeriod parameter. For more information about the IA storage class, see [Storage class overview](https://help.aliyun.com/document_detail/51374.html).

> Default value: 180.

This parameter is required.', example='190'),
  ownerId?: string(name='OwnerId'),
}

model ModifyStorageStrategyResponseBody = {
  backupPlanId?: string(name='BackupPlanId', description='The ID of the backup schedule.', example='dbsqdss5tmh****'),
  errCode?: string(name='ErrCode', description='The error code.', example='Param.NotFound'),
  errMessage?: string(name='ErrMessage', description='The error message.', example='findValidDBSJob error'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  needPrecheck?: boolean(name='NeedPrecheck', description='Indicates whether a precheck is triggered. Valid values:

*   **true**: A precheck is triggered. You must manually call the [StartBackupPlan](https://help.aliyun.com/document_detail/2869818.html) operation to start the backup schedule.
*   **false**: No precheck is triggered.', example='false'),
  requestId?: string(name='RequestId', description='The ID of the request.', example='E995F91F-6F89-503B-9F7D-502F58FD****'),
  success?: boolean(name='Success', description='Indicates whether the request is successful. Valid values:

*   **true**: The request is successful.
*   **false**: The request fails.', example='true'),
}

model ModifyStorageStrategyResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ModifyStorageStrategyResponseBody(name='body'),
}

/**
 * @summary Modifies the lifecycle of data that is backed up based on a backup schedule.
 *
 * @param request ModifyStorageStrategyRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ModifyStorageStrategyResponse
 */
@context("modifyStorageStrategyWithContext")
async function modifyStorageStrategyWithOptions(request: ModifyStorageStrategyRequest, runtime: $RuntimeOptions): ModifyStorageStrategyResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.backupPlanId)) {
    query['BackupPlanId'] = request.backupPlanId;
  }
  if (!$isNull(request.backupRetentionPeriod)) {
    query['BackupRetentionPeriod'] = request.backupRetentionPeriod;
  }
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.duplicationArchivePeriod)) {
    query['DuplicationArchivePeriod'] = request.duplicationArchivePeriod;
  }
  if (!$isNull(request.duplicationInfrequentAccessPeriod)) {
    query['DuplicationInfrequentAccessPeriod'] = request.duplicationInfrequentAccessPeriod;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ModifyStorageStrategy',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Modifies the lifecycle of data that is backed up based on a backup schedule.
 *
 * @param request ModifyStorageStrategyRequest
 * @return ModifyStorageStrategyResponse
 */
async function modifyStorageStrategy(request: ModifyStorageStrategyRequest): ModifyStorageStrategyResponse {
  var runtime = new $RuntimeOptions{};
  return modifyStorageStrategyWithOptions(request, runtime);
}

model ReleaseBackupPlanRequest {
  backupPlanId?: string(name='BackupPlanId', description='The backup schedule ID. You can call the [DescribeBackupPlanList](https://help.aliyun.com/document_detail/2869825.html) operation to query the ID.

This parameter is required.', example='dbstooi01****'),
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request.', example='dbs'),
  ownerId?: string(name='OwnerId'),
}

model ReleaseBackupPlanResponseBody = {
  backupPlanId?: string(name='BackupPlanId', description='The ID of the backup schedule.', example='dbstooi01****'),
  errCode?: string(name='ErrCode', description='The error code returned if the request failed.', example='Param.NotFound'),
  errMessage?: string(name='ErrMessage', description='The error message returned if the request failed.', example='findValidDBSJob error'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code returned.', example='200'),
  requestId?: string(name='RequestId', description='The ID of the request.', example='D6E068C3-25BC-455A-85FE-45F0B22ECB1F'),
  success?: boolean(name='Success', description='Indicates whether the request succeeded. Valid values:

*   true
*   false', example='true'),
}

model ReleaseBackupPlanResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ReleaseBackupPlanResponseBody(name='body'),
}

/**
 * @summary Releases a pay-as-you-go backup schedule.
 *
 * @description ## Impacts
 * After a pay-as-you-go backup schedule is released, it stops providing services. Database Backup (DBS) no longer charges you fees for this backup schedule.
 *
 * @param request ReleaseBackupPlanRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ReleaseBackupPlanResponse
 */
@context("releaseBackupPlanWithContext")
async function releaseBackupPlanWithOptions(request: ReleaseBackupPlanRequest, runtime: $RuntimeOptions): ReleaseBackupPlanResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.backupPlanId)) {
    query['BackupPlanId'] = request.backupPlanId;
  }
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ReleaseBackupPlan',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Releases a pay-as-you-go backup schedule.
 *
 * @description ## Impacts
 * After a pay-as-you-go backup schedule is released, it stops providing services. Database Backup (DBS) no longer charges you fees for this backup schedule.
 *
 * @param request ReleaseBackupPlanRequest
 * @return ReleaseBackupPlanResponse
 */
async function releaseBackupPlan(request: ReleaseBackupPlanRequest): ReleaseBackupPlanResponse {
  var runtime = new $RuntimeOptions{};
  return releaseBackupPlanWithOptions(request, runtime);
}

model RenewBackupPlanRequest {
  backupPlanId?: string(name='BackupPlanId', description='The ID of the backup schedule.

This parameter is required.', example='dbstooi01e****'),
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request.', example='HKAJHFIUEQWBFIJSNFO****'),
  ownerId?: string(name='OwnerId'),
  period?: string(name='Period', description='Specifies whether to use yearly subscription or monthly subscription for the instance. Valid values:

*   Year
*   Month

This parameter is required.', example='Month'),
  usedTime?: int32(name='UsedTime', description='The subscription duration of the instance. Valid values:

*   If the Period parameter is set to Year, the value of the UsedTime parameter ranges from 1 to 9.
*   If the Period parameter is set to Month, the value of the UsedTime parameter ranges from 1 to 11.

This parameter is required.', example='1'),
}

model RenewBackupPlanResponseBody = {
  backupPlanId?: string(name='BackupPlanId', description='The ID of the backup schedule.', example='dbstooi01e****'),
  errCode?: string(name='ErrCode', description='The error code.', example='Param.NotFound'),
  errMessage?: string(name='ErrMessage', description='The error message.', example='The specified parameter %s value is not valid.'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  orderId?: string(name='OrderId', description='The ID of the order.', example='20202****'),
  requestId?: string(name='RequestId', description='The ID of the request.', example='D6E068C3-25BC-455A-85FE-45F0B22ECB1F'),
  success?: boolean(name='Success', description='Indicates whether the request is successful.', example='true'),
}

model RenewBackupPlanResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: RenewBackupPlanResponseBody(name='body'),
}

/**
 * @summary Renews a backup schedule.
 *
 * @param request RenewBackupPlanRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return RenewBackupPlanResponse
 */
@context("renewBackupPlanWithContext")
async function renewBackupPlanWithOptions(request: RenewBackupPlanRequest, runtime: $RuntimeOptions): RenewBackupPlanResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.backupPlanId)) {
    query['BackupPlanId'] = request.backupPlanId;
  }
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  if (!$isNull(request.period)) {
    query['Period'] = request.period;
  }
  if (!$isNull(request.usedTime)) {
    query['UsedTime'] = request.usedTime;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'RenewBackupPlan',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Renews a backup schedule.
 *
 * @param request RenewBackupPlanRequest
 * @return RenewBackupPlanResponse
 */
async function renewBackupPlan(request: RenewBackupPlanRequest): RenewBackupPlanResponse {
  var runtime = new $RuntimeOptions{};
  return renewBackupPlanWithOptions(request, runtime);
}

model StartBackupPlanRequest {
  backupPlanId?: string(name='BackupPlanId', description='The ID of the backup schedule. You can call the [DescribeBackupPlanList](https://help.aliyun.com/document_detail/2869825.html) operation to obtain it.

This parameter is required.', example='dbsqdss5tmh****'),
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request.', example='ETnLKlblzczshOTUbOCzxxxxxxx'),
  ownerId?: string(name='OwnerId'),
}

model StartBackupPlanResponseBody = {
  backupPlanId?: string(name='BackupPlanId', description='The ID of the backup schedule.', example='dbsqdss5tm****'),
  createdFullBackupsetId?: string(name='CreatedFullBackupsetId', description='The ID of the full backup set.', example='1h7toien3****'),
  errCode?: string(name='ErrCode', description='The error code.', example='Param.NotFound'),
  errMessage?: string(name='ErrMessage', description='The error message.', example='The specified parameter %s value is not valid.'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='The ID of the request.', example='D13761C3-9971-5C02-B789-3F3CD159****'),
  success?: boolean(name='Success', description='Indicates whether the request is successful. Valid values:

*   **true**: The request is successful.
*   **false**: The request fails.', example='true'),
}

model StartBackupPlanResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: StartBackupPlanResponseBody(name='body'),
}

/**
 * @summary Starts a backup schedule.
 *
 * @param request StartBackupPlanRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return StartBackupPlanResponse
 */
@context("startBackupPlanWithContext")
async function startBackupPlanWithOptions(request: StartBackupPlanRequest, runtime: $RuntimeOptions): StartBackupPlanResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.backupPlanId)) {
    query['BackupPlanId'] = request.backupPlanId;
  }
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'StartBackupPlan',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Starts a backup schedule.
 *
 * @param request StartBackupPlanRequest
 * @return StartBackupPlanResponse
 */
async function startBackupPlan(request: StartBackupPlanRequest): StartBackupPlanResponse {
  var runtime = new $RuntimeOptions{};
  return startBackupPlanWithOptions(request, runtime);
}

model StartRestoreTaskRequest {
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request.', example='ETnLKlblzczshOTUbOCzxxxxxxx'),
  ownerId?: string(name='OwnerId'),
  restoreTaskId?: string(name='RestoreTaskId', description='The ID of the restore task.

This parameter is required.', example='s102h7rfXXXX'),
}

model StartRestoreTaskResponseBody = {
  errCode?: string(name='ErrCode', description='The error code.', example='Param.NotFound'),
  errMessage?: string(name='ErrMessage', description='The error message.', example='The specified parameter %s value is not valid.'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='The ID of the request.', example='60AF7C5D-EF4D-4D48-8FD5-C0823FDF28AC'),
  restoreTaskId?: string(name='RestoreTaskId', description='The ID of the restore task.', example='s102h7rfXXXX'),
  success?: boolean(name='Success', description='Indicates whether the request is successful.', example='true'),
}

model StartRestoreTaskResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: StartRestoreTaskResponseBody(name='body'),
}

/**
 * @summary Starts a restore task.
 *
 * @param request StartRestoreTaskRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return StartRestoreTaskResponse
 */
@context("startRestoreTaskWithContext")
async function startRestoreTaskWithOptions(request: StartRestoreTaskRequest, runtime: $RuntimeOptions): StartRestoreTaskResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  if (!$isNull(request.restoreTaskId)) {
    query['RestoreTaskId'] = request.restoreTaskId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'StartRestoreTask',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Starts a restore task.
 *
 * @param request StartRestoreTaskRequest
 * @return StartRestoreTaskResponse
 */
async function startRestoreTask(request: StartRestoreTaskRequest): StartRestoreTaskResponse {
  var runtime = new $RuntimeOptions{};
  return startRestoreTaskWithOptions(request, runtime);
}

model StopBackupPlanRequest {
  backupPlanId?: string(name='BackupPlanId', description='The ID of the backup schedule.

This parameter is required.', example='dbstooi01****'),
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request.', example='ETnLKlblzczshOTUbOCz****'),
  ownerId?: string(name='OwnerId'),
  stopMethod?: string(name='StopMethod', description='The method that is used to stop the backup schedule. Valid values:

*   ALL: stops the backup schedule, full data backup tasks, incremental log backup tasks, and restore tasks
*   PLAN: stops only the backup schedule.

This parameter is required.', example='ALL'),
}

model StopBackupPlanResponseBody = {
  backupPlanId?: string(name='BackupPlanId', description='The ID of the backup schedule.', example='dbs1h****usfa'),
  errCode?: string(name='ErrCode', description='The error code.', example='Param.NotFound'),
  errMessage?: string(name='ErrMessage', description='The error message.', example='The specified parameter %s value is not valid.'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='The ID of the request.', example='D6E068C3-25BC-455A-85FE-45F0B22ECB1F'),
  success?: boolean(name='Success', description='Indicates whether the request is successful.', example='true'),
}

model StopBackupPlanResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: StopBackupPlanResponseBody(name='body'),
}

/**
 * @summary Stops a backup schedule.
 *
 * @param request StopBackupPlanRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return StopBackupPlanResponse
 */
@context("stopBackupPlanWithContext")
async function stopBackupPlanWithOptions(request: StopBackupPlanRequest, runtime: $RuntimeOptions): StopBackupPlanResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.backupPlanId)) {
    query['BackupPlanId'] = request.backupPlanId;
  }
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  if (!$isNull(request.stopMethod)) {
    query['StopMethod'] = request.stopMethod;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'StopBackupPlan',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Stops a backup schedule.
 *
 * @param request StopBackupPlanRequest
 * @return StopBackupPlanResponse
 */
async function stopBackupPlan(request: StopBackupPlanRequest): StopBackupPlanResponse {
  var runtime = new $RuntimeOptions{};
  return stopBackupPlanWithOptions(request, runtime);
}

model UpgradeBackupPlanRequest {
  backupPlanId?: string(name='BackupPlanId', description='The ID of the backup schedule.

This parameter is required.', example='dbstooi01e****'),
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request.', example='ETnLKlblzczshOTUbOCz****'),
  instanceClass?: string(name='InstanceClass', description='The specifications of the instance. Valid values:

*   micro
*   small
*   medium
*   large
*   xlarge

This parameter is required.', example='micro'),
  ownerId?: string(name='OwnerId'),
}

model UpgradeBackupPlanResponseBody = {
  backupPlanId?: string(name='BackupPlanId', description='The ID of the backup schedule.', example='dbstooi01****'),
  errCode?: string(name='ErrCode', description='The error code.', example='Param.NotFound'),
  errMessage?: string(name='ErrMessage', description='The error message.', example='The specified parameter %s value is not valid.'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  orderId?: string(name='OrderId', description='The ID of the order.', example='2056157****'),
  requestId?: string(name='RequestId', description='The ID of the request.', example='D6E068C3-25BC-455A-85FE-45F0B22ECB1F'),
  success?: boolean(name='Success', description='Indicates whether the request is successful.', example='true'),
}

model UpgradeBackupPlanResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpgradeBackupPlanResponseBody(name='body'),
}

/**
 * @summary Upgrades a backup schedule.
 *
 * @param request UpgradeBackupPlanRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpgradeBackupPlanResponse
 */
@context("upgradeBackupPlanWithContext")
async function upgradeBackupPlanWithOptions(request: UpgradeBackupPlanRequest, runtime: $RuntimeOptions): UpgradeBackupPlanResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.backupPlanId)) {
    query['BackupPlanId'] = request.backupPlanId;
  }
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.instanceClass)) {
    query['InstanceClass'] = request.instanceClass;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpgradeBackupPlan',
    version = '2019-03-06',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Upgrades a backup schedule.
 *
 * @param request UpgradeBackupPlanRequest
 * @return UpgradeBackupPlanResponse
 */
async function upgradeBackupPlan(request: UpgradeBackupPlanRequest): UpgradeBackupPlanResponse {
  var runtime = new $RuntimeOptions{};
  return upgradeBackupPlanWithOptions(request, runtime);
}

