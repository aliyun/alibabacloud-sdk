/**
 *
 */
import OpenApi;
import OpenApi.OpenApiUtil;

extends OpenApi;


init(config: OpenApiUtil.Config){
  super(config);
  @endpointRule = '';
  
  checkConfig(config);
  @endpoint = getEndpoint('safconsole', @regionId, @endpointRule, @network, @suffix, @endpointMap, @endpoint);
}

function getEndpoint(productId: string, regionId: string, endpointRule: string, network: string, suffix: string, endpointMap: map[string]string, endpoint: string) throws: string{
  if (!$isNull(endpoint)) {
    return endpoint;
  }
  
  if (!$isNull(endpointMap) && !$isNull(endpointMap[regionId])) {
    return endpointMap[regionId];
  }
  return OpenApiUtil.getEndpointRules(productId, regionId, endpointRule, network, suffix);
}

model ApplyBastionAccountRequest {
  mobile?: long(name='Mobile', description='Mobile Phone Number', example='***'),
  projectId?: long(name='ProjectId', description='Project ID', example='90912'),
}

model ApplyBastionAccountResponseBody = {
  code?: long(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: boolean(name='ResultObject', description='Result object.', example='true'),
  success?: boolean(name='Success', description='Whether the call was successful.

- **true**: Call succeeded.
- **false**: Call failed.', example='true'),
}

model ApplyBastionAccountResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ApplyBastionAccountResponseBody(name='body'),
}

/**
 * @summary Apply for Bastion Host Account
 *
 * @description ## Request Description
 * - This interface is used for customers to create a modeling project for the first time.
 * - `projectName` is a required field, with a maximum length of 50 characters.
 * - `remark` and `instanceSpec` are optional, where `remark` has a maximum length of 200 characters.
 * - The available values for `instanceSpec` include `SECURE_ENV_LITE` and `SECURE_ENV_PRO`.
 *
 * @param request ApplyBastionAccountRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ApplyBastionAccountResponse
 */
@context("applyBastionAccountWithContext")
async function applyBastionAccountWithOptions(request: ApplyBastionAccountRequest, runtime: $RuntimeOptions): ApplyBastionAccountResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.mobile)) {
    query['Mobile'] = request.mobile;
  }
  if (!$isNull(request.projectId)) {
    query['ProjectId'] = request.projectId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ApplyBastionAccount',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Apply for Bastion Host Account
 *
 * @description ## Request Description
 * - This interface is used for customers to create a modeling project for the first time.
 * - `projectName` is a required field, with a maximum length of 50 characters.
 * - `remark` and `instanceSpec` are optional, where `remark` has a maximum length of 200 characters.
 * - The available values for `instanceSpec` include `SECURE_ENV_LITE` and `SECURE_ENV_PRO`.
 *
 * @param request ApplyBastionAccountRequest
 * @return ApplyBastionAccountResponse
 */
async function applyBastionAccount(request: ApplyBastionAccountRequest): ApplyBastionAccountResponse {
  var runtime = new $RuntimeOptions{};
  return applyBastionAccountWithOptions(request, runtime);
}

model AssociatePocTaskRequest {
  projectId?: long(name='ProjectId', description='Project ID', example='01'),
  taskId?: long(name='TaskId', description='Retrospective task ID.', example='01'),
}

model AssociatePocTaskResponseBody = {
  code?: long(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: boolean(name='ResultObject', description='Return result.', example='true'),
  success?: boolean(name='Success', description='Whether the call was successful.

- **true**: Call succeeded.
- **false**: Call failed.', example='true'),
}

model AssociatePocTaskResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: AssociatePocTaskResponseBody(name='body'),
}

/**
 * @summary Associate Retrospective Task
 *
 * @description ## 请求说明
 * - 本接口用于客户首次创建建模项目。
 * - `projectName` 是必填项，长度不超过50个字符。
 * - `remark` 和 `instanceSpec` 为可选项，其中 `remark` 长度不超过200个字符。
 * - `instanceSpec` 可选值包括 `SECURE_ENV_LITE` 和 `SECURE_ENV_PRO`。
 *
 * @param request AssociatePocTaskRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return AssociatePocTaskResponse
 */
@context("associatePocTaskWithContext")
async function associatePocTaskWithOptions(request: AssociatePocTaskRequest, runtime: $RuntimeOptions): AssociatePocTaskResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.projectId)) {
    query['ProjectId'] = request.projectId;
  }
  if (!$isNull(request.taskId)) {
    query['TaskId'] = request.taskId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'AssociatePocTask',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Associate Retrospective Task
 *
 * @description ## 请求说明
 * - 本接口用于客户首次创建建模项目。
 * - `projectName` 是必填项，长度不超过50个字符。
 * - `remark` 和 `instanceSpec` 为可选项，其中 `remark` 长度不超过200个字符。
 * - `instanceSpec` 可选值包括 `SECURE_ENV_LITE` 和 `SECURE_ENV_PRO`。
 *
 * @param request AssociatePocTaskRequest
 * @return AssociatePocTaskResponse
 */
async function associatePocTask(request: AssociatePocTaskRequest): AssociatePocTaskResponse {
  var runtime = new $RuntimeOptions{};
  return associatePocTaskWithOptions(request, runtime);
}

model CompleteModelingProjectRequest {
  projectId?: long(name='ProjectId', description='Project ID

This parameter is required.', example='01'),
}

model CompleteModelingProjectResponseBody = {
  code?: long(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='055f1546-f465-4c92-a2da-bfb86abe6f56'),
  resultObject?: string(name='ResultObject', description='Return result, indicating whether the creation was successful:
- true: Success
- false: Failure', example='true'),
  success?: boolean(name='Success', description='Indicates whether the call was successful.

- **true**: Call succeeded.
- **false**: Call failed.', example='true'),
}

model CompleteModelingProjectResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CompleteModelingProjectResponseBody(name='body'),
}

/**
 * @summary Complete project and release resources.
 *
 * @description ## 请求说明
 * - 本接口用于客户首次创建建模项目。
 * - `projectName` 是必填项，长度不超过50个字符。
 * - `remark` 和 `instanceSpec` 为可选项，其中 `remark` 长度不超过200个字符。
 * - `instanceSpec` 可选值包括 `SECURE_ENV_LITE` 和 `SECURE_ENV_PRO`。
 *
 * @param request CompleteModelingProjectRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CompleteModelingProjectResponse
 */
@context("completeModelingProjectWithContext")
async function completeModelingProjectWithOptions(request: CompleteModelingProjectRequest, runtime: $RuntimeOptions): CompleteModelingProjectResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.projectId)) {
    query['ProjectId'] = request.projectId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'CompleteModelingProject',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Complete project and release resources.
 *
 * @description ## 请求说明
 * - 本接口用于客户首次创建建模项目。
 * - `projectName` 是必填项，长度不超过50个字符。
 * - `remark` 和 `instanceSpec` 为可选项，其中 `remark` 长度不超过200个字符。
 * - `instanceSpec` 可选值包括 `SECURE_ENV_LITE` 和 `SECURE_ENV_PRO`。
 *
 * @param request CompleteModelingProjectRequest
 * @return CompleteModelingProjectResponse
 */
async function completeModelingProject(request: CompleteModelingProjectRequest): CompleteModelingProjectResponse {
  var runtime = new $RuntimeOptions{};
  return completeModelingProjectWithOptions(request, runtime);
}

model CreateCustomerModuleBasicInfoRequest {
  customerModuleName?: string(name='CustomerModuleName', description='The display identifier of the model for customers.', example='module01'),
  description?: string(name='Description', description='Model description.', example='模型描述'),
  moduleType?: string(name='ModuleType', description='Model deployment type.', example='PMML'),
  storePath?: string(name='StorePath', description='Model storage path.', example='customer/xxxxxxxxx/xxxxxxxx.pmml'),
}

model CreateCustomerModuleBasicInfoResponseBody = {
  code?: long(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: string(name='ResultObject', description='Result object.', example='1'),
  success?: boolean(name='Success', description='Indicates whether the call was successful.

- **true**: Call succeeded.
- **false**: Call failed.', example='true'),
}

model CreateCustomerModuleBasicInfoResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateCustomerModuleBasicInfoResponseBody(name='body'),
}

/**
 * @summary Create basic model information
 *
 * @param request CreateCustomerModuleBasicInfoRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateCustomerModuleBasicInfoResponse
 */
@context("createCustomerModuleBasicInfoWithContext")
async function createCustomerModuleBasicInfoWithOptions(request: CreateCustomerModuleBasicInfoRequest, runtime: $RuntimeOptions): CreateCustomerModuleBasicInfoResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.customerModuleName)) {
    query['CustomerModuleName'] = request.customerModuleName;
  }
  if (!$isNull(request.description)) {
    query['Description'] = request.description;
  }
  if (!$isNull(request.moduleType)) {
    query['ModuleType'] = request.moduleType;
  }
  if (!$isNull(request.storePath)) {
    query['StorePath'] = request.storePath;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateCustomerModuleBasicInfo',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Create basic model information
 *
 * @param request CreateCustomerModuleBasicInfoRequest
 * @return CreateCustomerModuleBasicInfoResponse
 */
async function createCustomerModuleBasicInfo(request: CreateCustomerModuleBasicInfoRequest): CreateCustomerModuleBasicInfoResponse {
  var runtime = new $RuntimeOptions{};
  return createCustomerModuleBasicInfoWithOptions(request, runtime);
}

model CreateCustomerModuleMetaInfoRequest {
  customerModuleId?: int32(name='CustomerModuleId', description='Customer model ID.', example='1'),
  featureString?: string(name='FeatureString', description='A string representation of List<Object>, where Object has the following structure:
{
"mappingName": "xxx"
}', example='{\\\\"mappingName\\\\":\\\\"f1\\\\",\\\\"fieldName\\\\":\\\\"saf_shanghai_new.f1\\\\",\\\\"valueType\\\\":\\\\"DOUBLE\\\\",\\\\"defaultValue\\\\":\\\\"\\\\"}'),
  featureTemplate?: string(name='FeatureTemplate', description='Version of the feature adopted.', example='FINANCE_51'),
}

model CreateCustomerModuleMetaInfoResponseBody = {
  code?: long(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  httpStatusCode?: long(name='HttpStatusCode', description='HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: boolean(name='ResultObject', description='The result indicating if the creation was successful:
- true: Success
- false: Failure', example='true'),
  success?: boolean(name='Success', description='Indicates whether the request was successful, with values as follows:
- true, request succeeded
- false, request failed', example='true'),
}

model CreateCustomerModuleMetaInfoResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateCustomerModuleMetaInfoResponseBody(name='body'),
}

/**
 * @summary Save Model Feature Configuration
 *
 * @description ## Request Description
 * - This interface is used to query all available feature templates in the system.
 * - The request method is GET, and no additional parameters are required.
 * - The returned result includes multiple feature template options, each option including a label (label) and value (value).
 *
 * @param request CreateCustomerModuleMetaInfoRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateCustomerModuleMetaInfoResponse
 */
@context("createCustomerModuleMetaInfoWithContext")
async function createCustomerModuleMetaInfoWithOptions(request: CreateCustomerModuleMetaInfoRequest, runtime: $RuntimeOptions): CreateCustomerModuleMetaInfoResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.customerModuleId)) {
    query['CustomerModuleId'] = request.customerModuleId;
  }
  if (!$isNull(request.featureString)) {
    query['FeatureString'] = request.featureString;
  }
  if (!$isNull(request.featureTemplate)) {
    query['FeatureTemplate'] = request.featureTemplate;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateCustomerModuleMetaInfo',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Save Model Feature Configuration
 *
 * @description ## Request Description
 * - This interface is used to query all available feature templates in the system.
 * - The request method is GET, and no additional parameters are required.
 * - The returned result includes multiple feature template options, each option including a label (label) and value (value).
 *
 * @param request CreateCustomerModuleMetaInfoRequest
 * @return CreateCustomerModuleMetaInfoResponse
 */
async function createCustomerModuleMetaInfo(request: CreateCustomerModuleMetaInfoRequest): CreateCustomerModuleMetaInfoResponse {
  var runtime = new $RuntimeOptions{};
  return createCustomerModuleMetaInfoWithOptions(request, runtime);
}

model CreateCustomerModuleOutputInfoRequest {
  customerModuleId?: int32(name='CustomerModuleId', description='Customer model ID', example='1'),
  finalScoreFormat?: string(name='FinalScoreFormat', description='Number of decimal places to retain.', example='2'),
  processExpression?: string(name='ProcessExpression', description='Score processing logic.', example='score'),
  testFileUrl?: string(name='TestFileUrl', description='Test file URL.', example='customer/xxxxxxxxx/xxxxxxxx.pmml'),
}

model CreateCustomerModuleOutputInfoResponseBody = {
  code?: long(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  httpStatusCode?: long(name='HttpStatusCode', description='HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: boolean(name='ResultObject', description='Result object.', example='true'),
  success?: boolean(name='Success', description='Indicates whether the call was successful.
- **true**: Call succeeded.
- **false**: Call failed.', example='true'),
}

model CreateCustomerModuleOutputInfoResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateCustomerModuleOutputInfoResponseBody(name='body'),
}

/**
 * @summary Create Model Output Information
 *
 * @param request CreateCustomerModuleOutputInfoRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateCustomerModuleOutputInfoResponse
 */
@context("createCustomerModuleOutputInfoWithContext")
async function createCustomerModuleOutputInfoWithOptions(request: CreateCustomerModuleOutputInfoRequest, runtime: $RuntimeOptions): CreateCustomerModuleOutputInfoResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.customerModuleId)) {
    query['CustomerModuleId'] = request.customerModuleId;
  }
  if (!$isNull(request.finalScoreFormat)) {
    query['FinalScoreFormat'] = request.finalScoreFormat;
  }
  if (!$isNull(request.processExpression)) {
    query['ProcessExpression'] = request.processExpression;
  }
  if (!$isNull(request.testFileUrl)) {
    query['TestFileUrl'] = request.testFileUrl;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateCustomerModuleOutputInfo',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Create Model Output Information
 *
 * @param request CreateCustomerModuleOutputInfoRequest
 * @return CreateCustomerModuleOutputInfoResponse
 */
async function createCustomerModuleOutputInfo(request: CreateCustomerModuleOutputInfoRequest): CreateCustomerModuleOutputInfoResponse {
  var runtime = new $RuntimeOptions{};
  return createCustomerModuleOutputInfoWithOptions(request, runtime);
}

model CreateModelingProjectRequest {
  instanceSpec?: string(name='InstanceSpec', description='Instance specification.

This parameter is required.', example='SECURE_ENV_LITE'),
  projectName?: string(name='ProjectName', description='Project name

This parameter is required.', example='xxx'),
  remark?: string(name='Remark', description='Remark.', example='remark'),
}

model CreateModelingProjectResponseBody = {
  code?: long(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: {
    projectId?: string(name='ProjectId', description='Project ID', example='01'),
  }(name='ResultObject', description='Return result.'),
  success?: boolean(name='Success', description='Whether the call was successful.

- **true**: Call succeeded.
- **false**: Call failed.', example='true'),
}

model CreateModelingProjectResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateModelingProjectResponseBody(name='body'),
}

/**
 * @summary Initialize a modeling project.
 *
 * @description ## Request Description
 * - This interface is used for customers to create a modeling project for the first time.
 * - `projectName` is a required field, with a maximum length of 50 characters.
 * - `remark` and `instanceSpec` are optional, where `remark` has a maximum length of 200 characters.
 * - The available values for `instanceSpec` include `SECURE_ENV_LITE` and `SECURE_ENV_PRO`.
 *
 * @param request CreateModelingProjectRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateModelingProjectResponse
 */
@context("createModelingProjectWithContext")
async function createModelingProjectWithOptions(request: CreateModelingProjectRequest, runtime: $RuntimeOptions): CreateModelingProjectResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.instanceSpec)) {
    query['InstanceSpec'] = request.instanceSpec;
  }
  if (!$isNull(request.projectName)) {
    query['ProjectName'] = request.projectName;
  }
  if (!$isNull(request.remark)) {
    query['Remark'] = request.remark;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateModelingProject',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Initialize a modeling project.
 *
 * @description ## Request Description
 * - This interface is used for customers to create a modeling project for the first time.
 * - `projectName` is a required field, with a maximum length of 50 characters.
 * - `remark` and `instanceSpec` are optional, where `remark` has a maximum length of 200 characters.
 * - The available values for `instanceSpec` include `SECURE_ENV_LITE` and `SECURE_ENV_PRO`.
 *
 * @param request CreateModelingProjectRequest
 * @return CreateModelingProjectResponse
 */
async function createModelingProject(request: CreateModelingProjectRequest): CreateModelingProjectResponse {
  var runtime = new $RuntimeOptions{};
  return createModelingProjectWithOptions(request, runtime);
}

model DeleteModelRequest {
  customerModuleId?: int32(name='CustomerModuleId', description='Customer model ID', example='1'),
}

model DeleteModelResponseBody = {
  code?: long(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  httpStatusCode?: long(name='HttpStatusCode', description='HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: boolean(name='ResultObject', description='Result of the operation, indicating whether it was successful:
- true: Success
- false: Failure', example='true'),
  success?: boolean(name='Success', description='Indicates whether the call was successful.

- **true**: Call succeeded.
- **false**: Call failed.', example='true'),
}

model DeleteModelResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DeleteModelResponseBody(name='body'),
}

/**
 * @summary Deletes the specified customer model based on the provided customer model ID.
 *
 * @description ## Request Description
 * This API is used to delete a specified customer model from the system. When calling, you must provide the `customerModuleId` parameter, which identifies the specific model to be deleted.
 * - **Note**: Deletion is irreversible, please use with caution.
 *
 * @param request DeleteModelRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteModelResponse
 */
@context("deleteModelWithContext")
async function deleteModelWithOptions(request: DeleteModelRequest, runtime: $RuntimeOptions): DeleteModelResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.customerModuleId)) {
    query['CustomerModuleId'] = request.customerModuleId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DeleteModel',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Deletes the specified customer model based on the provided customer model ID.
 *
 * @description ## Request Description
 * This API is used to delete a specified customer model from the system. When calling, you must provide the `customerModuleId` parameter, which identifies the specific model to be deleted.
 * - **Note**: Deletion is irreversible, please use with caution.
 *
 * @param request DeleteModelRequest
 * @return DeleteModelResponse
 */
async function deleteModel(request: DeleteModelRequest): DeleteModelResponse {
  var runtime = new $RuntimeOptions{};
  return deleteModelWithOptions(request, runtime);
}

model DeployModelFileRequest {
  groupId?: long(name='GroupId', description='File Group ID.

This parameter is required.', example='01'),
  projectId?: long(name='ProjectId', description='Project ID.

This parameter is required.', example='01'),
}

model DeployModelFileResponseBody = {
  code?: long(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: long(name='ResultObject', description='Return result.', example='true'),
  success?: boolean(name='Success', description='Indicates whether the call was successful.

- **true**: Call succeeded.
- **false**: Call failed.', example='true'),
}

model DeployModelFileResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DeployModelFileResponseBody(name='body'),
}

/**
 * @summary Deploy Model File
 *
 * @description ## 请求说明
 * - 本接口用于客户首次创建建模项目。
 * - `projectName` 是必填项，长度不超过50个字符。
 * - `remark` 和 `instanceSpec` 为可选项，其中 `remark` 长度不超过200个字符。
 * - `instanceSpec` 可选值包括 `SECURE_ENV_LITE` 和 `SECURE_ENV_PRO`。
 *
 * @param request DeployModelFileRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeployModelFileResponse
 */
@context("deployModelFileWithContext")
async function deployModelFileWithOptions(request: DeployModelFileRequest, runtime: $RuntimeOptions): DeployModelFileResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.groupId)) {
    query['GroupId'] = request.groupId;
  }
  if (!$isNull(request.projectId)) {
    query['ProjectId'] = request.projectId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DeployModelFile',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Deploy Model File
 *
 * @description ## 请求说明
 * - 本接口用于客户首次创建建模项目。
 * - `projectName` 是必填项，长度不超过50个字符。
 * - `remark` 和 `instanceSpec` 为可选项，其中 `remark` 长度不超过200个字符。
 * - `instanceSpec` 可选值包括 `SECURE_ENV_LITE` 和 `SECURE_ENV_PRO`。
 *
 * @param request DeployModelFileRequest
 * @return DeployModelFileResponse
 */
async function deployModelFile(request: DeployModelFileRequest): DeployModelFileResponse {
  var runtime = new $RuntimeOptions{};
  return deployModelFileWithOptions(request, runtime);
}

model DescribeCustomerModuleBasicInfoRequest {
  authType?: string(name='AuthType', description='Authorization type.', example='READ'),
  customerModuleId?: int32(name='CustomerModuleId', description='Customer model ID', example='1'),
}

model DescribeCustomerModuleBasicInfoResponseBody = {
  code?: string(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  httpStatusCode?: long(name='HttpStatusCode', description='HTTP status code.', example='200'),
  message?: string(name='Message', description='Return message.', example='success'),
  requestId?: string(name='RequestId', description='Request ID', example='055f1546-f465-4c92-a2da-bfb86abe6f56'),
  resultObject?: {
    customerModuleName?: string(name='CustomerModuleName', description='Customer model name.', example='ModuleName'),
    description?: string(name='Description', description='Remarks.', example='备注。'),
    innerModuleName?: string(name='InnerModuleName', description='Model identifier.', example='ModuleName'),
    moduleType?: string(name='ModuleType', description='Module type.', example='PMML'),
    storePath?: string(name='StorePath', description='Test address.', example='model.pmml'),
  }(name='ResultObject', description='Returned result.'),
  success?: boolean(name='Success', description='Indicates whether the application configuration information was successfully retrieved. Possible values: true: Success; false: Failure.', example='true'),
}

model DescribeCustomerModuleBasicInfoResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeCustomerModuleBasicInfoResponseBody(name='body'),
}

/**
 * @summary Query basic model information based on the customer model ID
 *
 * @description ## Request Description
 * By providing the `customerModuleId` parameter, you can query the current status of a specified customer model. The status values may include but are not limited to "EDIT", "ONLINE", etc.
 *
 * @param request DescribeCustomerModuleBasicInfoRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeCustomerModuleBasicInfoResponse
 */
@context("describeCustomerModuleBasicInfoWithContext")
async function describeCustomerModuleBasicInfoWithOptions(request: DescribeCustomerModuleBasicInfoRequest, runtime: $RuntimeOptions): DescribeCustomerModuleBasicInfoResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.authType)) {
    query['AuthType'] = request.authType;
  }
  if (!$isNull(request.customerModuleId)) {
    query['CustomerModuleId'] = request.customerModuleId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeCustomerModuleBasicInfo',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Query basic model information based on the customer model ID
 *
 * @description ## Request Description
 * By providing the `customerModuleId` parameter, you can query the current status of a specified customer model. The status values may include but are not limited to "EDIT", "ONLINE", etc.
 *
 * @param request DescribeCustomerModuleBasicInfoRequest
 * @return DescribeCustomerModuleBasicInfoResponse
 */
async function describeCustomerModuleBasicInfo(request: DescribeCustomerModuleBasicInfoRequest): DescribeCustomerModuleBasicInfoResponse {
  var runtime = new $RuntimeOptions{};
  return describeCustomerModuleBasicInfoWithOptions(request, runtime);
}

model DescribeCustomerModuleMetaInfoRequest {
  customerModuleId?: int32(name='CustomerModuleId', description='Customer model ID', example='456'),
}

model DescribeCustomerModuleMetaInfoResponseBody = {
  code?: long(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  httpStatusCode?: long(name='HttpStatusCode', description='HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='Request ID', example='055f1546-f465-4c92-a2da-bfb86abe6f56'),
  resultObject?: {
    featureList?: [ 
      {
        defaultValue?: string(name='DefaultValue', description='Default value of the feature.', example='0.1'),
        featureName?: string(name='FeatureName', description='Feature mapping name.', example='f1'),
        featureType?: string(name='FeatureType', description='Feature type.', example='double'),
        name?: string(name='Name', description='Feature name.', example='f1'),
      }
    ](name='FeatureList', description='Feature list.'),
    featureTemplate?: string(name='FeatureTemplate', description='Feature template.', example='FINANCE_60'),
  }(name='ResultObject', description='Return result.'),
  success?: boolean(name='Success', description='Indicates whether the call was successful.

- **true**: Call succeeded.
- **false**: Call failed.', example='true'),
}

model DescribeCustomerModuleMetaInfoResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeCustomerModuleMetaInfoResponseBody(name='body'),
}

/**
 * @summary Query model feature information based on the customer model ID
 *
 * @description ## Request Description
 * By providing the `customerModuleId` parameter, you can query the current status of a specified customer model. The status values may include, but are not limited to, "EDIT", "ONLINE", etc.
 *
 * @param request DescribeCustomerModuleMetaInfoRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeCustomerModuleMetaInfoResponse
 */
@context("describeCustomerModuleMetaInfoWithContext")
async function describeCustomerModuleMetaInfoWithOptions(request: DescribeCustomerModuleMetaInfoRequest, runtime: $RuntimeOptions): DescribeCustomerModuleMetaInfoResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.customerModuleId)) {
    query['CustomerModuleId'] = request.customerModuleId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeCustomerModuleMetaInfo',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Query model feature information based on the customer model ID
 *
 * @description ## Request Description
 * By providing the `customerModuleId` parameter, you can query the current status of a specified customer model. The status values may include, but are not limited to, "EDIT", "ONLINE", etc.
 *
 * @param request DescribeCustomerModuleMetaInfoRequest
 * @return DescribeCustomerModuleMetaInfoResponse
 */
async function describeCustomerModuleMetaInfo(request: DescribeCustomerModuleMetaInfoRequest): DescribeCustomerModuleMetaInfoResponse {
  var runtime = new $RuntimeOptions{};
  return describeCustomerModuleMetaInfoWithOptions(request, runtime);
}

model DescribeCustomerModuleOutputInfoRequest {
  authType?: string(name='AuthType', description='Authorization type.', example='READ'),
  customerModuleId?: int32(name='CustomerModuleId', description='Customer model ID', example='456'),
}

model DescribeCustomerModuleOutputInfoResponseBody = {
  code?: long(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  httpStatusCode?: long(name='HttpStatusCode', description='HTTP status code.', example='200'),
  message?: string(name='Message', description='Return message.', example='success'),
  requestId?: string(name='RequestId', description='Request ID', example='055f1546-f465-4c92-a2da-bfb86abe6f56'),
  resultObject?: {
    finalScoreFormat?: string(name='FinalScoreFormat', description='Number of decimal places for the output.', example='2'),
    processExpression?: string(name='ProcessExpression', description='Score processing logic.', example='score'),
    testFile?: string(name='TestFile', description='Test file.', example='customer/xxxxxxxxx/xxxxxxxx.pmml'),
  }(name='ResultObject', description='Returned result.'),
  success?: boolean(name='Success', description='Indicates whether the request was successful, with possible values as follows:
- true, indicating the request was successful
- false, indicating the request failed', example='true'),
}

model DescribeCustomerModuleOutputInfoResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeCustomerModuleOutputInfoResponseBody(name='body'),
}

/**
 * @summary Query model output information based on the customer model ID
 *
 * @description ## Request Description
 * By providing the `customerModuleId` parameter, you can query the current status of a specified customer model. The status values may include but are not limited to "EDIT", "ONLINE", etc.
 *
 * @param request DescribeCustomerModuleOutputInfoRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeCustomerModuleOutputInfoResponse
 */
@context("describeCustomerModuleOutputInfoWithContext")
async function describeCustomerModuleOutputInfoWithOptions(request: DescribeCustomerModuleOutputInfoRequest, runtime: $RuntimeOptions): DescribeCustomerModuleOutputInfoResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.authType)) {
    query['AuthType'] = request.authType;
  }
  if (!$isNull(request.customerModuleId)) {
    query['CustomerModuleId'] = request.customerModuleId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeCustomerModuleOutputInfo',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Query model output information based on the customer model ID
 *
 * @description ## Request Description
 * By providing the `customerModuleId` parameter, you can query the current status of a specified customer model. The status values may include but are not limited to "EDIT", "ONLINE", etc.
 *
 * @param request DescribeCustomerModuleOutputInfoRequest
 * @return DescribeCustomerModuleOutputInfoResponse
 */
async function describeCustomerModuleOutputInfo(request: DescribeCustomerModuleOutputInfoRequest): DescribeCustomerModuleOutputInfoResponse {
  var runtime = new $RuntimeOptions{};
  return describeCustomerModuleOutputInfoWithOptions(request, runtime);
}

model DescribeFeatureOptionRequest {
  featureTemplate?: string(name='FeatureTemplate', description='Feature template.

This parameter is required.', example='FINANCE_51'),
}

model DescribeFeatureOptionResponseBody = {
  code?: string(name='Code', description='Status code. A return of 200 indicates success.', example='200'),
  httpStatusCode?: long(name='HttpStatusCode', description='HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: [ 
    {
      featureName?: string(name='FeatureName', description='Feature name.', example='xxx'),
    }
  ](name='ResultObject', description='Returned result.'),
  success?: boolean(name='Success', description='Indicates whether the call was successful.
- **true**: Call succeeded.
- **false**: Call failed.', example='true'),
}

model DescribeFeatureOptionResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeFeatureOptionResponseBody(name='body'),
}

/**
 * @summary Return specific feature options based on the feature template
 *
 * @description ## Request Description
 * - This interface is used to query all available feature templates in the system.
 * - The request method is GET, and no additional parameters are required.
 * - The returned result includes multiple feature template options, each of which includes a label (label) and a value (value).
 *
 * @param request DescribeFeatureOptionRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeFeatureOptionResponse
 */
@context("describeFeatureOptionWithContext")
async function describeFeatureOptionWithOptions(request: DescribeFeatureOptionRequest, runtime: $RuntimeOptions): DescribeFeatureOptionResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.featureTemplate)) {
    query['FeatureTemplate'] = request.featureTemplate;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeFeatureOption',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Return specific feature options based on the feature template
 *
 * @description ## Request Description
 * - This interface is used to query all available feature templates in the system.
 * - The request method is GET, and no additional parameters are required.
 * - The returned result includes multiple feature template options, each of which includes a label (label) and a value (value).
 *
 * @param request DescribeFeatureOptionRequest
 * @return DescribeFeatureOptionResponse
 */
async function describeFeatureOption(request: DescribeFeatureOptionRequest): DescribeFeatureOptionResponse {
  var runtime = new $RuntimeOptions{};
  return describeFeatureOptionWithOptions(request, runtime);
}

model DescribeFeatureTemplateListResponseBody = {
  code?: string(name='Code', description='Status code', example='200'),
  httpStatusCode?: long(name='HttpStatusCode', description='HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: [ 
    {
      label?: string(name='Label', description='Parameter label.', example='51维特征'),
      value?: string(name='Value', description='Label value.', example='FINANCE_51'),
    }
  ](name='ResultObject', description='Returned result.'),
  success?: boolean(name='Success', description='Success flag', example='true'),
}

model DescribeFeatureTemplateListResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeFeatureTemplateListResponseBody(name='body'),
}

/**
 * @summary Get the list of available feature templates for model configuration.
 *
 * @description ## Request Description
 * - This interface is used to query all available feature templates in the system.
 * - The request method is GET, and no additional parameters are required.
 * - The returned result includes multiple feature template options, each of which consists of a label (label) and a value (value).
 *
 * @param request DescribeFeatureTemplateListRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeFeatureTemplateListResponse
 */
async function describeFeatureTemplateListWithOptions(runtime: $RuntimeOptions): DescribeFeatureTemplateListResponse {
  var req = new OpenApiUtil.OpenApiRequest{};
  var params = new OpenApiUtil.Params{
    action = 'DescribeFeatureTemplateList',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Get the list of available feature templates for model configuration.
 *
 * @description ## Request Description
 * - This interface is used to query all available feature templates in the system.
 * - The request method is GET, and no additional parameters are required.
 * - The returned result includes multiple feature template options, each of which consists of a label (label) and a value (value).
 *
 * @return DescribeFeatureTemplateListResponse
 */
async function describeFeatureTemplateList(): DescribeFeatureTemplateListResponse {
  var runtime = new $RuntimeOptions{};
  return describeFeatureTemplateListWithOptions(runtime);
}

model DescribeFileDownloadUrlRequest {
  fileId?: long(name='FileId', description='File ID.

This parameter is required.', example='01'),
  projectId?: long(name='ProjectId', description='Project ID.

This parameter is required.', example='01'),
}

model DescribeFileDownloadUrlResponseBody = {
  code?: long(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: string(name='ResultObject', description='Return result.', example='xxx'),
  success?: boolean(name='Success', description='Whether the call was successful.

- **true**: Call succeeded.
- **false**: Call failed.', example='true'),
}

model DescribeFileDownloadUrlResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeFileDownloadUrlResponseBody(name='body'),
}

/**
 * @summary Get File Download Link
 *
 * @description ## 请求说明
 * - 本接口用于客户首次创建建模项目。
 * - `projectName` 是必填项，长度不超过50个字符。
 * - `remark` 和 `instanceSpec` 为可选项，其中 `remark` 长度不超过200个字符。
 * - `instanceSpec` 可选值包括 `SECURE_ENV_LITE` 和 `SECURE_ENV_PRO`。
 *
 * @param request DescribeFileDownloadUrlRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeFileDownloadUrlResponse
 */
@context("describeFileDownloadUrlWithContext")
async function describeFileDownloadUrlWithOptions(request: DescribeFileDownloadUrlRequest, runtime: $RuntimeOptions): DescribeFileDownloadUrlResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.fileId)) {
    query['FileId'] = request.fileId;
  }
  if (!$isNull(request.projectId)) {
    query['ProjectId'] = request.projectId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeFileDownloadUrl',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Get File Download Link
 *
 * @description ## 请求说明
 * - 本接口用于客户首次创建建模项目。
 * - `projectName` 是必填项，长度不超过50个字符。
 * - `remark` 和 `instanceSpec` 为可选项，其中 `remark` 长度不超过200个字符。
 * - `instanceSpec` 可选值包括 `SECURE_ENV_LITE` 和 `SECURE_ENV_PRO`。
 *
 * @param request DescribeFileDownloadUrlRequest
 * @return DescribeFileDownloadUrlResponse
 */
async function describeFileDownloadUrl(request: DescribeFileDownloadUrlRequest): DescribeFileDownloadUrlResponse {
  var runtime = new $RuntimeOptions{};
  return describeFileDownloadUrlWithOptions(request, runtime);
}

model DescribeModelFeatureRequest {
  customerModuleId?: int32(name='CustomerModuleId', description='Customer model ID', example='456'),
  featureTemplate?: string(name='FeatureTemplate', description='Feature template.', example='FINANCE_51'),
}

model DescribeModelFeatureResponseBody = {
  code?: long(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  httpStatusCode?: long(name='HttpStatusCode', description='HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: [ 
    {
      defaultValue?: string(name='DefaultValue', description='Default value of the feature.', example='0.1'),
      featureName?: string(name='FeatureName', description='Feature mapping name.', example='f1'),
      featureType?: string(name='FeatureType', description='Feature type.', example='double'),
      name?: string(name='Name', description='Feature name.', example='f1'),
    }
  ](name='ResultObject', description='Return result.'),
  success?: boolean(name='Success', description='Whether the call was successful.

- **true**: Call succeeded.
- **false**: Call failed.', example='true'),
}

model DescribeModelFeatureResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeModelFeatureResponseBody(name='body'),
}

/**
 * @summary Render the feature configuration of the model
 *
 * @description ## Request Description
 * - This interface is used to query all available feature templates in the system.
 * - The request method is GET, and no additional parameters are required.
 * - The returned result includes multiple feature template options, each including a label (label) and value (value).
 *
 * @param request DescribeModelFeatureRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeModelFeatureResponse
 */
@context("describeModelFeatureWithContext")
async function describeModelFeatureWithOptions(request: DescribeModelFeatureRequest, runtime: $RuntimeOptions): DescribeModelFeatureResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.customerModuleId)) {
    query['CustomerModuleId'] = request.customerModuleId;
  }
  if (!$isNull(request.featureTemplate)) {
    query['FeatureTemplate'] = request.featureTemplate;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeModelFeature',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Render the feature configuration of the model
 *
 * @description ## Request Description
 * - This interface is used to query all available feature templates in the system.
 * - The request method is GET, and no additional parameters are required.
 * - The returned result includes multiple feature template options, each including a label (label) and value (value).
 *
 * @param request DescribeModelFeatureRequest
 * @return DescribeModelFeatureResponse
 */
async function describeModelFeature(request: DescribeModelFeatureRequest): DescribeModelFeatureResponse {
  var runtime = new $RuntimeOptions{};
  return describeModelFeatureWithOptions(request, runtime);
}

model DescribeModelOssTokenResponseBody = {
  accessId?: string(name='AccessId', description='AccessKeyId for uploading files to OSS.', example='Lxxxxxxxxxxxxxxxxxxxxx'),
  code?: long(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  host?: string(name='Host', description='OSS domain name.', example='https://safxxxxxxxxx.aliyuncs.com'),
  httpStatusCode?: long(name='HttpStatusCode', description='HTTP status code.', example='200'),
  key?: string(name='Key', description='Key required for file upload.', example='saf/xxxxxx/xxxxxx'),
  policy?: string(name='Policy', description='Authorization policy for security group control rules.', example='eyJleHBpcmF0aW9uIjoiMxxxxxxxxxxxxxxxxxxxxxxxxxxx'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: {
    accessId?: string(name='AccessId', description='AccessKeyId for uploading files to OSS.', example='Lxxxxxxxxxxxxxxxxxxxxx'),
    host?: string(name='Host', description='OSS domain name.', example='https://safxxxxxxxxx.aliyuncs.com'),
    key?: string(name='Key', description='Key required for file upload.', example='saf/xxxxxx/xxxxxx'),
    policy?: string(name='Policy', description='Authorization policy for security group control rules.', example='eyJleHBpcmF0aW9uIjoiMxxxxxxxxxxxxxxxxxxxxxxxxxxx'),
    signature?: string(name='Signature', description='OSS signature.', example='lUxxxxxxxxxxxxxxxxxxxx'),
    XOssSecurityToken?: string(name='XOssSecurityToken', description='Temporary authorization token for OSS.', example='smxxxxxxxxxxx'),
  }(name='ResultObject', description='Returned result.'),
  signature?: string(name='Signature', description='OSS signature.', example='lUxxxxxxxxxxxxxxxxxxxx'),
  success?: boolean(name='Success', description='Indicates whether the call was successful.

- **true**: The call was successful.
- **false**: The call failed.', example='true'),
  XOssSecurityToken?: string(name='XOssSecurityToken', description='Temporary authorization token for OSS.', example='smxxxxxxxxxxx'),
}

model DescribeModelOssTokenResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeModelOssTokenResponseBody(name='body'),
}

/**
 * @summary Obtain OSS Authentication Data for Upload
 *
 * @param request DescribeModelOssTokenRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeModelOssTokenResponse
 */
async function describeModelOssTokenWithOptions(runtime: $RuntimeOptions): DescribeModelOssTokenResponse {
  var req = new OpenApiUtil.OpenApiRequest{};
  var params = new OpenApiUtil.Params{
    action = 'DescribeModelOssToken',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Obtain OSS Authentication Data for Upload
 *
 * @return DescribeModelOssTokenResponse
 */
async function describeModelOssToken(): DescribeModelOssTokenResponse {
  var runtime = new $RuntimeOptions{};
  return describeModelOssTokenWithOptions(runtime);
}

model DescribeModelingProjectDetailRequest {
  projectId?: long(name='ProjectId', description='Project ID.', example='01'),
}

model DescribeModelingProjectDetailResponseBody = {
  code?: long(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: {
    endTime?: long(name='EndTime', description='End time of the security modeling project.', example='2026-02-07T02:22:30Z'),
    envStatus?: string(name='EnvStatus', description='Security environment status.', example='ACTIVE'),
    loginAccount?: string(name='LoginAccount', description='Login account.', example='xxx'),
    machineSpec?: string(name='MachineSpec', description='Machine specification.', example='SECURE_ENV_LITE'),
    manualPhase?: string(name='ManualPhase', description='Manual operation phase.', example='INIT'),
    modelingStatus?: string(name='ModelingStatus', description='Modeling project status.', example='PREPARING'),
    pocTasks?: [ 
      {
        fileName?: string(name='FileName', description='Retrospective file name.', example='xxx.csv'),
        serviceCode?: string(name='ServiceCode', description='Service code', example='xxx'),
        status?: string(name='Status', description='File synchronization status.', example='1'),
        taskName?: string(name='TaskName', description='Retrospective task name.', example='xxx'),
      }
    ](name='PocTasks', description='Associated POC tasks.'),
    projectId?: string(name='ProjectId', description='Project ID', example='01'),
    projectName?: string(name='ProjectName', description='Project name.', example='project_01'),
    remark?: string(name='Remark', description='Remark information.', example='remark'),
    startTime?: long(name='StartTime', description='Start time of the security modeling project.', example='2025-08-13T01:09:00Z'),
    syncedFiles?: [ 
      {
        deployable?: boolean(name='Deployable', description='Whether deployment is supported.', example='true'),
        files?: [ 
          {
            downloadable?: boolean(name='Downloadable', description='Whether it is downloadable.'),
            fileId?: long(name='FileId', description='The ID of the file.', example='376920'),
            fileName?: string(name='FileName', description='The name of the file.', example='融山-个贷-精催-演示-0625.json'),
            visible?: boolean(name='Visible', description='Visibility:

**true**: Visible

**false**: Not visible', example='true'),
          }
        ](name='Files', description='Files generated by modeling.'),
        groupId?: long(name='GroupId', description='Application group ID.', example='56790766'),
        groupName?: string(name='GroupName', description='File group name.', example='am'),
        syncedTime?: string(name='SyncedTime', description='Synchronization time.', example='1770607862000'),
      }
    ](name='SyncedFiles', description='Synchronized files.'),
  }(name='ResultObject', description='Returned result.'),
  success?: boolean(name='Success', description='Whether the call was successful.

- **true**: Call succeeded.
- **false**: Call failed.', example='true'),
}

model DescribeModelingProjectDetailResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeModelingProjectDetailResponseBody(name='body'),
}

/**
 * @summary Get detailed project data
 *
 * @description ## 请求说明
 * - 本接口用于客户首次创建建模项目。
 * - `projectName` 是必填项，长度不超过50个字符。
 * - `remark` 和 `instanceSpec` 为可选项，其中 `remark` 长度不超过200个字符。
 * - `instanceSpec` 可选值包括 `SECURE_ENV_LITE` 和 `SECURE_ENV_PRO`。
 *
 * @param request DescribeModelingProjectDetailRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeModelingProjectDetailResponse
 */
@context("describeModelingProjectDetailWithContext")
async function describeModelingProjectDetailWithOptions(request: DescribeModelingProjectDetailRequest, runtime: $RuntimeOptions): DescribeModelingProjectDetailResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.projectId)) {
    query['ProjectId'] = request.projectId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeModelingProjectDetail',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Get detailed project data
 *
 * @description ## 请求说明
 * - 本接口用于客户首次创建建模项目。
 * - `projectName` 是必填项，长度不超过50个字符。
 * - `remark` 和 `instanceSpec` 为可选项，其中 `remark` 长度不超过200个字符。
 * - `instanceSpec` 可选值包括 `SECURE_ENV_LITE` 和 `SECURE_ENV_PRO`。
 *
 * @param request DescribeModelingProjectDetailRequest
 * @return DescribeModelingProjectDetailResponse
 */
async function describeModelingProjectDetail(request: DescribeModelingProjectDetailRequest): DescribeModelingProjectDetailResponse {
  var runtime = new $RuntimeOptions{};
  return describeModelingProjectDetailWithOptions(request, runtime);
}

model DescribeModelingProjectListRequest {
  currentPage?: int32(name='CurrentPage', description='Current page.', example='1'),
  pageSize?: int32(name='PageSize', description='Pagination parameter: number of items per page, with a default value of 10.', example='10'),
  status?: string(name='Status', description='Project status.', example='PREPARING'),
}

model DescribeModelingProjectListResponseBody = {
  code?: long(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  currentPage?: long(name='CurrentPage', description='Current page.', example='1'),
  pageSize?: long(name='PageSize', description='Pagination parameter: number of items per page, with a default value of 10.', example='10'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: [ 
    {
      endTime?: long(name='EndTime', description='End time of the secure modeling project.', example='2025-12-29T02:15:10Z'),
      envStatus?: string(name='EnvStatus', description='Secure environment status.', example='PREPARING'),
      loginAccount?: string(name='LoginAccount', description='Login account.', example='xxx'),
      modelingStatus?: string(name='ModelingStatus', description='Modeling project status.', example='PREPARING'),
      projectId?: string(name='ProjectId', description='Project ID.', example='01'),
      projectName?: string(name='ProjectName', description='Project name.', example='project'),
      startTime?: long(name='StartTime', description='Start time of the secure modeling project.', example='2025-03-23T01:20:00Z'),
    }
  ](name='ResultObject', description='Returned result.'),
  success?: boolean(name='Success', description='Indicates whether the call was successful.

- **true**: Call succeeded.
- **false**: Call failed.', example='true'),
  totalItem?: long(name='TotalItem', description='Total number of records.', example='1'),
  totalPage?: long(name='TotalPage', description='Total number of pages.', example='1'),
}

model DescribeModelingProjectListResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeModelingProjectListResponseBody(name='body'),
}

/**
 * @summary Paginated query for the list of modeling projects under the current user.
 *
 * @description ## 请求说明
 * - 该API用于获取指定租户下的所有建模项目的概览信息。
 * - 支持通过`pageSize`和`currentPage`参数进行分页查询，默认每页显示10条记录。
 * - 可选地，使用`status`参数来过滤特定状态（如`active`, `released`等）的项目。
 * - 返回结果中包含每个项目的ID、名称、环境状态、建模状态、开始时间及结束时间（如果有的话），以及创建该项目的登录账号。
 *
 * @param request DescribeModelingProjectListRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeModelingProjectListResponse
 */
@context("describeModelingProjectListWithContext")
async function describeModelingProjectListWithOptions(request: DescribeModelingProjectListRequest, runtime: $RuntimeOptions): DescribeModelingProjectListResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.currentPage)) {
    query['CurrentPage'] = request.currentPage;
  }
  if (!$isNull(request.pageSize)) {
    query['PageSize'] = request.pageSize;
  }
  if (!$isNull(request.status)) {
    query['Status'] = request.status;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeModelingProjectList',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Paginated query for the list of modeling projects under the current user.
 *
 * @description ## 请求说明
 * - 该API用于获取指定租户下的所有建模项目的概览信息。
 * - 支持通过`pageSize`和`currentPage`参数进行分页查询，默认每页显示10条记录。
 * - 可选地，使用`status`参数来过滤特定状态（如`active`, `released`等）的项目。
 * - 返回结果中包含每个项目的ID、名称、环境状态、建模状态、开始时间及结束时间（如果有的话），以及创建该项目的登录账号。
 *
 * @param request DescribeModelingProjectListRequest
 * @return DescribeModelingProjectListResponse
 */
async function describeModelingProjectList(request: DescribeModelingProjectListRequest): DescribeModelingProjectListResponse {
  var runtime = new $RuntimeOptions{};
  return describeModelingProjectListWithOptions(request, runtime);
}

model DescribeModuleServiceExistRequest {
  customerModuleId?: int32(name='CustomerModuleId', description='Customer model ID', example='456'),
}

model DescribeModuleServiceExistResponseBody = {
  code?: long(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  httpStatusCode?: long(name='HttpStatusCode', description='HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: boolean(name='ResultObject', description='Return result.', example='true'),
  success?: boolean(name='Success', description='Indicates whether the call was successful.

- **true**: Call succeeded.
- **false**: Call failed.', example='true'),
}

model DescribeModuleServiceExistResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeModuleServiceExistResponseBody(name='body'),
}

/**
 * @summary Query Whether the Service Corresponding to a Specific Model Exists Based on Customer Model ID
 *
 * @description ## Request Description
 * This interface is used to check whether a specific model service exists by providing the `customerModuleId`. If it exists, it returns `true`; otherwise, it returns `false`.
 * ### Notes
 * - `customerModuleId` is a required parameter and must be of string type.
 * - This API is mainly used for front-end page display or logical judgment to confirm whether the service corresponding to the user\\"s selected model has been created.
 *
 * @param request DescribeModuleServiceExistRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeModuleServiceExistResponse
 */
@context("describeModuleServiceExistWithContext")
async function describeModuleServiceExistWithOptions(request: DescribeModuleServiceExistRequest, runtime: $RuntimeOptions): DescribeModuleServiceExistResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.customerModuleId)) {
    query['CustomerModuleId'] = request.customerModuleId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeModuleServiceExist',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Query Whether the Service Corresponding to a Specific Model Exists Based on Customer Model ID
 *
 * @description ## Request Description
 * This interface is used to check whether a specific model service exists by providing the `customerModuleId`. If it exists, it returns `true`; otherwise, it returns `false`.
 * ### Notes
 * - `customerModuleId` is a required parameter and must be of string type.
 * - This API is mainly used for front-end page display or logical judgment to confirm whether the service corresponding to the user\\"s selected model has been created.
 *
 * @param request DescribeModuleServiceExistRequest
 * @return DescribeModuleServiceExistResponse
 */
async function describeModuleServiceExist(request: DescribeModuleServiceExistRequest): DescribeModuleServiceExistResponse {
  var runtime = new $RuntimeOptions{};
  return describeModuleServiceExistWithOptions(request, runtime);
}

model DescribeModuleStatusRequest {
  customerModuleId?: int32(name='CustomerModuleId', description='Customer model ID', example='456'),
}

model DescribeModuleStatusResponseBody = {
  code?: int32(name='Code', description='Status code', example='200'),
  httpStatusCode?: long(name='HttpStatusCode', description='HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: string(name='ResultObject', description='Returned result.', example='true'),
  success?: boolean(name='Success', description='Whether the operation was successful', example='true'),
}

model DescribeModuleStatusResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeModuleStatusResponseBody(name='body'),
}

/**
 * @summary Query the current status of a model based on the customer model ID
 *
 * @description ## Request Description
 * By providing the `customerModuleId` parameter, you can query the current status of a specified customer model. The status values may include, but are not limited to, "EDIT", "ONLINE", etc.
 *
 * @param request DescribeModuleStatusRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeModuleStatusResponse
 */
@context("describeModuleStatusWithContext")
async function describeModuleStatusWithOptions(request: DescribeModuleStatusRequest, runtime: $RuntimeOptions): DescribeModuleStatusResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.customerModuleId)) {
    query['CustomerModuleId'] = request.customerModuleId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeModuleStatus',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Query the current status of a model based on the customer model ID
 *
 * @description ## Request Description
 * By providing the `customerModuleId` parameter, you can query the current status of a specified customer model. The status values may include, but are not limited to, "EDIT", "ONLINE", etc.
 *
 * @param request DescribeModuleStatusRequest
 * @return DescribeModuleStatusResponse
 */
async function describeModuleStatus(request: DescribeModuleStatusRequest): DescribeModuleStatusResponse {
  var runtime = new $RuntimeOptions{};
  return describeModuleStatusWithOptions(request, runtime);
}

model DescribePocTaskListResponseBody = {
  code?: string(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  httpStatusCode?: string(name='HttpStatusCode', description='HTTP status code.', example='200'),
  message?: boolean(name='Message', description='Return message.', example='success'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: [ 
    {
      taskId?: string(name='TaskId', description='Retro task ID.', example='01'),
      taskName?: string(name='TaskName', description='Retro task name.', example='xxx'),
    }
  ](name='ResultObject', description='Return result.'),
}

model DescribePocTaskListResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribePocTaskListResponseBody(name='body'),
}

/**
 * @summary Query POC task list.
 *
 * @param request DescribePocTaskListRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribePocTaskListResponse
 */
async function describePocTaskListWithOptions(runtime: $RuntimeOptions): DescribePocTaskListResponse {
  var req = new OpenApiUtil.OpenApiRequest{};
  var params = new OpenApiUtil.Params{
    action = 'DescribePocTaskList',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Query POC task list.
 *
 * @return DescribePocTaskListResponse
 */
async function describePocTaskList(): DescribePocTaskListResponse {
  var runtime = new $RuntimeOptions{};
  return describePocTaskListWithOptions(runtime);
}

model DescribeSafRmmpOrderResponseBody = {
  code?: long(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  httpStatusCode?: long(name='HttpStatusCode', description='HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: {
    accessible?: boolean(name='Accessible', description='Indicates whether there is functional permission.', example='true'),
  }(name='ResultObject', description='Return result.'),
  success?: boolean(name='Success', description='Indicates whether the call was successful.

- **true**: The call was successful.
- **false**: The call failed.', example='true'),
}

model DescribeSafRmmpOrderResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeSafRmmpOrderResponseBody(name='body'),
}

/**
 * @summary Query Model Hosting Orders
 *
 * @param request DescribeSafRmmpOrderRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeSafRmmpOrderResponse
 */
async function describeSafRmmpOrderWithOptions(runtime: $RuntimeOptions): DescribeSafRmmpOrderResponse {
  var req = new OpenApiUtil.OpenApiRequest{};
  var params = new OpenApiUtil.Params{
    action = 'DescribeSafRmmpOrder',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Query Model Hosting Orders
 *
 * @return DescribeSafRmmpOrderResponse
 */
async function describeSafRmmpOrder(): DescribeSafRmmpOrderResponse {
  var runtime = new $RuntimeOptions{};
  return describeSafRmmpOrderWithOptions(runtime);
}

model DescribeServiceAndSceneRequest {
  authType?: string(name='AuthType', description='Authorization type.', example='READ'),
  customerModuleId?: int32(name='CustomerModuleId', description='Customer model ID', example='456'),
}

model DescribeServiceAndSceneResponseBody = {
  code?: long(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  httpStatusCode?: long(name='HttpStatusCode', description='HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: {
    scene?: string(name='Scene', description='Model corresponding scene.', example='scene_A'),
    service?: string(name='Service', description='Model corresponding service.', example='service_A'),
  }(name='ResultObject', description='Returned result.'),
  success?: boolean(name='Success', description='Indicates whether the call was successful.

- **true**: Call succeeded.
- **false**: Call failed.', example='true'),
}

model DescribeServiceAndSceneResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeServiceAndSceneResponseBody(name='body'),
}

/**
 * @summary Query Scene and Service
 *
 * @param request DescribeServiceAndSceneRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeServiceAndSceneResponse
 */
@context("describeServiceAndSceneWithContext")
async function describeServiceAndSceneWithOptions(request: DescribeServiceAndSceneRequest, runtime: $RuntimeOptions): DescribeServiceAndSceneResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.authType)) {
    query['AuthType'] = request.authType;
  }
  if (!$isNull(request.customerModuleId)) {
    query['CustomerModuleId'] = request.customerModuleId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeServiceAndScene',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Query Scene and Service
 *
 * @param request DescribeServiceAndSceneRequest
 * @return DescribeServiceAndSceneResponse
 */
async function describeServiceAndScene(request: DescribeServiceAndSceneRequest): DescribeServiceAndSceneResponse {
  var runtime = new $RuntimeOptions{};
  return describeServiceAndSceneWithOptions(request, runtime);
}

model DescribeUserModelListRequest {
  authType?: string(name='AuthType', description='Authorization type.', example='READ'),
  currentPage?: string(name='CurrentPage', description='Current page number.', example='1'),
  name?: string(name='Name', description='Model name.', example='StudyX'),
  pageSize?: string(name='PageSize', description='Pagination parameter: number of items per page, default value 10.', example='10'),
}

model DescribeUserModelListResponseBody = {
  code?: int32(name='Code', description='`code`', example='200'),
  currentPage?: long(name='CurrentPage', description='Current page.', example='1'),
  httpStatusCode?: long(name='HttpStatusCode', description='HTTP status code.', example='200'),
  pageSize?: long(name='PageSize', description='Pagination parameter: number of items per page, default value is 10.', example='10'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: [ 
    {
      authType?: string(name='AuthType', description='Authorization type.', example='READ'),
      customerModuleName?: string(name='CustomerModuleName', description='Model name.', example='Model_A'),
      description?: string(name='Description', description='Remarks.', example='备注。'),
      featureTemplate?: string(name='FeatureTemplate', description='Associated features.', example='template_a'),
      gmtModified?: long(name='GmtModified', description='Modification time.', example='1673578656000'),
      id?: long(name='Id', description='Primary key ID of the model.', example='1'),
      innerDefineStatus?: string(name='InnerDefineStatus', description='Publication status.', example='ONLINE'),
      innerModuleName?: string(name='InnerModuleName', description='Model identifier.', example='inner_model_a'),
      isAllowIterate?: boolean(name='IsAllowIterate', description='Whether iteration is allowed.', example='true'),
      isAllowRollback?: boolean(name='IsAllowRollback', description='Whether rollback is allowed.', example='true'),
      iterationVersion?: long(name='IterationVersion', description='Iteration version.', example='1'),
      rootModuleId?: long(name='RootModuleId', description='Root model ID.', example='10'),
    }
  ](name='ResultObject', description='Return result.'),
  success?: boolean(name='Success', description='Indicates whether the request was successful, with values as follows:
- true, request succeeded
- false, request failed', example='true'),
  totalItem?: long(name='TotalItem', description='Total number of records.', example='1'),
  totalPage?: long(name='TotalPage', description='Total number of pages.', example='1'),
}

model DescribeUserModelListResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeUserModelListResponseBody(name='body'),
}

/**
 * @summary Get all model information for a specified user, supporting pagination
 *
 * @description ## Request Description
 * This API is used to query all model information under a specific user and supports pagination through page parameters. Fuzzy search can be performed using the `name` parameter.
 * - `regId`: Region identifier, required.
 * - `pageSize`: Number of items per page, required.
 * - `currentPage`: Current page number, starting from 1, required.
 * - `userId`: User ID, required.
 *
 * @param request DescribeUserModelListRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeUserModelListResponse
 */
@context("describeUserModelListWithContext")
async function describeUserModelListWithOptions(request: DescribeUserModelListRequest, runtime: $RuntimeOptions): DescribeUserModelListResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.authType)) {
    query['AuthType'] = request.authType;
  }
  if (!$isNull(request.currentPage)) {
    query['CurrentPage'] = request.currentPage;
  }
  if (!$isNull(request.name)) {
    query['Name'] = request.name;
  }
  if (!$isNull(request.pageSize)) {
    query['PageSize'] = request.pageSize;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeUserModelList',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Get all model information for a specified user, supporting pagination
 *
 * @description ## Request Description
 * This API is used to query all model information under a specific user and supports pagination through page parameters. Fuzzy search can be performed using the `name` parameter.
 * - `regId`: Region identifier, required.
 * - `pageSize`: Number of items per page, required.
 * - `currentPage`: Current page number, starting from 1, required.
 * - `userId`: User ID, required.
 *
 * @param request DescribeUserModelListRequest
 * @return DescribeUserModelListResponse
 */
async function describeUserModelList(request: DescribeUserModelListRequest): DescribeUserModelListResponse {
  var runtime = new $RuntimeOptions{};
  return describeUserModelListWithOptions(request, runtime);
}

model DuplicateModelRequest {
  customerModuleId?: int32(name='CustomerModuleId', description='Customer model ID', example='456'),
}

model DuplicateModelResponseBody = {
  code?: long(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  httpStatusCode?: long(name='HttpStatusCode', description='HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: string(name='ResultObject', description='Return result.', example='374'),
  success?: boolean(name='Success', description='Whether the call was successful.

- **true**: Call succeeded.
- **false**: Call failed.', example='true'),
}

model DuplicateModelResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DuplicateModelResponseBody(name='body'),
}

/**
 * @summary Iterate Model
 *
 * @description ## Request Description
 * This API is used to delete a specified customer model from the system. When calling, you must provide the `customerModuleId` parameter, which identifies the specific model to be deleted.
 * - **Note**: The deletion operation is irreversible, please use with caution.
 *
 * @param request DuplicateModelRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DuplicateModelResponse
 */
@context("duplicateModelWithContext")
async function duplicateModelWithOptions(request: DuplicateModelRequest, runtime: $RuntimeOptions): DuplicateModelResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.customerModuleId)) {
    query['CustomerModuleId'] = request.customerModuleId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DuplicateModel',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Iterate Model
 *
 * @description ## Request Description
 * This API is used to delete a specified customer model from the system. When calling, you must provide the `customerModuleId` parameter, which identifies the specific model to be deleted.
 * - **Note**: The deletion operation is irreversible, please use with caution.
 *
 * @param request DuplicateModelRequest
 * @return DuplicateModelResponse
 */
async function duplicateModel(request: DuplicateModelRequest): DuplicateModelResponse {
  var runtime = new $RuntimeOptions{};
  return duplicateModelWithOptions(request, runtime);
}

model EditModelRequest {
  customerModuleId?: int32(name='CustomerModuleId', description='Customer model ID', example='456'),
}

model EditModelResponseBody = {
  code?: long(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  httpStatusCode?: long(name='HttpStatusCode', description='HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: boolean(name='ResultObject', description='Result object.', example='true'),
  success?: boolean(name='Success', description='Indicates whether the call was successful.

- **true**: The call was successful.
- **false**: The call failed.', example='true'),
}

model EditModelResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: EditModelResponseBody(name='body'),
}

/**
 * @summary Edit Model
 *
 * @param request EditModelRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return EditModelResponse
 */
@context("editModelWithContext")
async function editModelWithOptions(request: EditModelRequest, runtime: $RuntimeOptions): EditModelResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.customerModuleId)) {
    query['CustomerModuleId'] = request.customerModuleId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'EditModel',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Edit Model
 *
 * @param request EditModelRequest
 * @return EditModelResponse
 */
async function editModel(request: EditModelRequest): EditModelResponse {
  var runtime = new $RuntimeOptions{};
  return editModelWithOptions(request, runtime);
}

model GetBastionHostCertificationRequest {
  projectId?: long(name='ProjectId', description='Project ID

This parameter is required.', example='01'),
}

model GetBastionHostCertificationResponseBody = {
  code?: long(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: string(name='ResultObject', description='Return result.', example='xxx'),
  success?: boolean(name='Success', description='Whether the call was successful.

- **true**: Call succeeded.
- **false**: Call failed.', example='true'),
}

model GetBastionHostCertificationResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetBastionHostCertificationResponseBody(name='body'),
}

/**
 * @summary View Bastion Host Initial Password
 *
 * @description ## 请求说明
 * - 本接口用于客户首次创建建模项目。
 * - `projectName` 是必填项，长度不超过50个字符。
 * - `remark` 和 `instanceSpec` 为可选项，其中 `remark` 长度不超过200个字符。
 * - `instanceSpec` 可选值包括 `SECURE_ENV_LITE` 和 `SECURE_ENV_PRO`。
 *
 * @param request GetBastionHostCertificationRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetBastionHostCertificationResponse
 */
@context("getBastionHostCertificationWithContext")
async function getBastionHostCertificationWithOptions(request: GetBastionHostCertificationRequest, runtime: $RuntimeOptions): GetBastionHostCertificationResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.projectId)) {
    query['ProjectId'] = request.projectId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetBastionHostCertification',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary View Bastion Host Initial Password
 *
 * @description ## 请求说明
 * - 本接口用于客户首次创建建模项目。
 * - `projectName` 是必填项，长度不超过50个字符。
 * - `remark` 和 `instanceSpec` 为可选项，其中 `remark` 长度不超过200个字符。
 * - `instanceSpec` 可选值包括 `SECURE_ENV_LITE` 和 `SECURE_ENV_PRO`。
 *
 * @param request GetBastionHostCertificationRequest
 * @return GetBastionHostCertificationResponse
 */
async function getBastionHostCertification(request: GetBastionHostCertificationRequest): GetBastionHostCertificationResponse {
  var runtime = new $RuntimeOptions{};
  return getBastionHostCertificationWithOptions(request, runtime);
}

model IterateModelRequest {
  customerModuleId?: int32(name='CustomerModuleId', description='Customer model ID', example='456'),
}

model IterateModelResponseBody = {
  code?: long(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  httpStatusCode?: long(name='HttpStatusCode', description='HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: string(name='ResultObject', description='Result object.', example='true'),
  success?: boolean(name='Success', description='Whether the call was successful.

- **true**: Call succeeded.
- **false**: Call failed.', example='true'),
}

model IterateModelResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: IterateModelResponseBody(name='body'),
}

/**
 * @summary Iterate Model
 *
 * @description ## Request Description
 * This API is used to delete a specified customer model from the system. When calling, you must provide the `customerModuleId` parameter, which identifies the specific model to be deleted.
 * - **Note**: The deletion operation is irreversible, please use with caution.
 *
 * @param request IterateModelRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return IterateModelResponse
 */
@context("iterateModelWithContext")
async function iterateModelWithOptions(request: IterateModelRequest, runtime: $RuntimeOptions): IterateModelResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.customerModuleId)) {
    query['CustomerModuleId'] = request.customerModuleId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'IterateModel',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Iterate Model
 *
 * @description ## Request Description
 * This API is used to delete a specified customer model from the system. When calling, you must provide the `customerModuleId` parameter, which identifies the specific model to be deleted.
 * - **Note**: The deletion operation is irreversible, please use with caution.
 *
 * @param request IterateModelRequest
 * @return IterateModelResponse
 */
async function iterateModel(request: IterateModelRequest): IterateModelResponse {
  var runtime = new $RuntimeOptions{};
  return iterateModelWithOptions(request, runtime);
}

model OfflineModelRequest {
  customerModuleId?: int32(name='CustomerModuleId', description='Customer model ID', example='456'),
}

model OfflineModelResponseBody = {
  code?: long(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  httpStatusCode?: long(name='HttpStatusCode', description='HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: boolean(name='ResultObject', description='Return result.', example='true'),
  success?: boolean(name='Success', description='Indicates whether the call was successful.

- **true**: The call was successful.
- **false**: The call failed.', example='true'),
}

model OfflineModelResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: OfflineModelResponseBody(name='body'),
}

/**
 * @summary Offline Model
 *
 * @param request OfflineModelRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return OfflineModelResponse
 */
@context("offlineModelWithContext")
async function offlineModelWithOptions(request: OfflineModelRequest, runtime: $RuntimeOptions): OfflineModelResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.customerModuleId)) {
    query['CustomerModuleId'] = request.customerModuleId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'OfflineModel',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Offline Model
 *
 * @param request OfflineModelRequest
 * @return OfflineModelResponse
 */
async function offlineModel(request: OfflineModelRequest): OfflineModelResponse {
  var runtime = new $RuntimeOptions{};
  return offlineModelWithOptions(request, runtime);
}

model OnlineModelRequest {
  customerModuleId?: int32(name='CustomerModuleId', description='Customer model ID', example='456'),
}

model OnlineModelResponseBody = {
  code?: long(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  httpStatusCode?: long(name='HttpStatusCode', description='HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: boolean(name='ResultObject', description='Return result.', example='true'),
  success?: boolean(name='Success', description='Indicates whether the call was successful.

- **true**: Call succeeded.
- **false**: Call failed.', example='true'),
}

model OnlineModelResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: OnlineModelResponseBody(name='body'),
}

/**
 * @summary Online Model
 *
 * @description ## Request Description
 * This API is used to delete a specified customer model from the system. When calling, you must provide the `customerModuleId` parameter, which identifies the specific model to be deleted.
 * - **Note**: Deletion is irreversible, please use with caution.
 *
 * @param request OnlineModelRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return OnlineModelResponse
 */
@context("onlineModelWithContext")
async function onlineModelWithOptions(request: OnlineModelRequest, runtime: $RuntimeOptions): OnlineModelResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.customerModuleId)) {
    query['CustomerModuleId'] = request.customerModuleId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'OnlineModel',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Online Model
 *
 * @description ## Request Description
 * This API is used to delete a specified customer model from the system. When calling, you must provide the `customerModuleId` parameter, which identifies the specific model to be deleted.
 * - **Note**: Deletion is irreversible, please use with caution.
 *
 * @param request OnlineModelRequest
 * @return OnlineModelResponse
 */
async function onlineModel(request: OnlineModelRequest): OnlineModelResponse {
  var runtime = new $RuntimeOptions{};
  return onlineModelWithOptions(request, runtime);
}

model ParseExpressionParametersRequest {
  expression?: string(name='Expression', description='Expression.', example='score'),
}

model ParseExpressionParametersResponseBody = {
  code?: long(name='Code', description='Status code. A return of 200 indicates success.', example='200'),
  httpStatusCode?: long(name='HttpStatusCode', description='HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: {
    params?: [ string ](name='Params', description='List of parameters.'),
  }(name='ResultObject', description='Return result.'),
  success?: boolean(name='Success', description='Indicates whether the call was successful.

- **true**: Call succeeded.
- **false**: Call failed.', example='true'),
}

model ParseExpressionParametersResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ParseExpressionParametersResponseBody(name='body'),
}

/**
 * @summary Parse Expression Parameters
 *
 * @description ## Request Description
 * - This interface is used to query all available feature templates in the system.
 * - The request method is GET, and no additional parameters are required.
 * - The returned result includes multiple feature template options, each of which includes a label (label) and a value (value).
 *
 * @param request ParseExpressionParametersRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ParseExpressionParametersResponse
 */
@context("parseExpressionParametersWithContext")
async function parseExpressionParametersWithOptions(request: ParseExpressionParametersRequest, runtime: $RuntimeOptions): ParseExpressionParametersResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.expression)) {
    query['Expression'] = request.expression;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ParseExpressionParameters',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Parse Expression Parameters
 *
 * @description ## Request Description
 * - This interface is used to query all available feature templates in the system.
 * - The request method is GET, and no additional parameters are required.
 * - The returned result includes multiple feature template options, each of which includes a label (label) and a value (value).
 *
 * @param request ParseExpressionParametersRequest
 * @return ParseExpressionParametersResponse
 */
async function parseExpressionParameters(request: ParseExpressionParametersRequest): ParseExpressionParametersResponse {
  var runtime = new $RuntimeOptions{};
  return parseExpressionParametersWithOptions(request, runtime);
}

model PrepublishModelRequest {
  customerModuleId?: int32(name='CustomerModuleId', description='Customer model ID', example='456'),
}

model PrepublishModelResponseBody = {
  code?: long(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  httpStatusCode?: long(name='HttpStatusCode', description='HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: boolean(name='ResultObject', description='Return result.', example='true'),
  success?: boolean(name='Success', description='Indicates whether the call was successful.

- **true**: Call succeeded.
- **false**: Call failed.', example='true'),
}

model PrepublishModelResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: PrepublishModelResponseBody(name='body'),
}

/**
 * @summary Pre-release Model
 *
 * @param request PrepublishModelRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return PrepublishModelResponse
 */
@context("prepublishModelWithContext")
async function prepublishModelWithOptions(request: PrepublishModelRequest, runtime: $RuntimeOptions): PrepublishModelResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.customerModuleId)) {
    query['CustomerModuleId'] = request.customerModuleId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'PrepublishModel',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Pre-release Model
 *
 * @param request PrepublishModelRequest
 * @return PrepublishModelResponse
 */
async function prepublishModel(request: PrepublishModelRequest): PrepublishModelResponse {
  var runtime = new $RuntimeOptions{};
  return prepublishModelWithOptions(request, runtime);
}

model RequestModelFileSyncRequest {
  projectId?: long(name='ProjectId', description='Project ID

This parameter is required.', example='01'),
}

model RequestModelFileSyncResponseBody = {
  code?: long(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: boolean(name='ResultObject', description='Return result.', example='true'),
  success?: boolean(name='Success', description='Whether the call was successful.

- **true**: Call succeeded.
- **false**: Call failed.', example='true'),
}

model RequestModelFileSyncResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: RequestModelFileSyncResponseBody(name='body'),
}

/**
 * @summary Request to Sync Model Files
 *
 * @description ## 请求说明
 * - 本接口用于客户首次创建建模项目。
 * - `projectName` 是必填项，长度不超过50个字符。
 * - `remark` 和 `instanceSpec` 为可选项，其中 `remark` 长度不超过200个字符。
 * - `instanceSpec` 可选值包括 `SECURE_ENV_LITE` 和 `SECURE_ENV_PRO`。
 *
 * @param request RequestModelFileSyncRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return RequestModelFileSyncResponse
 */
@context("requestModelFileSyncWithContext")
async function requestModelFileSyncWithOptions(request: RequestModelFileSyncRequest, runtime: $RuntimeOptions): RequestModelFileSyncResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.projectId)) {
    query['ProjectId'] = request.projectId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'RequestModelFileSync',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Request to Sync Model Files
 *
 * @description ## 请求说明
 * - 本接口用于客户首次创建建模项目。
 * - `projectName` 是必填项，长度不超过50个字符。
 * - `remark` 和 `instanceSpec` 为可选项，其中 `remark` 长度不超过200个字符。
 * - `instanceSpec` 可选值包括 `SECURE_ENV_LITE` 和 `SECURE_ENV_PRO`。
 *
 * @param request RequestModelFileSyncRequest
 * @return RequestModelFileSyncResponse
 */
async function requestModelFileSync(request: RequestModelFileSyncRequest): RequestModelFileSyncResponse {
  var runtime = new $RuntimeOptions{};
  return requestModelFileSyncWithOptions(request, runtime);
}

model RollbackModelRequest {
  customerModuleId?: int32(name='CustomerModuleId', description='Customer model ID', example='456'),
}

model RollbackModelResponseBody = {
  code?: long(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  httpStatusCode?: long(name='HttpStatusCode', description='HTTP状态码。', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: boolean(name='ResultObject', description='Return result.', example='true'),
  success?: boolean(name='Success', description='Whether the call was successful.

- **true**: Call succeeded.
- **false**: Call failed.', example='true'),
}

model RollbackModelResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: RollbackModelResponseBody(name='body'),
}

/**
 * @summary Roll back the specified customer model based on the provided customer model ID.
 *
 * @description ## Request Description
 * This API is used to delete a specified customer model from the system. When calling, you must provide the `customerModuleId` parameter, which identifies the specific model to be deleted.
 * - **Note**: The deletion operation is irreversible, please use with caution.
 *
 * @param request RollbackModelRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return RollbackModelResponse
 */
@context("rollbackModelWithContext")
async function rollbackModelWithOptions(request: RollbackModelRequest, runtime: $RuntimeOptions): RollbackModelResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.customerModuleId)) {
    query['CustomerModuleId'] = request.customerModuleId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'RollbackModel',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Roll back the specified customer model based on the provided customer model ID.
 *
 * @description ## Request Description
 * This API is used to delete a specified customer model from the system. When calling, you must provide the `customerModuleId` parameter, which identifies the specific model to be deleted.
 * - **Note**: The deletion operation is irreversible, please use with caution.
 *
 * @param request RollbackModelRequest
 * @return RollbackModelResponse
 */
async function rollbackModel(request: RollbackModelRequest): RollbackModelResponse {
  var runtime = new $RuntimeOptions{};
  return rollbackModelWithOptions(request, runtime);
}

model TestModelRequest {
  customerModuleId?: int32(name='CustomerModuleId', description='Customer model ID', example='456'),
}

model TestModelResponseBody = {
  code?: long(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  httpStatusCode?: long(name='HttpStatusCode', description='HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: {
    consistencyCount?: long(name='ConsistencyCount', description='Number of consistencies.', example='49.0'),
    consistencyRate?: double(name='ConsistencyRate', description='Consistency rate.', example='98.0'),
    testResult?: [ 
      {
        actualResult?: string(name='ActualResult', description='Actual output.', example='0.00'),
        consistency?: boolean(name='Consistency', description='Whether it is consistent.', example='true'),
        testResult?: string(name='TestResult', description='Test output.', example='0.00'),
        trainResult?: string(name='TrainResult', description='Training output.', example='0.00'),
      }
    ](name='TestResult', description='Test results.'),
    total?: long(name='Total', description='Total number.', example='50'),
  }(name='ResultObject', description='Return result.'),
  success?: boolean(name='Success', description='Indicates whether the call was successful.

- **true**: Call succeeded.
- **false**: Call failed.', example='true'),
}

model TestModelResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: TestModelResponseBody(name='body'),
}

/**
 * @summary Pre-release Model Testing
 *
 * @description ## Request Description
 * - This interface is used to query all available feature templates in the system.
 * - The request method is GET, and no additional parameters are required.
 * - The returned result includes multiple feature template options, each of which includes a label (label) and a value (value).
 *
 * @param request TestModelRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return TestModelResponse
 */
@context("testModelWithContext")
async function testModelWithOptions(request: TestModelRequest, runtime: $RuntimeOptions): TestModelResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.customerModuleId)) {
    query['CustomerModuleId'] = request.customerModuleId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'TestModel',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Pre-release Model Testing
 *
 * @description ## Request Description
 * - This interface is used to query all available feature templates in the system.
 * - The request method is GET, and no additional parameters are required.
 * - The returned result includes multiple feature template options, each of which includes a label (label) and a value (value).
 *
 * @param request TestModelRequest
 * @return TestModelResponse
 */
async function testModel(request: TestModelRequest): TestModelResponse {
  var runtime = new $RuntimeOptions{};
  return testModelWithOptions(request, runtime);
}

model TestPreModelRequest {
  customerModuleId?: int32(name='CustomerModuleId', description='Customer model ID', example='456'),
}

model TestPreModelResponseBody = {
  code?: long(name='Code', description='Status code. A return of 200 indicates success.', example='200'),
  httpStatusCode?: long(name='HttpStatusCode', description='HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: [ boolean ](name='ResultObject', description='Return result.'),
  success?: boolean(name='Success', description='Whether the call was successful.

- **true**: Call succeeded.
- **false**: Call failed.', example='true'),
}

model TestPreModelResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: TestPreModelResponseBody(name='body'),
}

/**
 * @summary Pre-release Test Model
 *
 * @description ## Request Description
 * - This interface is used to query all available feature templates in the system.
 * - The request method is GET, and no additional parameters are required.
 * - The returned result includes multiple feature template options, each of which includes a label (label) and a value (value).
 *
 * @param request TestPreModelRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return TestPreModelResponse
 */
@context("testPreModelWithContext")
async function testPreModelWithOptions(request: TestPreModelRequest, runtime: $RuntimeOptions): TestPreModelResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.customerModuleId)) {
    query['CustomerModuleId'] = request.customerModuleId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'TestPreModel',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Pre-release Test Model
 *
 * @description ## Request Description
 * - This interface is used to query all available feature templates in the system.
 * - The request method is GET, and no additional parameters are required.
 * - The returned result includes multiple feature template options, each of which includes a label (label) and a value (value).
 *
 * @param request TestPreModelRequest
 * @return TestPreModelResponse
 */
async function testPreModel(request: TestPreModelRequest): TestPreModelResponse {
  var runtime = new $RuntimeOptions{};
  return testPreModelWithOptions(request, runtime);
}

model TestProcessExpressionRequest {
  expression?: string(name='Expression', description='Expression.', example='score'),
  params?: string(name='Params', description='Parameter list.', example='{"score": "1"}'),
}

model TestProcessExpressionResponseBody = {
  code?: long(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  httpStatusCode?: long(name='HttpStatusCode', description='HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: {
    score?: double(name='Score', description='Test score.', example='10'),
  }(name='ResultObject', description='Return result.'),
  success?: boolean(name='Success', description='Indicates whether the call was successful.

- **true**: Call succeeded.
- **false**: Call failed.', example='true'),
}

model TestProcessExpressionResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: TestProcessExpressionResponseBody(name='body'),
}

/**
 * @summary Test Expression
 *
 * @description ## Request Description
 * - This interface is used to query all available feature templates in the system.
 * - The request method is GET, and no additional parameters are required.
 * - The returned result includes multiple feature template options, each of which includes a label (label) and a value (value).
 *
 * @param request TestProcessExpressionRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return TestProcessExpressionResponse
 */
@context("testProcessExpressionWithContext")
async function testProcessExpressionWithOptions(request: TestProcessExpressionRequest, runtime: $RuntimeOptions): TestProcessExpressionResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.expression)) {
    query['Expression'] = request.expression;
  }
  if (!$isNull(request.params)) {
    query['Params'] = request.params;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'TestProcessExpression',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Test Expression
 *
 * @description ## Request Description
 * - This interface is used to query all available feature templates in the system.
 * - The request method is GET, and no additional parameters are required.
 * - The returned result includes multiple feature template options, each of which includes a label (label) and a value (value).
 *
 * @param request TestProcessExpressionRequest
 * @return TestProcessExpressionResponse
 */
async function testProcessExpression(request: TestProcessExpressionRequest): TestProcessExpressionResponse {
  var runtime = new $RuntimeOptions{};
  return testProcessExpressionWithOptions(request, runtime);
}

model UpdateModuleBasicInfoRequest {
  customerModuleId?: int32(name='CustomerModuleId', description='Customer model ID', example='456'),
  customerModuleName?: string(name='CustomerModuleName', description='Model name.', example='moduleName'),
  description?: string(name='Description', description='Description.', example='备注'),
  moduleType?: string(name='ModuleType', description='Module type.', example='PMML'),
  storePath?: string(name='StorePath', description='Test address.', example='saf/xxxxx/xxx.pmml'),
}

model UpdateModuleBasicInfoResponseBody = {
  code?: long(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  httpStatusCode?: long(name='HttpStatusCode', description='HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: boolean(name='ResultObject', description='Return result.', example='true'),
  success?: boolean(name='Success', description='Indicates whether the call was successful.

- **true**: The call was successful.
- **false**: The call failed.', example='true'),
}

model UpdateModuleBasicInfoResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateModuleBasicInfoResponseBody(name='body'),
}

/**
 * @summary Update basic model information
 *
 * @param request UpdateModuleBasicInfoRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateModuleBasicInfoResponse
 */
@context("updateModuleBasicInfoWithContext")
async function updateModuleBasicInfoWithOptions(request: UpdateModuleBasicInfoRequest, runtime: $RuntimeOptions): UpdateModuleBasicInfoResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.customerModuleId)) {
    query['CustomerModuleId'] = request.customerModuleId;
  }
  if (!$isNull(request.customerModuleName)) {
    query['CustomerModuleName'] = request.customerModuleName;
  }
  if (!$isNull(request.description)) {
    query['Description'] = request.description;
  }
  if (!$isNull(request.moduleType)) {
    query['ModuleType'] = request.moduleType;
  }
  if (!$isNull(request.storePath)) {
    query['StorePath'] = request.storePath;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpdateModuleBasicInfo',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Update basic model information
 *
 * @param request UpdateModuleBasicInfoRequest
 * @return UpdateModuleBasicInfoResponse
 */
async function updateModuleBasicInfo(request: UpdateModuleBasicInfoRequest): UpdateModuleBasicInfoResponse {
  var runtime = new $RuntimeOptions{};
  return updateModuleBasicInfoWithOptions(request, runtime);
}

model ValidateModelFileRequest {
  filePath?: string(name='FilePath', description='File path.', example='saf/xxxxxxx/xxx.pmml'),
}

model ValidateModelFileResponseBody = {
  code?: long(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  httpStatusCode?: long(name='HttpStatusCode', description='HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: {
    fileValid?: boolean(name='FileValid', description='Validation result.', example='true'),
  }(name='ResultObject', description='Returned result.'),
  success?: boolean(name='Success', description='Indicates whether the call was successful.

- **true**: The call was successful.
- **false**: The call failed.', example='true'),
}

model ValidateModelFileResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ValidateModelFileResponseBody(name='body'),
}

/**
 * @summary Validate model file
 *
 * @param request ValidateModelFileRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ValidateModelFileResponse
 */
@context("validateModelFileWithContext")
async function validateModelFileWithOptions(request: ValidateModelFileRequest, runtime: $RuntimeOptions): ValidateModelFileResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.filePath)) {
    query['FilePath'] = request.filePath;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ValidateModelFile',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Validate model file
 *
 * @param request ValidateModelFileRequest
 * @return ValidateModelFileResponse
 */
async function validateModelFile(request: ValidateModelFileRequest): ValidateModelFileResponse {
  var runtime = new $RuntimeOptions{};
  return validateModelFileWithOptions(request, runtime);
}

model ValidateTestFileRequest {
  customerModuleId?: string(name='CustomerModuleId', description='Model ID.', example='456'),
  filePath?: string(name='FilePath', description='File path.', example='saf/xxxxxx/xxxxx.pmml'),
}

model ValidateTestFileResponseBody = {
  code?: long(name='Code', description='Status code. A return value of 200 indicates success.', example='200'),
  httpStatusCode?: long(name='HttpStatusCode', description='HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='Request ID.', example='4A91D2D1-AEC9-1658-ABCE-5A39DE66A5C2'),
  resultObject?: {
    fileValid?: boolean(name='FileValid', description='Whether the test file is valid.'),
  }(name='ResultObject', description='Return result.'),
  success?: boolean(name='Success', description='Indicates whether the call was successful.
- **true**: Call succeeded.
- **false**: Call failed.', example='true'),
}

model ValidateTestFileResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ValidateTestFileResponseBody(name='body'),
}

/**
 * @summary Validate Test File
 *
 * @param request ValidateTestFileRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ValidateTestFileResponse
 */
@context("validateTestFileWithContext")
async function validateTestFileWithOptions(request: ValidateTestFileRequest, runtime: $RuntimeOptions): ValidateTestFileResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.customerModuleId)) {
    query['CustomerModuleId'] = request.customerModuleId;
  }
  if (!$isNull(request.filePath)) {
    query['FilePath'] = request.filePath;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ValidateTestFile',
    version = '2025-05-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Validate Test File
 *
 * @param request ValidateTestFileRequest
 * @return ValidateTestFileResponse
 */
async function validateTestFile(request: ValidateTestFileRequest): ValidateTestFileResponse {
  var runtime = new $RuntimeOptions{};
  return validateTestFileWithOptions(request, runtime);
}

