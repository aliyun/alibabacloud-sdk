/**
 *
 */
import Util;
import OpenApi;

import EndpointUtil;

extends OpenApi;

init(config: OpenApi.Config){
  super(config);
  @endpointRule = '';
  
  checkConfig(config);
  @endpoint = getEndpoint('xixikf-autotest', @regionId, @endpointRule, @network, @suffix, @endpointMap, @endpoint);
}

function getEndpoint(productId: string, regionId: string, endpointRule: string, network: string, suffix: string, endpointMap: map[string]string, endpoint: string) throws: string{
  if (!Util.empty(endpoint)) {
    return endpoint;
  }

  if (!Util.isUnset(endpointMap) && !Util.empty(endpointMap[regionId])) {
    return endpointMap[regionId];
  }
  return EndpointUtil.getEndpointRules(productId, regionId, endpointRule, network, suffix);
}
model CommerOutRequest {
  body?: string(name='body'),
}

model CommerOutResponseBody = {
  requestId?: string(name='RequestId', description='Id of the request'),
}

model CommerOutResponse = {
  headers: map[string]string(name='headers'),
  body: CommerOutResponseBody(name='body'),
}

async function commerOutWithOptions(request: CommerOutRequest, runtime: Util.RuntimeOptions): CommerOutResponse {
  Util.validateModel(request);
  var req = new OpenApi.OpenApiRequest{ 
    body = Util.toMap(request),
  };
  return doRPCRequest('CommerOut', '2021-07-20', 'HTTPS', 'POST', 'AK', 'json', req, runtime);
}

async function commerOut(request: CommerOutRequest): CommerOutResponse {
  var runtime = new Util.RuntimeOptions{};
  return commerOutWithOptions(request, runtime);
}

model CreateEnvironmentRequest {
  userId?: string(name='userId', description='A short description of struct'),
  environment?: string(name='environment'),
}

model CreateEnvironmentResponseBody = {
  environment?: string(name='environment', description='Id of the request'),
}

model CreateEnvironmentResponse = {
  headers: map[string]string(name='headers'),
  body: CreateEnvironmentResponseBody(name='body'),
}

async function createEnvironmentWithOptions(request: CreateEnvironmentRequest, runtime: Util.RuntimeOptions): CreateEnvironmentResponse {
  Util.validateModel(request);
  var req = new OpenApi.OpenApiRequest{ 
    body = Util.toMap(request),
  };
  return doRPCRequest('CreateEnvironment', '2021-07-20', 'HTTPS', 'POST', 'AK', 'json', req, runtime);
}

async function createEnvironment(request: CreateEnvironmentRequest): CreateEnvironmentResponse {
  var runtime = new Util.RuntimeOptions{};
  return createEnvironmentWithOptions(request, runtime);
}

model CommerInRequest {
  body?: string(name='body'),
}

model CommerInResponseBody = {
  requestId?: string(name='RequestId', description='Id of the request'),
}

model CommerInResponse = {
  headers: map[string]string(name='headers'),
  body: CommerInResponseBody(name='body'),
}

async function commerInWithOptions(request: CommerInRequest, runtime: Util.RuntimeOptions): CommerInResponse {
  Util.validateModel(request);
  var req = new OpenApi.OpenApiRequest{ 
    body = Util.toMap(request),
  };
  return doRPCRequest('CommerIn', '2021-07-20', 'HTTPS', 'POST', 'AK', 'json', req, runtime);
}

async function commerIn(request: CommerInRequest): CommerInResponse {
  var runtime = new Util.RuntimeOptions{};
  return commerInWithOptions(request, runtime);
}

model XixiServicerMockServiceRequest {
  id?: string(name='id'),
}

model XixiServicerMockServiceResponseBody = {
  requestId?: string(name='RequestId', description='Id of the request'),
}

model XixiServicerMockServiceResponse = {
  headers: map[string]string(name='headers'),
  body: XixiServicerMockServiceResponseBody(name='body'),
}

async function xixiServicerMockServiceWithOptions(request: XixiServicerMockServiceRequest, runtime: Util.RuntimeOptions): XixiServicerMockServiceResponse {
  Util.validateModel(request);
  var req = new OpenApi.OpenApiRequest{ 
    body = Util.toMap(request),
  };
  return doRPCRequest('XixiServicerMockService', '2021-07-20', 'HTTPS', 'POST', 'AK', 'json', req, runtime);
}

async function xixiServicerMockService(request: XixiServicerMockServiceRequest): XixiServicerMockServiceResponse {
  var runtime = new Util.RuntimeOptions{};
  return xixiServicerMockServiceWithOptions(request, runtime);
}

