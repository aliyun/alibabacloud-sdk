/**
 *
 */
import OpenApi;
import OpenApi.OpenApiUtil;

extends OpenApi;


init(config: OpenApiUtil.Config){
  super(config);
  @endpointRule = 'regional';
  @endpointMap = {
    'ap-southeast-1' = 'wss.ap-southeast-1.aliyuncs.com',
    'cn-shanghai' = 'wss.cn-shanghai.aliyuncs.com',
  };

  checkConfig(config);
  @endpoint = getEndpoint('wss', @regionId, @endpointRule, @network, @suffix, @endpointMap, @endpoint);
}

function getEndpoint(productId: string, regionId: string, endpointRule: string, network: string, suffix: string, endpointMap: map[string]string, endpoint: string) throws: string{
  if (!$isNull(endpoint)) {
    return endpoint;
  }
  
  if (!$isNull(endpointMap) && !$isNull(endpointMap[regionId])) {
    return endpointMap[regionId];
  }
  return OpenApiUtil.getEndpointRules(productId, regionId, endpointRule, network, suffix);
}

model CreateMultiOrderRequest {
  channelCookie?: string(name='ChannelCookie', description='The channel cookie information.'),
  orderItems?: [ 
    {
      amount?: int32(name='Amount', description='The quantity to purchase.', example='1'),
      autoPay?: boolean(name='AutoPay', description='Specifies whether to enable automatic payment.', example='true'),
      autoRenew?: boolean(name='AutoRenew', description='Specifies whether to enable auto-renewal.', example='false'),
      buyChange?: boolean(name='BuyChange', description='Specifies whether this is a change purchase.'),
      components?: [ 
        {
          key?: string(name='Key', description='The key of the module.', example='RegionId'),
          value?: string(name='Value', description='The value of the module.

The following section describes the sample values and valid values for each key of the Enterprise Edition monthly duration package:

- RegionId: ap-southeast-1
- InstanceType: eds.enterprise_office.4c8g
- DurationType (hours): Valid values: 
   - 120
   - 250
- OsType: Valid values: 
   - Windows
   - Linux
- RootDiskSize (GiB): 80
- RootDiskCategory: Valid values: 
   - cloud_efficiency (ultra cloud disk)
   - cloud_auto (ultra-fast cloud disk)
   - cloud_essd (enhanced standard SSD. Only specific instance types support this value.)
- RootPerformanceLevel: Valid values: 
   - PL0
   - PL1
   - PL2
   - PL3
- DataDiskSize (GiB): Valid values are the same as those of RootDiskSize.
- DataDiskCategory: Valid values are the same as those of RootDiskCategory.
- DataPerformanceLevel: Valid values are the same as those of RootPerformanceLevel.', example='cn-shanghai'),
        }
      ](name='Components', description='The product modules.'),
      instanceIds?: [ string ](name='InstanceIds', description='The list of instance IDs.'),
      paidCallBackUrl?: string(name='PaidCallBackUrl', description='The callback URL after the payment is completed.'),
      period?: int32(name='Period', description='The subscription duration. Valid values:

- If PeriodUnit is set to Year: 1, 2, 3, and 5.

- If PeriodUnit is set to Month: 1, 2, 3, and 6.', example='1'),
      periodUnit?: string(name='PeriodUnit', description='The unit of the subscription duration for a subscription instance.

> This parameter is required only when the billing method of the instance is subscription. This parameter is case-sensitive. Make sure that the value is spelled correctly.', example='Year'),
      promotionId?: string(name='PromotionId', description='The promotion ID.', example='youhuiquan_promotion_option_id_for_blank'),
      resourceIds?: [ string ](name='ResourceIds', description='The list of resource IDs.
> For monthly duration packages, this parameter corresponds to the cloud desktop ID. This parameter is required when OrderType is not set to `create`.'),
      resourceType?: string(name='ResourceType', description='The resource type.
> This parameter is case-sensitive. Make sure that the value is spelled correctly.

This parameter is required.', example='DurationPackage'),
    }
  ](name='OrderItems', description='The product information.'),
  orderType?: string(name='OrderType', description='The order type.', example='create'),
  properties?: map[string]string(name='Properties', description='The extended properties.'),
  resellerOwnerUid?: long(name='ResellerOwnerUid', description='The UID of the reseller owner.'),
}

model CreateMultiOrderShrinkRequest {
  channelCookie?: string(name='ChannelCookie', description='The channel cookie information.'),
  orderItems?: [ 
    {
      amount?: int32(name='Amount', description='The quantity to purchase.', example='1'),
      autoPay?: boolean(name='AutoPay', description='Specifies whether to enable automatic payment.', example='true'),
      autoRenew?: boolean(name='AutoRenew', description='Specifies whether to enable auto-renewal.', example='false'),
      buyChange?: boolean(name='BuyChange', description='Specifies whether this is a change purchase.'),
      components?: [ 
        {
          key?: string(name='Key', description='The key of the module.', example='RegionId'),
          value?: string(name='Value', description='The value of the module.

The following section describes the sample values and valid values for each key of the Enterprise Edition monthly duration package:

- RegionId: ap-southeast-1
- InstanceType: eds.enterprise_office.4c8g
- DurationType (hours): Valid values: 
   - 120
   - 250
- OsType: Valid values: 
   - Windows
   - Linux
- RootDiskSize (GiB): 80
- RootDiskCategory: Valid values: 
   - cloud_efficiency (ultra cloud disk)
   - cloud_auto (ultra-fast cloud disk)
   - cloud_essd (enhanced standard SSD. Only specific instance types support this value.)
- RootPerformanceLevel: Valid values: 
   - PL0
   - PL1
   - PL2
   - PL3
- DataDiskSize (GiB): Valid values are the same as those of RootDiskSize.
- DataDiskCategory: Valid values are the same as those of RootDiskCategory.
- DataPerformanceLevel: Valid values are the same as those of RootPerformanceLevel.', example='cn-shanghai'),
        }
      ](name='Components', description='The product modules.'),
      instanceIds?: [ string ](name='InstanceIds', description='The list of instance IDs.'),
      paidCallBackUrl?: string(name='PaidCallBackUrl', description='The callback URL after the payment is completed.'),
      period?: int32(name='Period', description='The subscription duration. Valid values:

- If PeriodUnit is set to Year: 1, 2, 3, and 5.

- If PeriodUnit is set to Month: 1, 2, 3, and 6.', example='1'),
      periodUnit?: string(name='PeriodUnit', description='The unit of the subscription duration for a subscription instance.

> This parameter is required only when the billing method of the instance is subscription. This parameter is case-sensitive. Make sure that the value is spelled correctly.', example='Year'),
      promotionId?: string(name='PromotionId', description='The promotion ID.', example='youhuiquan_promotion_option_id_for_blank'),
      resourceIds?: [ string ](name='ResourceIds', description='The list of resource IDs.
> For monthly duration packages, this parameter corresponds to the cloud desktop ID. This parameter is required when OrderType is not set to `create`.'),
      resourceType?: string(name='ResourceType', description='The resource type.
> This parameter is case-sensitive. Make sure that the value is spelled correctly.

This parameter is required.', example='DurationPackage'),
    }
  ](name='OrderItems', description='The product information.'),
  orderType?: string(name='OrderType', description='The order type.', example='create'),
  propertiesShrink?: string(name='Properties', description='The extended properties.'),
  resellerOwnerUid?: long(name='ResellerOwnerUid', description='The UID of the reseller owner.'),
}

model CreateMultiOrderResponseBody = {
  orderIds?: [ long ](name='OrderIds', description='The list of order IDs.'),
  requestId?: string(name='RequestId', description='The request ID.', example='833C4D2C-09C7-5CE6-8159-06758B964970'),
}

model CreateMultiOrderResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateMultiOrderResponseBody(name='body'),
}

/**
 * @summary Places, renews, or modifies orders for specific products of Elastic Desktop Service (EDS) Enterprise Edition, such as monthly duration packages.
 *
 * @description <props="china">Before you use this operation, make sure that you fully understand the billing methods and [pricing](https://www.aliyun.com/price/product?#/gws/detail/gws) of EDS.
 * <props="intl">Before you use this operation, make sure that you fully understand the billing methods and [pricing](https://www.alibabacloud.com/zh/product/cloud-desktop?#J_8623712560) of EDS.
 * If automatic payment is not specified, this operation does not process the payment. You must use the order ID returned by this operation to construct a payment redirect URL and complete the payment before the order takes effect and the resources are provisioned.
 *
 * @param tmpReq CreateMultiOrderRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateMultiOrderResponse
 */
@context("createMultiOrderWithContext")
async function createMultiOrderWithOptions(tmpReq: CreateMultiOrderRequest, runtime: $RuntimeOptions): CreateMultiOrderResponse {
  tmpReq.validate();
  var request = new CreateMultiOrderShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.properties)) {
    request.propertiesShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.properties, 'Properties', 'json');
  }
  var query = {};
  if (!$isNull(request.channelCookie)) {
    query['ChannelCookie'] = request.channelCookie;
  }
  if (!$isNull(request.orderItems)) {
    query['OrderItems'] = request.orderItems;
  }
  if (!$isNull(request.orderType)) {
    query['OrderType'] = request.orderType;
  }
  if (!$isNull(request.propertiesShrink)) {
    query['Properties'] = request.propertiesShrink;
  }
  if (!$isNull(request.resellerOwnerUid)) {
    query['ResellerOwnerUid'] = request.resellerOwnerUid;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateMultiOrder',
    version = '2021-12-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Places, renews, or modifies orders for specific products of Elastic Desktop Service (EDS) Enterprise Edition, such as monthly duration packages.
 *
 * @description <props="china">Before you use this operation, make sure that you fully understand the billing methods and [pricing](https://www.aliyun.com/price/product?#/gws/detail/gws) of EDS.
 * <props="intl">Before you use this operation, make sure that you fully understand the billing methods and [pricing](https://www.alibabacloud.com/zh/product/cloud-desktop?#J_8623712560) of EDS.
 * If automatic payment is not specified, this operation does not process the payment. You must use the order ID returned by this operation to construct a payment redirect URL and complete the payment before the order takes effect and the resources are provisioned.
 *
 * @param request CreateMultiOrderRequest
 * @return CreateMultiOrderResponse
 */
async function createMultiOrder(request: CreateMultiOrderRequest): CreateMultiOrderResponse {
  var runtime = new $RuntimeOptions{};
  return createMultiOrderWithOptions(request, runtime);
}

model DescribeCreditPackageAgentsRequest {
  agentIds?: [ string ](name='AgentIds', description='A JSON string of the AgentId list. Example: `["agent-1","agent-2"]`.'),
  agentType?: string(name='AgentType', description='The Agent type. Valid values: `CREDIT_PACKAGE`, `JVS_CLAW`, `OPEN_CLAW`, and `JVS_COPILOT`.', example='JVS_COPILOT'),
  bizType?: string(name='BizType', description='The business type.', example='BUSINESS'),
  maxResults?: int32(name='MaxResults', description='The number of entries per page.', example='100'),
  nextToken?: string(name='NextToken', description='The pagination token. Set this parameter to an empty string for the first request. For subsequent requests, use the `nextToken` value from the previous response.', example='eyJvZmZzZXQiOjIwfQ=='),
}

model DescribeCreditPackageAgentsResponseBody = {
  agents?: [ 
    {
      agentId?: string(name='AgentId', description='Agent ID', example='agent-abc'),
      createdTime?: string(name='CreatedTime', description='**The instance creation time.**', example='2026-04-01 10:00:00'),
      creditPackageId?: string(name='CreditPackageId', description='**The ID of the currently active credit package instance.**', example='cp-inst-001'),
      expiredTime?: string(name='ExpiredTime', description='**The time when the instance expires.**', example='2026-10-01 10:00:00'),
      instanceType?: string(name='InstanceType', description='The instance type.', example='jvs-copilot.standard'),
      totalCredit?: long(name='TotalCredit', description='**The total number of credits.**', example='2000'),
      usedCredit?: long(name='UsedCredit', description='**The amount of credits used.**', example='100'),
      warnPercent?: int32(name='WarnPercent', description='**The alert threshold percentage (0–100).**', example='80'),
    }
  ](name='Agents', description='The Agent list.'),
  maxResults?: int32(name='MaxResults', description='The number of entries per page.', example='20'),
  nextToken?: string(name='NextToken', description='The pagination token. Set this parameter to an empty string for the first request. For subsequent requests, use the `nextToken` value from the previous response.', example='eyJvZmZzZXQiOjIwfQ=='),
  requestId?: string(name='RequestId', description='The request trace ID, which is used for troubleshooting.', example='xxxx-xxxx-xxxx'),
}

model DescribeCreditPackageAgentsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeCreditPackageAgentsResponseBody(name='body'),
}

/**
 * @summary Queries the list of Agents and usage summary information under the current username.
 *
 * @description ## Request description
 * - **Paging support**: Use the `NextToken` and `MaxResults` parameters for paging. Set `NextToken` to an empty character string for the first request.
 * - **Filtering**: Use the `AgentType` and `AgentIds` parameters to filter the returned Agent list.
 * - **Status filtering**: Use the `Status` parameter to filter Agents by status (0: deleted, 1: active).
 * - **Sorting**: Results are sorted by `id` in ascending order by default.
 * - **Additional parameter for anonymous edition**: The `FillInstance` parameter automatically populates the bound JVS_COPILOT AgentId of the currently logged-on user.
 *
 * @param request DescribeCreditPackageAgentsRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeCreditPackageAgentsResponse
 */
@context("describeCreditPackageAgentsWithContext")
async function describeCreditPackageAgentsWithOptions(request: DescribeCreditPackageAgentsRequest, runtime: $RuntimeOptions): DescribeCreditPackageAgentsResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.agentIds)) {
    query['AgentIds'] = request.agentIds;
  }
  if (!$isNull(request.agentType)) {
    query['AgentType'] = request.agentType;
  }
  if (!$isNull(request.bizType)) {
    query['BizType'] = request.bizType;
  }
  if (!$isNull(request.maxResults)) {
    query['MaxResults'] = request.maxResults;
  }
  if (!$isNull(request.nextToken)) {
    query['NextToken'] = request.nextToken;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeCreditPackageAgents',
    version = '2021-12-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the list of Agents and usage summary information under the current username.
 *
 * @description ## Request description
 * - **Paging support**: Use the `NextToken` and `MaxResults` parameters for paging. Set `NextToken` to an empty character string for the first request.
 * - **Filtering**: Use the `AgentType` and `AgentIds` parameters to filter the returned Agent list.
 * - **Status filtering**: Use the `Status` parameter to filter Agents by status (0: deleted, 1: active).
 * - **Sorting**: Results are sorted by `id` in ascending order by default.
 * - **Additional parameter for anonymous edition**: The `FillInstance` parameter automatically populates the bound JVS_COPILOT AgentId of the currently logged-on user.
 *
 * @param request DescribeCreditPackageAgentsRequest
 * @return DescribeCreditPackageAgentsResponse
 */
async function describeCreditPackageAgents(request: DescribeCreditPackageAgentsRequest): DescribeCreditPackageAgentsResponse {
  var runtime = new $RuntimeOptions{};
  return describeCreditPackageAgentsWithOptions(request, runtime);
}

model DescribeCreditUsageInfoRequest {
  bizType?: string(name='BizType', description='The business type.', example='BUSINESS'),
  instanceIds?: [ string ](name='InstanceIds', description='The JSON string of instance IDs. This parameter can be omitted when `UsageType=User`. Pass a credit package instance ID when `UsageType=CreditPackage`, or pass an `AgentId` when `UsageType=Agent`.'),
  usageType?: string(name='UsageType', description='The usage type (UsageType). Valid values:

*   User: Returns the usage, remaining credits, and consumption trends of the current user\\\\"s active credit packages.
*   CreditPackage: Requires a CreditPackageId. Returns the total and remaining credits of the specified credit package.
*   Agent: Requires an AgentId. Returns the cumulative credit usage, cumulative allocated quota, and their percentages.', example='User'),
}

model DescribeCreditUsageInfoResponseBody = {
  requestId?: string(name='RequestId', description='The request ID.', example='68BD3312-53D8-123E-BB32-1A9F25E07A03'),
  usageInfoList?: [ 
    {
      usageInfo?: {
        availableAmount?: int32(name='AvailableAmount'),
        contactGroupNames?: [ string ](name='ContactGroupNames'),
        creditTrendList?: [ 
          {
            timePoint?: string(name='TimePoint', description='The time point in the format `yyyy-MM-dd HH` (accurate to the hour).', example='2026-05-02 10'),
            usedCredit?: long(name='UsedCredit', description='The number of credits consumed in this hour.', example='12'),
          }
        ](name='CreditTrendList', description='The hourly consumption samples of the current credit package.'),
        currentInstanceExpiredTime?: string(name='CurrentInstanceExpiredTime'),
        currentInstanceId?: string(name='CurrentInstanceId', description='The instance ID of the current active credit package.', example='cp-inst-001'),
        currentRemainCredit?: long(name='CurrentRemainCredit', description='The remaining credits of the current active credit package.', example='Current period remaining credits'),
        currentTotalCredit?: long(name='CurrentTotalCredit', description='The total credits of the current active credit package.', example='Current period credit quota'),
        currentUsedCredit?: long(name='CurrentUsedCredit', description='The used credits of the current active credit package.', example='Current period credits consumed'),
        dayUsedCredit?: long(name='DayUsedCredit', description='The credit usage in the last 1 day.', example='Credits consumed in the last day'),
        lastTriggeredAt?: string(name='LastTriggeredAt'),
        periodTotalCredit?: long(name='PeriodTotalCredit', description='The shared credit quota in the current active period.', example='300'),
        periodUsedCredit?: long(name='PeriodUsedCredit', description='The shared credit usage in the current active period.', example='120'),
        remainCredit?: long(name='RemainCredit', description='The cumulative remaining credits.', example='Remaining credits'),
        remainCreditInfo?: {
          deductingAmount?: int32(name='DeductingAmount'),
          pendingAmount?: int32(name='PendingAmount'),
        }(name='RemainCreditInfo'),
        todayUsed?: string(name='TodayUsed', description='The quota used today.'),
        totalCredit?: long(name='TotalCredit', description='The total cumulative credits.', example='Credit quota'),
        totalUsed?: string(name='TotalUsed', description='The cumulative used quota.'),
        totalUsedCredit?: long(name='TotalUsedCredit', description='The cumulative credit usage.', example='Total credits consumed'),
        warnPercent?: int32(name='WarnPercent', description='The alert threshold percentage (0–100).', example='80'),
        weekUsedCredit?: long(name='WeekUsedCredit', description='The credit usage in the last 1 week.', example='Credits consumed in the last week'),
      }(name='UsageInfo', description='The usage data details.'),
      usageInfoKey?: string(name='UsageInfoKey', description='The usage primary key. When `UsageType=User`, this is the `aliUid`. When `UsageType=CreditPackage`, this is the credit package instance ID. When `UsageType=Agent`, this is the `AgentId`.', example='agent-abc'),
    }
  ](name='UsageInfoList', description='The array of usage data.'),
}

model DescribeCreditUsageInfoResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeCreditUsageInfoResponseBody(name='body'),
}

/**
 * @summary Queries credit usage details by a specified dimension such as user, credit package, or agent.
 *
 * @description ## Request description
 * This API queries credit usage details based on the dimension specified by `UsageType` (User / CreditPackage / Agent). The response includes the total credits, remaining credits, used credits, hourly consumption samples, alert thresholds, and period quotas of the current credit package.
 * - **User**: User dimension. Returns the aggregated usage and remaining credits of all active credit packages for the current user.
 * - **CreditPackage**: Credit package dimension. Returns the total credits, remaining credits, and consumption samples of a specified credit package instance.
 * - **Agent**: Agent dimension. Returns the cumulative usage, current period usage, quota, and alert information of a specified agent.
 * **Notes**:
 * - The `InstanceIds` parameter can be omitted when `UsageType=User`. Pass a credit package instance ID when `UsageType=CreditPackage`, or pass an AgentId when `UsageType=Agent`.
 * - Anonymous requests support the `FillInstance` parameter. When `InstanceIds` is not explicitly provided and `FillInstance=true`, the server automatically populates the bound `JVS_COPILOT` AgentId based on the current logon `wyId`.
 * - Time window constants: The `dayUsedCredit` statistics window is `now - ONE_DAY_MILLIS`, and the `weekUsedCredit` statistics window is `now - ONE_WEEK_MILLIS`.
 * - The consumption samples in `currentCreditConsumeList` are aggregated by hour and may have an asynchronous synchronization delay of up to 5 minutes.
 *
 * @param request DescribeCreditUsageInfoRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeCreditUsageInfoResponse
 */
@context("describeCreditUsageInfoWithContext")
async function describeCreditUsageInfoWithOptions(request: DescribeCreditUsageInfoRequest, runtime: $RuntimeOptions): DescribeCreditUsageInfoResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.bizType)) {
    query['BizType'] = request.bizType;
  }
  if (!$isNull(request.instanceIds)) {
    query['InstanceIds'] = request.instanceIds;
  }
  if (!$isNull(request.usageType)) {
    query['UsageType'] = request.usageType;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeCreditUsageInfo',
    version = '2021-12-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries credit usage details by a specified dimension such as user, credit package, or agent.
 *
 * @description ## Request description
 * This API queries credit usage details based on the dimension specified by `UsageType` (User / CreditPackage / Agent). The response includes the total credits, remaining credits, used credits, hourly consumption samples, alert thresholds, and period quotas of the current credit package.
 * - **User**: User dimension. Returns the aggregated usage and remaining credits of all active credit packages for the current user.
 * - **CreditPackage**: Credit package dimension. Returns the total credits, remaining credits, and consumption samples of a specified credit package instance.
 * - **Agent**: Agent dimension. Returns the cumulative usage, current period usage, quota, and alert information of a specified agent.
 * **Notes**:
 * - The `InstanceIds` parameter can be omitted when `UsageType=User`. Pass a credit package instance ID when `UsageType=CreditPackage`, or pass an AgentId when `UsageType=Agent`.
 * - Anonymous requests support the `FillInstance` parameter. When `InstanceIds` is not explicitly provided and `FillInstance=true`, the server automatically populates the bound `JVS_COPILOT` AgentId based on the current logon `wyId`.
 * - Time window constants: The `dayUsedCredit` statistics window is `now - ONE_DAY_MILLIS`, and the `weekUsedCredit` statistics window is `now - ONE_WEEK_MILLIS`.
 * - The consumption samples in `currentCreditConsumeList` are aggregated by hour and may have an asynchronous synchronization delay of up to 5 minutes.
 *
 * @param request DescribeCreditUsageInfoRequest
 * @return DescribeCreditUsageInfoResponse
 */
async function describeCreditUsageInfo(request: DescribeCreditUsageInfoRequest): DescribeCreditUsageInfoResponse {
  var runtime = new $RuntimeOptions{};
  return describeCreditUsageInfoWithOptions(request, runtime);
}

model DescribeDeductionStatisticRequest {
  endTime?: long(name='EndTime', description='The end time (timestamp).', example='2025-06-06T02:14:00Z'),
  instanceIds?: [ string ](name='InstanceIds', description='The list of instance IDs (JSON array).'),
  periods?: [ 
    {
      baseTime?: string(name='BaseTime', description='The base time.', example='2024-01-01'),
      periodUnit?: string(name='PeriodUnit', description='The time period unit. For more information, see the `PeriodUnit` enumeration.', example='Year'),
    }
  ](name='Periods', description='The list of time periods in JSON array format. Each element is a `PeriodParam` object.', nullable=true),
  resourceTypes?: [ string ](name='ResourceTypes', description='The list of resource types in JSON array format.'),
  startTime?: long(name='StartTime', description='The start time (timestamp).', example='2026-03-20T16:00:00Z'),
}

model DescribeDeductionStatisticResponseBody = {
  data?: {
    availableCorePackages?: [ 
      {
        aliUid?: long(name='AliUid', description='The Alibaba Cloud UID.', example='1568857911493620'),
        createTime?: string(name='CreateTime', description='The creation time.', example='2024-01-01 00:00:00'),
        endTime?: string(name='EndTime', description='The end time.', example='2024-12-31 23:59:59'),
        expiredTime?: string(name='ExpiredTime', description='The expiration time.', example='2024-12-31 23:59:59'),
        groupId?: string(name='GroupId', description='The group ID.', example='g-xxxxx'),
        groupResourceType?: string(name='GroupResourceType', description='The group resource type.', example='CoreHour'),
        noLx?: boolean(name='NoLx', description='Indicates whether LingXiao is excluded.'),
        noLxSource?: string(name='NoLxSource', description='The source when LingXiao is excluded.', example='无凌霄来源示例值'),
        resourceId?: string(name='ResourceId', description='The resource ID.', example='res-xxxxx'),
        resourceType?: string(name='ResourceType', description='The group resource type.', example='CoreHour'),
        startTime?: string(name='StartTime', description='The start time.', example='2024-01-01 00:00:00'),
        status?: string(name='Status', description='The status.', example='Active'),
        totalTime?: long(name='TotalTime', description='The total duration.', example='1000000'),
        usedTime?: long(name='UsedTime', description='The used duration.', example='40000'),
      }
    ](name='AvailableCorePackages', description='**The list of available core-hour package resources.**'),
    deductions?: [ 
      {
        consumeSecond?: long(name='ConsumeSecond', description='The consumption duration, in seconds.', example='7000'),
        deductionDate?: string(name='DeductionDate', description='The deduction date.', example='2024-01-01'),
        resourceType?: string(name='ResourceType', description='The resource type.', example='CloudDesktop'),
      }
    ](name='Deductions', description='The deduction details.'),
    usages?: [ 
      {
        consumeSecond?: long(name='ConsumeSecond', description='**The consumption duration, in seconds.**', example='6000'),
        period?: string(name='Period', description='**The time period.**', example='2024-01-01'),
        resourceType?: string(name='ResourceType', description='**The resource type.**', example='CloudDesktop'),
      }
    ](name='Usages', description='**The usage list.**'),
  }(name='Data', description='The deduction data.'),
  requestId?: string(name='RequestId', description='The request ID.', example='A1B2C3D4-xxxx-xxxx-xxxx-xxxxxxxxxxxx'),
}

model DescribeDeductionStatisticResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeDeductionStatisticResponseBody(name='body'),
}

/**
 * @summary Retrieves resource deduction and usage statistics based on specified conditions.
 *
 * @description ## Request description
 * - This operation supports GET and POST methods.
 * - The `periods` parameter is in JSON array format. Each element is a `PeriodParam` object that contains the `periodUnit` and `baseTime` fields.
 * - The `resourceTypes` parameter is in JSON array format and contains multiple resource type strings.
 * - The `startTime` and `endTime` parameters are in timestamp format and specify the time range for the query.
 * - Pagination parameters include `nextToken`, `maxResults`, `pageNo`, and `pageSize`, which control the number and pagination of returned results.
 * - When the `resourceTypes` parameter contains invalid values or the `periods` parameter fails JSON parsing, a `SalesClientException` exception is thrown.
 *
 * @param request DescribeDeductionStatisticRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeDeductionStatisticResponse
 */
@context("describeDeductionStatisticWithContext")
async function describeDeductionStatisticWithOptions(request: DescribeDeductionStatisticRequest, runtime: $RuntimeOptions): DescribeDeductionStatisticResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.endTime)) {
    query['EndTime'] = request.endTime;
  }
  if (!$isNull(request.instanceIds)) {
    query['InstanceIds'] = request.instanceIds;
  }
  if (!$isNull(request.periods)) {
    query['Periods'] = request.periods;
  }
  if (!$isNull(request.resourceTypes)) {
    query['ResourceTypes'] = request.resourceTypes;
  }
  if (!$isNull(request.startTime)) {
    query['StartTime'] = request.startTime;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeDeductionStatistic',
    version = '2021-12-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves resource deduction and usage statistics based on specified conditions.
 *
 * @description ## Request description
 * - This operation supports GET and POST methods.
 * - The `periods` parameter is in JSON array format. Each element is a `PeriodParam` object that contains the `periodUnit` and `baseTime` fields.
 * - The `resourceTypes` parameter is in JSON array format and contains multiple resource type strings.
 * - The `startTime` and `endTime` parameters are in timestamp format and specify the time range for the query.
 * - Pagination parameters include `nextToken`, `maxResults`, `pageNo`, and `pageSize`, which control the number and pagination of returned results.
 * - When the `resourceTypes` parameter contains invalid values or the `periods` parameter fails JSON parsing, a `SalesClientException` exception is thrown.
 *
 * @param request DescribeDeductionStatisticRequest
 * @return DescribeDeductionStatisticResponse
 */
async function describeDeductionStatistic(request: DescribeDeductionStatisticRequest): DescribeDeductionStatisticResponse {
  var runtime = new $RuntimeOptions{};
  return describeDeductionStatisticWithOptions(request, runtime);
}

model DescribeDeliveryAddressResponseBody = {
  addresses?: [ 
    {
      area?: {
        areaId?: long(name='AreaId', description='The area ID.', example='33****'),
        areaName?: string(name='AreaName', description='The area name.', example='西湖区'),
      }(name='Area', description='The area.'),
      city?: {
        cityId?: long(name='CityId', description='The city ID.', example='33****'),
        cityName?: string(name='CityName', description='The city name.', example='杭州市'),
      }(name='City', description='The city.'),
      contacts?: string(name='Contacts', description='The contact name.', example='Alice'),
      defaultAddress?: boolean(name='DefaultAddress', description='Indicates whether this is the default address.', example='true'),
      detail?: string(name='Detail', description='The detailed address, which should include the street name and house number.', example='灯彩街1008号云园区2号楼测试中心'),
      mobile?: string(name='Mobile', description='The mobile number.', example='1381111****'),
      postalCode?: string(name='PostalCode', description='The postal code.', example='03****'),
      province?: {
        provinceId?: long(name='ProvinceId', description='The province ID. For more information, see [China regions, countries and territories codes](https://help.aliyun.com/document_detail/167926.html).

For example, **110000** represents Beijing and **120000** represents Tianjin.', example='330000'),
        provinceName?: string(name='ProvinceName', description='The province name.', example='浙江省'),
      }(name='Province', description='The province.'),
      town?: {
        townId?: long(name='TownId', description='The town ID.', example='3001****'),
        townName?: string(name='TownName', description='The town name.', example='三墩镇'),
      }(name='Town', description='The town.'),
    }
  ](name='Addresses', description='The address list.'),
  requestId?: string(name='RequestId', description='The request ID. Alibaba Cloud generates this unique identifier for each request.', example='72481C12-69AB-5CE1-8A35-A8EFA921****'),
  totalCount?: int32(name='TotalCount', description='The total count of addresses.

> Up to 20 addresses are supported.', example='6'),
}

model DescribeDeliveryAddressResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeDeliveryAddressResponseBody(name='body'),
}

/**
 * @summary Retrieves information about delivery addresses.
 *
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeDeliveryAddressResponse
 */
async function describeDeliveryAddressWithOptions(runtime: $RuntimeOptions): DescribeDeliveryAddressResponse {
  var req = new OpenApiUtil.OpenApiRequest{};
  var params = new OpenApiUtil.Params{
    action = 'DescribeDeliveryAddress',
    version = '2021-12-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves information about delivery addresses.
 *
 * @return DescribeDeliveryAddressResponse
 */
async function describeDeliveryAddress(): DescribeDeliveryAddressResponse {
  var runtime = new $RuntimeOptions{};
  return describeDeliveryAddressWithOptions(runtime);
}

model DescribeMultiPriceRequest {
  orderItems?: [ 
    {
      amount?: int32(name='Amount', description='The quantity to purchase.', example='1'),
      components?: [ 
        {
          key?: string(name='Key', description='The key of the module.', example='RegionId'),
          value?: string(name='Value', description='The value of the module.', example='cn-shanghai'),
        }
      ](name='Components', description='The list of product modules.'),
      data?: string(name='Data', description='The extended properties.'),
      instanceIds?: [ string ](name='InstanceIds', description='The list of instance IDs.'),
      period?: int32(name='Period', description='The subscription duration. Valid values:', example='1'),
      periodUnit?: string(name='PeriodUnit', description='The unit of the subscription duration.', example='Year'),
      promotionId?: string(name='PromotionId', description='The promotion ID.', example='youhuiquan_promotion_option_id_for_blank'),
      resourceIds?: [ string ](name='ResourceIds', description='The list of resource IDs.'),
      resourceType?: string(name='ResourceType', description='The resource type.', example='DurationPackage'),
      savingPlanPeriod?: string(name='SavingPlanPeriod', description='The period of the savings plan.'),
    }
  ](name='OrderItems', description='The product information.'),
  orderType?: string(name='OrderType', description='The order type.', example='create'),
  packageCode?: string(name='PackageCode', description='The package code. You do not need to specify this parameter if the product is not a package.', example='pacakge'),
  resellerOwnerUid?: long(name='ResellerOwnerUid', description='The user ID of resource ownership in the reselling pattern. You do not need to specify this parameter if the product is not in the reselling pattern.', example='182864463481****'),
}

model DescribeMultiPriceResponseBody = {
  priceInfo?: {
    price?: {
      currency?: string(name='Currency', description='The currency unit.', example='CNY'),
      discountPrice?: float(name='DiscountPrice', description='The discount price.', example='534.6'),
      originalPrice?: float(name='OriginalPrice', description='The original price.', example='6800'),
      priceDetails?: [ 
        {
          moduleDetails?: [ 
            {
              discountPrice?: float(name='DiscountPrice', description='The discount price.', example='734.65'),
              moduleCode?: string(name='ModuleCode', description='The module code.', example='DesktopType'),
              moduleName?: string(name='ModuleName', description='The module name.', example='Enterprise Office-8C32G'),
              moduleValue?: string(name='ModuleValue', description='The module value.', example='eds.enterprise_office.8c32g'),
              originalPrice?: float(name='OriginalPrice', description='The original price.', example='10900'),
              savingPlanDiscountPrice?: float(name='SavingPlanDiscountPrice', description='The savings plan discount price.'),
              tradePrice?: float(name='TradePrice', description='The actual payment price. The value is the original price minus the discount.', example='292.2'),
            }
          ](name='ModuleDetails', description='The pricing module details.'),
          orderItem?: int32(name='OrderItem', description='The sort property.', example='1'),
          priceDetail?: {
            discountPrice?: float(name='DiscountPrice', description='The discount price.', example='20.00'),
            originalPrice?: float(name='OriginalPrice', description='The original price.', example='100.00'),
            resourceType?: string(name='ResourceType', description='The resource type.', example='DurationPackage'),
            savingPlanRecommendPrice?: float(name='SavingPlanRecommendPrice', description='The savings plan recommended price.'),
            tradePrice?: float(name='TradePrice', description='The actual payment price. The value is the original price minus the discount.', example='80.00'),
          }(name='PriceDetail', description='The price details.'),
        }
      ](name='PriceDetails', description='The price details.'),
      promotions?: [ 
        {
          activityId?: string(name='ActivityId', description='The activity ID.'),
          effective?: boolean(name='Effective', description='Indicates whether the activity is effective.'),
          endTimestamp?: string(name='EndTimestamp', description='The timestamp when the activity ends.'),
          optionCode?: string(name='OptionCode', description='The description of the promotion rule.', example='new'),
          priceBreakReduceValue?: string(name='PriceBreakReduceValue', description='The tiered pricing discount value.'),
          priceBreakThreshold?: string(name='PriceBreakThreshold', description='The tiered pricing threshold.'),
          promotionDesc?: string(name='PromotionDesc', description='The description of the promotion.', example='Promotion'),
          promotionId?: string(name='PromotionId', description='The promotion ID.', example='youhuiquan_promotion_option_id_for_blank'),
          promotionName?: string(name='PromotionName', description='The promotion name.', example='Promotion name'),
          selected?: boolean(name='Selected', description='Indicates whether the promotion is selected.', example='true'),
          startTimestamp?: string(name='StartTimestamp', description='The timestamp when the activity starts.'),
        }
      ](name='Promotions', description='The promotion information.'),
      refundInstanceIdPriceMap?: map[string]float(name='RefundInstanceIdPriceMap', description='The unsubscription instance and pricing details.'),
      refundPrice?: float(name='RefundPrice', description='The unsubscription price.', example='60.00'),
      tradePrice?: float(name='TradePrice', description='The actual payment price. The value is the original price minus the discount.', example='82.6'),
    }(name='Price', description='The price information.'),
    rules?: [ 
      {
        description?: string(name='Description', description='The description of the promotion rule.', example='accounts_suspect_users'),
        ruleId?: long(name='RuleId', description='The promotion rule ID.', example='hrzdvc'),
      }
    ](name='Rules', description='The promotion rule information.'),
  }(name='PriceInfo', description='The price details.'),
  requestId?: string(name='RequestId', description='The request ID.', example='833C4D2C-09C7-5CE6-8159-06758B964970'),
}

model DescribeMultiPriceResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeMultiPriceResponseBody(name='body'),
}

/**
 * @summary Queries the prices of Elastic Desktop Service products, including prices for new purchases, renewals, specification changes, and unsubscriptions.
 *
 * @description <props="china">Before you call this operation, make sure that you fully understand the billing of Elastic Desktop Service and its [pricing](https://www.aliyun.com/price/product?#/gws/detail/gws).
 *
 * @param request DescribeMultiPriceRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeMultiPriceResponse
 */
@context("describeMultiPriceWithContext")
async function describeMultiPriceWithOptions(request: DescribeMultiPriceRequest, runtime: $RuntimeOptions): DescribeMultiPriceResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.orderItems)) {
    query['OrderItems'] = request.orderItems;
  }
  if (!$isNull(request.orderType)) {
    query['OrderType'] = request.orderType;
  }
  if (!$isNull(request.packageCode)) {
    query['PackageCode'] = request.packageCode;
  }
  if (!$isNull(request.resellerOwnerUid)) {
    query['ResellerOwnerUid'] = request.resellerOwnerUid;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeMultiPrice',
    version = '2021-12-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the prices of Elastic Desktop Service products, including prices for new purchases, renewals, specification changes, and unsubscriptions.
 *
 * @description <props="china">Before you call this operation, make sure that you fully understand the billing of Elastic Desktop Service and its [pricing](https://www.aliyun.com/price/product?#/gws/detail/gws).
 *
 * @param request DescribeMultiPriceRequest
 * @return DescribeMultiPriceResponse
 */
async function describeMultiPrice(request: DescribeMultiPriceRequest): DescribeMultiPriceResponse {
  var runtime = new $RuntimeOptions{};
  return describeMultiPriceWithOptions(request, runtime);
}

model DescribePackageDeductionsRequest {
  endTime?: long(name='EndTime', description='The end time in UNIX timestamp format.', example='1709117383000'),
  instanceIds?: [ string ](name='InstanceIds', description='The list of cloud computer IDs. If this parameter is specified, the `PackageIds` parameter is required.'),
  packageIds?: [ string ](name='PackageIds', description='The list of core-hour package IDs in JSON format.'),
  pageNum?: int32(name='PageNum', description='The current page number. Default value: 1.', example='1'),
  pageSize?: int32(name='PageSize', description='The number of entries per page in a paged query. Settings the number of rows per page for paging. Maximum value: 100.', example='100'),
  resourceType?: string(name='ResourceType', description='The resource type.', example='Desktop'),
  resourceTypes?: [ string ](name='ResourceTypes', description='The list of resource types.'),
  startTime?: long(name='StartTime', description='The start time in UNIX timestamp format.', example='1709117383000'),
}

model DescribePackageDeductionsResponseBody = {
  deductions?: [ 
    {
      cpu?: int32(name='Cpu', description='The number of vCPUs.', example='4'),
      desktopId?: string(name='DesktopId', description='The cloud computer ID.', example='ecd-6wye9optu0kag****'),
      desktopName?: string(name='DesktopName', description='The name of the cloud computer.', example='DemoComputer'),
      desktopType?: string(name='DesktopType', description='The specifications of the cloud computer.', example='eds.enterprise_office.4c8g'),
      endTime?: string(name='EndTime', description='The end time of the period.', example='2024-07-31T03:00Z'),
      groupResourceType?: string(name='GroupResourceType', description='The group resource type.'),
      instanceId?: string(name='InstanceId', description='The instance ID.'),
      instanceState?: string(name='InstanceState', description='The status of the core-hour package.', example='Deleted'),
      instanceType?: string(name='InstanceType', description='The instance type.'),
      memory?: long(name='Memory', description='The memory size. Unit: MB.', example='8192'),
      osType?: string(name='OsType', description='The operating system type of the cloud computer.', example='Windows'),
      regionId?: string(name='RegionId', description='The region ID of the cloud computer.', example='cn-hangzhou'),
      resourceType?: string(name='ResourceType', description='The resource type.', example='Desktop'),
      sessionId?: string(name='SessionId', description='The session ID.'),
      staTime?: string(name='StaTime', description='The start time of the period.', example='2024-07-31T02:00Z'),
      usedCoreTime?: float(name='UsedCoreTime', description='The number of cores deducted in this period.', example='4.0'),
      usedTime?: long(name='UsedTime', description='The usage duration. Unit: seconds.', example='3600'),
      usedTimeDecimal?: string(name='UsedTimeDecimal', description='The usage duration in decimal format.'),
      usedTimeWithScale?: long(name='UsedTimeWithScale', description='The usage duration after proportional scaling.'),
    }
  ](name='Deductions', description='The list of deduction details.'),
  pageNum?: int32(name='PageNum', description='The current page number.', example='1'),
  pageSize?: int32(name='PageSize', description='The number of entries per page in a paged query. Settings the number of rows per page for paging.', example='100'),
  requestId?: string(name='RequestId', description='The request ID.', example='833C4D2C-09C7-5CE6-8159-06758B964970'),
  totalCount?: long(name='TotalCount', description='The total number of deduction detail records returned.', example='100'),
  totalUsedCoreTime?: float(name='TotalUsedCoreTime', description='The total deducted core-hours.', example='10.1'),
  totalUsedTime?: long(name='TotalUsedTime', description='The total usage duration.', example='100000'),
  totalUsedTimeDecimal?: string(name='TotalUsedTimeDecimal', description='The total usage duration in decimal format.'),
}

model DescribePackageDeductionsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribePackageDeductionsResponseBody(name='body'),
}

/**
 * @summary Queries the deduction details of a core-hour package.
 *
 * @param request DescribePackageDeductionsRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribePackageDeductionsResponse
 */
@context("describePackageDeductionsWithContext")
async function describePackageDeductionsWithOptions(request: DescribePackageDeductionsRequest, runtime: $RuntimeOptions): DescribePackageDeductionsResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.endTime)) {
    query['EndTime'] = request.endTime;
  }
  if (!$isNull(request.instanceIds)) {
    query['InstanceIds'] = request.instanceIds;
  }
  if (!$isNull(request.packageIds)) {
    query['PackageIds'] = request.packageIds;
  }
  if (!$isNull(request.pageNum)) {
    query['PageNum'] = request.pageNum;
  }
  if (!$isNull(request.pageSize)) {
    query['PageSize'] = request.pageSize;
  }
  if (!$isNull(request.resourceType)) {
    query['ResourceType'] = request.resourceType;
  }
  if (!$isNull(request.resourceTypes)) {
    query['ResourceTypes'] = request.resourceTypes;
  }
  if (!$isNull(request.startTime)) {
    query['StartTime'] = request.startTime;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribePackageDeductions',
    version = '2021-12-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the deduction details of a core-hour package.
 *
 * @param request DescribePackageDeductionsRequest
 * @return DescribePackageDeductionsResponse
 */
async function describePackageDeductions(request: DescribePackageDeductionsRequest): DescribePackageDeductionsResponse {
  var runtime = new $RuntimeOptions{};
  return describePackageDeductionsWithOptions(request, runtime);
}

model DescribeRunIdDeductionsRequest {
  agentType?: string(name='AgentType', description='The agent type: `CREDIT_PACKAGE` / `JVS_CLAW` / `OPEN_CLAW` / `JVS_COPILOT`.', example='JVSCopilot、JVSClaw、OpenClaw'),
  agentTypes?: [ string ](name='AgentTypes'),
  aliUid?: long(name='AliUid', description='The Alibaba Cloud UID.', example='1457450820614624'),
  bizType?: string(name='BizType', description='The business type.', example='ENTERPRISE、BUSINESS'),
  deductionTypes?: [ string ](name='DeductionTypes', description='The deduction types. Do not specify this parameter for non-knowledge base scenarios.'),
  endTime?: long(name='EndTime', description='The end time of the period.', example='Millisecond timestamp: 1785205179000'),
  groupByFields?: [ string ](name='GroupByFields'),
  groupResourceTypes?: [ string ](name='GroupResourceTypes'),
  groupSeparator?: boolean(name='GroupSeparator', description='Specifies whether to group results by deduction type.', example='false'),
  instanceIdType?: string(name='InstanceIdType', description='The instance ID type. Do not specify this parameter for non-knowledge base scenarios.', example='KnowledgeSpaceId、AgentId'),
  instanceIds?: [ string ](name='InstanceIds', description='The list of cloud computer IDs. If this field has a value, the `PackageIds` field is required.'),
  maxResults?: int32(name='MaxResults', description='The number of entries per page.', example='20'),
  nextToken?: string(name='NextToken', description='The pagination token. Leave this parameter empty for the first request. For subsequent requests, use the `nextToken` value from the previous response.', example='eyJvZmZzZXQiOjIwfQ=='),
  packageIds?: [ string ](name='PackageIds', description='The list of core-hour package IDs in JSON format.'),
  pageNum?: int32(name='PageNum', description='The page number. Default value: 1.', example='1'),
  pageSize?: int32(name='PageSize', description='The number of entries per page for a paged query.', example='100'),
  resourceType?: string(name='ResourceType', description='The resource type.', example='Enterprise Edition: CreditPackage, Commercial Edition: BusinessCreditPackage'),
  resourceTypes?: [ string ](name='ResourceTypes', description='The list of resource types in JSON array format.'),
  startTime?: long(name='StartTime', description='The start time.', example='Millisecond timestamp: 1785205179000'),
  wyId?: string(name='WyId'),
}

model DescribeRunIdDeductionsResponseBody = {
  deductions?: [ 
    {
      agentType?: string(name='AgentType', description='The agent type: `CREDIT_PACKAGE` / `JVS_CLAW` / `OPEN_CLAW` / `JVS_COPILOT`.', example='OpenClaw'),
      endTime?: string(name='EndTime', description='The end time of the period.', example='2024-07-31T03:00Z'),
      groupResourceType?: string(name='GroupResourceType', description='The group resource type.', example='GROUP_CREDIT_PACKAGE、GROUP_BUSINESS_CREDIT_PACKAGE'),
      instanceId?: string(name='InstanceId', description='The instance ID.', example='jvs-xxxx'),
      model?: string(name='Model'),
      packageId?: string(name='PackageId', description='The credit or plan package ID.', example='crp-xxx'),
      resourceId?: string(name='ResourceId', description='The resource ID.', example='sunwyic.com'),
      resourceType?: string(name='ResourceType', description='The resource type.', example='CreditPackage、BusinessCreditPackage'),
      runId?: string(name='RunId', description='The unique run ID.', example='run-szwB1fYHCTocjGkFAIf6V8A'),
      startTime?: string(name='StartTime', description='The start time.', example='2025-12-16T02:10:00Z'),
      summary?: string(name='Summary', description='The summary of the large language model call.', example='How is the weather today'),
      usedTime?: long(name='UsedTime', description='The usage duration. Unit: seconds. Do not use this field for AI scenarios.', example='360000000'),
      usedTimeDecimal?: string(name='UsedTimeDecimal', description='The credits used.', example='1.23'),
    }
  ](name='Deductions', description='The deduction details.'),
  maxResults?: int32(name='MaxResults', description='The number of entries per page.', example='20'),
  nextToken?: string(name='NextToken', description='The pagination token. Leave this parameter empty for the first request. For subsequent requests, use the `nextToken` value from the previous response.', example='eyJvZmZzZXQiOjIwfQ=='),
  pageNum?: int32(name='PageNum', description='The page number. Default value: 1.', example='1'),
  pageSize?: int32(name='PageSize', description='The number of entries per page for a paged query.', example='100'),
  requestId?: string(name='RequestId', description='Id of the request', example='68BD3312-53D8-123E-BB32-1A9F25E07A03'),
  totalCount?: long(name='TotalCount', description='The total number of core-hour package deduction details in the query result.', example='100'),
  totalUsedTime?: long(name='TotalUsedTime', description='The total usage duration. Unit: seconds. Do not use this field for AI scenarios.', example='100000'),
  totalUsedTimeDecimal?: string(name='TotalUsedTimeDecimal', description='The total credits used that match the specified conditions.', example='1.23'),
}

model DescribeRunIdDeductionsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeRunIdDeductionsResponseBody(name='body'),
}

/**
 * @summary Queries AI credit deductions.
 *
 * @description ## Operation description
 * - This operation supports GET and POST methods.
 * - The `periods` parameter is in JSON array format. Each element is a `PeriodParam` object that contains the `periodUnit` and `baseTime` fields.
 * - The `resourceTypes` parameter is in JSON array format and contains multiple resource type strings.
 * - The `startTime` and `endTime` parameters are in timestamp format and specify the time range for the query.
 * - Pagination parameters include `nextToken`, `maxResults`, `pageNo`, and `pageSize`, which control the number of returned results and pagination.
 * - When the `resourceTypes` parameter contains invalid values or the `periods` parameter fails JSON parsing, a `SalesClientException` exception is thrown.
 *
 * @param request DescribeRunIdDeductionsRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeRunIdDeductionsResponse
 */
@context("describeRunIdDeductionsWithContext")
async function describeRunIdDeductionsWithOptions(request: DescribeRunIdDeductionsRequest, runtime: $RuntimeOptions): DescribeRunIdDeductionsResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.agentType)) {
    query['AgentType'] = request.agentType;
  }
  if (!$isNull(request.agentTypes)) {
    query['AgentTypes'] = request.agentTypes;
  }
  if (!$isNull(request.aliUid)) {
    query['AliUid'] = request.aliUid;
  }
  if (!$isNull(request.bizType)) {
    query['BizType'] = request.bizType;
  }
  if (!$isNull(request.deductionTypes)) {
    query['DeductionTypes'] = request.deductionTypes;
  }
  if (!$isNull(request.endTime)) {
    query['EndTime'] = request.endTime;
  }
  if (!$isNull(request.groupByFields)) {
    query['GroupByFields'] = request.groupByFields;
  }
  if (!$isNull(request.groupResourceTypes)) {
    query['GroupResourceTypes'] = request.groupResourceTypes;
  }
  if (!$isNull(request.groupSeparator)) {
    query['GroupSeparator'] = request.groupSeparator;
  }
  if (!$isNull(request.instanceIdType)) {
    query['InstanceIdType'] = request.instanceIdType;
  }
  if (!$isNull(request.instanceIds)) {
    query['InstanceIds'] = request.instanceIds;
  }
  if (!$isNull(request.maxResults)) {
    query['MaxResults'] = request.maxResults;
  }
  if (!$isNull(request.nextToken)) {
    query['NextToken'] = request.nextToken;
  }
  if (!$isNull(request.packageIds)) {
    query['PackageIds'] = request.packageIds;
  }
  if (!$isNull(request.pageNum)) {
    query['PageNum'] = request.pageNum;
  }
  if (!$isNull(request.pageSize)) {
    query['PageSize'] = request.pageSize;
  }
  if (!$isNull(request.resourceType)) {
    query['ResourceType'] = request.resourceType;
  }
  if (!$isNull(request.resourceTypes)) {
    query['ResourceTypes'] = request.resourceTypes;
  }
  if (!$isNull(request.startTime)) {
    query['StartTime'] = request.startTime;
  }
  if (!$isNull(request.wyId)) {
    query['WyId'] = request.wyId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeRunIdDeductions',
    version = '2021-12-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries AI credit deductions.
 *
 * @description ## Operation description
 * - This operation supports GET and POST methods.
 * - The `periods` parameter is in JSON array format. Each element is a `PeriodParam` object that contains the `periodUnit` and `baseTime` fields.
 * - The `resourceTypes` parameter is in JSON array format and contains multiple resource type strings.
 * - The `startTime` and `endTime` parameters are in timestamp format and specify the time range for the query.
 * - Pagination parameters include `nextToken`, `maxResults`, `pageNo`, and `pageSize`, which control the number of returned results and pagination.
 * - When the `resourceTypes` parameter contains invalid values or the `periods` parameter fails JSON parsing, a `SalesClientException` exception is thrown.
 *
 * @param request DescribeRunIdDeductionsRequest
 * @return DescribeRunIdDeductionsResponse
 */
async function describeRunIdDeductions(request: DescribeRunIdDeductionsRequest): DescribeRunIdDeductionsResponse {
  var runtime = new $RuntimeOptions{};
  return describeRunIdDeductionsWithOptions(request, runtime);
}

model ModifyInstancePropertiesRequest {
  instanceId?: string(name='InstanceId', description='The ID of the instance.', example='mdp-0c62ayep0nk4v****'),
  instanceIds?: [ string ](name='InstanceIds', description='The instance ID.'),
  key?: string(name='Key', description='The key of the attribute.', example='PackageUsedUpStrategy'),
  resourceType?: string(name='ResourceType', description='The resource type.
> This parameter is case-sensitive. Ensure that the value is spelled correctly.

This parameter is required.', example='DurationPackage'),
  value?: string(name='Value', description='The value of the attribute.

- PackageUsedUpStrategy: Valid values:
   - Postpaid: Enters the pay-as-you-go phase.
   - Shutdown: Hibernation.
   - Maintenance: Shuts down and enters O&M mode. Client connections are not allowed.', example='Postpaid'),
}

model ModifyInstancePropertiesResponseBody = {
  requestId?: string(name='RequestId', description='The request ID.', example='833C4D2C-09C7-5CE6-8159-06758B964970'),
}

model ModifyInstancePropertiesResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ModifyInstancePropertiesResponseBody(name='body'),
}

/**
 * @summary Modifies the attributes of an instance.
 *
 * @param request ModifyInstancePropertiesRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ModifyInstancePropertiesResponse
 */
@context("modifyInstancePropertiesWithContext")
async function modifyInstancePropertiesWithOptions(request: ModifyInstancePropertiesRequest, runtime: $RuntimeOptions): ModifyInstancePropertiesResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.instanceIds)) {
    query['InstanceIds'] = request.instanceIds;
  }
  if (!$isNull(request.key)) {
    query['Key'] = request.key;
  }
  if (!$isNull(request.resourceType)) {
    query['ResourceType'] = request.resourceType;
  }
  if (!$isNull(request.value)) {
    query['Value'] = request.value;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ModifyInstanceProperties',
    version = '2021-12-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Modifies the attributes of an instance.
 *
 * @param request ModifyInstancePropertiesRequest
 * @return ModifyInstancePropertiesResponse
 */
async function modifyInstanceProperties(request: ModifyInstancePropertiesRequest): ModifyInstancePropertiesResponse {
  var runtime = new $RuntimeOptions{};
  return modifyInstancePropertiesWithOptions(request, runtime);
}

model SetAgentCreditQuotaRequest {
  agentIds?: [ string ](name='AgentIds', description='A list of Agent IDs.'),
  agentType?: string(name='AgentType', description='The Agent type.', example='JVSClaw'),
  bizType?: string(name='BizType', description='The business type.', example='BUSINESS'),
  creditQuota?: int32(name='CreditQuota', description='The credit quota.', example='200'),
}

model SetAgentCreditQuotaResponseBody = {
  requestId?: string(name='RequestId', description='The request ID.', example='9B353D6D-53C9-1C24-95D5-2D24596DBCF1'),
}

model SetAgentCreditQuotaResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: SetAgentCreditQuotaResponseBody(name='body'),
}

/**
 * @summary Sets the credit quota for specified Agents.
 *
 * @description ## Description
 * This operation sets the credit quota for one or more Agents of a specific type.
 * ### Usage notes
 * - The `AgentType` parameter specifies the type of Agent to which the quota applies, such as `JVSClaw` or `OpenClaw`.
 * - The `AgentIds` parameter is an array of up to 100 Agent IDs.
 * - The `CreditQuota` parameter specifies the credit quota for each Agent.
 * ### Examples
 *
 * @param request SetAgentCreditQuotaRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return SetAgentCreditQuotaResponse
 */
@context("setAgentCreditQuotaWithContext")
async function setAgentCreditQuotaWithOptions(request: SetAgentCreditQuotaRequest, runtime: $RuntimeOptions): SetAgentCreditQuotaResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.agentIds)) {
    query['AgentIds'] = request.agentIds;
  }
  if (!$isNull(request.agentType)) {
    query['AgentType'] = request.agentType;
  }
  if (!$isNull(request.bizType)) {
    query['BizType'] = request.bizType;
  }
  if (!$isNull(request.creditQuota)) {
    query['CreditQuota'] = request.creditQuota;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'SetAgentCreditQuota',
    version = '2021-12-21',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Sets the credit quota for specified Agents.
 *
 * @description ## Description
 * This operation sets the credit quota for one or more Agents of a specific type.
 * ### Usage notes
 * - The `AgentType` parameter specifies the type of Agent to which the quota applies, such as `JVSClaw` or `OpenClaw`.
 * - The `AgentIds` parameter is an array of up to 100 Agent IDs.
 * - The `CreditQuota` parameter specifies the credit quota for each Agent.
 * ### Examples
 *
 * @param request SetAgentCreditQuotaRequest
 * @return SetAgentCreditQuotaResponse
 */
async function setAgentCreditQuota(request: SetAgentCreditQuotaRequest): SetAgentCreditQuotaResponse {
  var runtime = new $RuntimeOptions{};
  return setAgentCreditQuotaWithOptions(request, runtime);
}

