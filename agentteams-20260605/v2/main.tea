/**
 *
 */
import OpenApi;
import OpenApi.OpenApiUtil;

extends OpenApi;


init(config: OpenApiUtil.Config){
  super(config);
  @endpointRule = 'regional';
  @endpointMap = {
    'cn-beijing' = 'agentteams.cn-beijing.aliyuncs.com',
    'ap-southeast-1' = 'agentteams.ap-southeast-1.aliyuncs.com',
    'cn-hangzhou' = 'agentteams.cn-hangzhou.aliyuncs.com',
  };

  checkConfig(config);
  @endpoint = getEndpoint('agentteams', @regionId, @endpointRule, @network, @suffix, @endpointMap, @endpoint);
}

function getEndpoint(productId: string, regionId: string, endpointRule: string, network: string, suffix: string, endpointMap: map[string]string, endpoint: string) throws: string{
  if (!$isNull(endpoint)) {
    return endpoint;
  }
  
  if (!$isNull(endpointMap) && !$isNull(endpointMap[regionId])) {
    return endpointMap[regionId];
  }
  return OpenApiUtil.getEndpointRules(productId, regionId, endpointRule, network, suffix);
}

model BindIdentityProviderRequest {
  clientToken?: string(name='ClientToken', description='The client idempotency token that is used to ensure the idempotence of the request.', example='ct-1234abcd'),
  identityProviderType?: string(name='IdentityProviderType', description='The identity provider type. Currently, only DingTalk is supported.

This parameter is required.', example='DingTalk'),
  idpMetadata?: string(name='IdpMetadata', description='The identity provider metadata. In the DingTalk scenario, this is a JSON object string that contains the `appKey`, `appSecret`, `corpId`, `encryptKey`, and `verificationToken` fields. This is a sensitive parameter.', example='{"appKey":"dingxxxx","appSecret":"xxxxx","corpId":"dingxxxx","encryptKey":"xxxxx","verificationToken":"xxxxx"}'),
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='agentteams-xxx'),
  loginEnabled?: boolean(name='LoginEnabled', description='Specifies whether to enable logon. Default value: `true`. This default value is used if the parameter is not specified or is left empty.', example='true'),
  syncEnabled?: boolean(name='SyncEnabled', description='Specifies whether to enable user synchronization. Default value: `false`. User synchronization is enabled only when this parameter is explicitly set to `true`.', example='true'),
}

model BindIdentityProviderResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  data?: {
    identityProviderType?: string(name='IdentityProviderType', description='The identity provider type.', example='DingTalk'),
    instanceId?: string(name='InstanceId', description='The instance ID.', example='agentteams-xxx'),
  }(name='Data', description='The response data.', example='{}'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model BindIdentityProviderResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: BindIdentityProviderResponseBody(name='body'),
}

/**
 * @summary Binds an upstream identity provider to a specified instance and triggers a synchronization task.
 *
 * @description ## Operation description
 * - This is an asynchronous operation that immediately returns binding task information after the call.
 * - Use `GetInstanceAsyncTask` to poll for the asynchronous task result. The default polling interval is 30 seconds, with a maximum of 20 attempts.
 * - The `IdpMetadata` parameter contains sensitive information. Ensure secure transmission.
 * - The default values of `LoginEnabled` and `SyncEnabled` are `true` and `false`, respectively. If not explicitly specified, the default values are used.
 *
 * @param request BindIdentityProviderRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return BindIdentityProviderResponse
 */
@context("bindIdentityProviderWithContext")
async function bindIdentityProviderWithOptions(request: BindIdentityProviderRequest, runtime: $RuntimeOptions): BindIdentityProviderResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.identityProviderType)) {
    query['IdentityProviderType'] = request.identityProviderType;
  }
  if (!$isNull(request.idpMetadata)) {
    query['IdpMetadata'] = request.idpMetadata;
  }
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.loginEnabled)) {
    query['LoginEnabled'] = request.loginEnabled;
  }
  if (!$isNull(request.syncEnabled)) {
    query['SyncEnabled'] = request.syncEnabled;
  }
  var body : map[string]any = {};
  if (!$isNull(request.clientToken)) {
    body['ClientToken'] = request.clientToken;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'BindIdentityProvider',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Binds an upstream identity provider to a specified instance and triggers a synchronization task.
 *
 * @description ## Operation description
 * - This is an asynchronous operation that immediately returns binding task information after the call.
 * - Use `GetInstanceAsyncTask` to poll for the asynchronous task result. The default polling interval is 30 seconds, with a maximum of 20 attempts.
 * - The `IdpMetadata` parameter contains sensitive information. Ensure secure transmission.
 * - The default values of `LoginEnabled` and `SyncEnabled` are `true` and `false`, respectively. If not explicitly specified, the default values are used.
 *
 * @param request BindIdentityProviderRequest
 * @return BindIdentityProviderResponse
 */
async function bindIdentityProvider(request: BindIdentityProviderRequest): BindIdentityProviderResponse {
  var runtime = new $RuntimeOptions{};
  return bindIdentityProviderWithOptions(request, runtime);
}

model ConfigureNatGatewayRequest {
  clientToken?: string(name='ClientToken', description='The client idempotency token.', example='test'),
  description?: string(name='Description', description='The description for the NAT gateway, EIP, and SNAT rule creation.', example='create public nat gateway for agentteams instance'),
  eipAllocationId?: string(name='EipAllocationId', description='The allocation ID of the EIP.', example='eip-xxx'),
  eipBandwidth?: int32(name='EipBandwidth', description='The bandwidth of the automatically applied EIP. Valid values: 1 to 200. Unit: Mbit/s.', example='5'),
  instanceId?: string(name='InstanceId', description='The ID of the AgentTeams instance.

This parameter is required.', example='<instanceId>'),
  natGatewayInstanceId?: string(name='NatGatewayInstanceId', description='The ID of the NAT gateway instance.', example='nat-xxxx'),
}

model ConfigureNatGatewayResponseBody = {
  code?: string(name='Code', description='The business status code.', example='SUCCESS'),
  data?: map[string]any(name='Data', description='The data object that contains the AgentTeams instance ID and the asynchronous task ID.', example='{}'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-xxxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model ConfigureNatGatewayResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ConfigureNatGatewayResponseBody(name='body'),
}

/**
 * @summary Activates and configures an Internet NAT gateway for a specified AgentTeams instance.
 *
 * @description ## Operation description
 * - This operation creates an Internet NAT gateway and automatically applies for an elastic IP address (EIP), bindS the EIP, and configures SNAT rules.
 * - An asynchronous task ID is returned after the call. The actual resource ID is provided in the task result.
 * - NAT gateway name format: `magic-create-for-vpc-{vpcId}`.
 * - GET and POST methods are supported.
 * - The `eipBandwidth` parameter ranges from 1 to 200 Mbit/s. Default value: 5 Mbit/s.
 * - If `InstanceId` is invalid or `eipBandwidth` is out of range, the API returns an error response.
 *
 * @param request ConfigureNatGatewayRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ConfigureNatGatewayResponse
 */
@context("configureNatGatewayWithContext")
async function configureNatGatewayWithOptions(request: ConfigureNatGatewayRequest, runtime: $RuntimeOptions): ConfigureNatGatewayResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.description)) {
    query['Description'] = request.description;
  }
  if (!$isNull(request.eipAllocationId)) {
    query['EipAllocationId'] = request.eipAllocationId;
  }
  if (!$isNull(request.eipBandwidth)) {
    query['EipBandwidth'] = request.eipBandwidth;
  }
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.natGatewayInstanceId)) {
    query['NatGatewayInstanceId'] = request.natGatewayInstanceId;
  }
  var body : map[string]any = {};
  if (!$isNull(request.clientToken)) {
    body['ClientToken'] = request.clientToken;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'ConfigureNatGateway',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Activates and configures an Internet NAT gateway for a specified AgentTeams instance.
 *
 * @description ## Operation description
 * - This operation creates an Internet NAT gateway and automatically applies for an elastic IP address (EIP), bindS the EIP, and configures SNAT rules.
 * - An asynchronous task ID is returned after the call. The actual resource ID is provided in the task result.
 * - NAT gateway name format: `magic-create-for-vpc-{vpcId}`.
 * - GET and POST methods are supported.
 * - The `eipBandwidth` parameter ranges from 1 to 200 Mbit/s. Default value: 5 Mbit/s.
 * - If `InstanceId` is invalid or `eipBandwidth` is out of range, the API returns an error response.
 *
 * @param request ConfigureNatGatewayRequest
 * @return ConfigureNatGatewayResponse
 */
async function configureNatGateway(request: ConfigureNatGatewayRequest): ConfigureNatGatewayResponse {
  var runtime = new $RuntimeOptions{};
  return configureNatGatewayWithOptions(request, runtime);
}

model CreateCredentialRequest {
  apiKey?: string(name='ApiKey', description='The credential value. This is a sensitive field and is not returned in plaintext.

This parameter is required.', example='sk-xxxxxxxxxxxx'),
  clientToken?: string(name='ClientToken', description='The idempotency token that is used to ensure the idempotence of the request.', example='a1b2c3d4-e5f6-7890-1234-567890abcdef'),
  description?: string(name='Description', description='The credential description.', example='OpenAI API key for the team'),
  instanceId?: string(name='InstanceId', description='The AgentTeams instance ID.

This parameter is required.', example='ami-2ze8x9c6f'),
  name?: string(name='Name', description='The credential key. The value must match the regular expression `^[A-Z_\\\\][A-Z0-9_\\\\]*\\\\$`.

This parameter is required.', example='OPENAI_API_KEY'),
}

model CreateCredentialResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  data?: {
    createTime?: string(name='CreateTime', description='The creation time in ISO 8601 format.', example='2026-07-13T12:00:00Z'),
    description?: string(name='Description', description='The credential description.', example='OpenAI API key for the team'),
    instanceId?: string(name='InstanceId', description='The instance ID.', example='ami-2ze8x9c6f'),
    name?: string(name='Name', description='The credential key.', example='OPENAI_API_KEY'),
    status?: string(name='Status', description='The resource status.', example='Active'),
    updateTime?: string(name='UpdateTime', description='The update time in ISO 8601 format.', example='2026-07-13T12:00:00Z'),
  }(name='Data', description='The returned data.', example='{}'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model CreateCredentialResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateCredentialResponseBody(name='body'),
}

/**
 * @summary Creates a new key-value credential under a specified AgentTeams instance.
 *
 * @description ## Operation description
 * - `ApiKey` is a sensitive field and is not returned in plaintext.
 * - `ClientToken` is used to ensure idempotence of the request. This parameter is optional but recommended.
 * - The credential name (Name) must match the regular expression `^[A-Z_][A-Z0-9_]*$`.
 * - If the specified credential name already exists in the specified instance, the error code `Credential.Name.AlreadyExists` is returned.
 *
 * @param request CreateCredentialRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateCredentialResponse
 */
@context("createCredentialWithContext")
async function createCredentialWithOptions(request: CreateCredentialRequest, runtime: $RuntimeOptions): CreateCredentialResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.description)) {
    query['Description'] = request.description;
  }
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.name)) {
    query['Name'] = request.name;
  }
  var body : map[string]any = {};
  if (!$isNull(request.apiKey)) {
    body['ApiKey'] = request.apiKey;
  }
  if (!$isNull(request.clientToken)) {
    body['ClientToken'] = request.clientToken;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateCredential',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates a new key-value credential under a specified AgentTeams instance.
 *
 * @description ## Operation description
 * - `ApiKey` is a sensitive field and is not returned in plaintext.
 * - `ClientToken` is used to ensure idempotence of the request. This parameter is optional but recommended.
 * - The credential name (Name) must match the regular expression `^[A-Z_][A-Z0-9_]*$`.
 * - If the specified credential name already exists in the specified instance, the error code `Credential.Name.AlreadyExists` is returned.
 *
 * @param request CreateCredentialRequest
 * @return CreateCredentialResponse
 */
async function createCredential(request: CreateCredentialRequest): CreateCredentialResponse {
  var runtime = new $RuntimeOptions{};
  return createCredentialWithOptions(request, runtime);
}

model CreateInstanceRequest {
  clientToken?: string(name='ClientToken', description='The idempotency token that is used to ensure the idempotence of the request.', example='client-token-xxx'),
  instanceName?: string(name='InstanceName', description='`instanceName`: `string`, required.

This parameter is required.', example='agentteams-demo'),
  instanceSpec?: string(name='InstanceSpec', description='`instanceSpec`: `string`, optional, default `SMALL_X1`

This parameter is required.', example='SMALL_X1'),
  networkType?: string(name='NetworkType', description='`networkType`: `string`, optional, default `PRIVATE_NET`  
Valid values:

*   `PRIVATE_PUBNET`
*   `PRIVATE_NET`
*   `PUB_NET`

This parameter is required.', example='PRIVATE_NET'),
  paymentType?: string(name='PaymentType', description='The billing method.', example='Subscription'),
  vpcId?: string(name='VpcId', description='`vpcId`: `string`, required.

This parameter is required.', example='vpc-bp1xxxx'),
  zones?: [ 
    {
      vSwitchId?: string(name='VSwitchId', description='The vSwitch ID.', example='vsw-bp1xxxx'),
      zoneId?: string(name='ZoneId', description='The zone ID.', example='cn-hangzhou-h'),
    }
  ](name='Zones', description='The zones.', example='[]'),
}

model CreateInstanceShrinkRequest {
  clientToken?: string(name='ClientToken', description='The idempotency token that is used to ensure the idempotence of the request.', example='client-token-xxx'),
  instanceName?: string(name='InstanceName', description='`instanceName`: `string`, required.

This parameter is required.', example='agentteams-demo'),
  instanceSpec?: string(name='InstanceSpec', description='`instanceSpec`: `string`, optional, default `SMALL_X1`

This parameter is required.', example='SMALL_X1'),
  networkType?: string(name='NetworkType', description='`networkType`: `string`, optional, default `PRIVATE_NET`  
Valid values:

*   `PRIVATE_PUBNET`
*   `PRIVATE_NET`
*   `PUB_NET`

This parameter is required.', example='PRIVATE_NET'),
  paymentType?: string(name='PaymentType', description='The billing method.', example='Subscription'),
  vpcId?: string(name='VpcId', description='`vpcId`: `string`, required.

This parameter is required.', example='vpc-bp1xxxx'),
  zonesShrink?: string(name='Zones', description='The zones.', example='[]'),
}

model CreateInstanceResponseBody = {
  code?: string(name='Code', description='**The response code.**', example='SUCCESS'),
  data?: {
    instanceId?: string(name='InstanceId', description='**The instance ID.**', example='agentteams-test-001'),
    orderId?: string(name='OrderId', description='**The order ID.**', example='open-create-agentteams-test-001-req-create-001'),
  }(name='Data', description='The returned data.', example='{}'),
  httpStatusCode?: int32(name='HttpStatusCode', description='**The HTTP status code.**', example='200'),
  instanceId?: string(name='InstanceId', description='The instance ID.', example='agentteams-test-001'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-create-001'),
  success?: boolean(name='Success', description='**Indicates whether the request was successful.**', example='true'),
}

model CreateInstanceResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateInstanceResponseBody(name='body'),
}

/**
 * @summary Asynchronously creates a cluster instance with the specified configurations.
 *
 * @description ## Operation description
 * - This is an asynchronous operation. After a successful call, the instance status changes to CREATING.
 * - The actual resource creation is completed asynchronously in the background. Poll the creation result by calling the `GetInstance` operation.
 * - You can pass request information by using form parameters or query parameters.
 * - If optional parameters are not provided, default values are used.
 * - Use `ClientToken` to ensure the idempotence of the request.
 *
 * @param tmpReq CreateInstanceRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateInstanceResponse
 */
@context("createInstanceWithContext")
async function createInstanceWithOptions(tmpReq: CreateInstanceRequest, runtime: $RuntimeOptions): CreateInstanceResponse {
  tmpReq.validate();
  var request = new CreateInstanceShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.zones)) {
    request.zonesShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.zones, 'Zones', 'json');
  }
  var query = {};
  if (!$isNull(request.instanceName)) {
    query['InstanceName'] = request.instanceName;
  }
  if (!$isNull(request.instanceSpec)) {
    query['InstanceSpec'] = request.instanceSpec;
  }
  if (!$isNull(request.networkType)) {
    query['NetworkType'] = request.networkType;
  }
  if (!$isNull(request.vpcId)) {
    query['VpcId'] = request.vpcId;
  }
  if (!$isNull(request.zonesShrink)) {
    query['Zones'] = request.zonesShrink;
  }
  var body : map[string]any = {};
  if (!$isNull(request.clientToken)) {
    body['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.paymentType)) {
    body['PaymentType'] = request.paymentType;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateInstance',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Asynchronously creates a cluster instance with the specified configurations.
 *
 * @description ## Operation description
 * - This is an asynchronous operation. After a successful call, the instance status changes to CREATING.
 * - The actual resource creation is completed asynchronously in the background. Poll the creation result by calling the `GetInstance` operation.
 * - You can pass request information by using form parameters or query parameters.
 * - If optional parameters are not provided, default values are used.
 * - Use `ClientToken` to ensure the idempotence of the request.
 *
 * @param request CreateInstanceRequest
 * @return CreateInstanceResponse
 */
async function createInstance(request: CreateInstanceRequest): CreateInstanceResponse {
  var runtime = new $RuntimeOptions{};
  return createInstanceWithOptions(request, runtime);
}

model CreateMcpRequest {
  addresses?: [ string ](name='Addresses', description='The list of MCP server addresses.

This parameter is required.', example='["https://example.com/mcp"]'),
  authConfig?: string(name='AuthConfig', description='The authentication information.', example='{"type":"bearer","token":"xxx"}'),
  authEnabled?: boolean(name='AuthEnabled', description='Specifies whether to enable authentication.', example='false'),
  clientToken?: string(name='ClientToken', description='The client idempotency token that is used to ensure the idempotence of the request.', example='client-token-xxx'),
  createType?: string(name='CreateType', description='The creation type. Default value: DIRECT_PROXY.', example='DIRECT_PROXY'),
  description?: string(name='Description', description='The description of the MCP server.', example='This is an MCP server.'),
  instanceId?: string(name='InstanceId', description='The AgentTeams instance ID.

This parameter is required.', example='ins-xxx'),
  name?: string(name='Name', description='Optional. Fuzzy match by instance name.

This parameter is required.', example='AgentTeams'),
  protocol?: string(name='Protocol', description='The protocol type.', example='streamable'),
  swaggerConfig?: string(name='SwaggerConfig', description='The Swagger configuration. This parameter is required if Type is set to HTTP_TO_MCP.', example='{"url":"https://example.com/swagger.json"}'),
}

model CreateMcpShrinkRequest {
  addressesShrink?: string(name='Addresses', description='The list of MCP server addresses.

This parameter is required.', example='["https://example.com/mcp"]'),
  authConfig?: string(name='AuthConfig', description='The authentication information.', example='{"type":"bearer","token":"xxx"}'),
  authEnabled?: boolean(name='AuthEnabled', description='Specifies whether to enable authentication.', example='false'),
  clientToken?: string(name='ClientToken', description='The client idempotency token that is used to ensure the idempotence of the request.', example='client-token-xxx'),
  createType?: string(name='CreateType', description='The creation type. Default value: DIRECT_PROXY.', example='DIRECT_PROXY'),
  description?: string(name='Description', description='The description of the MCP server.', example='This is an MCP server.'),
  instanceId?: string(name='InstanceId', description='The AgentTeams instance ID.

This parameter is required.', example='ins-xxx'),
  name?: string(name='Name', description='Optional. Fuzzy match by instance name.

This parameter is required.', example='AgentTeams'),
  protocol?: string(name='Protocol', description='The protocol type.', example='streamable'),
  swaggerConfig?: string(name='SwaggerConfig', description='The Swagger configuration. This parameter is required if Type is set to HTTP_TO_MCP.', example='{"url":"https://example.com/swagger.json"}'),
}

model CreateMcpResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  data?: {
    addresses?: [ string ](name='Addresses', description='The list of MCP server addresses.', example='["https://example.com/mcp"]'),
    createType?: string(name='CreateType', description='The creation type.', example='7'),
    deployStatus?: string(name='DeployStatus', description='The deployment status.', example='DEPLOYED'),
    description?: string(name='Description', description='The description of the MCP server.', example='This is an MCP server.'),
    id?: string(name='Id', description='The export task ID.', example='418273109406496841-cn-shenzhen'),
    mcpServerConfig?: string(name='McpServerConfig', description='The MCP server configuration.', example='{}'),
    name?: string(name='Name', description='The MCP server name.', example='my-mcp-server'),
    protocol?: string(name='Protocol', description='The protocol type.', example='streamable'),
  }(name='Data', description='The returned data.', example='{}'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='True'),
}

model CreateMcpResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateMcpResponseBody(name='body'),
}

/**
 * @summary Creates an MCP server.
 *
 * @description ## Operation description
 * - You must specify the `InstanceId`, `Name`, and `Addresses` parameters when you create an MCP server.
 * - The `CreateType` parameter defaults to `DIRECT_PROXY`. If you select the `HTTP_TO_MCP` mode, you must also specify `SwaggerConfig`.
 * - Set `AuthEnabled` to enable or disable authentication. If authentication is enabled, you must specify `AuthConfig`.
 * - Use `ClientToken` to ensure the idempotence of the request.
 * - Custom protocol types are supported. The `streamable` protocol is used by default.
 *
 * @param tmpReq CreateMcpRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateMcpResponse
 */
@context("createMcpWithContext")
async function createMcpWithOptions(tmpReq: CreateMcpRequest, runtime: $RuntimeOptions): CreateMcpResponse {
  tmpReq.validate();
  var request = new CreateMcpShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.addresses)) {
    request.addressesShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.addresses, 'Addresses', 'json');
  }
  var query = {};
  if (!$isNull(request.protocol)) {
    query['Protocol'] = request.protocol;
  }
  var body : map[string]any = {};
  if (!$isNull(request.addressesShrink)) {
    body['Addresses'] = request.addressesShrink;
  }
  if (!$isNull(request.authConfig)) {
    body['AuthConfig'] = request.authConfig;
  }
  if (!$isNull(request.authEnabled)) {
    body['AuthEnabled'] = request.authEnabled;
  }
  if (!$isNull(request.clientToken)) {
    body['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.createType)) {
    body['CreateType'] = request.createType;
  }
  if (!$isNull(request.description)) {
    body['Description'] = request.description;
  }
  if (!$isNull(request.instanceId)) {
    body['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.name)) {
    body['Name'] = request.name;
  }
  if (!$isNull(request.swaggerConfig)) {
    body['SwaggerConfig'] = request.swaggerConfig;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateMcp',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates an MCP server.
 *
 * @description ## Operation description
 * - You must specify the `InstanceId`, `Name`, and `Addresses` parameters when you create an MCP server.
 * - The `CreateType` parameter defaults to `DIRECT_PROXY`. If you select the `HTTP_TO_MCP` mode, you must also specify `SwaggerConfig`.
 * - Set `AuthEnabled` to enable or disable authentication. If authentication is enabled, you must specify `AuthConfig`.
 * - Use `ClientToken` to ensure the idempotence of the request.
 * - Custom protocol types are supported. The `streamable` protocol is used by default.
 *
 * @param request CreateMcpRequest
 * @return CreateMcpResponse
 */
async function createMcp(request: CreateMcpRequest): CreateMcpResponse {
  var runtime = new $RuntimeOptions{};
  return createMcpWithOptions(request, runtime);
}

model CreateModelRequest {
  clientToken?: string(name='ClientToken', description='The idempotency token that ensures the idempotence of the request.', example='ct-xxx'),
  description?: string(name='Description', description='The model description.', example='my model'),
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='i-xxx'),
  name?: string(name='Name', description='Optional. Fuzzy matches instances by name.

This parameter is required.', example='AgentTeams'),
  protocols?: [ string ](name='Protocols', description='The list of protocols supported by the model, in JSON format.

This parameter is required.', example='["openai"]'),
  provider?: string(name='Provider', description='The provider identifier.', example='openai'),
  providerId?: string(name='ProviderId', description='The ID of the model provider.

This parameter is required.', example='p-xxx'),
  providerName?: string(name='ProviderName', description='The name of the model provider.

This parameter is required.', example='my-provider'),
}

model CreateModelShrinkRequest {
  clientToken?: string(name='ClientToken', description='The idempotency token that ensures the idempotence of the request.', example='ct-xxx'),
  description?: string(name='Description', description='The model description.', example='my model'),
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='i-xxx'),
  name?: string(name='Name', description='Optional. Fuzzy matches instances by name.

This parameter is required.', example='AgentTeams'),
  protocolsShrink?: string(name='Protocols', description='The list of protocols supported by the model, in JSON format.

This parameter is required.', example='["openai"]'),
  provider?: string(name='Provider', description='The provider identifier.', example='openai'),
  providerId?: string(name='ProviderId', description='The ID of the model provider.

This parameter is required.', example='p-xxx'),
  providerName?: string(name='ProviderName', description='The name of the model provider.

This parameter is required.', example='my-provider'),
}

model CreateModelResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  data?: {
    createTime?: long(name='CreateTime', description='The creation time, in millisecond timestamp format.', example='1718000000000'),
    description?: string(name='Description', description='The model description.', example='my model'),
    id?: string(name='Id', description='The model ID.', example='m-xxx'),
    instanceId?: string(name='InstanceId', description='The instance ID.', example='i-xxx'),
    name?: string(name='Name', description='The model name.', example='my-model'),
    protocols?: [ string ](name='Protocols', description='The list of protocols supported by the model.', example='["openai"]'),
    provider?: string(name='Provider', description='The provider identifier.', example='openai'),
    providerName?: string(name='ProviderName', description='The name of the model provider.', example='my-provider'),
    updateTime?: long(name='UpdateTime', description='The update time, in millisecond timestamp format.', example='1718000000000'),
  }(name='Data', description='The returned data.', example='{}'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='Id of the request', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model CreateModelResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateModelResponseBody(name='body'),
}

/**
 * @summary Creates an AI model under a specified AgentTeams instance. You must specify the model name, the model provider, and the list of supported protocols.
 *
 * @description Creates an AI model under a specified AgentTeams instance. You must specify the model name, the model provider, and the list of supported protocols.
 *
 * @param tmpReq CreateModelRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateModelResponse
 */
@context("createModelWithContext")
async function createModelWithOptions(tmpReq: CreateModelRequest, runtime: $RuntimeOptions): CreateModelResponse {
  tmpReq.validate();
  var request = new CreateModelShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.protocols)) {
    request.protocolsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.protocols, 'Protocols', 'json');
  }
  var body : map[string]any = {};
  if (!$isNull(request.clientToken)) {
    body['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.description)) {
    body['Description'] = request.description;
  }
  if (!$isNull(request.instanceId)) {
    body['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.name)) {
    body['Name'] = request.name;
  }
  if (!$isNull(request.protocolsShrink)) {
    body['Protocols'] = request.protocolsShrink;
  }
  if (!$isNull(request.provider)) {
    body['Provider'] = request.provider;
  }
  if (!$isNull(request.providerId)) {
    body['ProviderId'] = request.providerId;
  }
  if (!$isNull(request.providerName)) {
    body['ProviderName'] = request.providerName;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateModel',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates an AI model under a specified AgentTeams instance. You must specify the model name, the model provider, and the list of supported protocols.
 *
 * @description Creates an AI model under a specified AgentTeams instance. You must specify the model name, the model provider, and the list of supported protocols.
 *
 * @param request CreateModelRequest
 * @return CreateModelResponse
 */
async function createModel(request: CreateModelRequest): CreateModelResponse {
  var runtime = new $RuntimeOptions{};
  return createModelWithOptions(request, runtime);
}

model CreateModelProviderRequest {
  address?: string(name='Address', description='The address of the model provider.

This parameter is required.', example='https://api.example.com'),
  apiKeys?: [ string ](name='ApiKeys', description='The list of API keys for the model provider, in JSON format.

This parameter is required.', example='["sk-xxx"]'),
  clientToken?: string(name='ClientToken', description='The idempotency token that ensures the idempotence of the request.', example='ct-xxx'),
  description?: string(name='Description', description='The description of the model provider.', example='my provider'),
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='i-xxx'),
  name?: string(name='Name', description='Optional. The instance name for fuzzy matching.

This parameter is required.', example='AgentTeams'),
  protocols?: [ string ](name='Protocols', description='The list of protocols supported by the model provider, in JSON format.

This parameter is required.', example='["openai"]'),
  provider?: string(name='Provider', description='Optional. The instance status for exact matching.

This parameter is required.', example='RUNNING'),
}

model CreateModelProviderShrinkRequest {
  address?: string(name='Address', description='The address of the model provider.

This parameter is required.', example='https://api.example.com'),
  apiKeysShrink?: string(name='ApiKeys', description='The list of API keys for the model provider, in JSON format.

This parameter is required.', example='["sk-xxx"]'),
  clientToken?: string(name='ClientToken', description='The idempotency token that ensures the idempotence of the request.', example='ct-xxx'),
  description?: string(name='Description', description='The description of the model provider.', example='my provider'),
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='i-xxx'),
  name?: string(name='Name', description='Optional. The instance name for fuzzy matching.

This parameter is required.', example='AgentTeams'),
  protocolsShrink?: string(name='Protocols', description='The list of protocols supported by the model provider, in JSON format.

This parameter is required.', example='["openai"]'),
  provider?: string(name='Provider', description='Optional. The instance status for exact matching.

This parameter is required.', example='RUNNING'),
}

model CreateModelProviderResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  data?: {
    address?: string(name='Address', description='The address of the model provider.', example='https://api.example.com'),
    createTime?: string(name='CreateTime', description='The time when the model provider was created.', example='2026-06-11T10:00:00Z'),
    description?: string(name='Description', description='The description of the model provider.', example='my provider'),
    id?: string(name='Id', description='The ID of the model provider.', example='p-xxx'),
    name?: string(name='Name', description='The name of the model provider.', example='my-provider'),
    protocols?: [ string ](name='Protocols', description='The list of protocols supported by the model provider.', example='["openai"]'),
    provider?: string(name='Provider', description='The provider identifier.', example='openai'),
  }(name='Data', description='The response data.', example='{}'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='Id of the request', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model CreateModelProviderResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateModelProviderResponseBody(name='body'),
}

/**
 * @summary Creates an AI model provider under a specified AgentTeams instance. You must specify the provider name, address, supported protocol list, and API keys.
 *
 * @description Creates an AI model provider under a specified AgentTeams instance. You must specify the provider name, address, supported protocol list, and API keys.
 *
 * @param tmpReq CreateModelProviderRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateModelProviderResponse
 */
@context("createModelProviderWithContext")
async function createModelProviderWithOptions(tmpReq: CreateModelProviderRequest, runtime: $RuntimeOptions): CreateModelProviderResponse {
  tmpReq.validate();
  var request = new CreateModelProviderShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.apiKeys)) {
    request.apiKeysShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.apiKeys, 'ApiKeys', 'json');
  }
  if (!$isNull(tmpReq.protocols)) {
    request.protocolsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.protocols, 'Protocols', 'json');
  }
  var body : map[string]any = {};
  if (!$isNull(request.address)) {
    body['Address'] = request.address;
  }
  if (!$isNull(request.apiKeysShrink)) {
    body['ApiKeys'] = request.apiKeysShrink;
  }
  if (!$isNull(request.clientToken)) {
    body['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.description)) {
    body['Description'] = request.description;
  }
  if (!$isNull(request.instanceId)) {
    body['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.name)) {
    body['Name'] = request.name;
  }
  if (!$isNull(request.protocolsShrink)) {
    body['Protocols'] = request.protocolsShrink;
  }
  if (!$isNull(request.provider)) {
    body['Provider'] = request.provider;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateModelProvider',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates an AI model provider under a specified AgentTeams instance. You must specify the provider name, address, supported protocol list, and API keys.
 *
 * @description Creates an AI model provider under a specified AgentTeams instance. You must specify the provider name, address, supported protocol list, and API keys.
 *
 * @param request CreateModelProviderRequest
 * @return CreateModelProviderResponse
 */
async function createModelProvider(request: CreateModelProviderRequest): CreateModelProviderResponse {
  var runtime = new $RuntimeOptions{};
  return createModelProviderWithOptions(request, runtime);
}

model CreateServiceEndpointRequest {
  certIdentifier?: string(name='CertIdentifier', description='The certificate ID.', example='cert-001'),
  clientToken?: string(name='ClientToken', description='The idempotency token that ensures the idempotence of the request.', example='client-token-xxx'),
  component?: string(name='Component', description='The component type. Valid values: `ELEMENT`, `TUNNEL`, `MATRIX`, and `WORKER`.

This parameter is required.', example='WORKER'),
  domain?: string(name='Domain', description='The custom domain name. The server trims and converts the value to lowercase.', example='matrix.example.com'),
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='ins-001'),
  resourceName?: string(name='ResourceName', description='The service name.', example='my-service'),
}

model CreateServiceEndpointResponseBody = {
  code?: string(name='Code', description='The status code.', example='SUCCESS'),
  data?: {
    endpointId?: string(name='EndpointId', description='Endpoint ID', example='ep-xxx'),
    status?: string(name='Status', description='The status. Valid values:
 * CONFIGURED: Configured.
 * DELETED: Deleted.', example='CONFIGURED'),
  }(name='Data', description='The response data returned after the endpoint is created.', example='{}'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='2b7f1c2d-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model CreateServiceEndpointResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateServiceEndpointResponseBody(name='body'),
}

/**
 * @summary Creates an endpoint for a specified instance. Multiple component types and gateway types are supported.
 *
 * @description Creates an endpoint for a specified instance. Multiple component types and gateway types are supported.
 * - The current controller uses standard parameter binding instead of @RequestBody, so parameters are better suited for query/form-based transmission.
 * - The domain field is trimmed and converted to lowercase on the server side.
 * - The query and headers must be in JSON object string format and cannot be arrays.
 * - The create operation only saves data to the database. The AI Gateway domain name synchronization logic is triggered only during an update when the conditions ELEMENT/MATRIX + AI_GATEWAY + INTERNET are met and the domain name or certificate has changed.
 *
 * @param request CreateServiceEndpointRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateServiceEndpointResponse
 */
@context("createServiceEndpointWithContext")
async function createServiceEndpointWithOptions(request: CreateServiceEndpointRequest, runtime: $RuntimeOptions): CreateServiceEndpointResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.certIdentifier)) {
    query['CertIdentifier'] = request.certIdentifier;
  }
  if (!$isNull(request.component)) {
    query['Component'] = request.component;
  }
  if (!$isNull(request.domain)) {
    query['Domain'] = request.domain;
  }
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.resourceName)) {
    query['ResourceName'] = request.resourceName;
  }
  var body : map[string]any = {};
  if (!$isNull(request.clientToken)) {
    body['ClientToken'] = request.clientToken;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateServiceEndpoint',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates an endpoint for a specified instance. Multiple component types and gateway types are supported.
 *
 * @description Creates an endpoint for a specified instance. Multiple component types and gateway types are supported.
 * - The current controller uses standard parameter binding instead of @RequestBody, so parameters are better suited for query/form-based transmission.
 * - The domain field is trimmed and converted to lowercase on the server side.
 * - The query and headers must be in JSON object string format and cannot be arrays.
 * - The create operation only saves data to the database. The AI Gateway domain name synchronization logic is triggered only during an update when the conditions ELEMENT/MATRIX + AI_GATEWAY + INTERNET are met and the domain name or certificate has changed.
 *
 * @param request CreateServiceEndpointRequest
 * @return CreateServiceEndpointResponse
 */
async function createServiceEndpoint(request: CreateServiceEndpointRequest): CreateServiceEndpointResponse {
  var runtime = new $RuntimeOptions{};
  return createServiceEndpointWithOptions(request, runtime);
}

model CreateTeamRequest {
  adminName?: string(name='AdminName', description='The team administrator name. The administrator must be an existing user under the instance with sufficient permissions.', example='admin-user'),
  clientToken?: string(name='ClientToken', description='The idempotency token that is used to ensure the idempotence of the request.', example='client-token-xxx'),
  description?: string(name='Description', description='The team description.', example='This is a team.'),
  instanceId?: string(name='InstanceId', description='The instance ID.', example='agentteams-test-001'),
  name?: string(name='Name', description='The team name.', example='worker-demo'),
  teamMembers?: [ 
    {
      name?: string(name='Name', description='The member name. The member must be an existing user under the instance.', example='member-user'),
    }
  ](name='TeamMembers', description='The initial member list of the team.', example='[...]'),
}

model CreateTeamShrinkRequest {
  adminName?: string(name='AdminName', description='The team administrator name. The administrator must be an existing user under the instance with sufficient permissions.', example='admin-user'),
  clientToken?: string(name='ClientToken', description='The idempotency token that is used to ensure the idempotence of the request.', example='client-token-xxx'),
  description?: string(name='Description', description='The team description.', example='This is a team.'),
  instanceId?: string(name='InstanceId', description='The instance ID.', example='agentteams-test-001'),
  name?: string(name='Name', description='The team name.', example='worker-demo'),
  teamMembersShrink?: string(name='TeamMembers', description='The initial member list of the team.', example='[...]'),
}

model CreateTeamResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  data?: {
    adminName?: string(name='AdminName', description='The team administrator name.', example='admin-user'),
    description?: string(name='Description', description='The team description.', example='This is a team.'),
    instanceId?: string(name='InstanceId', description='The instance ID.', example='agentteams-test-001'),
    name?: string(name='Name', description='The team name.', example='my-team'),
    teamMembers?: [ 
      {
        name?: string(name='Name', description='The member name.', example='member-user'),
      }
    ](name='TeamMembers', description='The team member list.', example='[...]'),
  }(name='Data', description='The response data.', example='{}'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-create-team-001'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model CreateTeamResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateTeamResponseBody(name='body'),
}

/**
 * @summary Creates a team under a specified instance. You can set the team name, description, administrator, and initial member list.
 *
 * @description Creates a team under a specified instance. You can set the team name, description, administrator, and initial member list.
 *
 * @param tmpReq CreateTeamRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateTeamResponse
 */
@context("createTeamWithContext")
async function createTeamWithOptions(tmpReq: CreateTeamRequest, runtime: $RuntimeOptions): CreateTeamResponse {
  tmpReq.validate();
  var request = new CreateTeamShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.teamMembers)) {
    request.teamMembersShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.teamMembers, 'TeamMembers', 'json');
  }
  var query = {};
  if (!$isNull(request.adminName)) {
    query['AdminName'] = request.adminName;
  }
  if (!$isNull(request.description)) {
    query['Description'] = request.description;
  }
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.name)) {
    query['Name'] = request.name;
  }
  if (!$isNull(request.teamMembersShrink)) {
    query['TeamMembers'] = request.teamMembersShrink;
  }
  var body : map[string]any = {};
  if (!$isNull(request.clientToken)) {
    body['ClientToken'] = request.clientToken;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateTeam',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates a team under a specified instance. You can set the team name, description, administrator, and initial member list.
 *
 * @description Creates a team under a specified instance. You can set the team name, description, administrator, and initial member list.
 *
 * @param request CreateTeamRequest
 * @return CreateTeamResponse
 */
async function createTeam(request: CreateTeamRequest): CreateTeamResponse {
  var runtime = new $RuntimeOptions{};
  return createTeamWithOptions(request, runtime);
}

model CreateUserRequest {
  authMethod?: string(name='AuthMethod', description='The authentication method.', example='password'),
  clientToken?: string(name='ClientToken', description='The idempotency token that is used to ensure the idempotence of the request.', example='client-token-xxx'),
  displayName?: string(name='DisplayName', description='The display name.', example='Demo User'),
  email?: string(name='Email', description='The email address.', example='demo@example.com'),
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='agentteams-test-001'),
  name?: string(name='Name', description='The name.

This parameter is required.', example='worker-demo'),
  note?: string(name='Note', description='The note.', example='TestUser.'),
  password?: string(name='Password', description='The password. If not specified, the system automatically generates an initial password.', example='P@ssw0rd123'),
}

model CreateUserResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  data?: {
    authMethod?: string(name='AuthMethod', description='The authentication method.', example='password'),
    displayName?: string(name='DisplayName', description='The display name.', example='Demo User'),
    email?: string(name='Email', description='The email address.', example='demo@example.com'),
    initialPassword?: string(name='InitialPassword', description='The initial password. This value is automatically generated by the system if the Password parameter is not specified.', example='P@ssw0rd123'),
    instanceId?: string(name='InstanceId', description='The instance ID.', example='agentteams-test-001'),
    name?: string(name='Name', description='The username.', example='demo-user'),
    note?: string(name='Note', description='The note.', example='TestUser.'),
  }(name='Data', description='The returned data.', example='{}'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-create-user-001'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model CreateUserResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateUserResponseBody(name='body'),
}

/**
 * @summary Creates a user under a specified instance. You can set the username, display name, email address, authentication method, note, and password. If no password is specified, the system automatically generates an initial password and returns it in the response.
 *
 * @description Creates a user under a specified instance. You can set the username, display name, email address, authentication method, note, and password. If no password is specified, the system automatically generates an initial password and returns it in the response.
 *
 * @param request CreateUserRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateUserResponse
 */
@context("createUserWithContext")
async function createUserWithOptions(request: CreateUserRequest, runtime: $RuntimeOptions): CreateUserResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.authMethod)) {
    query['AuthMethod'] = request.authMethod;
  }
  if (!$isNull(request.displayName)) {
    query['DisplayName'] = request.displayName;
  }
  if (!$isNull(request.email)) {
    query['Email'] = request.email;
  }
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.name)) {
    query['Name'] = request.name;
  }
  if (!$isNull(request.note)) {
    query['Note'] = request.note;
  }
  if (!$isNull(request.password)) {
    query['Password'] = request.password;
  }
  var body : map[string]any = {};
  if (!$isNull(request.clientToken)) {
    body['ClientToken'] = request.clientToken;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateUser',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates a user under a specified instance. You can set the username, display name, email address, authentication method, note, and password. If no password is specified, the system automatically generates an initial password and returns it in the response.
 *
 * @description Creates a user under a specified instance. You can set the username, display name, email address, authentication method, note, and password. If no password is specified, the system automatically generates an initial password and returns it in the response.
 *
 * @param request CreateUserRequest
 * @return CreateUserResponse
 */
async function createUser(request: CreateUserRequest): CreateUserResponse {
  var runtime = new $RuntimeOptions{};
  return createUserWithOptions(request, runtime);
}

model CreateWorkerRequest {
  agentType?: string(name='AgentType', description='The Agent runtime type.', example='Qwenpaw'),
  agents?: string(name='Agents', description='The Agents configuration.', example='agents-demo'),
  channels?: [ 
    {
      config?: {
        cardTemplateId?: string(name='CardTemplateId', description='The returned data.', example='tpl-demo'),
        clientId?: string(name='ClientId', description='The returned data.', example='cli-demo'),
        extension?: string(name='Extension', description='The returned data.', example='extension-demo'),
        messageType?: string(name='MessageType', description='The returned data.', example='markdown'),
        robotCode?: string(name='RobotCode', description='The returned data.', example='robot-demo'),
        showThinking?: boolean(name='ShowThinking', description='The returned data.', example='true'),
        showToolCalls?: boolean(name='ShowToolCalls', description='The returned data.', example='false'),
        streamingEnabled?: boolean(name='StreamingEnabled', description='The returned data.', example='true'),
      }(name='Config', description='The channel configuration.', example='{}'),
      enabled?: boolean(name='Enabled', description='Specifies whether the channel is enabled.', example='true'),
      secrets?: {
        clientSecret?: string(name='ClientSecret', description='The returned data.', example='secret-demo'),
      }(name='Secrets', description='The channel secrets.', example='{}'),
      type?: string(name='Type', description='The channel type.', example='dingtalk'),
    }
  ](name='Channels', description='The list of channel configurations.', example='[...]'),
  clientToken?: string(name='ClientToken', description='The idempotency token that ensures the idempotence of the request.', example='client-token-demo'),
  credentials?: [ 
    {
      name?: string(name='Name', description='The credential name.', example='cred-demo'),
    }
  ](name='Credentials', description='The list of credential bindings.', example='[...]'),
  deployType?: string(name='DeployType', description='The deployment type.', example='SelfHosted'),
  groups?: [ 
    {
      name?: string(name='Name', description='The group name.', example='team-demo'),
      role?: string(name='Role', description='The group role.', example='member'),
      type?: string(name='Type', description='The group type.', example='team'),
    }
  ](name='Groups', description='The list of team groups.', example='[...]'),
  instanceId?: string(name='InstanceId', description='The instance ID.', example='inst-demo'),
  limitConfig?: {
    limitType?: string(name='LimitType', description='The quota type.', example='token'),
    periodType?: string(name='PeriodType', description='The period type.', example='day'),
    usageLimit?: long(name='UsageLimit', description='The quota limit.', example='1000000'),
  }(name='LimitConfig', description='The quota configuration.', example='{}'),
  mcpServers?: [ 
    {
      name?: string(name='Name', description='The MCP server name.', example='mcp-server-demo'),
    }
  ](name='McpServers', description='The list of MCP servers.', example='[...]'),
  model?: {
    modelName?: string(name='ModelName', description='The returned data.', example='qwen-max'),
    modelProvider?: string(name='ModelProvider', description='The returned data.', example='default'),
  }(name='Model', description='The model configuration.', example='{}'),
  name?: string(name='Name', description='The name.', example='worker-demo'),
  skills?: [ 
    {
      label?: string(name='Label', description='The skill label.', example='Code Review.'),
      name?: string(name='Name', description='The skill name.', example='code-review'),
      version?: string(name='Version', description='The skill version.', example='1.0.0'),
    }
  ](name='Skills', description='The list of skills.', example='[...]'),
  soul?: string(name='Soul', description='The Soul configuration.', example='soul-demo'),
  subagents?: [ 
    {
      agents?: string(name='Agents', description='The sub-agent configuration.', example='agents-demo'),
      skills?: [ 
        {
          label?: string(name='Label', description='The returned data.', example='Label.'),
          name?: string(name='Name', description='The returned data.', example='worker-demo'),
          version?: string(name='Version', description='The returned data.', example='1.0.0'),
        }
      ](name='Skills', description='The list of sub-agent skills.', example='[...]'),
      subagentId?: string(name='SubagentId', description='The sub-agent ID.', example='sub-demo'),
    }
  ](name='Subagents', description='The list of sub-agents.', example='[...]'),
  template?: {
    label?: string(name='Label', description='The returned data.', example='Label.'),
    name?: string(name='Name', description='The returned data.', example='worker-demo'),
    version?: string(name='Version', description='The returned data.', example='1.0.0'),
  }(name='Template', description='The template configuration.', example='{}'),
  versionCode?: string(name='VersionCode', description='The Worker version number.', example='v1.0.0'),
}

model CreateWorkerShrinkRequest {
  agentType?: string(name='AgentType', description='The Agent runtime type.', example='Qwenpaw'),
  agents?: string(name='Agents', description='The Agents configuration.', example='agents-demo'),
  channelsShrink?: string(name='Channels', description='The list of channel configurations.', example='[...]'),
  clientToken?: string(name='ClientToken', description='The idempotency token that ensures the idempotence of the request.', example='client-token-demo'),
  credentialsShrink?: string(name='Credentials', description='The list of credential bindings.', example='[...]'),
  deployType?: string(name='DeployType', description='The deployment type.', example='SelfHosted'),
  groupsShrink?: string(name='Groups', description='The list of team groups.', example='[...]'),
  instanceId?: string(name='InstanceId', description='The instance ID.', example='inst-demo'),
  limitConfigShrink?: string(name='LimitConfig', description='The quota configuration.', example='{}'),
  mcpServersShrink?: string(name='McpServers', description='The list of MCP servers.', example='[...]'),
  modelShrink?: string(name='Model', description='The model configuration.', example='{}'),
  name?: string(name='Name', description='The name.', example='worker-demo'),
  skillsShrink?: string(name='Skills', description='The list of skills.', example='[...]'),
  soul?: string(name='Soul', description='The Soul configuration.', example='soul-demo'),
  subagentsShrink?: string(name='Subagents', description='The list of sub-agents.', example='[...]'),
  templateShrink?: string(name='Template', description='The template configuration.', example='{}'),
  versionCode?: string(name='VersionCode', description='The Worker version number.', example='v1.0.0'),
}

model CreateWorkerResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  data?: {
    agentType?: string(name='AgentType', description='The Agent runtime type.', example='Qwenpaw'),
    agents?: string(name='Agents', description='The Agents configuration.', example='agents-demo'),
    credentials?: [ 
      {
        name?: string(name='Name', description='The credential name.', example='cred-demo'),
      }
    ](name='Credentials', description='The list of credential bindings.', example='[...]'),
    deployType?: string(name='DeployType', description='The deployment type.', example='Managed'),
    groups?: [ 
      {
        name?: string(name='Name', description='The group name.', example='team-demo'),
        role?: string(name='Role', description='The group role.', example='member'),
        type?: string(name='Type', description='The group type.', example='team'),
      }
    ](name='Groups', description='The list of team groups.', example='[...]'),
    instanceId?: string(name='InstanceId', description='The instance ID.', example='inst-demo'),
    limitConfig?: {
      limitType?: string(name='LimitType', description='The quota type.', example='token'),
      periodType?: string(name='PeriodType', description='The period type.', example='day'),
      usageLimit?: long(name='UsageLimit', description='The quota limit.', example='1000000'),
    }(name='LimitConfig', description='The quota configuration.', example='{}'),
    mcpServers?: [ 
      {
        name?: string(name='Name', description='The MCP server name.', example='mcp-server-demo'),
        transport?: string(name='Transport', description='The MCP transport protocol.', example='sse'),
        url?: string(name='Url', description='The MCP server URL.', example='https://mcp-demo/mcp'),
      }
    ](name='McpServers', description='The list of MCP servers.', example='[...]'),
    model?: {
      modelName?: string(name='ModelName', description='The model name.', example='qwen-max'),
      modelProvider?: string(name='ModelProvider', description='The model provider.', example='dashscope'),
    }(name='Model', description='The model configuration.', example='{}'),
    name?: string(name='Name', description='The worker name.', example='worker-demo'),
    skills?: [ 
      {
        label?: string(name='Label', description='The skill label.', example='Code Review.'),
        name?: string(name='Name', description='The skill name.', example='code-review'),
        version?: string(name='Version', description='The skill version.', example='1.0.0'),
      }
    ](name='Skills', description='The list of skills.', example='[...]'),
    soul?: string(name='Soul', description='The Soul configuration.', example='soul-demo'),
    startTime?: string(name='StartTime', description='The start time.', example='2026-07-13T10:00:00Z'),
    status?: string(name='Status', description='The worker status.', example='Running'),
    template?: {
      label?: string(name='Label', description='The template label.', example='Default template.'),
      name?: string(name='Name', description='The template name.', example='default-template'),
      version?: string(name='Version', description='The template version.', example='1.0.0'),
    }(name='Template', description='The template configuration.', example='{}'),
    versionCode?: string(name='VersionCode', description='The Worker version number.', example='v1.0.0'),
  }(name='Data', description='The returned data.', example='{}'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='473469C7-XX-XX-XX'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model CreateWorkerResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateWorkerResponseBody(name='body'),
}

/**
 * @summary Creates a Worker instance with specified configurations such as model, skills, template, MCP servers, and channels.
 *
 * @description Creates a Worker instance with specified configurations such as model, skills, template, MCP servers, and channels.
 *
 * @param tmpReq CreateWorkerRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateWorkerResponse
 */
@context("createWorkerWithContext")
async function createWorkerWithOptions(tmpReq: CreateWorkerRequest, runtime: $RuntimeOptions): CreateWorkerResponse {
  tmpReq.validate();
  var request = new CreateWorkerShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.channels)) {
    request.channelsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.channels, 'Channels', 'json');
  }
  if (!$isNull(tmpReq.credentials)) {
    request.credentialsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.credentials, 'Credentials', 'json');
  }
  if (!$isNull(tmpReq.groups)) {
    request.groupsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.groups, 'Groups', 'json');
  }
  if (!$isNull(tmpReq.limitConfig)) {
    request.limitConfigShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.limitConfig, 'LimitConfig', 'json');
  }
  if (!$isNull(tmpReq.mcpServers)) {
    request.mcpServersShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.mcpServers, 'McpServers', 'json');
  }
  if (!$isNull(tmpReq.model)) {
    request.modelShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.model, 'Model', 'json');
  }
  if (!$isNull(tmpReq.skills)) {
    request.skillsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.skills, 'Skills', 'json');
  }
  if (!$isNull(tmpReq.subagents)) {
    request.subagentsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.subagents, 'Subagents', 'json');
  }
  if (!$isNull(tmpReq.template)) {
    request.templateShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.template, 'Template', 'json');
  }
  var query = {};
  if (!$isNull(request.agentType)) {
    query['AgentType'] = request.agentType;
  }
  if (!$isNull(request.agents)) {
    query['Agents'] = request.agents;
  }
  if (!$isNull(request.channelsShrink)) {
    query['Channels'] = request.channelsShrink;
  }
  if (!$isNull(request.deployType)) {
    query['DeployType'] = request.deployType;
  }
  if (!$isNull(request.groupsShrink)) {
    query['Groups'] = request.groupsShrink;
  }
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.limitConfigShrink)) {
    query['LimitConfig'] = request.limitConfigShrink;
  }
  if (!$isNull(request.mcpServersShrink)) {
    query['McpServers'] = request.mcpServersShrink;
  }
  if (!$isNull(request.modelShrink)) {
    query['Model'] = request.modelShrink;
  }
  if (!$isNull(request.name)) {
    query['Name'] = request.name;
  }
  if (!$isNull(request.skillsShrink)) {
    query['Skills'] = request.skillsShrink;
  }
  if (!$isNull(request.soul)) {
    query['Soul'] = request.soul;
  }
  if (!$isNull(request.subagentsShrink)) {
    query['Subagents'] = request.subagentsShrink;
  }
  if (!$isNull(request.templateShrink)) {
    query['Template'] = request.templateShrink;
  }
  if (!$isNull(request.versionCode)) {
    query['VersionCode'] = request.versionCode;
  }
  var body : map[string]any = {};
  if (!$isNull(request.clientToken)) {
    body['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.credentialsShrink)) {
    body['Credentials'] = request.credentialsShrink;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateWorker',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates a Worker instance with specified configurations such as model, skills, template, MCP servers, and channels.
 *
 * @description Creates a Worker instance with specified configurations such as model, skills, template, MCP servers, and channels.
 *
 * @param request CreateWorkerRequest
 * @return CreateWorkerResponse
 */
async function createWorker(request: CreateWorkerRequest): CreateWorkerResponse {
  var runtime = new $RuntimeOptions{};
  return createWorkerWithOptions(request, runtime);
}

model CreateWorkerBootstrapTokenRequest {
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='inst-demo'),
  name?: string(name='Name', description='The name.

This parameter is required.', example='worker-demo'),
  networkType?: string(name='NetworkType', description='The network type.

This parameter is required.', example='INTRANET'),
}

model CreateWorkerBootstrapTokenResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  data?: {
    bootstrapToken?: string(name='BootstrapToken', description='The bootstrap token.', example='bt-xxx'),
    cms?: {
      endpoint?: string(name='Endpoint', description='The access endpoint.', example='https://cms-demo'),
      licenseKey?: string(name='LicenseKey', description='The license key.', example='lk-xxx'),
      workspace?: string(name='Workspace', description='The workspace.', example='ws-demo'),
    }(name='Cms', description='The CMS configuration.', example='{}'),
    instanceId?: string(name='InstanceId', description='The instance ID.', example='inst-demo'),
    name?: string(name='Name', description='The Worker name.', example='worker-demo'),
    networkType?: string(name='NetworkType', description='The network type.', example='INTRANET'),
    tokenFingerprint?: string(name='TokenFingerprint', description='The token fingerprint.', example='sha256:xxx'),
  }(name='Data', description='The returned data.', example='{}'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='473469C7-XX-XX-XX'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model CreateWorkerBootstrapTokenResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateWorkerBootstrapTokenResponseBody(name='body'),
}

/**
 * @summary Creates a local management bootstrap token for a Worker, with support for specifying the network type.
 *
 * @description Creates a local management bootstrap token for a Worker, with support for specifying the network type.
 *
 * @param request CreateWorkerBootstrapTokenRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateWorkerBootstrapTokenResponse
 */
@context("createWorkerBootstrapTokenWithContext")
async function createWorkerBootstrapTokenWithOptions(request: CreateWorkerBootstrapTokenRequest, runtime: $RuntimeOptions): CreateWorkerBootstrapTokenResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.name)) {
    query['Name'] = request.name;
  }
  if (!$isNull(request.networkType)) {
    query['NetworkType'] = request.networkType;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateWorkerBootstrapToken',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates a local management bootstrap token for a Worker, with support for specifying the network type.
 *
 * @description Creates a local management bootstrap token for a Worker, with support for specifying the network type.
 *
 * @param request CreateWorkerBootstrapTokenRequest
 * @return CreateWorkerBootstrapTokenResponse
 */
async function createWorkerBootstrapToken(request: CreateWorkerBootstrapTokenRequest): CreateWorkerBootstrapTokenResponse {
  var runtime = new $RuntimeOptions{};
  return createWorkerBootstrapTokenWithOptions(request, runtime);
}

model DeleteCredentialRequest {
  clientToken?: string(name='ClientToken', description='The idempotence token that ensures the idempotency of the request.', example='a1b2c3d4-e5f6-7890-1234-567890abcdef'),
  instanceId?: string(name='InstanceId', description='The AgentTeams instance ID.

This parameter is required.', example='ami-2ze8x9c6f'),
  name?: string(name='Name', description='The name of the credential.

This parameter is required.', example='worker-demo'),
}

model DeleteCredentialResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model DeleteCredentialResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DeleteCredentialResponseBody(name='body'),
}

/**
 * @summary Deletes an existing credential from a specified AgentTeams instance. The credential cannot be deleted if it is still attached to a Worker.
 *
 * @description Deletes an existing credential from a specified AgentTeams instance. The credential cannot be deleted if it is still attached to a Worker.
 *
 * @param request DeleteCredentialRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteCredentialResponse
 */
@context("deleteCredentialWithContext")
async function deleteCredentialWithOptions(request: DeleteCredentialRequest, runtime: $RuntimeOptions): DeleteCredentialResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.name)) {
    query['Name'] = request.name;
  }
  var body : map[string]any = {};
  if (!$isNull(request.clientToken)) {
    body['ClientToken'] = request.clientToken;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'DeleteCredential',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Deletes an existing credential from a specified AgentTeams instance. The credential cannot be deleted if it is still attached to a Worker.
 *
 * @description Deletes an existing credential from a specified AgentTeams instance. The credential cannot be deleted if it is still attached to a Worker.
 *
 * @param request DeleteCredentialRequest
 * @return DeleteCredentialResponse
 */
async function deleteCredential(request: DeleteCredentialRequest): DeleteCredentialResponse {
  var runtime = new $RuntimeOptions{};
  return deleteCredentialWithOptions(request, runtime);
}

model DeleteInstanceRequest {
  instanceId?: string(name='InstanceId', description='The ID of the AgentTeams instance to release.

This parameter is required.', example='inst-xxx'),
}

model DeleteInstanceResponseBody = {
  code?: string(name='Code', description='The status code.', example='OK'),
  data?: {
    instanceId?: string(name='InstanceId', description='The ID of the released instance.', example='inst-xxx'),
    taskId?: string(name='TaskId', description='The ID of the asynchronous release task. You can use this ID to query the task status.', example='task-xxx'),
  }(name='Data', description='The object that contains the released instance ID and the asynchronous release task ID.', example='{}'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model DeleteInstanceResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DeleteInstanceResponseBody(name='body'),
}

/**
 * @summary Releases a specified AgentTeams instance and cleans up related resources. Supports GET and POST methods. After a successful call, the instance status changes to DELETING, and resource cleanup is performed asynchronously in the background.
 *
 * @description Releases a specified AgentTeams instance and cleans up related resources. Supports GET and POST methods. After a successful call, the instance status changes to DELETING, and resource cleanup is performed asynchronously in the background.
 *
 * @param request DeleteInstanceRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteInstanceResponse
 */
@context("deleteInstanceWithContext")
async function deleteInstanceWithOptions(request: DeleteInstanceRequest, runtime: $RuntimeOptions): DeleteInstanceResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DeleteInstance',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Releases a specified AgentTeams instance and cleans up related resources. Supports GET and POST methods. After a successful call, the instance status changes to DELETING, and resource cleanup is performed asynchronously in the background.
 *
 * @description Releases a specified AgentTeams instance and cleans up related resources. Supports GET and POST methods. After a successful call, the instance status changes to DELETING, and resource cleanup is performed asynchronously in the background.
 *
 * @param request DeleteInstanceRequest
 * @return DeleteInstanceResponse
 */
async function deleteInstance(request: DeleteInstanceRequest): DeleteInstanceResponse {
  var runtime = new $RuntimeOptions{};
  return deleteInstanceWithOptions(request, runtime);
}

model DeleteMcpRequest {
  id?: string(name='Id', description='Optional. Fuzzy matches by instance name.

This parameter is required.', example='AgentTeams'),
  instanceId?: string(name='InstanceId', description='The ID of the AgentTeams instance.

This parameter is required.', example='ins-xxx'),
}

model DeleteMcpResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='Id of the request', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the operation was successful.', example='true'),
}

model DeleteMcpResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DeleteMcpResponseBody(name='body'),
}

/**
 * @summary Deletes an MCP server from a specified AgentTeams instance. The server cannot be deleted if it is associated with any Workers.
 *
 * @description Deletes an MCP server from a specified AgentTeams instance. The server cannot be deleted if it is associated with any Workers.
 *
 * @param request DeleteMcpRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteMcpResponse
 */
@context("deleteMcpWithContext")
async function deleteMcpWithOptions(request: DeleteMcpRequest, runtime: $RuntimeOptions): DeleteMcpResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.id)) {
    body['Id'] = request.id;
  }
  if (!$isNull(request.instanceId)) {
    body['InstanceId'] = request.instanceId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'DeleteMcp',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Deletes an MCP server from a specified AgentTeams instance. The server cannot be deleted if it is associated with any Workers.
 *
 * @description Deletes an MCP server from a specified AgentTeams instance. The server cannot be deleted if it is associated with any Workers.
 *
 * @param request DeleteMcpRequest
 * @return DeleteMcpResponse
 */
async function deleteMcp(request: DeleteMcpRequest): DeleteMcpResponse {
  var runtime = new $RuntimeOptions{};
  return deleteMcpWithOptions(request, runtime);
}

model DeleteModelRequest {
  id?: string(name='Id', description='The model ID.

This parameter is required.', example='m-xxx'),
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='i-xxx'),
  providerId?: string(name='ProviderId', description='The model provider ID.', example='p-xxx'),
}

model DeleteModelResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model DeleteModelResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DeleteModelResponseBody(name='body'),
}

/**
 * @summary Deletes an AI model from a specified AgentTeams instance. The model cannot be deleted if it is still associated with a Worker.
 *
 * @description Deletes an AI model from a specified AgentTeams instance. The model cannot be deleted if it is still associated with a Worker.
 *
 * @param request DeleteModelRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteModelResponse
 */
@context("deleteModelWithContext")
async function deleteModelWithOptions(request: DeleteModelRequest, runtime: $RuntimeOptions): DeleteModelResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.id)) {
    body['Id'] = request.id;
  }
  if (!$isNull(request.instanceId)) {
    body['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.providerId)) {
    body['ProviderId'] = request.providerId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'DeleteModel',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Deletes an AI model from a specified AgentTeams instance. The model cannot be deleted if it is still associated with a Worker.
 *
 * @description Deletes an AI model from a specified AgentTeams instance. The model cannot be deleted if it is still associated with a Worker.
 *
 * @param request DeleteModelRequest
 * @return DeleteModelResponse
 */
async function deleteModel(request: DeleteModelRequest): DeleteModelResponse {
  var runtime = new $RuntimeOptions{};
  return deleteModelWithOptions(request, runtime);
}

model DeleteModelProviderRequest {
  id?: string(name='Id', description='The ID of the model provider.

This parameter is required.', example='p-xxx'),
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='i-xxx'),
}

model DeleteModelProviderResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model DeleteModelProviderResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DeleteModelProviderResponseBody(name='body'),
}

/**
 * @summary Deletes an AI model provider from a specified AgentTeams instance. The provider cannot be deleted if it still has associated models.
 *
 * @description Deletes an AI model provider from a specified AgentTeams instance. The provider cannot be deleted if it still has associated models.
 *
 * @param request DeleteModelProviderRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteModelProviderResponse
 */
@context("deleteModelProviderWithContext")
async function deleteModelProviderWithOptions(request: DeleteModelProviderRequest, runtime: $RuntimeOptions): DeleteModelProviderResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.id)) {
    body['Id'] = request.id;
  }
  if (!$isNull(request.instanceId)) {
    body['InstanceId'] = request.instanceId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'DeleteModelProvider',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Deletes an AI model provider from a specified AgentTeams instance. The provider cannot be deleted if it still has associated models.
 *
 * @description Deletes an AI model provider from a specified AgentTeams instance. The provider cannot be deleted if it still has associated models.
 *
 * @param request DeleteModelProviderRequest
 * @return DeleteModelProviderResponse
 */
async function deleteModelProvider(request: DeleteModelProviderRequest): DeleteModelProviderResponse {
  var runtime = new $RuntimeOptions{};
  return deleteModelProviderWithOptions(request, runtime);
}

model DeleteServiceEndpointRequest {
  endpointId?: string(name='EndpointId', description='**Endpoint ID**

This parameter is required.', example='ep-xxx'),
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='agentteams-demo'),
}

model DeleteServiceEndpointResponseBody = {
  code?: string(name='Code', description='The business response code. The value is `SUCCESS` upon success.', example='SUCCESS'),
  data?: boolean(name='Data', description='The value is `true` when the deletion is successful.', example='true'),
  message?: string(name='Message', description='**Response message**, which is fixed to `success` upon success.', example='success'),
  requestId?: string(name='RequestId', description='**Request ID**, which preferentially uses the `requestId` from the request context, and then the `traceId`.', example='xxx'),
  success?: boolean(name='Success', description='Indicates whether the operation is successful.', example='true'),
}

model DeleteServiceEndpointResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DeleteServiceEndpointResponseBody(name='body'),
}

/**
 * @summary Deletes an endpoint from a specified AgentTeams instance and cleans up related resources.
 *
 * @description Deletes an endpoint from a specified AgentTeams instance and cleans up related resources.
 * - This operation supports GET or POST methods.
 * - If the target endpoint is of the WORKER type, the system automatically cleans up associated APIG/AI Gateway cloud resources and KubeOne worker service configurations.
 * - The request parameters must include instanceId and endpointId, and neither can be empty.
 * - A successful response returns HTTP status code 200 and a success flag. An error response returns the corresponding HTTP status code (such as 400, 404, or 409) and an error message.
 *
 * @param request DeleteServiceEndpointRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteServiceEndpointResponse
 */
@context("deleteServiceEndpointWithContext")
async function deleteServiceEndpointWithOptions(request: DeleteServiceEndpointRequest, runtime: $RuntimeOptions): DeleteServiceEndpointResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.endpointId)) {
    query['EndpointId'] = request.endpointId;
  }
  var body : map[string]any = {};
  if (!$isNull(request.instanceId)) {
    body['InstanceId'] = request.instanceId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'DeleteServiceEndpoint',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Deletes an endpoint from a specified AgentTeams instance and cleans up related resources.
 *
 * @description Deletes an endpoint from a specified AgentTeams instance and cleans up related resources.
 * - This operation supports GET or POST methods.
 * - If the target endpoint is of the WORKER type, the system automatically cleans up associated APIG/AI Gateway cloud resources and KubeOne worker service configurations.
 * - The request parameters must include instanceId and endpointId, and neither can be empty.
 * - A successful response returns HTTP status code 200 and a success flag. An error response returns the corresponding HTTP status code (such as 400, 404, or 409) and an error message.
 *
 * @param request DeleteServiceEndpointRequest
 * @return DeleteServiceEndpointResponse
 */
async function deleteServiceEndpoint(request: DeleteServiceEndpointRequest): DeleteServiceEndpointResponse {
  var runtime = new $RuntimeOptions{};
  return deleteServiceEndpointWithOptions(request, runtime);
}

model DeleteTeamRequest {
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='agentteams-test-001'),
  name?: string(name='Name', description='The name.

This parameter is required.', example='worker-demo'),
}

model DeleteTeamResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-delete-team-001'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model DeleteTeamResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DeleteTeamResponseBody(name='body'),
}

/**
 * @summary Deletes a team under a specified instance. After deletion, the team and associated resources enter an asynchronous cleanup process.
 *
 * @description Deletes a team under a specified instance. After deletion, the team and associated resources enter an asynchronous cleanup process.
 *
 * @param request DeleteTeamRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteTeamResponse
 */
@context("deleteTeamWithContext")
async function deleteTeamWithOptions(request: DeleteTeamRequest, runtime: $RuntimeOptions): DeleteTeamResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.name)) {
    query['Name'] = request.name;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DeleteTeam',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Deletes a team under a specified instance. After deletion, the team and associated resources enter an asynchronous cleanup process.
 *
 * @description Deletes a team under a specified instance. After deletion, the team and associated resources enter an asynchronous cleanup process.
 *
 * @param request DeleteTeamRequest
 * @return DeleteTeamResponse
 */
async function deleteTeam(request: DeleteTeamRequest): DeleteTeamResponse {
  var runtime = new $RuntimeOptions{};
  return deleteTeamWithOptions(request, runtime);
}

model DeleteUserRequest {
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='agentteams-test-001'),
  name?: string(name='Name', description='The name.

This parameter is required.', example='worker-demo'),
}

model DeleteUserResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-delete-user-001'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model DeleteUserResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DeleteUserResponseBody(name='body'),
}

/**
 * @summary Deletes a specified user from a specified instance. After deletion, the user cannot log on to or access instance resources. Proceed with caution.
 *
 * @description Deletes a specified user from a specified instance. After deletion, the user cannot log on to or access instance resources. Proceed with caution.
 *
 * @param request DeleteUserRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteUserResponse
 */
@context("deleteUserWithContext")
async function deleteUserWithOptions(request: DeleteUserRequest, runtime: $RuntimeOptions): DeleteUserResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.name)) {
    query['Name'] = request.name;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DeleteUser',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Deletes a specified user from a specified instance. After deletion, the user cannot log on to or access instance resources. Proceed with caution.
 *
 * @description Deletes a specified user from a specified instance. After deletion, the user cannot log on to or access instance resources. Proceed with caution.
 *
 * @param request DeleteUserRequest
 * @return DeleteUserResponse
 */
async function deleteUser(request: DeleteUserRequest): DeleteUserResponse {
  var runtime = new $RuntimeOptions{};
  return deleteUserWithOptions(request, runtime);
}

model DeleteWorkerRequest {
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='inst-demo'),
  name?: string(name='Name', description='The name.

This parameter is required.', example='worker-demo'),
}

model DeleteWorkerResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='473469C7-XX-XX-XX'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model DeleteWorkerResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DeleteWorkerResponseBody(name='body'),
}

/**
 * @summary Deletes a Worker under a specified instance.
 *
 * @description Deletes a Worker under a specified instance.
 *
 * @param request DeleteWorkerRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteWorkerResponse
 */
@context("deleteWorkerWithContext")
async function deleteWorkerWithOptions(request: DeleteWorkerRequest, runtime: $RuntimeOptions): DeleteWorkerResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.name)) {
    query['Name'] = request.name;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DeleteWorker',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Deletes a Worker under a specified instance.
 *
 * @description Deletes a Worker under a specified instance.
 *
 * @param request DeleteWorkerRequest
 * @return DeleteWorkerResponse
 */
async function deleteWorker(request: DeleteWorkerRequest): DeleteWorkerResponse {
  var runtime = new $RuntimeOptions{};
  return deleteWorkerWithOptions(request, runtime);
}

model GetCredentialRequest {
  instanceId?: string(name='InstanceId', description='The AgentTeams instance ID.

This parameter is required.', example='ami-2ze8x9c6f'),
  name?: string(name='Name', description='The name.

This parameter is required.', example='worker-demo'),
}

model GetCredentialResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  data?: {
    boundWorkers?: [ 
      {
        name?: string(name='Name', description='The Worker name.', example='worker-xxx'),
        status?: string(name='Status', description='The Worker status.', example='Running'),
        updateTime?: string(name='UpdateTime', description='The binding update time in ISO 8601 format.', example='2026-07-13T12:00:00Z'),
      }
    ](name='BoundWorkers', description='The list of bound Workers.', example='[]'),
    createTime?: string(name='CreateTime', description='The creation time in ISO 8601 format.', example='2026-07-13T12:00:00Z'),
    description?: string(name='Description', description='The credential description.', example='OpenAI API key for the team'),
    instanceId?: string(name='InstanceId', description='The instance ID.', example='ami-2ze8x9c6f'),
    name?: string(name='Name', description='The credential key.', example='OPENAI_API_KEY'),
    regionId?: string(name='RegionId', description='The region ID.', example='cn-hangzhou'),
    status?: string(name='Status', description='The resource status.', example='Active'),
    updateTime?: string(name='UpdateTime', description='The update time in ISO 8601 format.', example='2026-07-13T12:00:00Z'),
  }(name='Data', description='The returned data.', example='{}'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model GetCredentialResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetCredentialResponseBody(name='body'),
}

/**
 * @summary Queries the details of a specified credential under an AgentTeams instance, including the status, description, and list of bound Workers.
 *
 * @description Queries the details of a specified credential under an AgentTeams instance, including the status, description, and list of bound Workers.
 *
 * @param request GetCredentialRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetCredentialResponse
 */
@context("getCredentialWithContext")
async function getCredentialWithOptions(request: GetCredentialRequest, runtime: $RuntimeOptions): GetCredentialResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.name)) {
    query['Name'] = request.name;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetCredential',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the details of a specified credential under an AgentTeams instance, including the status, description, and list of bound Workers.
 *
 * @description Queries the details of a specified credential under an AgentTeams instance, including the status, description, and list of bound Workers.
 *
 * @param request GetCredentialRequest
 * @return GetCredentialResponse
 */
async function getCredential(request: GetCredentialRequest): GetCredentialResponse {
  var runtime = new $RuntimeOptions{};
  return getCredentialWithOptions(request, runtime);
}

model GetIdentityProviderRequest {
  identityProviderType?: string(name='IdentityProviderType', description='The identity provider type. Currently, only DingTalk is supported.

This parameter is required.', example='DingTalk'),
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='agentteams-xxx'),
}

model GetIdentityProviderResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  data?: {
    eventSubscriptionCallbackUrl?: string(name='EventSubscriptionCallbackUrl', description='The event subscription callback URL.', example='https://example.com/callback'),
    identityProviderType?: string(name='IdentityProviderType', description='The identity provider type.', example='DingTalk'),
    idpMetadata?: map[string]string(name='IdpMetadata', description='The identity provider metadata.', example='{}'),
    instanceId?: string(name='InstanceId', description='The instance ID.', example='agentteams-demo'),
    loginCallbackUrl?: string(name='LoginCallbackUrl', description='The logon callback URL.', example='https://example.com/login'),
    loginEnabled?: boolean(name='LoginEnabled', description='Indicates whether logon is enabled.', example='true'),
    status?: string(name='Status', description='The status.', example='Running'),
    syncEnabled?: boolean(name='SyncEnabled', description='Indicates whether synchronization is enabled.', example='false'),
  }(name='Data', description='The returned data.', example='{}'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model GetIdentityProviderResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetIdentityProviderResponseBody(name='body'),
}

/**
 * @summary Queries the binding details of an upstream identity provider for a specified instance and identity provider type, including the logon callback URL and metadata.
 *
 * @description Queries the binding details of an upstream identity provider for a specified instance and identity provider type, including the logon callback URL and metadata.
 *
 * @param request GetIdentityProviderRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetIdentityProviderResponse
 */
@context("getIdentityProviderWithContext")
async function getIdentityProviderWithOptions(request: GetIdentityProviderRequest, runtime: $RuntimeOptions): GetIdentityProviderResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.identityProviderType)) {
    query['IdentityProviderType'] = request.identityProviderType;
  }
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetIdentityProvider',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the binding details of an upstream identity provider for a specified instance and identity provider type, including the logon callback URL and metadata.
 *
 * @description Queries the binding details of an upstream identity provider for a specified instance and identity provider type, including the logon callback URL and metadata.
 *
 * @param request GetIdentityProviderRequest
 * @return GetIdentityProviderResponse
 */
async function getIdentityProvider(request: GetIdentityProviderRequest): GetIdentityProviderResponse {
  var runtime = new $RuntimeOptions{};
  return getIdentityProviderWithOptions(request, runtime);
}

model GetInstanceRequest {
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='instance-2'),
}

model GetInstanceResponseBody = {
  code?: string(name='Code', description='The response code. The value is fixed as SUCCESS.', example='SUCCESS'),
  data?: {
    aiRegistryNamespaceId?: string(name='AiRegistryNamespaceId', description='The AI Registry namespace ID.', example='b5b4e373-xxxx-xxxx-xxxx-5d273df67432'),
    createTime?: string(name='CreateTime', description='The creation time.', example='2026-06-05T10:00:00Z'),
    instanceId?: string(name='InstanceId', description='The instance ID.', example='instance-2'),
    instanceName?: string(name='InstanceName', description='The instance name.', example='agentteams-demo'),
    instanceSpec?: string(name='InstanceSpec', description='The instance specification. The value is returned in a format such as small.x1.', example='small.x1'),
    networkType?: string(name='NetworkType', description='The network type.', example='PRIVATE_NET'),
    ossBucketName?: string(name='OssBucketName', description='The OSS bucket name.', example='agentteams-bucket'),
    paymentType?: string(name='PaymentType', description='The billing method.', example='Subscription'),
    regionId?: string(name='RegionId', description='The region ID.', example='cn-hangzhou'),
    securityGroup?: string(name='SecurityGroup', description='The security group ID.', example='sg-bp1xxxx'),
    status?: string(name='Status', description='The instance status. Valid values:
- RUNNING: running.
- STOPPED: stopped.
- CREATING: being created.', example='RUNNING'),
    updateTime?: string(name='UpdateTime', description='The update time.', example='2026-06-05T10:05:00Z'),
    vpcId?: string(name='VpcId', description='VPC ID', example='vpc-bp1xxxx'),
    zones?: [ 
      {
        vswitchId?: string(name='VswitchId', description='The vSwitch ID.', example='vsw-bp1xxxx'),
        zoneId?: string(name='ZoneId', description='The zone ID.', example='cn-hangzhou-h'),
      }
    ](name='Zones', description='The list of vSwitch IDs.', example='[...]'),
  }(name='Data', description='The instance details.', example='{}'),
  message?: string(name='Message', description='The response message. The value is fixed as success.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='request-1'),
  success?: boolean(name='Success', description='Indicates whether the request was successful. The value is fixed as `true`.', example='true'),
}

model GetInstanceResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetInstanceResponseBody(name='body'),
}

/**
 * @summary Queries the details of a specified instance by instance ID. Supports GET and POST methods. A successful response returns the detailed configuration and status of the instance.
 *
 * @description Queries the details of a specified instance by instance ID. Supports GET and POST methods. A successful response returns the detailed configuration and status of the instance.
 *
 * @param request GetInstanceRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetInstanceResponse
 */
@context("getInstanceWithContext")
async function getInstanceWithOptions(request: GetInstanceRequest, runtime: $RuntimeOptions): GetInstanceResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetInstance',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the details of a specified instance by instance ID. Supports GET and POST methods. A successful response returns the detailed configuration and status of the instance.
 *
 * @description Queries the details of a specified instance by instance ID. Supports GET and POST methods. A successful response returns the detailed configuration and status of the instance.
 *
 * @param request GetInstanceRequest
 * @return GetInstanceResponse
 */
async function getInstance(request: GetInstanceRequest): GetInstanceResponse {
  var runtime = new $RuntimeOptions{};
  return getInstanceWithOptions(request, runtime);
}

model GetInstanceAsyncTaskRequest {
  instanceId?: string(name='InstanceId', description='The ID of the AgentTeams instance.

This parameter is required.', example='at-xxx'),
  maxResults?: int32(name='MaxResults', description='The page size. Default value: `100`. Valid values: `1` to `100`.', example='100'),
  nextToken?: string(name='NextToken', description='The token for the next page. Do not specify this parameter for the first request.', example='null'),
  status?: string(name='Status', description='The task status filter.', example='PAUSED'),
  taskCode?: string(name='TaskCode', description='The task code. If not specified, all asynchronous tasks are queried. You can pass in a `TaskCode` to filter results. To query multiple task types, submit separate requests.

Currently supported `taskCode` values include: `CreateInstance`, `RenewInstance`, `UpdateInstance`, `ReleaseInstance`, `ConfigureNatGateway`, `UpgradeInstance`, `CreateWorkerEndpoint`, and `DeleteWorkerEndpoint`.

Legacy task codes such as `LIFECYCLE_MAGIC_PAY_ORDER_CALLBACK_CREATE` are supported for backward compatibility, but `CreateInstance` is recommended.', example='LIFECYCLE_MAGIC_PAY_ORDER_CALLBACK_CREATE'),
}

model GetInstanceAsyncTaskResponseBody = {
  code?: string(name='Code', description='SUCCESS', example='SUCCESS'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  items?: [ 
    {
      currentStep?: string(name='CurrentStep', description='The current step.', example='create-cms-workspace'),
      recoveryMessage?: {
        code?: string(name='Code', description='The error code. Valid values:
- CMS_WORKSPACE_QUOTA_EXCEEDED: CMS workspace quota exceeded.', example='CMS_WORKSPACE_QUOTA_EXCEEDED'),
        message?: string(name='Message', description='The specific error message.', example='CMS workspace quota exceeded'),
        occurredAt?: string(name='OccurredAt', description='The time when the error occurred.', example='2026-06-01T12:00:00Z'),
        recoverySuggestion?: string(name='RecoverySuggestion', description='The suggested recovery action.', example='Please increase the CMS workspace quota or clean up unused workspaces.'),
        retryable?: boolean(name='Retryable', description='Indicates whether the task can be retried.', example='true'),
        source?: string(name='Source', description='The error source. Valid values:
- CMS: content management system.', example='CMS'),
        type?: string(name='Type', description='The recovery type. Valid values:
- ERROR_RECOVERY: error recovery.', example='ERROR_RECOVERY'),
      }(name='RecoveryMessage', description='The recovery message returned to the user when the task status is paused.', example='{}'),
      taskCode?: string(name='TaskCode', description='The task code. Valid values:
- LIFECYCLE_MAGIC_PAY_ORDER_CALLBACK_CREATE: instance creation task.
- MAGIC_BIND_IDENTITY_PROVIDER: upstream identity provider binding task.', example='LIFECYCLE_MAGIC_PAY_ORDER_CALLBACK_CREATE'),
      taskId?: string(name='TaskId', description='The task ID.', example='task-xxx'),
      taskStatus?: string(name='TaskStatus', description='The task status. Valid values:
- PAUSED: paused.
- RUNNING: running.
- SUCCEEDED: succeeded.
- FAILED: failed.', example='PAUSED'),
      waitingForUserAction?: boolean(name='WaitingForUserAction', description='Indicates whether the task is waiting for user action. When `taskStatus = PAUSED`, this value is true and `recoveryMessage` is returned to display recovery instructions to the user. When the task is not paused, this value is false and `recoveryMessage` is empty.', example='true'),
    }
  ](name='Items', description='The returned list. The `items` array may contain multiple tasks with the same `taskCode`.', example='[...]'),
  maxResults?: int32(name='MaxResults', description='The page size. Default value: 100. Valid values: 1 to 100.', example='100'),
  message?: string(name='Message', description='success', example='success'),
  nextToken?: string(name='NextToken', description='The token for the next page. This parameter is not returned for the first request.', example='null'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
  totalCount?: int32(name='TotalCount', description='The total number of tasks.', example='1'),
}

model GetInstanceAsyncTaskResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetInstanceAsyncTaskResponseBody(name='body'),
}

/**
 * @summary Queries the status of asynchronous tasks associated with a specified AgentTeams instance, with pagination support.
 *
 * @description Queries the status of asynchronous tasks associated with a specified AgentTeams instance, with paging support.
 * - This operation queries the execution status of asynchronous tasks under a specific AgentTeams instance.
 * - Currently, only instance creation tasks related to the instance lifecycle are supported.
 * - Use the taskCode parameter to specify the task type to query. The default is the instance creation task.
 * - Use maxResults and nextToken for result paging.
 * - When a task is in the PAUSED state, the response includes information about the action the user needs to take (recoveryMessage).
 * - Querying task status directly by taskId is not currently supported.
 *
 * @param request GetInstanceAsyncTaskRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetInstanceAsyncTaskResponse
 */
@context("getInstanceAsyncTaskWithContext")
async function getInstanceAsyncTaskWithOptions(request: GetInstanceAsyncTaskRequest, runtime: $RuntimeOptions): GetInstanceAsyncTaskResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.maxResults)) {
    query['MaxResults'] = request.maxResults;
  }
  if (!$isNull(request.nextToken)) {
    query['NextToken'] = request.nextToken;
  }
  if (!$isNull(request.status)) {
    query['Status'] = request.status;
  }
  if (!$isNull(request.taskCode)) {
    query['TaskCode'] = request.taskCode;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetInstanceAsyncTask',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the status of asynchronous tasks associated with a specified AgentTeams instance, with pagination support.
 *
 * @description Queries the status of asynchronous tasks associated with a specified AgentTeams instance, with paging support.
 * - This operation queries the execution status of asynchronous tasks under a specific AgentTeams instance.
 * - Currently, only instance creation tasks related to the instance lifecycle are supported.
 * - Use the taskCode parameter to specify the task type to query. The default is the instance creation task.
 * - Use maxResults and nextToken for result paging.
 * - When a task is in the PAUSED state, the response includes information about the action the user needs to take (recoveryMessage).
 * - Querying task status directly by taskId is not currently supported.
 *
 * @param request GetInstanceAsyncTaskRequest
 * @return GetInstanceAsyncTaskResponse
 */
async function getInstanceAsyncTask(request: GetInstanceAsyncTaskRequest): GetInstanceAsyncTaskResponse {
  var runtime = new $RuntimeOptions{};
  return getInstanceAsyncTaskWithOptions(request, runtime);
}

model GetInstanceOssMountRamAuthorizeUrlRequest {
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='at-cn-plt4uk4bm15'),
}

model GetInstanceOssMountRamAuthorizeUrlResponseBody = {
  code?: string(name='Code', description='The status code.', example='SUCCESS'),
  data?: {
    authorizeUrl?: string(name='AuthorizeUrl', description='The RAM console authorization URL.', example='https://ram.console.aliyun.com/authorize?requestId=xxx'),
  }(name='Data', description='The RAM authorization URL information.', example='{}'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='request-1'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model GetInstanceOssMountRamAuthorizeUrlResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetInstanceOssMountRamAuthorizeUrlResponseBody(name='body'),
}

/**
 * @summary Retrieves the RAM authorization URL required to mount OSS to ACS for an instance.
 *
 * @description Retrieves the RAM authorization URL required to mount OSS to ACS for an instance.
 * - This operation supports GET and POST methods.
 * - The X-User-Id header must be included in the request to verify instance ownership.
 * - The InstanceId parameter is required to specify the instance. The backend generates the authorization URL based on the instance information.
 * - A successful response returns the RAM console authorization URL without creating a RAM role or policy.
 *
 * @param request GetInstanceOssMountRamAuthorizeUrlRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetInstanceOssMountRamAuthorizeUrlResponse
 */
@context("getInstanceOssMountRamAuthorizeUrlWithContext")
async function getInstanceOssMountRamAuthorizeUrlWithOptions(request: GetInstanceOssMountRamAuthorizeUrlRequest, runtime: $RuntimeOptions): GetInstanceOssMountRamAuthorizeUrlResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetInstanceOssMountRamAuthorizeUrl',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves the RAM authorization URL required to mount OSS to ACS for an instance.
 *
 * @description Retrieves the RAM authorization URL required to mount OSS to ACS for an instance.
 * - This operation supports GET and POST methods.
 * - The X-User-Id header must be included in the request to verify instance ownership.
 * - The InstanceId parameter is required to specify the instance. The backend generates the authorization URL based on the instance information.
 * - A successful response returns the RAM console authorization URL without creating a RAM role or policy.
 *
 * @param request GetInstanceOssMountRamAuthorizeUrlRequest
 * @return GetInstanceOssMountRamAuthorizeUrlResponse
 */
async function getInstanceOssMountRamAuthorizeUrl(request: GetInstanceOssMountRamAuthorizeUrlRequest): GetInstanceOssMountRamAuthorizeUrlResponse {
  var runtime = new $RuntimeOptions{};
  return getInstanceOssMountRamAuthorizeUrlWithOptions(request, runtime);
}

model GetMcpRequest {
  id?: string(name='Id', description='The MCP server ID.

This parameter is required.', example='mcp-xxx'),
  instanceId?: string(name='InstanceId', description='Optional. The instance name for fuzzy matching.

This parameter is required.', example='AgentTeams'),
}

model GetMcpResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  data?: {
    addresses?: [ string ](name='Addresses', description='The list of MCP server addresses.', example='["https://example.com/mcp"]'),
    authConfig?: string(name='AuthConfig', description='The authentication information.', example='{"type":"bearer","token":"xxx"}'),
    authEnabled?: boolean(name='AuthEnabled', description='Indicates whether authentication is enabled.', example='false'),
    createType?: string(name='CreateType', description='The creation type.', example='DIRECT_PROXY'),
    deployStatus?: string(name='DeployStatus', description='The deployment status.', example='DEPLOYED'),
    description?: string(name='Description', description='The MCP server description.', example='This is an MCP server.'),
    id?: string(name='Id', description='The MCP server ID.', example='mcp-xxx'),
    mcpServerConfig?: string(name='McpServerConfig', description='The MCP server configuration.', example='{}'),
    name?: string(name='Name', description='The MCP server name.', example='my-mcp-server'),
    protocol?: string(name='Protocol', description='The protocol type.', example='streamable'),
    swaggerConfig?: string(name='SwaggerConfig', description='The Swagger configuration.', example='{"url":"https://example.com/swagger.json"}'),
    url?: string(name='Url', description='The MCP server access URL.', example='https://example.com/mcp'),
  }(name='Data', description='The returned data.', example='{}'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='Id of the request', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model GetMcpResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetMcpResponseBody(name='body'),
}

/**
 * @summary Queries the details of a specified MCP server, including the address, authentication configuration, deployment status, and protocol.
 *
 * @description Queries the details of a specified MCP server, including the address, authentication configuration, deployment status, and protocol.
 *
 * @param request GetMcpRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetMcpResponse
 */
@context("getMcpWithContext")
async function getMcpWithOptions(request: GetMcpRequest, runtime: $RuntimeOptions): GetMcpResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.id)) {
    query['Id'] = request.id;
  }
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetMcp',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the details of a specified MCP server, including the address, authentication configuration, deployment status, and protocol.
 *
 * @description Queries the details of a specified MCP server, including the address, authentication configuration, deployment status, and protocol.
 *
 * @param request GetMcpRequest
 * @return GetMcpResponse
 */
async function getMcp(request: GetMcpRequest): GetMcpResponse {
  var runtime = new $RuntimeOptions{};
  return getMcpWithOptions(request, runtime);
}

model GetModelInvocationSummaryRequest {
  endTime?: string(name='EndTime', description='The end time of the query. Unit: milliseconds (UNIX timestamp).', example='1718086400000'),
  instanceId?: string(name='InstanceId', description='The instance ID.', example='i-xxx'),
  startTime?: string(name='StartTime', description='The start time of the query. Unit: milliseconds (UNIX timestamp).', example='1718000000000'),
}

model GetModelInvocationSummaryResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  data?: {
    callFrequency?: double(name='CallFrequency', description='The call frequency. Unit: calls per minute.', example='0.5'),
    providerDistribution?: [ 
      {
        count?: int32(name='Count', description='The number of calls.', example='455'),
        percentage?: double(name='Percentage', description='The percentage.', example='0.65'),
        providerName?: string(name='ProviderName', description='The provider name.', example='my-provider'),
      }
    ](name='ProviderDistribution', description='The provider call distribution.', example='[...]'),
    todayCallCount?: int32(name='TodayCallCount', description='The number of calls made today.', example='100'),
    todayChangeRate?: double(name='TodayChangeRate', description='The change rate of calls made today.', example='0.15'),
    weekCallCount?: int32(name='WeekCallCount', description='The number of calls made this week.', example='700'),
    weekChangeRate?: double(name='WeekChangeRate', description='The change rate of calls made this week.', example='0.05'),
  }(name='Data', description='The returned data.', example='{}'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model GetModelInvocationSummaryResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetModelInvocationSummaryResponseBody(name='body'),
}

/**
 * @summary Queries the model call summary for a specified AgentTeams instance within a specified time range, including today\\"s and this week\\"s call counts, change rates, call frequency, and provider distribution.
 *
 * @description Queries the model call summary for a specified AgentTeams instance within a specified time range, including today\\"s and this week\\"s call counts, change rates, call frequency, and provider distribution.
 *
 * @param request GetModelInvocationSummaryRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetModelInvocationSummaryResponse
 */
@context("getModelInvocationSummaryWithContext")
async function getModelInvocationSummaryWithOptions(request: GetModelInvocationSummaryRequest, runtime: $RuntimeOptions): GetModelInvocationSummaryResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.endTime)) {
    query['EndTime'] = request.endTime;
  }
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.startTime)) {
    query['StartTime'] = request.startTime;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetModelInvocationSummary',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the model call summary for a specified AgentTeams instance within a specified time range, including today\\"s and this week\\"s call counts, change rates, call frequency, and provider distribution.
 *
 * @description Queries the model call summary for a specified AgentTeams instance within a specified time range, including today\\"s and this week\\"s call counts, change rates, call frequency, and provider distribution.
 *
 * @param request GetModelInvocationSummaryRequest
 * @return GetModelInvocationSummaryResponse
 */
async function getModelInvocationSummary(request: GetModelInvocationSummaryRequest): GetModelInvocationSummaryResponse {
  var runtime = new $RuntimeOptions{};
  return getModelInvocationSummaryWithOptions(request, runtime);
}

model GetModelProviderRequest {
  id?: string(name='Id', description='The ID of the model provider.

This parameter is required.', example='p-xxx'),
  instanceId?: string(name='InstanceId', description='Optional. Fuzzy matches by instance name.

This parameter is required.', example='AgentTeams'),
}

model GetModelProviderResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  data?: {
    address?: string(name='Address', description='The address of the model provider.', example='https://api.example.com'),
    apiKeys?: [ string ](name='ApiKeys', description='The list of API keys.', example='["sk-xxx"]'),
    createTime?: string(name='CreateTime', description='The creation time.', example='2026-06-11T10:00:00Z'),
    deployStatus?: string(name='DeployStatus', description='The deployment status.', example='DEPLOYED'),
    description?: string(name='Description', description='The description of the model provider.', example='my provider'),
    id?: string(name='Id', description='The ID of the model provider.', example='p-xxx'),
    instanceId?: string(name='InstanceId', description='The instance ID.', example='i-xxx'),
    name?: string(name='Name', description='The name of the model provider.', example='my-provider'),
    protocols?: [ string ](name='Protocols', description='The list of protocols supported by the model provider.', example='["openai"]'),
    provider?: string(name='Provider', description='The provider identifier.', example='openai'),
    regionId?: string(name='RegionId', description='The region ID.', example='cn-hangzhou'),
  }(name='Data', description='The returned data.', example='{}'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='Id of the request', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model GetModelProviderResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetModelProviderResponseBody(name='body'),
}

/**
 * @summary Queries the details of a single AI model provider, including the name, address, protocol list, API keys, and deployment status.
 *
 * @description Queries the details of a single AI model provider, including the name, address, protocol list, API keys, and deployment status.
 *
 * @param request GetModelProviderRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetModelProviderResponse
 */
@context("getModelProviderWithContext")
async function getModelProviderWithOptions(request: GetModelProviderRequest, runtime: $RuntimeOptions): GetModelProviderResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.id)) {
    query['Id'] = request.id;
  }
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetModelProvider',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the details of a single AI model provider, including the name, address, protocol list, API keys, and deployment status.
 *
 * @description Queries the details of a single AI model provider, including the name, address, protocol list, API keys, and deployment status.
 *
 * @param request GetModelProviderRequest
 * @return GetModelProviderResponse
 */
async function getModelProvider(request: GetModelProviderRequest): GetModelProviderResponse {
  var runtime = new $RuntimeOptions{};
  return getModelProviderWithOptions(request, runtime);
}

model GetNatGatewayStatusRequest {
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='inst-1'),
}

model GetNatGatewayStatusResponseBody = {
  code?: string(name='Code', description='The status code.', example='Success'),
  data?: {
    instanceId?: string(name='InstanceId', description='The instance ID.', example='inst-1'),
    natGatewayConfigured?: boolean(name='NatGatewayConfigured', description='Indicates whether the NAT gateway is configured.', example='true'),
    natGateways?: [ 
      {
        natGatewayId?: string(name='NatGatewayId', description='The NAT gateway ID.', example='ngw-xxx'),
        snatConfigured?: boolean(name='SnatConfigured', description='Indicates whether the SNAT rule is configured.', example='true'),
        snatTableId?: string(name='SnatTableId', description='The SNAT table ID.', example='stb-xxx'),
        status?: string(name='Status', description='The status of the NAT gateway. Valid values:
 * Available: available', example='Available'),
      }
    ](name='NatGateways', description='The list of NAT gateway details.', example='[...]'),
    snatConfigured?: boolean(name='SnatConfigured', description='Indicates whether the SNAT rule is configured.', example='true'),
    status?: string(name='Status', description='The status of the NAT gateway. Valid values:
 * READY: The NAT gateway exists, and the zone/vSwitch CIDR selected by the current instance is covered by SNAT.
 * NEED_CONFIGURE_NAT_GATEWAY: No NAT gateway exists in the VPC associated with the current instance. You need to purchase or configure a NAT gateway.
 * NEED_CONFIGURE_SNAT_RULE: The NAT gateway exists, but the zone/vSwitch CIDR selected by the current instance is not fully added to SNAT. You need to add SNAT rules.', example='READY'),
    vpcId?: string(name='VpcId', description='VPC ID', example='vpc-xxx'),
    zoneCidrCovered?: boolean(name='ZoneCidrCovered', description='Indicates whether the selected zone/vSwitch CIDR is covered.', example='true'),
    zoneCidrs?: [ 
      {
        cidrBlock?: string(name='CidrBlock', description='The CIDR block.', example='192.168.1.0/24'),
        covered?: boolean(name='Covered', description='Indicates whether the CIDR block is covered by a SNAT rule.', example='true'),
        natGatewayId?: string(name='NatGatewayId', description='The associated NAT gateway ID. This value is null if not configured.', example='ngw-xxx'),
        snatEntryId?: string(name='SnatEntryId', description='The SNAT entry ID. This value is null if not configured.', example='snat-xxx'),
        snatSourceCidr?: string(name='SnatSourceCidr', description='The SNAT source CIDR. This value is null if not configured.', example='192.168.1.0/24'),
        vSwitchId?: string(name='VSwitchId', description='The vSwitch ID.', example='vsw-xxx'),
        zoneId?: string(name='ZoneId', description='The zone ID.', example='cn-hangzhou-i'),
      }
    ](name='ZoneCidrs', description='The CIDR blocks and SNAT configuration status of each zone/vSwitch in the VPC.', example='[...]'),
  }(name='Data', description='The NAT gateway configuration status information.', example='{}'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model GetNatGatewayStatusResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetNatGatewayStatusResponseBody(name='body'),
}

/**
 * @summary Queries the configuration status of the NAT gateway and its SNAT rules for a specified instance.
 *
 * @description Queries the configuration status of the NAT gateway and its SNAT rules for a specified instance.
 * - This API allows you to retrieve the NAT gateway configuration details and SNAT rule status associated with a specific instance. This operation supports GET or POST method calls and requires the instanceId as a request parameter to specify the instance to query.
 * - Ensure that the provided instanceId is valid and belongs to your account.
 * - Based on the returned status values (such as READY, NEED_CONFIGURE_NAT_GATEWAY, or NEED_CONFIGURE_SNAT_RULE), take the corresponding actions to complete the NAT gateway or SNAT rule configuration.
 * - When the status is NEED_CONFIGURE_NAT_GATEWAY, it indicates that no available NAT gateway exists in the current VPC. NEED_CONFIGURE_SNAT_RULE means that a NAT gateway exists but some subnet CIDRs are not covered by SNAT rules.
 *
 * @param request GetNatGatewayStatusRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetNatGatewayStatusResponse
 */
@context("getNatGatewayStatusWithContext")
async function getNatGatewayStatusWithOptions(request: GetNatGatewayStatusRequest, runtime: $RuntimeOptions): GetNatGatewayStatusResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetNatGatewayStatus',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the configuration status of the NAT gateway and its SNAT rules for a specified instance.
 *
 * @description Queries the configuration status of the NAT gateway and its SNAT rules for a specified instance.
 * - This API allows you to retrieve the NAT gateway configuration details and SNAT rule status associated with a specific instance. This operation supports GET or POST method calls and requires the instanceId as a request parameter to specify the instance to query.
 * - Ensure that the provided instanceId is valid and belongs to your account.
 * - Based on the returned status values (such as READY, NEED_CONFIGURE_NAT_GATEWAY, or NEED_CONFIGURE_SNAT_RULE), take the corresponding actions to complete the NAT gateway or SNAT rule configuration.
 * - When the status is NEED_CONFIGURE_NAT_GATEWAY, it indicates that no available NAT gateway exists in the current VPC. NEED_CONFIGURE_SNAT_RULE means that a NAT gateway exists but some subnet CIDRs are not covered by SNAT rules.
 *
 * @param request GetNatGatewayStatusRequest
 * @return GetNatGatewayStatusResponse
 */
async function getNatGatewayStatus(request: GetNatGatewayStatusRequest): GetNatGatewayStatusResponse {
  var runtime = new $RuntimeOptions{};
  return getNatGatewayStatusWithOptions(request, runtime);
}

model GetServiceEndpointRequest {
  endpointId?: string(name='EndpointId', description='**Endpoint ID**

This parameter is required.', example='mep-test0001'),
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='agentteams-demo'),
}

model GetServiceEndpointResponseBody = {
  code?: string(name='Code', description='The response code returned on success.', example='SUCCESS'),
  data?: {
    certIdentifier?: string(name='CertIdentifier', description='The certificate identifier.', example='cert-xxx'),
    component?: string(name='Component', description='The component type. Valid values:
 * ELEMENT: element
 * TUNNEL: tunnel
 * MATRIX: matrix
 * WORKER: worker', example='WORKER'),
    createTime?: string(name='CreateTime', description='The creation time.', example='2024-01-01T00:00:00Z'),
    domain?: string(name='Domain', description='The domain name.', example='matrix.example.com'),
    domainType?: string(name='DomainType', description='The domain name type. Valid values:
 * BUILTIN: built-in
 * CUSTOM: custom', example='CUSTOM'),
    endpointId?: string(name='EndpointId', description='Endpoint ID', example='mep-test0001'),
    endpointName?: string(name='EndpointName', description='The service name.', example='matrix-service'),
    instanceId?: string(name='InstanceId', description='The instance ID.', example='agentteams-demo'),
    networkType?: string(name='NetworkType', description='The network type. Valid values:
 * NONE: none
 * INTRANET: internal network
 * INTERNET: Internet', example='INTERNET'),
    regionId?: string(name='RegionId', description='The region ID.', example='cn-hangzhou'),
    status?: string(name='Status', description='The status.', example='CONFIGURED'),
    updateTime?: string(name='UpdateTime', description='The update time.', example='2024-01-01T00:00:00Z'),
  }(name='Data', description='The details of the endpoint.', example='{}'),
  message?: string(name='Message', description='**message**', example='success'),
  requestId?: string(name='RequestId', description='**requestId**', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the operation was successful.', example='true'),
}

model GetServiceEndpointResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetServiceEndpointResponseBody(name='body'),
}

/**
 * @summary Queries the details of an endpoint with a specified ID. You can verify the endpoint by instance ID.
 *
 * @description Queries the details of an endpoint with a specified ID. You can verify the endpoint by instance ID.
 * - This API operation queries the configuration and status information of a single endpoint based on the endpointId.
 * - The optional parameter instanceId is used to verify whether the endpoint belongs to a specific instance.
 * - The request supports both GET and POST methods. GET passes parameters through the query string, while POST submits parameters through form data.
 * - If endpointId is missing or empty, an InvalidParameter error is returned.
 * - If the requested endpoint does not exist, does not belong to the specified instance, or does not belong to the current user, a resource-not-found error is returned.
 *
 * @param request GetServiceEndpointRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetServiceEndpointResponse
 */
@context("getServiceEndpointWithContext")
async function getServiceEndpointWithOptions(request: GetServiceEndpointRequest, runtime: $RuntimeOptions): GetServiceEndpointResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.endpointId)) {
    query['EndpointId'] = request.endpointId;
  }
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetServiceEndpoint',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the details of an endpoint with a specified ID. You can verify the endpoint by instance ID.
 *
 * @description Queries the details of an endpoint with a specified ID. You can verify the endpoint by instance ID.
 * - This API operation queries the configuration and status information of a single endpoint based on the endpointId.
 * - The optional parameter instanceId is used to verify whether the endpoint belongs to a specific instance.
 * - The request supports both GET and POST methods. GET passes parameters through the query string, while POST submits parameters through form data.
 * - If endpointId is missing or empty, an InvalidParameter error is returned.
 * - If the requested endpoint does not exist, does not belong to the specified instance, or does not belong to the current user, a resource-not-found error is returned.
 *
 * @param request GetServiceEndpointRequest
 * @return GetServiceEndpointResponse
 */
async function getServiceEndpoint(request: GetServiceEndpointRequest): GetServiceEndpointResponse {
  var runtime = new $RuntimeOptions{};
  return getServiceEndpointWithOptions(request, runtime);
}

model GetTaskStatsSummaryRequest {
  endTime?: string(name='EndTime', description='The query end time. Unit: milliseconds (UNIX timestamp).', example='1718086400000'),
  instanceId?: string(name='InstanceId', description='The instance ID.', example='i-xxx'),
  startTime?: string(name='StartTime', description='The query start time. Unit: milliseconds (UNIX timestamp).', example='1718000000000'),
}

model GetTaskStatsSummaryResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  data?: {
    averageTaskDuration?: double(name='AverageTaskDuration', description='The average task duration. Unit: milliseconds.', example='5000.5'),
    statusDistribution?: [ 
      {
        count?: int32(name='Count', description='The number of tasks in this status.', example='800'),
        status?: string(name='Status', description='The task status.', example='SUCCESS'),
      }
    ](name='StatusDistribution', description='The task status distribution.', example='[...]'),
    taskTokenConsumption?: long(name='TaskTokenConsumption', description='The total token consumption of tasks.', example='500000'),
    totalTasks?: int32(name='TotalTasks', description='The total number of tasks.', example='1000'),
  }(name='Data', description='The returned data.', example='{}'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model GetTaskStatsSummaryResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetTaskStatsSummaryResponseBody(name='body'),
}

/**
 * @summary Queries the task statistics summary of a specified AgentTeams instance within a specified time range, including total tasks, average task duration, token consumption, and status distribution.
 *
 * @description Queries the task statistics summary of a specified AgentTeams instance within a specified time range, including total tasks, average task duration, token consumption, and status distribution.
 *
 * @param request GetTaskStatsSummaryRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetTaskStatsSummaryResponse
 */
@context("getTaskStatsSummaryWithContext")
async function getTaskStatsSummaryWithOptions(request: GetTaskStatsSummaryRequest, runtime: $RuntimeOptions): GetTaskStatsSummaryResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.endTime)) {
    query['EndTime'] = request.endTime;
  }
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.startTime)) {
    query['StartTime'] = request.startTime;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetTaskStatsSummary',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the task statistics summary of a specified AgentTeams instance within a specified time range, including total tasks, average task duration, token consumption, and status distribution.
 *
 * @description Queries the task statistics summary of a specified AgentTeams instance within a specified time range, including total tasks, average task duration, token consumption, and status distribution.
 *
 * @param request GetTaskStatsSummaryRequest
 * @return GetTaskStatsSummaryResponse
 */
async function getTaskStatsSummary(request: GetTaskStatsSummaryRequest): GetTaskStatsSummaryResponse {
  var runtime = new $RuntimeOptions{};
  return getTaskStatsSummaryWithOptions(request, runtime);
}

model GetTeamRequest {
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='agentteams-test-001'),
  name?: string(name='Name', description='The name.

This parameter is required.', example='worker-demo'),
}

model GetTeamResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  data?: {
    adminName?: string(name='AdminName', description='The name of the team administrator.', example='admin-user'),
    createdAt?: string(name='CreatedAt', description='The time when the team was created.', example='2026-07-01T12:00:00'),
    description?: string(name='Description', description='The team description.', example='This is a team.'),
    instanceId?: string(name='InstanceId', description='The instance ID.', example='agentteams-test-001'),
    leaderName?: string(name='LeaderName', description='The name of the team leader.', example='leader-user'),
    name?: string(name='Name', description='The team name.', example='my-team'),
    rooms?: [ 
      {
        roomId?: string(name='RoomId', description='The room ID.', example='room-xxx'),
        type?: string(name='Type', description='The room type.', example='group'),
      }
    ](name='Rooms', description='The list of rooms associated with the team.', example='[...]'),
    status?: string(name='Status', description='The running status of the team. This field is empty when the team is running normally.', example='Deleting'),
    teamMembers?: [ 
      {
        name?: string(name='Name', description='The member name.', example='member-user'),
      }
    ](name='TeamMembers', description='The list of team members.', example='[...]'),
    updatedAt?: string(name='UpdatedAt', description='The time when the team was last updated.', example='2026-07-10T12:00:00'),
    workerNames?: [ string ](name='WorkerNames', description='The list of worker names associated with the team.', example='["worker-1"]'),
  }(name='Data', description='The returned data.', example='{}'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-get-team-001'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model GetTeamResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetTeamResponseBody(name='body'),
}

/**
 * @summary Queries the details of a specified team under a specified instance, including the description, administrator, leader, members, associated workers, and room status.
 *
 * @description Queries the details of a specified team under a specified instance, including the description, administrator, leader, members, associated workers, and room status.
 *
 * @param request GetTeamRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetTeamResponse
 */
@context("getTeamWithContext")
async function getTeamWithOptions(request: GetTeamRequest, runtime: $RuntimeOptions): GetTeamResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.name)) {
    query['Name'] = request.name;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetTeam',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the details of a specified team under a specified instance, including the description, administrator, leader, members, associated workers, and room status.
 *
 * @description Queries the details of a specified team under a specified instance, including the description, administrator, leader, members, associated workers, and room status.
 *
 * @param request GetTeamRequest
 * @return GetTeamResponse
 */
async function getTeam(request: GetTeamRequest): GetTeamResponse {
  var runtime = new $RuntimeOptions{};
  return getTeamWithOptions(request, runtime);
}

model GetTokenTrendRequest {
  endTime?: string(name='EndTime', description='The query end time. Unit: milliseconds (UNIX timestamp).', example='1718086400000'),
  groupBy?: string(name='GroupBy', description='The grouping dimension, such as by day or by hour.', example='day'),
  instanceId?: string(name='InstanceId', description='The instance ID.', example='i-xxx'),
  startTime?: string(name='StartTime', description='The query start time. Unit: milliseconds (UNIX timestamp).', example='1718000000000'),
}

model GetTokenTrendResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  data?: {
    groupBy?: string(name='GroupBy', description='The grouping dimension.', example='day'),
    series?: [ 
      {
        data?: [ any ](name='Data', description='The returned data.', example='{}'),
        name?: string(name='Name', description='The series name.', example='input_tokens'),
      }
    ](name='Series', description='The trend data series.', example='[...]'),
  }(name='Data', description='The returned data.', example='{}'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model GetTokenTrendResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetTokenTrendResponseBody(name='body'),
}

/**
 * @summary Queries the token consumption trend of a specified AgentTeams instance within a specified time range, supports grouping by time dimension, and returns time series data that can be used for charting.
 *
 * @description Queries the token consumption trend of a specified AgentTeams instance within a specified time range, supports grouping by time dimension, and returns time series data that can be used for charting.
 *
 * @param request GetTokenTrendRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetTokenTrendResponse
 */
@context("getTokenTrendWithContext")
async function getTokenTrendWithOptions(request: GetTokenTrendRequest, runtime: $RuntimeOptions): GetTokenTrendResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.endTime)) {
    query['EndTime'] = request.endTime;
  }
  if (!$isNull(request.groupBy)) {
    query['GroupBy'] = request.groupBy;
  }
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.startTime)) {
    query['StartTime'] = request.startTime;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetTokenTrend',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the token consumption trend of a specified AgentTeams instance within a specified time range, supports grouping by time dimension, and returns time series data that can be used for charting.
 *
 * @description Queries the token consumption trend of a specified AgentTeams instance within a specified time range, supports grouping by time dimension, and returns time series data that can be used for charting.
 *
 * @param request GetTokenTrendRequest
 * @return GetTokenTrendResponse
 */
async function getTokenTrend(request: GetTokenTrendRequest): GetTokenTrendResponse {
  var runtime = new $RuntimeOptions{};
  return getTokenTrendWithOptions(request, runtime);
}

model GetToolCallDistributionRequest {
  endTime?: string(name='EndTime', description='The end time of the query. This value is a UNIX timestamp in milliseconds.', example='1718086400000'),
  instanceId?: string(name='InstanceId', description='The instance ID.', example='i-xxx'),
  startTime?: string(name='StartTime', description='The start time of the query. This value is a UNIX timestamp in milliseconds.', example='1718000000000'),
}

model GetToolCallDistributionResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  data?: {
    items?: [ 
      {
        callCount?: int32(name='CallCount', description='The number of calls for the tool.', example='300'),
        toolName?: string(name='ToolName', description='The tool name.', example='web_search'),
      }
    ](name='Items', description='The tool calling distribution list.', example='[...]'),
    totalCalls?: int32(name='TotalCalls', description='The total number of tool invocations.', example='500'),
  }(name='Data', description='The returned data.', example='{}'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model GetToolCallDistributionResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetToolCallDistributionResponseBody(name='body'),
}

/**
 * @summary Queries the tool calling distribution of a specified AgentTeams instance within a specified time range, and returns the number of calls for each tool and the total number of calls.
 *
 * @description Queries the tool calling distribution of a specified AgentTeams instance within a specified time range, and returns the number of calls for each tool and the total number of calls.
 *
 * @param request GetToolCallDistributionRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetToolCallDistributionResponse
 */
@context("getToolCallDistributionWithContext")
async function getToolCallDistributionWithOptions(request: GetToolCallDistributionRequest, runtime: $RuntimeOptions): GetToolCallDistributionResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.endTime)) {
    query['EndTime'] = request.endTime;
  }
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.startTime)) {
    query['StartTime'] = request.startTime;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetToolCallDistribution',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the tool calling distribution of a specified AgentTeams instance within a specified time range, and returns the number of calls for each tool and the total number of calls.
 *
 * @description Queries the tool calling distribution of a specified AgentTeams instance within a specified time range, and returns the number of calls for each tool and the total number of calls.
 *
 * @param request GetToolCallDistributionRequest
 * @return GetToolCallDistributionResponse
 */
async function getToolCallDistribution(request: GetToolCallDistributionRequest): GetToolCallDistributionResponse {
  var runtime = new $RuntimeOptions{};
  return getToolCallDistributionWithOptions(request, runtime);
}

model GetUserRequest {
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='agentteams-test-001'),
  name?: string(name='Name', description='The name.

This parameter is required.', example='worker-demo'),
}

model GetUserResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  data?: {
    authMethod?: string(name='AuthMethod', description='The authentication method.', example='password'),
    createTime?: string(name='CreateTime', description='The resource creation time.', example='2026-06-05T10:00:00Z'),
    createdAt?: string(name='CreatedAt', description='The creation time.', example='2026-06-05T10:00:00'),
    displayName?: string(name='DisplayName', description='The display name.', example='Demo User'),
    email?: string(name='Email', description='The email address.', example='demo@example.com'),
    instanceId?: string(name='InstanceId', description='The instance ID.', example='agentteams-test-001'),
    name?: string(name='Name', description='The username.', example='demo-user'),
    note?: string(name='Note', description='The remarks.', example='Test user.'),
    regionId?: string(name='RegionId', description='The region ID.', example='cn-hangzhou'),
    resourceGroupId?: string(name='ResourceGroupId', description='The resource group ID.', example='rg-xxx'),
    status?: string(name='Status', description='The user status.', example='Active'),
  }(name='Data', description='The returned data.', example='{}'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-get-user-001'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model GetUserResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetUserResponseBody(name='body'),
}

/**
 * @summary Queries the details of a specified user under a specified instance, including the username, display name, email address, authentication method, status, and creation time.
 *
 * @description Queries the details of a specified user under a specified instance, including the username, display name, email address, authentication method, status, and creation time.
 *
 * @param request GetUserRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetUserResponse
 */
@context("getUserWithContext")
async function getUserWithOptions(request: GetUserRequest, runtime: $RuntimeOptions): GetUserResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.name)) {
    query['Name'] = request.name;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetUser',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the details of a specified user under a specified instance, including the username, display name, email address, authentication method, status, and creation time.
 *
 * @description Queries the details of a specified user under a specified instance, including the username, display name, email address, authentication method, status, and creation time.
 *
 * @param request GetUserRequest
 * @return GetUserResponse
 */
async function getUser(request: GetUserRequest): GetUserResponse {
  var runtime = new $RuntimeOptions{};
  return getUserWithOptions(request, runtime);
}

model GetUserPasswordRequest {
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='agentteams-test-001'),
  name?: string(name='Name', description='The name.

This parameter is required.', example='worker-demo'),
}

model GetUserPasswordResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  data?: {
    initialPassword?: string(name='InitialPassword', description='The initial password.', example='P@ssw0rd123'),
    instanceId?: string(name='InstanceId', description='The instance ID.', example='agentteams-test-001'),
    name?: string(name='Name', description='The username.', example='demo-user'),
  }(name='Data', description='The returned data.', example='{}'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-get-password-001'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model GetUserPasswordResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetUserPasswordResponseBody(name='body'),
}

/**
 * @summary Retrieves the initial password of a specified user under a specified instance. The initial password is generated by the system or specified by the user when the user is created.
 *
 * @description Retrieves the initial password of a specified user under a specified instance. The initial password is generated by the system or specified by the user when the user is created.
 *
 * @param request GetUserPasswordRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetUserPasswordResponse
 */
@context("getUserPasswordWithContext")
async function getUserPasswordWithOptions(request: GetUserPasswordRequest, runtime: $RuntimeOptions): GetUserPasswordResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.name)) {
    query['Name'] = request.name;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetUserPassword',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves the initial password of a specified user under a specified instance. The initial password is generated by the system or specified by the user when the user is created.
 *
 * @description Retrieves the initial password of a specified user under a specified instance. The initial password is generated by the system or specified by the user when the user is created.
 *
 * @param request GetUserPasswordRequest
 * @return GetUserPasswordResponse
 */
async function getUserPassword(request: GetUserPasswordRequest): GetUserPasswordResponse {
  var runtime = new $RuntimeOptions{};
  return getUserPasswordWithOptions(request, runtime);
}

model GetWorkerRequest {
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='inst-demo'),
  name?: string(name='Name', description='The name.

This parameter is required.', example='worker-demo'),
}

model GetWorkerResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  data?: {
    agentType?: string(name='AgentType', description='The Agent runtime type.', example='Qwenpaw'),
    agents?: string(name='Agents', description='The Agents configuration.', example='agents-demo'),
    channels?: [ 
      {
        config?: {
          cardTemplateId?: string(name='CardTemplateId', description='The card template ID.', example='tpl-demo'),
          clientId?: string(name='ClientId', description='The client ID.', example='cli-demo'),
          extension?: string(name='Extension', description='The extension configuration.', example='extension-demo'),
          messageType?: string(name='MessageType', description='The message type.', example='markdown'),
          robotCode?: string(name='RobotCode', description='The robot code.', example='robot-demo'),
          showThinking?: boolean(name='ShowThinking', description='Indicates whether the thinking process is displayed.', example='true'),
          showToolCalls?: boolean(name='ShowToolCalls', description='Indicates whether tool invocations are displayed.', example='false'),
          streamingEnabled?: boolean(name='StreamingEnabled', description='Indicates whether streaming output is enabled.', example='true'),
        }(name='Config', description='The channel configuration.', example='{}'),
        enabled?: boolean(name='Enabled', description='Indicates whether the channel is enabled.', example='true'),
        secretStatus?: {
          clientSecret?: boolean(name='ClientSecret', description='Indicates whether the client secret has been set.', example='true'),
        }(name='SecretStatus', description='The secret status.', example='{}'),
        type?: string(name='Type', description='The channel type.', example='dingtalk'),
      }
    ](name='Channels', description='The channel list.', example='[...]'),
    credentials?: [ 
      {
        name?: string(name='Name', description='The credential name.', example='cred-demo'),
      }
    ](name='Credentials', description='The credential binding list.', example='[...]'),
    deployType?: string(name='DeployType', description='The deployment type.', example='Managed'),
    groups?: [ 
      {
        name?: string(name='Name', description='The group name.', example='team-demo'),
        role?: string(name='Role', description='The group role.', example='member'),
        type?: string(name='Type', description='The group type.', example='team'),
      }
    ](name='Groups', description='The team group list.', example='[...]'),
    instanceId?: string(name='InstanceId', description='The instance ID.', example='inst-demo'),
    limitConfig?: {
      limitType?: string(name='LimitType', description='The quota type.', example='token'),
      overLimit?: boolean(name='OverLimit', description='Indicates whether the quota is exceeded.', example='false'),
      periodType?: string(name='PeriodType', description='The period type.', example='day'),
      ruleStatus?: string(name='RuleStatus', description='The quota rule status.', example='active'),
      usageLimit?: long(name='UsageLimit', description='The quota upper limit.', example='1000000'),
      usedAmount?: long(name='UsedAmount', description='The used amount.', example='50000'),
    }(name='LimitConfig', description='The quota configuration.', example='{}'),
    mcpServers?: [ 
      {
        name?: string(name='Name', description='The MCP server name.', example='mcp-server-demo'),
        transport?: string(name='Transport', description='The MCP transport protocol.', example='sse'),
        url?: string(name='Url', description='The MCP server URL.', example='https://mcp-demo/mcp'),
      }
    ](name='McpServers', description='The MCP server list.', example='[...]'),
    model?: {
      modelName?: string(name='ModelName', description='The model name.', example='qwen-max'),
      modelProvider?: string(name='ModelProvider', description='The model provider.', example='dashscope'),
    }(name='Model', description='The model configuration.', example='{}'),
    name?: string(name='Name', description='The Worker name.', example='worker-demo'),
    regionId?: string(name='RegionId', description='The region ID.', example='cn-hangzhou'),
    skills?: [ 
      {
        label?: string(name='Label', description='The skill label.', example='Code Review.'),
        name?: string(name='Name', description='The skill name.', example='code-review'),
        version?: string(name='Version', description='The skill version.', example='1.0.0'),
      }
    ](name='Skills', description='The skill list.', example='[...]'),
    soul?: string(name='Soul', description='The Soul configuration.', example='soul-demo'),
    startTime?: string(name='StartTime', description='The start time.', example='2026-07-13T10:00:00Z'),
    status?: string(name='Status', description='The Worker status.', example='Running'),
    subagents?: [ 
      {
        description?: string(name='Description', description='The sub-Agent description.', example='Sub-Agent description.'),
        hasAgentsMd?: boolean(name='HasAgentsMd', description='Indicates whether the sub-Agent contains an agents.md file.', example='true'),
        name?: string(name='Name', description='The sub-Agent name.', example='sub-worker-demo'),
        skills?: [ 
          {
            hasSkillMd?: boolean(name='HasSkillMd', description='Indicates whether the skill contains a skill.md file.', example='true'),
            name?: string(name='Name', description='The skill name.', example='search'),
            skillId?: string(name='SkillId', description='The skill ID.', example='skill-demo'),
          }
        ](name='Skills', description='The skill list of the sub-Agent.', example='[...]'),
        sourcePath?: string(name='SourcePath', description='The source path.', example='/workspace/sub'),
        subagentId?: string(name='SubagentId', description='The sub-Agent ID.', example='sub-demo'),
      }
    ](name='Subagents', description='The sub-Agent list.', example='[...]'),
    template?: {
      label?: string(name='Label', description='The template label.', example='Default template.'),
      name?: string(name='Name', description='The template name.', example='default-template'),
      version?: string(name='Version', description='The template version.', example='1.0.0'),
    }(name='Template', description='The template configuration.', example='{}'),
    versionCode?: string(name='VersionCode', description='The Worker version number.', example='v1.0.0'),
  }(name='Data', description='The response data.', example='{}'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='473469C7-XX-XX-XX'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model GetWorkerResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetWorkerResponseBody(name='body'),
}

/**
 * @summary Queries the details of a specified worker, including configurations such as model, skills, sub-agents, MCP servers, channels, and quotas.
 *
 * @description Queries the details of a specified worker, including configurations such as model, skills, sub-agents, MCP servers, channels, and quotas.
 *
 * @param request GetWorkerRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetWorkerResponse
 */
@context("getWorkerWithContext")
async function getWorkerWithOptions(request: GetWorkerRequest, runtime: $RuntimeOptions): GetWorkerResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.name)) {
    query['Name'] = request.name;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetWorker',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the details of a specified worker, including configurations such as model, skills, sub-agents, MCP servers, channels, and quotas.
 *
 * @description Queries the details of a specified worker, including configurations such as model, skills, sub-agents, MCP servers, channels, and quotas.
 *
 * @param request GetWorkerRequest
 * @return GetWorkerResponse
 */
async function getWorker(request: GetWorkerRequest): GetWorkerResponse {
  var runtime = new $RuntimeOptions{};
  return getWorkerWithOptions(request, runtime);
}

model GetWorkerBootstrapOptionsRequest {
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='inst-demo'),
  name?: string(name='Name', description='The name.

This parameter is required.', example='worker-demo'),
}

model GetWorkerBootstrapOptionsResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  data?: {
    instanceId?: string(name='InstanceId', description='The instance ID.', example='inst-demo'),
    name?: string(name='Name', description='The Worker name.', example='worker-demo'),
    networkOptions?: [ 
      {
        available?: boolean(name='Available', description='Indicates whether the network type is available.', example='true'),
        networkType?: string(name='NetworkType', description='The network type.', example='INTRANET'),
      }
    ](name='NetworkOptions', description='The list of network options.', example='{}'),
  }(name='Data', description='The returned data.', example='{}'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='473469C7-XX-XX-XX'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model GetWorkerBootstrapOptionsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetWorkerBootstrapOptionsResponseBody(name='body'),
}

/**
 * @summary Queries the startup options for local Worker management and returns available network types.
 *
 * @description Queries the startup options for local Worker management and returns available network types.
 *
 * @param request GetWorkerBootstrapOptionsRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetWorkerBootstrapOptionsResponse
 */
@context("getWorkerBootstrapOptionsWithContext")
async function getWorkerBootstrapOptionsWithOptions(request: GetWorkerBootstrapOptionsRequest, runtime: $RuntimeOptions): GetWorkerBootstrapOptionsResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.name)) {
    query['Name'] = request.name;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetWorkerBootstrapOptions',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the startup options for local Worker management and returns available network types.
 *
 * @description Queries the startup options for local Worker management and returns available network types.
 *
 * @param request GetWorkerBootstrapOptionsRequest
 * @return GetWorkerBootstrapOptionsResponse
 */
async function getWorkerBootstrapOptions(request: GetWorkerBootstrapOptionsRequest): GetWorkerBootstrapOptionsResponse {
  var runtime = new $RuntimeOptions{};
  return getWorkerBootstrapOptionsWithOptions(request, runtime);
}

model GetWorkerMaxVersionRequest {
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='inst-demo'),
}

model GetWorkerMaxVersionResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  data?: {
    instanceId?: string(name='InstanceId', description='The instance ID.', example='inst-demo'),
    versionCode?: string(name='VersionCode', description='The maximum upgradable version number of the worker.', example='v2.0.0'),
  }(name='Data', description='The returned data.', example='{}'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='473469C7-XX-XX-XX'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model GetWorkerMaxVersionResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetWorkerMaxVersionResponseBody(name='body'),
}

/**
 * @summary Queries the maximum upgradable version of a worker.
 *
 * @description Queries the maximum upgradable version of a worker.
 *
 * @param request GetWorkerMaxVersionRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetWorkerMaxVersionResponse
 */
@context("getWorkerMaxVersionWithContext")
async function getWorkerMaxVersionWithOptions(request: GetWorkerMaxVersionRequest, runtime: $RuntimeOptions): GetWorkerMaxVersionResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetWorkerMaxVersion',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the maximum upgradable version of a worker.
 *
 * @description Queries the maximum upgradable version of a worker.
 *
 * @param request GetWorkerMaxVersionRequest
 * @return GetWorkerMaxVersionResponse
 */
async function getWorkerMaxVersion(request: GetWorkerMaxVersionRequest): GetWorkerMaxVersionResponse {
  var runtime = new $RuntimeOptions{};
  return getWorkerMaxVersionWithOptions(request, runtime);
}

model GetWorkerStatsSummaryRequest {
  endTime?: string(name='EndTime', description='The end time for statistics collection. This value is a UNIX timestamp in milliseconds.', example='1720054400000'),
  instanceId?: string(name='InstanceId', description='The instance ID.', example='inst-demo'),
  startTime?: string(name='StartTime', description='The start time for statistics collection. This value is a UNIX timestamp in milliseconds.', example='1719504000000'),
}

model GetWorkerStatsSummaryResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  data?: {
    otherWorkers?: int32(name='OtherWorkers', description='The number of workers in other states.', example='2'),
    runningWorkers?: int32(name='RunningWorkers', description='The number of running workers.', example='20'),
    stoppedWorkers?: int32(name='StoppedWorkers', description='The number of stopped workers.', example='3'),
    totalWorkers?: int32(name='TotalWorkers', description='The total number of workers.', example='25'),
  }(name='Data', description='The returned data.', example='{}'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='473469C7-XX-XX-XX'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model GetWorkerStatsSummaryResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetWorkerStatsSummaryResponseBody(name='body'),
}

/**
 * @summary Queries the Worker statistics summary, including the total number of workers, the number of running workers, the number of stopped workers, and more.
 *
 * @description Queries the Worker statistics summary, including the total number of workers, the number of running workers, the number of stopped workers, and more.
 *
 * @param request GetWorkerStatsSummaryRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetWorkerStatsSummaryResponse
 */
@context("getWorkerStatsSummaryWithContext")
async function getWorkerStatsSummaryWithOptions(request: GetWorkerStatsSummaryRequest, runtime: $RuntimeOptions): GetWorkerStatsSummaryResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.endTime)) {
    query['EndTime'] = request.endTime;
  }
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.startTime)) {
    query['StartTime'] = request.startTime;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetWorkerStatsSummary',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the Worker statistics summary, including the total number of workers, the number of running workers, the number of stopped workers, and more.
 *
 * @description Queries the Worker statistics summary, including the total number of workers, the number of running workers, the number of stopped workers, and more.
 *
 * @param request GetWorkerStatsSummaryRequest
 * @return GetWorkerStatsSummaryResponse
 */
async function getWorkerStatsSummary(request: GetWorkerStatsSummaryRequest): GetWorkerStatsSummaryResponse {
  var runtime = new $RuntimeOptions{};
  return getWorkerStatsSummaryWithOptions(request, runtime);
}

model ListCredentialsRequest {
  instanceId?: string(name='InstanceId', description='The AgentTeams instance ID.

This parameter is required.', example='ami-2ze8x9c6f'),
  maxResults?: int32(name='MaxResults', description='The maximum number of entries per page. Valid values: 1 to 100. Default value: 10.', example='10'),
  nameLike?: string(name='NameLike', description='The keyword for fuzzy match of the credential name.', example='OPENAI'),
  nextToken?: string(name='NextToken', description='The pagination token. Leave this parameter empty for the first request. For subsequent requests, use the NextToken value returned in the previous response.', example='0'),
}

model ListCredentialsResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  items?: [ 
    {
      boundWorkerCount?: int32(name='BoundWorkerCount', description='The number of bound Workers.', example='1'),
      createTime?: string(name='CreateTime', description='The creation time in ISO 8601 format.', example='2026-07-13T12:00:00Z'),
      description?: string(name='Description', description='The credential description.', example='OpenAI API key for the team'),
      instanceId?: string(name='InstanceId', description='The instance ID.', example='ami-2ze8x9c6f'),
      name?: string(name='Name', description='The credential key.', example='OPENAI_API_KEY'),
      updateTime?: string(name='UpdateTime', description='The update time in ISO 8601 format.', example='2026-07-13T12:00:00Z'),
    }
  ](name='Items', description='The list of credential summaries.', example='[]'),
  maxResults?: int32(name='MaxResults', description='The maximum number of entries per page.', example='10'),
  message?: string(name='Message', description='The response message.', example='success'),
  nextToken?: string(name='NextToken', description='The pagination token for the next page. An empty value indicates that the last page has been reached.', example='10'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
  totalCount?: long(name='TotalCount', description='The total number of records that match the query conditions.', example='2'),
}

model ListCredentialsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListCredentialsResponseBody(name='body'),
}

/**
 * @summary Queries the credential list under a specified AgentTeams instance with paging, returning credential summary information and the number of Workers attached to each credential.
 *
 * @description Queries the credential list under a specified AgentTeams instance with paging, returning credential summary information and the number of Workers attached to each credential.
 *
 * @param request ListCredentialsRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListCredentialsResponse
 */
@context("listCredentialsWithContext")
async function listCredentialsWithOptions(request: ListCredentialsRequest, runtime: $RuntimeOptions): ListCredentialsResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.maxResults)) {
    query['MaxResults'] = request.maxResults;
  }
  if (!$isNull(request.nameLike)) {
    query['NameLike'] = request.nameLike;
  }
  if (!$isNull(request.nextToken)) {
    query['NextToken'] = request.nextToken;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListCredentials',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the credential list under a specified AgentTeams instance with paging, returning credential summary information and the number of Workers attached to each credential.
 *
 * @description Queries the credential list under a specified AgentTeams instance with paging, returning credential summary information and the number of Workers attached to each credential.
 *
 * @param request ListCredentialsRequest
 * @return ListCredentialsResponse
 */
async function listCredentials(request: ListCredentialsRequest): ListCredentialsResponse {
  var runtime = new $RuntimeOptions{};
  return listCredentialsWithOptions(request, runtime);
}

model ListIdentityProvidersRequest {
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='agentteams-xxx'),
  maxResults?: int32(name='MaxResults', description='The maximum number of entries to return in this query. Valid values: 1 to 100. Default value: 10.', example='10'),
  nextToken?: string(name='NextToken', description='The pagination token that indicates the offset from which to start the query. The value must be a non-negative integer. Do not specify this parameter for the first query.', example='10'),
}

model ListIdentityProvidersResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  items?: [ 
    {
      identityProviderType?: string(name='IdentityProviderType', description='The identity provider type.', example='DingTalk'),
      instanceId?: string(name='InstanceId', description='The instance ID.', example='agentteams-demo'),
      loginEnabled?: boolean(name='LoginEnabled', description='Indicates whether logon is enabled.', example='true'),
      status?: string(name='Status', description='The status.', example='Running'),
      syncEnabled?: boolean(name='SyncEnabled', description='Indicates whether synchronization is enabled.', example='false'),
    }
  ](name='Items', description='The list of identity provider bindings.', example='[...]'),
  maxResults?: int32(name='MaxResults', description='The maximum number of entries returned in this query.', example='10'),
  message?: string(name='Message', description='The response message.', example='success'),
  nextToken?: string(name='NextToken', description='The pagination token for the next page. This parameter is empty if no more data exists.', example='10'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
  totalCount?: long(name='TotalCount', description='The total number of records.', example='1'),
}

model ListIdentityProvidersResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListIdentityProvidersResponseBody(name='body'),
}

/**
 * @summary Queries the list of upstream identity provider bindings for a specified instance, with support for paginated responses.
 *
 * @description Queries the list of upstream identity provider bindings for a specified instance, with support for paginated responses.
 *
 * @param request ListIdentityProvidersRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListIdentityProvidersResponse
 */
@context("listIdentityProvidersWithContext")
async function listIdentityProvidersWithOptions(request: ListIdentityProvidersRequest, runtime: $RuntimeOptions): ListIdentityProvidersResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.maxResults)) {
    query['MaxResults'] = request.maxResults;
  }
  if (!$isNull(request.nextToken)) {
    query['NextToken'] = request.nextToken;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListIdentityProviders',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the list of upstream identity provider bindings for a specified instance, with support for paginated responses.
 *
 * @description Queries the list of upstream identity provider bindings for a specified instance, with support for paginated responses.
 *
 * @param request ListIdentityProvidersRequest
 * @return ListIdentityProvidersResponse
 */
async function listIdentityProviders(request: ListIdentityProvidersRequest): ListIdentityProvidersResponse {
  var runtime = new $RuntimeOptions{};
  return listIdentityProvidersWithOptions(request, runtime);
}

model ListInstancesRequest {
  instanceName?: string(name='InstanceName', description='* **Description**: The instance name. Supports fuzzy match.', example='AgentTeams'),
  maxResults?: int32(name='MaxResults', description='* **Description**: The page size.
* **Default value**: 20
* **Current implementation limit**: 1 to 100', example='10'),
  nextToken?: string(name='NextToken', description='* **Description**: The pagination token for the next page.', example='20'),
  skip?: int32(name='Skip', description='* **Description**: The number of records to skip.
* **Default value**: 0', example='0'),
  status?: string(name='Status', description='* **Description**: The instance status.', example='RUNNING'),
}

model ListInstancesResponseBody = {
  code?: string(name='Code', description='**Business code**', example='SUCCESS'),
  items?: [ 
    {
      instanceId?: string(name='InstanceId', description='The instance ID.', example='instance-1'),
      instanceName?: string(name='InstanceName', description='The instance name.', example='AgentTeams-demo'),
      instanceSpec?: string(name='InstanceSpec', description='The instance specifications.', example='small.x1'),
      ossBucketName?: string(name='OssBucketName', description='The OSS bucket name.', example='agentteams-bucket'),
      regionId?: string(name='RegionId', description='The region ID.', example='cn-hangzhou'),
      securityGroup?: string(name='SecurityGroup', description='The security group ID.', example='sg-bp1xxxx'),
      status?: string(name='Status', description='The instance status. Valid values:
 * RUNNING: running
 * STOPPED: stopped
 * PENDING: being created
 * ERROR: error', example='RUNNING'),
      vpcId?: string(name='VpcId', description='VPC ID', example='vpc-bp1xxxx'),
      zones?: [ 
        {
          vswitchId?: string(name='VswitchId', description='The vSwitch ID.', example='vsw-bp1xxxx'),
          zoneId?: string(name='ZoneId', description='The zone ID.', example='cn-hangzhou-h'),
        }
      ](name='Zones', description='The list of vSwitch IDs.', example='[...]'),
    }
  ](name='Items', description='The list of data on the current page.', example='[...]'),
  maxResults?: int32(name='MaxResults', description='**The page size used in this response**', example='10'),
  message?: string(name='Message', description='**Response message**', example='success'),
  nextToken?: string(name='NextToken', description='The pagination token for the next page. This value is typically `null` when no more pages are available.', example='10'),
  requestId?: string(name='RequestId', description='**Request ID**', example='request-1'),
  success?: boolean(name='Success', description='**Indicates whether the request is successful**', example='true'),
  totalCount?: int32(name='TotalCount', description='The total number of records.', example='23'),
}

model ListInstancesResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListInstancesResponseBody(name='body'),
}

/**
 * @summary Queries a list of instances that meet the specified conditions. Supports pagination and fuzzy match. Supports GET and POST methods. The list is returned in reverse chronological order by creation time.
 *
 * @description Queries a list of instances that meet the specified conditions. Supports pagination and fuzzy match. Supports GET and POST methods. The list is returned in reverse chronological order by creation time.
 *
 * @param request ListInstancesRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListInstancesResponse
 */
@context("listInstancesWithContext")
async function listInstancesWithOptions(request: ListInstancesRequest, runtime: $RuntimeOptions): ListInstancesResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.instanceName)) {
    query['InstanceName'] = request.instanceName;
  }
  if (!$isNull(request.maxResults)) {
    query['MaxResults'] = request.maxResults;
  }
  if (!$isNull(request.nextToken)) {
    query['NextToken'] = request.nextToken;
  }
  if (!$isNull(request.skip)) {
    query['Skip'] = request.skip;
  }
  if (!$isNull(request.status)) {
    query['Status'] = request.status;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListInstances',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries a list of instances that meet the specified conditions. Supports pagination and fuzzy match. Supports GET and POST methods. The list is returned in reverse chronological order by creation time.
 *
 * @description Queries a list of instances that meet the specified conditions. Supports pagination and fuzzy match. Supports GET and POST methods. The list is returned in reverse chronological order by creation time.
 *
 * @param request ListInstancesRequest
 * @return ListInstancesResponse
 */
async function listInstances(request: ListInstancesRequest): ListInstancesResponse {
  var runtime = new $RuntimeOptions{};
  return listInstancesWithOptions(request, runtime);
}

model ListMcpToolsRequest {
  id?: string(name='Id', description='Optional. The exact match filter for instance status.

This parameter is required.', example='RUNNING'),
  instanceId?: string(name='InstanceId', description='The AgentTeams instance ID.

This parameter is required.', example='ins-xxx'),
}

model ListMcpToolsResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  items?: [ 
    {
      description?: string(name='Description', description='The tool description.', example='Perform a search operation.'),
      inputSchema?: string(name='InputSchema', description='The input parameter schema of the tool.', example='{"type":"object","properties":{"query":{"type":"string"}}}'),
      name?: string(name='Name', description='The tool name.', example='search'),
      title?: string(name='Title', description='The tool title.', example='Search tool.'),
    }
  ](name='Items', description='The list of tools.', example='[...]'),
  maxResults?: int32(name='MaxResults', description='The maximum number of records per page.', example='20'),
  message?: string(name='Message', description='The response message.', example='success'),
  nextToken?: string(name='NextToken', description='The token for the next page.', example='next-token-xxx'),
  requestId?: string(name='RequestId', description='Id of the request', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
  totalCount?: int32(name='TotalCount', description='The total number of records.', example='2'),
}

model ListMcpToolsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListMcpToolsResponseBody(name='body'),
}

/**
 * @summary Queries the list of tools provided by a specified MCP server, including tool names, titles, descriptions, and input schemas.
 *
 * @description Queries the list of tools provided by a specified MCP server, including tool names, titles, descriptions, and input schemas.
 *
 * @param request ListMcpToolsRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListMcpToolsResponse
 */
@context("listMcpToolsWithContext")
async function listMcpToolsWithOptions(request: ListMcpToolsRequest, runtime: $RuntimeOptions): ListMcpToolsResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.id)) {
    body['Id'] = request.id;
  }
  if (!$isNull(request.instanceId)) {
    body['InstanceId'] = request.instanceId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListMcpTools',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the list of tools provided by a specified MCP server, including tool names, titles, descriptions, and input schemas.
 *
 * @description Queries the list of tools provided by a specified MCP server, including tool names, titles, descriptions, and input schemas.
 *
 * @param request ListMcpToolsRequest
 * @return ListMcpToolsResponse
 */
async function listMcpTools(request: ListMcpToolsRequest): ListMcpToolsResponse {
  var runtime = new $RuntimeOptions{};
  return listMcpToolsWithOptions(request, runtime);
}

model ListMcpsRequest {
  instanceId?: string(name='InstanceId', description='Optional. Fuzzy matches by instance name.

This parameter is required.', example='AgentTeams'),
  maxResults?: int32(name='MaxResults', description='The maximum number of records per page.', example='20'),
  nextToken?: string(name='NextToken', description='The token for the next page, used for paginated queries.', example='next-token-xxx'),
}

model ListMcpsResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  items?: [ 
    {
      addresses?: [ string ](name='Addresses', description='The list of MCP server addresses.', example='["https://example.com/mcp"]'),
      createType?: string(name='CreateType', description='The creation type.', example='DIRECT_PROXY'),
      deployStatus?: string(name='DeployStatus', description='The deployment status.', example='DEPLOYED'),
      description?: string(name='Description', description='The MCP server description.', example='This is an MCP server.'),
      id?: string(name='Id', description='The MCP server ID.', example='mcp-xxx'),
      instanceId?: string(name='InstanceId', description='The AgentTeams instance ID.', example='ins-xxx'),
      mcpServerConfig?: string(name='McpServerConfig', description='The MCP server configuration.', example='{}'),
      name?: string(name='Name', description='The MCP server name.', example='my-mcp-server'),
      protocol?: string(name='Protocol', description='The protocol type.', example='streamable'),
      url?: string(name='Url', description='The MCP server access URL.', example='https://example.com/mcp'),
    }
  ](name='Items', description='The list of MCP servers.', example='[...]'),
  maxResults?: int32(name='MaxResults', description='The maximum number of records per page.', example='20'),
  message?: string(name='Message', description='The response message.', example='success'),
  nextToken?: string(name='NextToken', description='The token for the next page.', example='next-token-xxx'),
  requestId?: string(name='RequestId', description='Id of the request', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
  totalCount?: int32(name='TotalCount', description='The total number of records.', example='10'),
}

model ListMcpsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListMcpsResponseBody(name='body'),
}

/**
 * @summary Queries the MCP server list under a specified AgentTeams instance by using paging.
 *
 * @description Queries the MCP server list under a specified AgentTeams instance by using paging.
 *
 * @param request ListMcpsRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListMcpsResponse
 */
@context("listMcpsWithContext")
async function listMcpsWithOptions(request: ListMcpsRequest, runtime: $RuntimeOptions): ListMcpsResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.maxResults)) {
    query['MaxResults'] = request.maxResults;
  }
  if (!$isNull(request.nextToken)) {
    query['NextToken'] = request.nextToken;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListMcps',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the MCP server list under a specified AgentTeams instance by using paging.
 *
 * @description Queries the MCP server list under a specified AgentTeams instance by using paging.
 *
 * @param request ListMcpsRequest
 * @return ListMcpsResponse
 */
async function listMcps(request: ListMcpsRequest): ListMcpsResponse {
  var runtime = new $RuntimeOptions{};
  return listMcpsWithOptions(request, runtime);
}

model ListModelProvidersRequest {
  instanceId?: string(name='InstanceId', description='Optional. Fuzzy matches by instance name.

This parameter is required.', example='AgentTeams'),
  maxResults?: int32(name='MaxResults', description='The maximum number of entries to return per page.', example='20'),
  nextToken?: string(name='NextToken', description='The pagination token used to retrieve the next page of results.', example='nt-xxx'),
}

model ListModelProvidersResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  items?: [ 
    {
      address?: string(name='Address', description='The model provider address.', example='https://api.example.com'),
      apiKeys?: [ string ](name='ApiKeys', description='The list of API keys.', example='["sk-xxx"]'),
      createTime?: string(name='CreateTime', description='The creation time.', example='2026-06-11T10:00:00Z'),
      deployStatus?: string(name='DeployStatus', description='The deployment status.', example='DEPLOYED'),
      description?: string(name='Description', description='The model provider description.', example='my provider'),
      id?: string(name='Id', description='The model provider ID.', example='p-xxx'),
      instanceId?: string(name='InstanceId', description='The instance ID.', example='i-xxx'),
      name?: string(name='Name', description='The model provider name.', example='my-provider'),
      protocols?: [ string ](name='Protocols', description='The list of protocols supported by the model provider.', example='["openai"]'),
      provider?: string(name='Provider', description='The provider identifier.', example='openai'),
    }
  ](name='Items', description='The list of model providers.', example='[...]'),
  maxResults?: int32(name='MaxResults', description='The maximum number of entries to return per page.', example='20'),
  message?: string(name='Message', description='The response message.', example='success'),
  nextToken?: string(name='NextToken', description='The pagination token.', example='nt-xxx'),
  requestId?: string(name='RequestId', description='Id of the request', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
  totalCount?: int32(name='TotalCount', description='The total number of records.', example='5'),
}

model ListModelProvidersResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListModelProvidersResponseBody(name='body'),
}

/**
 * @summary Queries the list of AI model providers under a specified AgentTeams instance. Paging is supported.
 *
 * @description Queries the list of AI model providers under a specified AgentTeams instance. Paging is supported.
 *
 * @param request ListModelProvidersRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListModelProvidersResponse
 */
@context("listModelProvidersWithContext")
async function listModelProvidersWithOptions(request: ListModelProvidersRequest, runtime: $RuntimeOptions): ListModelProvidersResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.maxResults)) {
    query['MaxResults'] = request.maxResults;
  }
  if (!$isNull(request.nextToken)) {
    query['NextToken'] = request.nextToken;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListModelProviders',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the list of AI model providers under a specified AgentTeams instance. Paging is supported.
 *
 * @description Queries the list of AI model providers under a specified AgentTeams instance. Paging is supported.
 *
 * @param request ListModelProvidersRequest
 * @return ListModelProvidersResponse
 */
async function listModelProviders(request: ListModelProvidersRequest): ListModelProvidersResponse {
  var runtime = new $RuntimeOptions{};
  return listModelProvidersWithOptions(request, runtime);
}

model ListModelsRequest {
  id?: string(name='Id', description='The model ID used to query a specific model.', example='m-xxx'),
  instanceId?: string(name='InstanceId', description='Optional. Fuzzy match by instance name.

This parameter is required.', example='AgentTeams'),
  maxResults?: int32(name='MaxResults', description='The maximum number of entries to return per page.', example='20'),
  name?: string(name='Name', description='The name.', example='worker-demo'),
  nextToken?: string(name='NextToken', description='The pagination token used to retrieve the next page of results.', example='nt-xxx'),
  providerName?: string(name='ProviderName', description='The model provider name used for filtering.', example='my-provider'),
}

model ListModelsResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  items?: [ 
    {
      description?: string(name='Description', description='The model description.', example='my model'),
      id?: string(name='Id', description='The model ID.', example='m-xxx'),
      instanceId?: string(name='InstanceId', description='The instance ID.', example='i-xxx'),
      name?: string(name='Name', description='The model name.', example='my-model'),
      protocols?: [ string ](name='Protocols', description='The list of protocols supported by the model.', example='["openai"]'),
      provider?: string(name='Provider', description='The provider identifier.', example='openai'),
      providerId?: string(name='ProviderId', description='The model provider ID.', example='p-xxx'),
      providerName?: string(name='ProviderName', description='The model provider name.', example='my-provider'),
      workerNum?: long(name='WorkerNum', description='The number of associated workers.', example='3'),
    }
  ](name='Items', description='The list of models.', example='[...]'),
  maxResults?: int32(name='MaxResults', description='The maximum number of entries to return per page.', example='20'),
  message?: string(name='Message', description='The response message.', example='success'),
  nextToken?: string(name='NextToken', description='The pagination token.', example='nt-xxx'),
  requestId?: string(name='RequestId', description='Id of the request', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
  totalCount?: int32(name='TotalCount', description='The total number of records.', example='10'),
}

model ListModelsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListModelsResponseBody(name='body'),
}

/**
 * @summary Queries the list of AI models under a specified AgentTeams instance. You can filter results by model name or provider name, and paging is supported.
 *
 * @description Queries the list of AI models under a specified AgentTeams instance. You can filter results by model name or provider name, and paging is supported.
 *
 * @param request ListModelsRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListModelsResponse
 */
@context("listModelsWithContext")
async function listModelsWithOptions(request: ListModelsRequest, runtime: $RuntimeOptions): ListModelsResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.id)) {
    query['Id'] = request.id;
  }
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.maxResults)) {
    query['MaxResults'] = request.maxResults;
  }
  if (!$isNull(request.name)) {
    query['Name'] = request.name;
  }
  if (!$isNull(request.nextToken)) {
    query['NextToken'] = request.nextToken;
  }
  if (!$isNull(request.providerName)) {
    query['ProviderName'] = request.providerName;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListModels',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the list of AI models under a specified AgentTeams instance. You can filter results by model name or provider name, and paging is supported.
 *
 * @description Queries the list of AI models under a specified AgentTeams instance. You can filter results by model name or provider name, and paging is supported.
 *
 * @param request ListModelsRequest
 * @return ListModelsResponse
 */
async function listModels(request: ListModelsRequest): ListModelsResponse {
  var runtime = new $RuntimeOptions{};
  return listModelsWithOptions(request, runtime);
}

model ListServiceEndpointsRequest {
  component?: string(name='Component', description='The endpoint component. Valid values: ELEMENT, MATRIX, WORKER, and TUNNEL.', example='MATRIX'),
  domainType?: string(name='DomainType', description='The domain name type. Valid values: BUILTIN and CUSTOM.', example='CUSTOM'),
  instanceId?: string(name='InstanceId', description='The AgentTeams instance ID.

This parameter is required.', example='agentteams-cn-xxxx'),
  maxResults?: int32(name='MaxResults', description='The maximum number of records to return in this request.', example='10'),
  networkType?: string(name='NetworkType', description='The network type. Valid values: INTRANET and INTERNET.', example='INTERNET'),
  nextToken?: string(name='NextToken', description='The pagination token used to retrieve the next page of data.', example='2'),
  resourceName?: string(name='ResourceName', description='The service name. Exact match is used.', example='matrix-service'),
  skip?: string(name='Skip', description='The number of records to skip.', example='0'),
}

model ListServiceEndpointsResponseBody = {
  code?: string(name='Code', description='The status code.', example='SUCCESS'),
  items?: [ 
    {
      certIdentifier?: string(name='CertIdentifier', description='The certificate identifier.', example='cert-xxx'),
      component?: string(name='Component', description='The endpoint component. Valid values: `ELEMENT`, `MATRIX`, `WORKER`, and `TUNNEL`.', example='MATRIX'),
      createTime?: string(name='CreateTime', description='The creation time.', example='2024-01-01T00:00:00Z'),
      domain?: string(name='Domain', description='The domain name.', example='matrix.example.com'),
      domainType?: string(name='DomainType', description='The domain name type. Valid values: `BUILTIN` and `CUSTOM`.', example='CUSTOM'),
      endpointConfig?: {
        auth?: {
          apiKey?: string(name='ApiKey', description='API Key', example='xxx'),
          apiKeyName?: string(name='ApiKeyName', description='The API key name.', example='Authorization'),
        }(name='Auth', description='The authentication configuration.', example='{}'),
      }(name='EndpointConfig', description='The configuration information of the endpoint.', example='{}'),
      endpointId?: string(name='EndpointId', description='Endpoint ID', example='ep-xxx'),
      endpointName?: string(name='EndpointName', description='The service name. Exact match is used.', example='matrix-service'),
      instanceId?: string(name='InstanceId', description='The instance ID.', example='agentteams-cn-xxxx'),
      networkType?: string(name='NetworkType', description='The network type. Valid values: `INTRANET` and `INTERNET`.', example='INTERNET'),
      status?: string(name='Status', description='The status.', example='CONFIGURED'),
      updateTime?: string(name='UpdateTime', description='The update time.', example='2024-01-01T00:00:00Z'),
    }
  ](name='Items', description='items', example='[...]'),
  maxResults?: int32(name='MaxResults', description='The maximum number of results returned.', example='1'),
  message?: string(name='Message', description='The response message.', example='success'),
  nextToken?: string(name='NextToken', description='nextToken', example='2'),
  requestId?: string(name='RequestId', description='The request ID.', example='xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
  totalCount?: int32(name='TotalCount', description='totalCount', example='1'),
}

model ListServiceEndpointsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListServiceEndpointsResponseBody(name='body'),
}

/**
 * @summary Queries the list of AI gateway endpoints under a specified instance.
 *
 * @description This API queries the list of AI gateway endpoints under a specified instance.
 * - instanceId is a required parameter that specifies the AgentTeams instance ID.
 * - Optional parameters include component, serviceName, networkType, and domainType, which are used to further filter the returned endpoint list.
 * - Filtering by the status parameter is not supported.
 *
 * @param request ListServiceEndpointsRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListServiceEndpointsResponse
 */
@context("listServiceEndpointsWithContext")
async function listServiceEndpointsWithOptions(request: ListServiceEndpointsRequest, runtime: $RuntimeOptions): ListServiceEndpointsResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.component)) {
    query['Component'] = request.component;
  }
  if (!$isNull(request.domainType)) {
    query['DomainType'] = request.domainType;
  }
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.maxResults)) {
    query['MaxResults'] = request.maxResults;
  }
  if (!$isNull(request.networkType)) {
    query['NetworkType'] = request.networkType;
  }
  if (!$isNull(request.nextToken)) {
    query['NextToken'] = request.nextToken;
  }
  if (!$isNull(request.resourceName)) {
    query['ResourceName'] = request.resourceName;
  }
  if (!$isNull(request.skip)) {
    query['Skip'] = request.skip;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListServiceEndpoints',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the list of AI gateway endpoints under a specified instance.
 *
 * @description This API queries the list of AI gateway endpoints under a specified instance.
 * - instanceId is a required parameter that specifies the AgentTeams instance ID.
 * - Optional parameters include component, serviceName, networkType, and domainType, which are used to further filter the returned endpoint list.
 * - Filtering by the status parameter is not supported.
 *
 * @param request ListServiceEndpointsRequest
 * @return ListServiceEndpointsResponse
 */
async function listServiceEndpoints(request: ListServiceEndpointsRequest): ListServiceEndpointsResponse {
  var runtime = new $RuntimeOptions{};
  return listServiceEndpointsWithOptions(request, runtime);
}

model ListSslCertsRequest {
  instanceId?: string(name='InstanceId', description='The AgentTeams instance ID. This parameter is used to verify the ownership of the current user\\\\"s instance and to query certificates by the instance region.

This parameter is required.', example='instance-1'),
  maxResults?: int32(name='MaxResults', description='The maximum number of records to return in this request.', example='10'),
  nextToken?: string(name='NextToken', description='The pagination token used to retrieve the next page of data.', example='2'),
}

model ListSslCertsResponseBody = {
  code?: string(name='Code', description='The error code. This field is typically empty when the request is successful.', example='null'),
  items?: [ 
    {
      algorithm?: string(name='Algorithm', description='The certificate algorithm.', example='RSA'),
      certId?: long(name='CertId', description='The certificate ID.', example='123'),
      certIdentifier?: string(name='CertIdentifier', description='The certificate identifier used by APIG. Pass this value when creating or updating an HTTPS domain name.', example='22584627-cn-hangzhou'),
      certName?: string(name='CertName', description='The certificate name.', example='example-cert'),
      chainCompleted?: boolean(name='ChainCompleted', description='Indicates whether the certificate chain is complete.', example='true'),
      commonName?: string(name='CommonName', description='The certificate common name.', example='example.com'),
      domain?: string(name='Domain', description='The certificate domain name.', example='example.com'),
      issuer?: string(name='Issuer', description='The certificate issuer.', example='DigiCert'),
      notAfterTimestamp?: long(name='NotAfterTimestamp', description='The expiration timestamp, in milliseconds.', example='1893456000000'),
      notBeforeTimestamp?: long(name='NotBeforeTimestamp', description='The effective period UNIX timestamp, in milliseconds.', example='1704067200000'),
    }
  ](name='Items', description='The list of certificates.', example='[...]'),
  maxResults?: int32(name='MaxResults', description='The maximum number of records to return in this request.', example='10'),
  message?: string(name='Message', description='The response message.', example='success'),
  nextToken?: string(name='NextToken', description='The pagination token for the next page. The value is `null` if no more pages are available.', example='2'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-123'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
  totalCount?: long(name='TotalCount', description='The total number of certificates.', example='11'),
}

model ListSslCertsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListSslCertsResponseBody(name='body'),
}

/**
 * @summary Queries the list of SSL certificates available to the user in APIG.
 *
 * @description Queries the list of SSL certificates available to the user in APIG.
 * - This API retrieves the list of SSL certificates associated with a specified AgentTeams instance.
 * - The pagination parameters MaxResults and NextToken allow the client to control the number of returned results and retrieve the next page of data.
 * - A successful response includes the request ID, success flag, error code, next page token, total certificate count, and the certificate details list.
 *
 * @param request ListSslCertsRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListSslCertsResponse
 */
@context("listSslCertsWithContext")
async function listSslCertsWithOptions(request: ListSslCertsRequest, runtime: $RuntimeOptions): ListSslCertsResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.maxResults)) {
    query['MaxResults'] = request.maxResults;
  }
  if (!$isNull(request.nextToken)) {
    query['NextToken'] = request.nextToken;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListSslCerts',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the list of SSL certificates available to the user in APIG.
 *
 * @description Queries the list of SSL certificates available to the user in APIG.
 * - This API retrieves the list of SSL certificates associated with a specified AgentTeams instance.
 * - The pagination parameters MaxResults and NextToken allow the client to control the number of returned results and retrieve the next page of data.
 * - A successful response includes the request ID, success flag, error code, next page token, total certificate count, and the certificate details list.
 *
 * @param request ListSslCertsRequest
 * @return ListSslCertsResponse
 */
async function listSslCerts(request: ListSslCertsRequest): ListSslCertsResponse {
  var runtime = new $RuntimeOptions{};
  return listSslCertsWithOptions(request, runtime);
}

model ListTeamDetailsRequest {
  endTime?: string(name='EndTime', description='The end time of the statistical interval. The value is a millisecond-level timestamp string.', example='1751932800000'),
  instanceId?: string(name='InstanceId', description='The instance ID.', example='agentteams-test-001'),
  maxResults?: int32(name='MaxResults', description='The maximum number of records to return in this request.', example='10'),
  nextToken?: string(name='NextToken', description='The pagination token used to retrieve the next page of data.', example='10'),
  startTime?: string(name='StartTime', description='The start time of the statistical interval. The value is a millisecond-level timestamp string.', example='1751328000000'),
}

model ListTeamDetailsResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  items?: [ 
    {
      avatarUrl?: string(name='AvatarUrl', description='The URL of the team avatar.', example='https://example.com/avatar.png'),
      status?: string(name='Status', description='The running status of the team.', example='Deleting'),
      successRate?: double(name='SuccessRate', description='The task success rate. Valid values: 0 to 100.', example='95.5'),
      taskCount?: int32(name='TaskCount', description='The number of tasks for the team.', example='12'),
      teamName?: string(name='TeamName', description='The team name.', example='my-team'),
      tokenUsage?: long(name='TokenUsage', description='The token usage within the time interval.', example='1024000'),
      workerCount?: int32(name='WorkerCount', description='The number of workers associated with the team.', example='3'),
    }
  ](name='Items', description='The list of team statistical details.', example='[...]'),
  maxResults?: int32(name='MaxResults', description='The maximum number of records to return in this request.', example='10'),
  message?: string(name='Message', description='The response message.', example='success'),
  nextToken?: string(name='NextToken', description='The pagination token for the next page. This parameter is empty if no more data is available.', example='10'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-list-team-details-001'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
  totalCount?: long(name='TotalCount', description='The total number of records that meet the conditions.', example='5'),
}

model ListTeamDetailsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListTeamDetailsResponseBody(name='body'),
}

/**
 * @summary Queries the statistical details of teams under a specified instance, including aggregate metrics such as the number of workers, number of tasks, success rate, and token usage for each team.
 *
 * @description Queries the statistical details of teams under a specified instance, including aggregate metrics such as the number of workers, number of tasks, success rate, and token usage for each team.
 *
 * @param request ListTeamDetailsRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListTeamDetailsResponse
 */
@context("listTeamDetailsWithContext")
async function listTeamDetailsWithOptions(request: ListTeamDetailsRequest, runtime: $RuntimeOptions): ListTeamDetailsResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.endTime)) {
    query['EndTime'] = request.endTime;
  }
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.maxResults)) {
    query['MaxResults'] = request.maxResults;
  }
  if (!$isNull(request.nextToken)) {
    query['NextToken'] = request.nextToken;
  }
  if (!$isNull(request.startTime)) {
    query['StartTime'] = request.startTime;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListTeamDetails',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the statistical details of teams under a specified instance, including aggregate metrics such as the number of workers, number of tasks, success rate, and token usage for each team.
 *
 * @description Queries the statistical details of teams under a specified instance, including aggregate metrics such as the number of workers, number of tasks, success rate, and token usage for each team.
 *
 * @param request ListTeamDetailsRequest
 * @return ListTeamDetailsResponse
 */
async function listTeamDetails(request: ListTeamDetailsRequest): ListTeamDetailsResponse {
  var runtime = new $RuntimeOptions{};
  return listTeamDetailsWithOptions(request, runtime);
}

model ListTeamTasksRequest {
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='agentteams-test-001'),
  maxResults?: int32(name='MaxResults', description='The maximum number of records to return in this request. Valid values: 1 to 100. Default value: 10.', example='10'),
  nextToken?: string(name='NextToken', description='The pagination token, which is the offset integer string returned from the previous page.', example='10'),
  team?: string(name='Team', description='The team name.

This parameter is required.', example='my-team'),
}

model ListTeamTasksResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  items?: [ 
    {
      assignedTo?: string(name='AssignedTo', description='The assignee of the task.', example='worker-1'),
      createdAt?: string(name='CreatedAt', description='The time when the task was created.', example='2026-07-01T12:00:00'),
      status?: string(name='Status', description='The task status.', example='running'),
      taskId?: string(name='TaskId', description='The task ID.', example='task-xxx'),
      taskTitle?: string(name='TaskTitle', description='The task title.', example='数据分析任务'),
    }
  ](name='Items', description='The task list.', example='[...]'),
  maxResults?: int32(name='MaxResults', description='The maximum number of records returned in this request.', example='10'),
  message?: string(name='Message', description='The response message.', example='success'),
  nextToken?: string(name='NextToken', description='The pagination token for the next page. This parameter is empty if no more data exists.', example='10'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-list-team-tasks-001'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
  totalCount?: long(name='TotalCount', description='The total number of records that meet the conditions.', example='25'),
}

model ListTeamTasksResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListTeamTasksResponseBody(name='body'),
}

/**
 * @summary Queries the task list of a specified team under a specified instance. The task metadata is sourced from the OSS bucket bound to the instance.
 *
 * @description Queries the task list of a specified team under a specified instance. The task metadata is sourced from the OSS bucket bound to the instance.
 *
 * @param request ListTeamTasksRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListTeamTasksResponse
 */
@context("listTeamTasksWithContext")
async function listTeamTasksWithOptions(request: ListTeamTasksRequest, runtime: $RuntimeOptions): ListTeamTasksResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.maxResults)) {
    query['MaxResults'] = request.maxResults;
  }
  if (!$isNull(request.nextToken)) {
    query['NextToken'] = request.nextToken;
  }
  if (!$isNull(request.team)) {
    query['Team'] = request.team;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListTeamTasks',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the task list of a specified team under a specified instance. The task metadata is sourced from the OSS bucket bound to the instance.
 *
 * @description Queries the task list of a specified team under a specified instance. The task metadata is sourced from the OSS bucket bound to the instance.
 *
 * @param request ListTeamTasksRequest
 * @return ListTeamTasksResponse
 */
async function listTeamTasks(request: ListTeamTasksRequest): ListTeamTasksResponse {
  var runtime = new $RuntimeOptions{};
  return listTeamTasksWithOptions(request, runtime);
}

model ListTeamsRequest {
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='agentteams-test-001'),
  maxResults?: int32(name='MaxResults', description='The maximum number of records to return in this request. Valid values: 1 to 100. Default value: 10.', example='10'),
  nameLike?: string(name='NameLike', description='The keyword for fuzzy match of team names.', example='my'),
  nextToken?: string(name='NextToken', description='The pagination token. Set this parameter to the offset integer string returned by the previous request.', example='10'),
}

model ListTeamsResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  items?: [ 
    {
      adminName?: string(name='AdminName', description='The name of the team administrator.', example='admin-user'),
      createdAt?: string(name='CreatedAt', description='The time when the team was created.', example='2026-07-01T12:00:00'),
      description?: string(name='Description', description='The team description.', example='This is a team.'),
      instanceId?: string(name='InstanceId', description='The instance ID.', example='agentteams-test-001'),
      leaderName?: string(name='LeaderName', description='The name of the team leader.', example='leader-user'),
      name?: string(name='Name', description='The team name.', example='my-team'),
      status?: string(name='Status', description='The running status of the team. This parameter is empty when the team is running normally.', example='Deleting'),
      teamMembers?: [ 
        {
          name?: string(name='Name', description='The team name.', example='my-team'),
        }
      ](name='TeamMembers', description='The list of team members.', example='[...]'),
      workerNames?: [ string ](name='WorkerNames', description='The list of Worker names associated with the team.', example='["worker-1"]'),
    }
  ](name='Items', description='The list of team summaries.', example='[...]'),
  maxResults?: int32(name='MaxResults', description='The maximum number of records returned in this request.', example='10'),
  message?: string(name='Message', description='The response message.', example='success'),
  nextToken?: string(name='NextToken', description='The pagination token for the next page. This parameter is empty if no more data is available.', example='10'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-list-teams-001'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
  totalCount?: long(name='TotalCount', description='The total number of records that match the specified conditions.', example='25'),
}

model ListTeamsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListTeamsResponseBody(name='body'),
}

/**
 * @summary Queries the list of teams under a specified instance, with support for fuzzy filtering by name and pagination.
 *
 * @description Queries the list of teams under a specified instance, with support for fuzzy filtering by name and pagination.
 *
 * @param request ListTeamsRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListTeamsResponse
 */
@context("listTeamsWithContext")
async function listTeamsWithOptions(request: ListTeamsRequest, runtime: $RuntimeOptions): ListTeamsResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.maxResults)) {
    query['MaxResults'] = request.maxResults;
  }
  if (!$isNull(request.nameLike)) {
    query['NameLike'] = request.nameLike;
  }
  if (!$isNull(request.nextToken)) {
    query['NextToken'] = request.nextToken;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListTeams',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the list of teams under a specified instance, with support for fuzzy filtering by name and pagination.
 *
 * @description Queries the list of teams under a specified instance, with support for fuzzy filtering by name and pagination.
 *
 * @param request ListTeamsRequest
 * @return ListTeamsResponse
 */
async function listTeams(request: ListTeamsRequest): ListTeamsResponse {
  var runtime = new $RuntimeOptions{};
  return listTeamsWithOptions(request, runtime);
}

model ListUsersRequest {
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='agentteams-test-001'),
  maxResults?: int32(name='MaxResults', description='The page size. Default value: 10. Valid values: 1 to 100.', example='10'),
  nameLike?: string(name='NameLike', description='The keyword for fuzzy match by username.', example='demo'),
  nextToken?: string(name='NextToken', description='The pagination token for the next page. A non-negative integer that represents the offset.', example='0'),
}

model ListUsersResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  items?: [ 
    {
      authMethod?: string(name='AuthMethod', description='The authentication method.', example='password'),
      displayName?: string(name='DisplayName', description='The display name.', example='Demo User'),
      email?: string(name='Email', description='The email address.', example='demo@example.com'),
      instanceId?: string(name='InstanceId', description='The instance ID.', example='agentteams-test-001'),
      name?: string(name='Name', description='The username.', example='demo-user'),
      status?: string(name='Status', description='The user status.', example='Active'),
    }
  ](name='Items', description='The list of users on the current page.', example='[...]'),
  maxResults?: int32(name='MaxResults', description='The page size used in this response.', example='10'),
  message?: string(name='Message', description='The response message.', example='success'),
  nextToken?: string(name='NextToken', description='The pagination token for the next page. This parameter is null if no more pages exist.', example='10'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-list-users-001'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
  totalCount?: long(name='TotalCount', description='The total number of records.', example='23'),
}

model ListUsersResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListUsersResponseBody(name='body'),
}

/**
 * @summary Queries the list of users under a specified instance. Supports fuzzy match by username and paging query. The list is returned in reverse chronological order by creation time.
 *
 * @description Queries the list of users under a specified instance. Supports fuzzy match by username and paging query. The list is returned in reverse chronological order by creation time.
 *
 * @param request ListUsersRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListUsersResponse
 */
@context("listUsersWithContext")
async function listUsersWithOptions(request: ListUsersRequest, runtime: $RuntimeOptions): ListUsersResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.maxResults)) {
    query['MaxResults'] = request.maxResults;
  }
  if (!$isNull(request.nameLike)) {
    query['NameLike'] = request.nameLike;
  }
  if (!$isNull(request.nextToken)) {
    query['NextToken'] = request.nextToken;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListUsers',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the list of users under a specified instance. Supports fuzzy match by username and paging query. The list is returned in reverse chronological order by creation time.
 *
 * @description Queries the list of users under a specified instance. Supports fuzzy match by username and paging query. The list is returned in reverse chronological order by creation time.
 *
 * @param request ListUsersRequest
 * @return ListUsersResponse
 */
async function listUsers(request: ListUsersRequest): ListUsersResponse {
  var runtime = new $RuntimeOptions{};
  return listUsersWithOptions(request, runtime);
}

model ListWorkerStatsDetailsRequest {
  endTime?: string(name='EndTime', description='The end time for statistics. This value is a UNIX timestamp in milliseconds.', example='1720054400000'),
  instanceId?: string(name='InstanceId', description='The instance ID.', example='inst-demo'),
  maxResults?: int32(name='MaxResults', description='The maximum number of entries to return per page.', example='10'),
  nextToken?: string(name='NextToken', description='The pagination token used to query the next page of data.', example='0'),
  startTime?: string(name='StartTime', description='The start time for statistics. This value is a UNIX timestamp in milliseconds.', example='1719504000000'),
}

model ListWorkerStatsDetailsResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  items?: [ 
    {
      llmCallCount?: long(name='LlmCallCount', description='The LLM call count.', example='350'),
      model?: string(name='Model', description='The model name.', example='qwen-max'),
      name?: string(name='Name', description='The Worker name.', example='worker-demo'),
      status?: string(name='Status', description='The Worker status.', example='Running'),
      taskCount?: long(name='TaskCount', description='The task count.', example='120'),
      tokenUsage?: long(name='TokenUsage', description='The token usage.', example='500000'),
    }
  ](name='Items', description='The list of Worker statistics details.', example='[...]'),
  maxResults?: int32(name='MaxResults', description='The maximum number of entries to return per page.', example='10'),
  message?: string(name='Message', description='The response message.', example='success'),
  nextToken?: string(name='NextToken', description='The pagination token for the next page.', example='10'),
  requestId?: string(name='RequestId', description='The request ID.', example='473469C7-XX-XX-XX'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
  totalCount?: long(name='TotalCount', description='The total number of entries.', example='25'),
}

model ListWorkerStatsDetailsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListWorkerStatsDetailsResponseBody(name='body'),
}

/**
 * @summary Queries a list of Worker statistics details by paging, including task count, token usage, and LLM call count.
 *
 * @description Queries a list of Worker statistics details by paging, including task count, token usage, and LLM call count.
 *
 * @param request ListWorkerStatsDetailsRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListWorkerStatsDetailsResponse
 */
@context("listWorkerStatsDetailsWithContext")
async function listWorkerStatsDetailsWithOptions(request: ListWorkerStatsDetailsRequest, runtime: $RuntimeOptions): ListWorkerStatsDetailsResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.endTime)) {
    query['EndTime'] = request.endTime;
  }
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.maxResults)) {
    query['MaxResults'] = request.maxResults;
  }
  if (!$isNull(request.nextToken)) {
    query['NextToken'] = request.nextToken;
  }
  if (!$isNull(request.startTime)) {
    query['StartTime'] = request.startTime;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListWorkerStatsDetails',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries a list of Worker statistics details by paging, including task count, token usage, and LLM call count.
 *
 * @description Queries a list of Worker statistics details by paging, including task count, token usage, and LLM call count.
 *
 * @param request ListWorkerStatsDetailsRequest
 * @return ListWorkerStatsDetailsResponse
 */
async function listWorkerStatsDetails(request: ListWorkerStatsDetailsRequest): ListWorkerStatsDetailsResponse {
  var runtime = new $RuntimeOptions{};
  return listWorkerStatsDetailsWithOptions(request, runtime);
}

model ListWorkersRequest {
  agentType?: string(name='AgentType', description='The Agent runtime type filter.', example='Qwenpaw'),
  credential?: string(name='Credential', description='The credential name filter.', example='cred-demo'),
  group?: {
    name?: string(name='Name', description='The returned data.', example='worker-demo'),
    role?: string(name='Role', description='The returned data.', example='member'),
    type?: string(name='Type', description='The returned data.', example='team'),
  }(name='Group', description='The team group filter.', example='{}'),
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='inst-demo'),
  maxResults?: int32(name='MaxResults', description='The maximum number of entries to return per page. Valid values: 1 to 100. Default value: 10.', example='10'),
  mcp?: string(name='Mcp', description='The MCP server name filter.', example='mcp-demo'),
  modelName?: string(name='ModelName', description='The model name filter.', example='qwen-max'),
  modelProvider?: string(name='ModelProvider', description='The model provider filter.', example='dashscope'),
  nameLike?: string(name='NameLike', description='The fuzzy match filter for the Worker name.', example='worker'),
  nextToken?: string(name='NextToken', description='The pagination token used to query the next page of data.', example='0'),
  template?: {
    label?: string(name='Label', description='The returned data.', example='标签'),
    name?: string(name='Name', description='The returned data.', example='worker-demo'),
    version?: string(name='Version', description='The returned data.', example='1.0.0'),
  }(name='Template', description='The template filter.', example='{}'),
  versionCode?: string(name='VersionCode', description='The Worker version number filter.', example='v1.0.0'),
}

model ListWorkersShrinkRequest {
  agentType?: string(name='AgentType', description='The Agent runtime type filter.', example='Qwenpaw'),
  credential?: string(name='Credential', description='The credential name filter.', example='cred-demo'),
  groupShrink?: string(name='Group', description='The team group filter.', example='{}'),
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='inst-demo'),
  maxResults?: int32(name='MaxResults', description='The maximum number of entries to return per page. Valid values: 1 to 100. Default value: 10.', example='10'),
  mcp?: string(name='Mcp', description='The MCP server name filter.', example='mcp-demo'),
  modelName?: string(name='ModelName', description='The model name filter.', example='qwen-max'),
  modelProvider?: string(name='ModelProvider', description='The model provider filter.', example='dashscope'),
  nameLike?: string(name='NameLike', description='The fuzzy match filter for the Worker name.', example='worker'),
  nextToken?: string(name='NextToken', description='The pagination token used to query the next page of data.', example='0'),
  templateShrink?: string(name='Template', description='The template filter.', example='{}'),
  versionCode?: string(name='VersionCode', description='The Worker version number filter.', example='v1.0.0'),
}

model ListWorkersResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  items?: [ 
    {
      agentType?: string(name='AgentType', description='The Agent runtime type.', example='Qwenpaw'),
      deployType?: string(name='DeployType', description='The deployment type.', example='Managed'),
      groups?: [ 
        {
          name?: string(name='Name', description='The Worker name.', example='worker-demo'),
          role?: string(name='Role', description='The group role.', example='member'),
          type?: string(name='Type', description='The group type.', example='team'),
        }
      ](name='Groups', description='The list of team groups.', example='[...]'),
      instanceId?: string(name='InstanceId', description='The instance ID.', example='inst-demo'),
      name?: string(name='Name', description='The Worker name.', example='worker-demo'),
      status?: string(name='Status', description='The Worker status.', example='Running'),
      template?: {
        label?: string(name='Label', description='The template label.', example='默认模板'),
        name?: string(name='Name', description='The template name.', example='default-template'),
        version?: string(name='Version', description='The template version.', example='1.0.0'),
      }(name='Template', description='The template configuration.', example='{}'),
      versionCode?: string(name='VersionCode', description='The Worker version number.', example='v1.0.0'),
    }
  ](name='Items', description='The list of Workers.', example='[...]'),
  maxResults?: int32(name='MaxResults', description='The maximum number of entries returned per page.', example='10'),
  message?: string(name='Message', description='The response message.', example='success'),
  nextToken?: string(name='NextToken', description='The pagination token for the next page.', example='10'),
  requestId?: string(name='RequestId', description='The request ID.', example='473469C7-XX-XX-XX'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
  totalCount?: long(name='TotalCount', description='The total number of entries.', example='25'),
}

model ListWorkersResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListWorkersResponseBody(name='body'),
}

/**
 * @summary Queries the list of Workers under a specified instance by using paging, with support for filtering by name, model, template, and other conditions.
 *
 * @description Queries the list of Workers under a specified instance by using paging, with support for filtering by name, model, template, and other conditions.
 *
 * @param tmpReq ListWorkersRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListWorkersResponse
 */
@context("listWorkersWithContext")
async function listWorkersWithOptions(tmpReq: ListWorkersRequest, runtime: $RuntimeOptions): ListWorkersResponse {
  tmpReq.validate();
  var request = new ListWorkersShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.group)) {
    request.groupShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.group, 'Group', 'json');
  }
  if (!$isNull(tmpReq.template)) {
    request.templateShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.template, 'Template', 'json');
  }
  var query = {};
  if (!$isNull(request.agentType)) {
    query['AgentType'] = request.agentType;
  }
  if (!$isNull(request.credential)) {
    query['Credential'] = request.credential;
  }
  if (!$isNull(request.groupShrink)) {
    query['Group'] = request.groupShrink;
  }
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.maxResults)) {
    query['MaxResults'] = request.maxResults;
  }
  if (!$isNull(request.mcp)) {
    query['Mcp'] = request.mcp;
  }
  if (!$isNull(request.modelName)) {
    query['ModelName'] = request.modelName;
  }
  if (!$isNull(request.modelProvider)) {
    query['ModelProvider'] = request.modelProvider;
  }
  if (!$isNull(request.nameLike)) {
    query['NameLike'] = request.nameLike;
  }
  if (!$isNull(request.nextToken)) {
    query['NextToken'] = request.nextToken;
  }
  if (!$isNull(request.templateShrink)) {
    query['Template'] = request.templateShrink;
  }
  if (!$isNull(request.versionCode)) {
    query['VersionCode'] = request.versionCode;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListWorkers',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the list of Workers under a specified instance by using paging, with support for filtering by name, model, template, and other conditions.
 *
 * @description Queries the list of Workers under a specified instance by using paging, with support for filtering by name, model, template, and other conditions.
 *
 * @param request ListWorkersRequest
 * @return ListWorkersResponse
 */
async function listWorkers(request: ListWorkersRequest): ListWorkersResponse {
  var runtime = new $RuntimeOptions{};
  return listWorkersWithOptions(request, runtime);
}

model PutCmsWorkspaceRequest {
  instanceId?: string(name='InstanceId', description='The AgentTeams instance ID.

This parameter is required.', example='at-xxx'),
}

model PutCmsWorkspaceResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  data?: {
    requestId?: string(name='RequestId', description='The request ID.', example='req-xxx'),
    workspaceName?: string(name='WorkspaceName', description='The workspace name.', example='magic-workspace-at-xxx'),
  }(name='Data', description='The returned data.', example='{}'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model PutCmsWorkspaceResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: PutCmsWorkspaceResponseBody(name='body'),
}

/**
 * @summary Creates or updates a CMS workspace.
 *
 * @description Creates or updates a CMS workspace.
 *
 * @param request PutCmsWorkspaceRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return PutCmsWorkspaceResponse
 */
@context("putCmsWorkspaceWithContext")
async function putCmsWorkspaceWithOptions(request: PutCmsWorkspaceRequest, runtime: $RuntimeOptions): PutCmsWorkspaceResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'PutCmsWorkspace',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates or updates a CMS workspace.
 *
 * @description Creates or updates a CMS workspace.
 *
 * @param request PutCmsWorkspaceRequest
 * @return PutCmsWorkspaceResponse
 */
async function putCmsWorkspace(request: PutCmsWorkspaceRequest): PutCmsWorkspaceResponse {
  var runtime = new $RuntimeOptions{};
  return putCmsWorkspaceWithOptions(request, runtime);
}

model QueryFeaturesRequest {
  instanceId?: string(name='InstanceId', description='The AgentTeams instance ID.

This parameter is required.', example='at-xxx'),
  resourceName?: string(name='ResourceName', description='The target resource name. Required for `WORKER`, `TEAM`, and `HUMAN`. Not required for `INSTANCE`.', example='worker-a'),
  targetScope?: string(name='TargetScope', description='The query target type: `INSTANCE` / `WORKER` / `TEAM` / `HUMAN`.

This parameter is required.', example='INSTANCE'),
}

model QueryFeaturesResponseBody = {
  code?: string(name='Code', description='The response code.', example='Success'),
  data?: {
    features?: [ 
      {
        description?: string(name='Description', description='The feature description.', example='Supports mounting an OSS bucket to an Agent container.'),
        displayName?: string(name='DisplayName', description='The feature display name.', example='OSS Mount.'),
        featureCode?: string(name='FeatureCode', description='The feature code.', example='OSS_MOUNT'),
        supported?: boolean(name='Supported', description='Indicates whether the feature is supported.', example='true'),
        unsupportedReason?: string(name='UnsupportedReason', description='The detailed reason why the feature is not supported.', example='The current base version does not support this feature.'),
        unsupportedReasonCode?: string(name='UnsupportedReasonCode', description='The reason code for why the feature is not supported. Valid values:
 * BASE_VERSION_UNSUPPORTED: The base version does not support this feature.
 * WORKER_VERSION_UNSUPPORTED: The worker version does not support this feature.
 * FEATURE_DISABLED: The feature is disabled.
 * FEATURE_DEPRECATED: The feature is deprecated.
 * OVERRIDDEN_DENY: The feature is denied by an override.
 * CUSTOM_RULE_UNSUPPORTED: A custom rule does not support this feature.
 * VERSION_METADATA_MISSING: The version metadata is missing.
 * RULE_NOT_MATCHED: No rule is matched.', example='BASE_VERSION_UNSUPPORTED'),
      }
    ](name='Features', description='The list of features.', example='[...]'),
    instanceId?: string(name='InstanceId', description='The AgentTeams instance ID.', example='at-xxx'),
    resourceName?: string(name='ResourceName', description='The target resource name. Required for `WORKER`, `TEAM`, and `HUMAN`. Not required for `INSTANCE`.', example='null'),
    targetScope?: string(name='TargetScope', description='The query target type. Valid values:
 * INSTANCE: instance level
 * WORKER: worker level
 * TEAM: team level
 * HUMAN: human level', example='INSTANCE'),
  }(name='Data', description='The queried feature information.', example='{}'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model QueryFeaturesResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: QueryFeaturesResponseBody(name='body'),
}

/**
 * @summary Queries the attribute status of a specified instance, worker, team, or individual.
 *
 * @description Queries the feature status of a specified instance, worker, team, or individual.
 * - This operation queries the feature status of different targets (such as INSTANCE, WORKER, TEAM, or HUMAN) under a specific instanceId.
 * - The targetScope parameter defines the target type for the query. Depending on the targetScope value, you may need to provide an additional resourceName parameter to specify the resource name.
 * - If a featureCodes list is provided, the status of those specific features is returned. Otherwise, the status of all features under the specified targetScope is returned.
 * - When using WORKER, TEAM, or HUMAN as the targetScope, make sure to correctly specify the corresponding resourceName.
 * - For INSTANCE-level queries, resourceName is not required.
 * - Feature support is affected by factors such as the base version and worker version. The unsupportedReasonCode and unsupportedReason fields provide the specific reason why a feature is not supported.
 *
 * @param request QueryFeaturesRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return QueryFeaturesResponse
 */
@context("queryFeaturesWithContext")
async function queryFeaturesWithOptions(request: QueryFeaturesRequest, runtime: $RuntimeOptions): QueryFeaturesResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.resourceName)) {
    query['ResourceName'] = request.resourceName;
  }
  if (!$isNull(request.targetScope)) {
    query['TargetScope'] = request.targetScope;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'QueryFeatures',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the attribute status of a specified instance, worker, team, or individual.
 *
 * @description Queries the feature status of a specified instance, worker, team, or individual.
 * - This operation queries the feature status of different targets (such as INSTANCE, WORKER, TEAM, or HUMAN) under a specific instanceId.
 * - The targetScope parameter defines the target type for the query. Depending on the targetScope value, you may need to provide an additional resourceName parameter to specify the resource name.
 * - If a featureCodes list is provided, the status of those specific features is returned. Otherwise, the status of all features under the specified targetScope is returned.
 * - When using WORKER, TEAM, or HUMAN as the targetScope, make sure to correctly specify the corresponding resourceName.
 * - For INSTANCE-level queries, resourceName is not required.
 * - Feature support is affected by factors such as the base version and worker version. The unsupportedReasonCode and unsupportedReason fields provide the specific reason why a feature is not supported.
 *
 * @param request QueryFeaturesRequest
 * @return QueryFeaturesResponse
 */
async function queryFeatures(request: QueryFeaturesRequest): QueryFeaturesResponse {
  var runtime = new $RuntimeOptions{};
  return queryFeaturesWithOptions(request, runtime);
}

model QuerySupportedZonesRequest {
  maxResults?: int32(name='MaxResults', description='The page size.', example='2'),
  nextToken?: string(name='NextToken', description='The token for the next page.', example='null'),
}

model QuerySupportedZonesResponseBody = {
  code?: string(name='Code', description='The response code, which is `SUCCESS` when the operation succeeds.', example='SUCCESS'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code, which is `200` when the operation succeeds.', example='200'),
  items?: [ 
    {
      zoneId?: string(name='ZoneId', description='The zone ID.', example='cn-hangzhou-i'),
    }
  ](name='Items', description='The list of zones.', example='[...]'),
  maxResults?: int32(name='MaxResults', description='The number of entries returned in this request.', example='2'),
  message?: string(name='Message', description='The response message, which is `success` when the operation succeeds.', example='success'),
  nextToken?: string(name='NextToken', description='The token for the next page, which is `null` because this operation does not support pagination.', example='null'),
  requestId?: string(name='RequestId', description='The request ID, which is `null` when the operation succeeds.', example='null'),
  success?: boolean(name='Success', description='Indicates whether the operation was successful.', example='true'),
  totalCount?: int32(name='TotalCount', description='The total number of zones.', example='2'),
}

model QuerySupportedZonesResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: QuerySupportedZonesResponseBody(name='body'),
}

/**
 * @summary Retrieves all zone IDs supported by the current AgentTeams Resource Pool configuration.
 *
 * @description Retrieves all zone IDs supported by the current AgentTeams Resource Pool configuration.
 *
 * @param request QuerySupportedZonesRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return QuerySupportedZonesResponse
 */
@context("querySupportedZonesWithContext")
async function querySupportedZonesWithOptions(request: QuerySupportedZonesRequest, runtime: $RuntimeOptions): QuerySupportedZonesResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.maxResults)) {
    query['MaxResults'] = request.maxResults;
  }
  if (!$isNull(request.nextToken)) {
    query['NextToken'] = request.nextToken;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'QuerySupportedZones',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves all zone IDs supported by the current AgentTeams Resource Pool configuration.
 *
 * @description Retrieves all zone IDs supported by the current AgentTeams Resource Pool configuration.
 *
 * @param request QuerySupportedZonesRequest
 * @return QuerySupportedZonesResponse
 */
async function querySupportedZones(request: QuerySupportedZonesRequest): QuerySupportedZonesResponse {
  var runtime = new $RuntimeOptions{};
  return querySupportedZonesWithOptions(request, runtime);
}

model ResetUserPasswordRequest {
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='agentteams-test-001'),
  name?: string(name='Name', description='The name.

This parameter is required.', example='worker-demo'),
  password?: string(name='Password', description='The new password.', example='NewP@ssw0rd456'),
}

model ResetUserPasswordResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  data?: {
    instanceId?: string(name='InstanceId', description='The instance ID.', example='agentteams-test-001'),
    name?: string(name='Name', description='The username.', example='demo-user'),
    password?: string(name='Password', description='The password after the reset.', example='NewP@ssw0rd456'),
    subject?: string(name='Subject', description='The user principal identifier.', example='sub-xxx'),
    userPoolId?: string(name='UserPoolId', description='The user pool ID.', example='pool-xxx'),
  }(name='Data', description='The returned data.', example='{}'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-reset-password-001'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model ResetUserPasswordResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ResetUserPasswordResponseBody(name='body'),
}

/**
 * @summary Resets the password of a specified user under a specified instance. After the reset, the user must log on with the new password.
 *
 * @description Resets the password of a specified user under a specified instance. After the reset, the user must log on with the new password.
 *
 * @param request ResetUserPasswordRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ResetUserPasswordResponse
 */
@context("resetUserPasswordWithContext")
async function resetUserPasswordWithOptions(request: ResetUserPasswordRequest, runtime: $RuntimeOptions): ResetUserPasswordResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.name)) {
    query['Name'] = request.name;
  }
  if (!$isNull(request.password)) {
    query['Password'] = request.password;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ResetUserPassword',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Resets the password of a specified user under a specified instance. After the reset, the user must log on with the new password.
 *
 * @description Resets the password of a specified user under a specified instance. After the reset, the user must log on with the new password.
 *
 * @param request ResetUserPasswordRequest
 * @return ResetUserPasswordResponse
 */
async function resetUserPassword(request: ResetUserPasswordRequest): ResetUserPasswordResponse {
  var runtime = new $RuntimeOptions{};
  return resetUserPasswordWithOptions(request, runtime);
}

model TestModelProviderRequest {
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='i-xxx'),
  modelName?: string(name='ModelName', description='The model name. Optional. Filters by fuzzy match on instance name.

This parameter is required.', example='AgentTeams'),
  prompt?: string(name='Prompt', description='The content of the test prompt.

This parameter is required.', example='Hello, how are you?'),
  providerId?: string(name='ProviderId', description='The provider ID. Optional. Filters by exact match on instance status.

This parameter is required.', example='RUNNING'),
  providerName?: string(name='ProviderName', description='The name of the model provider.

This parameter is required.', example='my-provider'),
}

model TestModelProviderResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  data?: {
    errorMessage?: string(name='ErrorMessage', description='The error message. This parameter is returned when the test fails.', example='The model provider returned an error'),
    inputTokens?: long(name='InputTokens', description='The number of input tokens.', example='10'),
    latencyMs?: long(name='LatencyMs', description='The call latency, in milliseconds.', example='500'),
    outputTokens?: long(name='OutputTokens', description='The number of output tokens.', example='20'),
    response?: string(name='Response', description='The response content returned by the model.', example='I am fine, thank you!'),
    status?: string(name='Status', description='The test status.', example='success'),
    success?: boolean(name='Success', description='Indicates whether the test was successful.', example='true'),
  }(name='Data', description='The returned data.', example='{}'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='Id of the request', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model TestModelProviderResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: TestModelProviderResponseBody(name='body'),
}

/**
 * @summary Tests the connectivity of an AI model provider and model under a specified AgentTeams instance by sending a test prompt and returning the call result, latency, and token usage.
 *
 * @description Tests the connectivity of an AI model provider and model under a specified AgentTeams instance by sending a test prompt and returning the call result, latency, and token usage.
 *
 * @param request TestModelProviderRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return TestModelProviderResponse
 */
@context("testModelProviderWithContext")
async function testModelProviderWithOptions(request: TestModelProviderRequest, runtime: $RuntimeOptions): TestModelProviderResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.instanceId)) {
    body['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.modelName)) {
    body['ModelName'] = request.modelName;
  }
  if (!$isNull(request.prompt)) {
    body['Prompt'] = request.prompt;
  }
  if (!$isNull(request.providerId)) {
    body['ProviderId'] = request.providerId;
  }
  if (!$isNull(request.providerName)) {
    body['ProviderName'] = request.providerName;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'TestModelProvider',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Tests the connectivity of an AI model provider and model under a specified AgentTeams instance by sending a test prompt and returning the call result, latency, and token usage.
 *
 * @description Tests the connectivity of an AI model provider and model under a specified AgentTeams instance by sending a test prompt and returning the call result, latency, and token usage.
 *
 * @param request TestModelProviderRequest
 * @return TestModelProviderResponse
 */
async function testModelProvider(request: TestModelProviderRequest): TestModelProviderResponse {
  var runtime = new $RuntimeOptions{};
  return testModelProviderWithOptions(request, runtime);
}

model UnbindIdentityProviderRequest {
  identityProviderType?: string(name='IdentityProviderType', description='The identity provider type. Currently, only DingTalk is supported. Specify this parameter to locate the binding to be unbound.', example='DingTalk'),
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='agentteams-xxx'),
}

model UnbindIdentityProviderResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the call is successful.', example='true'),
}

model UnbindIdentityProviderResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UnbindIdentityProviderResponseBody(name='body'),
}

/**
 * @summary Unbinds the upstream identity provider from a specified instance, dissociates the identity federation relationship, and cleans up associated user identities and data.
 *
 * @description Unbinds the upstream identity provider from a specified instance, dissociates the identity federation relationship, and cleans up associated user identities and data.
 *
 * @param request UnbindIdentityProviderRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return UnbindIdentityProviderResponse
 */
@context("unbindIdentityProviderWithContext")
async function unbindIdentityProviderWithOptions(request: UnbindIdentityProviderRequest, runtime: $RuntimeOptions): UnbindIdentityProviderResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.identityProviderType)) {
    query['IdentityProviderType'] = request.identityProviderType;
  }
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'UnbindIdentityProvider',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Unbinds the upstream identity provider from a specified instance, dissociates the identity federation relationship, and cleans up associated user identities and data.
 *
 * @description Unbinds the upstream identity provider from a specified instance, dissociates the identity federation relationship, and cleans up associated user identities and data.
 *
 * @param request UnbindIdentityProviderRequest
 * @return UnbindIdentityProviderResponse
 */
async function unbindIdentityProvider(request: UnbindIdentityProviderRequest): UnbindIdentityProviderResponse {
  var runtime = new $RuntimeOptions{};
  return unbindIdentityProviderWithOptions(request, runtime);
}

model UpdateCredentialRequest {
  apiKey?: string(name='ApiKey', description='The credential value.

This parameter is required.', example='sk-yyyyyyyyyyyy'),
  clientToken?: string(name='ClientToken', description='The idempotency token that is used to ensure the idempotence of the request.', example='a1b2c3d4-e5f6-7890-1234-567890abcdef'),
  description?: string(name='Description', description='The credential description.', example='OpenAI API key for the team'),
  instanceId?: string(name='InstanceId', description='The AgentTeams instance ID.

This parameter is required.', example='ami-2ze8x9c6f'),
  name?: string(name='Name', description='The name.

This parameter is required.', example='worker-demo'),
}

model UpdateCredentialResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  data?: {
    createTime?: string(name='CreateTime', description='The creation time in ISO 8601 format.', example='2026-07-13T12:00:00Z'),
    description?: string(name='Description', description='The credential description.', example='OpenAI API key for the team'),
    instanceId?: string(name='InstanceId', description='The instance ID.', example='ami-2ze8x9c6f'),
    name?: string(name='Name', description='The credential key.', example='OPENAI_API_KEY'),
    status?: string(name='Status', description='The resource status.', example='Active'),
    updateTime?: string(name='UpdateTime', description='The update time in ISO 8601 format.', example='2026-07-13T13:00:00Z'),
  }(name='Data', description='The returned data.', example='{}'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model UpdateCredentialResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateCredentialResponseBody(name='body'),
}

/**
 * @summary Updates the plaintext key of an existing credential in an AgentTeams instance.
 *
 * @description Updates the plaintext key of an existing credential in an AgentTeams instance.
 * - This operation updates the plaintext key of an existing Credential in an AgentTeams instance.
 * - Only the key value of the APIKeyCredentialProvider with the same name in the Agent Identity TokenVault is updated. Local metadata (description, createTime, updateTime, and status) is not modified.
 * - The response does not contain the apiKey plaintext. To obtain Worker details, call GetCredential.
 *
 * @param request UpdateCredentialRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateCredentialResponse
 */
@context("updateCredentialWithContext")
async function updateCredentialWithOptions(request: UpdateCredentialRequest, runtime: $RuntimeOptions): UpdateCredentialResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.description)) {
    query['Description'] = request.description;
  }
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.name)) {
    query['Name'] = request.name;
  }
  var body : map[string]any = {};
  if (!$isNull(request.apiKey)) {
    body['ApiKey'] = request.apiKey;
  }
  if (!$isNull(request.clientToken)) {
    body['ClientToken'] = request.clientToken;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpdateCredential',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Updates the plaintext key of an existing credential in an AgentTeams instance.
 *
 * @description Updates the plaintext key of an existing credential in an AgentTeams instance.
 * - This operation updates the plaintext key of an existing Credential in an AgentTeams instance.
 * - Only the key value of the APIKeyCredentialProvider with the same name in the Agent Identity TokenVault is updated. Local metadata (description, createTime, updateTime, and status) is not modified.
 * - The response does not contain the apiKey plaintext. To obtain Worker details, call GetCredential.
 *
 * @param request UpdateCredentialRequest
 * @return UpdateCredentialResponse
 */
async function updateCredential(request: UpdateCredentialRequest): UpdateCredentialResponse {
  var runtime = new $RuntimeOptions{};
  return updateCredentialWithOptions(request, runtime);
}

model UpdateIdentityProviderRequest {
  clientToken?: string(name='ClientToken', description='The client idempotency token that is used to ensure the idempotence of the request.', example='ct-1234abcd'),
  identityProviderType?: string(name='IdentityProviderType', description='The identity provider type. Currently, only DingTalk is supported.

This parameter is required.', example='DingTalk'),
  idpMetadata?: string(name='IdpMetadata', description='The identity provider metadata. In the DingTalk scenario, this is a JSON object string that contains the appKey, appSecret, corpId, encryptKey, and verificationToken fields.', example='{"appKey":"dingxxxx","appSecret":"xxxxx","corpId":"dingxxxx","encryptKey":"xxxxx","verificationToken":"xxxxx"}'),
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='agentteams-xxx'),
  loginEnabled?: boolean(name='LoginEnabled', description='Specifies whether to enable logon. If this parameter is not specified, the existing configuration is retained.', example='false'),
  syncEnabled?: boolean(name='SyncEnabled', description='Specifies whether to enable user synchronization. If this parameter is not specified, the existing configuration is retained.', example='false'),
}

model UpdateIdentityProviderResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  data?: {
    bindingId?: long(name='BindingId', description='The binding ID.', example='binding-xxx'),
    identityProviderType?: string(name='IdentityProviderType', description='The identity provider type.', example='DingTalk'),
    instanceId?: string(name='InstanceId', description='The instance ID.', example='agentteams-demo'),
    loginEnabled?: boolean(name='LoginEnabled', description='Indicates whether logon is enabled.', example='true'),
    syncEnabled?: boolean(name='SyncEnabled', description='Indicates whether synchronization is enabled.', example='false'),
  }(name='Data', description='The response data.', example='{}'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model UpdateIdentityProviderResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateIdentityProviderResponseBody(name='body'),
}

/**
 * @summary Updates the upstream identity provider configuration bound to a specified instance. You can adjust the logon switch and user synchronization switch.
 *
 * @description Updates the upstream identity provider configuration bound to a specified instance. You can adjust the logon switch and user synchronization switch.
 *
 * @param request UpdateIdentityProviderRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateIdentityProviderResponse
 */
@context("updateIdentityProviderWithContext")
async function updateIdentityProviderWithOptions(request: UpdateIdentityProviderRequest, runtime: $RuntimeOptions): UpdateIdentityProviderResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.identityProviderType)) {
    query['IdentityProviderType'] = request.identityProviderType;
  }
  if (!$isNull(request.idpMetadata)) {
    query['IdpMetadata'] = request.idpMetadata;
  }
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.loginEnabled)) {
    query['LoginEnabled'] = request.loginEnabled;
  }
  if (!$isNull(request.syncEnabled)) {
    query['SyncEnabled'] = request.syncEnabled;
  }
  var body : map[string]any = {};
  if (!$isNull(request.clientToken)) {
    body['ClientToken'] = request.clientToken;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpdateIdentityProvider',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Updates the upstream identity provider configuration bound to a specified instance. You can adjust the logon switch and user synchronization switch.
 *
 * @description Updates the upstream identity provider configuration bound to a specified instance. You can adjust the logon switch and user synchronization switch.
 *
 * @param request UpdateIdentityProviderRequest
 * @return UpdateIdentityProviderResponse
 */
async function updateIdentityProvider(request: UpdateIdentityProviderRequest): UpdateIdentityProviderResponse {
  var runtime = new $RuntimeOptions{};
  return updateIdentityProviderWithOptions(request, runtime);
}

model UpdateInstanceRequest {
  clientToken?: string(name='ClientToken', description='The idempotency token that is used to ensure the idempotence of the request.', example='client-token-xxx'),
  instanceId?: string(name='InstanceId', description='**The ID of the AgentTeams instance.**

This parameter is required.', example='agentteams-abc123'),
  instanceName?: string(name='InstanceName', description='**The new instance name. This parameter cannot be empty.**', example='New instance name.'),
  networkType?: string(name='NetworkType', description='The network type.', example='PrivateNet'),
  zones?: [ 
    {
      vSwitchId?: string(name='VSwitchId', description='The vSwitch ID.', example='vsw-bp1xxxx'),
      zoneId?: string(name='ZoneId', description='The zone ID.', example='cn-hangzhou-h'),
    }
  ](name='Zones', description='The list of zones and vSwitches.', example='[...]'),
}

model UpdateInstanceShrinkRequest {
  clientToken?: string(name='ClientToken', description='The idempotency token that is used to ensure the idempotence of the request.', example='client-token-xxx'),
  instanceId?: string(name='InstanceId', description='**The ID of the AgentTeams instance.**

This parameter is required.', example='agentteams-abc123'),
  instanceName?: string(name='InstanceName', description='**The new instance name. This parameter cannot be empty.**', example='New instance name.'),
  networkType?: string(name='NetworkType', description='The network type.', example='PrivateNet'),
  zonesShrink?: string(name='Zones', description='The list of zones and vSwitches.', example='[...]'),
}

model UpdateInstanceResponseBody = {
  code?: string(name='Code', description='The status code in the successful response.', example='SUCCESS'),
  data?: {
    instanceId?: string(name='InstanceId', description='The ID of the AgentTeams instance.', example='agentteams-abc123'),
    instanceName?: string(name='InstanceName', description='The new instance name.', example='New instance name.'),
  }(name='Data', description='The details of the updated instance.', example='{}'),
  httpStatusCode?: int32(name='HttpStatusCode', description='**The HTTP status code.**', example='200'),
  message?: string(name='Message', description='**The response message.**', example='success'),
  requestId?: string(name='RequestId', description='**The request ID.**', example='request-xxx'),
  success?: boolean(name='Success', description='Indicates whether the operation is successful.', example='true'),
}

model UpdateInstanceResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateInstanceResponseBody(name='body'),
}

/**
 * @summary Changes the name of a specified AgentTeams instance. This operation supports GET and POST methods. You can only modify the instance name through this operation. You cannot change the namespace through this operation.
 *
 * @description Changes the name of a specified AgentTeams instance. This operation supports GET and POST methods. You can only modify the instance name through this operation. You cannot change the namespace through this operation.
 *
 * @param tmpReq UpdateInstanceRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateInstanceResponse
 */
@context("updateInstanceWithContext")
async function updateInstanceWithOptions(tmpReq: UpdateInstanceRequest, runtime: $RuntimeOptions): UpdateInstanceResponse {
  tmpReq.validate();
  var request = new UpdateInstanceShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.zones)) {
    request.zonesShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.zones, 'Zones', 'json');
  }
  var query = {};
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.instanceName)) {
    query['InstanceName'] = request.instanceName;
  }
  if (!$isNull(request.networkType)) {
    query['NetworkType'] = request.networkType;
  }
  if (!$isNull(request.zonesShrink)) {
    query['Zones'] = request.zonesShrink;
  }
  var body : map[string]any = {};
  if (!$isNull(request.clientToken)) {
    body['ClientToken'] = request.clientToken;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpdateInstance',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Changes the name of a specified AgentTeams instance. This operation supports GET and POST methods. You can only modify the instance name through this operation. You cannot change the namespace through this operation.
 *
 * @description Changes the name of a specified AgentTeams instance. This operation supports GET and POST methods. You can only modify the instance name through this operation. You cannot change the namespace through this operation.
 *
 * @param request UpdateInstanceRequest
 * @return UpdateInstanceResponse
 */
async function updateInstance(request: UpdateInstanceRequest): UpdateInstanceResponse {
  var runtime = new $RuntimeOptions{};
  return updateInstanceWithOptions(request, runtime);
}

model UpdateInstanceAsyncTaskRequest {
  instanceId?: string(name='InstanceId', description='The AgentTeams instance ID.

This parameter is required.', example='at-xxx'),
  isResume?: boolean(name='IsResume', description='Specifies whether to resume the task.', example='true'),
  taskCode?: string(name='TaskCode', description='The asynchronous task code. Only `agentteams:pay-order:create` is supported.

This parameter is required.', example='agentteams:pay-order:create'),
  taskId?: string(name='TaskId', description='The asynchronous task ID.

This parameter is required.', example='task-xxx'),
}

model UpdateInstanceAsyncTaskResponseBody = {
  code?: string(name='Code', description='The status code in the success response.', example='SUCCESS'),
  data?: {
    createdAt?: string(name='CreatedAt', description='The time when the task was created.', example='2026-06-01T12:00:00Z'),
    currentStep?: string(name='CurrentStep', description='The current step.', example='create-cms-workspace'),
    modifiedAt?: string(name='ModifiedAt', description='The time when the task was last modified.', example='2026-06-01T12:05:00Z'),
    taskCode?: string(name='TaskCode', description='The asynchronous task code. Only \\\\"agentteams:pay-order:create\\\\" is supported. Valid values:
 * agentteams:pay-order:create: create instance task.', example='agentteams:pay-order:create'),
    taskId?: string(name='TaskId', description='The asynchronous task ID.', example='task-xxx'),
    taskStatus?: string(name='TaskStatus', description='The asynchronous task status. Valid values:
 * PENDING: pending.
 * RUNNING: running.
 * PAUSED: paused.
 * COMPLETED: completed.
 * FAILED: failed.', example='RUNNING'),
    waitingForUserAction?: boolean(name='WaitingForUserAction', description='Indicates whether the task is waiting for user action.', example='false'),
  }(name='Data', description='The details of the asynchronous task.', example='{}'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='**message**', example='success'),
  requestId?: string(name='RequestId', description='**requestId**', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model UpdateInstanceAsyncTaskResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateInstanceAsyncTaskResponseBody(name='body'),
}

/**
 * @summary Restarts a paused asynchronous task for creating an instance.
 *
 * @description Restarts a paused asynchronous task for creating an instance.
 * - This operation restarts a create-instance task that is in the paused state.
 * - Only tasks of the agentteams:pay-order:create type are supported.
 * - Ensure that the InstanceId, TaskCode, and TaskId parameters are accurate. Otherwise, the request may fail.
 * - If the task is not in the paused state (PAUSED), you cannot call this operation to update the task.
 *
 * @param request UpdateInstanceAsyncTaskRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateInstanceAsyncTaskResponse
 */
@context("updateInstanceAsyncTaskWithContext")
async function updateInstanceAsyncTaskWithOptions(request: UpdateInstanceAsyncTaskRequest, runtime: $RuntimeOptions): UpdateInstanceAsyncTaskResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.isResume)) {
    query['IsResume'] = request.isResume;
  }
  if (!$isNull(request.taskCode)) {
    query['TaskCode'] = request.taskCode;
  }
  if (!$isNull(request.taskId)) {
    query['TaskId'] = request.taskId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpdateInstanceAsyncTask',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Restarts a paused asynchronous task for creating an instance.
 *
 * @description Restarts a paused asynchronous task for creating an instance.
 * - This operation restarts a create-instance task that is in the paused state.
 * - Only tasks of the agentteams:pay-order:create type are supported.
 * - Ensure that the InstanceId, TaskCode, and TaskId parameters are accurate. Otherwise, the request may fail.
 * - If the task is not in the paused state (PAUSED), you cannot call this operation to update the task.
 *
 * @param request UpdateInstanceAsyncTaskRequest
 * @return UpdateInstanceAsyncTaskResponse
 */
async function updateInstanceAsyncTask(request: UpdateInstanceAsyncTaskRequest): UpdateInstanceAsyncTaskResponse {
  var runtime = new $RuntimeOptions{};
  return updateInstanceAsyncTaskWithOptions(request, runtime);
}

model UpdateMcpRequest {
  addresses?: [ string ](name='Addresses', description='The address list of the MCP server.

This parameter is required.', example='["https://example.com/mcp"]'),
  authConfig?: string(name='AuthConfig', description='The authentication information.', example='{"type":"bearer","token":"xxx"}'),
  authEnabled?: boolean(name='AuthEnabled', description='Specifies whether to enable authentication.', example='false'),
  clientToken?: string(name='ClientToken', description='The client idempotency token that ensures the idempotence of the request.', example='client-token-xxx'),
  createType?: string(name='CreateType', description='The creation type.', example='DIRECT_PROXY'),
  description?: string(name='Description', description='The description of the MCP server.', example='This is an MCP server.'),
  id?: string(name='Id', description='Optional. Fuzzy matches by instance name.

This parameter is required.', example='AgentTeams'),
  instanceId?: string(name='InstanceId', description='The AgentTeams instance ID.

This parameter is required.', example='ins-xxx'),
  swaggerConfig?: string(name='SwaggerConfig', description='Required if Type is set to HTTP_TO_MCP.', example='{"url":"https://example.com/swagger.json"}'),
}

model UpdateMcpShrinkRequest {
  addressesShrink?: string(name='Addresses', description='The address list of the MCP server.

This parameter is required.', example='["https://example.com/mcp"]'),
  authConfig?: string(name='AuthConfig', description='The authentication information.', example='{"type":"bearer","token":"xxx"}'),
  authEnabled?: boolean(name='AuthEnabled', description='Specifies whether to enable authentication.', example='false'),
  clientToken?: string(name='ClientToken', description='The client idempotency token that ensures the idempotence of the request.', example='client-token-xxx'),
  createType?: string(name='CreateType', description='The creation type.', example='DIRECT_PROXY'),
  description?: string(name='Description', description='The description of the MCP server.', example='This is an MCP server.'),
  id?: string(name='Id', description='Optional. Fuzzy matches by instance name.

This parameter is required.', example='AgentTeams'),
  instanceId?: string(name='InstanceId', description='The AgentTeams instance ID.

This parameter is required.', example='ins-xxx'),
  swaggerConfig?: string(name='SwaggerConfig', description='Required if Type is set to HTTP_TO_MCP.', example='{"url":"https://example.com/swagger.json"}'),
}

model UpdateMcpResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='Id of the request', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model UpdateMcpResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateMcpResponseBody(name='body'),
}

/**
 * @summary Updates the configuration of a specified MCP server, including the address list, authentication information, and description.
 *
 * @description Updates the configuration of a specified MCP server, including the address list, authentication information, and description.
 *
 * @param tmpReq UpdateMcpRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateMcpResponse
 */
@context("updateMcpWithContext")
async function updateMcpWithOptions(tmpReq: UpdateMcpRequest, runtime: $RuntimeOptions): UpdateMcpResponse {
  tmpReq.validate();
  var request = new UpdateMcpShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.addresses)) {
    request.addressesShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.addresses, 'Addresses', 'json');
  }
  var body : map[string]any = {};
  if (!$isNull(request.addressesShrink)) {
    body['Addresses'] = request.addressesShrink;
  }
  if (!$isNull(request.authConfig)) {
    body['AuthConfig'] = request.authConfig;
  }
  if (!$isNull(request.authEnabled)) {
    body['AuthEnabled'] = request.authEnabled;
  }
  if (!$isNull(request.clientToken)) {
    body['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.createType)) {
    body['CreateType'] = request.createType;
  }
  if (!$isNull(request.description)) {
    body['Description'] = request.description;
  }
  if (!$isNull(request.id)) {
    body['Id'] = request.id;
  }
  if (!$isNull(request.instanceId)) {
    body['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.swaggerConfig)) {
    body['SwaggerConfig'] = request.swaggerConfig;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpdateMcp',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Updates the configuration of a specified MCP server, including the address list, authentication information, and description.
 *
 * @description Updates the configuration of a specified MCP server, including the address list, authentication information, and description.
 *
 * @param request UpdateMcpRequest
 * @return UpdateMcpResponse
 */
async function updateMcp(request: UpdateMcpRequest): UpdateMcpResponse {
  var runtime = new $RuntimeOptions{};
  return updateMcpWithOptions(request, runtime);
}

model UpdateModelRequest {
  clientToken?: string(name='ClientToken', description='The idempotency token that is used to ensure the idempotence of the request.', example='ct-xxx'),
  description?: string(name='Description', description='The model description.', example='updated model'),
  id?: string(name='Id', description='The model ID.

This parameter is required.', example='m-xxx'),
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='i-xxx'),
}

model UpdateModelResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='Id of the request', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model UpdateModelResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateModelResponseBody(name='body'),
}

/**
 * @summary Updates the description and other information of an AI model in a specified AgentTeams instance.
 *
 * @description Updates the description and other information of an AI model in a specified AgentTeams instance.
 *
 * @param request UpdateModelRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateModelResponse
 */
@context("updateModelWithContext")
async function updateModelWithOptions(request: UpdateModelRequest, runtime: $RuntimeOptions): UpdateModelResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.clientToken)) {
    body['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.description)) {
    body['Description'] = request.description;
  }
  if (!$isNull(request.id)) {
    body['Id'] = request.id;
  }
  if (!$isNull(request.instanceId)) {
    body['InstanceId'] = request.instanceId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpdateModel',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Updates the description and other information of an AI model in a specified AgentTeams instance.
 *
 * @description Updates the description and other information of an AI model in a specified AgentTeams instance.
 *
 * @param request UpdateModelRequest
 * @return UpdateModelResponse
 */
async function updateModel(request: UpdateModelRequest): UpdateModelResponse {
  var runtime = new $RuntimeOptions{};
  return updateModelWithOptions(request, runtime);
}

model UpdateModelProviderRequest {
  address?: string(name='Address', description='The address of the model provider.

This parameter is required.', example='https://api.example.com'),
  apiKeys?: [ string ](name='ApiKeys', description='The list of API keys for the model provider, in JSON format.

This parameter is required.', example='["sk-xxx"]'),
  clientToken?: string(name='ClientToken', description='The idempotency token that is used to ensure the idempotence of the request.', example='ct-xxx'),
  description?: string(name='Description', description='The description of the model provider.', example='updated provider'),
  id?: string(name='Id', description='The model provider ID.

This parameter is required.', example='p-xxx'),
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='i-xxx'),
  protocols?: [ string ](name='Protocols', description='The list of protocols supported by the model provider, in JSON format.

This parameter is required.', example='["openai"]'),
}

model UpdateModelProviderShrinkRequest {
  address?: string(name='Address', description='The address of the model provider.

This parameter is required.', example='https://api.example.com'),
  apiKeysShrink?: string(name='ApiKeys', description='The list of API keys for the model provider, in JSON format.

This parameter is required.', example='["sk-xxx"]'),
  clientToken?: string(name='ClientToken', description='The idempotency token that is used to ensure the idempotence of the request.', example='ct-xxx'),
  description?: string(name='Description', description='The description of the model provider.', example='updated provider'),
  id?: string(name='Id', description='The model provider ID.

This parameter is required.', example='p-xxx'),
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='i-xxx'),
  protocolsShrink?: string(name='Protocols', description='The list of protocols supported by the model provider, in JSON format.

This parameter is required.', example='["openai"]'),
}

model UpdateModelProviderResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='Id of the request', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model UpdateModelProviderResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateModelProviderResponseBody(name='body'),
}

/**
 * @summary Updates the address, protocol list, API key, and other information of an AI model provider in a specified AgentTeams instance.
 *
 * @description Updates the address, protocol list, API key, and other information of an AI model provider in a specified AgentTeams instance.
 *
 * @param tmpReq UpdateModelProviderRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateModelProviderResponse
 */
@context("updateModelProviderWithContext")
async function updateModelProviderWithOptions(tmpReq: UpdateModelProviderRequest, runtime: $RuntimeOptions): UpdateModelProviderResponse {
  tmpReq.validate();
  var request = new UpdateModelProviderShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.apiKeys)) {
    request.apiKeysShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.apiKeys, 'ApiKeys', 'json');
  }
  if (!$isNull(tmpReq.protocols)) {
    request.protocolsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.protocols, 'Protocols', 'json');
  }
  var body : map[string]any = {};
  if (!$isNull(request.address)) {
    body['Address'] = request.address;
  }
  if (!$isNull(request.apiKeysShrink)) {
    body['ApiKeys'] = request.apiKeysShrink;
  }
  if (!$isNull(request.clientToken)) {
    body['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.description)) {
    body['Description'] = request.description;
  }
  if (!$isNull(request.id)) {
    body['Id'] = request.id;
  }
  if (!$isNull(request.instanceId)) {
    body['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.protocolsShrink)) {
    body['Protocols'] = request.protocolsShrink;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpdateModelProvider',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Updates the address, protocol list, API key, and other information of an AI model provider in a specified AgentTeams instance.
 *
 * @description Updates the address, protocol list, API key, and other information of an AI model provider in a specified AgentTeams instance.
 *
 * @param request UpdateModelProviderRequest
 * @return UpdateModelProviderResponse
 */
async function updateModelProvider(request: UpdateModelProviderRequest): UpdateModelProviderResponse {
  var runtime = new $RuntimeOptions{};
  return updateModelProviderWithOptions(request, runtime);
}

model UpdateServiceEndpointRequest {
  certIdentifier?: string(name='CertIdentifier', description='The new SSL certificate identifier. If this parameter is not specified, the existing certificate is retained.', example='22584627-cn-hangzhou'),
  clientToken?: string(name='ClientToken', description='The idempotency token that is used to ensure the idempotence of the request.', example='client-token-xxx'),
  domain?: string(name='Domain', description='The new domain name. If this parameter is not specified, the existing domain name is retained.', example='api.example.com'),
  endpointId?: string(name='EndpointId', description='The globally unique endpoint ID.

This parameter is required.', example='mep-abc123'),
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='agentteams-demo'),
}

model UpdateServiceEndpointResponseBody = {
  code?: string(name='Code', description='The error code. This parameter is null if the request is successful.', example='null'),
  data?: {
    certIdentifier?: string(name='CertIdentifier', description='The SSL certificate identifier.', example='cert-xxx'),
    component?: string(name='Component', description='The component type. Valid values:
 * ELEMENT: element type.
 * MATRIX: matrix type.', example='MATRIX'),
    domain?: string(name='Domain', description='The domain name.', example='matrix.example.com'),
    domainType?: string(name='DomainType', description='The domain name type.', example='CUSTOM'),
    endpointId?: string(name='EndpointId', description='The globally unique endpoint ID.', example='mep-abc123'),
    endpointName?: string(name='EndpointName', description='The service name.', example='matrix-service'),
    instanceId?: string(name='InstanceId', description='The AgentTeams instance ID, which is used to verify endpoint ownership and current user permissions.', example='agentteams-demo'),
    networkType?: string(name='NetworkType', description='The network type.', example='INTERNET'),
  }(name='Data', description='The updated endpoint information.', example='{}'),
  message?: string(name='Message', description='The error message. This parameter is null if the request is successful.', example='null'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-xxx'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model UpdateServiceEndpointResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateServiceEndpointResponseBody(name='body'),
}

/**
 * @summary Updates the domain name and SSL certificate information for a specified endpoint.
 *
 * @description Updates the domain name and SSL certificate information for a specified endpoint.
 * - This API operation supports updating endpoints of the ELEMENT or MATRIX type.
 * - If you attempt to update an endpoint of another type, a 400 error is returned.
 * - If the endpointId does not exist or does not belong to the current user instance, a 404 error is returned.
 * - When updating a domain name, the system creates or reuses a new HTTPS domain and binds it to the original endpoint route. The old domain is unbound but not deleted.
 * - If the domain or certIdentifier parameter is not specified, the existing settings are retained.
 * - Other fields such as component and gatewayType are not updated even if they are specified in the request.
 *
 * @param request UpdateServiceEndpointRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateServiceEndpointResponse
 */
@context("updateServiceEndpointWithContext")
async function updateServiceEndpointWithOptions(request: UpdateServiceEndpointRequest, runtime: $RuntimeOptions): UpdateServiceEndpointResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.certIdentifier)) {
    query['CertIdentifier'] = request.certIdentifier;
  }
  if (!$isNull(request.domain)) {
    query['Domain'] = request.domain;
  }
  if (!$isNull(request.endpointId)) {
    query['EndpointId'] = request.endpointId;
  }
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  var body : map[string]any = {};
  if (!$isNull(request.clientToken)) {
    body['ClientToken'] = request.clientToken;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpdateServiceEndpoint',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Updates the domain name and SSL certificate information for a specified endpoint.
 *
 * @description Updates the domain name and SSL certificate information for a specified endpoint.
 * - This API operation supports updating endpoints of the ELEMENT or MATRIX type.
 * - If you attempt to update an endpoint of another type, a 400 error is returned.
 * - If the endpointId does not exist or does not belong to the current user instance, a 404 error is returned.
 * - When updating a domain name, the system creates or reuses a new HTTPS domain and binds it to the original endpoint route. The old domain is unbound but not deleted.
 * - If the domain or certIdentifier parameter is not specified, the existing settings are retained.
 * - Other fields such as component and gatewayType are not updated even if they are specified in the request.
 *
 * @param request UpdateServiceEndpointRequest
 * @return UpdateServiceEndpointResponse
 */
async function updateServiceEndpoint(request: UpdateServiceEndpointRequest): UpdateServiceEndpointResponse {
  var runtime = new $RuntimeOptions{};
  return updateServiceEndpointWithOptions(request, runtime);
}

model UpdateTeamRequest {
  clientToken?: string(name='ClientToken', description='The idempotency token that is used to ensure the idempotence of the request.', example='client-token-xxx'),
  description?: string(name='Description', description='The team description.', example='Updated team description.'),
  instanceId?: string(name='InstanceId', description='The instance ID.', example='agentteams-test-001'),
  name?: string(name='Name', description='The name.', example='worker-demo'),
  teamMembers?: [ 
    {
      name?: string(name='Name', description='The member name. The name must be an existing user under the instance.', example='member-user'),
    }
  ](name='TeamMembers', description='The team member list. If specified, this overwrites the existing members entirely.', example='[...]'),
}

model UpdateTeamShrinkRequest {
  clientToken?: string(name='ClientToken', description='The idempotency token that is used to ensure the idempotence of the request.', example='client-token-xxx'),
  description?: string(name='Description', description='The team description.', example='Updated team description.'),
  instanceId?: string(name='InstanceId', description='The instance ID.', example='agentteams-test-001'),
  name?: string(name='Name', description='The name.', example='worker-demo'),
  teamMembersShrink?: string(name='TeamMembers', description='The team member list. If specified, this overwrites the existing members entirely.', example='[...]'),
}

model UpdateTeamResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  data?: {
    adminName?: string(name='AdminName', description='The team administrator name.', example='admin-user'),
    createdAt?: string(name='CreatedAt', description='The time when the team was created.', example='2026-07-01T12:00:00'),
    description?: string(name='Description', description='The team description.', example='Updated team description.'),
    instanceId?: string(name='InstanceId', description='The instance ID.', example='agentteams-test-001'),
    leaderName?: string(name='LeaderName', description='The team leader name.', example='leader-user'),
    name?: string(name='Name', description='The team name.', example='my-team'),
    teamMembers?: [ 
      {
        name?: string(name='Name', description='The member name.', example='member-user'),
      }
    ](name='TeamMembers', description='The team member list.', example='[...]'),
    updatedAt?: string(name='UpdatedAt', description='The time when the team was last updated.', example='2026-07-10T12:00:00'),
    workerNames?: [ string ](name='WorkerNames', description='The list of Worker names associated with the team.', example='["worker-1"]'),
  }(name='Data', description='The response data.', example='{}'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-update-team-001'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model UpdateTeamResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateTeamResponseBody(name='body'),
}

/**
 * @summary Updates the description and member list of a team under a specified instance, and returns the latest team information after the update.
 *
 * @description Updates the description and member list of a team under a specified instance, and returns the latest team information after the update.
 *
 * @param tmpReq UpdateTeamRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateTeamResponse
 */
@context("updateTeamWithContext")
async function updateTeamWithOptions(tmpReq: UpdateTeamRequest, runtime: $RuntimeOptions): UpdateTeamResponse {
  tmpReq.validate();
  var request = new UpdateTeamShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.teamMembers)) {
    request.teamMembersShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.teamMembers, 'TeamMembers', 'json');
  }
  var query = {};
  if (!$isNull(request.description)) {
    query['Description'] = request.description;
  }
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.name)) {
    query['Name'] = request.name;
  }
  if (!$isNull(request.teamMembersShrink)) {
    query['TeamMembers'] = request.teamMembersShrink;
  }
  var body : map[string]any = {};
  if (!$isNull(request.clientToken)) {
    body['ClientToken'] = request.clientToken;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpdateTeam',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Updates the description and member list of a team under a specified instance, and returns the latest team information after the update.
 *
 * @description Updates the description and member list of a team under a specified instance, and returns the latest team information after the update.
 *
 * @param request UpdateTeamRequest
 * @return UpdateTeamResponse
 */
async function updateTeam(request: UpdateTeamRequest): UpdateTeamResponse {
  var runtime = new $RuntimeOptions{};
  return updateTeamWithOptions(request, runtime);
}

model UpdateUserRequest {
  authMethod?: string(name='AuthMethod', description='The authentication method.', example='password'),
  clientToken?: string(name='ClientToken', description='The idempotency token that is used to ensure the idempotence of the request.', example='client-token-xxx'),
  displayName?: string(name='DisplayName', description='The display name.', example='Demo User Updated'),
  email?: string(name='Email', description='The email address.', example='demo.updated@example.com'),
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='agentteams-test-001'),
  name?: string(name='Name', description='The name.

This parameter is required.', example='worker-demo'),
  note?: string(name='Note', description='The remarks.', example='Test user updated.'),
}

model UpdateUserResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  data?: {
    authMethod?: string(name='AuthMethod', description='The authentication method.', example='password'),
    createdAt?: string(name='CreatedAt', description='The time when the user was created.', example='2026-06-05T10:00:00'),
    displayName?: string(name='DisplayName', description='The display name.', example='Demo User Updated'),
    email?: string(name='Email', description='The email address.', example='demo.updated@example.com'),
    instanceId?: string(name='InstanceId', description='The instance ID.', example='agentteams-test-001'),
    name?: string(name='Name', description='The username.', example='demo-user'),
    note?: string(name='Note', description='The remarks.', example='Test user updated.'),
    status?: string(name='Status', description='The user status.', example='Active'),
  }(name='Data', description='The returned data.', example='{}'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='req-update-user-001'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model UpdateUserResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateUserResponseBody(name='body'),
}

/**
 * @summary Updates the information of a specified user under a specified instance, including the display name, email address, authentication method, and remarks.
 *
 * @description Updates the information of a specified user under a specified instance, including the display name, email address, authentication method, and remarks.
 *
 * @param request UpdateUserRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateUserResponse
 */
@context("updateUserWithContext")
async function updateUserWithOptions(request: UpdateUserRequest, runtime: $RuntimeOptions): UpdateUserResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.authMethod)) {
    query['AuthMethod'] = request.authMethod;
  }
  if (!$isNull(request.displayName)) {
    query['DisplayName'] = request.displayName;
  }
  if (!$isNull(request.email)) {
    query['Email'] = request.email;
  }
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.name)) {
    query['Name'] = request.name;
  }
  if (!$isNull(request.note)) {
    query['Note'] = request.note;
  }
  var body : map[string]any = {};
  if (!$isNull(request.clientToken)) {
    body['ClientToken'] = request.clientToken;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpdateUser',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Updates the information of a specified user under a specified instance, including the display name, email address, authentication method, and remarks.
 *
 * @description Updates the information of a specified user under a specified instance, including the display name, email address, authentication method, and remarks.
 *
 * @param request UpdateUserRequest
 * @return UpdateUserResponse
 */
async function updateUser(request: UpdateUserRequest): UpdateUserResponse {
  var runtime = new $RuntimeOptions{};
  return updateUserWithOptions(request, runtime);
}

model UpdateWorkerRequest {
  agents?: string(name='Agents', description='The Agents configuration.', example='agents-demo'),
  channels?: [ 
    {
      config?: {
        cardTemplateId?: string(name='CardTemplateId', description='The response data.', example='tpl-demo'),
        clientId?: string(name='ClientId', description='The response data.', example='cli-demo'),
        extension?: string(name='Extension', description='The response data.', example='extension-demo'),
        messageType?: string(name='MessageType', description='The response data.', example='markdown'),
        robotCode?: string(name='RobotCode', description='The response data.', example='robot-demo'),
        showThinking?: boolean(name='ShowThinking', description='The response data.', example='true'),
        showToolCalls?: boolean(name='ShowToolCalls', description='The response data.', example='false'),
        streamingEnabled?: boolean(name='StreamingEnabled', description='The response data.', example='true'),
      }(name='Config', description='The channel configuration.', example='{}'),
      enabled?: boolean(name='Enabled', description='Specifies whether the channel is enabled.', example='true'),
      secrets?: {
        clientSecret?: string(name='ClientSecret', description='The response data.', example='secret-demo'),
      }(name='Secrets', description='The channel secrets.', example='{}'),
      type?: string(name='Type', description='The channel type.', example='dingtalk'),
    }
  ](name='Channels', description='The list of channel configurations.', example='[...]'),
  clientToken?: string(name='ClientToken', description='The idempotency token that ensures the idempotence of the request.', example='client-token-demo'),
  credentials?: [ 
    {
      name?: string(name='Name', description='The credential name.', example='cred-demo'),
    }
  ](name='Credentials', description='The list of credential bindings.', example='[...]'),
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='inst-demo'),
  limitConfig?: {
    limitType?: string(name='LimitType', description='The response data.', example='token'),
    periodType?: string(name='PeriodType', description='The response data.', example='day'),
    usageLimit?: long(name='UsageLimit', description='The usage limit.', example='1000000'),
  }(name='LimitConfig', description='The quota configuration.', example='{}'),
  mcpServers?: [ 
    {
      name?: string(name='Name', description='The MCP server name.', example='mcp-server-demo'),
    }
  ](name='McpServers', description='The list of MCP servers.', example='[...]'),
  model?: {
    modelName?: string(name='ModelName', description='The response data.', example='qwen-max'),
    modelProvider?: string(name='ModelProvider', description='The response data.', example='dashscope'),
  }(name='Model', description='The model configuration.', example='{}'),
  name?: string(name='Name', description='The name.

This parameter is required.', example='worker-demo'),
  skills?: [ 
    {
      label?: string(name='Label', description='The skill label.', example='Code Review.'),
      name?: string(name='Name', description='The skill name.', example='code-review'),
      version?: string(name='Version', description='The skill version.', example='1.0.0'),
    }
  ](name='Skills', description='The list of skills.', example='[...]'),
  soul?: string(name='Soul', description='The Soul configuration.', example='soul-demo'),
  template?: {
    label?: string(name='Label', description='The response data.', example='Label.'),
    name?: string(name='Name', description='The response data.', example='worker-demo'),
    version?: string(name='Version', description='The response data.', example='1.0.0'),
  }(name='Template', description='The template configuration.', example='{}'),
  versionCode?: string(name='VersionCode', description='The Worker version number.', example='v1.0.0'),
}

model UpdateWorkerShrinkRequest {
  agents?: string(name='Agents', description='The Agents configuration.', example='agents-demo'),
  channelsShrink?: string(name='Channels', description='The list of channel configurations.', example='[...]'),
  clientToken?: string(name='ClientToken', description='The idempotency token that ensures the idempotence of the request.', example='client-token-demo'),
  credentialsShrink?: string(name='Credentials', description='The list of credential bindings.', example='[...]'),
  instanceId?: string(name='InstanceId', description='The instance ID.

This parameter is required.', example='inst-demo'),
  limitConfigShrink?: string(name='LimitConfig', description='The quota configuration.', example='{}'),
  mcpServersShrink?: string(name='McpServers', description='The list of MCP servers.', example='[...]'),
  modelShrink?: string(name='Model', description='The model configuration.', example='{}'),
  name?: string(name='Name', description='The name.

This parameter is required.', example='worker-demo'),
  skillsShrink?: string(name='Skills', description='The list of skills.', example='[...]'),
  soul?: string(name='Soul', description='The Soul configuration.', example='soul-demo'),
  templateShrink?: string(name='Template', description='The template configuration.', example='{}'),
  versionCode?: string(name='VersionCode', description='The Worker version number.', example='v1.0.0'),
}

model UpdateWorkerResponseBody = {
  code?: string(name='Code', description='The response code.', example='SUCCESS'),
  data?: {
    agentType?: string(name='AgentType', description='The Agent runtime type.', example='Qwenpaw'),
    agents?: string(name='Agents', description='The Agents configuration.', example='agents-demo'),
    channels?: [ 
      {
        config?: {
          cardTemplateId?: string(name='CardTemplateId', description='The card template ID.', example='tpl-demo'),
          clientId?: string(name='ClientId', description='The client ID.', example='cli-demo'),
          extension?: string(name='Extension', description='The extension configuration.', example='extension-demo'),
          messageType?: string(name='MessageType', description='The message type.', example='markdown'),
          robotCode?: string(name='RobotCode', description='The robot code.', example='robot-demo'),
          showThinking?: boolean(name='ShowThinking', description='Indicates whether the thinking process is displayed.', example='true'),
          showToolCalls?: boolean(name='ShowToolCalls', description='Indicates whether tool invocations are displayed.', example='false'),
          streamingEnabled?: boolean(name='StreamingEnabled', description='Indicates whether streaming output is enabled.', example='true'),
        }(name='Config', description='The channel configuration.', example='{}'),
        enabled?: boolean(name='Enabled', description='Indicates whether the channel is enabled.', example='true'),
        secretStatus?: {
          clientSecret?: boolean(name='ClientSecret', description='Indicates whether the client secret is set.', example='true'),
        }(name='SecretStatus', description='The secret status.', example='{}'),
        type?: string(name='Type', description='The channel type.', example='dingtalk'),
      }
    ](name='Channels', description='The list of channels.', example='[...]'),
    credentials?: [ 
      {
        name?: string(name='Name', description='The credential name.', example='cred-demo'),
      }
    ](name='Credentials', description='The list of credential bindings.', example='[...]'),
    deployType?: string(name='DeployType', description='The deployment type.', example='Managed'),
    groups?: [ 
      {
        name?: string(name='Name', description='The group name.', example='team-demo'),
        role?: string(name='Role', description='The group role.', example='member'),
        type?: string(name='Type', description='The group type.', example='team'),
      }
    ](name='Groups', description='The list of team groups.', example='[...]'),
    instanceId?: string(name='InstanceId', description='The instance ID.', example='inst-demo'),
    limitConfig?: {
      limitType?: string(name='LimitType', description='The limit type.', example='token'),
      periodType?: string(name='PeriodType', description='The period type.', example='day'),
      usageLimit?: long(name='UsageLimit', description='The usage limit.', example='1000000'),
    }(name='LimitConfig', description='The quota configuration.', example='{}'),
    mcpServers?: [ 
      {
        name?: string(name='Name', description='The MCP server name.', example='mcp-server-demo'),
        transport?: string(name='Transport', description='The MCP transport protocol.', example='sse'),
        url?: string(name='Url', description='The MCP server URL.', example='https://mcp-demo/mcp'),
      }
    ](name='McpServers', description='The list of MCP servers.', example='[...]'),
    model?: {
      modelName?: string(name='ModelName', description='The model name.', example='qwen-max'),
      modelProvider?: string(name='ModelProvider', description='The model provider.', example='dashscope'),
    }(name='Model', description='The model configuration.', example='{}'),
    name?: string(name='Name', description='The Worker name.', example='worker-demo'),
    regionId?: string(name='RegionId', description='The region ID.', example='cn-hangzhou'),
    skills?: [ 
      {
        label?: string(name='Label', description='The skill label.', example='Code Review.'),
        name?: string(name='Name', description='The skill name.', example='code-review'),
        version?: string(name='Version', description='The skill version.', example='1.0.0'),
      }
    ](name='Skills', description='The list of skills.', example='[...]'),
    soul?: string(name='Soul', description='The Soul configuration.', example='soul-demo'),
    startTime?: string(name='StartTime', description='The start time.', example='2026-07-13T10:00:00Z'),
    status?: string(name='Status', description='The Worker status.', example='Running'),
    template?: {
      label?: string(name='Label', description='The template label.', example='Default template.'),
      name?: string(name='Name', description='The template name.', example='default-template'),
      version?: string(name='Version', description='The template version.', example='1.0.0'),
    }(name='Template', description='The template configuration.', example='{}'),
    versionCode?: string(name='VersionCode', description='The Worker version number.', example='v1.0.0'),
  }(name='Data', description='The response data.', example='{}'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  message?: string(name='Message', description='The response message.', example='success'),
  requestId?: string(name='RequestId', description='The request ID.', example='473469C7-XX-XX-XX'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model UpdateWorkerResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateWorkerResponseBody(name='body'),
}

/**
 * @summary Updates the configuration of a specified Worker, including model, skills, template, MCP servers, channels, and quotas.
 *
 * @description Updates the configuration of a specified Worker, including model, skills, template, MCP servers, channels, and quotas.
 *
 * @param tmpReq UpdateWorkerRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateWorkerResponse
 */
@context("updateWorkerWithContext")
async function updateWorkerWithOptions(tmpReq: UpdateWorkerRequest, runtime: $RuntimeOptions): UpdateWorkerResponse {
  tmpReq.validate();
  var request = new UpdateWorkerShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.channels)) {
    request.channelsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.channels, 'Channels', 'json');
  }
  if (!$isNull(tmpReq.credentials)) {
    request.credentialsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.credentials, 'Credentials', 'json');
  }
  if (!$isNull(tmpReq.limitConfig)) {
    request.limitConfigShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.limitConfig, 'LimitConfig', 'json');
  }
  if (!$isNull(tmpReq.mcpServers)) {
    request.mcpServersShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.mcpServers, 'McpServers', 'json');
  }
  if (!$isNull(tmpReq.model)) {
    request.modelShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.model, 'Model', 'json');
  }
  if (!$isNull(tmpReq.skills)) {
    request.skillsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.skills, 'Skills', 'json');
  }
  if (!$isNull(tmpReq.template)) {
    request.templateShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.template, 'Template', 'json');
  }
  var query = {};
  if (!$isNull(request.agents)) {
    query['Agents'] = request.agents;
  }
  if (!$isNull(request.channelsShrink)) {
    query['Channels'] = request.channelsShrink;
  }
  if (!$isNull(request.credentialsShrink)) {
    query['Credentials'] = request.credentialsShrink;
  }
  if (!$isNull(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.limitConfigShrink)) {
    query['LimitConfig'] = request.limitConfigShrink;
  }
  if (!$isNull(request.mcpServersShrink)) {
    query['McpServers'] = request.mcpServersShrink;
  }
  if (!$isNull(request.modelShrink)) {
    query['Model'] = request.modelShrink;
  }
  if (!$isNull(request.name)) {
    query['Name'] = request.name;
  }
  if (!$isNull(request.skillsShrink)) {
    query['Skills'] = request.skillsShrink;
  }
  if (!$isNull(request.soul)) {
    query['Soul'] = request.soul;
  }
  if (!$isNull(request.templateShrink)) {
    query['Template'] = request.templateShrink;
  }
  if (!$isNull(request.versionCode)) {
    query['VersionCode'] = request.versionCode;
  }
  var body : map[string]any = {};
  if (!$isNull(request.clientToken)) {
    body['ClientToken'] = request.clientToken;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpdateWorker',
    version = '2026-06-05',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Updates the configuration of a specified Worker, including model, skills, template, MCP servers, channels, and quotas.
 *
 * @description Updates the configuration of a specified Worker, including model, skills, template, MCP servers, channels, and quotas.
 *
 * @param request UpdateWorkerRequest
 * @return UpdateWorkerResponse
 */
async function updateWorker(request: UpdateWorkerRequest): UpdateWorkerResponse {
  var runtime = new $RuntimeOptions{};
  return updateWorkerWithOptions(request, runtime);
}

