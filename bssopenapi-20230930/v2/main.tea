/**
 *
 */
import OpenApi;
import OpenApi.OpenApiUtil;

extends OpenApi;


init(config: OpenApiUtil.Config){
  super(config);
  @endpointRule = 'regional';
  @endpointMap = {
    'cn-hangzhou' = 'business.aliyuncs.com',
    'cn-shanghai' = 'business.aliyuncs.com',
    'ap-southeast-1' = 'business.ap-southeast-1.aliyuncs.com',
    'ap-northeast-1' = 'business.ap-southeast-1.aliyuncs.com',
    'ap-northeast-2' = 'business.ap-southeast-1.aliyuncs.com',
    'ap-northeast-2-pop' = 'business.ap-southeast-1.aliyuncs.com',
    'ap-south-1' = 'business.ap-southeast-1.aliyuncs.com',
    'ap-southeast-2' = 'business.ap-southeast-1.aliyuncs.com',
    'ap-southeast-3' = 'business.ap-southeast-1.aliyuncs.com',
    'ap-southeast-5' = 'business.ap-southeast-1.aliyuncs.com',
    'cn-beijing' = 'business.aliyuncs.com',
    'cn-beijing-finance-1' = 'business.aliyuncs.com',
    'cn-beijing-finance-pop' = 'business.aliyuncs.com',
    'cn-beijing-gov-1' = 'business.aliyuncs.com',
    'cn-beijing-nu16-b01' = 'business.aliyuncs.com',
    'cn-chengdu' = 'business.aliyuncs.com',
    'cn-edge-1' = 'business.aliyuncs.com',
    'cn-fujian' = 'business.aliyuncs.com',
    'cn-haidian-cm12-c01' = 'business.aliyuncs.com',
    'cn-hangzhou-bj-b01' = 'business.aliyuncs.com',
    'cn-hangzhou-finance' = 'business.aliyuncs.com',
    'cn-hangzhou-internal-prod-1' = 'business.aliyuncs.com',
    'cn-hangzhou-internal-test-1' = 'business.aliyuncs.com',
    'cn-hangzhou-internal-test-2' = 'business.aliyuncs.com',
    'cn-hangzhou-internal-test-3' = 'business.aliyuncs.com',
    'cn-hangzhou-test-306' = 'business.aliyuncs.com',
    'cn-hongkong' = 'business.aliyuncs.com',
    'cn-hongkong-finance-pop' = 'business.aliyuncs.com',
    'cn-huhehaote' = 'business.aliyuncs.com',
    'cn-huhehaote-nebula-1' = 'business.aliyuncs.com',
    'cn-north-2-gov-1' = 'business.aliyuncs.com',
    'cn-qingdao' = 'business.aliyuncs.com',
    'cn-qingdao-nebula' = 'business.aliyuncs.com',
    'cn-shanghai-et15-b01' = 'business.aliyuncs.com',
    'cn-shanghai-et2-b01' = 'business.aliyuncs.com',
    'cn-shanghai-finance-1' = 'business.aliyuncs.com',
    'cn-shanghai-inner' = 'business.aliyuncs.com',
    'cn-shanghai-internal-test-1' = 'business.aliyuncs.com',
    'cn-shenzhen' = 'business.aliyuncs.com',
    'cn-shenzhen-finance-1' = 'business.aliyuncs.com',
    'cn-shenzhen-inner' = 'business.aliyuncs.com',
    'cn-shenzhen-st4-d01' = 'business.aliyuncs.com',
    'cn-shenzhen-su18-b01' = 'business.aliyuncs.com',
    'cn-wuhan' = 'business.aliyuncs.com',
    'cn-wulanchabu' = 'business.aliyuncs.com',
    'cn-yushanfang' = 'business.aliyuncs.com',
    'cn-zhangbei' = 'business.aliyuncs.com',
    'cn-zhangbei-na61-b01' = 'business.aliyuncs.com',
    'cn-zhangjiakou' = 'business.aliyuncs.com',
    'cn-zhangjiakou-na62-a01' = 'business.aliyuncs.com',
    'cn-zhengzhou-nebula-1' = 'business.aliyuncs.com',
    'eu-central-1' = 'business.ap-southeast-1.aliyuncs.com',
    'eu-west-1' = 'business.ap-southeast-1.aliyuncs.com',
    'eu-west-1-oxs' = 'business.ap-southeast-1.aliyuncs.com',
    'me-east-1' = 'business.ap-southeast-1.aliyuncs.com',
    'rus-west-1-pop' = 'business.ap-southeast-1.aliyuncs.com',
    'us-east-1' = 'business.ap-southeast-1.aliyuncs.com',
    'us-west-1' = 'business.ap-southeast-1.aliyuncs.com',
  };

  checkConfig(config);
  @endpoint = getEndpoint('bssopenapi', @regionId, @endpointRule, @network, @suffix, @endpointMap, @endpoint);
}

function getEndpoint(productId: string, regionId: string, endpointRule: string, network: string, suffix: string, endpointMap: map[string]string, endpoint: string) throws: string{
  if (!$isNull(endpoint)) {
    return endpoint;
  }
  
  if (!$isNull(endpointMap) && !$isNull(endpointMap[regionId])) {
    return endpointMap[regionId];
  }
  return OpenApiUtil.getEndpointRules(productId, regionId, endpointRule, network, suffix);
}

model DimItem {
  code?: string(name='Code'),
  name?: string(name='Name'),
  pageInfo?: {
    currentPage?: int32(name='CurrentPage'),
    pageSize?: int32(name='PageSize'),
    total?: int32(name='Total'),
  }(name='PageInfo'),
  values?: [
    ItemValues
  ](name='Values'),
}

model EcIdAccountId {
  accountIds?: [ long ](name='AccountIds'),
  ecId?: string(name='EcId'),
}

model FieldOption {
  functionCode?: string(name='FunctionCode'),
  isDefault?: boolean(name='IsDefault'),
  name?: string(name='Name'),
  value?: string(name='Value'),
}

model ItemValues {
}

model DataModuleMapListSpnTypeMapListValue = {
  filterModules?: [ 
    {
      moduleId?: long(name='ModuleId'),
      moduleCode?: string(name='ModuleCode'),
      moduleName?: string(name='ModuleName'),
    }
  ](name='FilterModules'),
  showModules?: [ 
    {
      moduleId?: long(name='ModuleId'),
      moduleCode?: string(name='ModuleCode'),
      moduleName?: string(name='ModuleName'),
    }
  ](name='ShowModules'),
}

model DataStepPriceMapValue = {
  rightClose?: boolean(name='RightClose'),
  min?: string(name='Min'),
  max?: string(name='Max'),
  currency?: string(name='Currency'),
  leftClose?: boolean(name='LeftClose'),
  stepPriceValue?: string(name='StepPriceValue'),
  priceValueType?: string(name='PriceValueType'),
  priceValue?: string(name='PriceValue'),
  deductCycleType?: string(name='DeductCycleType'),
}

model AddCouponDeductTagRequest {
  couponId?: string(name='CouponId'),
  ecIdAccountIds?: [ 
    {
      accountIds?: [ long ](name='AccountIds'),
      ecId?: string(name='EcId', description='This parameter is required.'),
    }
  ](name='EcIdAccountIds'),
  nbid?: string(name='Nbid'),
  tags?: [ 
    {
      key?: string(name='Key'),
      value?: string(name='Value'),
    }
  ](name='Tags'),
}

model AddCouponDeductTagShrinkRequest {
  couponId?: string(name='CouponId'),
  ecIdAccountIdsShrink?: string(name='EcIdAccountIds'),
  nbid?: string(name='Nbid'),
  tagsShrink?: string(name='Tags'),
}

model AddCouponDeductTagResponseBody = {
  data?: boolean(name='Data'),
  requestId?: string(name='RequestId'),
}

model AddCouponDeductTagResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: AddCouponDeductTagResponseBody(name='body'),
}

/**
 * @summary 添加优惠券抵扣标签
 *
 * @param tmpReq AddCouponDeductTagRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return AddCouponDeductTagResponse
 */
@context("addCouponDeductTagWithContext")
async function addCouponDeductTagWithOptions(tmpReq: AddCouponDeductTagRequest, runtime: $RuntimeOptions): AddCouponDeductTagResponse {
  tmpReq.validate();
  var request = new AddCouponDeductTagShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.ecIdAccountIds)) {
    request.ecIdAccountIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.ecIdAccountIds, 'EcIdAccountIds', 'json');
  }
  if (!$isNull(tmpReq.tags)) {
    request.tagsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.tags, 'Tags', 'json');
  }
  var query = {};
  if (!$isNull(request.couponId)) {
    query['CouponId'] = request.couponId;
  }
  if (!$isNull(request.ecIdAccountIdsShrink)) {
    query['EcIdAccountIds'] = request.ecIdAccountIdsShrink;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  if (!$isNull(request.tagsShrink)) {
    query['Tags'] = request.tagsShrink;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'AddCouponDeductTag',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 添加优惠券抵扣标签
 *
 * @param request AddCouponDeductTagRequest
 * @return AddCouponDeductTagResponse
 */
async function addCouponDeductTag(request: AddCouponDeductTagRequest): AddCouponDeductTagResponse {
  var runtime = new $RuntimeOptions{};
  return addCouponDeductTagWithOptions(request, runtime);
}

model AllocateCostCenterResourceRequest {
  fromCostCenterId?: long(name='FromCostCenterId', description='The ID of the source cost center. This parameter is required.

- 0 indicates that the cost center is unallocated.
- A value greater than 0 indicates an allocated cost center ID.', example='637180'),
  fromOwnerAccountId?: long(name='FromOwnerAccountId', description='The ID of the owner of the source cost center.', example='1529600453335198'),
  nbid?: string(name='Nbid', description='The primary sales channel ID. If this parameter is left empty, the sales channel ID of the current user is used by default.', example='2684201000001'),
  resourceInstanceList?: [ 
    {
      apportionCode?: string(name='ApportionCode', description='The attached resource type of the attached-resource instance. This parameter is required only for attached-resource product instances.
- Currently, eight commodities support attached resources. The commodity codes are oss, dcdn, snapshot, vod, cdn, live, and cbwp.
- You can call the QueryCostUnitResource operation to obtain all billing instances (including attached-resource instances with their attached resources) under a specific cost center (including the unallocated cost center) of a user.', example='qwer1-cn-beijing'),
      apportionName?: string(name='ApportionName', description='The attached resource name.', example='split-item-test1'),
      commodityCode?: string(name='CommodityCode', description='The commodity code of the billing instance. This parameter is required.', example='oss'),
      commodityName?: string(name='CommodityName', description='The commodity name of the resource.', example='RESOURCE_UDR'),
      instanceId?: string(name='InstanceId', description='The billing granularity ID. This parameter is required.', example='cn-hangzhou;standard'),
      pipCode?: string(name='PipCode', description='The product code, which is the same as the product code in User Center bills.', example='ecs'),
      relatedResources?: string(name='RelatedResources', description='The resources related to the resource instance.', example='related-resource'),
      resourceGroup?: string(name='ResourceGroup', description='The resource group.', example='xihe_mpp'),
      resourceId?: string(name='ResourceId', description='The resource ID.', example='cn-hangzhou;standard:app'),
      resourceNick?: string(name='ResourceNick', description='The custom nickname of the resource.', example='ecs-test-1'),
      resourceSource?: string(name='ResourceSource', description='The resource source. Valid values:
- AUTO_ALLOCATE: automatic allocation.
- MANUAL_ALLOCATE: manual allocation.', example='AUTO_ALLOCATE'),
      resourceStatus?: string(name='ResourceStatus', description='The resource status.', example='0'),
      resourceTag?: string(name='ResourceTag', description='The tag of the resource.', example='tag-test1'),
      resourceType?: string(name='ResourceType', description='The resource type.', example='SCU'),
      resourceUserId?: long(name='ResourceUserId', description='The ID of the owner of the billing instance. This parameter is required.', example='273394581313325532'),
      resourceUserName?: string(name='ResourceUserName', description='The resource ownership username.', example='test'),
    }
  ](name='ResourceInstanceList', description='The list of resource instances.

This parameter is required.'),
  toCostCenterId?: long(name='ToCostCenterId', description='The ID of the destination cost center. Valid values:

- -1: moves the allocated resource to the unallocated state.
- A value greater than 0: allocates the resource to the specified cost center.', example='638288'),
}

model AllocateCostCenterResourceShrinkRequest {
  fromCostCenterId?: long(name='FromCostCenterId', description='The ID of the source cost center. This parameter is required.

- 0 indicates that the cost center is unallocated.
- A value greater than 0 indicates an allocated cost center ID.', example='637180'),
  fromOwnerAccountId?: long(name='FromOwnerAccountId', description='The ID of the owner of the source cost center.', example='1529600453335198'),
  nbid?: string(name='Nbid', description='The primary sales channel ID. If this parameter is left empty, the sales channel ID of the current user is used by default.', example='2684201000001'),
  resourceInstanceListShrink?: string(name='ResourceInstanceList', description='The list of resource instances.

This parameter is required.'),
  toCostCenterId?: long(name='ToCostCenterId', description='The ID of the destination cost center. Valid values:

- -1: moves the allocated resource to the unallocated state.
- A value greater than 0: allocates the resource to the specified cost center.', example='638288'),
}

model AllocateCostCenterResourceResponseBody = {
  costCenterId?: long(name='CostCenterId', description='The cost center ID.', example='640403'),
  isSuccess?: boolean(name='IsSuccess', description='Indicates whether the operation was successful.', example='true'),
  metadata?: any(name='Metadata', description='The metadata of the response struct.', example='{}'),
  ownerAccountId?: long(name='OwnerAccountId', description='The owner of the cost center.', example='1529600453335198'),
  requestId?: string(name='RequestId', description='The request ID.', example='UUID'),
}

model AllocateCostCenterResourceResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: AllocateCostCenterResourceResponseBody(name='body'),
}

/**
 * @summary Allocates resource instances (instance-based and attached-resource-based) from a source cost center to a destination cost center.
 *
 * @param tmpReq AllocateCostCenterResourceRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return AllocateCostCenterResourceResponse
 */
@context("allocateCostCenterResourceWithContext")
async function allocateCostCenterResourceWithOptions(tmpReq: AllocateCostCenterResourceRequest, runtime: $RuntimeOptions): AllocateCostCenterResourceResponse {
  tmpReq.validate();
  var request = new AllocateCostCenterResourceShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.resourceInstanceList)) {
    request.resourceInstanceListShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.resourceInstanceList, 'ResourceInstanceList', 'json');
  }
  var query = {};
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  var body : map[string]any = {};
  if (!$isNull(request.fromCostCenterId)) {
    body['FromCostCenterId'] = request.fromCostCenterId;
  }
  if (!$isNull(request.fromOwnerAccountId)) {
    body['FromOwnerAccountId'] = request.fromOwnerAccountId;
  }
  if (!$isNull(request.resourceInstanceListShrink)) {
    body['ResourceInstanceList'] = request.resourceInstanceListShrink;
  }
  if (!$isNull(request.toCostCenterId)) {
    body['ToCostCenterId'] = request.toCostCenterId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'AllocateCostCenterResource',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Allocates resource instances (instance-based and attached-resource-based) from a source cost center to a destination cost center.
 *
 * @param request AllocateCostCenterResourceRequest
 * @return AllocateCostCenterResourceResponse
 */
async function allocateCostCenterResource(request: AllocateCostCenterResourceRequest): AllocateCostCenterResourceResponse {
  var runtime = new $RuntimeOptions{};
  return allocateCostCenterResourceWithOptions(request, runtime);
}

model CancelFundAccountLowAvailableAmountAlarmRequest {
  fundAccountId?: long(name='FundAccountId', description='The ID of the fund account. If this parameter is not specified, the ID of the account owned by the current account is used by default.', example='123321123'),
}

model CancelFundAccountLowAvailableAmountAlarmResponseBody = {
  data?: boolean(name='Data', description='Indicates whether the operation is successful.', example='true'),
  metadata?: any(name='Metadata', description='The metadata of the response struct.', example='{}'),
  requestId?: string(name='RequestId', description='The request ID.', example='DFC1F7F9-3BA9-BA4D-2F2E653'),
}

model CancelFundAccountLowAvailableAmountAlarmResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CancelFundAccountLowAvailableAmountAlarmResponseBody(name='body'),
}

/**
 * @summary Cancels the low balance alert for a fund account.
 *
 * @description Cancels the low balance alert for a fund account.
 *
 * @param request CancelFundAccountLowAvailableAmountAlarmRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CancelFundAccountLowAvailableAmountAlarmResponse
 */
@context("cancelFundAccountLowAvailableAmountAlarmWithContext")
async function cancelFundAccountLowAvailableAmountAlarmWithOptions(request: CancelFundAccountLowAvailableAmountAlarmRequest, runtime: $RuntimeOptions): CancelFundAccountLowAvailableAmountAlarmResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.fundAccountId)) {
    body['FundAccountId'] = request.fundAccountId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CancelFundAccountLowAvailableAmountAlarm',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Cancels the low balance alert for a fund account.
 *
 * @description Cancels the low balance alert for a fund account.
 *
 * @param request CancelFundAccountLowAvailableAmountAlarmRequest
 * @return CancelFundAccountLowAvailableAmountAlarmResponse
 */
async function cancelFundAccountLowAvailableAmountAlarm(request: CancelFundAccountLowAvailableAmountAlarmRequest): CancelFundAccountLowAvailableAmountAlarmResponse {
  var runtime = new $RuntimeOptions{};
  return cancelFundAccountLowAvailableAmountAlarmWithOptions(request, runtime);
}

model CheckAccountExistRequest {
  ecIdAccountIds?: [ 
    {
      accountIds?: [ long ](name='AccountIds'),
      ecId?: string(name='EcId', description='This parameter is required.', example='1004064243473974'),
    }
  ](name='EcIdAccountIds'),
  nbid?: string(name='Nbid', example='2684201000001'),
  toUserType?: int32(name='ToUserType', example='0'),
  transferAccount?: string(name='TransferAccount', example='12323'),
}

model CheckAccountExistResponseBody = {
  data?: {
    accountId?: string(name='AccountId', example='1990699401005016'),
    nickName?: string(name='NickName', example='01'),
    pk?: string(name='Pk', example='1904635303738977'),
    result?: boolean(name='Result', example='True'),
  }(name='Data'),
  requestId?: string(name='RequestId', example='UUID'),
}

model CheckAccountExistResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CheckAccountExistResponseBody(name='body'),
}

/**
 * @summary 提货券账户检查是否存在
 *
 * @param request CheckAccountExistRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CheckAccountExistResponse
 */
@context("checkAccountExistWithContext")
async function checkAccountExistWithOptions(request: CheckAccountExistRequest, runtime: $RuntimeOptions): CheckAccountExistResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.ecIdAccountIds)) {
    query['EcIdAccountIds'] = request.ecIdAccountIds;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  var body : map[string]any = {};
  if (!$isNull(request.toUserType)) {
    body['ToUserType'] = request.toUserType;
  }
  if (!$isNull(request.transferAccount)) {
    body['TransferAccount'] = request.transferAccount;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CheckAccountExist',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 提货券账户检查是否存在
 *
 * @param request CheckAccountExistRequest
 * @return CheckAccountExistResponse
 */
async function checkAccountExist(request: CheckAccountExistRequest): CheckAccountExistResponse {
  var runtime = new $RuntimeOptions{};
  return checkAccountExistWithOptions(request, runtime);
}

model CheckBudgetNameExistsRequest {
  budgetName?: string(name='BudgetName', description='The budget name.

This parameter is required.', example='department1'),
  nbid?: string(name='Nbid', description='The primary sales channel ID. If this parameter is left empty, the channel ID of the current user is used by default.', example='2688801000001'),
}

model CheckBudgetNameExistsResponseBody = {
  budgetName?: string(name='BudgetName', description='The budget name.', example='department1'),
  exists?: boolean(name='Exists', description='Indicates whether the budget name already exists.', example='true'),
  requestId?: string(name='RequestId', description='The request ID.', example='F1E2D3C4-B5A6-7890-1234-567890ABCDEF'),
}

model CheckBudgetNameExistsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CheckBudgetNameExistsResponseBody(name='body'),
}

/**
 * @summary Checks whether a specified budgetName exists.
 *
 * @param request CheckBudgetNameExistsRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CheckBudgetNameExistsResponse
 */
@context("checkBudgetNameExistsWithContext")
async function checkBudgetNameExistsWithOptions(request: CheckBudgetNameExistsRequest, runtime: $RuntimeOptions): CheckBudgetNameExistsResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  var body : map[string]any = {};
  if (!$isNull(request.budgetName)) {
    body['BudgetName'] = request.budgetName;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CheckBudgetNameExists',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Checks whether a specified budgetName exists.
 *
 * @param request CheckBudgetNameExistsRequest
 * @return CheckBudgetNameExistsResponse
 */
async function checkBudgetNameExists(request: CheckBudgetNameExistsRequest): CheckBudgetNameExistsResponse {
  var runtime = new $RuntimeOptions{};
  return checkBudgetNameExistsWithOptions(request, runtime);
}

model CreateBudgetRequest {
  budgetName?: string(name='BudgetName', description='The budget name.

This parameter is required.', example='Department_dev_budget'),
  budgetType?: string(name='BudgetType', description='The budget type.

This parameter is required.', example='CONSUME'),
  comment?: string(name='Comment', description='The remarks.', example='comment'),
  cycleEndPeriod?: string(name='CycleEndPeriod', description='The end cycle.

This parameter is required.', example='2026-12'),
  cycleQuota?: [ 
    {
      cyclePeriod?: string(name='CyclePeriod', description='The cycle.', example='2026-01'),
      quota?: string(name='Quota', description='The quota.', example='1000'),
    }
  ](name='CycleQuota', description='The per-cycle specified quota. This parameter is required when QuotaType is set to `SPECIFY`.'),
  cycleStartPeriod?: string(name='CycleStartPeriod', description='The start cycle.

This parameter is required.', example='2026-01'),
  cycleType?: string(name='CycleType', description='The cycle type.

This parameter is required.', example='MONTHLY'),
  ecIdAccountIds?: [ 
    {
      accountIds?: [ long ](name='AccountIds', description='The list of accounts to access. An empty value indicates all accounts under the current entity ID.'),
      ecId?: string(name='EcId', description='The enterprise entity ID.', example='1'),
    }
  ](name='EcIdAccountIds', description='The list of enterprises and accounts. An empty value indicates the current account.'),
  metric?: string(name='Metric', description='The budget metric.

This parameter is required.', example='REQUIRE_AMOUNT'),
  nbid?: string(name='Nbid', description='The level-1 marketplace ID. If empty, the marketplace ID of the current user is used by default.', example='26842'),
  queryFilter?: [ 
    {
      code?: string(name='Code', description='The parameter code.', example='RESOURCE_OWNER_ACCOUNT'),
      selectType?: string(name='SelectType', description='The selection mode.', example='IN'),
      values?: [ string ](name='Values', description='The list of filter values.'),
    }
  ](name='QueryFilter', description='The filter conditions.'),
  quota?: string(name='Quota', description='The fixed quota value. If the type is quota, the unit is percentage.', example='1000'),
  quotaType?: string(name='QuotaType', description='The quota type.

This parameter is required.', example='FIXED'),
  warnConfs?: [ 
    {
      comment?: string(name='Comment', description='The remarks.', example='comment'),
      eventBridge?: boolean(name='EventBridge', description='Specifies whether to enable EventBridge.', example='true'),
      mscChannels?: [ string ](name='MscChannels', description='The list of Message Center notification channels.'),
      mscContacts?: [ string ](name='MscContacts', description='The list of Message Center contacts.'),
      name?: string(name='Name', description='The alert name. This is a user-defined optional field. If not specified, the backend automatically generates a name.', example='Alter-1'),
      thresholdType?: string(name='ThresholdType', description='The threshold type.', example='FIXED'),
      thresholdValue?: string(name='ThresholdValue', description='The threshold value.', example='2000'),
      warnTarget?: string(name='WarnTarget', description='The alert target.', example='FORECAST'),
    }
  ](name='WarnConfs', description='The alert configurations.'),
}

model CreateBudgetShrinkRequest {
  budgetName?: string(name='BudgetName', description='The budget name.

This parameter is required.', example='Department_dev_budget'),
  budgetType?: string(name='BudgetType', description='The budget type.

This parameter is required.', example='CONSUME'),
  comment?: string(name='Comment', description='The remarks.', example='comment'),
  cycleEndPeriod?: string(name='CycleEndPeriod', description='The end cycle.

This parameter is required.', example='2026-12'),
  cycleQuotaShrink?: string(name='CycleQuota', description='The per-cycle specified quota. This parameter is required when QuotaType is set to `SPECIFY`.'),
  cycleStartPeriod?: string(name='CycleStartPeriod', description='The start cycle.

This parameter is required.', example='2026-01'),
  cycleType?: string(name='CycleType', description='The cycle type.

This parameter is required.', example='MONTHLY'),
  ecIdAccountIdsShrink?: string(name='EcIdAccountIds', description='The list of enterprises and accounts. An empty value indicates the current account.'),
  metric?: string(name='Metric', description='The budget metric.

This parameter is required.', example='REQUIRE_AMOUNT'),
  nbid?: string(name='Nbid', description='The level-1 marketplace ID. If empty, the marketplace ID of the current user is used by default.', example='26842'),
  queryFilterShrink?: string(name='QueryFilter', description='The filter conditions.'),
  quota?: string(name='Quota', description='The fixed quota value. If the type is quota, the unit is percentage.', example='1000'),
  quotaType?: string(name='QuotaType', description='The quota type.

This parameter is required.', example='FIXED'),
  warnConfsShrink?: string(name='WarnConfs', description='The alert configurations.'),
}

model CreateBudgetResponseBody = {
  budgetName?: string(name='BudgetName', description='The budget name.', example='Department_dev_budget'),
  requestId?: string(name='RequestId', description='The request ID.', example='39EDD65E-68C5-1B17-8440-C729C7591D74'),
}

model CreateBudgetResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateBudgetResponseBody(name='body'),
}

/**
 * @summary Creates a budget.
 *
 * @param tmpReq CreateBudgetRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateBudgetResponse
 */
@context("createBudgetWithContext")
async function createBudgetWithOptions(tmpReq: CreateBudgetRequest, runtime: $RuntimeOptions): CreateBudgetResponse {
  tmpReq.validate();
  var request = new CreateBudgetShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.cycleQuota)) {
    request.cycleQuotaShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.cycleQuota, 'CycleQuota', 'json');
  }
  if (!$isNull(tmpReq.ecIdAccountIds)) {
    request.ecIdAccountIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.ecIdAccountIds, 'EcIdAccountIds', 'json');
  }
  if (!$isNull(tmpReq.queryFilter)) {
    request.queryFilterShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.queryFilter, 'QueryFilter', 'json');
  }
  if (!$isNull(tmpReq.warnConfs)) {
    request.warnConfsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.warnConfs, 'WarnConfs', 'json');
  }
  var query = {};
  if (!$isNull(request.ecIdAccountIdsShrink)) {
    query['EcIdAccountIds'] = request.ecIdAccountIdsShrink;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  var body : map[string]any = {};
  if (!$isNull(request.budgetName)) {
    body['BudgetName'] = request.budgetName;
  }
  if (!$isNull(request.budgetType)) {
    body['BudgetType'] = request.budgetType;
  }
  if (!$isNull(request.comment)) {
    body['Comment'] = request.comment;
  }
  if (!$isNull(request.cycleEndPeriod)) {
    body['CycleEndPeriod'] = request.cycleEndPeriod;
  }
  if (!$isNull(request.cycleQuotaShrink)) {
    body['CycleQuota'] = request.cycleQuotaShrink;
  }
  if (!$isNull(request.cycleStartPeriod)) {
    body['CycleStartPeriod'] = request.cycleStartPeriod;
  }
  if (!$isNull(request.cycleType)) {
    body['CycleType'] = request.cycleType;
  }
  if (!$isNull(request.metric)) {
    body['Metric'] = request.metric;
  }
  if (!$isNull(request.queryFilterShrink)) {
    body['QueryFilter'] = request.queryFilterShrink;
  }
  if (!$isNull(request.quota)) {
    body['Quota'] = request.quota;
  }
  if (!$isNull(request.quotaType)) {
    body['QuotaType'] = request.quotaType;
  }
  if (!$isNull(request.warnConfsShrink)) {
    body['WarnConfs'] = request.warnConfsShrink;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateBudget',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates a budget.
 *
 * @param request CreateBudgetRequest
 * @return CreateBudgetResponse
 */
async function createBudget(request: CreateBudgetRequest): CreateBudgetResponse {
  var runtime = new $RuntimeOptions{};
  return createBudgetWithOptions(request, runtime);
}

model CreateCostCenterRequest {
  costCenterEntityList?: [ 
    {
      costCenterName?: string(name='CostCenterName', description='Cost center name (must be unique within the same account)

This parameter is required.', example='资源组'),
      ownerAccountId?: long(name='OwnerAccountId', description='Owner user ID of the new cost center

This parameter is required.', example='1314839403940987'),
      parentCostCenterId?: long(name='ParentCostCenterId', description='Parent cost center ID. A value of -1 indicates the root cost center.

This parameter is required.', example='-1'),
    }
  ](name='CostCenterEntityList', description='Cost center entity list

This parameter is required.'),
  nbid?: string(name='Nbid', description='Primary marketplace ID. If empty, the marketplace ID of the current user is used by default.', example='2084210001'),
}

model CreateCostCenterShrinkRequest {
  costCenterEntityListShrink?: string(name='CostCenterEntityList', description='Cost center entity list

This parameter is required.'),
  nbid?: string(name='Nbid', description='Primary marketplace ID. If empty, the marketplace ID of the current user is used by default.', example='2084210001'),
}

model CreateCostCenterResponseBody = {
  costCenterDtoList?: [ 
    {
      costCenterId?: long(name='CostCenterId', description='Cost center ID', example='485938'),
      costCenterName?: string(name='CostCenterName', description='Cost center name (must be unique within the same account)', example='资源组'),
      ownerAccountId?: long(name='OwnerAccountId', description='Owner user ID of the cost center', example='1314839403940987'),
      parentCostCenterId?: long(name='ParentCostCenterId', description='Parent cost center ID', example='-1'),
    }
  ](name='CostCenterDtoList', description='Data list'),
  metadata?: any(name='Metadata', description='Response metadata', example='{}'),
  requestId?: string(name='RequestId', description='Request ID', example='C1BD134E-D914-6AE0-1901-AEB2A99FA205'),
}

model CreateCostCenterResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateCostCenterResponseBody(name='body'),
}

/**
 * @summary Create Cost Center
 *
 * @description Creates one or more cost centers.
 *
 * @param tmpReq CreateCostCenterRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateCostCenterResponse
 */
@context("createCostCenterWithContext")
async function createCostCenterWithOptions(tmpReq: CreateCostCenterRequest, runtime: $RuntimeOptions): CreateCostCenterResponse {
  tmpReq.validate();
  var request = new CreateCostCenterShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.costCenterEntityList)) {
    request.costCenterEntityListShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.costCenterEntityList, 'CostCenterEntityList', 'json');
  }
  var query = {};
  if (!$isNull(request.costCenterEntityListShrink)) {
    query['CostCenterEntityList'] = request.costCenterEntityListShrink;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateCostCenter',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Create Cost Center
 *
 * @description Creates one or more cost centers.
 *
 * @param request CreateCostCenterRequest
 * @return CreateCostCenterResponse
 */
async function createCostCenter(request: CreateCostCenterRequest): CreateCostCenterResponse {
  var runtime = new $RuntimeOptions{};
  return createCostCenterWithOptions(request, runtime);
}

model CreateCostCenterRuleRequest {
  costCenterId?: long(name='CostCenterId', description='Financial unit ID', example='485938'),
  filterExpression?: {
    expressionType?: string(name='ExpressionType', description='Operation type', example='NARY'),
    filterValues?: {
      code?: string(name='Code', description='Condition filter key', example='TAG-test-xxx-key'),
      codeName?: string(name='CodeName', description='Condition filter key name (deprecated)', example='无'),
      selectType?: string(name='SelectType', description='Association relationship between code and value', example='IN'),
      values?: [ string ](name='Values', description='Condition filter value list'),
    }(name='FilterValues', description='Sub-condition filter'),
    operand?: any(name='Operand', description='Filter condition (deprecated)', example='无'),
    operands?: [ any ](name='Operands', description='Condition expression'),
    operatorType?: string(name='OperatorType', description='Relational expression', example='AND'),
  }(name='FilterExpression', description='Rule expression'),
  nbid?: string(name='Nbid', description='First-level marketplace ID. If left empty, the ID of the marketplace to which the current user belongs is used by default.', example='2084210001'),
}

model CreateCostCenterRuleShrinkRequest {
  costCenterId?: long(name='CostCenterId', description='Financial unit ID', example='485938'),
  filterExpressionShrink?: string(name='FilterExpression', description='Rule expression'),
  nbid?: string(name='Nbid', description='First-level marketplace ID. If left empty, the ID of the marketplace to which the current user belongs is used by default.', example='2084210001'),
}

model CreateCostCenterRuleResponseBody = {
  data?: long(name='Data', description='Financial unit allocation rule ID', example='1111'),
  metadata?: any(name='Metadata', description='Response structure metadata', example='{}'),
  requestId?: string(name='RequestId', description='Request ID.', example='79EE7556-0CFD-44EB-9CD6-B3B526E3A85F'),
}

model CreateCostCenterRuleResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateCostCenterRuleResponseBody(name='body'),
}

/**
 * @summary Create a financial unit auto-allocation rule
 *
 * @param tmpReq CreateCostCenterRuleRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateCostCenterRuleResponse
 */
@context("createCostCenterRuleWithContext")
async function createCostCenterRuleWithOptions(tmpReq: CreateCostCenterRuleRequest, runtime: $RuntimeOptions): CreateCostCenterRuleResponse {
  tmpReq.validate();
  var request = new CreateCostCenterRuleShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.filterExpression)) {
    request.filterExpressionShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.filterExpression, 'FilterExpression', 'json');
  }
  var query = {};
  if (!$isNull(request.filterExpressionShrink)) {
    query['FilterExpression'] = request.filterExpressionShrink;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  var body : map[string]any = {};
  if (!$isNull(request.costCenterId)) {
    body['CostCenterId'] = request.costCenterId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateCostCenterRule',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Create a financial unit auto-allocation rule
 *
 * @param request CreateCostCenterRuleRequest
 * @return CreateCostCenterRuleResponse
 */
async function createCostCenterRule(request: CreateCostCenterRuleRequest): CreateCostCenterRuleResponse {
  var runtime = new $RuntimeOptions{};
  return createCostCenterRuleWithOptions(request, runtime);
}

model CreateFundAccountPayRelationRequest {
  ecIdAccountIds?: [ 
    {
      accountIds?: [ long ](name='AccountIds', description='List of Alibaba Cloud accounts for delegated payment.

This parameter is required.'),
      ecId?: string(name='EcId', description='Enterprise entity ID.', example='1501603440974415'),
    }
  ](name='EcIdAccountIds', description='List of enterprises and accounts.

This parameter is required.'),
  fundAccountId?: string(name='FundAccountId', description='Fund account ID.

This parameter is required.', example='12332112'),
  nbid?: string(name='Nbid', description='Primary marketplace ID. If this parameter is left empty, the marketplace ID of the current user is used by default.', example='2684201000001'),
}

model CreateFundAccountPayRelationShrinkRequest {
  ecIdAccountIdsShrink?: string(name='EcIdAccountIds', description='List of enterprises and accounts.

This parameter is required.'),
  fundAccountId?: string(name='FundAccountId', description='Fund account ID.

This parameter is required.', example='12332112'),
  nbid?: string(name='Nbid', description='Primary marketplace ID. If this parameter is left empty, the marketplace ID of the current user is used by default.', example='2684201000001'),
}

model CreateFundAccountPayRelationResponseBody = {
  data?: [ 
    {
      accountId?: string(name='AccountId', description='AccountId of the user who pays with the account.', example='1501603440974415'),
      accountName?: string(name='AccountName', description='Name of the user who pays with the account.', example='test@test.aliyunid.com'),
      fundAccountId?: string(name='FundAccountId', description='Account ID.', example='12332112'),
      resultCode?: string(name='ResultCode', description='Result code.', example='Success'),
      resultMessage?: string(name='ResultMessage', description='Result message.', example='Successful'),
    }
  ](name='Data', description='Data list.'),
  metadata?: any(name='Metadata', description='Response metadata.', example='{}'),
  requestId?: string(name='RequestId', description='Request ID.', example='6000EE23-274B-4E07-A697-FF2E999'),
}

model CreateFundAccountPayRelationResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateFundAccountPayRelationResponseBody(name='body'),
}

/**
 * @summary Create payment relationships for a fund account
 *
 * @param tmpReq CreateFundAccountPayRelationRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateFundAccountPayRelationResponse
 */
@context("createFundAccountPayRelationWithContext")
async function createFundAccountPayRelationWithOptions(tmpReq: CreateFundAccountPayRelationRequest, runtime: $RuntimeOptions): CreateFundAccountPayRelationResponse {
  tmpReq.validate();
  var request = new CreateFundAccountPayRelationShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.ecIdAccountIds)) {
    request.ecIdAccountIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.ecIdAccountIds, 'EcIdAccountIds', 'json');
  }
  var query = {};
  if (!$isNull(request.ecIdAccountIdsShrink)) {
    query['EcIdAccountIds'] = request.ecIdAccountIdsShrink;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  var body : map[string]any = {};
  if (!$isNull(request.fundAccountId)) {
    body['FundAccountId'] = request.fundAccountId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateFundAccountPayRelation',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Create payment relationships for a fund account
 *
 * @param request CreateFundAccountPayRelationRequest
 * @return CreateFundAccountPayRelationResponse
 */
async function createFundAccountPayRelation(request: CreateFundAccountPayRelationRequest): CreateFundAccountPayRelationResponse {
  var runtime = new $RuntimeOptions{};
  return createFundAccountPayRelationWithOptions(request, runtime);
}

model CreateFundAccountTransferRequest {
  amount?: string(name='Amount', description='The transfer amount.

This parameter is required.', example='100'),
  currency?: string(name='Currency', description='The currency.

This parameter is required.', example='CNY'),
  financeType?: string(name='FinanceType', description='The asset type. Currently, only cash is supported. Valid values:
- cash: cash.

This parameter is required.', example='cash'),
  fromFundAccountId?: long(name='FromFundAccountId', description='The ID of the source account (payer account ID).

This parameter is required.', example='123212323'),
  remark?: string(name='Remark', description='The remarks.

This parameter is required.', example='转账的备注'),
  toFundAccountId?: long(name='ToFundAccountId', description='The ID of the destination account (payee account ID).

This parameter is required.', example='11112231'),
  transferType?: string(name='TransferType', description='The transfer type. Valid values:
- grant: transfer.
- recycle: revocation.

This parameter is required.', example='grant'),
}

model CreateFundAccountTransferResponseBody = {
  metadata?: any(name='Metadata', description='The metadata of the response struct.', example='{}'),
  requestId?: string(name='RequestId', description='The request ID.', example='1BB79-5B23-3EA-BB4F-352F93E'),
}

model CreateFundAccountTransferResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateFundAccountTransferResponseBody(name='body'),
}

/**
 * @summary Creates an account transfer or revocation.
 *
 * @param request CreateFundAccountTransferRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateFundAccountTransferResponse
 */
@context("createFundAccountTransferWithContext")
async function createFundAccountTransferWithOptions(request: CreateFundAccountTransferRequest, runtime: $RuntimeOptions): CreateFundAccountTransferResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.amount)) {
    body['Amount'] = request.amount;
  }
  if (!$isNull(request.currency)) {
    body['Currency'] = request.currency;
  }
  if (!$isNull(request.financeType)) {
    body['FinanceType'] = request.financeType;
  }
  if (!$isNull(request.fromFundAccountId)) {
    body['FromFundAccountId'] = request.fromFundAccountId;
  }
  if (!$isNull(request.remark)) {
    body['Remark'] = request.remark;
  }
  if (!$isNull(request.toFundAccountId)) {
    body['ToFundAccountId'] = request.toFundAccountId;
  }
  if (!$isNull(request.transferType)) {
    body['TransferType'] = request.transferType;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateFundAccountTransfer',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates an account transfer or revocation.
 *
 * @param request CreateFundAccountTransferRequest
 * @return CreateFundAccountTransferResponse
 */
async function createFundAccountTransfer(request: CreateFundAccountTransferRequest): CreateFundAccountTransferResponse {
  var runtime = new $RuntimeOptions{};
  return createFundAccountTransferWithOptions(request, runtime);
}

model CreateInvoiceRequest {
  amount?: string(name='Amount', description='Specifies the invoice amount. Supports up to two decimal places.
- If not specified, the invoice will be issued for the total invoiceable amount of all invoiceCandidateIds.
- If specified, the invoice will be issued for the specified amount. The specified amount cannot exceed the total invoiceable amount of all invoiceCandidateIds.', example='0.01'),
  ecIdAccountIds?: [ 
    {
      accountIds?: [ long ](name='AccountIds', description='List of accounts to access. If empty, all accounts under the current entity ID are selected.'),
      ecId?: string(name='EcId', description='Enterprise entity ID.', example='12345'),
    }
  ](name='EcIdAccountIds', description='Enterprise and account list. If empty, the current account is queried.'),
  invoiceCandidateIds?: [ string ](name='InvoiceCandidateIds', description='List of invoice candidate IDs.

This parameter is required.'),
  invoiceMode?: int32(name='InvoiceMode', description='Invoice mode.
- 0: Independent invoicing. Expenses of multiple accounts under the enterprise are invoiced separately for each account.
- 1: Consolidated invoicing. Expenses of multiple accounts under the enterprise are consolidated and invoiced under the invoicing entity.
If only one account is passed in the AccountIds parameter, independent invoicing is applied.

This parameter is required.', example='0'),
  invoiceRemark?: string(name='InvoiceRemark', description='Invoice remark.', example='备注信息'),
  invoiceTitleId?: string(name='InvoiceTitleId', description='Invoice title ID.

- The ID parameter returned by the ListInvoiceTitle API for the current logged-in account.

This parameter is required.', example='12345'),
  invoiceType?: int32(name='InvoiceType', description='Invoice type.

This parameter is required.', example='0'),
  nbid?: string(name='Nbid', description='Primary marketplace ID. If empty, the marketplace ID of the current user is used by default.', example='2684201000001'),
  recipientEmails?: [ string ](name='RecipientEmails', description='List of email addresses to receive the invoice. A maximum of 3 can be specified.

This parameter is required.'),
}

model CreateInvoiceShrinkRequest {
  amount?: string(name='Amount', description='Specifies the invoice amount. Supports up to two decimal places.
- If not specified, the invoice will be issued for the total invoiceable amount of all invoiceCandidateIds.
- If specified, the invoice will be issued for the specified amount. The specified amount cannot exceed the total invoiceable amount of all invoiceCandidateIds.', example='0.01'),
  ecIdAccountIdsShrink?: string(name='EcIdAccountIds', description='Enterprise and account list. If empty, the current account is queried.'),
  invoiceCandidateIdsShrink?: string(name='InvoiceCandidateIds', description='List of invoice candidate IDs.

This parameter is required.'),
  invoiceMode?: int32(name='InvoiceMode', description='Invoice mode.
- 0: Independent invoicing. Expenses of multiple accounts under the enterprise are invoiced separately for each account.
- 1: Consolidated invoicing. Expenses of multiple accounts under the enterprise are consolidated and invoiced under the invoicing entity.
If only one account is passed in the AccountIds parameter, independent invoicing is applied.

This parameter is required.', example='0'),
  invoiceRemark?: string(name='InvoiceRemark', description='Invoice remark.', example='备注信息'),
  invoiceTitleId?: string(name='InvoiceTitleId', description='Invoice title ID.

- The ID parameter returned by the ListInvoiceTitle API for the current logged-in account.

This parameter is required.', example='12345'),
  invoiceType?: int32(name='InvoiceType', description='Invoice type.

This parameter is required.', example='0'),
  nbid?: string(name='Nbid', description='Primary marketplace ID. If empty, the marketplace ID of the current user is used by default.', example='2684201000001'),
  recipientEmailsShrink?: string(name='RecipientEmails', description='List of email addresses to receive the invoice. A maximum of 3 can be specified.

This parameter is required.'),
}

model CreateInvoiceResponseBody = {
  data?: [ 
    {
      accountId?: long(name='AccountId', description='Account ID.', example='1990699401005016'),
      amount?: string(name='Amount', description='Invoice amount.', example='0.01'),
      errorCode?: string(name='ErrorCode', description='Error code.', example='1001'),
      invoiceIssuer?: string(name='InvoiceIssuer', description='Invoice issuer.', example='ALIYUN_SERVICE'),
      message?: string(name='Message', description='Error message.', example='不可开票'),
    }
  ](name='Data', description='Data list.'),
  metadata?: any(name='Metadata', description='Response metadata.', example='{}'),
  requestId?: string(name='RequestId', description='Request ID.', example='79EE7556-0CFD-44EB-9CD6-B3B526E3A85F'),
}

model CreateInvoiceResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateInvoiceResponseBody(name='body'),
}

/**
 * @summary Apply for Invoice
 *
 * @param tmpReq CreateInvoiceRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateInvoiceResponse
 */
@context("createInvoiceWithContext")
async function createInvoiceWithOptions(tmpReq: CreateInvoiceRequest, runtime: $RuntimeOptions): CreateInvoiceResponse {
  tmpReq.validate();
  var request = new CreateInvoiceShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.ecIdAccountIds)) {
    request.ecIdAccountIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.ecIdAccountIds, 'EcIdAccountIds', 'json');
  }
  if (!$isNull(tmpReq.invoiceCandidateIds)) {
    request.invoiceCandidateIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.invoiceCandidateIds, 'InvoiceCandidateIds', 'json');
  }
  if (!$isNull(tmpReq.recipientEmails)) {
    request.recipientEmailsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.recipientEmails, 'RecipientEmails', 'json');
  }
  var query = {};
  if (!$isNull(request.amount)) {
    query['Amount'] = request.amount;
  }
  if (!$isNull(request.ecIdAccountIdsShrink)) {
    query['EcIdAccountIds'] = request.ecIdAccountIdsShrink;
  }
  if (!$isNull(request.invoiceCandidateIdsShrink)) {
    query['InvoiceCandidateIds'] = request.invoiceCandidateIdsShrink;
  }
  if (!$isNull(request.invoiceMode)) {
    query['InvoiceMode'] = request.invoiceMode;
  }
  if (!$isNull(request.invoiceRemark)) {
    query['InvoiceRemark'] = request.invoiceRemark;
  }
  if (!$isNull(request.invoiceTitleId)) {
    query['InvoiceTitleId'] = request.invoiceTitleId;
  }
  if (!$isNull(request.invoiceType)) {
    query['InvoiceType'] = request.invoiceType;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  if (!$isNull(request.recipientEmailsShrink)) {
    query['RecipientEmails'] = request.recipientEmailsShrink;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateInvoice',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Apply for Invoice
 *
 * @param request CreateInvoiceRequest
 * @return CreateInvoiceResponse
 */
async function createInvoice(request: CreateInvoiceRequest): CreateInvoiceResponse {
  var runtime = new $RuntimeOptions{};
  return createInvoiceWithOptions(request, runtime);
}

model CreateReportDefinitionRequest {
  beginBillingCycle?: string(name='BeginBillingCycle', description='The start billing cycle for push. After the subscription succeeds, the system automatically pushes data from the start billing cycle to the current time. This parameter is invalid for monthly bill PDF subscriptions, and historical data will not be re-pushed. You can push data within the last year.', example='2025-05'),
  includeMembers?: string(name='IncludeMembers', description='The email subscription configuration that specifies whether to include multi-account members of the organization in bills.', example='true'),
  mcProject?: string(name='McProject', description='The name of the MaxCompute project.', example='project'),
  mcTableName?: string(name='McTableName', description='The name of the MaxCompute subscription table.', example='table'),
  nbid?: string(name='Nbid', description='The first-level site ID. If this parameter is left empty, the site ID of the current user is used by default.', example='2684201000001'),
  notSendOnNoData?: string(name='NotSendOnNoData', description='The email subscription configuration that specifies whether to skip sending emails when no bills are available.', example='true'),
  ossBucketName?: string(name='OssBucketName', description='The name of the OSS bucket for file storage.', example='sh-bill'),
  ossBucketOwnerAccountId?: long(name='OssBucketOwnerAccountId', description='The UID of the OSS bucket owner that stores the files. If this is a Bid/Reseller subscription and you need to push data to a sub-account\\\\"s OSS, specify this parameter. The account must be a sub-account of the calling account, and the AliyunConsumeDump2OSSRole permission must be granted to this account. Regular users do not need to specify this parameter. The default value is the calling account.', example='1234567812345678'),
  ossBucketPath?: string(name='OssBucketPath', description='The storage path of the OSS bucket.', example='bill/'),
  reportSourceType?: string(name='ReportSourceType', description='The subscription source. Valid values: OSS, MC, and MSC_EMAIL.', example='OSS'),
  reportType?: string(name='ReportType', description='The subscription type. Valid values:
- consumeDetailBillV2: consumption details. This value is supported only by OSS/MC subscriptions.
- splitDetailBillV2: split details. This value is supported only by OSS/MC subscriptions.
- costDetailBillV2: cost details. This value is supported only by OSS/MC subscriptions.
- monthBillOverview: monthly bill overview. This value is supported only by OSS/MSC_EMAIL subscriptions.
- focus: FOCUS bill. This value is supported only by OSS/MC subscriptions.

This parameter is required.', example='consumeDetailBillV2'),
  sendWithAttach?: string(name='SendWithAttach', description='The email subscription configuration that specifies whether to send emails with bill attachments.', example='true'),
  splitFileOnUserId?: string(name='SplitFileOnUserId', description='The email subscription configuration that specifies whether to split attachments by user ID.', example='true'),
}

model CreateReportDefinitionResponseBody = {
  beginBillingCycle?: string(name='BeginBillingCycle', description='The start billing cycle for push. After the subscription succeeds, the system automatically pushes data from the start billing cycle to the current time. This parameter is invalid for monthly bill PDF subscriptions, and historical data will not be re-pushed. You can push data within the last year.', example='2025-05'),
  metadata?: any(name='Metadata', description='The metadata of the response.', example='{}'),
  ossBucketName?: string(name='OssBucketName', description='The name of the OSS bucket for file storage.', example='sh-bill'),
  ossBucketOwnerAccountId?: long(name='OssBucketOwnerAccountId', description='The UID of the OSS bucket owner that stores the files. If this is a Bid/Reseller subscription and you need to push data to a sub-account\\\\"s OSS, specify this parameter. The account must be a sub-account of the calling account, and the AliyunConsumeDump2OSSRole permission must be granted to this account. Regular users do not need to specify this parameter. The default value is the calling account.', example='1234567812345678'),
  ossBucketPath?: string(name='OssBucketPath', description='The storage path of the OSS bucket.', example='bill/'),
  reportSourceName?: string(name='ReportSourceName', description='The name of the subscription source.', example='OSS'),
  reportSourceType?: string(name='ReportSourceType', description='The subscription source. Valid values: OSS and MC.', example='OSS'),
  reportTaskId?: long(name='ReportTaskId', description='The ID of the billing subscription task.', example='123123'),
  reportType?: string(name='ReportType', description='The subscription type. Valid values:
- consumeDetailBillV2: consumption details. This value is supported only by OSS/MC subscriptions.
- splitDetailBillV2: split details. This value is supported only by OSS/MC subscriptions.
- costDetailBillV2: cost details. This value is supported only by OSS/MC subscriptions.
- monthBillOverview: monthly bill overview. This value is supported only by OSS/MSC_EMAIL subscriptions.
- focus: FOCUS bill. This value is supported only by OSS/MC subscriptions.', example='consumeDetailBillV2'),
  requestId?: string(name='RequestId', description='The request ID.', example='340CAB45-0637-5875-9BE4-EFD5750F6BA5'),
  subscribeCreateTime?: string(name='SubscribeCreateTime', description='The time when the subscription was created.', example='2025-05-21 10:36:31'),
}

model CreateReportDefinitionResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateReportDefinitionResponseBody(name='body'),
}

/**
 * @summary Creates a billing report subscription.
 *
 * @description When you call this API operation, note the following information:
 * - You can subscribe to one type of billing file at a time.
 * - Except for monthly bill PDFs, starting from the day after the subscription, the system pushes daily billing files that contain full detailed data from the beginning of the current month to the present. Before the 4th of each month, the system pushes full billing files for the complete billing cycle of the previous month.
 * - Monthly bill PDFs are pushed before the 4th of each month for the previous month.
 * - Billing files generated on a daily basis may have delays. Delayed billing files are pushed on the day after they are generated and may also contain bills that were delayed from before the previous day and generated on the previous day. We recommend that you pull the full files for the previous month at the beginning of each month.
 * > Apply for the required permissions by following the procedure described in the documentation: [Billing Subscription](https://help.aliyun.com/zh/user-center/user-guide/billing-subscription?spm=5176.21213303.J_v8LsmxMG6alneH-O7TCPa.1.3ef82f3d5ZIf08&scm=20140722.S_help@@%E6%96%87%E6%A1%A3@@2861820._.ID_help@@%E6%96%87%E6%A1%A3@@2861820-RL_%E8%B4%A6%E5%8D%95%E8%AE%A2%E9%98%85-LOC_2024SPHelpResult-OR_ser-PAR1_2150419517478292121114501eaee8-V_4-RE_new5-P0_0-P1_0)
 * - This subscription and the Expenses and Costs - Billing Subscription are the same feature, and subscriptions are interchangeable.
 * - When subscribing to a directory under a bucket, follow the directory naming conventions:
 *     - Emojis are not allowed. Use compliant UTF-8 characters.
 *     - Forward slashes (/) are used to separate paths and can quickly create subdirectories. However, do not start with / or \\, and do not use consecutive forward slashes (/).
 *     - Subdirectories named .. are not allowed.
 *     - The total length must be 1 to 254 characters.
 * - File names:
 *     - Example: **consumeDetailBillV2** (billing item details)
 *     
 *         - Daily push file name format: `{Account UID}_{Site ID}_{Bill type}_{YYYYMM|YYYYMMDD}`, for example: `169**_2688801000001_consumeDetailBillV2_20190312`.
 *     
 *         - Full file name format at the beginning of the next month: `{Account UID}_{Site ID}_{Bill type}_{YYYYMM|YYYYMM}`, for example: `169**_2688801000001_consumeDetailBillV2_201903`.
 * - Monthly bill PDF files are in .pdf format, and other file types are in .csv format. When the data volume is large, the system automatically splits the exported bills into multiple files and compresses them into one or more .zip files. The .zip file name format is the same.
 *
 * @param request CreateReportDefinitionRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateReportDefinitionResponse
 */
@context("createReportDefinitionWithContext")
async function createReportDefinitionWithOptions(request: CreateReportDefinitionRequest, runtime: $RuntimeOptions): CreateReportDefinitionResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.beginBillingCycle)) {
    query['BeginBillingCycle'] = request.beginBillingCycle;
  }
  if (!$isNull(request.includeMembers)) {
    query['IncludeMembers'] = request.includeMembers;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  if (!$isNull(request.notSendOnNoData)) {
    query['NotSendOnNoData'] = request.notSendOnNoData;
  }
  if (!$isNull(request.ossBucketName)) {
    query['OssBucketName'] = request.ossBucketName;
  }
  if (!$isNull(request.ossBucketOwnerAccountId)) {
    query['OssBucketOwnerAccountId'] = request.ossBucketOwnerAccountId;
  }
  if (!$isNull(request.ossBucketPath)) {
    query['OssBucketPath'] = request.ossBucketPath;
  }
  if (!$isNull(request.reportType)) {
    query['ReportType'] = request.reportType;
  }
  if (!$isNull(request.sendWithAttach)) {
    query['SendWithAttach'] = request.sendWithAttach;
  }
  if (!$isNull(request.splitFileOnUserId)) {
    query['SplitFileOnUserId'] = request.splitFileOnUserId;
  }
  var body : map[string]any = {};
  if (!$isNull(request.mcProject)) {
    body['McProject'] = request.mcProject;
  }
  if (!$isNull(request.mcTableName)) {
    body['McTableName'] = request.mcTableName;
  }
  if (!$isNull(request.reportSourceType)) {
    body['ReportSourceType'] = request.reportSourceType;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateReportDefinition',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates a billing report subscription.
 *
 * @description When you call this API operation, note the following information:
 * - You can subscribe to one type of billing file at a time.
 * - Except for monthly bill PDFs, starting from the day after the subscription, the system pushes daily billing files that contain full detailed data from the beginning of the current month to the present. Before the 4th of each month, the system pushes full billing files for the complete billing cycle of the previous month.
 * - Monthly bill PDFs are pushed before the 4th of each month for the previous month.
 * - Billing files generated on a daily basis may have delays. Delayed billing files are pushed on the day after they are generated and may also contain bills that were delayed from before the previous day and generated on the previous day. We recommend that you pull the full files for the previous month at the beginning of each month.
 * > Apply for the required permissions by following the procedure described in the documentation: [Billing Subscription](https://help.aliyun.com/zh/user-center/user-guide/billing-subscription?spm=5176.21213303.J_v8LsmxMG6alneH-O7TCPa.1.3ef82f3d5ZIf08&scm=20140722.S_help@@%E6%96%87%E6%A1%A3@@2861820._.ID_help@@%E6%96%87%E6%A1%A3@@2861820-RL_%E8%B4%A6%E5%8D%95%E8%AE%A2%E9%98%85-LOC_2024SPHelpResult-OR_ser-PAR1_2150419517478292121114501eaee8-V_4-RE_new5-P0_0-P1_0)
 * - This subscription and the Expenses and Costs - Billing Subscription are the same feature, and subscriptions are interchangeable.
 * - When subscribing to a directory under a bucket, follow the directory naming conventions:
 *     - Emojis are not allowed. Use compliant UTF-8 characters.
 *     - Forward slashes (/) are used to separate paths and can quickly create subdirectories. However, do not start with / or \\, and do not use consecutive forward slashes (/).
 *     - Subdirectories named .. are not allowed.
 *     - The total length must be 1 to 254 characters.
 * - File names:
 *     - Example: **consumeDetailBillV2** (billing item details)
 *     
 *         - Daily push file name format: `{Account UID}_{Site ID}_{Bill type}_{YYYYMM|YYYYMMDD}`, for example: `169**_2688801000001_consumeDetailBillV2_20190312`.
 *     
 *         - Full file name format at the beginning of the next month: `{Account UID}_{Site ID}_{Bill type}_{YYYYMM|YYYYMM}`, for example: `169**_2688801000001_consumeDetailBillV2_201903`.
 * - Monthly bill PDF files are in .pdf format, and other file types are in .csv format. When the data volume is large, the system automatically splits the exported bills into multiple files and compresses them into one or more .zip files. The .zip file name format is the same.
 *
 * @param request CreateReportDefinitionRequest
 * @return CreateReportDefinitionResponse
 */
async function createReportDefinition(request: CreateReportDefinitionRequest): CreateReportDefinitionResponse {
  var runtime = new $RuntimeOptions{};
  return createReportDefinitionWithOptions(request, runtime);
}

model DeleteBudgetRequest {
  budgetName?: string(name='BudgetName', description='The budget name.

This parameter is required.', example='department1'),
  nbid?: string(name='Nbid', description='The ID of the primary marketplace. If this parameter is left empty, the marketplace ID of the current user is used by default.', example='2684201000001'),
}

model DeleteBudgetResponseBody = {
  budgetName?: string(name='BudgetName', description='The budget name.', example='department1'),
  requestId?: string(name='RequestId', description='The request ID.', example='7EA6C02D-06D0-4213-9C3B-E67910F7D1EB'),
}

model DeleteBudgetResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DeleteBudgetResponseBody(name='body'),
}

/**
 * @summary Deletes a budget.
 *
 * @param request DeleteBudgetRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteBudgetResponse
 */
@context("deleteBudgetWithContext")
async function deleteBudgetWithOptions(request: DeleteBudgetRequest, runtime: $RuntimeOptions): DeleteBudgetResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  var body : map[string]any = {};
  if (!$isNull(request.budgetName)) {
    body['BudgetName'] = request.budgetName;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'DeleteBudget',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Deletes a budget.
 *
 * @param request DeleteBudgetRequest
 * @return DeleteBudgetResponse
 */
async function deleteBudget(request: DeleteBudgetRequest): DeleteBudgetResponse {
  var runtime = new $RuntimeOptions{};
  return deleteBudgetWithOptions(request, runtime);
}

model DeleteCostCenterRequest {
  costCenterId?: long(name='CostCenterId', description='Cost center ID.

This parameter is required.', example='485938'),
  nbid?: string(name='Nbid', description='First-level marketplace ID. If left empty, the marketplace ID of the current user is used by default.', example='2684201000001'),
  ownerAccountId?: long(name='OwnerAccountId', description='User ID of the cost center owner.

This parameter is required.', example='1314839403940987'),
}

model DeleteCostCenterResponseBody = {
  costCenterId?: long(name='CostCenterId', description='Cost center ID.', example='485938'),
  isSuccess?: boolean(name='IsSuccess', description='Indicates whether the operation is successful.', example='True'),
  metadata?: any(name='Metadata', description='Response metadata.', example='{}'),
  ownerAccountId?: long(name='OwnerAccountId', description='User ID of the cost center owner.', example='1314839403940987'),
  requestId?: string(name='RequestId', description='Request ID.', example='C1BD134E-D914-6AE0-1901-AEB2A99FA205'),
}

model DeleteCostCenterResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DeleteCostCenterResponseBody(name='body'),
}

/**
 * @summary Delete Cost Center
 *
 * @description This API is in canary release and is only available to whitelisted users. Excessive calls may cause performance issues such as response timeouts.
 *
 * @param request DeleteCostCenterRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteCostCenterResponse
 */
@context("deleteCostCenterWithContext")
async function deleteCostCenterWithOptions(request: DeleteCostCenterRequest, runtime: $RuntimeOptions): DeleteCostCenterResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.costCenterId)) {
    query['CostCenterId'] = request.costCenterId;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  if (!$isNull(request.ownerAccountId)) {
    query['OwnerAccountId'] = request.ownerAccountId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DeleteCostCenter',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Delete Cost Center
 *
 * @description This API is in canary release and is only available to whitelisted users. Excessive calls may cause performance issues such as response timeouts.
 *
 * @param request DeleteCostCenterRequest
 * @return DeleteCostCenterResponse
 */
async function deleteCostCenter(request: DeleteCostCenterRequest): DeleteCostCenterResponse {
  var runtime = new $RuntimeOptions{};
  return deleteCostCenterWithOptions(request, runtime);
}

model DeleteCostCenterRuleRequest {
  costCenterId?: long(name='CostCenterId', description='Financial unit ID.', example='637127'),
  filterExpression?: {
    expressionType?: string(name='ExpressionType', description='The operation type.', example='NARY'),
    filterValues?: {
      code?: string(name='Code', description='The condition filter key.', example='TAG-test-xxx-key'),
      codeName?: string(name='CodeName', description='The condition filter key name.', example='无'),
      selectType?: string(name='SelectType', description='The association between Code and value.', example='IN'),
      values?: [ string ](name='Values', description='The condition filter values.'),
    }(name='FilterValues', description='The sub-expression filter conditions.'),
    operand?: any(name='Operand', description='The filter condition (no longer used).', example='无'),
    operands?: [ any ](name='Operands', description='The conditional expressions.'),
    operatorType?: string(name='OperatorType', description='The relational expression.', example='AND'),
  }(name='FilterExpression', description='Rule expression.
**This field does not need to be entered during the delete operation.**'),
  nbid?: string(name='Nbid', description='Level-1 marketplace ID. If empty, the marketplace ID of the current user is used by default.', example='2684201000001'),
}

model DeleteCostCenterRuleShrinkRequest {
  costCenterId?: long(name='CostCenterId', description='Financial unit ID.', example='637127'),
  filterExpressionShrink?: string(name='FilterExpression', description='Rule expression.
**This field does not need to be entered during the delete operation.**'),
  nbid?: string(name='Nbid', description='Level-1 marketplace ID. If empty, the marketplace ID of the current user is used by default.', example='2684201000001'),
}

model DeleteCostCenterRuleResponseBody = {
  data?: long(name='Data', description='The ID of the financial unit allocation rule.', example='37404'),
  metadata?: any(name='Metadata', description='The response metadata.', example='{}'),
  requestId?: string(name='RequestId', description='The request ID.', example='UUID'),
}

model DeleteCostCenterRuleResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DeleteCostCenterRuleResponseBody(name='body'),
}

/**
 * @summary Delete financial unit automatic allocation rule
 *
 * @description This API is in canary release and is only available to whitelisted users. Excessive calls may cause performance issues such as response timeouts.
 *
 * @param tmpReq DeleteCostCenterRuleRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteCostCenterRuleResponse
 */
@context("deleteCostCenterRuleWithContext")
async function deleteCostCenterRuleWithOptions(tmpReq: DeleteCostCenterRuleRequest, runtime: $RuntimeOptions): DeleteCostCenterRuleResponse {
  tmpReq.validate();
  var request = new DeleteCostCenterRuleShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.filterExpression)) {
    request.filterExpressionShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.filterExpression, 'FilterExpression', 'json');
  }
  var query = {};
  if (!$isNull(request.filterExpressionShrink)) {
    query['FilterExpression'] = request.filterExpressionShrink;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  var body : map[string]any = {};
  if (!$isNull(request.costCenterId)) {
    body['CostCenterId'] = request.costCenterId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'DeleteCostCenterRule',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Delete financial unit automatic allocation rule
 *
 * @description This API is in canary release and is only available to whitelisted users. Excessive calls may cause performance issues such as response timeouts.
 *
 * @param request DeleteCostCenterRuleRequest
 * @return DeleteCostCenterRuleResponse
 */
async function deleteCostCenterRule(request: DeleteCostCenterRuleRequest): DeleteCostCenterRuleResponse {
  var runtime = new $RuntimeOptions{};
  return deleteCostCenterRuleWithOptions(request, runtime);
}

model DeleteCouponDeductTagRequest {
  couponId?: string(name='CouponId'),
  ecIdAccountIds?: [ 
    {
      accountIds?: [ long ](name='AccountIds'),
      ecId?: string(name='EcId', description='This parameter is required.'),
    }
  ](name='EcIdAccountIds'),
  nbid?: string(name='Nbid'),
  tagKeys?: [ string ](name='TagKeys'),
}

model DeleteCouponDeductTagShrinkRequest {
  couponId?: string(name='CouponId'),
  ecIdAccountIdsShrink?: string(name='EcIdAccountIds'),
  nbid?: string(name='Nbid'),
  tagKeysShrink?: string(name='TagKeys'),
}

model DeleteCouponDeductTagResponseBody = {
  data?: boolean(name='Data'),
  requestId?: string(name='RequestId'),
}

model DeleteCouponDeductTagResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DeleteCouponDeductTagResponseBody(name='body'),
}

/**
 * @summary 删除优惠券的抵扣标签
 *
 * @param tmpReq DeleteCouponDeductTagRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteCouponDeductTagResponse
 */
@context("deleteCouponDeductTagWithContext")
async function deleteCouponDeductTagWithOptions(tmpReq: DeleteCouponDeductTagRequest, runtime: $RuntimeOptions): DeleteCouponDeductTagResponse {
  tmpReq.validate();
  var request = new DeleteCouponDeductTagShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.ecIdAccountIds)) {
    request.ecIdAccountIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.ecIdAccountIds, 'EcIdAccountIds', 'json');
  }
  if (!$isNull(tmpReq.tagKeys)) {
    request.tagKeysShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.tagKeys, 'TagKeys', 'json');
  }
  var query = {};
  if (!$isNull(request.couponId)) {
    query['CouponId'] = request.couponId;
  }
  if (!$isNull(request.ecIdAccountIdsShrink)) {
    query['EcIdAccountIds'] = request.ecIdAccountIdsShrink;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  if (!$isNull(request.tagKeysShrink)) {
    query['TagKeys'] = request.tagKeysShrink;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DeleteCouponDeductTag',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 删除优惠券的抵扣标签
 *
 * @param request DeleteCouponDeductTagRequest
 * @return DeleteCouponDeductTagResponse
 */
async function deleteCouponDeductTag(request: DeleteCouponDeductTagRequest): DeleteCouponDeductTagResponse {
  var runtime = new $RuntimeOptions{};
  return deleteCouponDeductTagWithOptions(request, runtime);
}

model DeleteReportDefinitionRequest {
  nbid?: string(name='Nbid', description='The ID of the primary sales channel. If this parameter is left empty, the ID of the sales channel to which the current user belongs is used by default.', example='2684201000001'),
  reportTaskId?: long(name='ReportTaskId', description='The ID of the bill subscription task.

This parameter is required.', example='123123'),
}

model DeleteReportDefinitionResponseBody = {
  data?: boolean(name='Data', description='Indicates whether the subscription is deleted.', example='true'),
  metadata?: any(name='Metadata', description='The metadata of the response struct.', example='{}'),
  requestId?: string(name='RequestId', description='The request ID.', example='79EE7556-0CFD-44EB-9CD6-B3B526E3A85F'),
}

model DeleteReportDefinitionResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DeleteReportDefinitionResponseBody(name='body'),
}

/**
 * @summary Deletes a bill report export subscription.
 *
 * @param request DeleteReportDefinitionRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteReportDefinitionResponse
 */
@context("deleteReportDefinitionWithContext")
async function deleteReportDefinitionWithOptions(request: DeleteReportDefinitionRequest, runtime: $RuntimeOptions): DeleteReportDefinitionResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  if (!$isNull(request.reportTaskId)) {
    query['ReportTaskId'] = request.reportTaskId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DeleteReportDefinition',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Deletes a bill report export subscription.
 *
 * @param request DeleteReportDefinitionRequest
 * @return DeleteReportDefinitionResponse
 */
async function deleteReportDefinition(request: DeleteReportDefinitionRequest): DeleteReportDefinitionResponse {
  var runtime = new $RuntimeOptions{};
  return deleteReportDefinitionWithOptions(request, runtime);
}

model DescribeBudgetRequest {
  budgetName?: string(name='BudgetName', description='Budget name.

This parameter is required.', example='department1'),
  nbid?: string(name='Nbid', description='Primary marketplace ID. If left empty, the marketplace ID of the current user is used by default.', example='2684201000001'),
}

model DescribeBudgetResponseBody = {
  budgetName?: string(name='BudgetName', description='Budget name.', example='department1'),
  budgetType?: string(name='BudgetType', description='Budget type.', example='CONSUME'),
  comment?: string(name='Comment', description='Remarks.', example='comment'),
  cycleEndPeriod?: string(name='CycleEndPeriod', description='End cycle period.', example='2026-12'),
  cycleQuota?: [ 
    {
      cyclePeriod?: string(name='CyclePeriod', description='Cycle period.', example='202601'),
      quota?: string(name='Quota', description='Quota.', example='100'),
    }
  ](name='CycleQuota', description='Quota specified per cycle.'),
  cycleStartPeriod?: string(name='CycleStartPeriod', description='Start cycle period.', example='2025-10'),
  cycleType?: string(name='CycleType', description='Cycle type.', example='MONTHLY'),
  ecIdAccountIdsFilter?: {
    accountIds?: [ long ](name='AccountIds', description='Member account IDs.'),
    ecId?: string(name='EcId', description='Enterprise entity ID.', example='E2024112210463400001'),
  }(name='EcIdAccountIdsFilter', description='Enterprise multi-account filter conditions.'),
  metadata?: any(name='Metadata', description='Response structure metadata.', example='{}'),
  metric?: string(name='Metric', description='Budget metric.', example='REQUIRE_AMOUNT'),
  queryFilter?: [ 
    {
      code?: string(name='Code', description='Parameter code.', example='RESOURCE_OWNER_ACCOUNT'),
      selectType?: string(name='SelectType', description='Selection mode.', example='IN'),
      values?: [ string ](name='Values', description='Filter value list.'),
    }
  ](name='QueryFilter', description='Filter conditions.'),
  quota?: string(name='Quota', description='Fixed quota value.', example='1000'),
  quotaType?: string(name='QuotaType', description='Quota type.', example='FIXED'),
  requestId?: string(name='RequestId', description='Request ID.', example='A1B2C3D4-E5F6-7890-ABCD-EF1234567890'),
  warnConfs?: [ 
    {
      comment?: string(name='Comment', description='Remarks.', example='comment'),
      eventBridge?: boolean(name='EventBridge', description='Specifies whether to enable EventBridge.'),
      mscChannels?: [ string ](name='MscChannels', description='Message center notification channel list.'),
      mscContacts?: [ string ](name='MscContacts', description='Message center contact list.'),
      name?: string(name='Name', description='Alert name. User-defined and optional. If not specified, the backend automatically generates a name.', example='Alter-1'),
      sequence?: int32(name='Sequence', description='Sequence number. Only present in responses. Alerts are numbered in ascending order of alert ID, starting from 1.', example='1'),
      thresholdType?: string(name='ThresholdType', description='Threshold type.', example='FIXED'),
      thresholdValue?: string(name='ThresholdValue', description='Threshold value.', example='1000'),
      warnTarget?: string(name='WarnTarget', description='Alert target.', example='ACTUAL'),
    }
  ](name='WarnConfs', description='Alert configurations.'),
}

model DescribeBudgetResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeBudgetResponseBody(name='body'),
}

/**
 * @summary Query a Single Budget
 *
 * @param request DescribeBudgetRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeBudgetResponse
 */
@context("describeBudgetWithContext")
async function describeBudgetWithOptions(request: DescribeBudgetRequest, runtime: $RuntimeOptions): DescribeBudgetResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  var body : map[string]any = {};
  if (!$isNull(request.budgetName)) {
    body['BudgetName'] = request.budgetName;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeBudget',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Query a Single Budget
 *
 * @param request DescribeBudgetRequest
 * @return DescribeBudgetResponse
 */
async function describeBudget(request: DescribeBudgetRequest): DescribeBudgetResponse {
  var runtime = new $RuntimeOptions{};
  return describeBudgetWithOptions(request, runtime);
}

model DescribeBudgetsRequest {
  budgetName?: string(name='BudgetName', description='The budget name. Fuzzy search is supported.', example='department1'),
  budgetType?: string(name='BudgetType', description='The budget type.', example='CONSUME'),
  expireStatus?: string(name='ExpireStatus', description='The expiration status.', example='NOT_EXPIRED'),
  nbid?: string(name='Nbid', description='The primary marketplace ID. If this parameter is left empty, the ID of the marketplace to which the current user belongs is used by default.', example='2688801000001'),
  pageNo?: int32(name='PageNo', description='Page number.', example='1'),
  pageSize?: int32(name='PageSize', description='The number of entries per page.', example='10'),
}

model DescribeBudgetsResponseBody = {
  currentPage?: int32(name='CurrentPage', description='The current page number.', example='1'),
  data?: [ 
    {
      budgetName?: string(name='BudgetName', description='The budget name.', example='department1-test'),
      budgetType?: string(name='BudgetType', description='The budget type.', example='CONSUME'),
      comment?: string(name='Comment', description='The remarks.', example='budget-1'),
      cycleEndPeriod?: string(name='CycleEndPeriod', description='The end period.', example='2026-12'),
      cycleQuota?: [ 
        {
          cyclePeriod?: string(name='CyclePeriod', description='The cycle period.', example='202601'),
          quota?: string(name='Quota', description='The quota.', example='100'),
        }
      ](name='CycleQuota', description='The specified quota per cycle.'),
      cycleStartPeriod?: string(name='CycleStartPeriod', description='The start period.', example='2025-10'),
      cycleType?: string(name='CycleType', description='The cycle type.', example='MONTHLY'),
      ecIdAccountIdsFilter?: {
        accountIds?: [ long ](name='AccountIds', description='The member account IDs.'),
        ecId?: string(name='EcId', description='The enterprise entity ID.', example='E2024112210463400001'),
      }(name='EcIdAccountIdsFilter', description='The enterprise multi-account filter conditions.'),
      expireStatus?: string(name='ExpireStatus', description='The expiration status.', example='NOT_EXPIRED'),
      metric?: string(name='Metric', description='The budget metric.', example='REQUIRE_AMOUNT'),
      queryFilter?: [ 
        {
          code?: string(name='Code', description='The parameter code.', example='RESOURCE_OWNER_ACCOUNT'),
          selectType?: string(name='SelectType', description='The selection mode.', example='IN'),
          values?: [ string ](name='Values', description='The filter value list.'),
        }
      ](name='QueryFilter', description='The filter conditions.'),
      quota?: string(name='Quota', description='The fixed quota value.', example='10000'),
      quotaType?: string(name='QuotaType', description='The quota type.', example='FIXED'),
      warnConfs?: [ 
        {
          comment?: string(name='Comment', description='The remarks.', example='comment1'),
          eventBridge?: boolean(name='EventBridge', description='Indicates whether EventBridge is enabled.'),
          mscChannels?: [ string ](name='MscChannels', description='The list of message center notification channels.'),
          mscContacts?: [ string ](name='MscContacts', description='The list of message center contacts.'),
          name?: string(name='Name', description='The alert name. This parameter is user-defined and optional. If not specified, the backend automatically generates a name.', example='Alert-1'),
          sequence?: int32(name='Sequence', description='The sort sequence number. This parameter is present only in responses. Alerts are numbered in ascending order by alert ID, starting from 1.', example='1'),
          thresholdType?: string(name='ThresholdType', description='The threshold type.', example='FIXED'),
          thresholdValue?: string(name='ThresholdValue', description='The threshold value.', example='100'),
          warnTarget?: string(name='WarnTarget', description='The alert target.', example='ACTUAL'),
        }
      ](name='WarnConfs', description='The alert configurations.'),
    }
  ](name='Data', description='The data list.'),
  pageSize?: int32(name='PageSize', description='The number of entries per page.', example='10'),
  requestId?: string(name='RequestId', description='The request ID.', example='DB2A9097-289C-11CE-AA74-235FCFD39204'),
  totalCount?: int32(name='TotalCount', description='The total number of records.', example='1'),
}

model DescribeBudgetsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeBudgetsResponseBody(name='body'),
}

/**
 * @summary Query budget list
 *
 * @param request DescribeBudgetsRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeBudgetsResponse
 */
@context("describeBudgetsWithContext")
async function describeBudgetsWithOptions(request: DescribeBudgetsRequest, runtime: $RuntimeOptions): DescribeBudgetsResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  var body : map[string]any = {};
  if (!$isNull(request.budgetName)) {
    body['BudgetName'] = request.budgetName;
  }
  if (!$isNull(request.budgetType)) {
    body['BudgetType'] = request.budgetType;
  }
  if (!$isNull(request.expireStatus)) {
    body['ExpireStatus'] = request.expireStatus;
  }
  if (!$isNull(request.pageNo)) {
    body['PageNo'] = request.pageNo;
  }
  if (!$isNull(request.pageSize)) {
    body['PageSize'] = request.pageSize;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeBudgets',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Query budget list
 *
 * @param request DescribeBudgetsRequest
 * @return DescribeBudgetsResponse
 */
async function describeBudgets(request: DescribeBudgetsRequest): DescribeBudgetsResponse {
  var runtime = new $RuntimeOptions{};
  return describeBudgetsWithOptions(request, runtime);
}

model DescribeCouponRequest {
  couponId?: long(name='CouponId', description='The coupon ID.', example='351430260343'),
  couponNo?: string(name='CouponNo', description='The coupon code.', example='554863270150'),
  couponTemplateIdList?: [ long ](name='CouponTemplateIdList'),
  couponType?: string(name='CouponType', description='The coupon type.', example='CERTAIN'),
  currentPage?: int32(name='CurrentPage', description='The current page number.

This parameter is required.', example='1'),
  ecIdAccountIds?: [ 
    {
      accountIds?: [ long ](name='AccountIds', description='The list of accessed accounts. If this parameter is left empty, all accounts under the current entity ID are selected.'),
      ecId?: string(name='EcId', description='The enterprise entity ID.', example='1501603440974415'),
    }
  ](name='EcIdAccountIds', description='The enterprise and account list. If this parameter is left empty, the current account is queried.'),
  effectiveEndTime?: long(name='EffectiveEndTime', description='The effective end time.', example='1708423156000'),
  effectiveStartTime?: long(name='EffectiveStartTime', description='The effective start time.', example='1684750028000'),
  expireEndDate?: long(name='ExpireEndDate', description='The expiration end time.', example='1708423156000'),
  expireStartDate?: long(name='ExpireStartDate', description='The expiration start time.', example='1684750028000'),
  includeShare?: boolean(name='IncludeShare'),
  maxResults?: int32(name='MaxResults'),
  nbid?: string(name='Nbid', description='The primary campaign information.', example='2684201000001'),
  nextToken?: string(name='NextToken'),
  pageSize?: int32(name='PageSize', description='The number of entries per page.

This parameter is required.', example='20'),
  productCode?: string(name='ProductCode'),
  status?: string(name='Status', description='The status.', example='AVAILABLE'),
}

model DescribeCouponShrinkRequest {
  couponId?: long(name='CouponId', description='The coupon ID.', example='351430260343'),
  couponNo?: string(name='CouponNo', description='The coupon code.', example='554863270150'),
  couponTemplateIdListShrink?: string(name='CouponTemplateIdList'),
  couponType?: string(name='CouponType', description='The coupon type.', example='CERTAIN'),
  currentPage?: int32(name='CurrentPage', description='The current page number.

This parameter is required.', example='1'),
  ecIdAccountIdsShrink?: string(name='EcIdAccountIds', description='The enterprise and account list. If this parameter is left empty, the current account is queried.'),
  effectiveEndTime?: long(name='EffectiveEndTime', description='The effective end time.', example='1708423156000'),
  effectiveStartTime?: long(name='EffectiveStartTime', description='The effective start time.', example='1684750028000'),
  expireEndDate?: long(name='ExpireEndDate', description='The expiration end time.', example='1708423156000'),
  expireStartDate?: long(name='ExpireStartDate', description='The expiration start time.', example='1684750028000'),
  includeShare?: boolean(name='IncludeShare'),
  maxResults?: int32(name='MaxResults'),
  nbid?: string(name='Nbid', description='The primary campaign information.', example='2684201000001'),
  nextToken?: string(name='NextToken'),
  pageSize?: int32(name='PageSize', description='The number of entries per page.

This parameter is required.', example='20'),
  productCode?: string(name='ProductCode'),
  status?: string(name='Status', description='The status.', example='AVAILABLE'),
}

model DescribeCouponResponseBody = {
  currentPage?: int32(name='CurrentPage', description='The current page number.', example='1'),
  data?: [ 
    {
      amount?: string(name='Amount', description='The face value.', example='9929.750000'),
      certainAmount?: string(name='CertainAmount'),
      couponId?: long(name='CouponId', description='The coupon ID.', example='59243658'),
      couponNo?: string(name='CouponNo', description='The coupon code.', example='731074910070'),
      couponType?: string(name='CouponType', description='The coupon type.', example='CERTAIN'),
      couponTypeName?: string(name='CouponTypeName', description='The coupon type name.', example='满减券'),
      currency?: string(name='Currency', description='The currency.', example='CNY'),
      endTime?: string(name='EndTime', description='The end time.', example='2021-03-06T15:12Z'),
      firstBuy?: boolean(name='FirstBuy', description='Indicates whether there is a first purchase restriction.'),
      gmtCreate?: string(name='GmtCreate', description='The coupon issuance time.', example='2021-03-02T15:12Z'),
      itemNames?: [ string ](name='ItemNames', description='The product code list.'),
      moneyLimit?: string(name='MoneyLimit', description='The amount limit.', example='无订单金额限制'),
      orderTimeRule?: string(name='OrderTimeRule', description='The order duration limit rule.', example='预付费规则：购买订单时长大于3600s才能使用'),
      remainAmount?: string(name='RemainAmount', description='The remaining amount.', example='100.00'),
      remark?: string(name='Remark', description='The remarks.', example='新买28号'),
      shareUidList?: [ 
        {
          uid?: string(name='Uid', description='The Alibaba Cloud UID.', example='1902671110151254'),
          userNick?: string(name='UserNick', description='The Alibaba Cloud account.', example='阿里云计算有限公司'),
        }
      ](name='ShareUidList', description='The shared account list.'),
      showSetDeductTagButton?: boolean(name='ShowSetDeductTagButton', description='Indicates whether to display the tag deduction button.', example='true'),
      site?: string(name='Site', description='The site.', example='CHINA'),
      siteName?: string(name='SiteName', description='The site name.', example='官网自营'),
      startTime?: string(name='StartTime', description='The start time.', example='2021-03-02T15:12Z'),
      status?: string(name='Status', description='The status.', example='Available'),
      suitAccount?: string(name='SuitAccount', description='The applicable account.', example='1902671110151254'),
      suitItemType?: string(name='SuitItemType', description='The applicable product type. Valid values: all, which indicates that the coupon is applicable to all products. white, which indicates that the coupon is applicable to specified products. black, which indicates that the coupon is not applicable to specified products.', example='all'),
      universalType?: string(name='UniversalType', description='The coupon applicable scope.', example='UNIVERSAL'),
      yhOrderTypes?: [ string ](name='YhOrderTypes', description='The list of order types applicable to the coupon.'),
    }
  ](name='Data', description='The data list.'),
  pageSize?: int32(name='PageSize', description='The number of entries per page.', example='20'),
  requestId?: string(name='RequestId', description='The request ID.', example='C880B065-A781-4F19-B6DD-3E0E3B715C64'),
  totalCount?: int32(name='TotalCount', description='The total number of records.', example='2'),
}

model DescribeCouponResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeCouponResponseBody(name='body'),
}

/**
 * @summary Queries a list of coupons.
 *
 * @param tmpReq DescribeCouponRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeCouponResponse
 */
@context("describeCouponWithContext")
async function describeCouponWithOptions(tmpReq: DescribeCouponRequest, runtime: $RuntimeOptions): DescribeCouponResponse {
  tmpReq.validate();
  var request = new DescribeCouponShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.couponTemplateIdList)) {
    request.couponTemplateIdListShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.couponTemplateIdList, 'CouponTemplateIdList', 'json');
  }
  if (!$isNull(tmpReq.ecIdAccountIds)) {
    request.ecIdAccountIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.ecIdAccountIds, 'EcIdAccountIds', 'json');
  }
  var query = {};
  if (!$isNull(request.couponId)) {
    query['CouponId'] = request.couponId;
  }
  if (!$isNull(request.couponNo)) {
    query['CouponNo'] = request.couponNo;
  }
  if (!$isNull(request.couponTemplateIdListShrink)) {
    query['CouponTemplateIdList'] = request.couponTemplateIdListShrink;
  }
  if (!$isNull(request.couponType)) {
    query['CouponType'] = request.couponType;
  }
  if (!$isNull(request.currentPage)) {
    query['CurrentPage'] = request.currentPage;
  }
  if (!$isNull(request.ecIdAccountIdsShrink)) {
    query['EcIdAccountIds'] = request.ecIdAccountIdsShrink;
  }
  if (!$isNull(request.effectiveEndTime)) {
    query['EffectiveEndTime'] = request.effectiveEndTime;
  }
  if (!$isNull(request.effectiveStartTime)) {
    query['EffectiveStartTime'] = request.effectiveStartTime;
  }
  if (!$isNull(request.expireEndDate)) {
    query['ExpireEndDate'] = request.expireEndDate;
  }
  if (!$isNull(request.expireStartDate)) {
    query['ExpireStartDate'] = request.expireStartDate;
  }
  if (!$isNull(request.includeShare)) {
    query['IncludeShare'] = request.includeShare;
  }
  if (!$isNull(request.maxResults)) {
    query['MaxResults'] = request.maxResults;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  if (!$isNull(request.nextToken)) {
    query['NextToken'] = request.nextToken;
  }
  if (!$isNull(request.pageSize)) {
    query['PageSize'] = request.pageSize;
  }
  if (!$isNull(request.productCode)) {
    query['ProductCode'] = request.productCode;
  }
  if (!$isNull(request.status)) {
    query['Status'] = request.status;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeCoupon',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries a list of coupons.
 *
 * @param request DescribeCouponRequest
 * @return DescribeCouponResponse
 */
async function describeCoupon(request: DescribeCouponRequest): DescribeCouponResponse {
  var runtime = new $RuntimeOptions{};
  return describeCouponWithOptions(request, runtime);
}

model DescribeCouponItemListRequest {
  couponId?: long(name='CouponId', description='The coupon ID.', example='59104570'),
  currentPage?: int32(name='CurrentPage', description='The current page number.', example='1'),
  ecIdAccountIds?: [ 
    {
      accountIds?: [ long ](name='AccountIds', description='The list of accounts to access. If this parameter is left empty, all accounts under the current entity ID are selected.'),
      ecId?: string(name='EcId', description='The enterprise entity ID.

This parameter is required.', example='1004064243473974'),
    }
  ](name='EcIdAccountIds', description='The list of enterprise entities and accounts. If this parameter is left empty, the current account is queried.'),
  name?: string(name='Name', description='The product name. Fuzzy matching is supported.', example='云服务器ECS'),
  nbid?: string(name='Nbid', description='The level-1 sales channel ID. If this parameter is left empty, the channel ID of the current user is used by default.', example='2684201000001'),
  pageSize?: int32(name='PageSize', description='The number of entries per page.', example='20'),
}

model DescribeCouponItemListShrinkRequest {
  couponId?: long(name='CouponId', description='The coupon ID.', example='59104570'),
  currentPage?: int32(name='CurrentPage', description='The current page number.', example='1'),
  ecIdAccountIdsShrink?: string(name='EcIdAccountIds', description='The list of enterprise entities and accounts. If this parameter is left empty, the current account is queried.'),
  name?: string(name='Name', description='The product name. Fuzzy matching is supported.', example='云服务器ECS'),
  nbid?: string(name='Nbid', description='The level-1 sales channel ID. If this parameter is left empty, the channel ID of the current user is used by default.', example='2684201000001'),
  pageSize?: int32(name='PageSize', description='The number of entries per page.', example='20'),
}

model DescribeCouponItemListResponseBody = {
  currentPage?: int32(name='CurrentPage', description='The current page number.', example='1'),
  data?: [ 
    {
      code?: string(name='Code', description='The product code.', example='vm'),
      name?: string(name='Name', description='The product name.', example='云服务器ECS'),
    }
  ](name='Data', description='The data list.'),
  pageSize?: int32(name='PageSize', description='The number of entries per page.', example='20'),
  requestId?: string(name='RequestId', description='The request ID.', example='EAE08A27-386C-579E-966D-8853EC3C5D0E'),
  totalCount?: int32(name='TotalCount', description='The total number of records.', example='5'),
}

model DescribeCouponItemListResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeCouponItemListResponseBody(name='body'),
}

/**
 * @summary Queries the list of products available for a coupon.
 *
 * @param tmpReq DescribeCouponItemListRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeCouponItemListResponse
 */
@context("describeCouponItemListWithContext")
async function describeCouponItemListWithOptions(tmpReq: DescribeCouponItemListRequest, runtime: $RuntimeOptions): DescribeCouponItemListResponse {
  tmpReq.validate();
  var request = new DescribeCouponItemListShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.ecIdAccountIds)) {
    request.ecIdAccountIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.ecIdAccountIds, 'EcIdAccountIds', 'json');
  }
  var query = {};
  if (!$isNull(request.couponId)) {
    query['CouponId'] = request.couponId;
  }
  if (!$isNull(request.currentPage)) {
    query['CurrentPage'] = request.currentPage;
  }
  if (!$isNull(request.ecIdAccountIdsShrink)) {
    query['EcIdAccountIds'] = request.ecIdAccountIdsShrink;
  }
  if (!$isNull(request.name)) {
    query['Name'] = request.name;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  if (!$isNull(request.pageSize)) {
    query['PageSize'] = request.pageSize;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeCouponItemList',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the list of products available for a coupon.
 *
 * @param request DescribeCouponItemListRequest
 * @return DescribeCouponItemListResponse
 */
async function describeCouponItemList(request: DescribeCouponItemListRequest): DescribeCouponItemListResponse {
  var runtime = new $RuntimeOptions{};
  return describeCouponItemListWithOptions(request, runtime);
}

model DescribeDeductLogsRequest {
  billInstanceId?: string(name='BillInstanceId', description='The instance ID for billing deduction.', example='ecs-******'),
  billingCommodityCode?: string(name='BillingCommodityCode', description='The commodity code of the deducted item.', example='snapshot'),
  billingEndTime?: long(name='BillingEndTime', description='The billing end time.

This parameter is required.', example='1679068799999'),
  billingStartTime?: long(name='BillingStartTime', description='The billing start time.

This parameter is required.', example='1647446400000'),
  commodityCode?: string(name='CommodityCode', description='The commodity code.', example='ossbag'),
  ecIdAccountIds?: [ 
    {
      accountIds?: [ long ](name='AccountIds', description='The list of accounts to access. If this parameter is empty, all accounts under the current entity ID are selected.'),
      ecId?: string(name='EcId', description='The enterprise entity ID.

This parameter is required.', example='1004064243473974'),
    }
  ](name='EcIdAccountIds', description='The enterprise and account list. If this parameter is empty, the current account is queried.'),
  group?: string(name='Group', description='The resource dimension for the query.', example='oss_rc'),
  instanceId?: string(name='InstanceId', description='The instance name.', example='OSSBAG-cn******'),
  nbid?: string(name='Nbid', description='The primary marketplace ID. If this parameter is empty, the marketplace ID of the current user is used by default.', example='2684201000001'),
  pageNum?: int32(name='PageNum', description='The current page number.', example='1'),
  pageSize?: int32(name='PageSize', description='The number of entries per page.', example='20'),
  relationAccountIds?: [ long ](name='RelationAccountIds', description='The list of deduction accounts.'),
}

model DescribeDeductLogsShrinkRequest {
  billInstanceId?: string(name='BillInstanceId', description='The instance ID for billing deduction.', example='ecs-******'),
  billingCommodityCode?: string(name='BillingCommodityCode', description='The commodity code of the deducted item.', example='snapshot'),
  billingEndTime?: long(name='BillingEndTime', description='The billing end time.

This parameter is required.', example='1679068799999'),
  billingStartTime?: long(name='BillingStartTime', description='The billing start time.

This parameter is required.', example='1647446400000'),
  commodityCode?: string(name='CommodityCode', description='The commodity code.', example='ossbag'),
  ecIdAccountIdsShrink?: string(name='EcIdAccountIds', description='The enterprise and account list. If this parameter is empty, the current account is queried.'),
  group?: string(name='Group', description='The resource dimension for the query.', example='oss_rc'),
  instanceId?: string(name='InstanceId', description='The instance name.', example='OSSBAG-cn******'),
  nbid?: string(name='Nbid', description='The primary marketplace ID. If this parameter is empty, the marketplace ID of the current user is used by default.', example='2684201000001'),
  pageNum?: int32(name='PageNum', description='The current page number.', example='1'),
  pageSize?: int32(name='PageSize', description='The number of entries per page.', example='20'),
  relationAccountIdsShrink?: string(name='RelationAccountIds', description='The list of deduction accounts.'),
}

model DescribeDeductLogsResponseBody = {
  currentPage?: int32(name='CurrentPage', description='The current page number.', example='1'),
  data?: [ 
    {
      accountId?: long(name='AccountId', description='The account ID.', example='1929817951466001'),
      accountName?: string(name='AccountName', description='The account name.', example='icloudtest'),
      billingCommodity?: {
        code?: string(name='Code', description='The property code.', example='snapshot'),
        name?: string(name='Name', description='The property name.', example='云服务器ECS-快照'),
      }(name='BillingCommodity', description='The deducted commodity.'),
      billingCommodityCode?: string(name='BillingCommodityCode', description='The deducted commodity code.', example='snapshot'),
      billingCommodityName?: string(name='BillingCommodityName', description='The deducted commodity name.', example='云服务器ECS-快照'),
      billingEndTime?: long(name='BillingEndTime', description='The expiration time.', example='1679036400000'),
      billingInstanceId?: string(name='BillingInstanceId', description='The deduction instance.', example='cn-beijing'),
      billingPriceField?: {
        code?: string(name='Code', description='The property code.', example='Storage'),
        name?: string(name='Name', description='The property name.', example='零折使用容量'),
      }(name='BillingPriceField', description='The deduction billable item.'),
      billingPriceFieldCode?: string(name='BillingPriceFieldCode', description='The deduction billable item code.', example='Storage'),
      billingPriceFieldName?: string(name='BillingPriceFieldName', description='The deduction billable item name.', example='零折使用容量'),
      billingStartTime?: long(name='BillingStartTime', description='The effective period.', example='1679032800000'),
      capacityAfterDeductViewUnit?: string(name='CapacityAfterDeductViewUnit', description='The display unit of the capacity after deduction.', example='GB'),
      capacityAfterDeductViewValue?: string(name='CapacityAfterDeductViewValue', description='The display value of the capacity after deduction.', example='23.896484'),
      capacityBeforeDeductViewUnit?: string(name='CapacityBeforeDeductViewUnit', description='The display unit of the capacity before deduction.', example='GB'),
      capacityBeforeDeductViewValue?: string(name='CapacityBeforeDeductViewValue', description='The display value of the capacity before deduction.', example='40.000000'),
      capacityDeductedViewUnit?: string(name='CapacityDeductedViewUnit', description='The display unit of the deducted capacity.', example='GB'),
      capacityDeductedViewValue?: string(name='CapacityDeductedViewValue', description='The display value of the deducted capacity.', example='16.103515'),
      capacityType?: {
        code?: string(name='Code', description='The property code.', example='absolute'),
        name?: string(name='Name', description='The property name.', example='总量恒定型'),
      }(name='CapacityType', description='The capacity type.'),
      capacityTypeCode?: string(name='CapacityTypeCode', description='The capacity type code.', example='absolute'),
      capacityTypeName?: string(name='CapacityTypeName', description='The capacity type name.', example='总量恒定型'),
      commodity?: {
        code?: string(name='Code', description='The property code.', example='ossbag'),
        name?: string(name='Name', description='The property name.', example='对象存储OSS资源包(包月)'),
      }(name='Commodity', description='The commodity.'),
      commodityCode?: string(name='CommodityCode', description='The commodity code.', example='ossbag'),
      commodityName?: string(name='CommodityName', description='The commodity name.', example='对象存储OSS资源包(包月)'),
      cycleType?: {
        code?: string(name='Code', description='The commitment cycle code.', example='hour'),
        name?: string(name='Name', description='The commitment cycle name.', example='小时'),
      }(name='CycleType', description='The commitment cycle.'),
      cycleTypeCode?: string(name='CycleTypeCode', description='The commitment cycle code.', example='hour'),
      cycleTypeName?: string(name='CycleTypeName', description='The commitment cycle name.', example='小时'),
      deductTime?: long(name='DeductTime', description='The deduction time.', example='1679039572000'),
      factor?: string(name='Factor', description='The deduction factor.', example='1'),
      instanceBelongAccountId?: long(name='InstanceBelongAccountId', description='The ID of the account to which the instance belongs.', example='1990699401005016'),
      instanceBelongAccountName?: string(name='InstanceBelongAccountName', description='The name of the account to which the instance belongs.', example='icloudtest'),
      instanceId?: string(name='InstanceId', description='The instance name.', example='OSSBAG-cn-****s'),
      measureAfterDeductViewUnit?: string(name='MeasureAfterDeductViewUnit', description='The display unit of the metering amount after deduction.', example='Byte'),
      measureAfterDeductViewValue?: string(name='MeasureAfterDeductViewValue', description='The display value of the metering amount after deduction.', example='0'),
      measureBeforeDeductViewUnit?: string(name='MeasureBeforeDeductViewUnit', description='The display unit of the metering amount before deduction.', example='GB'),
      measureBeforeDeductViewValue?: string(name='MeasureBeforeDeductViewValue', description='The display value of the metering amount before deduction.', example='16.103515'),
      measureDeductedViewUnit?: string(name='MeasureDeductedViewUnit', description='The display unit of the deducted metering amount.', example='GB'),
      measureDeductedViewValue?: string(name='MeasureDeductedViewValue', description='The display value of the deducted metering amount.', example='16.103515'),
      product?: {
        code?: string(name='Code', description='The property code.', example='oss'),
        name?: string(name='Name', description='The property name.', example='对象存储'),
      }(name='Product', description='The product.'),
      productCode?: string(name='ProductCode', description='The product code.', example='oss'),
      productName?: string(name='ProductName', description='The product name.', example='对象存储'),
      relationAccountId?: long(name='RelationAccountId', description='The deduction account ID.', example='1990699401005016'),
      relationAccountName?: string(name='RelationAccountName', description='The deduction account name.', example='icloudtest'),
      template?: {
        code?: string(name='Code', description='The property code.', example='FPT_ossbag********'),
        name?: string(name='Name', description='The property name.', example='标准存储包'),
      }(name='Template', description='The template.'),
      templateCode?: string(name='TemplateCode', description='The template code.', example='FPT_ossbag********'),
      templateName?: string(name='TemplateName', description='The template name.', example='标准存储包'),
    }
  ](name='Data', description='The data list.'),
  pageSize?: int32(name='PageSize', description='The number of entries per page.', example='20'),
  requestId?: string(name='RequestId', description='The request ID.', example='DF58589C-A06C-4224-8615-7797E6474FA3'),
  totalCount?: int32(name='TotalCount', description='The total number of records.', example='8'),
}

model DescribeDeductLogsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeDeductLogsResponseBody(name='body'),
}

/**
 * @summary Queries resource plan deduction records.
 *
 * @param tmpReq DescribeDeductLogsRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeDeductLogsResponse
 */
@context("describeDeductLogsWithContext")
async function describeDeductLogsWithOptions(tmpReq: DescribeDeductLogsRequest, runtime: $RuntimeOptions): DescribeDeductLogsResponse {
  tmpReq.validate();
  var request = new DescribeDeductLogsShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.ecIdAccountIds)) {
    request.ecIdAccountIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.ecIdAccountIds, 'EcIdAccountIds', 'json');
  }
  if (!$isNull(tmpReq.relationAccountIds)) {
    request.relationAccountIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.relationAccountIds, 'RelationAccountIds', 'json');
  }
  var query = {};
  if (!$isNull(request.billInstanceId)) {
    query['BillInstanceId'] = request.billInstanceId;
  }
  if (!$isNull(request.ecIdAccountIdsShrink)) {
    query['EcIdAccountIds'] = request.ecIdAccountIdsShrink;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  var body : map[string]any = {};
  if (!$isNull(request.billingCommodityCode)) {
    body['BillingCommodityCode'] = request.billingCommodityCode;
  }
  if (!$isNull(request.billingEndTime)) {
    body['BillingEndTime'] = request.billingEndTime;
  }
  if (!$isNull(request.billingStartTime)) {
    body['BillingStartTime'] = request.billingStartTime;
  }
  if (!$isNull(request.commodityCode)) {
    body['CommodityCode'] = request.commodityCode;
  }
  if (!$isNull(request.group)) {
    body['Group'] = request.group;
  }
  if (!$isNull(request.instanceId)) {
    body['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.pageNum)) {
    body['PageNum'] = request.pageNum;
  }
  if (!$isNull(request.pageSize)) {
    body['PageSize'] = request.pageSize;
  }
  if (!$isNull(request.relationAccountIdsShrink)) {
    body['RelationAccountIds'] = request.relationAccountIdsShrink;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeDeductLogs',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries resource plan deduction records.
 *
 * @param request DescribeDeductLogsRequest
 * @return DescribeDeductLogsResponse
 */
async function describeDeductLogs(request: DescribeDeductLogsRequest): DescribeDeductLogsResponse {
  var runtime = new $RuntimeOptions{};
  return describeDeductLogsWithOptions(request, runtime);
}

model DescribeFrInstancesRequest {
  capacityType?: string(name='CapacityType', description='The capacity type.', example='deadlineAcc'),
  commodityCode?: string(name='CommodityCode', description='The commodity code.', example='slb_albcubag_dp_cn'),
  cycleType?: string(name='CycleType', description='The cycle type.', example='dynamicMonth'),
  ecIdAccountIds?: [ 
    {
      accountIds?: [ long ](name='AccountIds', description='The list of accounts to access. If this parameter is empty, all accounts under the current entity ID are selected.'),
      ecId?: string(name='EcId', description='The enterprise entity ID.

This parameter is required.', example='1004064243473974'),
    }
  ](name='EcIdAccountIds', description='The enterprise and account list. If this parameter is empty, the current account is queried.'),
  endTime?: long(name='EndTime', description='The end time.', example='1710604800000'),
  group?: string(name='Group', description='The resource dimension to query.', example='cu'),
  instanceId?: string(name='InstanceId', description='The instance name.', example='alb_cubag*******'),
  instanceTag?: string(name='InstanceTag', description='The instance label value of the resource plan.', example='FR-***'),
  nbid?: string(name='Nbid', description='The primary marketplace ID. If this parameter is empty, the marketplace ID of the current user is used by default.', example='2684202000018'),
  pageNum?: int32(name='PageNum', description='The current page number.', example='1'),
  pageSize?: int32(name='PageSize', description='The number of entries per page.', example='20'),
  productCode?: string(name='ProductCode', description='The product code.', example='slb'),
  sortField?: string(name='SortField', description='The sort field.', example='startTime'),
  sortRule?: string(name='SortRule', description='The sorting rule.', example='asc'),
  spec?: string(name='Spec', description='The specification.', example='*'),
  startTime?: long(name='StartTime', description='The start time.', example='1678939035000'),
  status?: string(name='Status', description='The resource status.', example='valid'),
  templateCode?: string(name='TemplateCode', description='The template code.', example='slb_albcubag*******'),
}

model DescribeFrInstancesShrinkRequest {
  capacityType?: string(name='CapacityType', description='The capacity type.', example='deadlineAcc'),
  commodityCode?: string(name='CommodityCode', description='The commodity code.', example='slb_albcubag_dp_cn'),
  cycleType?: string(name='CycleType', description='The cycle type.', example='dynamicMonth'),
  ecIdAccountIdsShrink?: string(name='EcIdAccountIds', description='The enterprise and account list. If this parameter is empty, the current account is queried.'),
  endTime?: long(name='EndTime', description='The end time.', example='1710604800000'),
  group?: string(name='Group', description='The resource dimension to query.', example='cu'),
  instanceId?: string(name='InstanceId', description='The instance name.', example='alb_cubag*******'),
  instanceTag?: string(name='InstanceTag', description='The instance label value of the resource plan.', example='FR-***'),
  nbid?: string(name='Nbid', description='The primary marketplace ID. If this parameter is empty, the marketplace ID of the current user is used by default.', example='2684202000018'),
  pageNum?: int32(name='PageNum', description='The current page number.', example='1'),
  pageSize?: int32(name='PageSize', description='The number of entries per page.', example='20'),
  productCode?: string(name='ProductCode', description='The product code.', example='slb'),
  sortField?: string(name='SortField', description='The sort field.', example='startTime'),
  sortRule?: string(name='SortRule', description='The sorting rule.', example='asc'),
  spec?: string(name='Spec', description='The specification.', example='*'),
  startTime?: long(name='StartTime', description='The start time.', example='1678939035000'),
  status?: string(name='Status', description='The resource status.', example='valid'),
  templateCode?: string(name='TemplateCode', description='The template code.', example='slb_albcubag*******'),
}

model DescribeFrInstancesResponseBody = {
  currentPage?: int32(name='CurrentPage', description='The current page number.', example='1'),
  data?: [ 
    {
      accountId?: long(name='AccountId', description='The account ID.', example='1990699401005016'),
      accountName?: string(name='AccountName', description='The account name.', example='icloudtest'),
      capacitiyTypeName?: string(name='CapacitiyTypeName', description='The capacity type name.', example='总量递减型'),
      capacityType?: {
        code?: string(name='Code', description='The property code.', example='deadlineAcc'),
        name?: string(name='Name', description='The property name.', example='总量递减型'),
      }(name='CapacityType', description='The capacity type.'),
      capacityTypeCode?: string(name='CapacityTypeCode', description='The capacity type code.', example='deadlineAcc'),
      commodity?: {
        code?: string(name='Code', description='The property code.', example='slb_albcubag_dp_cn'),
        name?: string(name='Name', description='The property name.', example='ALB资源包'),
      }(name='Commodity', description='The commodity.'),
      commodityCode?: string(name='CommodityCode', description='The commodity code.', example='pts'),
      commodityName?: string(name='CommodityName', description='The commodity name.', example='性能测试'),
      currCapacityBaseUnit?: string(name='CurrCapacityBaseUnit', description='The current capacity baseline unit.', example='CU'),
      currCapacityBaseValue?: string(name='CurrCapacityBaseValue', description='The current capacity baseline value.', example='1000.000000'),
      currCapacityViewUnit?: string(name='CurrCapacityViewUnit', description='The current capacity display unit.', example='CU'),
      currCapacityViewValue?: string(name='CurrCapacityViewValue', description='The current capacity display value.', example='1000.000000'),
      cycleType?: {
        code?: string(name='Code', description='The commitment cycle code.', example='hour'),
        name?: string(name='Name', description='The commitment cycle name.', example='小时'),
      }(name='CycleType', description='The commitment cycle.'),
      cycleTypeCode?: string(name='CycleTypeCode', description='The commitment cycle code.', example='hour'),
      cycleTypeName?: string(name='CycleTypeName', description='The commitment cycle name.', example='小时'),
      deductRegions?: [ 
        {
          code?: string(name='Code', description='The deductible region code.', example='cn-beijing'),
          name?: string(name='Name', description='The deductible region.', example='北京'),
        }
      ](name='DeductRegions', description='The list of deductible regions.'),
      enableExchange?: boolean(name='EnableExchange', description='Indicates whether the resource plan can be exchanged.', example='false'),
      enableRenew?: boolean(name='EnableRenew', description='Indicates whether the resource plan can be renewed.', example='false'),
      enableUpgrade?: boolean(name='EnableUpgrade', description='Indicates whether the resource plan can be upgraded.', example='false'),
      endTime?: long(name='EndTime', description='The expiration time.', example='1710604800000'),
      exchangeCommodityCode?: string(name='ExchangeCommodityCode', description='The commodity code for exchange.', example='null'),
      initCapacityBaseUnit?: string(name='InitCapacityBaseUnit', description='The initial capacity baseline unit.', example='CU'),
      initCapacityBaseValue?: string(name='InitCapacityBaseValue', description='The initial capacity baseline value.', example='1000.000000'),
      initCapacityViewUnit?: string(name='InitCapacityViewUnit', description='The initial capacity display unit.', example='CU'),
      initCapacityViewValue?: string(name='InitCapacityViewValue', description='The initial capacity display value.', example='1000.000000'),
      instanceId?: string(name='InstanceId', description='The instance name.', example='alb_cubag*******'),
      periodCapacityViewUnit?: string(name='PeriodCapacityViewUnit', description='The period capacity display unit.', example='CU'),
      periodCapacityViewValue?: string(name='PeriodCapacityViewValue', description='The period capacity display value.', example='1000.000000'),
      periodTime?: string(name='PeriodTime', description='The period time.', example='hour'),
      product?: {
        code?: string(name='Code', description='The property code.', example='slb'),
        name?: string(name='Name', description='The property name.', example='负载均衡'),
      }(name='Product', description='The product.'),
      productCode?: string(name='ProductCode', description='The product code.', example='pts'),
      productName?: string(name='ProductName', description='The product name.', example='性能测试'),
      purchaseTime?: long(name='PurchaseTime', description='The purchase time.', example='1678939036000'),
      region?: string(name='Region', description='The region.', example='*'),
      regionName?: string(name='RegionName', description='The region name.', example='cn-qingdao'),
      spec?: string(name='Spec', description='The specification.', example='*'),
      startTime?: long(name='StartTime', description='The effective period.', example='1678939035000'),
      status?: {
        code?: string(name='Code', description='The property code.', example='valid'),
        name?: string(name='Name', description='The property name.', example='有效'),
      }(name='Status', description='The resource status.'),
      statusCode?: string(name='StatusCode', description='The resource status code.', example='valid'),
      statusName?: string(name='StatusName', description='The resource status name.', example='有效'),
      template?: {
        code?: string(name='Code', description='The property code.', example='alb_cubag*******'),
        name?: string(name='Name', description='The property name.', example='中国内地区域'),
      }(name='Template', description='The template.'),
      templateCode?: string(name='TemplateCode', description='The template code.', example='FPT_armsappbag_deadlineAcc_bj'),
      templateName?: string(name='TemplateName', description='The template name.', example='new_test'),
      periodCapacityBaseUnit?: string(name='periodCapacityBaseUnit', description='The period capacity baseline unit.', example='CU'),
      periodCapacityBaseValue?: string(name='periodCapacityBaseValue', description='The period capacity baseline value.', example='1000.000000'),
    }
  ](name='Data', description='The data list.'),
  pageSize?: int32(name='PageSize', description='The number of entries per page.', example='20'),
  requestId?: string(name='RequestId', description='The request ID.', example='6000EE23-274B-4E07-A697-FF2E999520A4'),
  totalCount?: int32(name='TotalCount', description='The total number of records.', example='1'),
}

model DescribeFrInstancesResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeFrInstancesResponseBody(name='body'),
}

/**
 * @summary Queries resource plan instances.
 *
 * @param tmpReq DescribeFrInstancesRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeFrInstancesResponse
 */
@context("describeFrInstancesWithContext")
async function describeFrInstancesWithOptions(tmpReq: DescribeFrInstancesRequest, runtime: $RuntimeOptions): DescribeFrInstancesResponse {
  tmpReq.validate();
  var request = new DescribeFrInstancesShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.ecIdAccountIds)) {
    request.ecIdAccountIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.ecIdAccountIds, 'EcIdAccountIds', 'json');
  }
  var query = {};
  if (!$isNull(request.ecIdAccountIdsShrink)) {
    query['EcIdAccountIds'] = request.ecIdAccountIdsShrink;
  }
  if (!$isNull(request.instanceTag)) {
    query['InstanceTag'] = request.instanceTag;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  var body : map[string]any = {};
  if (!$isNull(request.capacityType)) {
    body['CapacityType'] = request.capacityType;
  }
  if (!$isNull(request.commodityCode)) {
    body['CommodityCode'] = request.commodityCode;
  }
  if (!$isNull(request.cycleType)) {
    body['CycleType'] = request.cycleType;
  }
  if (!$isNull(request.endTime)) {
    body['EndTime'] = request.endTime;
  }
  if (!$isNull(request.group)) {
    body['Group'] = request.group;
  }
  if (!$isNull(request.instanceId)) {
    body['InstanceId'] = request.instanceId;
  }
  if (!$isNull(request.pageNum)) {
    body['PageNum'] = request.pageNum;
  }
  if (!$isNull(request.pageSize)) {
    body['PageSize'] = request.pageSize;
  }
  if (!$isNull(request.productCode)) {
    body['ProductCode'] = request.productCode;
  }
  if (!$isNull(request.sortField)) {
    body['SortField'] = request.sortField;
  }
  if (!$isNull(request.sortRule)) {
    body['SortRule'] = request.sortRule;
  }
  if (!$isNull(request.spec)) {
    body['Spec'] = request.spec;
  }
  if (!$isNull(request.startTime)) {
    body['StartTime'] = request.startTime;
  }
  if (!$isNull(request.status)) {
    body['Status'] = request.status;
  }
  if (!$isNull(request.templateCode)) {
    body['TemplateCode'] = request.templateCode;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeFrInstances',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries resource plan instances.
 *
 * @param request DescribeFrInstancesRequest
 * @return DescribeFrInstancesResponse
 */
async function describeFrInstances(request: DescribeFrInstancesRequest): DescribeFrInstancesResponse {
  var runtime = new $RuntimeOptions{};
  return describeFrInstancesWithOptions(request, runtime);
}

model DescribeUserSpnSummaryInfoRequest {
  ecIdAccountIds?: [ 
    {
      accountIds?: [ long ](name='AccountIds'),
      ecId?: string(name='EcId', description='This parameter is required.'),
    }
  ](name='EcIdAccountIds'),
  nbid?: string(name='Nbid'),
}

model DescribeUserSpnSummaryInfoShrinkRequest {
  ecIdAccountIdsShrink?: string(name='EcIdAccountIds'),
  nbid?: string(name='Nbid'),
}

model DescribeUserSpnSummaryInfoResponseBody = {
  instanceFamilyList?: [ string ](name='InstanceFamilyList'),
  regionList?: [ 
    {
      regionCode?: string(name='RegionCode'),
      regionName?: string(name='RegionName'),
    }
  ](name='RegionList'),
  requestId?: string(name='RequestId'),
  spnCodeAndTypeList?: [ 
    {
      productCode?: string(name='ProductCode'),
      spnCommodityCode?: string(name='SpnCommodityCode'),
      spnType?: string(name='SpnType'),
      spnTypeName?: string(name='SpnTypeName'),
    }
  ](name='SpnCodeAndTypeList'),
}

model DescribeUserSpnSummaryInfoResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeUserSpnSummaryInfoResponseBody(name='body'),
}

/**
 * @summary 获取客户使用SPN的概述信息
 *
 * @param tmpReq DescribeUserSpnSummaryInfoRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeUserSpnSummaryInfoResponse
 */
@context("describeUserSpnSummaryInfoWithContext")
async function describeUserSpnSummaryInfoWithOptions(tmpReq: DescribeUserSpnSummaryInfoRequest, runtime: $RuntimeOptions): DescribeUserSpnSummaryInfoResponse {
  tmpReq.validate();
  var request = new DescribeUserSpnSummaryInfoShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.ecIdAccountIds)) {
    request.ecIdAccountIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.ecIdAccountIds, 'EcIdAccountIds', 'json');
  }
  var query = {};
  if (!$isNull(request.ecIdAccountIdsShrink)) {
    query['EcIdAccountIds'] = request.ecIdAccountIdsShrink;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeUserSpnSummaryInfo',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 获取客户使用SPN的概述信息
 *
 * @param request DescribeUserSpnSummaryInfoRequest
 * @return DescribeUserSpnSummaryInfoResponse
 */
async function describeUserSpnSummaryInfo(request: DescribeUserSpnSummaryInfoRequest): DescribeUserSpnSummaryInfoResponse {
  var runtime = new $RuntimeOptions{};
  return describeUserSpnSummaryInfoWithOptions(request, runtime);
}

model GetFundAccountAvailableAmountRequest {
  fundAccountId?: string(name='FundAccountId', description='Account ID. If not specified, the account ID owned by the current account (owner) is used by default.', example='12332112'),
}

model GetFundAccountAvailableAmountResponseBody = {
  availableAmount?: string(name='AvailableAmount', description='Available amount', example='100'),
  availableCreditAmount?: string(name='AvailableCreditAmount', description='Available credit amount', example='50'),
  bankAcceptanceAmount?: string(name='BankAcceptanceAmount', description='Bank acceptance bill amount', example='0'),
  cashAmount?: string(name='CashAmount', description='Cash balance', example='50'),
  creditAmount?: string(name='CreditAmount', description='Credit quota', example='100'),
  creditRefundAmount?: string(name='CreditRefundAmount', description='Credit refund balance', example='0'),
  creditUser?: boolean(name='CreditUser', description='Indicates whether credit control is enabled'),
  currency?: string(name='Currency', description='Currency', example='CNY'),
  currentMonthUnclearedAmount?: string(name='CurrentMonthUnclearedAmount', description='Current month uncleared amount', example='20'),
  extendLedgerList?: [ 
    {
      currency?: string(name='Currency', description='Currency of the ledger amount, such as CNY and USD.', example='CNY'),
      ledgerName?: string(name='LedgerName', description='Ledger name', example='应付对冲账本'),
      originalAmount?: string(name='OriginalAmount', description='Ledger balance', example='50'),
    }
  ](name='ExtendLedgerList', description='Extended ledger list'),
  fundAccountId?: string(name='FundAccountId', description='Account ID', example='12332112'),
  fundAccountOwnerAccountId?: string(name='FundAccountOwnerAccountId', description='Account ID of the fund account owner', example='1344312434'),
  fundAccountStatus?: string(name='FundAccountStatus', description='Account status', example='valid'),
  fundAccountType?: string(name='FundAccountType', description='Fund account type. Valid values:
DIRECT_USER: Alibaba Cloud direct customer account.
RESELLER_QUOTA: ecosystem account.', example='REDIRECT_USER'),
  historyMonthUnclearedAmount?: string(name='HistoryMonthUnclearedAmount', description='Historical months uncleared amount', example='30'),
  metadata?: any(name='Metadata', description='Response metadata', example='{}'),
  negativeBillAmount?: string(name='NegativeBillAmount', description='Negative bill amount', example='0'),
  originalCashAmountList?: [ 
    {
      amount?: string(name='Amount', description='Amount', example='10'),
      currency?: string(name='Currency', description='Currency', example='USD'),
    }
  ](name='OriginalCashAmountList', description='Original cash ledger list. International site users may have cash ledgers in multiple currencies.'),
  quotaAmount?: string(name='QuotaAmount', description='Ecosystem end customer quota', example='20'),
  quotaConsumedAmount?: string(name='QuotaConsumedAmount', description='Consumed quota of ecosystem end customer', example='10'),
  requestId?: string(name='RequestId', description='Request ID', example='F96A2D13-7509-5DF9-A60E-E7E3A3CB68E8'),
  unclearedAmount?: string(name='UnclearedAmount', description='Uncleared amount (current month uncleared + historical months uncleared)', example='50'),
}

model GetFundAccountAvailableAmountResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetFundAccountAvailableAmountResponseBody(name='body'),
}

/**
 * @summary Query available balance of fund account
 *
 * @param request GetFundAccountAvailableAmountRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetFundAccountAvailableAmountResponse
 */
@context("getFundAccountAvailableAmountWithContext")
async function getFundAccountAvailableAmountWithOptions(request: GetFundAccountAvailableAmountRequest, runtime: $RuntimeOptions): GetFundAccountAvailableAmountResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.fundAccountId)) {
    body['FundAccountId'] = request.fundAccountId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetFundAccountAvailableAmount',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Query available balance of fund account
 *
 * @param request GetFundAccountAvailableAmountRequest
 * @return GetFundAccountAvailableAmountResponse
 */
async function getFundAccountAvailableAmount(request: GetFundAccountAvailableAmountRequest): GetFundAccountAvailableAmountResponse {
  var runtime = new $RuntimeOptions{};
  return getFundAccountAvailableAmountWithOptions(request, runtime);
}

model GetFundAccountCanAllocateCreditAmountRequest {
  fundAccountId?: long(name='FundAccountId', description='Fund account ID. If not specified, the account owned by the current account is used by default.', example='1233231'),
}

model GetFundAccountCanAllocateCreditAmountResponseBody = {
  ecid?: string(name='Ecid', description='The enterprise entity ID.', example='2032123221'),
  ecidAllocatedCreditAmount?: string(name='EcidAllocatedCreditAmount', description='The allocated credit limit of the enterprise.', example='300'),
  ecidCreditAmount?: string(name='EcidCreditAmount', description='The enterprise credit quota.', example='1000'),
  fundAccountEcid?: string(name='FundAccountEcid', description='The account ECID.', example='202321232'),
  fundAccountId?: long(name='FundAccountId', description='The account ID.', example='12332112'),
  fundAccountName?: string(name='FundAccountName', description='The account name.', example='云某的名称'),
  fundAccountOwnerAccountId?: long(name='FundAccountOwnerAccountId', description='The Alibaba Cloud account ID of the account owner.', example='123433121'),
  maxCanAllocateCreditAmount?: string(name='MaxCanAllocateCreditAmount', description='The maximum allocatable credit limit of the current account.', example='1500'),
  metadata?: any(name='Metadata', description='Response structure metadata.', example='{}'),
  minCanAllocateCreditAmount?: string(name='MinCanAllocateCreditAmount', description='The minimum allocatable credit limit of the current account.', example='200'),
  nbid?: string(name='Nbid', description='The primary marketplace.', example='2684210001'),
  requestId?: string(name='RequestId', description='The request ID.', example='CC706AAC-75A6-55B5-9AB7-7D171C6C7655'),
  site?: string(name='Site', description='The site.', example='26842'),
}

model GetFundAccountCanAllocateCreditAmountResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetFundAccountCanAllocateCreditAmountResponseBody(name='body'),
}

/**
 * @summary Query allocatable credit limit of a fund account
 *
 * @param request GetFundAccountCanAllocateCreditAmountRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetFundAccountCanAllocateCreditAmountResponse
 */
@context("getFundAccountCanAllocateCreditAmountWithContext")
async function getFundAccountCanAllocateCreditAmountWithOptions(request: GetFundAccountCanAllocateCreditAmountRequest, runtime: $RuntimeOptions): GetFundAccountCanAllocateCreditAmountResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.fundAccountId)) {
    body['FundAccountId'] = request.fundAccountId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetFundAccountCanAllocateCreditAmount',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Query allocatable credit limit of a fund account
 *
 * @param request GetFundAccountCanAllocateCreditAmountRequest
 * @return GetFundAccountCanAllocateCreditAmountResponse
 */
async function getFundAccountCanAllocateCreditAmount(request: GetFundAccountCanAllocateCreditAmountRequest): GetFundAccountCanAllocateCreditAmountResponse {
  var runtime = new $RuntimeOptions{};
  return getFundAccountCanAllocateCreditAmountWithOptions(request, runtime);
}

model GetFundAccountCanRecycleAmountRequest {
  currency?: string(name='Currency', description='The currency.

This parameter is required.', example='CNY'),
  recycleFromFundAccountId?: string(name='RecycleFromFundAccountId', description='The ID of the source account (payer account) from which funds are reclaimed. If not specified, the account ID owned by the current account is used by default.', example='122321223'),
}

model GetFundAccountCanRecycleAmountResponseBody = {
  availableAmount?: string(name='AvailableAmount', description='The available balance of the account from which funds are reclaimed.', example='300'),
  currency?: string(name='Currency', description='The currency.', example='CNY'),
  metadata?: any(name='Metadata', description='The metadata of the response struct.', example='{}'),
  recycleFromFundAccountId?: string(name='RecycleFromFundAccountId', description='The ID of the source account from which funds are reclaimed.', example='1232122132'),
  recycleToFundAccountList?: [ 
    {
      fundAccountId?: string(name='FundAccountId', description='The ID of the target account to which funds can be reclaimed.', example='122323121'),
      fundAccountName?: string(name='FundAccountName', description='The name of the target account to which funds can be reclaimed.', example='云某的账户'),
      fundAccountOwnerAccountId?: string(name='FundAccountOwnerAccountId', description='The account ID that owns the target account to which funds can be reclaimed.', example='183221321'),
      maxRecyclableAmount?: string(name='MaxRecyclableAmount', description='The maximum reclaimable amount.', example='120'),
      originalTransferRemainAmount?: string(name='OriginalTransferRemainAmount', description='The remaining transfer amount that can be reclaimed through the original transfer path.', example='120'),
    }
  ](name='RecycleToFundAccountList', description='The list of accounts to which funds can be reclaimed.'),
  requestId?: string(name='RequestId', description='The request ID.', example='6000EE23-274B-4E07-A697-FF2E999520A4'),
  transferAmount?: string(name='TransferAmount', description='The transfer ledger balance.', example='200'),
}

model GetFundAccountCanRecycleAmountResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetFundAccountCanRecycleAmountResponseBody(name='body'),
}

/**
 * @summary Queries the reclaimable amount of a fund account.
 *
 * @param request GetFundAccountCanRecycleAmountRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetFundAccountCanRecycleAmountResponse
 */
@context("getFundAccountCanRecycleAmountWithContext")
async function getFundAccountCanRecycleAmountWithOptions(request: GetFundAccountCanRecycleAmountRequest, runtime: $RuntimeOptions): GetFundAccountCanRecycleAmountResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.currency)) {
    body['Currency'] = request.currency;
  }
  if (!$isNull(request.recycleFromFundAccountId)) {
    body['RecycleFromFundAccountId'] = request.recycleFromFundAccountId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetFundAccountCanRecycleAmount',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the reclaimable amount of a fund account.
 *
 * @param request GetFundAccountCanRecycleAmountRequest
 * @return GetFundAccountCanRecycleAmountResponse
 */
async function getFundAccountCanRecycleAmount(request: GetFundAccountCanRecycleAmountRequest): GetFundAccountCanRecycleAmountResponse {
  var runtime = new $RuntimeOptions{};
  return getFundAccountCanRecycleAmountWithOptions(request, runtime);
}

model GetFundAccountCanTransferAmountRequest {
  currency?: string(name='Currency', description='Currency

This parameter is required.', example='CNY'),
  fundAccountId?: string(name='FundAccountId', description='Transfer source account ID. If not specified, the account ID owned by the current account is used by default.', example='123212'),
}

model GetFundAccountCanTransferAmountResponseBody = {
  availableAmount?: string(name='AvailableAmount', description='Available amount', example='100'),
  cashAmount?: string(name='CashAmount', description='Cash ledger balance', example='500'),
  currency?: string(name='Currency', description='Currency', example='CNY'),
  fundAccountEcid?: string(name='FundAccountEcid', description='Enterprise entity ID of the account owner', example='2032121324'),
  fundAccountId?: long(name='FundAccountId', description='Account ID', example='12332112'),
  fundAccountName?: string(name='FundAccountName', description='Account name', example='云某的账户'),
  fundAccountOwnerAccountId?: long(name='FundAccountOwnerAccountId', description='Alibaba Cloud account ID of the fund account owner', example='154738212323'),
  maxTransferableAmount?: string(name='MaxTransferableAmount', description='Transferable amount', example='100'),
  metadata?: any(name='Metadata', description='Response metadata', example='{}'),
  nbid?: string(name='Nbid', description='Primary marketplace', example='2684210001'),
  requestId?: string(name='RequestId', description='Request ID', example='6000EE23-274B-4E07-A697-FF2E999520A4'),
  site?: string(name='Site', description='Site', example='26842'),
  transferAmount?: string(name='TransferAmount', description='Transfer ledger balance', example='100'),
}

model GetFundAccountCanTransferAmountResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetFundAccountCanTransferAmountResponseBody(name='body'),
}

/**
 * @summary Query the transferable amount of a fund account
 *
 * @param request GetFundAccountCanTransferAmountRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetFundAccountCanTransferAmountResponse
 */
@context("getFundAccountCanTransferAmountWithContext")
async function getFundAccountCanTransferAmountWithOptions(request: GetFundAccountCanTransferAmountRequest, runtime: $RuntimeOptions): GetFundAccountCanTransferAmountResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.currency)) {
    body['Currency'] = request.currency;
  }
  if (!$isNull(request.fundAccountId)) {
    body['FundAccountId'] = request.fundAccountId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetFundAccountCanTransferAmount',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Query the transferable amount of a fund account
 *
 * @param request GetFundAccountCanTransferAmountRequest
 * @return GetFundAccountCanTransferAmountResponse
 */
async function getFundAccountCanTransferAmount(request: GetFundAccountCanTransferAmountRequest): GetFundAccountCanTransferAmountResponse {
  var runtime = new $RuntimeOptions{};
  return getFundAccountCanTransferAmountWithOptions(request, runtime);
}

model GetFundAccountCanWithdrawAmountRequest {
  fundAccountId?: long(name='FundAccountId', description='Fund account ID. If not specified, the account ID owned by the current account (owner) is used by default.', example='123212232'),
}

model GetFundAccountCanWithdrawAmountResponseBody = {
  canOriginalWithdrawAmount?: string(name='CanOriginalWithdrawAmount', description='Amount withdrawable via original payment method', example='400'),
  canWithdrawAmount?: string(name='CanWithdrawAmount', description='Withdrawable amount = Cash balance - Transfer ledger amount - (Outstanding invoice amount + Historical months unsettled amount + Current month unsettled amount - Transfer ledger amount) - Pay-as-you-go reserved amount', example='500'),
  cannotOriginalWithdrawAmount?: string(name='CannotOriginalWithdrawAmount', description='Amount not withdrawable via original payment method', example='100'),
  cashAmount?: string(name='CashAmount', description='Cash balance', example='1000'),
  creditMemoAmount?: string(name='CreditMemoAmount', description='Outstanding invoice amount', example='100'),
  currentMonthUnclearedAmount?: string(name='CurrentMonthUnclearedAmount', description='Current month unsettled amount', example='200'),
  historyMonthUnclearedAmount?: string(name='HistoryMonthUnclearedAmount', description='Historical months unsettled amount', example='100'),
  metadata?: any(name='Metadata', description='Response structure metadata', example='{}'),
  payAsYouGoReversedAmount?: string(name='PayAsYouGoReversedAmount', description='Pay-as-you-go reserved amount', example='100'),
  requestId?: string(name='RequestId', description='Request ID', example='DF58589C-A06C-4224-8615-7797E6474FA3'),
  transferAmount?: string(name='TransferAmount', description='Transfer amount', example='0'),
}

model GetFundAccountCanWithdrawAmountResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetFundAccountCanWithdrawAmountResponseBody(name='body'),
}

/**
 * @summary Query Withdrawable Amount of Fund Account
 *
 * @param request GetFundAccountCanWithdrawAmountRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetFundAccountCanWithdrawAmountResponse
 */
@context("getFundAccountCanWithdrawAmountWithContext")
async function getFundAccountCanWithdrawAmountWithOptions(request: GetFundAccountCanWithdrawAmountRequest, runtime: $RuntimeOptions): GetFundAccountCanWithdrawAmountResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.fundAccountId)) {
    body['FundAccountId'] = request.fundAccountId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetFundAccountCanWithdrawAmount',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Query Withdrawable Amount of Fund Account
 *
 * @param request GetFundAccountCanWithdrawAmountRequest
 * @return GetFundAccountCanWithdrawAmountResponse
 */
async function getFundAccountCanWithdrawAmount(request: GetFundAccountCanWithdrawAmountRequest): GetFundAccountCanWithdrawAmountResponse {
  var runtime = new $RuntimeOptions{};
  return getFundAccountCanWithdrawAmountWithOptions(request, runtime);
}

model GetFundAccountLowAvailableAmountAlarmRequest {
  fundAccountId?: long(name='FundAccountId', description='Fund account ID. If not specified, the account ID owned by the current account is used by default.', example='12332112'),
}

model GetFundAccountLowAvailableAmountAlarmResponseBody = {
  alarmEnabled?: boolean(name='AlarmEnabled', description='Whether the alert is enabled'),
  metadata?: any(name='Metadata', description='Response structure metadata', example='{}'),
  requestId?: string(name='RequestId', description='Request ID', example='6000EE23-274B-4E07-A697-FF2E999520A4'),
  thresholdAmount?: string(name='ThresholdAmount', description='Alert threshold', example='100'),
}

model GetFundAccountLowAvailableAmountAlarmResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetFundAccountLowAvailableAmountAlarmResponseBody(name='body'),
}

/**
 * @summary Query Fund Account Low Balance Alert
 *
 * @param request GetFundAccountLowAvailableAmountAlarmRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetFundAccountLowAvailableAmountAlarmResponse
 */
@context("getFundAccountLowAvailableAmountAlarmWithContext")
async function getFundAccountLowAvailableAmountAlarmWithOptions(request: GetFundAccountLowAvailableAmountAlarmRequest, runtime: $RuntimeOptions): GetFundAccountLowAvailableAmountAlarmResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.fundAccountId)) {
    body['FundAccountId'] = request.fundAccountId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetFundAccountLowAvailableAmountAlarm',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Query Fund Account Low Balance Alert
 *
 * @param request GetFundAccountLowAvailableAmountAlarmRequest
 * @return GetFundAccountLowAvailableAmountAlarmResponse
 */
async function getFundAccountLowAvailableAmountAlarm(request: GetFundAccountLowAvailableAmountAlarmRequest): GetFundAccountLowAvailableAmountAlarmResponse {
  var runtime = new $RuntimeOptions{};
  return getFundAccountLowAvailableAmountAlarmWithOptions(request, runtime);
}

model GetFundAccountTransactionDetailsRequest {
  billNumber?: string(name='BillNumber', description='Transaction order number', example='2023212312321'),
  channelTransactionNumber?: string(name='ChannelTransactionNumber', description='Transaction channel serial number', example='20250312334312322'),
  currentPage?: int32(name='CurrentPage', description='Current page number', example='1'),
  endTime?: long(name='EndTime', description='Query end timestamp (milliseconds)', example='1735664561000'),
  fundAccountId?: long(name='FundAccountId', description='Fund account ID. If not specified, the account ID owned by the current account (owner) is used by default.', example='123221232'),
  pageSize?: int32(name='PageSize', description='Page size (maximum 200)', example='20'),
  startTime?: long(name='StartTime', description='Query start timestamp (milliseconds)', example='1735664461000'),
  transactionChannelList?: [ string ](name='TransactionChannelList', description='Transaction channel. If specified, the query filters by transaction channel. If not specified, all channels are queried by default.

User balance: ACCT_CASH.

Alipay: ALIPAY.

Alipay Zhifutong: ALIPAY_ZHIFUTONG.

Offline remittance: OFFLINE_REMIT.

Credit control quota refund: REFUND.

Online banking: UNION_PAY_BANK.

Credit card: CREDIT_CARD. (International site only)

PayPal: PAYPAL. (International site only)'),
  transactionDirection?: string(name='TransactionDirection', description='Transaction direction: in/out (fund inflow/outflow)', example='IN'),
  transactionNumber?: long(name='TransactionNumber', description='Transaction serial number', example='543231231'),
  transactionType?: string(name='TransactionType', description='Transaction type. If a transaction type is specified, only results of that type are returned. If the specified type does not exist, the result is empty. If not specified, all types are returned by default.

Top-up: CHARGE.

Withdrawal: WITHDRAW.

Refund: REFUND.

Payment: PAY.

Transfer: TRANSFER.

Adjustment: ADJUST.

Order expiration refund: PAY_FAILED.', example='CHARGE'),
  transactionTypeList?: [ string ](name='TransactionTypeList', description='Transaction type list. Supports querying with multiple parameters.'),
}

model GetFundAccountTransactionDetailsShrinkRequest {
  billNumber?: string(name='BillNumber', description='Transaction order number', example='2023212312321'),
  channelTransactionNumber?: string(name='ChannelTransactionNumber', description='Transaction channel serial number', example='20250312334312322'),
  currentPage?: int32(name='CurrentPage', description='Current page number', example='1'),
  endTime?: long(name='EndTime', description='Query end timestamp (milliseconds)', example='1735664561000'),
  fundAccountId?: long(name='FundAccountId', description='Fund account ID. If not specified, the account ID owned by the current account (owner) is used by default.', example='123221232'),
  pageSize?: int32(name='PageSize', description='Page size (maximum 200)', example='20'),
  startTime?: long(name='StartTime', description='Query start timestamp (milliseconds)', example='1735664461000'),
  transactionChannelListShrink?: string(name='TransactionChannelList', description='Transaction channel. If specified, the query filters by transaction channel. If not specified, all channels are queried by default.

User balance: ACCT_CASH.

Alipay: ALIPAY.

Alipay Zhifutong: ALIPAY_ZHIFUTONG.

Offline remittance: OFFLINE_REMIT.

Credit control quota refund: REFUND.

Online banking: UNION_PAY_BANK.

Credit card: CREDIT_CARD. (International site only)

PayPal: PAYPAL. (International site only)'),
  transactionDirection?: string(name='TransactionDirection', description='Transaction direction: in/out (fund inflow/outflow)', example='IN'),
  transactionNumber?: long(name='TransactionNumber', description='Transaction serial number', example='543231231'),
  transactionType?: string(name='TransactionType', description='Transaction type. If a transaction type is specified, only results of that type are returned. If the specified type does not exist, the result is empty. If not specified, all types are returned by default.

Top-up: CHARGE.

Withdrawal: WITHDRAW.

Refund: REFUND.

Payment: PAY.

Transfer: TRANSFER.

Adjustment: ADJUST.

Order expiration refund: PAY_FAILED.', example='CHARGE'),
  transactionTypeListShrink?: string(name='TransactionTypeList', description='Transaction type list. Supports querying with multiple parameters.'),
}

model GetFundAccountTransactionDetailsResponseBody = {
  currentPage?: int32(name='CurrentPage', description='Current page number', example='1'),
  data?: [ 
    {
      balance?: string(name='Balance', description='Balance after the operation', example='5'),
      billNumber?: string(name='BillNumber', description='Order number or bill number', example='2323203243'),
      channelTransactionNumber?: string(name='ChannelTransactionNumber', description='External transaction serial number', example='20244389232'),
      currency?: string(name='Currency', description='Transaction amount currency', example='USD'),
      fundAccountEcid?: string(name='FundAccountEcid', description='Enterprise entity ID', example='23473943'),
      fundAccountId?: long(name='FundAccountId', description='Account ID', example='1232121'),
      fundAccountName?: string(name='FundAccountName', description='Account name', example='云某的名称'),
      fundAccountOwnerAccountId?: long(name='FundAccountOwnerAccountId', description='Alibaba Cloud account ID of the account owner', example='32343231'),
      fundType?: string(name='FundType', description='Fund type', example='ACCT_BOOK'),
      nbid?: string(name='Nbid', description='Primary marketplace', example='2684210001'),
      remark?: string(name='Remark', description='Remarks', example='订单备注'),
      site?: string(name='Site', description='Site', example='26842'),
      transactionAccount?: string(name='TransactionAccount', description='Corresponding transaction account, such as the Alipay top-up account or the counterparty account for transfers.', example='186****3975'),
      transactionAmount?: string(name='TransactionAmount', description='Transaction amount', example='10'),
      transactionChannel?: string(name='TransactionChannel', description='Transaction channel. If specified, the query filters by transaction channel. If not specified, all channels are queried by default.

User balance: ACCT_CASH.

Alipay: ALIPAY.

Alipay Zhifutong: ALIPAY_ZHIFUTONG.

Offline remittance: OFFLINE_REMIT.

Credit control quota refund: REFUND.

Online banking: UNION_PAY_BANK.

Credit card: CREDIT_CARD. (International site only)

PayPal: PAYPAL. (International site only)', example='ALIPAY'),
      transactionDirection?: string(name='TransactionDirection', description='Transaction direction: in/out (income/expenditure)', example='IN'),
      transactionNumber?: long(name='TransactionNumber', description='Transaction serial number', example='5423121'),
      transactionTime?: string(name='TransactionTime', description='Formatted transaction time string', example='2024-12-01 12:00:00'),
      transactionType?: string(name='TransactionType', description='Transaction type. If a transaction type is specified, only results of that type are returned. If the specified type does not exist, the result is empty. If not specified, all types are returned by default.

Top-up: CHARGE.

Withdrawal: WITHDRAW.

Refund: REFUND.

Payment: PAY.

Transfer: TRANSFER.

Adjustment: ADJUST.

Order expiration refund: PAY_FAILED.', example='CHARGE'),
    }
  ](name='Data', description='Data list'),
  metadata?: any(name='Metadata', description='Response structure metadata', example='{}'),
  pageSize?: int32(name='PageSize', description='Page size', example='20'),
  requestId?: string(name='RequestId', description='Request ID', example='D6E068C3-25BC-455A-85FE-45F0B22ECB1F'),
  totalCount?: int32(name='TotalCount', description='Total number of records', example='30'),
}

model GetFundAccountTransactionDetailsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetFundAccountTransactionDetailsResponseBody(name='body'),
}

/**
 * @summary Query fund account transaction details
 *
 * @param tmpReq GetFundAccountTransactionDetailsRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetFundAccountTransactionDetailsResponse
 */
@context("getFundAccountTransactionDetailsWithContext")
async function getFundAccountTransactionDetailsWithOptions(tmpReq: GetFundAccountTransactionDetailsRequest, runtime: $RuntimeOptions): GetFundAccountTransactionDetailsResponse {
  tmpReq.validate();
  var request = new GetFundAccountTransactionDetailsShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.transactionChannelList)) {
    request.transactionChannelListShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.transactionChannelList, 'TransactionChannelList', 'json');
  }
  if (!$isNull(tmpReq.transactionTypeList)) {
    request.transactionTypeListShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.transactionTypeList, 'TransactionTypeList', 'json');
  }
  var query = {};
  if (!$isNull(request.currentPage)) {
    query['CurrentPage'] = request.currentPage;
  }
  if (!$isNull(request.pageSize)) {
    query['PageSize'] = request.pageSize;
  }
  var body : map[string]any = {};
  if (!$isNull(request.billNumber)) {
    body['BillNumber'] = request.billNumber;
  }
  if (!$isNull(request.channelTransactionNumber)) {
    body['ChannelTransactionNumber'] = request.channelTransactionNumber;
  }
  if (!$isNull(request.endTime)) {
    body['EndTime'] = request.endTime;
  }
  if (!$isNull(request.fundAccountId)) {
    body['FundAccountId'] = request.fundAccountId;
  }
  if (!$isNull(request.startTime)) {
    body['StartTime'] = request.startTime;
  }
  if (!$isNull(request.transactionChannelListShrink)) {
    body['TransactionChannelList'] = request.transactionChannelListShrink;
  }
  if (!$isNull(request.transactionDirection)) {
    body['TransactionDirection'] = request.transactionDirection;
  }
  if (!$isNull(request.transactionNumber)) {
    body['TransactionNumber'] = request.transactionNumber;
  }
  if (!$isNull(request.transactionType)) {
    body['TransactionType'] = request.transactionType;
  }
  if (!$isNull(request.transactionTypeListShrink)) {
    body['TransactionTypeList'] = request.transactionTypeListShrink;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetFundAccountTransactionDetails',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Query fund account transaction details
 *
 * @param request GetFundAccountTransactionDetailsRequest
 * @return GetFundAccountTransactionDetailsResponse
 */
async function getFundAccountTransactionDetails(request: GetFundAccountTransactionDetailsRequest): GetFundAccountTransactionDetailsResponse {
  var runtime = new $RuntimeOptions{};
  return getFundAccountTransactionDetailsWithOptions(request, runtime);
}

model GetOrderDetailRequest {
  memberUid?: long(name='MemberUid', description='The UID of the enterprise multi-account member managed by the current account. Leave this parameter empty if this scenario does not apply.', example='1715322405372273'),
  orderId?: string(name='OrderId', description='The order ID.

This parameter is required.', example='233501558440169'),
  ownerId?: long(name='OwnerId'),
}

model GetOrderDetailResponseBody = {
  code?: string(name='Code', description='The status code.', example='Success'),
  data?: {
    hostName?: string(name='HostName', description='The hostname.', example='test'),
    orderList?: {
      order?: [ 
      {
        afterTaxAmount?: string(name='AfterTaxAmount'),
        billModuleConfig?: {
          billModuleConfig?: [ 
          {
            apiCode?: string(name='ApiCode'),
            billModuleProperties?: {
              billModuleProperties?: [ 
              {
                attrApiCode?: string(name='AttrApiCode'),
                moduleApiCode?: string(name='ModuleApiCode'),
                value?: string(name='Value'),
              }
            ](name='billModuleProperties')
            }(name='BillModuleProperties'),
            code?: string(name='Code'),
            name?: string(name='Name'),
          }
        ](name='billModuleConfig')
        }(name='BillModuleConfig'),
        commodityCode?: string(name='CommodityCode'),
        config?: string(name='Config'),
        createTime?: string(name='CreateTime'),
        currency?: string(name='Currency'),
        extendInfos?: map[string]string(name='ExtendInfos'),
        instanceIds?: string(name='InstanceIds'),
        operator?: string(name='Operator'),
        orderId?: string(name='OrderId'),
        orderSubType?: string(name='OrderSubType'),
        orderType?: string(name='OrderType'),
        originalConfig?: string(name='OriginalConfig'),
        originalModuleConfig?: {
          originalModuleConfig?: [ 
          {
            code?: string(name='Code'),
            moduleProperties?: {
              moduleProperties?: [ 
              {
                code?: string(name='Code'),
                name?: string(name='Name'),
                value?: string(name='Value'),
              }
            ](name='moduleProperties')
            }(name='ModuleProperties'),
            name?: string(name='Name'),
          }
        ](name='originalModuleConfig')
        }(name='OriginalModuleConfig'),
        paymentCurrency?: string(name='PaymentCurrency'),
        paymentStatus?: string(name='PaymentStatus'),
        paymentTime?: string(name='PaymentTime'),
        pretaxAmount?: string(name='PretaxAmount'),
        pretaxAmountLocal?: string(name='PretaxAmountLocal'),
        pretaxGrossAmount?: string(name='PretaxGrossAmount'),
        productCode?: string(name='ProductCode'),
        productType?: string(name='ProductType'),
        quantity?: string(name='Quantity'),
        region?: string(name='Region'),
        relatedOrderId?: string(name='RelatedOrderId'),
        subOrderId?: string(name='SubOrderId'),
        subscriptionType?: string(name='SubscriptionType'),
        tax?: string(name='Tax'),
        usageEndTime?: string(name='UsageEndTime'),
        usageStartTime?: string(name='UsageStartTime'),
      }
    ](name='Order')
    }(name='OrderList'),
    pageNum?: int32(name='PageNum', description='The page number.', example='1'),
    pageSize?: int32(name='PageSize', description='The number of entries per page.', example='20'),
    totalCount?: int32(name='TotalCount', description='The total number of records.', example='400'),
  }(name='Data', description='The returned data.'),
  message?: string(name='Message', description='The error message.', example='Successful!'),
  requestId?: string(name='RequestId', description='The request ID.', example='D6E068C3-25BC-455A-85FE-45F0B22ECB1F'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model GetOrderDetailResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetOrderDetailResponseBody(name='body'),
}

/**
 * @summary Queries the details of a specific order for a user or a reseller\\"s customer.
 *
 * @param request GetOrderDetailRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetOrderDetailResponse
 */
@context("getOrderDetailWithContext")
async function getOrderDetailWithOptions(request: GetOrderDetailRequest, runtime: $RuntimeOptions): GetOrderDetailResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.memberUid)) {
    query['MemberUid'] = request.memberUid;
  }
  if (!$isNull(request.orderId)) {
    query['OrderId'] = request.orderId;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetOrderDetail',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the details of a specific order for a user or a reseller\\"s customer.
 *
 * @param request GetOrderDetailRequest
 * @return GetOrderDetailResponse
 */
async function getOrderDetail(request: GetOrderDetailRequest): GetOrderDetailResponse {
  var runtime = new $RuntimeOptions{};
  return getOrderDetailWithOptions(request, runtime);
}

model GetOrdersRequest {
  createTimeEnd?: string(name='CreateTimeEnd', description='The end of the order creation time range. If not specified, orders created within the most recent hour are queried. Format: YYYY-MM-ddTHH:mm:ssZ. Time zone: UTC.', example='2016-05-23T12:00:00Z'),
  createTimeStart?: string(name='CreateTimeStart', description='The start of the order creation time range. If not specified, orders created within the most recent hour are queried. Format: YYYY-MM-ddTHH:mm:ssZ. Time zone: UTC.', example='2016-05-23T13:00:00Z'),
  memberUid?: long(name='MemberUid', description='The UID of a member account in an enterprise with multiple member accounts. Leave this parameter empty if this scenario does not apply.', example='1779628988149763'),
  orderType?: string(name='OrderType', description='The order type. Valid values:
- New: new purchase.
- Renew: renewal.
- Upgrade: upgrade.
- TempUpgrade: temporary upgrade.
- Downgrade: downgrade.
- Refund: refund.
- Convert: billing method conversion.
- ResizeDisk: cloud disk resizing.
- CompensatoryRenew: compensatory renewal.
- IncreaseUpgrade: bandwidth upgrade.
- Exchange: exchange.
- ChangeOperatingSystem: operating system change.', example='New'),
  ownerId?: long(name='OwnerId'),
  pageNum?: int32(name='PageNum', description='The page number.', example='1'),
  pageSize?: int32(name='PageSize', description='The number of entries per page.', example='20'),
  paymentStatus?: string(name='PaymentStatus', description='The payment status. For non-refund orders, valid values:
- Unpaid: unpaid.
- Paid: paid.
- Cancelled: canceled.

> For refund orders, set this parameter to NULL.', example='Paid'),
  productCode?: string(name='ProductCode', description='The product code.', example='rds'),
  productType?: string(name='ProductType', description='The product type.', example='rds'),
  subscriptionType?: string(name='SubscriptionType', description='The subscription type. Valid values:
- Subscription: upfront.
- PayAsYouGo: pay-as-you-go.', example='Subscription'),
}

model GetOrdersResponseBody = {
  code?: string(name='Code', description='The status code.', example='Success'),
  data?: {
    hostName?: string(name='HostName', description='The hostname.', example='test'),
    orderList?: {
      order?: [ 
      {
        afterTaxAmount?: string(name='AfterTaxAmount'),
        commodityCode?: string(name='CommodityCode'),
        createTime?: string(name='CreateTime'),
        currency?: string(name='Currency'),
        orderId?: string(name='OrderId'),
        orderType?: string(name='OrderType'),
        paymentCurrency?: string(name='PaymentCurrency'),
        paymentStatus?: string(name='PaymentStatus'),
        paymentTime?: string(name='PaymentTime'),
        pretaxAmount?: string(name='PretaxAmount'),
        pretaxAmountLocal?: string(name='PretaxAmountLocal'),
        pretaxGrossAmount?: string(name='PretaxGrossAmount'),
        productCode?: string(name='ProductCode'),
        productType?: string(name='ProductType'),
        relatedOrderId?: string(name='RelatedOrderId'),
        subscriptionType?: string(name='SubscriptionType'),
        tax?: string(name='Tax'),
      }
    ](name='Order')
    }(name='OrderList'),
    pageNum?: int32(name='PageNum', description='The page number.', example='1'),
    pageSize?: int32(name='PageSize', description='The number of entries per page. A maximum of 20 entries per page is recommended. A larger number may cause a timeout.', example='20'),
    totalCount?: int32(name='TotalCount', description='The total number of records.', example='1'),
  }(name='Data', description='The returned data.'),
  message?: string(name='Message', description='The error message.', example='Successful!'),
  requestId?: string(name='RequestId', description='The request ID.', example='6000EE23-274B-4E07-A697-FF2E999520A4'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model GetOrdersResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetOrdersResponseBody(name='body'),
}

/**
 * @summary Queries the order list of a user or a reseller customer. By default, this operation queries orders created within the most recent hour. To query orders over a longer time range, set the CreateTimeStart and CreateTimeEnd parameters.
 *
 * @param request GetOrdersRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetOrdersResponse
 */
@context("getOrdersWithContext")
async function getOrdersWithOptions(request: GetOrdersRequest, runtime: $RuntimeOptions): GetOrdersResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.createTimeEnd)) {
    query['CreateTimeEnd'] = request.createTimeEnd;
  }
  if (!$isNull(request.createTimeStart)) {
    query['CreateTimeStart'] = request.createTimeStart;
  }
  if (!$isNull(request.memberUid)) {
    query['MemberUid'] = request.memberUid;
  }
  if (!$isNull(request.orderType)) {
    query['OrderType'] = request.orderType;
  }
  if (!$isNull(request.ownerId)) {
    query['OwnerId'] = request.ownerId;
  }
  if (!$isNull(request.pageNum)) {
    query['PageNum'] = request.pageNum;
  }
  if (!$isNull(request.pageSize)) {
    query['PageSize'] = request.pageSize;
  }
  if (!$isNull(request.paymentStatus)) {
    query['PaymentStatus'] = request.paymentStatus;
  }
  if (!$isNull(request.productCode)) {
    query['ProductCode'] = request.productCode;
  }
  if (!$isNull(request.productType)) {
    query['ProductType'] = request.productType;
  }
  if (!$isNull(request.subscriptionType)) {
    query['SubscriptionType'] = request.subscriptionType;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetOrders',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the order list of a user or a reseller customer. By default, this operation queries orders created within the most recent hour. To query orders over a longer time range, set the CreateTimeStart and CreateTimeEnd parameters.
 *
 * @param request GetOrdersRequest
 * @return GetOrdersResponse
 */
async function getOrders(request: GetOrdersRequest): GetOrdersResponse {
  var runtime = new $RuntimeOptions{};
  return getOrdersWithOptions(request, runtime);
}

model GetSavingPlanDeductableCommodityRequest {
  ecIdAccountIds?: [ 
    {
      accountIds?: [ long ](name='AccountIds'),
      ecId?: string(name='EcId', description='This parameter is required.'),
    }
  ](name='EcIdAccountIds'),
  nbid?: string(name='Nbid'),
}

model GetSavingPlanDeductableCommodityShrinkRequest {
  ecIdAccountIdsShrink?: string(name='EcIdAccountIds'),
  nbid?: string(name='Nbid'),
}

model GetSavingPlanDeductableCommodityResponseBody = {
  data?: [ 
    {
      activityId?: long(name='ActivityId'),
      commodityCode?: string(name='CommodityCode'),
      commodityId?: long(name='CommodityId'),
      commodityName?: string(name='CommodityName'),
      cycleList?: [ 
        {
          code?: string(name='Code'),
          name?: string(name='Name'),
        }
      ](name='CycleList'),
      filterModules?: [ 
        {
          moduleCode?: string(name='ModuleCode'),
          moduleId?: long(name='ModuleId'),
          moduleName?: string(name='ModuleName'),
        }
      ](name='FilterModules'),
      itemCode?: string(name='ItemCode'),
      itemId?: long(name='ItemId'),
      itemName?: string(name='ItemName'),
      moduleMapList?: [ 
        {
          filterModules?: [ 
            {
              moduleCode?: string(name='ModuleCode'),
              moduleId?: long(name='ModuleId'),
              moduleName?: string(name='ModuleName'),
            }
          ](name='FilterModules'),
          moduleCode?: string(name='ModuleCode'),
          moduleId?: long(name='ModuleId'),
          moduleName?: string(name='ModuleName'),
          showModules?: [ 
            {
              moduleCode?: string(name='ModuleCode'),
              moduleId?: long(name='ModuleId'),
              moduleName?: string(name='ModuleName'),
            }
          ](name='ShowModules'),
          spnTypeList?: [ string ](name='SpnTypeList'),
          spnTypeMapList?: [ map[string]DataModuleMapListSpnTypeMapListValue ](name='SpnTypeMapList'),
          spnTypeNameList?: [ 
            {
              code?: string(name='Code'),
              name?: string(name='Name'),
            }
          ](name='SpnTypeNameList'),
        }
      ](name='ModuleMapList'),
      payModeList?: [ 
        {
          code?: string(name='Code'),
          name?: string(name='Name'),
        }
      ](name='PayModeList'),
      pricingModules?: [ 
        {
          moduleCode?: string(name='ModuleCode'),
          moduleId?: long(name='ModuleId'),
          moduleName?: string(name='ModuleName'),
        }
      ](name='PricingModules'),
      spnCommodityCode?: string(name='SpnCommodityCode'),
      spnCommodityName?: string(name='SpnCommodityName'),
      spnDiscountConfigType?: string(name='SpnDiscountConfigType'),
      stepPriceMap?: map[string][ DataStepPriceMapValue       ](name='StepPriceMap'),
    }
  ](name='Data'),
  requestId?: string(name='RequestId'),
}

model GetSavingPlanDeductableCommodityResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetSavingPlanDeductableCommodityResponseBody(name='body'),
}

/**
 * @summary 获取节省计划及可抵扣商品信息
 *
 * @param tmpReq GetSavingPlanDeductableCommodityRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetSavingPlanDeductableCommodityResponse
 */
@context("getSavingPlanDeductableCommodityWithContext")
async function getSavingPlanDeductableCommodityWithOptions(tmpReq: GetSavingPlanDeductableCommodityRequest, runtime: $RuntimeOptions): GetSavingPlanDeductableCommodityResponse {
  tmpReq.validate();
  var request = new GetSavingPlanDeductableCommodityShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.ecIdAccountIds)) {
    request.ecIdAccountIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.ecIdAccountIds, 'EcIdAccountIds', 'json');
  }
  var query = {};
  if (!$isNull(request.ecIdAccountIdsShrink)) {
    query['EcIdAccountIds'] = request.ecIdAccountIdsShrink;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetSavingPlanDeductableCommodity',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 获取节省计划及可抵扣商品信息
 *
 * @param request GetSavingPlanDeductableCommodityRequest
 * @return GetSavingPlanDeductableCommodityResponse
 */
async function getSavingPlanDeductableCommodity(request: GetSavingPlanDeductableCommodityRequest): GetSavingPlanDeductableCommodityResponse {
  var runtime = new $RuntimeOptions{};
  return getSavingPlanDeductableCommodityWithOptions(request, runtime);
}

model GetSavingPlanShareAccountsRequest {
  currentPage?: int32(name='CurrentPage'),
  ecIdAccountIds?: [ 
    {
      accountIds?: [ long ](name='AccountIds'),
      ecId?: string(name='EcId', description='This parameter is required.'),
    }
  ](name='EcIdAccountIds'),
  nbid?: string(name='Nbid'),
  pageSize?: int32(name='PageSize'),
  spnInstanceCode?: string(name='SpnInstanceCode'),
}

model GetSavingPlanShareAccountsShrinkRequest {
  currentPage?: int32(name='CurrentPage'),
  ecIdAccountIdsShrink?: string(name='EcIdAccountIds'),
  nbid?: string(name='Nbid'),
  pageSize?: int32(name='PageSize'),
  spnInstanceCode?: string(name='SpnInstanceCode'),
}

model GetSavingPlanShareAccountsResponseBody = {
  data?: [ 
    {
      accountId?: string(name='AccountId'),
      aliUid?: long(name='AliUid'),
      shareTimeList?: [ 
        {
          shareEndTime?: string(name='ShareEndTime'),
          shareStartTime?: string(name='ShareStartTime'),
        }
      ](name='ShareTimeList'),
    }
  ](name='Data'),
  requestId?: string(name='RequestId'),
}

model GetSavingPlanShareAccountsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetSavingPlanShareAccountsResponseBody(name='body'),
}

/**
 * @summary 获取节省计划实例共享账号信息
 *
 * @param tmpReq GetSavingPlanShareAccountsRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetSavingPlanShareAccountsResponse
 */
@context("getSavingPlanShareAccountsWithContext")
async function getSavingPlanShareAccountsWithOptions(tmpReq: GetSavingPlanShareAccountsRequest, runtime: $RuntimeOptions): GetSavingPlanShareAccountsResponse {
  tmpReq.validate();
  var request = new GetSavingPlanShareAccountsShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.ecIdAccountIds)) {
    request.ecIdAccountIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.ecIdAccountIds, 'EcIdAccountIds', 'json');
  }
  var query = {};
  if (!$isNull(request.currentPage)) {
    query['CurrentPage'] = request.currentPage;
  }
  if (!$isNull(request.ecIdAccountIdsShrink)) {
    query['EcIdAccountIds'] = request.ecIdAccountIdsShrink;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  if (!$isNull(request.pageSize)) {
    query['PageSize'] = request.pageSize;
  }
  if (!$isNull(request.spnInstanceCode)) {
    query['SpnInstanceCode'] = request.spnInstanceCode;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetSavingPlanShareAccounts',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 获取节省计划实例共享账号信息
 *
 * @param request GetSavingPlanShareAccountsRequest
 * @return GetSavingPlanShareAccountsResponse
 */
async function getSavingPlanShareAccounts(request: GetSavingPlanShareAccountsRequest): GetSavingPlanShareAccountsResponse {
  var runtime = new $RuntimeOptions{};
  return getSavingPlanShareAccountsWithOptions(request, runtime);
}

model GetSavingPlanUserDeductRuleRequest {
  currentPage?: int32(name='CurrentPage'),
  ecIdAccountIds?: [ 
    {
      accountIds?: [ long ](name='AccountIds'),
      ecId?: string(name='EcId', description='This parameter is required.'),
    }
  ](name='EcIdAccountIds'),
  nbid?: string(name='Nbid'),
  pageSize?: int32(name='PageSize'),
  spnInstanceCode?: string(name='SpnInstanceCode'),
}

model GetSavingPlanUserDeductRuleShrinkRequest {
  currentPage?: int32(name='CurrentPage'),
  ecIdAccountIdsShrink?: string(name='EcIdAccountIds'),
  nbid?: string(name='Nbid'),
  pageSize?: int32(name='PageSize'),
  spnInstanceCode?: string(name='SpnInstanceCode'),
}

model GetSavingPlanUserDeductRuleResponseBody = {
  data?: [ 
    {
      commodityCode?: string(name='CommodityCode'),
      commodityName?: string(name='CommodityName'),
      moduleCode?: string(name='ModuleCode'),
      moduleName?: string(name='ModuleName'),
      skipDeduct?: boolean(name='SkipDeduct'),
    }
  ](name='Data'),
  requestId?: string(name='RequestId'),
}

model GetSavingPlanUserDeductRuleResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetSavingPlanUserDeductRuleResponseBody(name='body'),
}

/**
 * @summary 获取节省计划实例客户自定义规则
 *
 * @param tmpReq GetSavingPlanUserDeductRuleRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetSavingPlanUserDeductRuleResponse
 */
@context("getSavingPlanUserDeductRuleWithContext")
async function getSavingPlanUserDeductRuleWithOptions(tmpReq: GetSavingPlanUserDeductRuleRequest, runtime: $RuntimeOptions): GetSavingPlanUserDeductRuleResponse {
  tmpReq.validate();
  var request = new GetSavingPlanUserDeductRuleShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.ecIdAccountIds)) {
    request.ecIdAccountIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.ecIdAccountIds, 'EcIdAccountIds', 'json');
  }
  var query = {};
  if (!$isNull(request.currentPage)) {
    query['CurrentPage'] = request.currentPage;
  }
  if (!$isNull(request.ecIdAccountIdsShrink)) {
    query['EcIdAccountIds'] = request.ecIdAccountIdsShrink;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  if (!$isNull(request.pageSize)) {
    query['PageSize'] = request.pageSize;
  }
  if (!$isNull(request.spnInstanceCode)) {
    query['SpnInstanceCode'] = request.spnInstanceCode;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetSavingPlanUserDeductRule',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 获取节省计划实例客户自定义规则
 *
 * @param request GetSavingPlanUserDeductRuleRequest
 * @return GetSavingPlanUserDeductRuleResponse
 */
async function getSavingPlanUserDeductRule(request: GetSavingPlanUserDeductRuleRequest): GetSavingPlanUserDeductRuleResponse {
  var runtime = new $RuntimeOptions{};
  return getSavingPlanUserDeductRuleWithOptions(request, runtime);
}

model ListCouponDeductTagRequest {
  couponId?: string(name='CouponId'),
  ecIdAccountIds?: [ 
    {
      accountIds?: [ long ](name='AccountIds'),
      ecId?: string(name='EcId', description='This parameter is required.'),
    }
  ](name='EcIdAccountIds'),
  nbid?: string(name='Nbid'),
}

model ListCouponDeductTagShrinkRequest {
  couponId?: string(name='CouponId'),
  ecIdAccountIdsShrink?: string(name='EcIdAccountIds'),
  nbid?: string(name='Nbid'),
}

model ListCouponDeductTagResponseBody = {
  data?: [ 
    {
      key?: string(name='Key'),
      value?: string(name='Value'),
    }
  ](name='Data'),
  requestId?: string(name='RequestId'),
}

model ListCouponDeductTagResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListCouponDeductTagResponseBody(name='body'),
}

/**
 * @summary 查询优惠券设置的抵扣标签
 *
 * @param tmpReq ListCouponDeductTagRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListCouponDeductTagResponse
 */
@context("listCouponDeductTagWithContext")
async function listCouponDeductTagWithOptions(tmpReq: ListCouponDeductTagRequest, runtime: $RuntimeOptions): ListCouponDeductTagResponse {
  tmpReq.validate();
  var request = new ListCouponDeductTagShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.ecIdAccountIds)) {
    request.ecIdAccountIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.ecIdAccountIds, 'EcIdAccountIds', 'json');
  }
  var query = {};
  if (!$isNull(request.couponId)) {
    query['CouponId'] = request.couponId;
  }
  if (!$isNull(request.ecIdAccountIdsShrink)) {
    query['EcIdAccountIds'] = request.ecIdAccountIdsShrink;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListCouponDeductTag',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 查询优惠券设置的抵扣标签
 *
 * @param request ListCouponDeductTagRequest
 * @return ListCouponDeductTagResponse
 */
async function listCouponDeductTag(request: ListCouponDeductTagRequest): ListCouponDeductTagResponse {
  var runtime = new $RuntimeOptions{};
  return listCouponDeductTagWithOptions(request, runtime);
}

model ListFundAccountRequest {
  nbid?: string(name='Nbid', description='Level-1 marketplace ID. If this parameter is left empty, the ID of the marketplace to which the current user belongs is used by default.', example='2084210001'),
  queryOnlyInUse?: boolean(name='QueryOnlyInUse', description='Specifies whether to query only the active account list. Default value: false.'),
  queryOnlyManage?: boolean(name='QueryOnlyManage', description='Specifies whether to query only the managed account list. Default value: false.'),
}

model ListFundAccountResponseBody = {
  data?: [ 
    {
      createDate?: string(name='CreateDate', description='Account creation time', example='2024-12-30 12:00:00'),
      fundAccountAdminAccountId?: string(name='FundAccountAdminAccountId', description='Account ID of the MA user associated with the account', example='1232121'),
      fundAccountAdminAccountName?: string(name='FundAccountAdminAccountName', description='Name of the MA user associated with the account', example='云某的账户'),
      fundAccountId?: string(name='FundAccountId', description='Account ID', example='1022231'),
      fundAccountName?: string(name='FundAccountName', description='Account name', example='云某的账户'),
      fundAccountOwnerAccountId?: string(name='FundAccountOwnerAccountId', description='Account ID of the fund account owner', example='132123211'),
      fundAccountStatus?: string(name='FundAccountStatus', description='Account status.
VALID: valid.
FROZEN: frozen.
CLOSED: closed.', example='VALID'),
      fundAccountType?: string(name='FundAccountType', description='Account type.
DIRECT_USER: Alibaba Cloud direct customer account.
RESELLER_QUOTA: ecosystem account.', example='DIRECT_USER'),
      nbid?: string(name='Nbid', description='Level-1 marketplace', example='2684210001'),
      permissions?: [ string ](name='Permissions', description='Set of account permissions that the user has.
using: use.
manage: manage.
owner: own.'),
      site?: string(name='Site', description='Site', example='26842'),
    }
  ](name='Data', description='Data list'),
  metadata?: any(name='Metadata', description='Response metadata', example='{}'),
  requestId?: string(name='RequestId', description='Request ID', example='6000EE23-274B-4E07-A697-FF2E999520A4'),
}

model ListFundAccountResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListFundAccountResponseBody(name='body'),
}

/**
 * @summary Query fund account list
 *
 * @param request ListFundAccountRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListFundAccountResponse
 */
@context("listFundAccountWithContext")
async function listFundAccountWithOptions(request: ListFundAccountRequest, runtime: $RuntimeOptions): ListFundAccountResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  var body : map[string]any = {};
  if (!$isNull(request.queryOnlyInUse)) {
    body['QueryOnlyInUse'] = request.queryOnlyInUse;
  }
  if (!$isNull(request.queryOnlyManage)) {
    body['QueryOnlyManage'] = request.queryOnlyManage;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListFundAccount',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Query fund account list
 *
 * @param request ListFundAccountRequest
 * @return ListFundAccountResponse
 */
async function listFundAccount(request: ListFundAccountRequest): ListFundAccountResponse {
  var runtime = new $RuntimeOptions{};
  return listFundAccountWithOptions(request, runtime);
}

model ListFundAccountPayRelationRequest {
  currentPage?: int32(name='CurrentPage', description='The current page number.', example='1'),
  fundAccountId?: string(name='FundAccountId', description='Fund account ID.

This parameter is required.', example='12323123'),
  nbid?: string(name='Nbid', description='The primary marketplace ID. If this parameter is left empty, the ID of the marketplace to which the current user belongs is used by default.', example='2684201000001'),
  pageSize?: int32(name='PageSize', description='The number of entries per page.', example='20'),
  status?: string(name='Status', description='The relationship status.
valid: valid.
expired: invalid.', example='valid'),
}

model ListFundAccountPayRelationResponseBody = {
  currentPage?: int32(name='CurrentPage', description='The current page number.', example='1'),
  data?: [ 
    {
      accountId?: string(name='AccountId', description='The account ID of the user associated with the payment relationship, that is, the account that uses this account for payment.', example='32812132121'),
      accountName?: string(name='AccountName', description='The user name.', example='云某的名称'),
      ecid?: string(name='Ecid', description='The enterprise entity ID of the user associated with the payment relationship.', example='213231232'),
      effectiveTime?: string(name='EffectiveTime', description='The time when the payment relationship takes effect.', example='2024-12-01 12:00:10'),
      fundAccountId?: string(name='FundAccountId', description='The account ID.', example='123231213'),
      fundAccountOwnerAccountId?: string(name='FundAccountOwnerAccountId', description='The Alibaba Cloud account ID of the account owner.', example='312328912'),
      ineffectiveTime?: string(name='IneffectiveTime', description='The time when the payment relationship expires.', example='2025-01-01 12:12:12'),
      nbid?: string(name='Nbid', description='The primary marketplace.', example='2684210001'),
      operatorName?: string(name='OperatorName', description='The operator name.
When the operator type is aliyun_pk, the operator name is the Alibaba Cloud nickname.
When the operator type is system, the operator name is "Alibaba Cloud assistant".', example='云某的名称'),
      operatorNo?: string(name='OperatorNo', description='The authorized operator.
When the operator type is aliyun_pk, operatorNo is the Alibaba Cloud account ID.', example='1232343423'),
      operatorType?: string(name='OperatorType', description='The type of the authorized operator.
aliyun_pk: user.
system: Alibaba Cloud system.', example='aliyun_pk'),
      relationType?: string(name='RelationType', description='The relationship type, which can be collection relationship or payment relationship.', example='PAYMENT'),
      site?: string(name='Site', description='The site.', example='26842'),
      status?: string(name='Status', description='The relationship status.
valid: valid.
expired: invalid.', example='valid'),
    }
  ](name='Data', description='The data list.'),
  metadata?: any(name='Metadata', description='The response metadata.', example='{}'),
  pageSize?: int32(name='PageSize', description='The number of entries per page.', example='20'),
  requestId?: string(name='RequestId', description='The request ID.', example='79EE7556-0CFD-44EB-9CD6-B3B526E3A85F'),
  totalCount?: int32(name='TotalCount', description='The total number of records.', example='50'),
}

model ListFundAccountPayRelationResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListFundAccountPayRelationResponseBody(name='body'),
}

/**
 * @summary Query payment relationships of an account
 *
 * @param request ListFundAccountPayRelationRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListFundAccountPayRelationResponse
 */
@context("listFundAccountPayRelationWithContext")
async function listFundAccountPayRelationWithOptions(request: ListFundAccountPayRelationRequest, runtime: $RuntimeOptions): ListFundAccountPayRelationResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.currentPage)) {
    query['CurrentPage'] = request.currentPage;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  if (!$isNull(request.pageSize)) {
    query['PageSize'] = request.pageSize;
  }
  var body : map[string]any = {};
  if (!$isNull(request.fundAccountId)) {
    body['FundAccountId'] = request.fundAccountId;
  }
  if (!$isNull(request.status)) {
    body['Status'] = request.status;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListFundAccountPayRelation',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Query payment relationships of an account
 *
 * @param request ListFundAccountPayRelationRequest
 * @return ListFundAccountPayRelationResponse
 */
async function listFundAccountPayRelation(request: ListFundAccountPayRelationRequest): ListFundAccountPayRelationResponse {
  var runtime = new $RuntimeOptions{};
  return listFundAccountPayRelationWithOptions(request, runtime);
}

model ListInvoiceCandidateRequest {
  billingCycles?: [ int32 ](name='BillingCycles', description='List of billing cycles.'),
  businessIds?: [ string ](name='BusinessIds', description='List of business document numbers.'),
  currentPage?: int32(name='CurrentPage', description='The current page number.', example='1'),
  ecIdAccountIds?: [ 
    {
      accountIds?: [ long ](name='AccountIds', description='List of accounts to access. If empty, all accounts under the current entity ID are selected.'),
      ecId?: string(name='EcId', description='Enterprise entity ID.', example='12345'),
    }
  ](name='EcIdAccountIds', description='List of enterprises and accounts. If empty, the current account itself is queried.'),
  endTime?: string(name='EndTime', description='The end time. Format: yyyy-mm-dd hh:mm:ss.', example='2025-07-01 00:00:00'),
  invoiceIssuers?: [ string ](name='InvoiceIssuers', description='List of invoice issuers.'),
  nbid?: string(name='Nbid', description='Primary marketplace ID. If empty, the marketplace ID of the current user is used by default.', example='2684201000001'),
  pageSize?: int32(name='PageSize', description='The number of entries per page.', example='20'),
  startTime?: string(name='StartTime', description='The start time. Format: yyyy-mm-dd hh:mm:ss.', example='2025-06-01 00:00:00'),
  status?: [ int32 ](name='Status', description='List of invoice candidate statuses.'),
  types?: [ int32 ](name='Types', description='List of invoice candidate types.'),
}

model ListInvoiceCandidateShrinkRequest {
  billingCyclesShrink?: string(name='BillingCycles', description='List of billing cycles.'),
  businessIdsShrink?: string(name='BusinessIds', description='List of business document numbers.'),
  currentPage?: int32(name='CurrentPage', description='The current page number.', example='1'),
  ecIdAccountIdsShrink?: string(name='EcIdAccountIds', description='List of enterprises and accounts. If empty, the current account itself is queried.'),
  endTime?: string(name='EndTime', description='The end time. Format: yyyy-mm-dd hh:mm:ss.', example='2025-07-01 00:00:00'),
  invoiceIssuersShrink?: string(name='InvoiceIssuers', description='List of invoice issuers.'),
  nbid?: string(name='Nbid', description='Primary marketplace ID. If empty, the marketplace ID of the current user is used by default.', example='2684201000001'),
  pageSize?: int32(name='PageSize', description='The number of entries per page.', example='20'),
  startTime?: string(name='StartTime', description='The start time. Format: yyyy-mm-dd hh:mm:ss.', example='2025-06-01 00:00:00'),
  statusShrink?: string(name='Status', description='List of invoice candidate statuses.'),
  typesShrink?: string(name='Types', description='List of invoice candidate types.'),
}

model ListInvoiceCandidateResponseBody = {
  currentPage?: int32(name='CurrentPage', description='The current page number.', example='1'),
  data?: [ 
    {
      acceptedOffsetAmount?: string(name='AcceptedOffsetAmount', description='The accepted offset amount.', example='0.01'),
      accountId?: long(name='AccountId', description='The account ID.', example='1990699401005016'),
      accountName?: string(name='AccountName', description='The account name.', example='测试账号'),
      billingCycle?: int32(name='BillingCycle', description='The billing cycle.', example='202506'),
      businessId?: string(name='BusinessId', description='The business document number.', example='202506'),
      businessTime?: string(name='BusinessTime', description='The time when the business event occurred.', example='2025-06-01 00:00:00'),
      commodityCode?: string(name='CommodityCode', description='The commodity code.', example='pts'),
      commodityName?: string(name='CommodityName', description='The commodity name.', example='性能测试'),
      createTime?: string(name='CreateTime', description='The creation time.', example='2025-06-91 00:00:00'),
      id?: string(name='Id', description='The ID of the invoice candidate.', example='12345'),
      invoiceIssuer?: string(name='InvoiceIssuer', description='The invoice issuer.', example='ALIYUN_SERVICE'),
      invoiceableAmount?: string(name='InvoiceableAmount', description='The invoiceable amount.', example='0.01'),
      invoicedAmount?: string(name='InvoicedAmount', description='The invoiced amount.', example='0'),
      offsetAmount?: string(name='OffsetAmount', description='The offset amount.', example='0'),
      productCode?: string(name='ProductCode', description='The product code.', example='pts'),
      productName?: string(name='ProductName', description='The product name.', example='性能测试'),
      resourceOwnerAccountId?: long(name='ResourceOwnerAccountId', description='The ID of the resource owner account.', example='1990699401005016'),
      resourceOwnerAccountName?: string(name='ResourceOwnerAccountName', description='The name of the resource owner account.', example='测试账号'),
      status?: int32(name='Status', description='The status of the invoice candidate.', example='1'),
      totalAmount?: string(name='TotalAmount', description='The total amount.', example='0.01'),
      type?: int32(name='Type', description='The type of the invoice candidate.', example='1'),
    }
  ](name='Data', description='The list of data entries.'),
  metadata?: any(name='Metadata', description='The metadata of the response.', example='{}'),
  pageSize?: int32(name='PageSize', description='The number of entries per page.', example='20'),
  requestId?: string(name='RequestId', description='The request ID.', example='6000EE23-274B-4E07-A697-FF2E999520A4'),
  totalCount?: int32(name='TotalCount', description='The total number of records.', example='100'),
}

model ListInvoiceCandidateResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListInvoiceCandidateResponseBody(name='body'),
}

/**
 * @summary Query invoice candidate data, which can be used for invoicing.
 *
 * @param tmpReq ListInvoiceCandidateRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListInvoiceCandidateResponse
 */
@context("listInvoiceCandidateWithContext")
async function listInvoiceCandidateWithOptions(tmpReq: ListInvoiceCandidateRequest, runtime: $RuntimeOptions): ListInvoiceCandidateResponse {
  tmpReq.validate();
  var request = new ListInvoiceCandidateShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.billingCycles)) {
    request.billingCyclesShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.billingCycles, 'BillingCycles', 'json');
  }
  if (!$isNull(tmpReq.businessIds)) {
    request.businessIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.businessIds, 'BusinessIds', 'json');
  }
  if (!$isNull(tmpReq.ecIdAccountIds)) {
    request.ecIdAccountIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.ecIdAccountIds, 'EcIdAccountIds', 'json');
  }
  if (!$isNull(tmpReq.invoiceIssuers)) {
    request.invoiceIssuersShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.invoiceIssuers, 'InvoiceIssuers', 'json');
  }
  if (!$isNull(tmpReq.status)) {
    request.statusShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.status, 'Status', 'json');
  }
  if (!$isNull(tmpReq.types)) {
    request.typesShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.types, 'Types', 'json');
  }
  var query = {};
  if (!$isNull(request.billingCyclesShrink)) {
    query['BillingCycles'] = request.billingCyclesShrink;
  }
  if (!$isNull(request.businessIdsShrink)) {
    query['BusinessIds'] = request.businessIdsShrink;
  }
  if (!$isNull(request.currentPage)) {
    query['CurrentPage'] = request.currentPage;
  }
  if (!$isNull(request.ecIdAccountIdsShrink)) {
    query['EcIdAccountIds'] = request.ecIdAccountIdsShrink;
  }
  if (!$isNull(request.endTime)) {
    query['EndTime'] = request.endTime;
  }
  if (!$isNull(request.invoiceIssuersShrink)) {
    query['InvoiceIssuers'] = request.invoiceIssuersShrink;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  if (!$isNull(request.pageSize)) {
    query['PageSize'] = request.pageSize;
  }
  if (!$isNull(request.startTime)) {
    query['StartTime'] = request.startTime;
  }
  if (!$isNull(request.statusShrink)) {
    query['Status'] = request.statusShrink;
  }
  if (!$isNull(request.typesShrink)) {
    query['Types'] = request.typesShrink;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListInvoiceCandidate',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Query invoice candidate data, which can be used for invoicing.
 *
 * @param request ListInvoiceCandidateRequest
 * @return ListInvoiceCandidateResponse
 */
async function listInvoiceCandidate(request: ListInvoiceCandidateRequest): ListInvoiceCandidateResponse {
  var runtime = new $RuntimeOptions{};
  return listInvoiceCandidateWithOptions(request, runtime);
}

model ListInvoiceTitleResponseBody = {
  data?: [ 
    {
      accountBankName?: string(name='AccountBankName', description='Bank name.', example='招商银行'),
      accountId?: long(name='AccountId', description='Account ID.', example='1990699401005016'),
      bankAccountNumber?: string(name='BankAccountNumber', description='Basic bank account number.', example='1234567890'),
      createTime?: string(name='CreateTime', description='The time when the invoice title was created.', example='2025-06-01 00:00:00'),
      id?: string(name='Id', description='Invoice title ID.', example='123456'),
      invoiceTitle?: string(name='InvoiceTitle', description='Invoice title.', example='xx公司'),
      registeredAddress?: string(name='RegisteredAddress', description='Registered address.', example='北京市朝阳区'),
      registeredLandline?: string(name='RegisteredLandline', description='Registered landline number.', example='010-12345678'),
      unifiedSocialCreditCode?: string(name='UnifiedSocialCreditCode', description='Unified social credit code. 15, 17, 18, or 20 characters.', example='111111111111111'),
    }
  ](name='Data', description='Invoice title list'),
  metadata?: any(name='Metadata', description='Response metadata.', example='{}'),
  requestId?: string(name='RequestId', description='Request ID.', example='6000EE23-274B-4E07-A697-FF2E999520A4'),
}

model ListInvoiceTitleResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListInvoiceTitleResponseBody(name='body'),
}

/**
 * @summary Query the list of available invoice titles
 *
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListInvoiceTitleResponse
 */
async function listInvoiceTitleWithOptions(runtime: $RuntimeOptions): ListInvoiceTitleResponse {
  var req = new OpenApiUtil.OpenApiRequest{};
  var params = new OpenApiUtil.Params{
    action = 'ListInvoiceTitle',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Query the list of available invoice titles
 *
 * @return ListInvoiceTitleResponse
 */
async function listInvoiceTitle(): ListInvoiceTitleResponse {
  var runtime = new $RuntimeOptions{};
  return listInvoiceTitleWithOptions(runtime);
}

model ListReportDefinitionsRequest {
  nbid?: string(name='Nbid', description='The level-1 marketplace ID. If this parameter is left empty, the marketplace ID of the current user is used by default.', example='2684201000001'),
}

model ListReportDefinitionsResponseBody = {
  metadata?: any(name='Metadata', description='The metadata of the response struct.'),
  reportDefinitions?: [ 
    {
      beginBillingCycle?: string(name='BeginBillingCycle', description='The start billing cycle for push. After the subscription is created, the system automatically pushes data from the start billing cycle to the current time. This parameter does not take effect for monthly bill PDF subscriptions, and historical data is not re-pushed. Data within the last year can be pushed.', example='2025-05'),
      ossBucketName?: string(name='OssBucketName', description='The name of the OSS bucket that stores the files.', example='oss-bill'),
      ossBucketOwnerAccountId?: long(name='OssBucketOwnerAccountId', description='The UID of the OSS bucket owner that stores the files. Specify this parameter when a Bid/Reseller subscription needs to push data to the OSS bucket of a sub-account. The specified account must be a sub-account of the calling account and must be granted the AliyunConsumeDump2OSSRole permission. Regular users do not need to specify this parameter. The calling account is used by default.', example='1234567812345678'),
      ossBucketPath?: string(name='OssBucketPath', description='The storage path of the OSS bucket.', example='bill/'),
      reportSourceName?: string(name='ReportSourceName', description='The subscription source name.', example='OSS'),
      reportSourceType?: string(name='ReportSourceType', description='The subscription source. Valid values: OSS and MC.', example='OSS'),
      reportTaskId?: long(name='ReportTaskId', description='The bill subscription task ID.', example='123321'),
      reportType?: string(name='ReportType', description='The subscription type. Valid values:
- BillingItemDetailForBillingPeriod: billable item consumption details.
- InstanceDetailForBillingPeriod: instance consumption details.
- BillingItemDetailMonthly: billable item consumption summary by billing cycle.', example='BillingItemDetailForBillingPeriod'),
      subscribeCreateTime?: string(name='SubscribeCreateTime', description='The time when the subscription was created.', example='2025-05-21 10:36:31'),
    }
  ](name='ReportDefinitions', description='The data list.'),
  requestId?: string(name='RequestId', description='The request ID.', example='79EE7556-0CFD-44EB-9CD6-B3B526E3A85F'),
}

model ListReportDefinitionsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListReportDefinitionsResponseBody(name='body'),
}

/**
 * @summary Queries the list of subscribed reports.
 *
 * @param request ListReportDefinitionsRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListReportDefinitionsResponse
 */
@context("listReportDefinitionsWithContext")
async function listReportDefinitionsWithOptions(request: ListReportDefinitionsRequest, runtime: $RuntimeOptions): ListReportDefinitionsResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListReportDefinitions',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the list of subscribed reports.
 *
 * @param request ListReportDefinitionsRequest
 * @return ListReportDefinitionsResponse
 */
async function listReportDefinitions(request: ListReportDefinitionsRequest): ListReportDefinitionsResponse {
  var runtime = new $RuntimeOptions{};
  return listReportDefinitionsWithOptions(request, runtime);
}

model ModifyCostCenterRequest {
  costCenterEntityList?: [ 
    {
      costCenterId?: long(name='CostCenterId', description='The ID of the cost center.

This parameter is required.', example='485938'),
      costCenterName?: string(name='CostCenterName', description='The new name of the cost center. The name must be unique within the same account.

This parameter is required.', example='资源组'),
      ownerAccountId?: long(name='OwnerAccountId', description='The ID of the owner to which the cost center belongs.

This parameter is required.', example='1314839403940987'),
    }
  ](name='CostCenterEntityList', description='The list of cost center entities.

This parameter is required.'),
  nbid?: string(name='Nbid', description='The level-1 marketplace ID. If this parameter is left empty, the marketplace ID of the current user is used by default.', example='2684201000001'),
}

model ModifyCostCenterShrinkRequest {
  costCenterEntityListShrink?: string(name='CostCenterEntityList', description='The list of cost center entities.

This parameter is required.'),
  nbid?: string(name='Nbid', description='The level-1 marketplace ID. If this parameter is left empty, the marketplace ID of the current user is used by default.', example='2684201000001'),
}

model ModifyCostCenterResponseBody = {
  costCenterOperateDto?: [ 
    {
      costCenterId?: long(name='CostCenterId', description='The ID of the cost center.', example='485938'),
      isSuccess?: boolean(name='IsSuccess', description='Indicates whether the operation is successful.', example='True'),
      ownerAccountId?: long(name='OwnerAccountId', description='The ID of the owner to which the cost center belongs.', example='1314839403940987'),
    }
  ](name='CostCenterOperateDto', description='The list of data entries.'),
  metadata?: any(name='Metadata', description='The metadata of the response.', example='{}'),
  requestId?: string(name='RequestId', description='The request ID.', example='6000EE23-274B-4E07-A697-FF2E999520A4'),
}

model ModifyCostCenterResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ModifyCostCenterResponseBody(name='body'),
}

/**
 * @summary Modify cost centers
 *
 * @description Modifies one or more cost centers.
 *
 * @param tmpReq ModifyCostCenterRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ModifyCostCenterResponse
 */
@context("modifyCostCenterWithContext")
async function modifyCostCenterWithOptions(tmpReq: ModifyCostCenterRequest, runtime: $RuntimeOptions): ModifyCostCenterResponse {
  tmpReq.validate();
  var request = new ModifyCostCenterShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.costCenterEntityList)) {
    request.costCenterEntityListShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.costCenterEntityList, 'CostCenterEntityList', 'json');
  }
  var query = {};
  if (!$isNull(request.costCenterEntityListShrink)) {
    query['CostCenterEntityList'] = request.costCenterEntityListShrink;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ModifyCostCenter',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Modify cost centers
 *
 * @description Modifies one or more cost centers.
 *
 * @param request ModifyCostCenterRequest
 * @return ModifyCostCenterResponse
 */
async function modifyCostCenter(request: ModifyCostCenterRequest): ModifyCostCenterResponse {
  var runtime = new $RuntimeOptions{};
  return modifyCostCenterWithOptions(request, runtime);
}

model ModifyCostCenterRuleRequest {
  costCenterId?: long(name='CostCenterId', description='Financial unit ID', example='485938'),
  filterExpression?: {
    expressionType?: string(name='ExpressionType', description='Operation type', example='NARY'),
    filterValues?: {
      code?: string(name='Code', description='Condition filter key', example='TAG-test-xxx-key'),
      codeName?: string(name='CodeName', description='Condition filter key name (deprecated)', example='无'),
      selectType?: string(name='SelectType', description='Association between code and value', example='IN'),
      values?: [ string ](name='Values', description='Condition filter value list'),
    }(name='FilterValues', description='Sub-condition filter'),
    operand?: any(name='Operand', description='Filter condition (deprecated)', example='无'),
    operands?: [ any ](name='Operands', description='Condition expression'),
    operatorType?: string(name='OperatorType', description='Relational expression', example='AND'),
  }(name='FilterExpression', description='Rule expression'),
  nbid?: string(name='Nbid', description='Level-1 marketplace ID. If empty, the marketplace ID of the current user is used by default.', example='2684201000001'),
  ownerAccountId?: long(name='OwnerAccountId', description='Owner of the financial unit (deprecated)', example='1234567812345678'),
}

model ModifyCostCenterRuleShrinkRequest {
  costCenterId?: long(name='CostCenterId', description='Financial unit ID', example='485938'),
  filterExpressionShrink?: string(name='FilterExpression', description='Rule expression'),
  nbid?: string(name='Nbid', description='Level-1 marketplace ID. If empty, the marketplace ID of the current user is used by default.', example='2684201000001'),
  ownerAccountId?: long(name='OwnerAccountId', description='Owner of the financial unit (deprecated)', example='1234567812345678'),
}

model ModifyCostCenterRuleResponseBody = {
  data?: long(name='Data', description='Financial unit allocation rule ID', example='5632'),
  metadata?: any(name='Metadata', description='Response metadata', example='{}'),
  requestId?: string(name='RequestId', description='Request ID.', example='6000EE23-274B-4E07-A697-FF2E999520A4'),
}

model ModifyCostCenterRuleResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ModifyCostCenterRuleResponseBody(name='body'),
}

/**
 * @summary Modify financial unit rules
 *
 * @description Modify one or more financial units
 *
 * @param tmpReq ModifyCostCenterRuleRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ModifyCostCenterRuleResponse
 */
@context("modifyCostCenterRuleWithContext")
async function modifyCostCenterRuleWithOptions(tmpReq: ModifyCostCenterRuleRequest, runtime: $RuntimeOptions): ModifyCostCenterRuleResponse {
  tmpReq.validate();
  var request = new ModifyCostCenterRuleShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.filterExpression)) {
    request.filterExpressionShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.filterExpression, 'FilterExpression', 'json');
  }
  var query = {};
  if (!$isNull(request.filterExpressionShrink)) {
    query['FilterExpression'] = request.filterExpressionShrink;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  var body : map[string]any = {};
  if (!$isNull(request.costCenterId)) {
    body['CostCenterId'] = request.costCenterId;
  }
  if (!$isNull(request.ownerAccountId)) {
    body['OwnerAccountId'] = request.ownerAccountId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'ModifyCostCenterRule',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Modify financial unit rules
 *
 * @description Modify one or more financial units
 *
 * @param request ModifyCostCenterRuleRequest
 * @return ModifyCostCenterRuleResponse
 */
async function modifyCostCenterRule(request: ModifyCostCenterRuleRequest): ModifyCostCenterRuleResponse {
  var runtime = new $RuntimeOptions{};
  return modifyCostCenterRuleWithOptions(request, runtime);
}

model PayOrderRequest {
  buyerId?: long(name='BuyerId', description='This parameter is required.', example='1178418164369806'),
  ecIdAccountIds?: [ 
    {
      accountIds?: [ long ](name='AccountIds'),
      ecId?: string(name='EcId', example='123'),
    }
  ](name='EcIdAccountIds'),
  nbid?: string(name='Nbid', example='26888345'),
  orderId?: long(name='OrderId', description='This parameter is required.', example='260591304500425'),
  paySubmitUid?: long(name='PaySubmitUid', description='This parameter is required.', example='201437655683478597'),
  payerId?: long(name='PayerId', description='This parameter is required.', example='201437655683478597'),
  token?: string(name='Token', description='This parameter is required.', example='eyJwYWdlTnVtIjoyLCJwYWdlU2l6ZSI6MTB9'),
}

model PayOrderResponseBody = {
  metadata?: any(name='Metadata', example='{}'),
  orderId?: long(name='OrderId', example='260591304500425'),
  payStatus?: long(name='PayStatus', example='1'),
  payerId?: long(name='PayerId', example='201437655683478597'),
  requestId?: string(name='RequestId', example='D6E068C3-25BC-455A-85FE-45F0B22ECB1F'),
}

model PayOrderResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: PayOrderResponseBody(name='body'),
}

/**
 * @summary 对客订单支付接口
 *
 * @param request PayOrderRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return PayOrderResponse
 */
@context("payOrderWithContext")
async function payOrderWithOptions(request: PayOrderRequest, runtime: $RuntimeOptions): PayOrderResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.buyerId)) {
    query['BuyerId'] = request.buyerId;
  }
  if (!$isNull(request.ecIdAccountIds)) {
    query['EcIdAccountIds'] = request.ecIdAccountIds;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  if (!$isNull(request.orderId)) {
    query['OrderId'] = request.orderId;
  }
  if (!$isNull(request.paySubmitUid)) {
    query['PaySubmitUid'] = request.paySubmitUid;
  }
  if (!$isNull(request.payerId)) {
    query['PayerId'] = request.payerId;
  }
  if (!$isNull(request.token)) {
    query['Token'] = request.token;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'PayOrder',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 对客订单支付接口
 *
 * @param request PayOrderRequest
 * @return PayOrderResponse
 */
async function payOrder(request: PayOrderRequest): PayOrderResponse {
  var runtime = new $RuntimeOptions{};
  return payOrderWithOptions(request, runtime);
}

model QueryCostByCostCenterRequest {
  billingMonth?: int32(name='BillingMonth', description='Billing month (format: YYYYMM)

This parameter is required.', example='202506'),
  displayZeroAmountBills?: boolean(name='DisplayZeroAmountBills', description='Whether to display data rows with zero payable amount', example='false'),
  groupByCostCenterLevel?: boolean(name='GroupByCostCenterLevel', description='Whether to aggregate and display by cost center level', example='false'),
  metrics?: string(name='Metrics', description='Cost type

This parameter is required.', example='REQUIRE_AMOUNT'),
  ownerAccountId?: long(name='OwnerAccountId', description='Account ID that owns the resource', example='1374729705039203'),
}

model QueryCostByCostCenterResponseBody = {
  consumeAmountList?: [ 
    {
      allocatedAmount?: string(name='AllocatedAmount', description='Shared allocation amount', example='20'),
      costCenterCode?: string(name='CostCenterCode', description='Cost center code', example='15945703968#'),
      costCenterId?: long(name='CostCenterId', description='Cost center ID', example='530658'),
      costCenterName?: string(name='CostCenterName', description='Cost center name', example='项目组A'),
      directAmount?: string(name='DirectAmount', description='Cost type amount', example='80'),
      level?: int32(name='Level', description='Level', example='1'),
      ownerAccountId?: long(name='OwnerAccountId', description='Account ID that owns the resource', example='1857464601594004'),
      ownerAccountName?: string(name='OwnerAccountName', description='Account name that owns the resource', example='研发团队测试账号'),
      parentCostCenterId?: long(name='ParentCostCenterId', description='Parent cost center ID', example='637537'),
      preCostCenterId?: long(name='PreCostCenterId', description='Previous cost center ID', example='6375371'),
      totalAllocatedAmount?: string(name='TotalAllocatedAmount', description='Total (sum of cost type amount and shared allocation amount)', example='100'),
      totalAllocatedAmountPercent?: string(name='TotalAllocatedAmountPercent', description='Cost type amount percentage', example='0.01'),
    }
  ](name='ConsumeAmountList', description='Cost center consumption amount details list'),
  metadata?: any(name='Metadata', description='Response structure metadata', example='{}'),
  requestId?: string(name='RequestId', description='Request ID.', example='79EE7556-0CFD-44EB-9CD6-B3B526E3A85F'),
  totalAmount?: string(name='TotalAmount', description='Sum of totalAllocatedAmount across all cost centers', example='130296.64'),
}

model QueryCostByCostCenterResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: QueryCostByCostCenterResponseBody(name='body'),
}

/**
 * @summary Query cost center expense overview
 *
 * @description Query cost center expense overview results for a specified billing period
 *
 * @param request QueryCostByCostCenterRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return QueryCostByCostCenterResponse
 */
@context("queryCostByCostCenterWithContext")
async function queryCostByCostCenterWithOptions(request: QueryCostByCostCenterRequest, runtime: $RuntimeOptions): QueryCostByCostCenterResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.billingMonth)) {
    query['BillingMonth'] = request.billingMonth;
  }
  if (!$isNull(request.displayZeroAmountBills)) {
    query['DisplayZeroAmountBills'] = request.displayZeroAmountBills;
  }
  if (!$isNull(request.groupByCostCenterLevel)) {
    query['GroupByCostCenterLevel'] = request.groupByCostCenterLevel;
  }
  if (!$isNull(request.metrics)) {
    query['Metrics'] = request.metrics;
  }
  if (!$isNull(request.ownerAccountId)) {
    query['OwnerAccountId'] = request.ownerAccountId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'QueryCostByCostCenter',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Query cost center expense overview
 *
 * @description Query cost center expense overview results for a specified billing period
 *
 * @param request QueryCostByCostCenterRequest
 * @return QueryCostByCostCenterResponse
 */
async function queryCostByCostCenter(request: QueryCostByCostCenterRequest): QueryCostByCostCenterResponse {
  var runtime = new $RuntimeOptions{};
  return queryCostByCostCenterWithOptions(request, runtime);
}

model QueryCostCenterRequest {
  currentPage?: int32(name='CurrentPage', description='The current page number.

This parameter is required.', example='1'),
  ecIdAccountIds?: [ 
    {
      accountIds?: [ long ](name='AccountIds', description='The list of accounts to access. If this parameter is left empty, all accounts under the current entity ID are selected.'),
      ecId?: string(name='EcId', description='The ID of the enterprise entity.

This parameter is required.', example='1004064243473974'),
    }
  ](name='EcIdAccountIds', description='The list of enterprises and accounts. If this parameter is left empty, the current account is queried.'),
  nbid?: string(name='Nbid', description='The ID of the primary sales channel. If this parameter is left empty, the sales channel ID of the current user is used by default.', example='2684201000001'),
  ownerAccountId?: long(name='OwnerAccountId', description='The ID of the user who owns the financial unit.

This parameter is required.', example='1314839403940987'),
  pageSize?: int32(name='PageSize', description='The number of entries per page.

This parameter is required.', example='20'),
  parentCostCenterId?: long(name='ParentCostCenterId', description='The ID of the parent financial unit. A value of -1 indicates the root financial unit.

This parameter is required.', example='-1'),
}

model QueryCostCenterShrinkRequest {
  currentPage?: int32(name='CurrentPage', description='The current page number.

This parameter is required.', example='1'),
  ecIdAccountIdsShrink?: string(name='EcIdAccountIds', description='The list of enterprises and accounts. If this parameter is left empty, the current account is queried.'),
  nbid?: string(name='Nbid', description='The ID of the primary sales channel. If this parameter is left empty, the sales channel ID of the current user is used by default.', example='2684201000001'),
  ownerAccountId?: long(name='OwnerAccountId', description='The ID of the user who owns the financial unit.

This parameter is required.', example='1314839403940987'),
  pageSize?: int32(name='PageSize', description='The number of entries per page.

This parameter is required.', example='20'),
  parentCostCenterId?: long(name='ParentCostCenterId', description='The ID of the parent financial unit. A value of -1 indicates the root financial unit.

This parameter is required.', example='-1'),
}

model QueryCostCenterResponseBody = {
  costCenterDtoList?: [ 
    {
      costCenterCode?: string(name='CostCenterCode', description='The code of the financial unit.', example='15945703968#'),
      costCenterId?: long(name='CostCenterId', description='The ID of the financial unit.', example='485938'),
      costCenterName?: string(name='CostCenterName', description='The name of the financial unit. The name must be unique within the same account.', example='资源组'),
      level?: int32(name='Level', description='The level of the financial unit node.', example='loose'),
      ownerAccountId?: long(name='OwnerAccountId', description='The ID of the user who owns the financial unit.', example='1314839403940987'),
      parentCostCenterId?: long(name='ParentCostCenterId', description='The ID of the parent financial unit.', example='-1'),
      prevCostCenterId?: long(name='PrevCostCenterId', description='The ID of the previous financial unit.', example='485996'),
      priority?: int32(name='Priority'),
    }
  ](name='CostCenterDtoList', description='The list of financial unit data.'),
  currentPage?: int32(name='CurrentPage', description='The current page number.', example='1'),
  metadata?: any(name='Metadata', description='The metadata of the response struct.', example='{}'),
  pageSize?: int32(name='PageSize', description='The number of entries per page.', example='20'),
  requestId?: string(name='RequestId', description='Id of the request', example='79EE7556-0CFD-44EB-9CD6-B3B526E3A85F'),
  totalCount?: int32(name='TotalCount', description='The total number of records.', example='30'),
}

model QueryCostCenterResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: QueryCostCenterResponseBody(name='body'),
}

/**
 * @summary Queries financial units.
 *
 * @description Queries a parent financial unit and its child financial units.
 *
 * @param tmpReq QueryCostCenterRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return QueryCostCenterResponse
 */
@context("queryCostCenterWithContext")
async function queryCostCenterWithOptions(tmpReq: QueryCostCenterRequest, runtime: $RuntimeOptions): QueryCostCenterResponse {
  tmpReq.validate();
  var request = new QueryCostCenterShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.ecIdAccountIds)) {
    request.ecIdAccountIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.ecIdAccountIds, 'EcIdAccountIds', 'json');
  }
  var query = {};
  if (!$isNull(request.currentPage)) {
    query['CurrentPage'] = request.currentPage;
  }
  if (!$isNull(request.ecIdAccountIdsShrink)) {
    query['EcIdAccountIds'] = request.ecIdAccountIdsShrink;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  if (!$isNull(request.ownerAccountId)) {
    query['OwnerAccountId'] = request.ownerAccountId;
  }
  if (!$isNull(request.pageSize)) {
    query['PageSize'] = request.pageSize;
  }
  if (!$isNull(request.parentCostCenterId)) {
    query['ParentCostCenterId'] = request.parentCostCenterId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'QueryCostCenter',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries financial units.
 *
 * @description Queries a parent financial unit and its child financial units.
 *
 * @param request QueryCostCenterRequest
 * @return QueryCostCenterResponse
 */
async function queryCostCenter(request: QueryCostCenterRequest): QueryCostCenterResponse {
  var runtime = new $RuntimeOptions{};
  return queryCostCenterWithOptions(request, runtime);
}

model QueryCostCenterResourceRequest {
  costCenterId?: long(name='CostCenterId', description='The ID of the cost center.', example='123456'),
  ecIdAccountIds?: [ 
    {
      accountIds?: [ long ](name='AccountIds', description='The list of accessed accounts. An empty value indicates that all accounts under the current entity ID are selected.'),
      ecId?: string(name='EcId', description='The enterprise entity ID.

This parameter is required.', example='1501603440974415'),
    }
  ](name='EcIdAccountIds', description='The list of enterprises and accounts. An empty value indicates querying the current account.'),
  maxResults?: int32(name='MaxResults', description='The maximum number of records per query.', example='200'),
  nbid?: string(name='Nbid', description='The first-level marketplace ID. If empty, the marketplace ID of the current user is used by default.', example='2684201000001'),
  nextToken?: string(name='NextToken', description='The token for the next query. An empty NextToken indicates that there are no more results.', example='CAESEgoQCg4KCmd'),
  ownerAccountId?: long(name='OwnerAccountId', description='The owner user ID of the cost center.', example='1234567812345678'),
}

model QueryCostCenterResourceResponseBody = {
  costCenterResourceDtoList?: [ 
    {
      addStrategy?: string(name='AddStrategy', example='AUTO_ALLOCATE'),
      addStrategyName?: string(name='AddStrategyName', example='自动分配'),
      applicablePeriodNum?: long(name='ApplicablePeriodNum', example='3'),
      apportionItemCode?: string(name='ApportionItemCode', description='The apportionment item code of the resource.', example='test'),
      apportionItemName?: string(name='ApportionItemName', description='The apportionment item name of the resource.', example='test'),
      commodityCode?: string(name='CommodityCode', description='The commodity code of the resource.', example='otsbag'),
      commodityName?: string(name='CommodityName', description='The commodity name of the resource.', example='云数据库'),
      costCenterCode?: string(name='CostCenterCode', description='The cost center code.', example='code'),
      costCenterCreateTime?: string(name='CostCenterCreateTime', description='The creation time of the cost center.', example='2025-05-18 12:12:25'),
      costCenterId?: long(name='CostCenterId', description='The cost center ID.', example='123456'),
      costCenterName?: string(name='CostCenterName', description='The cost center name.', example='test'),
      costCenterUpdateTime?: string(name='CostCenterUpdateTime', description='The last update time of the cost center.', example='2025-05-18 16:12:25'),
      financeUnitRuleVersion?: long(name='FinanceUnitRuleVersion', example='3'),
      instanceId?: string(name='InstanceId', description='The billing granularity ID.', example='cn-hangzhou;standard'),
      masterCommodityCode?: string(name='MasterCommodityCode', example='ecs'),
      masterInstanceId?: string(name='MasterInstanceId', example='i-xxxxx'),
      ownerAccountId?: long(name='OwnerAccountId', description='The customer ID to which the cost center belongs.', example='1234567812345678'),
      ownerAccountName?: string(name='OwnerAccountName', description='The customer name to which the cost center belongs.', example='客户A'),
      parentCostCenterId?: long(name='ParentCostCenterId', description='The parent cost center ID.', example='123456'),
      pipCode?: string(name='PipCode', description='The product code, which is the same as the product code in Cost Center bills.', example='rds'),
      pipName?: string(name='PipName', description='The product name.', example='云数据库'),
      recentBillingMonth?: long(name='RecentBillingMonth', example='202509'),
      regionName?: string(name='RegionName', example='上海'),
      regionNo?: string(name='RegionNo', example='cn-shanghai'),
      resourceGroup?: string(name='ResourceGroup', description='The resource group to which the resource belongs.', example='默认资源组'),
      resourceId?: string(name='ResourceId', description='The resource ID.', example='OSSBAG-cn-v0h1s4hma018'),
      resourceNick?: string(name='ResourceNick', description='The custom alias of the resource.', example='testResource'),
      resourceSource?: string(name='ResourceSource', description='The resource source. Valid values:
- AUTO_ALLOCATE: automatically allocated.
- MANUAL_ALLOCATE: manually allocated.', example='MANUAL_ALLOCATE'),
      resourceTag?: string(name='ResourceTag', description='The tag of the resource.', example='tag'),
      resourceType?: string(name='ResourceType', description='The resource type.', example='FPT_ossbag_absolute_Storage_bj'),
      resourceUpdateTime?: string(name='ResourceUpdateTime', description='The last update time of the cost center. This is a reserved field and is currently unavailable.', example='2025-05-18 16:12:25'),
      resourceUserId?: long(name='ResourceUserId', description='The owner user ID of the resource.', example='1234567812345678'),
      resourceUserName?: string(name='ResourceUserName', description='The owner username of the resource.', example='test@test.aliyun.com'),
      rootCostCenterId?: long(name='RootCostCenterId', description='The root cost center ID.', example='-1'),
      startBillingMonth?: long(name='StartBillingMonth', example='202509'),
    }
  ](name='CostCenterResourceDtoList', description='The data list.'),
  maxResults?: int32(name='MaxResults', description='The maximum number of records per query.', example='200'),
  metadata?: any(name='Metadata', description='The metadata of the response structure.', example='{}'),
  nextToken?: string(name='NextToken', description='The token for the next query. An empty NextToken indicates that there are no more results.

This parameter is required.', example='eyJwYWdlTnVtIjoyLCJwYWdlU2l6ZSI6MTB9'),
  requestId?: string(name='RequestId', description='The request ID.', example='79EE7556-0CFD-44EB-9CD6-B3B526E3A85F'),
  totalCount?: int32(name='TotalCount', description='The total number of records.', example='2000'),
}

model QueryCostCenterResourceResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: QueryCostCenterResourceResponseBody(name='body'),
}

/**
 * @summary Queries the list of resource instances that belong to a cost center of the user. When CostCenterId is 0, it queries unallocated primary and sub-resource instances.
 *
 * @param request QueryCostCenterResourceRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return QueryCostCenterResourceResponse
 */
@context("queryCostCenterResourceWithContext")
async function queryCostCenterResourceWithOptions(request: QueryCostCenterResourceRequest, runtime: $RuntimeOptions): QueryCostCenterResourceResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.ecIdAccountIds)) {
    query['EcIdAccountIds'] = request.ecIdAccountIds;
  }
  if (!$isNull(request.maxResults)) {
    query['MaxResults'] = request.maxResults;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  if (!$isNull(request.nextToken)) {
    query['NextToken'] = request.nextToken;
  }
  var body : map[string]any = {};
  if (!$isNull(request.costCenterId)) {
    body['CostCenterId'] = request.costCenterId;
  }
  if (!$isNull(request.ownerAccountId)) {
    body['OwnerAccountId'] = request.ownerAccountId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'QueryCostCenterResource',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the list of resource instances that belong to a cost center of the user. When CostCenterId is 0, it queries unallocated primary and sub-resource instances.
 *
 * @param request QueryCostCenterResourceRequest
 * @return QueryCostCenterResourceResponse
 */
async function queryCostCenterResource(request: QueryCostCenterResourceRequest): QueryCostCenterResourceResponse {
  var runtime = new $RuntimeOptions{};
  return queryCostCenterResourceWithOptions(request, runtime);
}

model QueryCostCenterRuleRequest {
  costCenterId?: long(name='CostCenterId', description='The ID of the cost center.', example='597745'),
  ecIdAccountIds?: [ 
    {
      accountIds?: [ long ](name='AccountIds', description='List of accounts to access. If empty, all accounts under the current entity ID are selected.'),
      ecId?: string(name='EcId', description='Enterprise entity ID.

This parameter is required.', example='1004064243473974'),
    }
  ](name='EcIdAccountIds', description='List of enterprises and accounts. If empty, queries the current account itself.'),
  nbid?: string(name='Nbid', description='Primary marketplace ID. If empty, the marketplace ID of the current user is used by default.', example='2684201000001'),
}

model QueryCostCenterRuleResponseBody = {
  costCenterId?: long(name='CostCenterId', description='The cost center ID.', example='597745'),
  filterExpression?: {
    expressionType?: string(name='ExpressionType', description='The operation type.', example='NARY'),
    filterValues?: {
      code?: string(name='Code', description='The condition filter key.', example='TAG-test-xxx-key'),
      codeName?: string(name='CodeName', description='The condition filter key name (deprecated).', example='无'),
      selectType?: string(name='SelectType', description='The association between code and value.', example='IN'),
      values?: [ string ](name='Values', description='The attribute value types corresponding to the module code. Valid values:
- single_float: single value type.
- range_float: range value type.'),
    }(name='FilterValues', description='The sub-condition filter.'),
    operand?: any(name='Operand', description='The filter condition (deprecated).', example='无'),
    operands?: [ any ](name='Operands', description='The condition expression.'),
    operatorType?: string(name='OperatorType', description='The relational expression.', example='AND'),
  }(name='FilterExpression', description='The rule expression.'),
  gmtCreate?: string(name='GmtCreate', description='The creation time.', example='Tue Nov 12 14:49:43 CST 2024'),
  gmtModified?: string(name='GmtModified', description='The modification time.', example='Wed Oct 16 10:15:37 CST 2024'),
  id?: long(name='Id', description='The ID of the cost center auto-allocation rule.', example='32048'),
  isDeleted?: int32(name='IsDeleted', description='The deletion status. Valid values:
* **0**: active.
* **1**: inactive.', example='false'),
  metadata?: any(name='Metadata', description='Response structure metadata.', example='{}'),
  ownerAccountId?: long(name='OwnerAccountId', description='The owner user of the cost center.', example='1977800748053695'),
  requestId?: string(name='RequestId', description='The ID of the request.', example='UUID'),
  rootCostCenterId?: long(name='RootCostCenterId', description='The root cost center ID.', example='0'),
  status?: string(name='Status', description='The status. Valid values:
- on: active.
- off: inactive.', example='on'),
}

model QueryCostCenterRuleResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: QueryCostCenterRuleResponseBody(name='body'),
}

/**
 * @summary Query cost center rules
 *
 * @description Query parent cost center and its child cost centers.
 *
 * @param request QueryCostCenterRuleRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return QueryCostCenterRuleResponse
 */
@context("queryCostCenterRuleWithContext")
async function queryCostCenterRuleWithOptions(request: QueryCostCenterRuleRequest, runtime: $RuntimeOptions): QueryCostCenterRuleResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.ecIdAccountIds)) {
    query['EcIdAccountIds'] = request.ecIdAccountIds;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  var body : map[string]any = {};
  if (!$isNull(request.costCenterId)) {
    body['CostCenterId'] = request.costCenterId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'QueryCostCenterRule',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Query cost center rules
 *
 * @description Query parent cost center and its child cost centers.
 *
 * @param request QueryCostCenterRuleRequest
 * @return QueryCostCenterRuleResponse
 */
async function queryCostCenterRule(request: QueryCostCenterRuleRequest): QueryCostCenterRuleResponse {
  var runtime = new $RuntimeOptions{};
  return queryCostCenterRuleWithOptions(request, runtime);
}

model QueryCostCenterShareRuleRequest {
  ecIdAccountIds?: [ 
    {
      accountIds?: [ long ](name='AccountIds', description='The list of accounts to access. If empty, all accounts under the current entity ID are selected.'),
      ecId?: string(name='EcId', description='The enterprise entity ID.

This parameter is required.', example='1004064243473974'),
    }
  ](name='EcIdAccountIds', description='The list of enterprises and accounts. If empty, the current account is queried.'),
  maxResults?: int32(name='MaxResults', description='Reserved field.', example='20'),
  nbid?: string(name='Nbid', description='The primary marketplace ID. If empty, the marketplace ID of the current user is used by default.', example='2684201000001'),
  nextToken?: string(name='NextToken', description='Reserved field.', example='AAAAAV3MpHK1AP0pfERHZN5pu6nYCisJwqt18pP5E9yb47iu'),
  ownerAccountId?: long(name='OwnerAccountId', description='The owner account ID of the cost center.', example='1529600453335198'),
}

model QueryCostCenterShareRuleResponseBody = {
  data?: [ 
    {
      fromCostCenterShareRuleDetails?: [ 
        {
          costCenterCode?: string(name='CostCenterCode', description='The cost center code.', example='970354711219#'),
          costCenterId?: long(name='CostCenterId', description='The cost center ID.', example='637127'),
          costCenterName?: string(name='CostCenterName', description='The cost center name.', example='成本计划'),
          gmtCreate?: string(name='GmtCreate', description='The creation time.', example='2025-07-16 13:49:59'),
          gmtModified?: string(name='GmtModified', description='The modification time.', example='2025-07-16 13:49:59'),
          ownerAccountId?: long(name='OwnerAccountId', description='The owner account ID of the cost center.', example='1529600453335198'),
          parentCostCenterId?: long(name='ParentCostCenterId', description='The parent cost center ID.', example='637537'),
          prevCostCenterId?: long(name='PrevCostCenterId', description='The previous cost center ID.', example='583059'),
          rootCostCenterId?: long(name='RootCostCenterId', description='The root cost center ID.', example='583050'),
        }
      ](name='FromCostCenterShareRuleDetails', description='The source cost center rule details.'),
      gmtModified?: string(name='GmtModified', example='2026-01-01 00:00:00'),
      ownerAccountId?: long(name='OwnerAccountId', description='The owner account ID of the cost center.', example='1529600453335198'),
      shareRuleId?: long(name='ShareRuleId', description='The sharing rule ID.', example='1826'),
      shareRuleName?: string(name='ShareRuleName', description='The sharing rule name.', example='test'),
      shareRuleType?: string(name='ShareRuleType', description='The sharing rule type.', example='AVERAGE'),
      toCostCenterShareRuleDetails?: [ 
        {
          costCenterCode?: string(name='CostCenterCode', description='The cost center code.', example='970354711215#'),
          costCenterId?: long(name='CostCenterId', description='The cost center ID.', example='637127'),
          costCenterName?: string(name='CostCenterName', description='The cost center name.', example='成本分摊'),
          gmtCreate?: string(name='GmtCreate', description='The creation time.', example='2025-07-16 13:49:59'),
          gmtModified?: string(name='GmtModified', description='The modification time.', example='2025-07-16 13:49:59'),
          ownerAccountId?: long(name='OwnerAccountId', description='The owner account ID of the cost center.', example='1529600453335198'),
          parentCostCenterId?: long(name='ParentCostCenterId', description='The parent cost center ID.', example='637537'),
          prevCostCenterId?: long(name='PrevCostCenterId', description='The previous cost center ID.', example='583055'),
          rootCostCenterId?: long(name='RootCostCenterId', description='The root cost center ID.', example='583050'),
          shareRatio?: double(name='ShareRatio', description='The sharing ratio.', example='0.2'),
        }
      ](name='ToCostCenterShareRuleDetails', description='The target cost center rule details.'),
    }
  ](name='Data', description='The returned data list.'),
  maxResults?: int32(name='MaxResults', description='Reserved field.', example='20'),
  metadata?: any(name='Metadata', description='The response metadata.', example='{}'),
  nextToken?: string(name='NextToken', description='Reserved field.', example='eyJwYWdlTnVtIjoyLCJwYWdlU2l6ZSI6NH0='),
  requestId?: string(name='RequestId', description='The request ID.', example='79EE7556-0CFD-44EB-9CD6-B3B526E3A85F'),
  totalCount?: int32(name='TotalCount', description='The total number of records.', example='20'),
}

model QueryCostCenterShareRuleResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: QueryCostCenterShareRuleResponseBody(name='body'),
}

/**
 * @summary Query cost center sharing rules
 *
 * @description Queries the sharing rules of user cost centers.
 *
 * @param request QueryCostCenterShareRuleRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return QueryCostCenterShareRuleResponse
 */
@context("queryCostCenterShareRuleWithContext")
async function queryCostCenterShareRuleWithOptions(request: QueryCostCenterShareRuleRequest, runtime: $RuntimeOptions): QueryCostCenterShareRuleResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.ecIdAccountIds)) {
    query['EcIdAccountIds'] = request.ecIdAccountIds;
  }
  if (!$isNull(request.maxResults)) {
    query['MaxResults'] = request.maxResults;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  if (!$isNull(request.nextToken)) {
    query['NextToken'] = request.nextToken;
  }
  if (!$isNull(request.ownerAccountId)) {
    query['OwnerAccountId'] = request.ownerAccountId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'QueryCostCenterShareRule',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Query cost center sharing rules
 *
 * @description Queries the sharing rules of user cost centers.
 *
 * @param request QueryCostCenterShareRuleRequest
 * @return QueryCostCenterShareRuleResponse
 */
async function queryCostCenterShareRule(request: QueryCostCenterShareRuleRequest): QueryCostCenterShareRuleResponse {
  var runtime = new $RuntimeOptions{};
  return queryCostCenterShareRuleWithOptions(request, runtime);
}

model QueryMonthlySlaListRequest {
  currentPage?: int32(name='CurrentPage', description='The current page number. Default value: 1, which indicates the first page.', example='1'),
  ecIdAccountIds?: [ 
    {
      accountIds?: [ long ](name='AccountIds', description='The list of accounts to query. If this parameter is left empty, all accounts under the current entity ID are selected.'),
      ecId?: string(name='EcId', description='The enterprise entity ID.', example='1501603440974415'),
    }
  ](name='EcIdAccountIds', description='The list of enterprise entities and accounts. If this parameter is left empty, the current account is queried.'),
  instanceIds?: [ string ](name='InstanceIds', description='Optional. Filter by instance ID.', example='["instance_1","instance_2"]'),
  months?: [ int32 ](name='Months', description='Optional. Month list in yyyyMM format.', example='[202602,202603]'),
  nbid?: string(name='Nbid', description='The level-1 marketplace ID. If this parameter is left empty, the marketplace ID of the current user is used by default.', example='2684201000001'),
  pageSize?: int32(name='PageSize', description='The number of entries per page. Default value: 10.', example='10'),
  payStatuses?: [ int32 ](name='PayStatuses', description='Optional. Filter by compensation status. Valid values: 0 and 1.'),
  productCodes?: [ string ](name='ProductCodes', description='Optional. Filter by product code.', example='["ecs","oss"]'),
}

model QueryMonthlySlaListResponseBody = {
  currentPage?: int32(name='CurrentPage', description='The current page number.', example='1'),
  data?: [ 
    {
      availableRate?: double(name='AvailableRate', description='The service availability.', example='99.9155'),
      damagedId?: string(name='DamagedId', description='The unique identifier of the damage record, which is used for targeted compensation application.', example='9b7***9413'),
      instanceId?: string(name='InstanceId', description='The instance ID.', example='OSSBAG-cn-0xl0n****003'),
      month?: int32(name='Month', description='The month in yyyyMM format.', example='202603'),
      monthlyServiceCharge?: double(name='MonthlyServiceCharge', description='The monthly service charge.', example='365.00'),
      payDescription?: string(name='PayDescription', description='The compensation description.', example='SLA赔付'),
      payRate?: double(name='PayRate', description='The compensation ratio. Unit: %.', example='10'),
      payStatus?: int32(name='PayStatus', description='The compensation status. Valid values: 0 (uncompensated) and 1 (compensated).', example='1'),
      productCode?: string(name='ProductCode', description='The product code.', example='oss'),
      shouldPaySum?: double(name='ShouldPaySum', description='The compensation amount.', example='36.5'),
    }
  ](name='Data', description='The data list.'),
  metadata?: any(name='Metadata', description='The response metadata.', example='{}'),
  pageSize?: int32(name='PageSize', description='The number of entries per page.', example='10'),
  requestId?: string(name='RequestId', description='The request ID.', example='6000EE23-274B-4E07-A697-FF2E999520A4'),
  totalCount?: int32(name='TotalCount', description='The total number of records.', example='5'),
}

model QueryMonthlySlaListResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: QueryMonthlySlaListResponseBody(name='body'),
}

/**
 * @summary Queries the SLA compensation list of a user.
 *
 * @description Queries the SLA compensation details list of a user. Only data from the last two months is available.
 *
 * @param request QueryMonthlySlaListRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return QueryMonthlySlaListResponse
 */
@context("queryMonthlySlaListWithContext")
async function queryMonthlySlaListWithOptions(request: QueryMonthlySlaListRequest, runtime: $RuntimeOptions): QueryMonthlySlaListResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.currentPage)) {
    query['CurrentPage'] = request.currentPage;
  }
  if (!$isNull(request.ecIdAccountIds)) {
    query['EcIdAccountIds'] = request.ecIdAccountIds;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  if (!$isNull(request.pageSize)) {
    query['PageSize'] = request.pageSize;
  }
  var body : map[string]any = {};
  if (!$isNull(request.instanceIds)) {
    body['InstanceIds'] = request.instanceIds;
  }
  if (!$isNull(request.months)) {
    body['Months'] = request.months;
  }
  if (!$isNull(request.payStatuses)) {
    body['PayStatuses'] = request.payStatuses;
  }
  if (!$isNull(request.productCodes)) {
    body['ProductCodes'] = request.productCodes;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'QueryMonthlySlaList',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the SLA compensation list of a user.
 *
 * @description Queries the SLA compensation details list of a user. Only data from the last two months is available.
 *
 * @param request QueryMonthlySlaListRequest
 * @return QueryMonthlySlaListResponse
 */
async function queryMonthlySlaList(request: QueryMonthlySlaListRequest): QueryMonthlySlaListResponse {
  var runtime = new $RuntimeOptions{};
  return queryMonthlySlaListWithOptions(request, runtime);
}

model SaveCostCenterShareRuleRequest {
  createShareRuleList?: [ 
    {
      fromCostCenterList?: [ long ](name='FromCostCenterList', description='The list of source cost centers.'),
      shareRatioList?: [ double ](name='ShareRatioList', description='The list of sharing ratios.'),
      shareRuleName?: string(name='ShareRuleName', description='The name of the sharing rule.', example='test'),
      shareType?: string(name='ShareType', description='The type of the sharing rule.

This parameter is required.', example='RATIO'),
      toCostCenterList?: [ long ](name='ToCostCenterList', description='The list of target cost centers.'),
    }
  ](name='CreateShareRuleList', description='The list of sharing rules to create.'),
  modifyShareRuleList?: [ 
    {
      fromCostCenterList?: [ long ](name='FromCostCenterList', description='The list of source cost centers.'),
      shareRatioList?: [ double ](name='ShareRatioList', description='The list of sharing ratios.'),
      shareRuleId?: long(name='ShareRuleId', description='The ID of the sharing rule.

This parameter is required.', example='1828'),
      shareRuleName?: string(name='ShareRuleName', description='The name of the sharing rule.', example='test'),
      shareType?: string(name='ShareType', description='The type of the sharing rule.

This parameter is required.', example='CUSTOM'),
      toCostCenterList?: [ long ](name='ToCostCenterList', description='The list of target cost centers.'),
    }
  ](name='ModifyShareRuleList', description='The list of sharing rules to modify.'),
  nbid?: string(name='Nbid', description='The primary marketplace ID. If left empty, the marketplace ID of the current user is used by default.', example='2684201000001'),
  ownerAccountId?: long(name='OwnerAccountId', description='The user ID of the cost center owner.', example='1977800748053695'),
  removeShareRuleList?: [ long ](name='RemoveShareRuleList', description='The list of sharing rules to delete.'),
}

model SaveCostCenterShareRuleShrinkRequest {
  createShareRuleListShrink?: string(name='CreateShareRuleList', description='The list of sharing rules to create.'),
  modifyShareRuleListShrink?: string(name='ModifyShareRuleList', description='The list of sharing rules to modify.'),
  nbid?: string(name='Nbid', description='The primary marketplace ID. If left empty, the marketplace ID of the current user is used by default.', example='2684201000001'),
  ownerAccountId?: long(name='OwnerAccountId', description='The user ID of the cost center owner.', example='1977800748053695'),
  removeShareRuleListShrink?: string(name='RemoveShareRuleList', description='The list of sharing rules to delete.'),
}

model SaveCostCenterShareRuleResponseBody = {
  metadata?: any(name='Metadata', description='The metadata of the response.', example='{}'),
  requestId?: string(name='RequestId', description='The request ID.', example='6000EE23-274B-4E07-A697-FF2E999520A4'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model SaveCostCenterShareRuleResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: SaveCostCenterShareRuleResponseBody(name='body'),
}

/**
 * @summary Modifies cost center sharing rules, including creating, modifying, and deleting sharing rules.
 *
 * @param tmpReq SaveCostCenterShareRuleRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return SaveCostCenterShareRuleResponse
 */
@context("saveCostCenterShareRuleWithContext")
async function saveCostCenterShareRuleWithOptions(tmpReq: SaveCostCenterShareRuleRequest, runtime: $RuntimeOptions): SaveCostCenterShareRuleResponse {
  tmpReq.validate();
  var request = new SaveCostCenterShareRuleShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.createShareRuleList)) {
    request.createShareRuleListShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.createShareRuleList, 'CreateShareRuleList', 'json');
  }
  if (!$isNull(tmpReq.modifyShareRuleList)) {
    request.modifyShareRuleListShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.modifyShareRuleList, 'ModifyShareRuleList', 'json');
  }
  if (!$isNull(tmpReq.removeShareRuleList)) {
    request.removeShareRuleListShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.removeShareRuleList, 'RemoveShareRuleList', 'json');
  }
  var query = {};
  if (!$isNull(request.createShareRuleListShrink)) {
    query['CreateShareRuleList'] = request.createShareRuleListShrink;
  }
  if (!$isNull(request.modifyShareRuleListShrink)) {
    query['ModifyShareRuleList'] = request.modifyShareRuleListShrink;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  if (!$isNull(request.ownerAccountId)) {
    query['OwnerAccountId'] = request.ownerAccountId;
  }
  if (!$isNull(request.removeShareRuleListShrink)) {
    query['RemoveShareRuleList'] = request.removeShareRuleListShrink;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'SaveCostCenterShareRule',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Modifies cost center sharing rules, including creating, modifying, and deleting sharing rules.
 *
 * @param request SaveCostCenterShareRuleRequest
 * @return SaveCostCenterShareRuleResponse
 */
async function saveCostCenterShareRule(request: SaveCostCenterShareRuleRequest): SaveCostCenterShareRuleResponse {
  var runtime = new $RuntimeOptions{};
  return saveCostCenterShareRuleWithOptions(request, runtime);
}

model SetFundAccountCreditAmountRequest {
  creditAmount?: string(name='CreditAmount', description='Credit limit

This parameter is required.', example='500'),
  currency?: string(name='Currency', description='Currency for the credit control limit. Currently, only CNY is supported in mainland China, and only USD is supported for international use.

This parameter is required.', example='CNY'),
  fundAccountId?: long(name='FundAccountId', description='Fund account ID. If not specified, the account owned by the current account (owner) is used by default.', example='1232312'),
}

model SetFundAccountCreditAmountResponseBody = {
  metadata?: any(name='Metadata', description='Response metadata', example='{}'),
  requestId?: string(name='RequestId', description='Request ID', example='79EE7556-0CFD-44EB-9CD6-B3B526E3A85F'),
}

model SetFundAccountCreditAmountResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: SetFundAccountCreditAmountResponseBody(name='body'),
}

/**
 * @summary Set the credit control limit for a fund account
 *
 * @param request SetFundAccountCreditAmountRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return SetFundAccountCreditAmountResponse
 */
@context("setFundAccountCreditAmountWithContext")
async function setFundAccountCreditAmountWithOptions(request: SetFundAccountCreditAmountRequest, runtime: $RuntimeOptions): SetFundAccountCreditAmountResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.creditAmount)) {
    body['CreditAmount'] = request.creditAmount;
  }
  if (!$isNull(request.currency)) {
    body['Currency'] = request.currency;
  }
  if (!$isNull(request.fundAccountId)) {
    body['FundAccountId'] = request.fundAccountId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'SetFundAccountCreditAmount',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Set the credit control limit for a fund account
 *
 * @param request SetFundAccountCreditAmountRequest
 * @return SetFundAccountCreditAmountResponse
 */
async function setFundAccountCreditAmount(request: SetFundAccountCreditAmountRequest): SetFundAccountCreditAmountResponse {
  var runtime = new $RuntimeOptions{};
  return setFundAccountCreditAmountWithOptions(request, runtime);
}

model SetFundAccountLowAvailableAmountAlarmRequest {
  fundAccountId?: long(name='FundAccountId', description='Fund account ID. If not specified, the account ID owned by the current account (owner) is used by default.', example='12321213'),
  thresholdAmount?: string(name='ThresholdAmount', description='Low balance alert threshold.

This parameter is required.', example='100'),
}

model SetFundAccountLowAvailableAmountAlarmResponseBody = {
  data?: boolean(name='Data', description='The returned data.', example='true'),
  metadata?: any(name='Metadata', description='The response metadata.', example='{}'),
  requestId?: string(name='RequestId', description='The request ID.', example='08108BF5-1AA3-518E-9986-95A3616E8DA9'),
}

model SetFundAccountLowAvailableAmountAlarmResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: SetFundAccountLowAvailableAmountAlarmResponseBody(name='body'),
}

/**
 * @summary Set Fund Account Low Balance Alert
 *
 * @param request SetFundAccountLowAvailableAmountAlarmRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return SetFundAccountLowAvailableAmountAlarmResponse
 */
@context("setFundAccountLowAvailableAmountAlarmWithContext")
async function setFundAccountLowAvailableAmountAlarmWithOptions(request: SetFundAccountLowAvailableAmountAlarmRequest, runtime: $RuntimeOptions): SetFundAccountLowAvailableAmountAlarmResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.fundAccountId)) {
    body['FundAccountId'] = request.fundAccountId;
  }
  if (!$isNull(request.thresholdAmount)) {
    body['ThresholdAmount'] = request.thresholdAmount;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'SetFundAccountLowAvailableAmountAlarm',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Set Fund Account Low Balance Alert
 *
 * @param request SetFundAccountLowAvailableAmountAlarmRequest
 * @return SetFundAccountLowAvailableAmountAlarmResponse
 */
async function setFundAccountLowAvailableAmountAlarm(request: SetFundAccountLowAvailableAmountAlarmRequest): SetFundAccountLowAvailableAmountAlarmResponse {
  var runtime = new $RuntimeOptions{};
  return setFundAccountLowAvailableAmountAlarmWithOptions(request, runtime);
}

model SetSavingPlanUserDeductRuleRequest {
  ecIdAccountIds?: [ 
    {
      accountIds?: [ long ](name='AccountIds'),
      ecId?: string(name='EcId'),
    }
  ](name='EcIdAccountIds'),
  nbid?: string(name='Nbid'),
  spnInstanceCode?: string(name='SpnInstanceCode'),
  userDeductRules?: [ 
    {
      commodityCode?: string(name='CommodityCode'),
      moduleCode?: string(name='ModuleCode'),
      skipDeduct?: boolean(name='SkipDeduct'),
    }
  ](name='UserDeductRules'),
}

model SetSavingPlanUserDeductRuleShrinkRequest {
  ecIdAccountIdsShrink?: string(name='EcIdAccountIds'),
  nbid?: string(name='Nbid'),
  spnInstanceCode?: string(name='SpnInstanceCode'),
  userDeductRulesShrink?: string(name='UserDeductRules'),
}

model SetSavingPlanUserDeductRuleResponseBody = {
  data?: boolean(name='Data'),
  requestId?: string(name='RequestId'),
}

model SetSavingPlanUserDeductRuleResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: SetSavingPlanUserDeductRuleResponseBody(name='body'),
}

/**
 * @summary 设置节省计划用户级抵扣规则
 *
 * @param tmpReq SetSavingPlanUserDeductRuleRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return SetSavingPlanUserDeductRuleResponse
 */
@context("setSavingPlanUserDeductRuleWithContext")
async function setSavingPlanUserDeductRuleWithOptions(tmpReq: SetSavingPlanUserDeductRuleRequest, runtime: $RuntimeOptions): SetSavingPlanUserDeductRuleResponse {
  tmpReq.validate();
  var request = new SetSavingPlanUserDeductRuleShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.ecIdAccountIds)) {
    request.ecIdAccountIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.ecIdAccountIds, 'EcIdAccountIds', 'json');
  }
  if (!$isNull(tmpReq.userDeductRules)) {
    request.userDeductRulesShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.userDeductRules, 'UserDeductRules', 'json');
  }
  var query = {};
  if (!$isNull(request.ecIdAccountIdsShrink)) {
    query['EcIdAccountIds'] = request.ecIdAccountIdsShrink;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  var body : map[string]any = {};
  if (!$isNull(request.spnInstanceCode)) {
    body['SpnInstanceCode'] = request.spnInstanceCode;
  }
  if (!$isNull(request.userDeductRulesShrink)) {
    body['UserDeductRules'] = request.userDeductRulesShrink;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'SetSavingPlanUserDeductRule',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 设置节省计划用户级抵扣规则
 *
 * @param request SetSavingPlanUserDeductRuleRequest
 * @return SetSavingPlanUserDeductRuleResponse
 */
async function setSavingPlanUserDeductRule(request: SetSavingPlanUserDeductRuleRequest): SetSavingPlanUserDeductRuleResponse {
  var runtime = new $RuntimeOptions{};
  return setSavingPlanUserDeductRuleWithOptions(request, runtime);
}

model SubmitSlaCouponApplyRequest {
  damagedIds?: [ string ](name='DamagedIds', description='Optional. Damaged record IDs.'),
  ecIdAccountIds?: [ 
    {
      accountIds?: [ long ](name='AccountIds', description='List of accounts to access. If empty, all accounts under the current entity ID are selected.'),
      ecId?: string(name='EcId', description='Enterprise entity ID.', example='1501603440974415'),
    }
  ](name='EcIdAccountIds', description='Enterprise and account list. If empty, the current account itself is queried.'),
  month?: int32(name='Month', description='Required. Application month in the yyyyMM format.

This parameter is required.', example='202603'),
  nbid?: string(name='Nbid', description='Primary marketplace ID. If empty, the marketplace ID of the current user is used by default.', example='2084210001'),
}

model SubmitSlaCouponApplyResponseBody = {
  metadata?: any(name='Metadata', description='Response structure metadata.', example='{}'),
  requestId?: string(name='RequestId', description='Unique request identifier used to tag a request for troubleshooting.', example='79EE7556-0CFD-44EB-9CD6-B3B526E3A85F'),
  sumCoupon?: double(name='SumCoupon', description='Total claimed amount.', example='6.4'),
  validEndTime?: string(name='ValidEndTime', description='End time.', example='Mon Apr 27 00:00:00 CST 2026'),
  validStartTime?: string(name='ValidStartTime', description='Start time.', example='Tue Oct 27 13:15:58 CST 2026'),
}

model SubmitSlaCouponApplyResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: SubmitSlaCouponApplyResponseBody(name='body'),
}

/**
 * @summary User claims coupons for the last two months.
 *
 * @description 1. Call QueryMonthlySlaList to obtain the claimable months and records.
 * 2. Claim by month or by record.
 * Note: Only compensation for the last two months can be claimed. Historical compensation has been automatically issued.
 *
 * @param request SubmitSlaCouponApplyRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return SubmitSlaCouponApplyResponse
 */
@context("submitSlaCouponApplyWithContext")
async function submitSlaCouponApplyWithOptions(request: SubmitSlaCouponApplyRequest, runtime: $RuntimeOptions): SubmitSlaCouponApplyResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.ecIdAccountIds)) {
    query['EcIdAccountIds'] = request.ecIdAccountIds;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  var body : map[string]any = {};
  if (!$isNull(request.damagedIds)) {
    body['DamagedIds'] = request.damagedIds;
  }
  if (!$isNull(request.month)) {
    body['Month'] = request.month;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'SubmitSlaCouponApply',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary User claims coupons for the last two months.
 *
 * @description 1. Call QueryMonthlySlaList to obtain the claimable months and records.
 * 2. Claim by month or by record.
 * Note: Only compensation for the last two months can be claimed. Historical compensation has been automatically issued.
 *
 * @param request SubmitSlaCouponApplyRequest
 * @return SubmitSlaCouponApplyResponse
 */
async function submitSlaCouponApply(request: SubmitSlaCouponApplyRequest): SubmitSlaCouponApplyResponse {
  var runtime = new $RuntimeOptions{};
  return submitSlaCouponApplyWithOptions(request, runtime);
}

model UpdateBudgetRequest {
  budgetName?: string(name='BudgetName', description='The budget name.

This parameter is required.', example='NewBudgetName'),
  budgetType?: string(name='BudgetType', description='The budget type.

This parameter is required.', example='CONSUME'),
  comment?: string(name='Comment', description='The comment.', example='comment'),
  cycleEndPeriod?: string(name='CycleEndPeriod', description='The end period of the cycle.

This parameter is required.', example='2026-12'),
  cycleQuota?: [ 
    {
      cyclePeriod?: string(name='CyclePeriod', description='The cycle period.', example='2026-01'),
      quota?: string(name='Quota', description='The quota.', example='1000'),
    }
  ](name='CycleQuota', description='The quota specified for each cycle.'),
  cycleStartPeriod?: string(name='CycleStartPeriod', description='The start period of the cycle.

This parameter is required.', example='2026-01'),
  cycleType?: string(name='CycleType', description='The cycle type.

This parameter is required.', example='MONTHLY'),
  ecIdAccountIds?: [ 
    {
      accountIds?: [ long ](name='AccountIds', description='The list of accounts to access. If this parameter is left empty, all accounts under the current entity ID are selected.'),
      ecId?: string(name='EcId', description='The enterprise entity ID.', example='1'),
    }
  ](name='EcIdAccountIds', description='The list of enterprise entities and accounts. If this parameter is left empty, the current account is queried.'),
  metric?: string(name='Metric', description='The budget metric.

This parameter is required.', example='REQUIRE_AMOUNT'),
  nbid?: string(name='Nbid', description='The primary sales channel ID. If this parameter is left empty, the sales channel ID of the current user is used by default.', example='26842'),
  originalBudgetName?: string(name='OriginalBudgetName', description='The original budget name.

This parameter is required.', example='OriginalBudgetName'),
  queryFilter?: [ 
    {
      code?: string(name='Code', description='The parameter code.', example='PRODUCT'),
      selectType?: string(name='SelectType', description='The selection mode.', example='IN'),
      values?: [ string ](name='Values', description='The list of filter values.'),
    }
  ](name='QueryFilter', description='The filter conditions.'),
  quota?: string(name='Quota', description='The fixed quota value.', example='1000'),
  quotaType?: string(name='QuotaType', description='The quota type.

This parameter is required.', example='FIXED'),
  warnConfs?: [ 
    {
      comment?: string(name='Comment', description='The comment.', example='comment'),
      eventBridge?: boolean(name='EventBridge', description='Specifies whether to enable EventBridge.', example='true'),
      mscChannels?: [ string ](name='MscChannels', description='The list of Message Center notification channels.'),
      mscContacts?: [ string ](name='MscContacts', description='The list of Message Center contacts.'),
      name?: string(name='Name', description='The alert name. This is user-defined and optional. If not specified, the backend automatically generates a name.', example='warn1'),
      thresholdType?: string(name='ThresholdType', description='The threshold type.', example='FIXED'),
      thresholdValue?: string(name='ThresholdValue', description='The threshold value.', example='1000'),
      warnTarget?: string(name='WarnTarget', description='The alert target.', example='ACTUAL'),
    }
  ](name='WarnConfs', description='The alert configurations.'),
}

model UpdateBudgetShrinkRequest {
  budgetName?: string(name='BudgetName', description='The budget name.

This parameter is required.', example='NewBudgetName'),
  budgetType?: string(name='BudgetType', description='The budget type.

This parameter is required.', example='CONSUME'),
  comment?: string(name='Comment', description='The comment.', example='comment'),
  cycleEndPeriod?: string(name='CycleEndPeriod', description='The end period of the cycle.

This parameter is required.', example='2026-12'),
  cycleQuotaShrink?: string(name='CycleQuota', description='The quota specified for each cycle.'),
  cycleStartPeriod?: string(name='CycleStartPeriod', description='The start period of the cycle.

This parameter is required.', example='2026-01'),
  cycleType?: string(name='CycleType', description='The cycle type.

This parameter is required.', example='MONTHLY'),
  ecIdAccountIdsShrink?: string(name='EcIdAccountIds', description='The list of enterprise entities and accounts. If this parameter is left empty, the current account is queried.'),
  metric?: string(name='Metric', description='The budget metric.

This parameter is required.', example='REQUIRE_AMOUNT'),
  nbid?: string(name='Nbid', description='The primary sales channel ID. If this parameter is left empty, the sales channel ID of the current user is used by default.', example='26842'),
  originalBudgetName?: string(name='OriginalBudgetName', description='The original budget name.

This parameter is required.', example='OriginalBudgetName'),
  queryFilterShrink?: string(name='QueryFilter', description='The filter conditions.'),
  quota?: string(name='Quota', description='The fixed quota value.', example='1000'),
  quotaType?: string(name='QuotaType', description='The quota type.

This parameter is required.', example='FIXED'),
  warnConfsShrink?: string(name='WarnConfs', description='The alert configurations.'),
}

model UpdateBudgetResponseBody = {
  budgetName?: string(name='BudgetName'),
  requestId?: string(name='RequestId', description='The request ID.', example='03A59CD4-6C6B-1A62-B64C-F1F2AF7830F3'),
}

model UpdateBudgetResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateBudgetResponseBody(name='body'),
}

/**
 * @summary Updates a budget.
 *
 * @param tmpReq UpdateBudgetRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateBudgetResponse
 */
@context("updateBudgetWithContext")
async function updateBudgetWithOptions(tmpReq: UpdateBudgetRequest, runtime: $RuntimeOptions): UpdateBudgetResponse {
  tmpReq.validate();
  var request = new UpdateBudgetShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.cycleQuota)) {
    request.cycleQuotaShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.cycleQuota, 'CycleQuota', 'json');
  }
  if (!$isNull(tmpReq.ecIdAccountIds)) {
    request.ecIdAccountIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.ecIdAccountIds, 'EcIdAccountIds', 'json');
  }
  if (!$isNull(tmpReq.queryFilter)) {
    request.queryFilterShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.queryFilter, 'QueryFilter', 'json');
  }
  if (!$isNull(tmpReq.warnConfs)) {
    request.warnConfsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.warnConfs, 'WarnConfs', 'json');
  }
  var query = {};
  if (!$isNull(request.ecIdAccountIdsShrink)) {
    query['EcIdAccountIds'] = request.ecIdAccountIdsShrink;
  }
  if (!$isNull(request.nbid)) {
    query['Nbid'] = request.nbid;
  }
  var body : map[string]any = {};
  if (!$isNull(request.budgetName)) {
    body['BudgetName'] = request.budgetName;
  }
  if (!$isNull(request.budgetType)) {
    body['BudgetType'] = request.budgetType;
  }
  if (!$isNull(request.comment)) {
    body['Comment'] = request.comment;
  }
  if (!$isNull(request.cycleEndPeriod)) {
    body['CycleEndPeriod'] = request.cycleEndPeriod;
  }
  if (!$isNull(request.cycleQuotaShrink)) {
    body['CycleQuota'] = request.cycleQuotaShrink;
  }
  if (!$isNull(request.cycleStartPeriod)) {
    body['CycleStartPeriod'] = request.cycleStartPeriod;
  }
  if (!$isNull(request.cycleType)) {
    body['CycleType'] = request.cycleType;
  }
  if (!$isNull(request.metric)) {
    body['Metric'] = request.metric;
  }
  if (!$isNull(request.originalBudgetName)) {
    body['OriginalBudgetName'] = request.originalBudgetName;
  }
  if (!$isNull(request.queryFilterShrink)) {
    body['QueryFilter'] = request.queryFilterShrink;
  }
  if (!$isNull(request.quota)) {
    body['Quota'] = request.quota;
  }
  if (!$isNull(request.quotaType)) {
    body['QuotaType'] = request.quotaType;
  }
  if (!$isNull(request.warnConfsShrink)) {
    body['WarnConfs'] = request.warnConfsShrink;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpdateBudget',
    version = '2023-09-30',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Updates a budget.
 *
 * @param request UpdateBudgetRequest
 * @return UpdateBudgetResponse
 */
async function updateBudget(request: UpdateBudgetRequest): UpdateBudgetResponse {
  var runtime = new $RuntimeOptions{};
  return updateBudgetWithOptions(request, runtime);
}

