/**
 *
 */
import Credential;
import OpenApi;
import OpenApi.OpenApiUtil;

extends OpenApi;


init(config: OpenApiUtil.Config){
  super(config);
  @endpointRule = '';
  
  checkConfig(config);
  @endpoint = getEndpoint('realtranslationagent', @regionId, @endpointRule, @network, @suffix, @endpointMap, @endpoint);
}

function getEndpoint(productId: string, regionId: string, endpointRule: string, network: string, suffix: string, endpointMap: map[string]string, endpoint: string) throws: string{
  if (!$isNull(endpoint)) {
    return endpoint;
  }
  
  if (!$isNull(endpointMap) && !$isNull(endpointMap[regionId])) {
    return endpointMap[regionId];
  }
  return OpenApiUtil.getEndpointRules(productId, regionId, endpointRule, network, suffix);
}

model CancelTranslationTaskRequest {
  APIKey?: string(name='APIKey'),
  taskId?: string(name='TaskId', description='This parameter is required.'),
}

model CancelTranslationTaskResponseBody = {
  code?: string(name='Code'),
  data?: {
    status?: string(name='Status'),
    taskId?: string(name='TaskId'),
  }(name='Data'),
  message?: string(name='Message'),
  requestId?: string(name='RequestId'),
  success?: boolean(name='Success'),
}

model CancelTranslationTaskResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CancelTranslationTaskResponseBody(name='body'),
}

/**
 * @summary 取消翻译任务
 *
 * @param request CancelTranslationTaskRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CancelTranslationTaskResponse
 */
@context("cancelTranslationTaskWithContext")
async function cancelTranslationTaskWithOptions(request: CancelTranslationTaskRequest, runtime: $RuntimeOptions): CancelTranslationTaskResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.APIKey)) {
    query['APIKey'] = request.APIKey;
  }
  var body : map[string]any = {};
  if (!$isNull(request.taskId)) {
    body['TaskId'] = request.taskId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CancelTranslationTask',
    version = '2026-06-22',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 取消翻译任务
 *
 * @param request CancelTranslationTaskRequest
 * @return CancelTranslationTaskResponse
 */
async function cancelTranslationTask(request: CancelTranslationTaskRequest): CancelTranslationTaskResponse {
  var runtime = new $RuntimeOptions{};
  return cancelTranslationTaskWithOptions(request, runtime);
}

model GetOriginalFileUrlRequest {
  APIKey?: string(name='APIKey'),
  taskId?: string(name='TaskId', description='This parameter is required.'),
}

model GetOriginalFileUrlResponseBody = {
  code?: string(name='Code'),
  data?: {
    downloadUrl?: string(name='DownloadUrl'),
    expireTime?: long(name='ExpireTime'),
    fileName?: string(name='FileName'),
  }(name='Data'),
  message?: string(name='Message'),
  requestId?: string(name='RequestId'),
  success?: boolean(name='Success'),
}

model GetOriginalFileUrlResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetOriginalFileUrlResponseBody(name='body'),
}

/**
 * @summary 获取原文文件下载URL
 *
 * @param request GetOriginalFileUrlRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetOriginalFileUrlResponse
 */
@context("getOriginalFileUrlWithContext")
async function getOriginalFileUrlWithOptions(request: GetOriginalFileUrlRequest, runtime: $RuntimeOptions): GetOriginalFileUrlResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.APIKey)) {
    query['APIKey'] = request.APIKey;
  }
  var body : map[string]any = {};
  if (!$isNull(request.taskId)) {
    body['TaskId'] = request.taskId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetOriginalFileUrl',
    version = '2026-06-22',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 获取原文文件下载URL
 *
 * @param request GetOriginalFileUrlRequest
 * @return GetOriginalFileUrlResponse
 */
async function getOriginalFileUrl(request: GetOriginalFileUrlRequest): GetOriginalFileUrlResponse {
  var runtime = new $RuntimeOptions{};
  return getOriginalFileUrlWithOptions(request, runtime);
}

model GetTranslatedFileUrlRequest {
  APIKey?: string(name='APIKey'),
  taskId?: string(name='TaskId', description='This parameter is required.'),
}

model GetTranslatedFileUrlResponseBody = {
  code?: string(name='Code'),
  data?: {
    downloadUrl?: string(name='DownloadUrl'),
    expireTime?: long(name='ExpireTime'),
    fileName?: string(name='FileName'),
  }(name='Data'),
  message?: string(name='Message'),
  requestId?: string(name='RequestId'),
  success?: boolean(name='Success'),
}

model GetTranslatedFileUrlResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetTranslatedFileUrlResponseBody(name='body'),
}

/**
 * @summary 获取译文文件下载URL
 *
 * @param request GetTranslatedFileUrlRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetTranslatedFileUrlResponse
 */
@context("getTranslatedFileUrlWithContext")
async function getTranslatedFileUrlWithOptions(request: GetTranslatedFileUrlRequest, runtime: $RuntimeOptions): GetTranslatedFileUrlResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.APIKey)) {
    query['APIKey'] = request.APIKey;
  }
  var body : map[string]any = {};
  if (!$isNull(request.taskId)) {
    body['TaskId'] = request.taskId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetTranslatedFileUrl',
    version = '2026-06-22',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 获取译文文件下载URL
 *
 * @param request GetTranslatedFileUrlRequest
 * @return GetTranslatedFileUrlResponse
 */
async function getTranslatedFileUrl(request: GetTranslatedFileUrlRequest): GetTranslatedFileUrlResponse {
  var runtime = new $RuntimeOptions{};
  return getTranslatedFileUrlWithOptions(request, runtime);
}

model GetTranslationTaskRequest {
  APIKey?: string(name='APIKey'),
  taskId?: string(name='TaskId', description='This parameter is required.'),
}

model GetTranslationTaskResponseBody = {
  code?: string(name='Code'),
  data?: {
    baseTaskId?: string(name='BaseTaskId'),
    config?: {
      securityLevel?: string(name='SecurityLevel'),
      sourceLanguage?: string(name='SourceLanguage'),
      style?: string(name='Style'),
      targetLanguage?: string(name='TargetLanguage'),
      template?: string(name='Template'),
    }(name='Config'),
    costCredits?: double(name='CostCredits'),
    costTime?: long(name='CostTime'),
    errorMessage?: string(name='ErrorMessage'),
    estimatedCostCredits?: double(name='EstimatedCostCredits'),
    estimatedTime?: long(name='EstimatedTime'),
    extractedTerms?: [ 
      {
        sourceTerm?: string(name='SourceTerm'),
        targetTerm?: string(name='TargetTerm'),
      }
    ](name='ExtractedTerms'),
    fileFormat?: string(name='FileFormat'),
    fileName?: string(name='FileName'),
    finishedAt?: string(name='FinishedAt'),
    fonts?: map[string][ string ](name='Fonts'),
    orgId?: string(name='OrgId'),
    originalFileName?: string(name='OriginalFileName'),
    pageCount?: long(name='PageCount'),
    progress?: int32(name='Progress'),
    status?: string(name='Status'),
    taskId?: string(name='TaskId'),
    taskType?: string(name='TaskType'),
    wordCount?: long(name='WordCount'),
    workSpaceId?: string(name='WorkSpaceId'),
  }(name='Data'),
  message?: string(name='Message'),
  requestId?: string(name='RequestId'),
  success?: boolean(name='Success'),
}

model GetTranslationTaskResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetTranslationTaskResponseBody(name='body'),
}

/**
 * @summary 获取翻译任务详情
 *
 * @param request GetTranslationTaskRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetTranslationTaskResponse
 */
@context("getTranslationTaskWithContext")
async function getTranslationTaskWithOptions(request: GetTranslationTaskRequest, runtime: $RuntimeOptions): GetTranslationTaskResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.APIKey)) {
    query['APIKey'] = request.APIKey;
  }
  var body : map[string]any = {};
  if (!$isNull(request.taskId)) {
    body['TaskId'] = request.taskId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetTranslationTask',
    version = '2026-06-22',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 获取翻译任务详情
 *
 * @param request GetTranslationTaskRequest
 * @return GetTranslationTaskResponse
 */
async function getTranslationTask(request: GetTranslationTaskRequest): GetTranslationTaskResponse {
  var runtime = new $RuntimeOptions{};
  return getTranslationTaskWithOptions(request, runtime);
}

model ListTranslationTasksRequest {
  APIKey?: string(name='APIKey'),
  endTime?: string(name='EndTime'),
  maxResults?: int32(name='MaxResults'),
  nextToken?: string(name='NextToken'),
  originalFileName?: string(name='OriginalFileName'),
  sourceLanguage?: string(name='SourceLanguage'),
  startTime?: string(name='StartTime'),
  status?: string(name='Status'),
  targetLanguage?: string(name='TargetLanguage'),
  taskId?: string(name='TaskId'),
}

model ListTranslationTasksResponseBody = {
  code?: string(name='Code'),
  data?: {
    list?: [ 
      {
        completeTime?: string(name='CompleteTime'),
        costCredits?: double(name='CostCredits'),
        costTime?: long(name='CostTime'),
        creator?: string(name='Creator'),
        creatorName?: string(name='CreatorName'),
        errorMessage?: string(name='ErrorMessage'),
        fileFormat?: string(name='FileFormat'),
        fileName?: string(name='FileName'),
        gmtCreate?: string(name='GmtCreate'),
        orgId?: string(name='OrgId'),
        originalFileName?: string(name='OriginalFileName'),
        pageCount?: long(name='PageCount'),
        progress?: int32(name='Progress'),
        sourceLanguage?: string(name='SourceLanguage'),
        startTime?: string(name='StartTime'),
        status?: string(name='Status'),
        targetLanguage?: string(name='TargetLanguage'),
        taskId?: string(name='TaskId'),
        taskType?: string(name='TaskType'),
        template?: string(name='Template'),
        wordCount?: long(name='WordCount'),
        workSpaceId?: string(name='WorkSpaceId'),
      }
    ](name='List'),
    maxResults?: int32(name='MaxResults'),
    nextToken?: string(name='NextToken'),
    total?: long(name='Total'),
  }(name='Data'),
  message?: string(name='Message'),
  requestId?: string(name='RequestId'),
  success?: boolean(name='Success'),
}

model ListTranslationTasksResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListTranslationTasksResponseBody(name='body'),
}

/**
 * @summary 获取翻译任务列表
 *
 * @param request ListTranslationTasksRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListTranslationTasksResponse
 */
@context("listTranslationTasksWithContext")
async function listTranslationTasksWithOptions(request: ListTranslationTasksRequest, runtime: $RuntimeOptions): ListTranslationTasksResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.APIKey)) {
    query['APIKey'] = request.APIKey;
  }
  if (!$isNull(request.endTime)) {
    query['EndTime'] = request.endTime;
  }
  if (!$isNull(request.maxResults)) {
    query['MaxResults'] = request.maxResults;
  }
  if (!$isNull(request.nextToken)) {
    query['NextToken'] = request.nextToken;
  }
  if (!$isNull(request.originalFileName)) {
    query['OriginalFileName'] = request.originalFileName;
  }
  if (!$isNull(request.sourceLanguage)) {
    query['SourceLanguage'] = request.sourceLanguage;
  }
  if (!$isNull(request.startTime)) {
    query['StartTime'] = request.startTime;
  }
  if (!$isNull(request.status)) {
    query['Status'] = request.status;
  }
  if (!$isNull(request.targetLanguage)) {
    query['TargetLanguage'] = request.targetLanguage;
  }
  if (!$isNull(request.taskId)) {
    query['TaskId'] = request.taskId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListTranslationTasks',
    version = '2026-06-22',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 获取翻译任务列表
 *
 * @param request ListTranslationTasksRequest
 * @return ListTranslationTasksResponse
 */
async function listTranslationTasks(request: ListTranslationTasksRequest): ListTranslationTasksResponse {
  var runtime = new $RuntimeOptions{};
  return listTranslationTasksWithOptions(request, runtime);
}

model SubmitTranslationTaskRequest {
  APIKey?: string(name='APIKey'),
  baseTaskId?: string(name='BaseTaskId'),
  config?: {
    font?: string(name='Font'),
    sourceLanguage?: string(name='SourceLanguage', description='This parameter is required.'),
    style?: string(name='Style'),
    targetLanguage?: string(name='TargetLanguage', description='This parameter is required.'),
  }(name='Config', description='This parameter is required.'),
  customTerms?: [ 
    {
      sourceTerm?: string(name='SourceTerm'),
      targetTerm?: string(name='TargetTerm'),
    }
  ](name='CustomTerms'),
  taskId?: string(name='TaskId'),
}

model SubmitTranslationTaskShrinkRequest {
  APIKey?: string(name='APIKey'),
  baseTaskId?: string(name='BaseTaskId'),
  configShrink?: string(name='Config', description='This parameter is required.'),
  customTermsShrink?: string(name='CustomTerms'),
  taskId?: string(name='TaskId'),
}

model SubmitTranslationTaskResponseBody = {
  code?: string(name='Code'),
  data?: {
    status?: string(name='Status'),
    taskId?: string(name='TaskId'),
  }(name='Data'),
  message?: string(name='Message'),
  requestId?: string(name='RequestId'),
  success?: boolean(name='Success'),
}

model SubmitTranslationTaskResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: SubmitTranslationTaskResponseBody(name='body'),
}

/**
 * @summary 提交翻译任务
 *
 * @param tmpReq SubmitTranslationTaskRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return SubmitTranslationTaskResponse
 */
@context("submitTranslationTaskWithContext")
async function submitTranslationTaskWithOptions(tmpReq: SubmitTranslationTaskRequest, runtime: $RuntimeOptions): SubmitTranslationTaskResponse {
  tmpReq.validate();
  var request = new SubmitTranslationTaskShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.config)) {
    request.configShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.config, 'Config', 'json');
  }
  if (!$isNull(tmpReq.customTerms)) {
    request.customTermsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.customTerms, 'CustomTerms', 'json');
  }
  var query = {};
  if (!$isNull(request.APIKey)) {
    query['APIKey'] = request.APIKey;
  }
  if (!$isNull(request.customTermsShrink)) {
    query['CustomTerms'] = request.customTermsShrink;
  }
  var body : map[string]any = {};
  if (!$isNull(request.baseTaskId)) {
    body['BaseTaskId'] = request.baseTaskId;
  }
  if (!$isNull(request.configShrink)) {
    body['Config'] = request.configShrink;
  }
  if (!$isNull(request.taskId)) {
    body['TaskId'] = request.taskId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'SubmitTranslationTask',
    version = '2026-06-22',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 提交翻译任务
 *
 * @param request SubmitTranslationTaskRequest
 * @return SubmitTranslationTaskResponse
 */
async function submitTranslationTask(request: SubmitTranslationTaskRequest): SubmitTranslationTaskResponse {
  var runtime = new $RuntimeOptions{};
  return submitTranslationTaskWithOptions(request, runtime);
}

model UploadTranslationFileRequest {
  APIKey?: string(name='APIKey'),
  file?: string(name='File', description='This parameter is required.'),
  fileName?: string(name='FileName', description='This parameter is required.'),
}

model UploadTranslationFileAdvanceRequest {
  APIKey?: string(name='APIKey'),
  fileObject?: readable(name='File', description='This parameter is required.'),
  fileName?: string(name='FileName', description='This parameter is required.'),
}

model UploadTranslationFileResponseBody = {
  code?: string(name='Code'),
  data?: {
    creditBreakdown?: string(name='CreditBreakdown'),
    creditsAvailable?: boolean(name='CreditsAvailable'),
    detectedLang?: string(name='DetectedLang'),
    estimatedCostCredits?: double(name='EstimatedCostCredits'),
    estimatedTime?: long(name='EstimatedTime'),
    fonts?: map[string][ string ](name='Fonts'),
    pageCount?: long(name='PageCount'),
    sensitiveDetected?: boolean(name='SensitiveDetected'),
    sensitiveTags?: [ string ](name='SensitiveTags'),
    taskId?: string(name='TaskId'),
    wordCount?: long(name='WordCount'),
  }(name='Data'),
  message?: string(name='Message'),
  requestId?: string(name='RequestId'),
  success?: boolean(name='Success'),
}

model UploadTranslationFileResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UploadTranslationFileResponseBody(name='body'),
}

/**
 * @summary 解析文档
 *
 * @param request UploadTranslationFileRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return UploadTranslationFileResponse
 */
@context("uploadTranslationFileWithContext")
async function uploadTranslationFileWithOptions(request: UploadTranslationFileRequest, runtime: $RuntimeOptions): UploadTranslationFileResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.APIKey)) {
    query['APIKey'] = request.APIKey;
  }
  if (!$isNull(request.file)) {
    query['File'] = request.file;
  }
  if (!$isNull(request.fileName)) {
    query['FileName'] = request.fileName;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'UploadTranslationFile',
    version = '2026-06-22',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 解析文档
 *
 * @param request UploadTranslationFileRequest
 * @return UploadTranslationFileResponse
 */
async function uploadTranslationFile(request: UploadTranslationFileRequest): UploadTranslationFileResponse {
  var runtime = new $RuntimeOptions{};
  return uploadTranslationFileWithOptions(request, runtime);
}

async function uploadTranslationFileAdvance(request: UploadTranslationFileAdvanceRequest, runtime: $RuntimeOptions): UploadTranslationFileResponse {
  // Step 0: init client
  if ($isNull(@credential)) {
    throw new OpenApi.Client{
      code = 'InvalidCredentials',
      message = 'Please set up the credentials correctly. If you are setting them through environment variables, please ensure that ALIBABA_CLOUD_ACCESS_KEY_ID and ALIBABA_CLOUD_ACCESS_KEY_SECRET are set correctly. See https://help.aliyun.com/zh/sdk/developer-reference/configure-the-alibaba-cloud-accesskey-environment-variable-on-linux-macos-and-windows-systems for more details.'
    }
  }
  var credentialModel = @credential.getCredential();
  var accessKeyId = credentialModel.accessKeyId;
  var accessKeySecret = credentialModel.accessKeySecret;
  var securityToken = credentialModel.securityToken;
  var credentialType = credentialModel.type;
  var openPlatformEndpoint = @openPlatformEndpoint;
  if($isNull(openPlatformEndpoint) || openPlatformEndpoint == "") {
    openPlatformEndpoint ='openplatform.aliyuncs.com';
  }
  if($isNull(credentialType)) {
    credentialType ='access_key';
  }
  var authConfig = new OpenApiUtil.Config{
    accessKeyId = accessKeyId,
    accessKeySecret = accessKeySecret,
    securityToken = securityToken,
    type = credentialType,
    endpoint = openPlatformEndpoint,
    protocol = @protocol,
    regionId = @regionId,
  };
  var authClient = new OpenApi(authConfig);
  var authRequest = {
    Product = 'RealTranslationAgent',
    RegionId = @regionId,
  };
  var authReq = new OpenApiUtil.OpenApiRequest{
    query = OpenApiUtil.query(authRequest),
  };
  var authParams = new OpenApiUtil.Params{
    action = 'AuthorizeFileUpload',
    version = '2019-12-19',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'GET',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  var authResponse : object = {};
  var fileObj = new $FileField{};
  var ossHeader: object = {};
  var tmpBody: object = {};
  var useAccelerate: boolean = false;
  var authResponseBody: map[string]string = {};
  var uploadTranslationFileReq = new UploadTranslationFileRequest{};
  OpenApiUtil.convert(request, uploadTranslationFileReq);

  if(!$isNull(request.fileObject)) {
    authResponse = authClient.callApi(authParams, authReq, runtime);
    tmpBody = $object(authResponse['body']);
    useAccelerate = $boolean(tmpBody['UseAccelerate']);
    authResponseBody = OpenApiUtil.stringifyMapValue(tmpBody);
    fileObj = new $FileField{
      filename = authResponseBody['ObjectKey'],
      content = request.fileObject,
      contentType = '',
    };
    ossHeader = {
      host = OpenApiUtil.getEndpoint(authResponseBody['Endpoint'], useAccelerate, @endpointType),
      OSSAccessKeyId = authResponseBody['AccessKeyId'],
      policy = authResponseBody['EncodedPolicy'],
      Signature = authResponseBody['Signature'],
      key = authResponseBody['ObjectKey'],
      file = fileObj,
      success_action_status = '201',
    };
    _postOSSObject(authResponseBody['Bucket'], ossHeader, runtime);
    uploadTranslationFileReq.file = `http://${authResponseBody['Bucket']}.${authResponseBody['Endpoint']}/${authResponseBody['ObjectKey']}`;
  }
  var uploadTranslationFileResp = uploadTranslationFileWithOptions(uploadTranslationFileReq, runtime);
  return uploadTranslationFileResp;
}

api _postOSSObject(bucketName: string, form: object, runtime: $RuntimeOptions): object {
  var boundary = $Form.getBoundary();
  var tmp = $string(form.host);
  var host = `${bucketName}.${tmp}`;
  __request.protocol = 'HTTPS';
  __request.method = 'POST';
  __request.pathname = `/`;
  __request.headers = {
    host = host,
    date = OpenApiUtil.getDateUTCString(),
    user-agent = OpenApiUtil.getUserAgent(''),
  };
  __request.headers.content-type = `multipart/form-data; boundary=${boundary}`;
  __request.body = $Form.toFileForm(form, boundary);
} returns {
  var respMap : object = null;
  var bodyStr = $Stream.readAsString(__response.body);
  if ((__response.statusCode >= 400) && (__response.statusCode < 600)) {
    respMap = $XML.parseXml(bodyStr, null);
    var err = $object(respMap.Error);
    throw new OpenApi.Client{
      code = $string(err.Code),
      message = $string(err.Message),
      data = {
        httpCode = __response.statusCode,
        requestId = $string(err.RequestId),
        hostId = $string(err.HostId),
      }
    };
  }
  respMap = $XML.parseXml(bodyStr, null);
  return {
    ...respMap,
  };
} runtime {
  key = $string($default(runtime.key, @key)),
  cert = $string($default(runtime.cert, @cert)),
  ca = $string($default(runtime.ca, @ca)),
  readTimeout = $number($default(runtime.readTimeout, @readTimeout)),
  connectTimeout = $number($default(runtime.connectTimeout, @connectTimeout)),
  httpProxy = $string($default(runtime.httpProxy, @httpProxy)),
  httpsProxy = $string($default(runtime.httpsProxy, @httpsProxy)),
  noProxy = $string($default(runtime.noProxy, @noProxy)),
  socks5Proxy = $string($default(runtime.socks5Proxy, @socks5Proxy)),
  socks5NetWork = $string($default(runtime.socks5NetWork, @socks5NetWork)),
  maxIdleConns = $number($default(runtime.maxIdleConns, @maxIdleConns)),
  retryOptions = @retryOptions,
  ignoreSSL = $boolean($default(runtime.ignoreSSL, false)),
  tlsMinVersion = @tlsMinVersion
}
