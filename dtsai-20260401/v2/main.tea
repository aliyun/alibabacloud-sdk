/**
 *
 */
import Credential;
import OpenApi;
import OpenApi.OpenApiUtil;

extends OpenApi;


init(config: OpenApiUtil.Config){
  super(config);
  @endpointRule = 'regional';
  @endpointMap = {
    'cn-beijing' = 'dtsai.cn-beijing.aliyuncs.com',
  };

  checkConfig(config);
  @endpoint = getEndpoint('dtsai', @regionId, @endpointRule, @network, @suffix, @endpointMap, @endpoint);
}

function getEndpoint(productId: string, regionId: string, endpointRule: string, network: string, suffix: string, endpointMap: map[string]string, endpoint: string) throws: string{
  if (!$isNull(endpoint)) {
    return endpoint;
  }
  
  if (!$isNull(endpointMap) && !$isNull(endpointMap[regionId])) {
    return endpointMap[regionId];
  }
  return OpenApiUtil.getEndpointRules(productId, regionId, endpointRule, network, suffix);
}

model CreateDocParserJobRequest {
  fileFormat?: string(name='FileFormat', description='The format of the input file. Valid values:

- **pdf**: PDF file.

- **docx**: Word file in docx format.

- **doc**: Word file in doc format.

- **pptx**: PPT file in pptx format.

- **ppt**: PPT file in ppt format.

- **txt**: plain text file.

- **md**: Markdown file.

- **png**: PNG image.

- **jpg**: JPG image.

- **jpeg**: JPEG image.

This parameter is required.', example='pdf'),
  fileName?: string(name='FileName', description='The file name, which must include the file name extension.

This parameter is required.', example='document.pdf'),
  fileUrl?: string(name='FileUrl', description='The HTTP or HTTPS URL of the file to be parsed.
>SDKs for various languages provide an additional `CreateDocParserJobAdvance` method that supports passing a local file stream directly (such as InputStream in Java), without the need to upload the file to OSS and construct a FileUrl in advance. When using the Advance method, replace the `FileUrl` parameter (URL string) with the `FileUrlObject` parameter (file stream). All other request parameters remain unchanged. The SDK automatically performs the following operations:
>1. Obtains temporary OSS upload credentials.
>2. Uploads the file stream directly to OSS.
>3. Calls the CreateDocParserJob operation with the generated OSS URL.', example='https://xxx.oss-cn-beijing.aliyuncs.com/document.pdf?Expires=xxx&OSSAccessKeyId=xxx&Signature=xxx'),
  imageMode?: string(name='ImageMode'),
  ossFileUrl?: string(name='OssFileUrl', description='The OSS file URL.'),
  outputFormat?: string(name='OutputFormat', description='The output format of the parsing result. Valid values:

- **markdown**: Markdown format.

This parameter is required.', example='markdown'),
  regionId?: string(name='RegionId', description='The region ID.

This parameter is required.', example='cn-beijing'),
  resultType?: string(name='ResultType'),
  tableFormat?: string(name='TableFormat'),
}

model CreateDocParserJobAdvanceRequest {
  fileFormat?: string(name='FileFormat', description='The format of the input file. Valid values:

- **pdf**: PDF file.

- **docx**: Word file in docx format.

- **doc**: Word file in doc format.

- **pptx**: PPT file in pptx format.

- **ppt**: PPT file in ppt format.

- **txt**: plain text file.

- **md**: Markdown file.

- **png**: PNG image.

- **jpg**: JPG image.

- **jpeg**: JPEG image.

This parameter is required.', example='pdf'),
  fileName?: string(name='FileName', description='The file name, which must include the file name extension.

This parameter is required.', example='document.pdf'),
  fileUrlObject?: readable(name='FileUrl', description='The HTTP or HTTPS URL of the file to be parsed.
>SDKs for various languages provide an additional `CreateDocParserJobAdvance` method that supports passing a local file stream directly (such as InputStream in Java), without the need to upload the file to OSS and construct a FileUrl in advance. When using the Advance method, replace the `FileUrl` parameter (URL string) with the `FileUrlObject` parameter (file stream). All other request parameters remain unchanged. The SDK automatically performs the following operations:
>1. Obtains temporary OSS upload credentials.
>2. Uploads the file stream directly to OSS.
>3. Calls the CreateDocParserJob operation with the generated OSS URL.', example='https://xxx.oss-cn-beijing.aliyuncs.com/document.pdf?Expires=xxx&OSSAccessKeyId=xxx&Signature=xxx'),
  imageMode?: string(name='ImageMode'),
  ossFileUrl?: string(name='OssFileUrl', description='The OSS file URL.'),
  outputFormat?: string(name='OutputFormat', description='The output format of the parsing result. Valid values:

- **markdown**: Markdown format.

This parameter is required.', example='markdown'),
  regionId?: string(name='RegionId', description='The region ID.

This parameter is required.', example='cn-beijing'),
  resultType?: string(name='ResultType'),
  tableFormat?: string(name='TableFormat'),
}

model CreateDocParserJobResponseBody = {
  errorCode?: string(name='ErrorCode', description='The error code.', example='InvalidParameter'),
  errorMessage?: string(name='ErrorMessage', description='The error message.', example='The parameter is not valid.'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  jobId?: string(name='JobId', description='The document parsing task ID.', example='job_abc123'),
  requestId?: string(name='RequestId', description='The request ID.', example='A30D0930-xxxx-xxxx-xxxx-C2C661CC8B58'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model CreateDocParserJobResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateDocParserJobResponseBody(name='body'),
}

/**
 * @summary Creates a document parsing task.
 *
 * @description - Region: Only China (Beijing) is supported.
 * - Fees: Free during the public preview period. No fees are charged.
 *
 * @param request CreateDocParserJobRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateDocParserJobResponse
 */
@context("createDocParserJobWithContext")
async function createDocParserJobWithOptions(request: CreateDocParserJobRequest, runtime: $RuntimeOptions): CreateDocParserJobResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.fileFormat)) {
    query['FileFormat'] = request.fileFormat;
  }
  if (!$isNull(request.fileName)) {
    query['FileName'] = request.fileName;
  }
  if (!$isNull(request.fileUrl)) {
    query['FileUrl'] = request.fileUrl;
  }
  if (!$isNull(request.imageMode)) {
    query['ImageMode'] = request.imageMode;
  }
  if (!$isNull(request.ossFileUrl)) {
    query['OssFileUrl'] = request.ossFileUrl;
  }
  if (!$isNull(request.outputFormat)) {
    query['OutputFormat'] = request.outputFormat;
  }
  if (!$isNull(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  if (!$isNull(request.resultType)) {
    query['ResultType'] = request.resultType;
  }
  if (!$isNull(request.tableFormat)) {
    query['TableFormat'] = request.tableFormat;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateDocParserJob',
    version = '2026-04-01',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates a document parsing task.
 *
 * @description - Region: Only China (Beijing) is supported.
 * - Fees: Free during the public preview period. No fees are charged.
 *
 * @param request CreateDocParserJobRequest
 * @return CreateDocParserJobResponse
 */
async function createDocParserJob(request: CreateDocParserJobRequest): CreateDocParserJobResponse {
  var runtime = new $RuntimeOptions{};
  return createDocParserJobWithOptions(request, runtime);
}

async function createDocParserJobAdvance(request: CreateDocParserJobAdvanceRequest, runtime: $RuntimeOptions): CreateDocParserJobResponse {
  // Step 0: init client
  if ($isNull(@credential)) {
    throw new OpenApi.Client{
      code = 'InvalidCredentials',
      message = 'Please set up the credentials correctly. If you are setting them through environment variables, please ensure that ALIBABA_CLOUD_ACCESS_KEY_ID and ALIBABA_CLOUD_ACCESS_KEY_SECRET are set correctly. See https://help.aliyun.com/zh/sdk/developer-reference/configure-the-alibaba-cloud-accesskey-environment-variable-on-linux-macos-and-windows-systems for more details.'
    }
  }
  var credentialModel = @credential.getCredential();
  var accessKeyId = credentialModel.accessKeyId;
  var accessKeySecret = credentialModel.accessKeySecret;
  var securityToken = credentialModel.securityToken;
  var credentialType = credentialModel.type;
  var openPlatformEndpoint = @openPlatformEndpoint;
  if($isNull(openPlatformEndpoint) || openPlatformEndpoint == "") {
    openPlatformEndpoint ='openplatform.aliyuncs.com';
  }
  if($isNull(credentialType)) {
    credentialType ='access_key';
  }
  var authConfig = new OpenApiUtil.Config{
    accessKeyId = accessKeyId,
    accessKeySecret = accessKeySecret,
    securityToken = securityToken,
    type = credentialType,
    endpoint = openPlatformEndpoint,
    protocol = @protocol,
    regionId = @regionId,
  };
  var authClient = new OpenApi(authConfig);
  var authRequest = {
    Product = 'DtsAI',
    RegionId = @regionId,
  };
  var authReq = new OpenApiUtil.OpenApiRequest{
    query = OpenApiUtil.query(authRequest),
  };
  var authParams = new OpenApiUtil.Params{
    action = 'AuthorizeFileUpload',
    version = '2019-12-19',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'GET',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  var authResponse : object = {};
  var fileObj = new $FileField{};
  var ossHeader: object = {};
  var tmpBody: object = {};
  var useAccelerate: boolean = false;
  var authResponseBody: map[string]string = {};
  var createDocParserJobReq = new CreateDocParserJobRequest{};
  OpenApiUtil.convert(request, createDocParserJobReq);

  if(!$isNull(request.fileUrlObject)) {
    authResponse = authClient.callApi(authParams, authReq, runtime);
    tmpBody = $object(authResponse['body']);
    useAccelerate = $boolean(tmpBody['UseAccelerate']);
    authResponseBody = OpenApiUtil.stringifyMapValue(tmpBody);
    fileObj = new $FileField{
      filename = authResponseBody['ObjectKey'],
      content = request.fileUrlObject,
      contentType = '',
    };
    ossHeader = {
      host = OpenApiUtil.getEndpoint(authResponseBody['Endpoint'], useAccelerate, @endpointType),
      OSSAccessKeyId = authResponseBody['AccessKeyId'],
      policy = authResponseBody['EncodedPolicy'],
      Signature = authResponseBody['Signature'],
      key = authResponseBody['ObjectKey'],
      file = fileObj,
      success_action_status = '201',
    };
    _postOSSObject(authResponseBody['Bucket'], ossHeader, runtime);
    createDocParserJobReq.fileUrl = `http://${authResponseBody['Bucket']}.${authResponseBody['Endpoint']}/${authResponseBody['ObjectKey']}`;
  }
  var createDocParserJobResp = createDocParserJobWithOptions(createDocParserJobReq, runtime);
  return createDocParserJobResp;
}

model DescribeDocParserJobResultRequest {
  jobId?: string(name='JobId', description='The document parsing task ID. You can obtain this ID by calling CreateDocParserJob.

This parameter is required.', example='job_abc123'),
  regionId?: string(name='RegionId', description='The region ID.

This parameter is required.', example='cn-beijing'),
}

model DescribeDocParserJobResultResponseBody = {
  errorCode?: string(name='ErrorCode', description='The error code.', example='JobNotFound'),
  errorMessage?: string(name='ErrorMessage', description='The error message.', example='The specified job does not exist.'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='The request ID.', example='A30D0930-xxxx-xxxx-xxxx-C2C661CC8B58'),
  result?: string(name='Result', description='The document parsing result. The format is determined by the OutputFormat parameter specified in the CreateDocParserJob request.', example='# Document Title'),
  resultType?: string(name='ResultType'),
  resultUrl?: string(name='ResultUrl'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model DescribeDocParserJobResultResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeDocParserJobResultResponseBody(name='body'),
}

/**
 * @summary Retrieves the result of a document parsing task.
 *
 * @description - Region: Only China (Beijing) is supported.
 * - Fees: Free of charge during the public preview period.
 * - Call DescribeDocParserJobResult to retrieve the parsing result of a document parsing task. Call this operation only after DescribeDocParserJobStatus returns a Status of success. Task results are retained for 72 hours and cannot be retrieved after expiration.
 *
 * @param request DescribeDocParserJobResultRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeDocParserJobResultResponse
 */
@context("describeDocParserJobResultWithContext")
async function describeDocParserJobResultWithOptions(request: DescribeDocParserJobResultRequest, runtime: $RuntimeOptions): DescribeDocParserJobResultResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.jobId)) {
    query['JobId'] = request.jobId;
  }
  if (!$isNull(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeDocParserJobResult',
    version = '2026-04-01',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves the result of a document parsing task.
 *
 * @description - Region: Only China (Beijing) is supported.
 * - Fees: Free of charge during the public preview period.
 * - Call DescribeDocParserJobResult to retrieve the parsing result of a document parsing task. Call this operation only after DescribeDocParserJobStatus returns a Status of success. Task results are retained for 72 hours and cannot be retrieved after expiration.
 *
 * @param request DescribeDocParserJobResultRequest
 * @return DescribeDocParserJobResultResponse
 */
async function describeDocParserJobResult(request: DescribeDocParserJobResultRequest): DescribeDocParserJobResultResponse {
  var runtime = new $RuntimeOptions{};
  return describeDocParserJobResultWithOptions(request, runtime);
}

model DescribeDocParserJobStatusRequest {
  jobId?: string(name='JobId', description='The ID of the document parsing task. You can obtain the ID by calling CreateDocParserJob.

This parameter is required.', example='job_abc123'),
  regionId?: string(name='RegionId', description='The region ID.

This parameter is required.', example='cn-beijing'),
}

model DescribeDocParserJobStatusResponseBody = {
  errorCode?: string(name='ErrorCode', description='The error code.', example='JobNotFound'),
  errorMessage?: string(name='ErrorMessage', description='The error message.', example='The specified job does not exist.'),
  failureMessage?: string(name='FailureMessage', description='The failure cause. This parameter has a value only when Status is failed.', example='Failed Message'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='The request ID.', example='A30D0930-xxxx-xxxx-xxxx-C2C661CC8B58'),
  status?: string(name='Status', description='The task status. Valid values:

- **init**: Created and being prepared.

- **pending**: Queued and waiting to be scheduled.

- **running**: Being processed and parsed.

- **success**: Completed. You can call DescribeDocParserJobResult to obtain the result.

- **failed**: Failed. The cause is provided in FailureMessage.

- **cancelled**: Canceled.', example='running'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model DescribeDocParserJobStatusResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeDocParserJobStatusResponseBody(name='body'),
}

/**
 * @summary Queries the status of a document parsing task.
 *
 * @description - Region: Only China (Beijing) is supported.
 * - Fees: The service is free of charge during the public preview period.
 *
 * @param request DescribeDocParserJobStatusRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeDocParserJobStatusResponse
 */
@context("describeDocParserJobStatusWithContext")
async function describeDocParserJobStatusWithOptions(request: DescribeDocParserJobStatusRequest, runtime: $RuntimeOptions): DescribeDocParserJobStatusResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.jobId)) {
    query['JobId'] = request.jobId;
  }
  if (!$isNull(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeDocParserJobStatus',
    version = '2026-04-01',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the status of a document parsing task.
 *
 * @description - Region: Only China (Beijing) is supported.
 * - Fees: The service is free of charge during the public preview period.
 *
 * @param request DescribeDocParserJobStatusRequest
 * @return DescribeDocParserJobStatusResponse
 */
async function describeDocParserJobStatus(request: DescribeDocParserJobStatusRequest): DescribeDocParserJobStatusResponse {
  var runtime = new $RuntimeOptions{};
  return describeDocParserJobStatusWithOptions(request, runtime);
}

model WebFetchRequest {
  outputFormat?: string(name='OutputFormat', description='The output format. Valid values:

- **markdown**: Markdown format.

- **html**: HTML format.

- **text**: Plain text format.', example='markdown'),
  regionId?: string(name='RegionId', description='The region ID.

This parameter is required.', example='cn-beijing'),
  url?: string(name='Url', description='The URL of the target web page to crawl.

This parameter is required.', example='https://xxx/projects/spring-boot'),
}

model WebFetchResponseBody = {
  content?: string(name='Content', description='The content of the web page.', example='Spring Boot makes it easy to create stand-alone, production-grade Spring based Applications.'),
  contentFormat?: string(name='ContentFormat', description='The actual output format. Valid values:

- **markdown**: Markdown format.

- **html**: HTML format.

- **text**: Plain text format.', example='markdown'),
  errorCode?: string(name='ErrorCode', description='The error code.', example='ErrorMessage Code'),
  errorMessage?: string(name='ErrorMessage', description='The error message.', example='ErrorMessage Example'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='The request ID.', example='A30D0930-xxxx-xxxx-xxxx-C2C661CC8B58'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
  title?: string(name='Title', description='The title of the web page.', example='Spring Boot'),
  url?: string(name='Url', description='The URL of the web page.', example='https://xxxx/projects/spring-boot'),
  urlType?: string(name='UrlType', description='The URL classification result. Valid values:
- **static_html**: Static HTML page.
- **dynamic_js**: Single-page application (SPA) page that requires JavaScript rendering.
- **audio**: Audio file.
- **video**: Video file.
- **document**: Document file (PDF, Word, PPT, or Excel).
- **unknown**: Unrecognizable.', example='static_html'),
}

model WebFetchResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: WebFetchResponseBody(name='body'),
}

/**
 * @summary Retrieves the content of a web page.
 *
 * @description - Region: Only China (Beijing) and Singapore regions are supported.
 * - Pricing: Free of charge during the public preview period.
 *
 * @param request WebFetchRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return WebFetchResponse
 */
@context("webFetchWithContext")
async function webFetchWithOptions(request: WebFetchRequest, runtime: $RuntimeOptions): WebFetchResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.outputFormat)) {
    query['OutputFormat'] = request.outputFormat;
  }
  if (!$isNull(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  if (!$isNull(request.url)) {
    query['Url'] = request.url;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'WebFetch',
    version = '2026-04-01',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves the content of a web page.
 *
 * @description - Region: Only China (Beijing) and Singapore regions are supported.
 * - Pricing: Free of charge during the public preview period.
 *
 * @param request WebFetchRequest
 * @return WebFetchResponse
 */
async function webFetch(request: WebFetchRequest): WebFetchResponse {
  var runtime = new $RuntimeOptions{};
  return webFetchWithOptions(request, runtime);
}

model WebSearchRequest {
  maxResults?: int32(name='MaxResults', description='The maximum number of results to return. Default value: 10. Valid values: 1 to 50.', example='10'),
  query?: string(name='Query', description='The search query statement.

This parameter is required.', example='Spring Boot'),
  regionId?: string(name='RegionId', description='The region ID.

This parameter is required.', example='cn-beijing'),
  urlScopeDomains?: string(name='UrlScopeDomains'),
  urlScopeMode?: string(name='UrlScopeMode'),
}

model WebSearchResponseBody = {
  errorCode?: string(name='ErrorCode', description='The error code.', example='ErrorMessage Code'),
  errorMessage?: string(name='ErrorMessage', description='The error message.', example='ErrorMessage Example'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  query?: string(name='Query', description='The search query statement.', example='Spring Boot'),
  requestId?: string(name='RequestId', description='The request ID.', example='A30D0930-xxxx-xxxx-xxxx-C2C661CC8B58'),
  searchResult?: [ 
    {
      snippet?: string(name='Snippet', description='The summary of the web page content.', example='Spring Boot makes it easy to create stand-alone, production-grade Spring based Applications.'),
      title?: string(name='Title', description='The title of the web page.', example='Spring Boot'),
      url?: string(name='Url', description='The URL of the web page.', example='https://xxx/projects/spring-boot'),
    }
  ](name='SearchResult', description='The search result list.'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
  totalResults?: int32(name='TotalResults', description='The total number of results returned.', example='10'),
}

model WebSearchResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: WebSearchResponseBody(name='body'),
}

/**
 * @summary Performs a web search.
 *
 * @description - Region: Only China (Beijing) and Singapore regions are supported.
 * - Fees: Free of charge during the public preview. No fees are charged.
 *
 * @param request WebSearchRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return WebSearchResponse
 */
@context("webSearchWithContext")
async function webSearchWithOptions(request: WebSearchRequest, runtime: $RuntimeOptions): WebSearchResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.maxResults)) {
    query['MaxResults'] = request.maxResults;
  }
  if (!$isNull(request.query)) {
    query['Query'] = request.query;
  }
  if (!$isNull(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  if (!$isNull(request.urlScopeDomains)) {
    query['UrlScopeDomains'] = request.urlScopeDomains;
  }
  if (!$isNull(request.urlScopeMode)) {
    query['UrlScopeMode'] = request.urlScopeMode;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'WebSearch',
    version = '2026-04-01',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Performs a web search.
 *
 * @description - Region: Only China (Beijing) and Singapore regions are supported.
 * - Fees: Free of charge during the public preview. No fees are charged.
 *
 * @param request WebSearchRequest
 * @return WebSearchResponse
 */
async function webSearch(request: WebSearchRequest): WebSearchResponse {
  var runtime = new $RuntimeOptions{};
  return webSearchWithOptions(request, runtime);
}

api _postOSSObject(bucketName: string, form: object, runtime: $RuntimeOptions): object {
  var boundary = $Form.getBoundary();
  var tmp = $string(form.host);
  var host = `${bucketName}.${tmp}`;
  __request.protocol = 'HTTPS';
  __request.method = 'POST';
  __request.pathname = `/`;
  __request.headers = {
    host = host,
    date = OpenApiUtil.getDateUTCString(),
    user-agent = OpenApiUtil.getUserAgent(''),
  };
  __request.headers.content-type = `multipart/form-data; boundary=${boundary}`;
  __request.body = $Form.toFileForm(form, boundary);
} returns {
  var respMap : object = null;
  var bodyStr = $Stream.readAsString(__response.body);
  if ((__response.statusCode >= 400) && (__response.statusCode < 600)) {
    respMap = $XML.parseXml(bodyStr, null);
    var err = $object(respMap.Error);
    throw new OpenApi.Client{
      code = $string(err.Code),
      message = $string(err.Message),
      data = {
        httpCode = __response.statusCode,
        requestId = $string(err.RequestId),
        hostId = $string(err.HostId),
      }
    };
  }
  respMap = $XML.parseXml(bodyStr, null);
  return {
    ...respMap,
  };
} runtime {
  key = $string($default(runtime.key, @key)),
  cert = $string($default(runtime.cert, @cert)),
  ca = $string($default(runtime.ca, @ca)),
  readTimeout = $number($default(runtime.readTimeout, @readTimeout)),
  connectTimeout = $number($default(runtime.connectTimeout, @connectTimeout)),
  httpProxy = $string($default(runtime.httpProxy, @httpProxy)),
  httpsProxy = $string($default(runtime.httpsProxy, @httpsProxy)),
  noProxy = $string($default(runtime.noProxy, @noProxy)),
  socks5Proxy = $string($default(runtime.socks5Proxy, @socks5Proxy)),
  socks5NetWork = $string($default(runtime.socks5NetWork, @socks5NetWork)),
  maxIdleConns = $number($default(runtime.maxIdleConns, @maxIdleConns)),
  retryOptions = @retryOptions,
  ignoreSSL = $boolean($default(runtime.ignoreSSL, false)),
  tlsMinVersion = @tlsMinVersion
}
