/**
 *
 */
import Util;
import XML;
import Credential;
import FileForm;
import OpenApi;
import OpenApiUtil;
import EndpointUtil;

extends OpenApi;


init(config: OpenApi.Config){
  super(config);
  @endpointRule = 'regional';
  @endpointMap = {
    'cn-beijing' = 'dtsai.cn-beijing.aliyuncs.com',
  };

  checkConfig(config);
  @endpoint = getEndpoint('dtsai', @regionId, @endpointRule, @network, @suffix, @endpointMap, @endpoint);
}

function getEndpoint(productId: string, regionId: string, endpointRule: string, network: string, suffix: string, endpointMap: map[string]string, endpoint: string) throws: string{
  if (!Util.empty(endpoint)) {
    return endpoint;
  }
  
  if (!Util.isUnset(endpointMap) && !Util.empty(endpointMap[regionId])) {
    return endpointMap[regionId];
  }
  return EndpointUtil.getEndpointRules(productId, regionId, endpointRule, network, suffix);
}

model AuthorizeFileUploadRequest {
  agentName?: string(name='AgentName', description='The Agent or client source that initiates the call, such as codex, cursor, or openapi. Maximum length: 32 characters. Used only for statistics and does not participate in authentication, throttling, quota, or billing.', example='codex'),
  batchSize?: string(name='BatchSize'),
  fileFormat?: string(name='FileFormat', description='The format of the file to be uploaded.', example='pdf'),
  regionId?: string(name='RegionId', description='The region ID, such as cn-beijing.

This parameter is required.', example='cn-beijing'),
}

model AuthorizeFileUploadResponseBody = {
  accessKeyId?: string(name='AccessKeyId', description='The temporary AccessKey ID used for OSS PostObject.', example='STS.NV5xxx'),
  authorizations?: [ 
    {
      encodedPolicy?: string(name='EncodedPolicy'),
      objectKey?: string(name='ObjectKey'),
      signature?: string(name='Signature'),
    }
  ](name='Authorizations'),
  bucket?: string(name='Bucket', description='The destination OSS bucket.', example='dts-ai-upload-cn-beijing7500163e0eae09'),
  encodedPolicy?: string(name='EncodedPolicy', description='The Base64-encoded PostObject policy, which includes the ObjectKey and file size limits.', example='eyJleHBpcmF0aW9uIjo...'),
  endpoint?: string(name='Endpoint', description='OSS Endpoint', example='oss-cn-beijing.aliyuncs.com'),
  errorCode?: string(name='ErrorCode', description='The error code.', example='InvalidParameter'),
  errorMessage?: string(name='ErrorMessage', description='The error message.', example='The parameter [Query] is not valid.'),
  expireTime?: long(name='ExpireTime'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The business-level HTTP status code.', example='200'),
  objectKey?: string(name='ObjectKey', description='The object key that must be used as-is for the upload.', example='0a1b_123456_0123456789abcdef0123456789abcdef'),
  requestId?: string(name='RequestId', description='The request ID.', example='A30D0930-xxxx-xxxx-xxxx-C2C661CC8B58'),
  securityToken?: string(name='SecurityToken', description='The Security Token Service (STS) temporary security token.', example='CAISxxx'),
  signature?: string(name='Signature', description='The policy signature.', example='masked-signature'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model AuthorizeFileUploadResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: AuthorizeFileUploadResponseBody(name='body'),
}

/**
 * @summary Uploads a file directly to the Bucket/ObjectKey specified in the response, and then uses the object URL as OssFileUrl to create a parsing task.
 *
 * @param request AuthorizeFileUploadRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return AuthorizeFileUploadResponse
 */
async function authorizeFileUploadWithOptions(request: AuthorizeFileUploadRequest, runtime: Util.RuntimeOptions): AuthorizeFileUploadResponse {
  Util.validateModel(request);
  var query = {};
  if (!Util.isUnset(request.agentName)) {
    query['AgentName'] = request.agentName;
  }
  if (!Util.isUnset(request.batchSize)) {
    query['BatchSize'] = request.batchSize;
  }
  if (!Util.isUnset(request.fileFormat)) {
    query['FileFormat'] = request.fileFormat;
  }
  if (!Util.isUnset(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  var req = new OpenApi.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'AuthorizeFileUpload',
    version = '2026-04-01',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Uploads a file directly to the Bucket/ObjectKey specified in the response, and then uses the object URL as OssFileUrl to create a parsing task.
 *
 * @param request AuthorizeFileUploadRequest
 * @return AuthorizeFileUploadResponse
 */
async function authorizeFileUpload(request: AuthorizeFileUploadRequest): AuthorizeFileUploadResponse {
  var runtime = new Util.RuntimeOptions{};
  return authorizeFileUploadWithOptions(request, runtime);
}

model CreateDocParserJobRequest {
  agentName?: string(name='AgentName', description='The agent name.'),
  asrLanguage?: string(name='AsrLanguage', description='The language type for speech recognition.'),
  audioClipOutput?: boolean(name='AudioClipOutput', description='The audio clip output.'),
  audioWindowSeconds?: int32(name='AudioWindowSeconds', description='The audio window duration in seconds.'),
  chunkSummary?: boolean(name='ChunkSummary', description='The chunk summary information.'),
  fileFormat?: string(name='FileFormat', description='The format of the input file. Valid values:

- **pdf**: PDF file.

- **docx**: Word file in docx format.

- **doc**: Word file in doc format.

- **pptx**: PPT file in pptx format.

- **ppt**: PPT file in ppt format.

- **txt**: Plain text file.

- **md**: Markdown file.

- **png**: PNG image.

- **jpg**: JPG image.

- **jpeg**: JPEG image.

This parameter is required.', example='pdf'),
  fileName?: string(name='FileName', description='The file name, which must include the file name extension.

This parameter is required.', example='document.pdf'),
  fileUrl?: string(name='FileUrl', description='The HTTP or HTTPS URL of the file to be parsed.
>SDKs for various programming languages additionally provide a `CreateDocParserJobAdvance` method that supports passing a local file stream directly (such as Java InputStream), without the need to upload the file to OSS and construct a FileUrl in advance. When using the Advance method, replace the `FileUrl` parameter (URL string) with the `FileUrlObject` parameter (file stream). All other request parameters remain unchanged. The SDK automatically performs the following operations:
>1. Obtains temporary OSS upload credentials.
>2. Uploads the file stream directly to OSS.
>3. Calls the CreateDocParserJob operation using the generated OSS URL.', example='https://xxx.oss-cn-beijing.aliyuncs.com/document.pdf?Expires=xxx&OSSAccessKeyId=xxx&Signature=xxx'),
  frameOutput?: boolean(name='FrameOutput', description='The frame output result.'),
  globalSummary?: boolean(name='GlobalSummary', description='The global summary information.'),
  imageMode?: string(name='ImageMode', description='The image processing format.'),
  imageUnderstanding?: string(name='ImageUnderstanding', description='The image understanding and analysis setting.'),
  mediaChunkIntervalSeconds?: int32(name='MediaChunkIntervalSeconds', description='The media chunk interval in seconds.'),
  mediaChunkStrategy?: string(name='MediaChunkStrategy', description='The media chunk strategy.'),
  mediaFramesPerMinute?: double(name='MediaFramesPerMinute', description='The number of media frames per minute.'),
  mediaMaxFrameBudget?: int32(name='MediaMaxFrameBudget', description='The maximum frame budget for media.'),
  mediaMinFrameBudget?: int32(name='MediaMinFrameBudget', description='The minimum frame budget for media.'),
  ossFileUrl?: string(name='OssFileUrl', description='The OSS file URL.'),
  outputFormat?: string(name='OutputFormat', description='The output format of the parsing result. Valid values:

- **markdown**: Markdown format.

This parameter is required.', example='markdown'),
  parseScene?: string(name='ParseScene', description='The parsing scene.'),
  regionId?: string(name='RegionId', description='The region ID.

This parameter is required.', example='cn-beijing'),
  responseMode?: string(name='ResponseMode', description='The response mode.'),
  resultType?: string(name='ResultType', description='The result type.'),
  tableFormat?: string(name='TableFormat', description='The table processing format.'),
}

model CreateDocParserJobAdvanceRequest {
  agentName?: string(name='AgentName', description='The agent name.'),
  asrLanguage?: string(name='AsrLanguage', description='The language type for speech recognition.'),
  audioClipOutput?: boolean(name='AudioClipOutput', description='The audio clip output.'),
  audioWindowSeconds?: int32(name='AudioWindowSeconds', description='The audio window duration in seconds.'),
  chunkSummary?: boolean(name='ChunkSummary', description='The chunk summary information.'),
  fileFormat?: string(name='FileFormat', description='The format of the input file. Valid values:

- **pdf**: PDF file.

- **docx**: Word file in docx format.

- **doc**: Word file in doc format.

- **pptx**: PPT file in pptx format.

- **ppt**: PPT file in ppt format.

- **txt**: Plain text file.

- **md**: Markdown file.

- **png**: PNG image.

- **jpg**: JPG image.

- **jpeg**: JPEG image.

This parameter is required.', example='pdf'),
  fileName?: string(name='FileName', description='The file name, which must include the file name extension.

This parameter is required.', example='document.pdf'),
  fileUrlObject?: readable(name='FileUrl', description='The HTTP or HTTPS URL of the file to be parsed.
>SDKs for various programming languages additionally provide a `CreateDocParserJobAdvance` method that supports passing a local file stream directly (such as Java InputStream), without the need to upload the file to OSS and construct a FileUrl in advance. When using the Advance method, replace the `FileUrl` parameter (URL string) with the `FileUrlObject` parameter (file stream). All other request parameters remain unchanged. The SDK automatically performs the following operations:
>1. Obtains temporary OSS upload credentials.
>2. Uploads the file stream directly to OSS.
>3. Calls the CreateDocParserJob operation using the generated OSS URL.', example='https://xxx.oss-cn-beijing.aliyuncs.com/document.pdf?Expires=xxx&OSSAccessKeyId=xxx&Signature=xxx'),
  frameOutput?: boolean(name='FrameOutput', description='The frame output result.'),
  globalSummary?: boolean(name='GlobalSummary', description='The global summary information.'),
  imageMode?: string(name='ImageMode', description='The image processing format.'),
  imageUnderstanding?: string(name='ImageUnderstanding', description='The image understanding and analysis setting.'),
  mediaChunkIntervalSeconds?: int32(name='MediaChunkIntervalSeconds', description='The media chunk interval in seconds.'),
  mediaChunkStrategy?: string(name='MediaChunkStrategy', description='The media chunk strategy.'),
  mediaFramesPerMinute?: double(name='MediaFramesPerMinute', description='The number of media frames per minute.'),
  mediaMaxFrameBudget?: int32(name='MediaMaxFrameBudget', description='The maximum frame budget for media.'),
  mediaMinFrameBudget?: int32(name='MediaMinFrameBudget', description='The minimum frame budget for media.'),
  ossFileUrl?: string(name='OssFileUrl', description='The OSS file URL.'),
  outputFormat?: string(name='OutputFormat', description='The output format of the parsing result. Valid values:

- **markdown**: Markdown format.

This parameter is required.', example='markdown'),
  parseScene?: string(name='ParseScene', description='The parsing scene.'),
  regionId?: string(name='RegionId', description='The region ID.

This parameter is required.', example='cn-beijing'),
  responseMode?: string(name='ResponseMode', description='The response mode.'),
  resultType?: string(name='ResultType', description='The result type.'),
  tableFormat?: string(name='TableFormat', description='The table processing format.'),
}

model CreateDocParserJobResponseBody = {
  errorCode?: string(name='ErrorCode', description='The error code.', example='InvalidParameter'),
  errorMessage?: string(name='ErrorMessage', description='The error message.', example='The parameter is not valid.'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  jobId?: string(name='JobId', description='The document parsing task ID.', example='job_abc123'),
  requestId?: string(name='RequestId', description='The request ID.', example='A30D0930-xxxx-xxxx-xxxx-C2C661CC8B58'),
  result?: string(name='Result', description='The result data.'),
  resultType?: string(name='ResultType', description='The result type.'),
  resultUrl?: string(name='ResultUrl', description='The result URL.'),
  status?: string(name='Status', description='The status information.'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model CreateDocParserJobResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateDocParserJobResponseBody(name='body'),
}

/**
 * @summary Creates a document parsing task.
 *
 * @description - Region: Only China (Beijing) is supported.
 * - Fees: The service is free of charge during the public preview period.
 *
 * @param request CreateDocParserJobRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateDocParserJobResponse
 */
async function createDocParserJobWithOptions(request: CreateDocParserJobRequest, runtime: Util.RuntimeOptions): CreateDocParserJobResponse {
  Util.validateModel(request);
  var query = {};
  if (!Util.isUnset(request.agentName)) {
    query['AgentName'] = request.agentName;
  }
  if (!Util.isUnset(request.asrLanguage)) {
    query['AsrLanguage'] = request.asrLanguage;
  }
  if (!Util.isUnset(request.audioClipOutput)) {
    query['AudioClipOutput'] = request.audioClipOutput;
  }
  if (!Util.isUnset(request.audioWindowSeconds)) {
    query['AudioWindowSeconds'] = request.audioWindowSeconds;
  }
  if (!Util.isUnset(request.chunkSummary)) {
    query['ChunkSummary'] = request.chunkSummary;
  }
  if (!Util.isUnset(request.fileFormat)) {
    query['FileFormat'] = request.fileFormat;
  }
  if (!Util.isUnset(request.fileName)) {
    query['FileName'] = request.fileName;
  }
  if (!Util.isUnset(request.fileUrl)) {
    query['FileUrl'] = request.fileUrl;
  }
  if (!Util.isUnset(request.frameOutput)) {
    query['FrameOutput'] = request.frameOutput;
  }
  if (!Util.isUnset(request.globalSummary)) {
    query['GlobalSummary'] = request.globalSummary;
  }
  if (!Util.isUnset(request.imageMode)) {
    query['ImageMode'] = request.imageMode;
  }
  if (!Util.isUnset(request.imageUnderstanding)) {
    query['ImageUnderstanding'] = request.imageUnderstanding;
  }
  if (!Util.isUnset(request.mediaChunkIntervalSeconds)) {
    query['MediaChunkIntervalSeconds'] = request.mediaChunkIntervalSeconds;
  }
  if (!Util.isUnset(request.mediaChunkStrategy)) {
    query['MediaChunkStrategy'] = request.mediaChunkStrategy;
  }
  if (!Util.isUnset(request.mediaFramesPerMinute)) {
    query['MediaFramesPerMinute'] = request.mediaFramesPerMinute;
  }
  if (!Util.isUnset(request.mediaMaxFrameBudget)) {
    query['MediaMaxFrameBudget'] = request.mediaMaxFrameBudget;
  }
  if (!Util.isUnset(request.mediaMinFrameBudget)) {
    query['MediaMinFrameBudget'] = request.mediaMinFrameBudget;
  }
  if (!Util.isUnset(request.ossFileUrl)) {
    query['OssFileUrl'] = request.ossFileUrl;
  }
  if (!Util.isUnset(request.outputFormat)) {
    query['OutputFormat'] = request.outputFormat;
  }
  if (!Util.isUnset(request.parseScene)) {
    query['ParseScene'] = request.parseScene;
  }
  if (!Util.isUnset(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  if (!Util.isUnset(request.responseMode)) {
    query['ResponseMode'] = request.responseMode;
  }
  if (!Util.isUnset(request.resultType)) {
    query['ResultType'] = request.resultType;
  }
  if (!Util.isUnset(request.tableFormat)) {
    query['TableFormat'] = request.tableFormat;
  }
  var req = new OpenApi.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'CreateDocParserJob',
    version = '2026-04-01',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates a document parsing task.
 *
 * @description - Region: Only China (Beijing) is supported.
 * - Fees: The service is free of charge during the public preview period.
 *
 * @param request CreateDocParserJobRequest
 * @return CreateDocParserJobResponse
 */
async function createDocParserJob(request: CreateDocParserJobRequest): CreateDocParserJobResponse {
  var runtime = new Util.RuntimeOptions{};
  return createDocParserJobWithOptions(request, runtime);
}

async function createDocParserJobAdvance(request: CreateDocParserJobAdvanceRequest, runtime: Util.RuntimeOptions): CreateDocParserJobResponse {
  // Step 0: init client
  var credentialModel: Credential.CredentialModel = null;
  if (Util.isUnset(@credential)) {
    throw {
      code = 'InvalidCredentials',
      message = 'Please set up the credentials correctly. If you are setting them through environment variables, please ensure that ALIBABA_CLOUD_ACCESS_KEY_ID and ALIBABA_CLOUD_ACCESS_KEY_SECRET are set correctly. See https://help.aliyun.com/zh/sdk/developer-reference/configure-the-alibaba-cloud-accesskey-environment-variable-on-linux-macos-and-windows-systems for more details.'
    }
  }
  credentialModel = @credential.getCredential();
  var accessKeyId = credentialModel.accessKeyId;
  var accessKeySecret = credentialModel.accessKeySecret;
  var securityToken = credentialModel.securityToken;
  var credentialType = credentialModel.type;
  var openPlatformEndpoint = @openPlatformEndpoint;
  if(Util.empty(openPlatformEndpoint)) {
    openPlatformEndpoint ='openplatform.aliyuncs.com';
  }
  if(Util.isUnset(credentialType)) {
    credentialType ='access_key';
  }
  var authConfig = new OpenApi.Config{
    accessKeyId = accessKeyId,
    accessKeySecret = accessKeySecret,
    securityToken = securityToken,
    type = credentialType,
    endpoint = openPlatformEndpoint,
    protocol = @protocol,
    regionId = @regionId,
  };
  var authClient = new OpenApi(authConfig);
  var authRequest = {
    Product = 'DtsAI',
    RegionId = @regionId,
  };
  var authReq = new OpenApi.OpenApiRequest{
    query = OpenApiUtil.query(authRequest),
  };
  var authParams = new OpenApi.Params{
    action = 'AuthorizeFileUpload',
    version = '2019-12-19',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'GET',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  var authResponse : object = {};
  var fileObj = new FileForm.FileField{};
  var ossHeader: object = {};
  var tmpBody: object = {};
  var useAccelerate: boolean = false;
  var authResponseBody: map[string]string = {};
  var createDocParserJobReq = new CreateDocParserJobRequest{};
  OpenApiUtil.convert(request, createDocParserJobReq);

  if(!Util.isUnset(request.fileUrlObject)) {
    var tmpResp0: any = authClient.callApi(authParams, authReq, runtime);
    authResponse = Util.assertAsMap(tmpResp0);
    tmpBody = Util.assertAsMap(authResponse['body']);
    useAccelerate = Util.assertAsBoolean(tmpBody['UseAccelerate']);
    authResponseBody = Util.stringifyMapValue(tmpBody);
    fileObj = new FileForm.FileField{
      filename = authResponseBody['ObjectKey'],
      content = request.fileUrlObject,
      contentType = '',
    };
    ossHeader = {
      host = OpenApiUtil.getEndpoint(authResponseBody['Endpoint'], useAccelerate, @endpointType),
      OSSAccessKeyId = authResponseBody['AccessKeyId'],
      policy = authResponseBody['EncodedPolicy'],
      Signature = authResponseBody['Signature'],
      key = authResponseBody['ObjectKey'],
      file = fileObj,
      success_action_status = '201',
    };
    _postOSSObject(authResponseBody['Bucket'], ossHeader, runtime);
    createDocParserJobReq.fileUrl = `http://${authResponseBody['Bucket']}.${authResponseBody['Endpoint']}/${authResponseBody['ObjectKey']}`;
  }
  var createDocParserJobResp = createDocParserJobWithOptions(createDocParserJobReq, runtime);
  return createDocParserJobResp;
}

model DescribeDocParserJobResultRequest {
  agentName?: string(name='AgentName', description='The agent name.'),
  jobId?: string(name='JobId', description='The document parsing task ID, obtained by calling CreateDocParserJob.

This parameter is required.', example='job_abc123'),
  regionId?: string(name='RegionId', description='The region ID.

This parameter is required.', example='cn-beijing'),
  resultType?: string(name='ResultType', description='The result type.'),
}

model DescribeDocParserJobResultResponseBody = {
  errorCode?: string(name='ErrorCode', description='The error code.', example='JobNotFound'),
  errorMessage?: string(name='ErrorMessage', description='The error message.', example='The specified job does not exist.'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='The request ID.', example='A30D0930-xxxx-xxxx-xxxx-C2C661CC8B58'),
  result?: string(name='Result', description='The document parsing result. The format is determined by the OutputFormat parameter specified in the CreateDocParserJob request.', example='# Document Title'),
  resultType?: string(name='ResultType', description='The result format.'),
  resultUrl?: string(name='ResultUrl', description='oss url'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model DescribeDocParserJobResultResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeDocParserJobResultResponseBody(name='body'),
}

/**
 * @summary Retrieves the result of a document parsing task.
 *
 * @description - Region: Only China (Beijing) is supported.
 * - Fees: Free of charge during the public preview period.
 * - Call DescribeDocParserJobResult to retrieve the parsing result of a document parsing task. Call this operation only after DescribeDocParserJobStatus returns a Status of success. Task results are retained for 72 hours and cannot be retrieved after expiration.
 *
 * @param request DescribeDocParserJobResultRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeDocParserJobResultResponse
 */
async function describeDocParserJobResultWithOptions(request: DescribeDocParserJobResultRequest, runtime: Util.RuntimeOptions): DescribeDocParserJobResultResponse {
  Util.validateModel(request);
  var query = {};
  if (!Util.isUnset(request.agentName)) {
    query['AgentName'] = request.agentName;
  }
  if (!Util.isUnset(request.jobId)) {
    query['JobId'] = request.jobId;
  }
  if (!Util.isUnset(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  if (!Util.isUnset(request.resultType)) {
    query['ResultType'] = request.resultType;
  }
  var req = new OpenApi.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'DescribeDocParserJobResult',
    version = '2026-04-01',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves the result of a document parsing task.
 *
 * @description - Region: Only China (Beijing) is supported.
 * - Fees: Free of charge during the public preview period.
 * - Call DescribeDocParserJobResult to retrieve the parsing result of a document parsing task. Call this operation only after DescribeDocParserJobStatus returns a Status of success. Task results are retained for 72 hours and cannot be retrieved after expiration.
 *
 * @param request DescribeDocParserJobResultRequest
 * @return DescribeDocParserJobResultResponse
 */
async function describeDocParserJobResult(request: DescribeDocParserJobResultRequest): DescribeDocParserJobResultResponse {
  var runtime = new Util.RuntimeOptions{};
  return describeDocParserJobResultWithOptions(request, runtime);
}

model DescribeDocParserJobStatusRequest {
  agentName?: string(name='AgentName', description='The agent name.'),
  jobId?: string(name='JobId', description='The ID of the document parsing task. You can obtain this ID by calling CreateDocParserJob.

This parameter is required.', example='job_abc123'),
  regionId?: string(name='RegionId', description='The region ID.

This parameter is required.', example='cn-beijing'),
}

model DescribeDocParserJobStatusResponseBody = {
  errorCode?: string(name='ErrorCode', description='The error code.', example='JobNotFound'),
  errorMessage?: string(name='ErrorMessage', description='The error message.', example='The specified job does not exist.'),
  failureMessage?: string(name='FailureMessage', description='The failure reason. This parameter has a value only when Status is failed.', example='Failed Message'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='The request ID.', example='A30D0930-xxxx-xxxx-xxxx-C2C661CC8B58'),
  status?: string(name='Status', description='The task status. Valid values:

- **init**: Created and being prepared.

- **pending**: Queued and waiting to be scheduled.

- **running**: Being processed and parsed.

- **success**: Completed. You can call DescribeDocParserJobResult to obtain the result.

- **failed**: Failed. The reason is provided in FailureMessage.

- **cancelled**: Cancelled.', example='running'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
}

model DescribeDocParserJobStatusResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeDocParserJobStatusResponseBody(name='body'),
}

/**
 * @summary Queries the status of a document parsing task.
 *
 * @description - Region: Only China (Beijing) is supported.
 * - Fees: The service is free of charge during the public preview period.
 *
 * @param request DescribeDocParserJobStatusRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeDocParserJobStatusResponse
 */
async function describeDocParserJobStatusWithOptions(request: DescribeDocParserJobStatusRequest, runtime: Util.RuntimeOptions): DescribeDocParserJobStatusResponse {
  Util.validateModel(request);
  var query = {};
  if (!Util.isUnset(request.agentName)) {
    query['AgentName'] = request.agentName;
  }
  if (!Util.isUnset(request.jobId)) {
    query['JobId'] = request.jobId;
  }
  if (!Util.isUnset(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  var req = new OpenApi.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'DescribeDocParserJobStatus',
    version = '2026-04-01',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the status of a document parsing task.
 *
 * @description - Region: Only China (Beijing) is supported.
 * - Fees: The service is free of charge during the public preview period.
 *
 * @param request DescribeDocParserJobStatusRequest
 * @return DescribeDocParserJobStatusResponse
 */
async function describeDocParserJobStatus(request: DescribeDocParserJobStatusRequest): DescribeDocParserJobStatusResponse {
  var runtime = new Util.RuntimeOptions{};
  return describeDocParserJobStatusWithOptions(request, runtime);
}

model WebFetchRequest {
  agentName?: string(name='AgentName', description='The agent name.'),
  outputFormat?: string(name='OutputFormat', description='The output format. Valid values:

- **markdown**: Markdown format.

- **html**: HTML format.

- **text**: Plain text format.', example='markdown'),
  regionId?: string(name='RegionId', description='The region ID.

This parameter is required.', example='cn-beijing'),
  url?: string(name='Url', description='The URL of the target web page to crawl.

This parameter is required.', example='https://xxx/projects/spring-boot'),
}

model WebFetchResponseBody = {
  content?: string(name='Content', description='The content of the web page.', example='Spring Boot makes it easy to create stand-alone, production-grade Spring based Applications.'),
  contentFormat?: string(name='ContentFormat', description='The actual output format. Valid values:

- **markdown**: Markdown format.

- **html**: HTML format.

- **text**: Plain text format.', example='markdown'),
  errorCode?: string(name='ErrorCode', description='The error code.', example='ErrorMessage Code'),
  errorMessage?: string(name='ErrorMessage', description='The error message.', example='ErrorMessage Example'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  requestId?: string(name='RequestId', description='The request ID.', example='A30D0930-xxxx-xxxx-xxxx-C2C661CC8B58'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
  title?: string(name='Title', description='The title of the web page.', example='Spring Boot'),
  url?: string(name='Url', description='The URL of the web page.', example='https://xxxx/projects/spring-boot'),
  urlType?: string(name='UrlType', description='The URL classification result. Valid values:
- **static_html**: A static HTML page.
- **dynamic_js**: A single-page application (SPA) page that requires JavaScript rendering.
- **audio**: An audio file.
- **video**: A video file.
- **document**: A document file (PDF, Word, PPT, or Excel).
- **unknown**: Unrecognizable.', example='static_html'),
}

model WebFetchResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: WebFetchResponseBody(name='body'),
}

/**
 * @summary Retrieves the content of a web page.
 *
 * @description - Region: Only China (Beijing) and Singapore regions are supported.
 * - Fees: Free of charge during the public preview period.
 *
 * @param request WebFetchRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return WebFetchResponse
 */
async function webFetchWithOptions(request: WebFetchRequest, runtime: Util.RuntimeOptions): WebFetchResponse {
  Util.validateModel(request);
  var query = {};
  if (!Util.isUnset(request.agentName)) {
    query['AgentName'] = request.agentName;
  }
  if (!Util.isUnset(request.outputFormat)) {
    query['OutputFormat'] = request.outputFormat;
  }
  if (!Util.isUnset(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  if (!Util.isUnset(request.url)) {
    query['Url'] = request.url;
  }
  var req = new OpenApi.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'WebFetch',
    version = '2026-04-01',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves the content of a web page.
 *
 * @description - Region: Only China (Beijing) and Singapore regions are supported.
 * - Fees: Free of charge during the public preview period.
 *
 * @param request WebFetchRequest
 * @return WebFetchResponse
 */
async function webFetch(request: WebFetchRequest): WebFetchResponse {
  var runtime = new Util.RuntimeOptions{};
  return webFetchWithOptions(request, runtime);
}

model WebSearchRequest {
  agentName?: string(name='AgentName', description='The agent name.'),
  maxResults?: int32(name='MaxResults', description='The maximum number of results to return. Default value: 10. Valid values: 1 to 50.', example='10'),
  query?: string(name='Query', description='The search query statement.

This parameter is required.', example='Spring Boot'),
  regionId?: string(name='RegionId', description='The region ID.

This parameter is required.', example='cn-beijing'),
  urlScopeDomains?: string(name='UrlScopeDomains', description='The list of domain names.'),
  urlScopeMode?: string(name='UrlScopeMode', description='The URL scope mode.'),
}

model WebSearchResponseBody = {
  errorCode?: string(name='ErrorCode', description='The error code.', example='ErrorMessage Code'),
  errorMessage?: string(name='ErrorMessage', description='The error message.', example='ErrorMessage Example'),
  httpStatusCode?: int32(name='HttpStatusCode', description='The HTTP status code.', example='200'),
  query?: string(name='Query', description='The search query statement.', example='Spring Boot'),
  requestId?: string(name='RequestId', description='The request ID.', example='A30D0930-xxxx-xxxx-xxxx-C2C661CC8B58'),
  searchResult?: [ 
    {
      snippet?: string(name='Snippet', description='The snippet of the web page content.', example='Spring Boot makes it easy to create stand-alone, production-grade Spring based Applications.'),
      title?: string(name='Title', description='The title of the web page.', example='Spring Boot'),
      url?: string(name='Url', description='The URL of the web page.', example='https://xxx/projects/spring-boot'),
    }
  ](name='SearchResult', description='The list of search results.'),
  success?: boolean(name='Success', description='Indicates whether the request was successful.', example='true'),
  totalResults?: int32(name='TotalResults', description='The total number of results returned.', example='10'),
}

model WebSearchResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: WebSearchResponseBody(name='body'),
}

/**
 * @summary Performs a web search.
 *
 * @description - Region: Only China (Beijing) and Singapore regions are supported.
 * - Fees: Free during the public preview period. No fees are charged.
 *
 * @param request WebSearchRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return WebSearchResponse
 */
async function webSearchWithOptions(request: WebSearchRequest, runtime: Util.RuntimeOptions): WebSearchResponse {
  Util.validateModel(request);
  var query = {};
  if (!Util.isUnset(request.agentName)) {
    query['AgentName'] = request.agentName;
  }
  if (!Util.isUnset(request.maxResults)) {
    query['MaxResults'] = request.maxResults;
  }
  if (!Util.isUnset(request.query)) {
    query['Query'] = request.query;
  }
  if (!Util.isUnset(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  if (!Util.isUnset(request.urlScopeDomains)) {
    query['UrlScopeDomains'] = request.urlScopeDomains;
  }
  if (!Util.isUnset(request.urlScopeMode)) {
    query['UrlScopeMode'] = request.urlScopeMode;
  }
  var req = new OpenApi.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'WebSearch',
    version = '2026-04-01',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Performs a web search.
 *
 * @description - Region: Only China (Beijing) and Singapore regions are supported.
 * - Fees: Free during the public preview period. No fees are charged.
 *
 * @param request WebSearchRequest
 * @return WebSearchResponse
 */
async function webSearch(request: WebSearchRequest): WebSearchResponse {
  var runtime = new Util.RuntimeOptions{};
  return webSearchWithOptions(request, runtime);
}

api _postOSSObject(bucketName: string, data: object, runtime: Util.RuntimeOptions): object {
  var form = Util.assertAsMap(data);
  var boundary = FileForm.getBoundary();
  var tmp = Util.assertAsString(form.host);
  var host = `${bucketName}.${tmp}`;
  __request.protocol = 'HTTPS';
  __request.method = 'POST';
  __request.pathname = `/`;
  __request.headers = {
    host = host,
    date = Util.getDateUTCString(),
    user-agent = Util.getUserAgent(''),
  };
  __request.headers.content-type = `multipart/form-data; boundary=${boundary}`;
  __request.body = FileForm.toFileForm(form, boundary);
} returns {
  var respMap : object = null;
  var bodyStr = Util.readAsString(__response.body);
  if (Util.is4xx(__response.statusCode) || Util.is5xx(__response.statusCode)) {
    respMap = XML.parseXml(bodyStr, null);
    var err = Util.assertAsMap(respMap.Error);
    throw {
      code = err.Code,
      message = err.Message,
      data = {
        httpCode = __response.statusCode,
        requestId = err.RequestId,
        hostId = err.HostId,
      }
    };
  }
  respMap = XML.parseXml(bodyStr, null);
  return {
    ...respMap,
  };
} runtime {
  timeouted = 'retry',
  key = Util.defaultString(runtime.key, @key),
  cert = Util.defaultString(runtime.cert, @cert),
  ca = Util.defaultString(runtime.ca, @ca),
  readTimeout = Util.defaultNumber(runtime.readTimeout, @readTimeout),
  connectTimeout = Util.defaultNumber(runtime.connectTimeout, @connectTimeout),
  httpProxy = Util.defaultString(runtime.httpProxy, @httpProxy),
  httpsProxy = Util.defaultString(runtime.httpsProxy, @httpsProxy),
  noProxy = Util.defaultString(runtime.noProxy, @noProxy),
  socks5Proxy = Util.defaultString(runtime.socks5Proxy, @socks5Proxy),
  socks5NetWork = Util.defaultString(runtime.socks5NetWork, @socks5NetWork),
  maxIdleConns = Util.defaultNumber(runtime.maxIdleConns, @maxIdleConns),
  retry = {
    retryable = runtime.autoretry,
    maxAttempts = Util.defaultNumber(runtime.maxAttempts, 3)
  },
  backoff = {
    policy = Util.defaultString(runtime.backoffPolicy, 'no'),
    period = Util.defaultNumber(runtime.backoffPeriod, 1)
  },
  ignoreSSL = OpenApi.defaultAny(runtime.ignoreSSL, false),
  tlsMinVersion = @tlsMinVersion
}
