/**
 *
 */
import OpenApi;
import OpenApi.OpenApiUtil;

extends OpenApi;


init(config: OpenApiUtil.Config){
  super(config);
  @endpointRule = '';
  
  checkConfig(config);
  @endpoint = getEndpoint('agentrun', @regionId, @endpointRule, @network, @suffix, @endpointMap, @endpoint);
}

function getEndpoint(productId: string, regionId: string, endpointRule: string, network: string, suffix: string, endpointMap: map[string]string, endpoint: string) throws: string{
  if (!$isNull(endpoint)) {
    return endpoint;
  }
  
  if (!$isNull(endpointMap) && !$isNull(endpointMap[regionId])) {
    return endpointMap[regionId];
  }
  return OpenApiUtil.getEndpointRules(productId, regionId, endpointRule, network, suffix);
}

model AgentRuntime {
  agentRuntimeArn?: string(name='agentRuntimeArn', description='智能体运行时的全局唯一资源名称', example='acs:agentrun:cn-hangzhou:1760720386195983:runtimes/7a1b6d39-9f8f-4ce2-b9c9-6db1b0b9e169'),
  agentRuntimeId?: string(name='agentRuntimeId', description='智能体运行时的唯一标识符', example='ar-1234567890abcdef'),
  agentRuntimeName?: string(name='agentRuntimeName', description='智能体运行时的名称，用于标识和区分不同的运行时实例', example='my-agent-runtime'),
  agentRuntimeVersion?: string(name='agentRuntimeVersion', description='智能体运行时的版本号，用于版本管理和回滚', example='1'),
  artifactType?: string(name='artifactType', description='智能体运行时的部署类型，支持Code（代码模式）和Container（容器模式）', example='Code'),
  codeConfiguration?: CodeConfiguration(name='codeConfiguration', description='当artifactType为Code时的代码配置信息', example='{}'),
  containerConfiguration?: ContainerConfiguration(name='containerConfiguration', description='当artifactType为Container时的容器配置信息', example='{}'),
  cpu?: float(name='cpu', description='智能体运行时分配的CPU资源，单位为核数', example='2.0'),
  createdAt?: string(name='createdAt', description='智能体运行时的创建时间，采用ISO 8601格式', example='2025-01-10T10:30:00Z'),
  credentialName?: string(name='credentialName', description='用于访问智能体的凭证名称，访问智能体运行时将使用此凭证进行身份验证', example='my-credential'),
  description?: string(name='description', description='智能体运行时的描述信息，说明该运行时的用途和功能', example='AI agent runtime for customer service automation'),
  environmentVariables?: map[string]string(name='environmentVariables', description='智能体运行时的环境变量配置', example='ENV_VAR1=value1,ENV_VAR2=value2'),
  executionRoleArn?: string(name='executionRoleArn', description='为智能体运行时提供访问云服务权限的执行角色ARN', example='acs:ram::1760720386195983:role/AgentRunExecutionRole'),
  healthCheckConfiguration?: HealthCheckConfiguration(name='healthCheckConfiguration', description='智能体运行时的健康检查配置，用于监控运行时实例的健康状态', example='{}'),
  lastUpdatedAt?: string(name='lastUpdatedAt', description='智能体运行时最后一次更新的时间，采用ISO 8601格式', example='2025-01-10T11:45:00Z'),
  logConfiguration?: LogConfiguration(name='logConfiguration', description='SLS（简单日志服务）配置', example='{}'),
  memory?: integer(name='memory', description='智能体运行时分配的内存资源，单位为MB', example='2048'),
  networkConfiguration?: NetworkConfiguration(name='networkConfiguration', description='智能体运行时的网络配置信息', example='{}'),
  port?: integer(name='port', description='智能体运行时监听的端口号', example='8080'),
  protocolConfiguration?: ProtocolConfiguration(name='protocolConfiguration', description='智能体运行时的通信协议配置', example='{}'),
  sessionConcurrencyLimitPerInstance?: integer(name='sessionConcurrencyLimitPerInstance', description='每个运行时实例允许的最大并发会话数', example='100'),
  sessionIdleTimeoutSeconds?: int32(name='sessionIdleTimeoutSeconds', description='会话的空闲超时时间，单位为秒。实例没有会话请求后处于空闲状态，空闲态为闲置计费模式，超过此超时时间后会话自动过期，不可继续使用', example='3600'),
  status?: string(name='status', description='智能体运行时的当前状态，如READY（就绪）、CREATING（创建中）、FAILED（失败）等', example='READY'),
  statusReason?: string(name='statusReason', description='当前状态的原因说明（如适用）', example='Runtime is ready for use'),
}

model AgentRuntimeEndpoint {
  agentRuntimeEndpointArn?: string(name='agentRuntimeEndpointArn', example='acs:agentrun:cn-hangzhou:1760720386195983:runtime-endpoints/e56d483c-db80-4116-9ec4-25f16f5facfd'),
  agentRuntimeEndpointId?: string(name='agentRuntimeEndpointId', example='are-1234567890abcdef'),
  agentRuntimeEndpointName?: string(name='agentRuntimeEndpointName', example='production-endpoint'),
  agentRuntimeId?: string(name='agentRuntimeId', example='ar-1234567890abcdef'),
  description?: string(name='description', example='Production endpoint for customer service agent'),
  endpointPublicUrl?: string(name='endpointPublicUrl', description='智能体运行时端点的公网访问地址', example='https://ar-1234567890abcdef.cn-hangzhou.agentrun.aliyuncs.com'),
  routingConfiguration?: RoutingConfiguration(name='routingConfiguration', description='智能体运行时端点的路由配置，支持多版本权重分配'),
  status?: string(name='status', example='ACTIVE'),
  statusReason?: string(name='statusReason', example='Endpoint is active and ready'),
  targetVersion?: string(name='targetVersion', example='LATEST'),
}

model AgentRuntimeEndpointResult {
  code?: string(name='code', description='SUCCESS 为成功，失败情况返回对应错误类型，比如 ERR_BAD_REQUEST ERR_VALIDATION_FAILED ERR_INTERNAL_SERVER_ERROR'),
  data?: AgentRuntimeEndpoint(name='data', description='智能体运行时端点的详细信息'),
  requestId?: string(name='requestId', description='唯一的请求标识符，用于问题追踪'),
}

model AgentRuntimeResult {
  code?: string(name='code', description='SUCCESS 为成功，失败情况返回对应错误类型，比如 ERR_BAD_REQUEST ERR_VALIDATION_FAILED ERR_INTERNAL_SERVER_ERROR', example='SUCCESS'),
  data?: AgentRuntime(name='data', description='智能体运行时的详细信息', example='{}'),
  requestId?: string(name='requestId', description='唯一的请求标识符，用于问题追踪', example='F8A0F5F3-0C3E-4C82-9D4F-5E4B6A7C8D9E'),
}

model AgentRuntimeVersion {
  agentRuntimeArn?: string(name='agentRuntimeArn', description='智能体运行时的ARN', example='acs:agentrun:cn-hangzhou:1760720386195983:runtimes/7a1b6d39-9f8f-4ce2-b9c9-6db1b0b9e169'),
  agentRuntimeId?: string(name='agentRuntimeId', description='智能体运行时的ID', example='ar-1234567890abcdef'),
  agentRuntimeName?: string(name='agentRuntimeName', description='智能体运行时的名称', example='my-agent-runtime'),
  agentRuntimeVersion?: string(name='agentRuntimeVersion', description='已发布版本的版本号', example='LATEST'),
  description?: string(name='description', description='此版本的描述', example='Initial release with basic functionality'),
  lastUpdatedAt?: string(name='lastUpdatedAt', description='最后更新的时间戳', example='2025-01-10T10:30:00Z'),
}

model AgentRuntimeVersionResult {
  code?: string(name='code', description='SUCCESS 为成功，失败情况返回对应错误类型，比如 ERR_BAD_REQUEST ERR_VALIDATION_FAILED ERR_INTERNAL_SERVER_ERROR'),
  data?: AgentRuntimeVersion(name='data', description='智能体运行时版本的详细信息'),
  requestId?: string(name='requestId', description='唯一的请求标识符，用于问题追踪'),
}

model AiFallbackConfig {
  serviceConfigs?: [
    AiFallbackServiceConfig
  ](name='serviceConfigs'),
}

model AiFallbackServiceConfig {
  passThroughModelName?: boolean(name='passThroughModelName'),
  serviceId?: string(name='serviceId'),
  targetModelName?: string(name='targetModelName'),
}

model AiServiceConfig {
  address?: string(name='address'),
  apiKeys?: [ string ](name='apiKeys'),
  enableHealthCheck?: boolean(name='enableHealthCheck'),
  protocols?: [ string ](name='protocols'),
  provider?: string(name='provider'),
}

model ApigLLMModel {
  address?: string(name='address'),
  apiKey?: string(name='apiKey'),
  createdTime?: string(name='createdTime'),
  desc?: string(name='desc'),
  gatewayId?: string(name='gatewayId'),
  modelId?: string(name='modelId'),
  models?: string(name='models'),
  modelsWeight?: string(name='modelsWeight'),
  name?: string(name='name'),
  provider?: string(name='provider'),
  targetId?: string(name='targetId'),
  tenantId?: string(name='tenantId'),
  type?: string(name='type'),
  updateTime?: string(name='updateTime'),
}

model ArmsConfiguration {
  armsLicenseKey?: string(name='armsLicenseKey', description='应用实时监控服务（ARMS）的许可证密钥', example='arms-license-key-123456'),
  enableArms?: boolean(name='enableArms', description='是否启用应用实时监控服务（ARMS）', example='true'),
}

model AttachPolicyConfig {
  className?: string(name='className'),
  config?: string(name='config'),
  name?: string(name='name'),
}

model Browser {
  browserId?: string(name='browserId', example='browser-1234567890abcdef'),
  browserName?: string(name='browserName', example='my-browser'),
  cpu?: float(name='cpu', example='2.0'),
  createdAt?: string(name='createdAt', example='2025-01-10T10:30:00Z'),
  credentialId?: string(name='credentialId', example='cred-1234567890abcdef'),
  description?: string(name='description', example='Web automation browser for testing'),
  executionRoleArn?: string(name='executionRoleArn', example='acs:ram::1760720386195983:role/BrowserExecutionRole'),
  lastUpdatedAt?: string(name='lastUpdatedAt', example='2025-01-10T11:45:00Z'),
  memory?: int32(name='memory', description='内存资源配置（单位：MB）', example='2048'),
  networkConfiguration?: NetworkConfiguration(name='networkConfiguration'),
  recording?: BrowserRecordingConfiguration(name='recording'),
  status?: string(name='status', example='READY'),
  statusReason?: string(name='statusReason', description='当前状态的原因说明（如适用）', example='Browser is ready for use'),
  tenantId?: string(name='tenantId', example='tenant-1234567890abcdef'),
}

model BrowserAutomationStream {
  streamEndpoint?: string(name='streamEndpoint', example='wss://browser-automation.cn-hangzhou.agentrun.aliyuncs.com/stream/bs-1234567890abcdef'),
  streamStatus?: string(name='streamStatus', example='ACTIVE'),
}

model BrowserConfiguration {
  browserType?: string(name='browserType'),
  enableExtension?: [ string ](name='enableExtension', description='要启用的浏览器扩展列表'),
  headless?: boolean(name='headless', description='是否以无头模式运行浏览器'),
  userAgent?: string(name='userAgent', description='浏览器用户代理字符串'),
  viewPort?: ViewPortConfiguration(name='viewPort'),
}

model BrowserLiveViewStream {
  streamEndpoint?: string(name='streamEndpoint', example='wss://browser-liveview.cn-hangzhou.agentrun.aliyuncs.com/stream/bs-1234567890abcdef'),
}

model BrowserOssLocation {
  bucket?: string(name='bucket'),
  prefix?: string(name='prefix'),
}

model BrowserRecordingConfiguration {
  enabled?: boolean(name='enabled'),
  ossLocation?: BrowserOssLocation(name='ossLocation'),
}

model BrowserResult {
  code?: string(name='code', description='SUCCESS 为成功，失败情况返回对应错误类型，比如 ERR_BAD_REQUEST ERR_VALIDATION_FAILED ERR_INTERNAL_SERVER_ERROR', example='SUCCESS'),
  data?: Browser(name='data', description='浏览器的详细信息', example='{}'),
  requestId?: string(name='requestId', description='唯一的请求标识符，用于问题追踪', example='F8A0F5F3-0C3E-4C82-9D4F-5E4B6A7C8D9E'),
}

model BrowserSessionListOut {
  items?: [
    BrowserSessionOut
  ](name='items', example='[]'),
  pageNumber?: int32(name='pageNumber', example='1'),
  pageSize?: int32(name='pageSize', example='20'),
  total?: long(name='total', example='100'),
}

model BrowserSessionOut {
  browserId?: string(name='browserId', example='browser-1234567890abcdef'),
  browserName?: string(name='browserName', example='my-browser-session'),
  createdAt?: string(name='createdAt', example='2025-01-10T10:30:00Z'),
  lastUpdatedAt?: string(name='lastUpdatedAt', example='2025-01-10T11:45:00Z'),
  sessionId?: string(name='sessionId', description='This parameter is required.', example='bs-1234567890abcdef'),
  sessionIdleTimeoutSeconds?: int32(name='sessionIdleTimeoutSeconds', description='会话空闲超时时间，单位为秒', example='3600'),
  status?: string(name='status', example='RUNNING'),
}

model BrowserStreams {
  automationStream?: BrowserAutomationStream(name='automationStream', example='{}'),
  liveViewStream?: BrowserLiveViewStream(name='liveViewStream', example='{}'),
}

model BrowserViewPort {
  height?: double(name='height', example='1080'),
  width?: double(name='width', example='1920'),
}

model CAPConfig {
  functionName?: string(name='functionName'),
  name?: string(name='name'),
  templateId?: int32(name='templateId'),
}

model CertConfig {
  certName?: string(name='certName', description='证书的名称。支持字母、数字、下划线（_）和短划线（-），不能以数字和短划线（-）开头。长度范围为 1~128 个字符。', example='my-cert'),
  certificate?: string(name='certificate', description='证书，如果是证书链，则需要依次填写多个证书。', example='-----BEGIN CERTIFICATE-----\\nxxxxx\\n-----END CERTIFICATE-----'),
  privateKey?: string(name='privateKey', description='私钥。', example='-----BEGIN RSA PRIVATE KEY-----\\nxxxxx\\n-----END RSA PRIVATE KEY-----'),
}

model CodeConfiguration {
  checksum?: string(name='checksum', description='代码包的CRC-64校验值。如果提供了checksum，则函数计算会校验代码包的checksum是否和提供的一致', example='1234567890123456789'),
  command?: [ string ](name='command', description='在运行时中运行的命令（例如：[\\"python\\"]）', example='python,main.py'),
  language?: string(name='language', description='代码运行时的编程语言，如 python3、nodejs 等', example='python3.12'),
  ossBucketName?: string(name='ossBucketName', example='my-agent-code-bucket'),
  ossObjectName?: string(name='ossObjectName', example='agent-code-v1.0.zip'),
  zipFile?: string(name='zipFile', description='智能体代码ZIP包的Base64编码', example='UEsDBAoAAAAAANF'),
}

model CodeInfo {
  ossBucketName?: string(name='ossBucketName'),
  ossObjectName?: string(name='ossObjectName'),
}

model CodeInterpreter {
  codeInterpreterId?: string(name='codeInterpreterId', description='代码解释器的唯一标识符', example='ci-1234567890abcdef'),
  codeInterpreterName?: string(name='codeInterpreterName', description='代码解释器的名称，用于标识和区分不同的代码解释器实例', example='my-code-interpreter'),
  cpu?: float(name='cpu', example='2.0'),
  createdAt?: string(name='createdAt', description='代码解释器的创建时间，采用ISO 8601格式', example='2025-01-10T10:30:00Z'),
  description?: string(name='description', description='代码解释器的描述信息，说明该解释器的用途和功能', example='Python code interpreter for data analysis'),
  executionRoleArn?: string(name='executionRoleArn', description='此代码解释器的执行角色', example='acs:ram::1760720386195983:role/CodeInterpreterExecutionRole'),
  lastUpdatedAt?: string(name='lastUpdatedAt', description='代码解释器的最后更新时间，采用ISO 8601格式', example='2025-01-10T11:45:00Z'),
  memory?: int32(name='memory', description='内存资源配置（单位：MB）', example='2048'),
  networkConfiguration?: NetworkConfiguration(name='networkConfiguration', description='代码解释器的网络配置信息'),
  status?: string(name='status', description='代码解释器的当前状态，如READY（就绪）、TERMINATED（已终止）等', example='READY'),
  statusReason?: string(name='statusReason', description='当前状态的原因说明（如适用）', example='Code interpreter is ready for use'),
  tenantId?: string(name='tenantId', example='tenant-1234567890abcdef'),
}

model CodeInterpreterResult {
  code?: string(name='code', description='SUCCESS 为成功，失败情况返回对应错误类型，比如 ERR_BAD_REQUEST ERR_VALIDATION_FAILED ERR_INTERNAL_SERVER_ERROR', example='SUCCESS'),
  data?: CodeInterpreter(name='data', description='代码解释器的详细信息', example='{}'),
  requestId?: string(name='requestId', description='唯一的请求标识符，用于问题追踪', example='F8A0F5F3-0C3E-4C82-9D4F-5E4B6A7C8D9E'),
}

model CodeInterpreterSessionConfig {
  environment?: map[string]string(name='environment', description='代码解释器会话的环境变量配置', example='PYTHONPATH=/app,DEBUG=true'),
  timeout?: int32(name='timeout', description='代码解释器会话的超时时间，单位为秒', example='3600'),
  workingDirectory?: string(name='workingDirectory', description='代码解释器会话的工作目录路径', example='/app'),
}

model CodeInterpreterSessionListOut {
  items?: [
    CodeInterpreterSessionOut
  ](name='items', example='[]'),
  pageNumber?: int32(name='pageNumber', example='1'),
  pageSize?: int32(name='pageSize', example='20'),
  total?: long(name='total', example='100'),
}

model CodeInterpreterSessionOut {
  codeInterpreterId?: string(name='codeInterpreterId', description='关联的代码解释器的唯一标识符

This parameter is required.'),
  codeInterpreterName?: string(name='codeInterpreterName', description='代码解释器会话的名称'),
  createdAt?: string(name='createdAt', description='代码解释器会话的创建时间，采用ISO 8601格式'),
  lastUpdatedAt?: string(name='lastUpdatedAt', description='代码解释器会话的最后更新时间，采用ISO 8601格式'),
  sessionId?: string(name='sessionId', description='代码解释器会话的唯一标识符

This parameter is required.'),
  sessionIdleTimeoutSeconds?: int32(name='sessionIdleTimeoutSeconds', description='代码解释器会话的空闲超时时间，单位为秒。实例没有会话请求后处于空闲状态，空闲态为闲置计费模式，超过此超时时间后会话自动过期，不可继续使用'),
  status?: string(name='status', description='代码解释器会话的当前状态

This parameter is required.'),
}

model CommonResult {
  code?: string(name='code', description='SUCCESS 为成功，失败情况返回对应错误类型，比如 ERR_BAD_REQUEST ERR_VALIDATION_FAILED ERR_INTERNAL_SERVER_ERROR'),
  data?: any(name='data', description='实际的业务数据内容'),
  requestId?: string(name='requestId', description='唯一的请求标识符，用于问题追踪'),
}

model ContainerConfiguration {
  acrInstanceId?: string(name='acrInstanceId', description='阿里云容器镜像服务（ACR）的实例ID或名称', example='cri-xxxxx'),
  command?: [ string ](name='command', description='在容器中运行的命令（例如：[\\"python3\\", \\"app.py\\"]）', example='python3,app.py'),
  image?: string(name='image', example='registry.cn-hangzhou.aliyuncs.com/my-namespace/agent-runtime:latest'),
  imageRegistryType?: string(name='imageRegistryType', description='容器镜像的来源类型，支持ACR（阿里云容器镜像服务）、ACREE（阿里云容器镜像服务企业版）、CUSTOM（自定义镜像仓库）', example='ACR'),
  port?: int32(name='port'),
}

model CreateAgentRuntimeEndpointInput {
  agentRuntimeEndpointName?: string(name='agentRuntimeEndpointName', example='production-endpoint'),
  description?: string(name='description', example='Production endpoint for customer service agent'),
  routingConfiguration?: RoutingConfiguration(name='routingConfiguration', description='智能体运行时端点的路由配置，支持多版本权重分配', example='{}'),
  targetVersion?: string(name='targetVersion', description='智能体运行时的目标版本', example='LATEST'),
}

model CreateAgentRuntimeInput {
  agentRuntimeName?: string(name='agentRuntimeName', description='智能体运行时的唯一标识名称，用于区分不同的智能体运行时实例

This parameter is required.', example='my-agent-runtime'),
  artifactType?: string(name='artifactType', description='指定智能体运行时的部署类型，支持Code（代码模式）和Container（容器模式）

This parameter is required.', example='Code'),
  codeConfiguration?: CodeConfiguration(name='codeConfiguration', description='当artifactType为Code时的代码配置信息，包括代码源、入口文件等', example='{}'),
  containerConfiguration?: ContainerConfiguration(name='containerConfiguration', description='当artifactType为Container时的容器配置信息，包括镜像地址、启动命令等', example='{}'),
  cpu?: float(name='cpu', description='为智能体运行时分配的CPU资源，单位为核数

This parameter is required.', example='2.0'),
  credentialId?: string(name='credentialId', description='用于访问外部服务的凭证ID，智能体运行时将使用此凭证进行身份验证', example='cred-1234567890abcdef', deprecated=true),
  credentialName?: string(name='credentialName', description='用于访问智能体的凭证名称，访问智能体运行时将使用此凭证进行身份验证', example='my-credential'),
  description?: string(name='description', description='智能体运行时的描述信息，用于说明该运行时的用途和功能', example='AI agent runtime for customer service automation'),
  environmentVariables?: map[string]string(name='environmentVariables', description='智能体运行时的环境变量配置，用于在运行时传递配置参数', example='ENV_VAR1=value1,ENV_VAR2=value2'),
  executionRoleArn?: string(name='executionRoleArn', description='为智能体运行时提供访问云服务权限的执行角色ARN', example='acs:ram::1760720386195983:role/AgentRunExecutionRole'),
  healthCheckConfiguration?: HealthCheckConfiguration(name='healthCheckConfiguration', description='智能体运行时的健康检查配置，用于监控运行时实例的健康状态', example='{}'),
  logConfiguration?: LogConfiguration(name='logConfiguration', description='SLS（简单日志服务）配置', example='{}'),
  memory?: int32(name='memory', description='为智能体运行时分配的内存资源，单位为MB

This parameter is required.', example='2048'),
  networkConfiguration?: NetworkConfiguration(name='networkConfiguration', description='智能体运行时的网络配置，包括VPC、安全组等网络访问设置

This parameter is required.', example='{}'),
  port?: int32(name='port', description='智能体运行时监听的端口号，用于接收外部请求

This parameter is required.', example='8080'),
  protocolConfiguration?: ProtocolConfiguration(name='protocolConfiguration', description='智能体运行时的通信协议配置，定义运行时如何与外部系统交互', example='{}'),
  sessionConcurrencyLimitPerInstance?: int32(name='sessionConcurrencyLimitPerInstance', description='每个运行时实例允许的最大并发会话数', example='100'),
  sessionIdleTimeoutSeconds?: int32(name='sessionIdleTimeoutSeconds', description='会话的空闲超时时间，单位为秒。实例没有会话请求后处于空闲状态，空闲态为闲置计费模式，超过此超时时间后会话自动过期，不可继续使用', example='3600'),
}

model CreateAgentRuntimeVersionInput {
  description?: string(name='description', description='版本描述', example='Version 1.0.0 release with new features'),
}

model CreateApigLLMModelInput {
  address?: string(name='address'),
  apiKey?: string(name='apiKey'),
  desc?: string(name='desc'),
  models?: [ string ](name='models'),
  name?: string(name='name'),
  provider?: string(name='provider'),
  type?: string(name='type'),
}

model CreateBrowserInput {
  browserName?: string(name='browserName', description='This parameter is required.', example='my-browser'),
  cpu?: float(name='cpu', description='CPU资源配置（单位：核）

This parameter is required.', example='2.0'),
  credentialId?: string(name='credentialId', example='cred-1234567890abcdef'),
  description?: string(name='description', example='Web automation browser for testing'),
  executionRoleArn?: string(name='executionRoleArn', example='acs:ram::1760720386195983:role/BrowserExecutionRole'),
  memory?: int32(name='memory', description='内存资源配置（单位：MB）

This parameter is required.', example='2048'),
  networkConfiguration?: NetworkConfiguration(name='networkConfiguration', description='This parameter is required.'),
  sessionIdleTimeoutSeconds?: int32(name='sessionIdleTimeoutSeconds', description='会话的空闲超时时间，单位为秒。实例没有会话请求后处于空闲状态，空闲态为闲置计费模式，超过此超时时间后会话自动过期，不可继续使用', example='3600'),
}

model CreateCodeInterpreterInput {
  codeInterpreterName?: string(name='codeInterpreterName', description='代码解释器的名称，用于标识和区分不同的代码解释器实例

This parameter is required.', example='my-code-interpreter'),
  cpu?: float(name='cpu', description='CPU资源配置（单位：核数）

This parameter is required.', example='2.0'),
  credentialId?: string(name='credentialId', example='cred-1234567890abcdef'),
  description?: string(name='description', description='代码解释器的描述信息，说明该解释器的用途和功能', example='Python code interpreter for data analysis'),
  executionRoleArn?: string(name='executionRoleArn', description='此代码解释器的执行角色', example='acs:ram::1760720386195983:role/CodeInterpreterExecutionRole'),
  memory?: int32(name='memory', description='内存资源配置（单位：MB）

This parameter is required.', example='1024'),
  networkConfiguration?: NetworkConfiguration(name='networkConfiguration', description='代码解释器的网络配置，包括VPC、安全组等网络访问设置

This parameter is required.', example='{}'),
  sessionIdleTimeoutSeconds?: int32(name='sessionIdleTimeoutSeconds', description='会话的空闲超时时间，单位为秒。实例没有会话请求后处于空闲状态，空闲态为闲置计费模式，超过此超时时间后会话自动过期，不可继续使用', example='3600'),
}

model CreateCredentialInput {
  credentialAuthType?: string(name='credentialAuthType', description='This parameter is required.'),
  credentialName?: string(name='credentialName', description='This parameter is required.'),
  credentialPublicConfig?: CredentialPublicConfig(name='credentialPublicConfig'),
  credentialSecret?: string(name='credentialSecret'),
  credentialSourceType?: string(name='credentialSourceType', description='This parameter is required.'),
  description?: string(name='description'),
  enabled?: boolean(name='enabled'),
}

model CreateCredentialOutput {
  createdAt?: string(name='createdAt'),
  credentialAuthType?: string(name='credentialAuthType'),
  credentialId?: string(name='credentialId'),
  credentialName?: string(name='credentialName'),
  credentialPublicConfig?: map[string]string(name='credentialPublicConfig'),
  credentialSecret?: string(name='credentialSecret'),
  credentialSourceType?: string(name='credentialSourceType'),
  description?: string(name='description'),
  enabled?: boolean(name='enabled'),
  relatedResources?: [
    RelatedResource
  ](name='relatedResources'),
  updatedAt?: string(name='updatedAt'),
}

model CreateCustomDomainInput {
  certConfig?: CertConfig(name='certConfig', description='HTTPS 证书的信息。', example='{}'),
  description?: string(name='description', description='描述'),
  domainName?: string(name='domainName', description='域名。填写已在阿里云备案或接入备案的自定义域名名称。

This parameter is required.', example='example.com'),
  domainType?: string(name='domainType'),
  protocol?: string(name='protocol', description='域名支持的协议类型：● HTTP：仅支持 HTTP 协议。● HTTPS：仅支持 HTTPS 协议。● HTTP,HTTPS：支持 HTTP 及 HTTPS 协议。

This parameter is required.', example='HTTP'),
  routeConfig?: RouteConfig(name='routeConfig', description='路由表：自定义域名访问时的 PATH 到 资源 的映射。', example='{}'),
  tlsConfig?: TLSConfig(name='tlsConfig', description='TLS 配置信息。', example='{}'),
}

model CreateDomainInput {
  certIdentifier?: string(name='certIdentifier'),
  name?: string(name='name'),
  protocol?: string(name='protocol'),
}

model CreateGatewayInput {
  identityId?: string(name='identityId'),
  name?: string(name='name'),
  networkConfiguration?: GatewayNetworkConfiguration(name='networkConfiguration'),
  type?: string(name='type'),
}

model CreateGatewayTargetInput {
  domainId?: string(name='domainId'),
  name?: string(name='name'),
  targetConfiguration?: TargetConfiguration(name='targetConfiguration'),
}

model CreateKnowledgeBaseInput {
  credentialName?: string(name='credentialName'),
  description?: string(name='description'),
  knowledgeBaseName?: string(name='knowledgeBaseName', description='This parameter is required.'),
  provider?: string(name='provider', description='This parameter is required.'),
  providerSettings?: map[string]any(name='providerSettings', description='This parameter is required.'),
  retrieveSettings?: map[string]any(name='retrieveSettings'),
}

model CreateMemoryCollectionInput {
  description?: string(name='description'),
  embedderConfig?: EmbedderConfig(name='embedderConfig'),
  executionRoleArn?: string(name='executionRoleArn'),
  llmConfig?: LLMConfig(name='llmConfig'),
  memoryCollectionName?: string(name='memoryCollectionName'),
  networkConfiguration?: NetworkConfiguration(name='networkConfiguration'),
  type?: string(name='type'),
  vectorStoreConfig?: VectorStoreConfig(name='vectorStoreConfig'),
}

model CreateModelInput {
  address?: string(name='address'),
  apiKey?: string(name='apiKey'),
  desc?: string(name='desc'),
  models?: [ string ](name='models'),
  name?: string(name='name'),
  provider?: string(name='provider'),
  type?: string(name='type'),
}

model CreateModelProxyInput {
  armsConfiguration?: ArmsConfiguration(name='armsConfiguration'),
  cpu?: float(name='cpu', description='This parameter is required.'),
  credentialName?: string(name='credentialName'),
  description?: string(name='description'),
  executionRoleArn?: string(name='executionRoleArn'),
  litellmVersion?: string(name='litellmVersion'),
  logConfiguration?: LogConfiguration(name='logConfiguration'),
  memory?: int32(name='memory', description='This parameter is required.'),
  modelProxyName?: string(name='modelProxyName', description='This parameter is required.'),
  modelType?: string(name='modelType'),
  networkConfiguration?: NetworkConfiguration(name='networkConfiguration'),
  proxyConfig?: ProxyConfig(name='proxyConfig', description='This parameter is required.'),
  proxyMode?: string(name='proxyMode', description='This parameter is required.'),
  serviceRegionId?: string(name='serviceRegionId'),
}

model CreateModelServiceInput {
  credentialName?: string(name='credentialName'),
  description?: string(name='description'),
  modelInfoConfigs?: [
    ModelInfoConfig
  ](name='modelInfoConfigs'),
  modelServiceName?: string(name='modelServiceName', description='This parameter is required.'),
  modelType?: string(name='modelType', description='This parameter is required.'),
  networkConfiguration?: NetworkConfiguration(name='networkConfiguration'),
  provider?: string(name='provider', description='This parameter is required.'),
  providerSettings?: ProviderSettings(name='providerSettings', description='This parameter is required.'),
}

model CreateSandboxInput {
  nasConfig?: NASConfig(name='nasConfig'),
  ossMountConfig?: OSSMountConfig(name='ossMountConfig'),
  sandboxId?: string(name='sandboxId'),
  sandboxIdleTimeoutSeconds?: int32(name='sandboxIdleTimeoutSeconds', description='沙箱空闲超时时间（秒）'),
  templateName?: string(name='templateName', description='模板名称（系统内部通过 templateName 查询 template_id）

This parameter is required.'),
}

model CreateTemplateInput {
  allowAnonymousManage?: boolean(name='allowAnonymousManage', nullable=true),
  armsConfiguration?: ArmsConfiguration(name='armsConfiguration'),
  containerConfiguration?: ContainerConfiguration(name='containerConfiguration', description='容器配置，只允许基于 Browser/Code Interpreter 基础镜像的 image'),
  cpu?: float(name='cpu', description='CPU资源配置（单位：核心）

This parameter is required.'),
  credentialConfiguration?: CredentialConfiguration(name='credentialConfiguration'),
  description?: string(name='description'),
  diskSize?: int32(name='diskSize'),
  environmentVariables?: map[string]string(name='environmentVariables'),
  executionRoleArn?: string(name='executionRoleArn'),
  logConfiguration?: LogConfiguration(name='logConfiguration'),
  memory?: int32(name='memory', description='内存资源配置（单位：MB）

This parameter is required.'),
  networkConfiguration?: NetworkConfiguration(name='networkConfiguration', description='This parameter is required.'),
  ossConfiguration?: [
    OssConfiguration
  ](name='ossConfiguration'),
  sandboxIdleTimeoutInSeconds?: int32(name='sandboxIdleTimeoutInSeconds', description='沙箱空闲超时时间（秒）'),
  sandboxTTLInSeconds?: int32(name='sandboxTTLInSeconds', description='沙箱存活时间（秒）', deprecated=true),
  templateConfiguration?: map[string]any(name='templateConfiguration', description='模板配置（灵活的对象结构，根据 templateType 不同而不同）'),
  templateName?: string(name='templateName', description='模板名称（要求账号唯一的）

This parameter is required.'),
  templateType?: string(name='templateType', description='This parameter is required.'),
}

model CreateToolData {
  createdAt?: string(name='createdAt'),
  id?: string(name='id'),
  name?: string(name='name'),
  sourceType?: string(name='sourceType'),
  toolType?: string(name='toolType'),
}

model CreateToolInput {
  CAPConfig?: CAPConfig(name='CAPConfig'),
  description?: string(name='description'),
  name?: string(name='name', description='This parameter is required.'),
  schema?: string(name='schema', description='This parameter is required.'),
  sourceType?: string(name='sourceType', description='This parameter is required.'),
  toolType?: string(name='toolType', description='This parameter is required.'),
}

model CreateToolOutput {
  data?: CreateToolData(name='data'),
  message?: string(name='message'),
  success?: boolean(name='success'),
}

model Credential {
  createdAt?: string(name='createdAt'),
  credentialAuthType?: string(name='credentialAuthType'),
  credentialId?: string(name='credentialId'),
  credentialName?: string(name='credentialName'),
  credentialPublicConfig?: map[string]string(name='credentialPublicConfig'),
  credentialSecret?: string(name='credentialSecret'),
  credentialSourceType?: string(name='credentialSourceType'),
  description?: string(name='description'),
  enabled?: boolean(name='enabled'),
  relatedResources?: [
    RelatedResource
  ](name='relatedResources'),
  updatedAt?: string(name='updatedAt'),
}

model CredentialConfiguration {
  credentialName?: string(name='credentialName', description='凭证的唯一标识符'),
}

model CredentialListItem {
  createdAt?: string(name='createdAt'),
  credentialAuthType?: string(name='credentialAuthType'),
  credentialId?: string(name='credentialId'),
  credentialName?: string(name='credentialName'),
  credentialSourceType?: string(name='credentialSourceType'),
  enabled?: boolean(name='enabled'),
  relatedResourceCount?: int32(name='relatedResourceCount'),
  updatedAt?: string(name='updatedAt'),
}

model CredentialPublicConfig {
  authConfig?: map[string]string(name='authConfig'),
  authType?: string(name='authType'),
  headerKey?: string(name='headerKey'),
  provider?: string(name='provider'),
  remoteConfig?: {
    timeout?: int32(name='timeout'),
    ttl?: int32(name='ttl'),
    uri?: string(name='uri'),
  }(name='remoteConfig'),
  users?: [ 
    {
      password?: string(name='password'),
      username?: string(name='username'),
    }
  ](name='users'),
}

model CredentialResult {
  code?: string(name='code'),
  data?: Credential(name='data'),
  requestId?: string(name='requestId'),
}

model CustomDomain {
  certConfig?: CertConfig(name='certConfig', description='HTTPS 证书的信息。', example='{}'),
  createdAt?: string(name='createdAt', description='创建时间', example='2025-01-10T10:30:00Z'),
  description?: string(name='description', description='描述'),
  domainName?: string(name='domainName', description='域名。填写已在阿里云备案或接入备案的自定义域名名称。', example='example.com'),
  domainType?: string(name='domainType'),
  protocol?: string(name='protocol', description='域名支持的协议类型：● HTTP：仅支持 HTTP 协议。● HTTPS：仅支持 HTTPS 协议。● HTTP,HTTPS：支持 HTTP 及 HTTPS 协议。', example='HTTP'),
  routeConfig?: RouteConfig(name='routeConfig', description='路由表：自定义域名访问时的 PATH 到 资源 的映射。', example='{}'),
  tlsConfig?: TLSConfig(name='tlsConfig', description='TLS 配置信息。', example='{}'),
  updatedAt?: string(name='updatedAt', description='更新时间', example='2025-01-10T11:45:00Z'),
}

model CustomDomainResult {
  code?: string(name='code', description='SUCCESS 为成功，失败情况返回对应错误类型，比如 ERR_BAD_REQUEST ERR_VALIDATION_FAILED ERR_INTERNAL_SERVER_ERROR', example='SUCCESS'),
  data?: CustomDomain(name='data', description='自定义域名的详细信息', example='{}'),
  requestId?: string(name='requestId', description='唯一的请求标识符，用于问题追踪', example='F8A0F5F3-0C3E-4C82-9D4F-5E4B6A7C8D9E'),
}

model CustomRuntimeConfig {
  args?: [ string ](name='args'),
  command?: [ string ](name='command'),
  port?: int32(name='port'),
}

model DeleteBrowserOut {
  browserId?: string(name='browserId'),
  browserName?: string(name='browserName'),
  status?: string(name='status'),
}

model DeleteBrowserResult {
  code?: string(name='code', description='SUCCESS 为成功，失败情况返回对应错误类型，比如 ERR_BAD_REQUEST ERR_VALIDATION_FAILED ERR_INTERNAL_SERVER_ERROR', example='SUCCESS'),
  data?: Browser(name='data', description='被删除的浏览器详细信息', example='{}'),
  requestId?: string(name='requestId', description='唯一的请求标识符，用于问题追踪', example='F8A0F5F3-0C3E-4C82-9D4F-5E4B6A7C8D9E'),
}

model DeleteCodeInterpreterOut {
  codeInterpreterId?: string(name='codeInterpreterId'),
  codeInterpreterName?: string(name='codeInterpreterName'),
  status?: string(name='status'),
}

model DeleteCodeInterpreterResult {
  code?: string(name='code', description='SUCCESS 为成功，失败情况返回对应错误类型，比如 ERR_BAD_REQUEST ERR_VALIDATION_FAILED ERR_INTERNAL_SERVER_ERROR', example='SUCCESS'),
  data?: CodeInterpreter(name='data', description='被删除的代码解释器详细信息', example='{}'),
  requestId?: string(name='requestId', description='唯一的请求标识符，用于问题追踪', example='F8A0F5F3-0C3E-4C82-9D4F-5E4B6A7C8D9E'),
}

model DeleteKnowledgeBaseResult {
  code?: string(name='code'),
  data?: KnowledgeBase(name='data'),
  requestId?: string(name='requestId'),
}

model DeleteModelProxyResult {
  code?: string(name='code'),
  data?: ModelProxy(name='data'),
  requestId?: string(name='requestId'),
}

model DeleteModelServiceResult {
  code?: string(name='code'),
  data?: ModelService(name='data'),
  requestId?: string(name='requestId'),
}

model DeleteSandboxResult {
  code?: string(name='code', description='SUCCESS 为成功'),
  data?: Sandbox(name='data'),
  requestId?: string(name='requestId', description='唯一的请求标识符，用于问题追踪'),
}

model DeleteTemplateResult {
  code?: string(name='code', description='SUCCESS 为成功'),
  data?: Template(name='data'),
  requestId?: string(name='requestId', description='唯一的请求标识符，用于问题追踪'),
}

model DeregisterServiceInput {
  serviceName?: string(name='serviceName', description='要注销的服务名称（UUID格式）

This parameter is required.', example='12345678-1234-1234-1234-123456789abc'),
}

model DomainInfo {
  certIdentifier?: string(name='certIdentifier'),
  domainId?: string(name='domainId'),
  name?: string(name='name'),
  protocol?: string(name='protocol'),
}

model EmbedderConfig {
  config?: {
    model?: string(name='model'),
  }(name='config'),
  modelServiceName?: string(name='modelServiceName'),
}

model ErrorResult {
  code?: string(name='code', description='SUCCESS 为成功，失败情况返回对应错误类型，比如 ERR_BAD_REQUEST ERR_VALIDATION_FAILED ERR_INTERNAL_SERVER_ERROR'),
  message?: string(name='message', description='错误信息描述'),
  requestId?: string(name='requestId', description='唯一的请求标识符，用于问题追踪'),
}

model FCLinkConfig {
  functionName?: string(name='functionName'),
  version?: string(name='version'),
}

model Gateway {
  createdAt?: string(name='createdAt'),
  gatewayId?: string(name='gatewayId'),
  internetUrl?: string(name='internetUrl'),
  intranetUrl?: string(name='intranetUrl'),
  name?: string(name='name'),
  status?: string(name='status'),
  updatedAt?: string(name='updatedAt'),
}

model GatewayNetworkConfiguration {
  networkMode?: string(name='networkMode'),
  vpcId?: string(name='vpcId'),
  vswitchIds?: [ string ](name='vswitchIds'),
}

model GetBrowserSessionResult {
  code?: string(name='code', description='SUCCESS 为成功，失败情况返回对应错误类型，比如 ERR_BAD_REQUEST ERR_VALIDATION_FAILED ERR_INTERNAL_SERVER_ERROR', example='SUCCESS'),
  data?: BrowserSessionOut(name='data', example='{}'),
  requestId?: string(name='requestId', example='F8A0F5F3-0C3E-4C82-9D4F-5E4B6A7C8D9E'),
}

model GetCodeInterpreterSessionResult {
  code?: string(name='code', description='SUCCESS 为成功，失败情况返回对应错误类型，比如 ERR_BAD_REQUEST ERR_VALIDATION_FAILED ERR_INTERNAL_SERVER_ERROR'),
  data?: CodeInterpreterSessionOut(name='data'),
  requestId?: string(name='requestId'),
}

model GetCredentialOutput {
  createdAt?: string(name='createdAt'),
  credentialAuthType?: string(name='credentialAuthType'),
  credentialId?: string(name='credentialId'),
  credentialName?: string(name='credentialName'),
  credentialPublicConfig?: map[string]string(name='credentialPublicConfig'),
  credentialSecret?: string(name='credentialSecret'),
  credentialSourceType?: string(name='credentialSourceType'),
  description?: string(name='description'),
  enabled?: boolean(name='enabled'),
  relatedResources?: [
    RelatedResource
  ](name='relatedResources'),
  updatedAt?: string(name='updatedAt'),
}

model GetToolOutput {
  data?: ToolInfo(name='data'),
  success?: boolean(name='success'),
}

model GrayTrafficWeight {
  version?: string(name='version', description='灰度版本号'),
  weight?: float(name='weight', description='流量权重比例（0.0-1.0）'),
}

model HealthCheckConfig {
  failureThreshold?: int32(name='failureThreshold', description='在将容器视为不健康之前，连续失败的健康检查次数'),
  httpGetUrl?: string(name='httpGetUrl', description='用于健康检查的HTTP GET请求的URL地址'),
  initialDelaySeconds?: int32(name='initialDelaySeconds', description='在容器启动后，首次执行健康检查前的延迟时间（秒）'),
  periodSeconds?: int32(name='periodSeconds', description='执行健康检查的时间间隔（秒）'),
  successThreshold?: int32(name='successThreshold', description='在将容器视为健康之前，连续成功的健康检查次数'),
  timeoutSeconds?: int32(name='timeoutSeconds', description='健康检查的超时时间（秒）'),
}

model HealthCheckConfiguration {
  failureThreshold?: int32(name='failureThreshold', description='在将容器视为不健康之前，连续失败的健康检查次数', example='3'),
  httpGetUrl?: string(name='httpGetUrl', description='用于健康检查的HTTP GET请求的URL地址', example='/ready'),
  initialDelaySeconds?: int32(name='initialDelaySeconds', description='在容器启动后，首次执行健康检查前的延迟时间（秒）', example='30'),
  periodSeconds?: int32(name='periodSeconds', description='执行健康检查的时间间隔（秒）', example='30'),
  successThreshold?: int32(name='successThreshold', description='在将容器视为健康之前，连续成功的健康检查次数', example='1'),
  timeoutSeconds?: int32(name='timeoutSeconds', description='健康检查的超时时间（秒）', example='3'),
}

model KnowledgeBase {
  createdAt?: string(name='createdAt'),
  credentialName?: string(name='credentialName'),
  description?: string(name='description'),
  knowledgeBaseId?: string(name='knowledgeBaseId'),
  knowledgeBaseName?: string(name='knowledgeBaseName'),
  lastUpdatedAt?: string(name='lastUpdatedAt'),
  provider?: string(name='provider'),
  providerSettings?: map[string]any(name='providerSettings'),
  retrieveSettings?: map[string]any(name='retrieveSettings'),
}

model KnowledgeBaseResult {
  code?: string(name='code'),
  data?: KnowledgeBase(name='data'),
  requestId?: string(name='requestId'),
}

model LLMAPIConfiguration {
  aiProtocols?: [ string ](name='aiProtocols'),
  attachPolicyConfigs?: [
    AttachPolicyConfig
  ](name='attachPolicyConfigs'),
  basePath?: string(name='basePath'),
  deployConfigs?: [
    LLMDeployConfig
  ](name='deployConfigs'),
  modelCategory?: string(name='modelCategory'),
  removeBasePathOnForward?: boolean(name='removeBasePathOnForward'),
  type?: string(name='type'),
}

model LLMConfig {
  config?: {
    model?: string(name='model'),
  }(name='config'),
  modelServiceName?: string(name='modelServiceName'),
}

model LLMDeployConfig {
  autoDeploy?: boolean(name='autoDeploy'),
  backendScene?: string(name='backendScene'),
  customDomainIds?: [ string ](name='customDomainIds'),
  gatewayType?: string(name='gatewayType'),
  policyConfigs?: [
    PolicyConfig
  ](name='policyConfigs'),
  serviceConfigs?: [
    TargetServiceConfig
  ](name='serviceConfigs'),
}

model ListAgentRuntimeEndpointsInput {
  endpointName?: string(name='endpointName', description='按端点名称过滤'),
  pageNumber?: integer(name='pageNumber', description='页码'),
  pageSize?: integer(name='pageSize', description='每页记录数'),
  statuses?: [ string ](name='statuses', description='按状态过滤'),
}

model ListAgentRuntimeEndpointsOutput {
  items?: [
    AgentRuntimeEndpoint
  ](name='items'),
  pageNumber?: integer(name='pageNumber'),
  pageSize?: integer(name='pageSize'),
  total?: integer(name='total'),
}

model ListAgentRuntimeEndpointsResult {
  code?: string(name='code', description='SUCCESS 为成功，失败情况返回对应错误类型，比如 ERR_BAD_REQUEST ERR_VALIDATION_FAILED ERR_INTERNAL_SERVER_ERROR', example='SUCCESS'),
  data?: ListAgentRuntimeEndpointsOutput(name='data', description='智能体运行时端点列表的详细信息', example='{}'),
  requestId?: string(name='requestId', description='唯一的请求标识符，用于问题追踪', example='F8A0F5F3-0C3E-4C82-9D4F-5E4B6A7C8D9E'),
}

model ListAgentRuntimeVersionsInput {
  pageNumber?: integer(name='pageNumber', description='页码'),
  pageSize?: integer(name='pageSize', description='每页记录数'),
}

model ListAgentRuntimeVersionsOutput {
  items?: [
    AgentRuntimeVersion
  ](name='items'),
  pageNumber?: integer(name='pageNumber'),
  pageSize?: integer(name='pageSize'),
  total?: integer(name='total'),
}

model ListAgentRuntimeVersionsResult {
  code?: string(name='code', description='SUCCESS 为成功，失败情况返回对应错误类型，比如 ERR_BAD_REQUEST ERR_VALIDATION_FAILED ERR_INTERNAL_SERVER_ERROR', example='SUCCESS'),
  data?: ListAgentRuntimeVersionsOutput(name='data', description='智能体运行时版本列表的详细信息', example='{}'),
  requestId?: string(name='requestId', description='唯一的请求标识符，用于问题追踪', example='F8A0F5F3-0C3E-4C82-9D4F-5E4B6A7C8D9E'),
}

model ListAgentRuntimesInput {
  agentRuntimeName?: string(name='agentRuntimeName', description='按名称过滤', example='my-agent-runtime'),
  pageNumber?: integer(name='pageNumber', description='页码', example='1'),
  pageSize?: integer(name='pageSize', description='每页记录数', example='20'),
  statuses?: [ string ](name='statuses', description='按状态过滤', example='READY,CREATING'),
}

model ListAgentRuntimesOutput {
  items?: [
    AgentRuntime
  ](name='items'),
  pageNumber?: integer(name='pageNumber'),
  pageSize?: integer(name='pageSize'),
  total?: integer(name='total'),
}

model ListAgentRuntimesResult {
  code?: string(name='code', description='SUCCESS 为成功，失败情况返回对应错误类型，比如 ERR_BAD_REQUEST ERR_VALIDATION_FAILED ERR_INTERNAL_SERVER_ERROR', example='SUCCESS'),
  data?: ListAgentRuntimesOutput(name='data', description='智能体运行时列表的详细信息', example='{}'),
  requestId?: string(name='requestId', description='唯一的请求标识符，用于问题追踪', example='F8A0F5F3-0C3E-4C82-9D4F-5E4B6A7C8D9E'),
}

model ListBrowserSessionResult {
  code?: string(name='code', description='SUCCESS 为成功，失败情况返回对应错误类型，比如 ERR_BAD_REQUEST ERR_VALIDATION_FAILED ERR_INTERNAL_SERVER_ERROR', example='SUCCESS'),
  data?: BrowserSessionListOut(name='data', example='{}'),
  requestId?: string(name='requestId', example='F8A0F5F3-0C3E-4C82-9D4F-5E4B6A7C8D9E'),
}

model ListBrowsersInput {
  browserName?: string(name='browserName', description='按浏览器名称过滤', example='my-browser'),
  pageNumber?: integer(name='pageNumber', example='1'),
  pageSize?: integer(name='pageSize', example='20'),
}

model ListBrowsersOutput {
  items?: [
    Browser
  ](name='items'),
  pageNumber?: int32(name='pageNumber'),
  pageSize?: int32(name='pageSize'),
  total?: long(name='total'),
}

model ListBrowsersResult {
  code?: string(name='code', description='SUCCESS 为成功，失败情况返回对应错误类型，比如 ERR_BAD_REQUEST ERR_VALIDATION_FAILED ERR_INTERNAL_SERVER_ERROR', example='SUCCESS'),
  data?: ListBrowsersOutput(name='data', description='浏览器列表的详细信息', example='{}'),
  requestId?: string(name='requestId', description='唯一的请求标识符，用于问题追踪', example='F8A0F5F3-0C3E-4C82-9D4F-5E4B6A7C8D9E'),
}

model ListCodeInterpreterSessionResult {
  code?: string(name='code', description='SUCCESS 为成功，失败情况返回对应错误类型，比如 ERR_BAD_REQUEST ERR_VALIDATION_FAILED ERR_INTERNAL_SERVER_ERROR', example='SUCCESS'),
  data?: CodeInterpreterSessionListOut(name='data', example='{}'),
  requestId?: string(name='requestId', example='F8A0F5F3-0C3E-4C82-9D4F-5E4B6A7C8D9E'),
}

model ListCodeInterpretersInput {
  codeInterpreterName?: string(name='codeInterpreterName', description='按代码解释器名称过滤'),
  pageNumber?: integer(name='pageNumber'),
  pageSize?: integer(name='pageSize'),
}

model ListCodeInterpretersOutput {
  items?: [
    CodeInterpreter
  ](name='items'),
  pageNumber?: int32(name='pageNumber'),
  pageSize?: int32(name='pageSize'),
  total?: long(name='total'),
}

model ListCodeInterpretersResult {
  code?: string(name='code', description='SUCCESS 为成功，失败情况返回对应错误类型，比如 ERR_BAD_REQUEST ERR_VALIDATION_FAILED ERR_INTERNAL_SERVER_ERROR', example='SUCCESS'),
  data?: ListCodeInterpretersOutput(name='data', description='代码解释器列表的详细信息', example='{}'),
  requestId?: string(name='requestId', description='唯一的请求标识符，用于问题追踪', example='F8A0F5F3-0C3E-4C82-9D4F-5E4B6A7C8D9E'),
}

model ListCredentialsOutput {
  items?: [
    CredentialListItem
  ](name='items'),
  pageNumber?: string(name='pageNumber'),
  pageSize?: string(name='pageSize'),
  total?: string(name='total'),
}

model ListCredentialsResult {
  code?: string(name='code'),
  data?: ListCredentialsOutput(name='data'),
  requestId?: string(name='requestId'),
}

model ListCustomDomainsOutput {
  items?: [
    CustomDomain
  ](name='items'),
  pageNumber?: int32(name='pageNumber'),
  pageSize?: int32(name='pageSize'),
  total?: int32(name='total'),
}

model ListCustomDomainsResult {
  code?: string(name='code'),
  data?: ListCustomDomainsOutput(name='data'),
  requestId?: string(name='requestId'),
}

model ListDomainsOutput {
  items?: [
    DomainInfo
  ](name='items'),
  pageNumber?: string(name='pageNumber'),
  pageSize?: string(name='pageSize'),
  totalCount?: string(name='totalCount'),
}

model ListGatewaysOutput {
  items?: Gateway(name='items'),
  pageNumber?: int32(name='pageNumber'),
  pageSize?: int32(name='pageSize'),
  total?: int32(name='total'),
}

model ListKnowledgeBasesOutput {
  items?: [
    KnowledgeBase
  ](name='items'),
  pageNumber?: int32(name='pageNumber'),
  pageSize?: int32(name='pageSize'),
  total?: long(name='total'),
}

model ListKnowledgeBasesResult {
  code?: string(name='code'),
  data?: ListKnowledgeBasesOutput(name='data'),
  requestId?: string(name='requestId'),
}

model ListMemoryCollectionsOutput {
  items?: [
    MemoryCollection
  ](name='items'),
  pageNumber?: int32(name='pageNumber'),
  pageSize?: int32(name='pageSize'),
  total?: long(name='total'),
}

model ListMemoryCollectionsResult {
  code?: string(name='code'),
  data?: ListMemoryCollectionsOutput(name='data'),
  requestId?: string(name='requestId'),
}

model ListModelProxiesOutput {
  items?: [
    ModelProxy
  ](name='items'),
  pageNumber?: int32(name='pageNumber'),
  pageSize?: int32(name='pageSize'),
  total?: long(name='total'),
}

model ListModelProxiesResult {
  code?: string(name='code'),
  data?: ListModelProxiesOutput(name='data'),
  requestId?: string(name='requestId'),
}

model ListModelServicesOutput {
  items?: [
    ModelService
  ](name='items'),
  pageNumber?: int32(name='pageNumber'),
  pageSize?: int32(name='pageSize'),
  total?: long(name='total'),
}

model ListModelServicesResult {
  code?: string(name='code'),
  data?: ListModelServicesOutput(name='data'),
  requestId?: string(name='requestId'),
}

model ListSandboxesOutput {
  items?: [
    Sandbox
  ](name='items', description='This parameter is required.'),
  nextToken?: string(name='nextToken'),
}

model ListSandboxesResult {
  code?: string(name='code', description='SUCCESS 为成功，失败情况返回对应错误类型，比如 ERR_BAD_REQUEST ERR_VALIDATION_FAILED ERR_INTERNAL_SERVER_ERROR'),
  data?: ListSandboxesOutput(name='data', description='沙箱列表的详细信息'),
  requestId?: string(name='requestId', description='唯一的请求标识符，用于问题追踪'),
}

model ListTemplatesOutput {
  items?: [
    Template
  ](name='items', description='This parameter is required.'),
  pageNumber?: int32(name='pageNumber', description='This parameter is required.'),
  pageSize?: int32(name='pageSize', description='This parameter is required.'),
  total?: int32(name='total', description='This parameter is required.'),
}

model ListTemplatesResult {
  code?: string(name='code', description='SUCCESS 为成功，失败情况返回对应错误类型，比如 ERR_BAD_REQUEST ERR_VALIDATION_FAILED ERR_INTERNAL_SERVER_ERROR'),
  data?: ListTemplatesOutput(name='data', description='模板列表的详细信息'),
  requestId?: string(name='requestId', description='唯一的请求标识符，用于问题追踪'),
}

model ListToolsOutput {
  data?: [
    ToolListItem
  ](name='data'),
  pageNum?: int32(name='pageNum'),
  pageSize?: int32(name='pageSize'),
  total?: int32(name='total'),
}

model LogConfiguration {
  logstore?: string(name='logstore', description='SLS日志库名称', example='agent-runtime-logs'),
  project?: string(name='project', description='SLS项目名称', example='agent-runtime-logs'),
}

model MCPAPI {
  backendConfig?: MCPBackendConfig(name='backendConfig'),
  description?: string(name='description'),
  exposedUriPath?: string(name='exposedUriPath'),
  match?: MCPMatch(name='match'),
  mcpStatisticsEnable?: boolean(name='mcpStatisticsEnable'),
  protocol?: string(name='protocol'),
  toolId?: string(name='toolId'),
  toolsConfig?: string(name='toolsConfig'),
}

model MCPAPIConfiguration {
  description?: string(name='description'),
  exposedUriPath?: string(name='exposedUriPath'),
  mcpStatisticsEnable?: boolean(name='mcpStatisticsEnable'),
  protocol?: string(name='protocol'),
  toolId?: string(name='toolId'),
}

model MCPBackendConfig {
  scene?: string(name='scene'),
  services?: [
    MCPServiceConfig
  ](name='services'),
}

model MCPMatch {
  path?: MCPPathMatch(name='path'),
}

model MCPPathMatch {
  type?: string(name='type'),
  value?: string(name='value'),
}

model MCPServerConfig {
  serverUrl?: string(name='serverUrl'),
  ssePath?: string(name='ssePath'),
  transportType?: string(name='transportType'),
}

model MCPServiceConfig {
  serviceId?: string(name='serviceId'),
}

model MemoryCollection {
  createdAt?: string(name='createdAt'),
  description?: string(name='description'),
  embedderConfig?: EmbedderConfig(name='embedderConfig'),
  executionRoleArn?: string(name='executionRoleArn'),
  lastUpdatedAt?: string(name='lastUpdatedAt'),
  llmConfig?: LLMConfig(name='llmConfig'),
  memoryCollectionId?: string(name='memoryCollectionId'),
  memoryCollectionName?: string(name='memoryCollectionName'),
  networkConfiguration?: NetworkConfiguration(name='networkConfiguration'),
  vectorStoreConfig?: VectorStoreConfig(name='vectorStoreConfig'),
}

model MemoryCollectionResult {
  code?: string(name='code'),
  data?: MemoryCollection(name='data'),
  requestId?: string(name='requestId'),
}

model Model {
  address?: string(name='address'),
  apiKey?: string(name='apiKey'),
  createdTime?: string(name='createdTime'),
  desc?: string(name='desc'),
  gatewayId?: string(name='gatewayId'),
  modelId?: string(name='modelId'),
  models?: string(name='models'),
  modelsWeight?: string(name='modelsWeight'),
  name?: string(name='name'),
  provider?: string(name='provider'),
  targetId?: string(name='targetId'),
  tenantId?: string(name='tenantId'),
  type?: string(name='type'),
  updateTime?: string(name='updateTime'),
}

model ModelFeatures {
  agentThought?: boolean(name='agentThought'),
  multiToolCall?: boolean(name='multiToolCall'),
  streamToolCall?: boolean(name='streamToolCall'),
  toolCall?: boolean(name='toolCall'),
  vision?: boolean(name='vision'),
}

model ModelInfoConfig {
  modelFeatures?: ModelFeatures(name='modelFeatures'),
  modelName?: string(name='modelName'),
  modelParameterRules?: [
    ModelParameterRule
  ](name='modelParameterRules'),
  modelProperties?: ModelProperties(name='modelProperties'),
}

model ModelParameterRule {
  default?: any(name='default'),
  max?: int32(name='max'),
  min?: int32(name='min'),
  name?: string(name='name'),
  required?: boolean(name='required'),
  type?: string(name='type'),
}

model ModelProperties {
  contextSize?: int32(name='contextSize'),
}

model ModelProxy {
  cpu?: float(name='cpu'),
  createdAt?: string(name='createdAt'),
  credentialName?: string(name='credentialName'),
  description?: string(name='description'),
  endpoint?: string(name='endpoint'),
  executionRoleArn?: string(name='executionRoleArn'),
  functionName?: string(name='functionName'),
  lastUpdatedAt?: string(name='lastUpdatedAt'),
  litellmVersion?: string(name='litellmVersion'),
  logConfiguration?: LogConfiguration(name='logConfiguration'),
  memory?: int32(name='memory'),
  modelProxyId?: string(name='modelProxyId'),
  modelProxyName?: string(name='modelProxyName'),
  modelType?: string(name='modelType'),
  networkConfiguration?: NetworkConfiguration(name='networkConfiguration'),
  proxyConfig?: ProxyConfig(name='proxyConfig'),
  proxyMode?: string(name='proxyMode'),
  serviceRegionId?: string(name='serviceRegionId'),
  status?: string(name='status'),
  statusReason?: string(name='statusReason'),
}

model ModelProxyResult {
  code?: string(name='code'),
  data?: ModelProxy(name='data'),
  requestId?: string(name='requestId'),
}

model ModelService {
  createdAt?: string(name='createdAt'),
  credentialName?: string(name='credentialName'),
  description?: string(name='description'),
  lastUpdatedAt?: string(name='lastUpdatedAt'),
  modelInfoConfigs?: [
    ModelInfoConfig
  ](name='modelInfoConfigs'),
  modelServiceId?: string(name='modelServiceId'),
  modelServiceName?: string(name='modelServiceName'),
  modelType?: string(name='modelType'),
  networkConfiguration?: NetworkConfiguration(name='networkConfiguration'),
  provider?: string(name='provider'),
  providerSettings?: ProviderSettings(name='providerSettings'),
  status?: string(name='status'),
  statusReason?: string(name='statusReason'),
}

model ModelServiceResult {
  code?: string(name='code'),
  data?: ModelService(name='data'),
  requestId?: string(name='requestId'),
}

model NASConfig {
  groupId?: int32(name='groupId', example='100'),
  mountPoints?: [
    NASMountConfig
  ](name='mountPoints'),
  userId?: int32(name='userId', example='100'),
}

model NASMountConfig {
  enableTLS?: boolean(name='enableTLS'),
  mountDir?: string(name='mountDir', example='/home/test'),
  serverAddr?: string(name='serverAddr', example='***-uni85.cn-hangzhou.nas.com:/'),
}

model NetworkConfiguration {
  networkMode?: string(name='networkMode', example='PRIVATE'),
  securityGroupId?: string(name='securityGroupId', example='sg-1234567890abcdef0'),
  vpcId?: string(name='vpcId', example='vpc-1234567890abcdef0'),
  vswitchIds?: [ string ](name='vswitchIds', example='vsw-1234567890abcdef0,vsw-abcdef1234567890'),
}

model OSSMountConfig {
  mountPoints?: [
    OSSMountPoint
  ](name='mountPoints'),
}

model OSSMountPoint {
  bucketName?: string(name='bucketName', example='my-bucket'),
  bucketPath?: string(name='bucketPath', example='/my-dir'),
  endpoint?: string(name='endpoint', example='http://oss-cn-shanghai.aliyuncs.com'),
  mountDir?: string(name='mountDir', example='/mnt/dir'),
  readOnly?: boolean(name='readOnly', example='true'),
}

model OssConfiguration {
  bucketName?: string(name='bucketName', description='This parameter is required.'),
  mountPoint?: string(name='mountPoint', description='This parameter is required.'),
  permission?: string(name='permission'),
  prefix?: string(name='prefix', description='This parameter is required.'),
  region?: string(name='region', example='ch-hangzhou'),
}

model PaginationInfo {
  limit?: int32(name='limit'),
  page?: int32(name='page'),
  total?: int32(name='total'),
  totalPages?: int32(name='totalPages'),
}

model PathConfig {
  agentRuntimeEndpointName?: string(name='agentRuntimeEndpointName', description='agent runtime 版本（仅当 resourceType 为 runtime 时有效）'),
  methods?: [ string ](name='methods', description='支持的方法有：HEAD, GET, POST, PUT, DELETE, PATCH, OPTIONS', example='[\\"GET\\"]'),
  path?: string(name='path', description='此条路由规则对应的请求路径。', example='/login'),
  resourceName?: string(name='resourceName', description='资源名称'),
  resourceType?: string(name='resourceType', description='资源类型（和凭证关联资源类型一致）', example='runtime'),
}

model PolarFsConfig {
  groupId?: int32(name='groupId'),
  mountPoints?: [
    PolarFsMountConfig
  ](name='mountPoints'),
  userId?: int32(name='userId'),
}

model PolarFsMountConfig {
  instanceId?: string(name='instanceId'),
  mountDir?: string(name='mountDir'),
  remoteDir?: string(name='remoteDir'),
}

model PolicyConfig {
  aiFallbackConfig?: AiFallbackConfig(name='aiFallbackConfig'),
  enable?: boolean(name='enable'),
  type?: string(name='type'),
}

model ProtocolConfiguration {
  protocolSettings?: [
    ProtocolSettings
  ](name='protocolSettings', description='详细的协议配置信息'),
  type?: string(name='type', example='HTTP', deprecated=true),
}

model ProtocolSettings {
  a2AAgentCard?: string(name='A2AAgentCard', description='A2A Agent Card'),
  headers?: string(name='headers', description='请求头'),
  inputBodyJsonSchema?: string(name='inputBodyJsonSchema', description='请求体JSON模式'),
  method?: string(name='method', description='HTTP方法'),
  name?: string(name='name', description='协议名称'),
  outputBodyJsonSchema?: string(name='outputBodyJsonSchema', description='响应体JSON模式'),
  path?: string(name='path', description='协议路径'),
  pathPrefix?: string(name='pathPrefix', description='协议路径前缀'),
  requestContentType?: string(name='requestContentType', description='请求内容类型'),
  responseContentType?: string(name='responseContentType', description='响应内容类型'),
}

model ProviderSettings {
  apiKey?: string(name='apiKey'),
  baseUrl?: string(name='baseUrl'),
  modelNames?: [ string ](name='modelNames'),
}

model ProxyConfig {
  endpoints?: [ 
    {
      baseUrl?: string(name='baseUrl'),
      modelNames?: [ string ](name='modelNames'),
      modelServiceName?: string(name='modelServiceName'),
      weight?: int32(name='weight'),
    }
  ](name='endpoints'),
  policies?: {
    aiGuardrailConfig?: {
      blockOnContentModeration?: boolean(name='blockOnContentModeration'),
      blockOnMaliciousUrl?: boolean(name='blockOnMaliciousUrl'),
      blockOnModelHallucination?: boolean(name='blockOnModelHallucination'),
      blockOnPromptAttack?: boolean(name='blockOnPromptAttack'),
      blockOnSensitiveData?: boolean(name='blockOnSensitiveData'),
      checkRequest?: boolean(name='checkRequest'),
      checkResponse?: boolean(name='checkResponse'),
      level?: string(name='level'),
      maxTextLength?: int32(name='maxTextLength'),
    }(name='aiGuardrailConfig'),
    cache?: boolean(name='cache'),
    concurrencyLimit?: int32(name='concurrencyLimit'),
    fallbacks?: [ 
      {
        modelName?: string(name='modelName'),
        modelServiceName?: string(name='modelServiceName'),
      }
    ](name='fallbacks'),
    numRetries?: int32(name='numRetries'),
    requestTimeout?: int32(name='requestTimeout'),
    tokenRateLimiter?: {
      tpd?: int32(name='tpd'),
      tph?: int32(name='tph'),
      tpm?: int32(name='tpm'),
      tps?: int32(name='tps'),
    }(name='tokenRateLimiter'),
  }(name='policies'),
}

model PublishRuntimeVersionInput {
  description?: string(name='description', description='此版本的描述'),
  publisher?: string(name='publisher', description='发布此版本的用户或系统标识', example='user123'),
}

model RecordingConfiguration {
  enabled?: boolean(name='enabled', description='是否启用录制

This parameter is required.'),
  ossLocation?: OssConfiguration(name='ossLocation'),
}

model RegisterServiceInput {
  credentialName?: string(name='credentialName', description='关联的凭证ID，用于服务认证', example='my-credential'),
  protocol?: string(name='protocol', description='服务的协议类型

This parameter is required.', example='HTTP'),
  resourceName?: string(name='resourceName', description='关联的资源名称', example='my-resource'),
  serviceBackendEndpoint?: string(name='serviceBackendEndpoint', description='转发的下游服务端点URL，必须是有效的HTTP/HTTPS地址（这里是 FC trigger endpoint）

This parameter is required.', example='https://123456789.cn-hangzhou.fc.aliyuncs.com/2016-08-15/proxy/my-service/my-function/'),
  serviceName?: string(name='serviceName', description='服务名称，在租户内唯一

This parameter is required.', example='my-service'),
  serviceType?: string(name='serviceType', description='服务类型

This parameter is required.', example='FC'),
  tenantId?: string(name='tenantId', description='租户ID，用于多租户隔离

This parameter is required.', example='1234567890123456'),
}

model RelatedResource {
  resourceId?: string(name='resourceId'),
  resourceName?: string(name='resourceName'),
  resourceType?: string(name='resourceType'),
}

model RelatedWorkload {
  resourceId?: string(name='resourceId'),
  resourceName?: string(name='resourceName'),
  resourceType?: string(name='resourceType'),
}

model RouteConfig {
  routes?: [
    PathConfig
  ](name='routes', description='路由表：自定义域名访问时的 PATH 到 Function 的映射列表。', example='[]'),
}

model RoutingConfiguration {
  versionWeights?: [
    VersionWeight
  ](name='versionWeights', description='不同版本的流量权重配置'),
}

model Sandbox {
  createdAt?: string(name='createdAt', description='沙箱创建时间

This parameter is required.'),
  endedAt?: string(name='endedAt'),
  lastUpdatedAt?: string(name='lastUpdatedAt', description='最后更新时间'),
  metadata?: map[string]any(name='metadata'),
  sandboxArn?: string(name='sandboxArn'),
  sandboxId?: string(name='sandboxId', description='This parameter is required.'),
  sandboxIdleTTLInSeconds?: int32(name='sandboxIdleTTLInSeconds'),
  sandboxIdleTimeoutSeconds?: int32(name='sandboxIdleTimeoutSeconds', description='沙箱空闲超时时间（秒）'),
  status?: string(name='status', description='This parameter is required.'),
  templateId?: string(name='templateId', description='This parameter is required.'),
  templateName?: string(name='templateName'),
}

model SandboxHealthCheckOut {
  status?: string(name='status', description='健康状态，OK表示健康

This parameter is required.'),
}

model SandboxHealthCheckResult {
  code?: string(name='code', description='SUCCESS 为成功'),
  data?: SandboxHealthCheckOut(name='data'),
  requestId?: string(name='requestId', description='唯一的请求标识符，用于问题追踪'),
}

model SandboxResult {
  code?: string(name='code', description='SUCCESS 为成功，失败情况返回对应错误类型'),
  data?: Sandbox(name='data', description='沙箱的详细信息

This parameter is required.'),
  requestId?: string(name='requestId', description='唯一的请求标识符，用于问题追踪'),
}

model ServiceConfig {
  aiServiceConfig?: AiServiceConfig(name='aiServiceConfig'),
  name?: string(name='name'),
}

model ServiceResult {
  code?: string(name='code', description='SUCCESS 为成功，失败情况返回对应错误类型，比如 ERR_BAD_REQUEST ERR_VALIDATION_FAILED ERR_INTERNAL_SERVER_ERROR'),
  data?: any(name='data', description='服务的详细信息'),
  requestId?: string(name='requestId', description='唯一的请求标识符，用于问题追踪'),
}

model StartBrowserSessionInput {
  name?: string(name='name', example='my-browser-session', nullable=true),
}

model StartBrowserSessionResult {
  code?: string(name='code', description='SUCCESS 为成功，失败情况返回对应错误类型，比如 ERR_BAD_REQUEST ERR_VALIDATION_FAILED ERR_INTERNAL_SERVER_ERROR', example='SUCCESS'),
  data?: BrowserSessionOut(name='data', example='{}'),
  requestId?: string(name='requestId', example='F8A0F5F3-0C3E-4C82-9D4F-5E4B6A7C8D9E'),
}

model StartCodeInterpreterSessionInput {
  name?: string(name='name', description='代码解释器会话的名称，用于标识和区分不同的会话实例', nullable=true),
}

model StartCodeInterpreterSessionResult {
  code?: string(name='code', description='SUCCESS 为成功，失败情况返回对应错误类型，比如 ERR_BAD_REQUEST ERR_VALIDATION_FAILED ERR_INTERNAL_SERVER_ERROR'),
  data?: CodeInterpreterSessionOut(name='data'),
  requestId?: string(name='requestId'),
}

model StopCodeInterpreterSessionResult {
  code?: string(name='code', description='SUCCESS 为成功，失败情况返回对应错误类型，比如 ERR_BAD_REQUEST ERR_VALIDATION_FAILED ERR_INTERNAL_SERVER_ERROR'),
  requestId?: string(name='requestId'),
}

model StopSandboxResult {
  code?: string(name='code'),
  data?: Sandbox(name='data'),
  requestId?: string(name='requestId'),
}

model TLSConfig {
  cipherSuites?: [ string ](name='cipherSuites', description='TLS 加密套件列表。', example='[\\"TLS_RSA_WITH_RC4_128_SHA\\"]'),
  maxVersion?: string(name='maxVersion', description='TLS 最大版本号。', example='TLSv1.3'),
  minVersion?: string(name='minVersion', description='TLS 最小版本号。', example='TLSv1.2'),
}

model Target {
  llmConfig?: LLMAPIConfiguration(name='llmConfig'),
  mcpAPI?: MCPAPI(name='mcpAPI'),
  targetType?: string(name='targetType'),
}

model TargetConfiguration {
  llmAPIConfig?: LLMAPIConfiguration(name='llmAPIConfig'),
  mcpAPIConfig?: MCPAPIConfiguration(name='mcpAPIConfig'),
  targetType?: string(name='targetType'),
}

model TargetServiceConfig {
  modelId?: string(name='modelId'),
  modelName?: string(name='modelName'),
  modelNamePattern?: string(name='modelNamePattern'),
  weight?: long(name='weight'),
}

model Template {
  allowAnonymousManage?: boolean(name='allowAnonymousManage'),
  containerConfiguration?: ContainerConfiguration(name='containerConfiguration'),
  cpu?: float(name='cpu', description='This parameter is required.'),
  createdAt?: string(name='createdAt'),
  credentialConfiguration?: CredentialConfiguration(name='credentialConfiguration'),
  description?: string(name='description'),
  diskSize?: int32(name='diskSize'),
  environmentVariables?: map[string]string(name='environmentVariables'),
  executionRoleArn?: string(name='executionRoleArn'),
  lastUpdatedAt?: string(name='lastUpdatedAt'),
  logConfiguration?: LogConfiguration(name='logConfiguration'),
  mcpOptions?: {
    enabledTools?: [ string ](name='enabledTools'),
    transport?: string(name='transport', example='streamable-http'),
  }(name='mcpOptions'),
  mcpState?: {
    accessEndpoint?: string(name='accessEndpoint'),
    status?: string(name='status'),
    statusReason?: string(name='statusReason'),
  }(name='mcpState'),
  memory?: int32(name='memory', description='This parameter is required.'),
  networkConfiguration?: NetworkConfiguration(name='networkConfiguration'),
  ossConfiguration?: [
    OssConfiguration
  ](name='ossConfiguration'),
  resourceName?: string(name='resourceName'),
  sandboxIdleTimeoutInSeconds?: string(name='sandboxIdleTimeoutInSeconds'),
  sandboxTTLInSeconds?: string(name='sandboxTTLInSeconds', deprecated=true),
  status?: string(name='status'),
  statusReason?: string(name='statusReason'),
  templateArn?: string(name='templateArn'),
  templateConfiguration?: map[string]any(name='templateConfiguration'),
  templateId?: string(name='templateId', description='This parameter is required.'),
  templateName?: string(name='templateName', description='This parameter is required.'),
  templateType?: string(name='templateType'),
  templateVersion?: string(name='templateVersion'),
}

model TemplateResult {
  code?: string(name='code', description='SUCCESS 为成功，失败情况返回对应错误类型'),
  data?: Template(name='data', description='模板的详细信息

This parameter is required.'),
  requestId?: string(name='requestId', description='唯一的请求标识符，用于问题追踪'),
}

model ToolInfo {
  CAPConfig?: CAPConfig(name='CAPConfig'),
  createdAt?: string(name='createdAt'),
  description?: string(name='description'),
  id?: string(name='id'),
  name?: string(name='name'),
  schema?: string(name='schema'),
  sourceType?: string(name='sourceType'),
  toolType?: string(name='toolType'),
  updatedAt?: string(name='updatedAt'),
}

model ToolListItem {
  CAPConfig?: CAPConfig(name='CAPConfig'),
  createdAt?: string(name='createdAt'),
  description?: string(name='description'),
  id?: string(name='id'),
  name?: string(name='name'),
  schema?: string(name='schema'),
  sourceType?: string(name='sourceType'),
  toolType?: string(name='toolType'),
  updatedAt?: string(name='updatedAt'),
}

model TriggerConfig {
  authType?: string(name='authType'),
  methods?: [ string ](name='methods'),
}

model UpdateAgentRuntimeEndpointInput {
  agentRuntimeEndpointName?: string(name='agentRuntimeEndpointName', example='production-endpoint'),
  description?: string(name='description', example='Updated endpoint configuration'),
  routingConfiguration?: RoutingConfiguration(name='routingConfiguration', description='智能体运行时端点的路由配置，支持多版本权重分配', example='{}'),
  targetVersion?: string(name='targetVersion', description='智能体运行时的目标版本', example='LATEST'),
}

model UpdateAgentRuntimeInput {
  agentRuntimeName?: string(name='agentRuntimeName', example='my-agent-runtime'),
  artifactType?: string(name='artifactType', example='Code'),
  codeConfiguration?: CodeConfiguration(name='codeConfiguration', description='当artifactType为Code时的代码配置信息，包括代码源、入口文件等', example='{}'),
  containerConfiguration?: ContainerConfiguration(name='containerConfiguration', description='当artifactType为Container时的容器配置信息，包括镜像地址、启动命令等', example='{}'),
  cpu?: float(name='cpu', description='This parameter is required.', example='1.0'),
  credentialName?: string(name='credentialName', description='用于访问智能体的凭证名称，访问智能体运行时将使用此凭证进行身份验证', example='my-credential'),
  description?: string(name='description', example='更新后的智能体运行时描述'),
  environmentVariables?: map[string]string(name='environmentVariables', description='智能体运行时的环境变量配置，用于在运行时传递配置参数', example='ENV_VAR1=value1,ENV_VAR2=value2'),
  executionRoleArn?: string(name='executionRoleArn', description='为智能体运行时提供访问云服务权限的执行角色ARN', example='acs:ram::1760720386195983:role/AgentRunExecutionRole'),
  healthCheckConfiguration?: HealthCheckConfiguration(name='healthCheckConfiguration', description='智能体运行时的健康检查配置，用于监控运行时实例的健康状态', example='{}'),
  logConfiguration?: LogConfiguration(name='logConfiguration', description='SLS（简单日志服务）配置', example='{}'),
  memory?: int32(name='memory', example='1024'),
  networkConfiguration?: NetworkConfiguration(name='networkConfiguration', description='智能体运行时的网络配置，包括VPC、安全组等网络访问设置', example='{}'),
  port?: int32(name='port', example='8080'),
  protocolConfiguration?: ProtocolConfiguration(name='protocolConfiguration', description='智能体运行时的通信协议配置，定义运行时如何与外部系统交互', example='{}'),
  sessionConcurrencyLimitPerInstance?: int32(name='sessionConcurrencyLimitPerInstance', description='每个运行时实例允许的最大并发会话数', example='100'),
  sessionIdleTimeoutSeconds?: int32(name='sessionIdleTimeoutSeconds', description='会话的空闲超时时间，单位为秒。实例没有会话请求后处于空闲状态，空闲态为闲置计费模式，超过此超时时间后会话自动过期，不可继续使用', example='3600'),
}

model UpdateApigLLMModelInput {
  address?: string(name='address'),
  apiKey?: string(name='apiKey'),
  desc?: string(name='desc'),
  models?: [ string ](name='models'),
  name?: string(name='name'),
  provider?: string(name='provider'),
  type?: string(name='type'),
}

model UpdateCredentialInput {
  credentialPublicConfig?: CredentialPublicConfig(name='credentialPublicConfig'),
  credentialSecret?: string(name='credentialSecret'),
  description?: string(name='description'),
  enabled?: boolean(name='enabled'),
}

model UpdateCredentialOutput {
  createdAt?: string(name='createdAt'),
  credentialAuthType?: string(name='credentialAuthType'),
  credentialId?: string(name='credentialId'),
  credentialName?: string(name='credentialName'),
  credentialPublicConfig?: map[string]string(name='credentialPublicConfig'),
  credentialSecret?: string(name='credentialSecret'),
  credentialSourceType?: string(name='credentialSourceType'),
  description?: string(name='description'),
  enabled?: boolean(name='enabled'),
  relatedResources?: [
    RelatedResource
  ](name='relatedResources'),
  updatedAt?: string(name='updatedAt'),
}

model UpdateCustomDomainInput {
  certConfig?: CertConfig(name='certConfig', description='HTTPS 证书的信息。', example='{}'),
  description?: string(name='description', description='描述'),
  protocol?: string(name='protocol', description='域名支持的协议类型：● HTTP：仅支持 HTTP 协议。● HTTPS：仅支持 HTTPS 协议。● HTTP,HTTPS：支持 HTTP 及 HTTPS 协议。', example='HTTP'),
  routeConfig?: RouteConfig(name='routeConfig', description='路由表：自定义域名访问时的 PATH 到 资源 的映射。', example='{}'),
  tlsConfig?: TLSConfig(name='tlsConfig', description='TLS 配置信息。', example='{}'),
}

model UpdateDomainInput {
  certIdentifier?: string(name='certIdentifier'),
  protocol?: string(name='protocol'),
}

model UpdateKnowledgeBaseInput {
  credentialName?: string(name='credentialName'),
  description?: string(name='description'),
  providerSettings?: map[string]any(name='providerSettings'),
  retrieveSettings?: map[string]any(name='retrieveSettings'),
}

model UpdateMemoryCollectionInput {
  description?: string(name='description'),
  embedderConfig?: EmbedderConfig(name='embedderConfig'),
  executionRoleArn?: string(name='executionRoleArn'),
  llmConfig?: LLMConfig(name='llmConfig'),
  networkConfiguration?: NetworkConfiguration(name='networkConfiguration'),
  vectorStoreConfig?: VectorStoreConfig(name='vectorStoreConfig'),
}

model UpdateModelInput {
  address?: string(name='address'),
  apiKey?: string(name='apiKey'),
  desc?: string(name='desc'),
  models?: [ string ](name='models'),
  name?: string(name='name'),
  provider?: string(name='provider'),
  type?: string(name='type'),
}

model UpdateModelProxyInput {
  armsConfiguration?: ArmsConfiguration(name='armsConfiguration'),
  credentialName?: string(name='credentialName'),
  description?: string(name='description'),
  executionRoleArn?: string(name='executionRoleArn'),
  logConfiguration?: LogConfiguration(name='logConfiguration'),
  networkConfiguration?: NetworkConfiguration(name='networkConfiguration'),
  proxyConfig?: ProxyConfig(name='proxyConfig'),
}

model UpdateModelServiceInput {
  credentialName?: string(name='credentialName'),
  description?: string(name='description'),
  modelInfoConfigs?: [
    ModelInfoConfig
  ](name='modelInfoConfigs'),
  networkConfiguration?: NetworkConfiguration(name='networkConfiguration'),
  providerSettings?: ProviderSettings(name='providerSettings'),
  status?: string(name='status'),
  statusReason?: string(name='statusReason'),
}

model UpdateTargetConfigurationInput {
  domainId?: string(name='domainId'),
  targetConfiguration?: TargetConfiguration(name='targetConfiguration'),
}

model UpdateTemplateInput {
  allowAnonymousManage?: boolean(name='allowAnonymousManage', nullable=true),
  armsConfiguration?: ArmsConfiguration(name='armsConfiguration'),
  containerConfiguration?: ContainerConfiguration(name='containerConfiguration', description='容器配置（内置的不可改）'),
  cpu?: float(name='cpu', description='CPU资源配置（单位：核心）'),
  credentialConfiguration?: CredentialConfiguration(name='credentialConfiguration'),
  description?: string(name='description'),
  environmentVariables?: map[string]string(name='environmentVariables'),
  executionRoleArn?: string(name='executionRoleArn'),
  logConfiguration?: LogConfiguration(name='logConfiguration'),
  memory?: int32(name='memory', description='内存资源配置（单位：MB）'),
  networkConfiguration?: NetworkConfiguration(name='networkConfiguration'),
  ossConfiguration?: [
    OssConfiguration
  ](name='ossConfiguration'),
  sandboxIdleTimeoutInSeconds?: int32(name='sandboxIdleTimeoutInSeconds', description='沙箱空闲超时时间（秒）'),
  sandboxTTLInSeconds?: int32(name='sandboxTTLInSeconds', description='沙箱存活时间（秒）', deprecated=true),
  templateConfiguration?: map[string]any(name='templateConfiguration', description='模板配置（灵活的对象结构，根据 templateType 不同而不同）'),
}

model UpdateToolData {
  id?: string(name='id'),
  name?: string(name='name'),
  sourceType?: string(name='sourceType'),
  toolType?: string(name='toolType'),
  updatedAt?: string(name='updatedAt'),
}

model UpdateToolInput {
  description?: string(name='description'),
  name?: string(name='name'),
  schema?: string(name='schema'),
}

model UpdateToolOutput {
  data?: UpdateToolData(name='data'),
  message?: string(name='message'),
  success?: boolean(name='success'),
}

model VectorStoreConfig {
  config?: {
    collectionName?: string(name='collectionName'),
    endpoint?: string(name='endpoint'),
    instanceName?: string(name='instanceName'),
    vectorDimension?: int32(name='vectorDimension'),
  }(name='config'),
  provider?: string(name='provider'),
}

model VersionWeight {
  version?: string(name='version', description='智能体运行时版本号'),
  weight?: float(name='weight', description='流量权重比例（0.0-1.0）'),
}

model ViewPortConfiguration {
  height?: float(name='height', description='视口高度（像素）

This parameter is required.'),
  width?: float(name='width', description='视口宽度（像素）

This parameter is required.'),
}

model ActivateTemplateMCPRequest {
  enabledTools?: [ string ](name='enabledTools'),
  transport?: string(name='transport', example='streamable-http'),
}

model ActivateTemplateMCPResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: TemplateResult  
}

/**
 * @summary 启动模板的MCP服务器
 *
 * @param request ActivateTemplateMCPRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ActivateTemplateMCPResponse
 */
@context("activateTemplateMCPWithContext")
async function activateTemplateMCPWithOptions(templateName: string, request: ActivateTemplateMCPRequest, headers: map[string]string, runtime: $RuntimeOptions): ActivateTemplateMCPResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.enabledTools)) {
    body['enabledTools'] = request.enabledTools;
  }
  if (!$isNull(request.transport)) {
    body['transport'] = request.transport;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'ActivateTemplateMCP',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/templates/${$URL.percentEncode(templateName)}/mcp/activate`,
    method = 'PATCH',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 启动模板的MCP服务器
 *
 * @param request ActivateTemplateMCPRequest
 * @return ActivateTemplateMCPResponse
 */
async function activateTemplateMCP(templateName: string, request: ActivateTemplateMCPRequest): ActivateTemplateMCPResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return activateTemplateMCPWithOptions(templateName, request, headers, runtime);
}

model CreateAgentRuntimeRequest {
  body?: CreateAgentRuntimeInput(name='body', description='创建智能体运行时所需的完整配置信息，包括运行时名称、资源规格、网络配置、协议配置等

This parameter is required.'),
}

model CreateAgentRuntimeResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: AgentRuntimeResult  
}

/**
 * @summary Create an agent runtime
 *
 * @description 创建一个新的智能体运行时实例，用于执行AI代理任务。智能体运行时是AgentRun服务的核心组件，提供代码执行、浏览器操作、内存管理等能力。
 *
 * @param request CreateAgentRuntimeRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateAgentRuntimeResponse
 */
@context("createAgentRuntimeWithContext")
async function createAgentRuntimeWithOptions(request: CreateAgentRuntimeRequest, headers: map[string]string, runtime: $RuntimeOptions): CreateAgentRuntimeResponse {
  request.validate();
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(request.body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateAgentRuntime',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/runtimes`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Create an agent runtime
 *
 * @description 创建一个新的智能体运行时实例，用于执行AI代理任务。智能体运行时是AgentRun服务的核心组件，提供代码执行、浏览器操作、内存管理等能力。
 *
 * @param request CreateAgentRuntimeRequest
 * @return CreateAgentRuntimeResponse
 */
async function createAgentRuntime(request: CreateAgentRuntimeRequest): CreateAgentRuntimeResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return createAgentRuntimeWithOptions(request, headers, runtime);
}

model CreateAgentRuntimeEndpointRequest {
  body?: CreateAgentRuntimeEndpointInput(name='body', description='包含要创建的智能体运行时端点配置信息的请求体

This parameter is required.'),
}

model CreateAgentRuntimeEndpointResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: AgentRuntimeEndpointResult  
}

/**
 * @summary 创建智能体运行时端点
 *
 * @description 为指定的智能体运行时创建新的端点，用于外部访问和调用。端点是智能体运行时对外提供服务的入口。
 *
 * @param request CreateAgentRuntimeEndpointRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateAgentRuntimeEndpointResponse
 */
@context("createAgentRuntimeEndpointWithContext")
async function createAgentRuntimeEndpointWithOptions(agentRuntimeId: string, request: CreateAgentRuntimeEndpointRequest, headers: map[string]string, runtime: $RuntimeOptions): CreateAgentRuntimeEndpointResponse {
  request.validate();
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(request.body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateAgentRuntimeEndpoint',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/runtimes/${$URL.percentEncode(agentRuntimeId)}/endpoints`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 创建智能体运行时端点
 *
 * @description 为指定的智能体运行时创建新的端点，用于外部访问和调用。端点是智能体运行时对外提供服务的入口。
 *
 * @param request CreateAgentRuntimeEndpointRequest
 * @return CreateAgentRuntimeEndpointResponse
 */
async function createAgentRuntimeEndpoint(agentRuntimeId: string, request: CreateAgentRuntimeEndpointRequest): CreateAgentRuntimeEndpointResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return createAgentRuntimeEndpointWithOptions(agentRuntimeId, request, headers, runtime);
}

model CreateBrowserRequest {
  body?: CreateBrowserInput(name='body', description='This parameter is required.'),
}

model CreateBrowserResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: BrowserResult  
}

/**
 * @summary 创建浏览器实例
 *
 * @description 创建一个新的浏览器实例，用于执行网页自动化任务。浏览器实例提供网页浏览、元素操作、截图录制等功能。
 *
 * @param request CreateBrowserRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateBrowserResponse
 */
@context("createBrowserWithContext")
async function createBrowserWithOptions(request: CreateBrowserRequest, headers: map[string]string, runtime: $RuntimeOptions): CreateBrowserResponse {
  request.validate();
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(request.body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateBrowser',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/browsers`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 创建浏览器实例
 *
 * @description 创建一个新的浏览器实例，用于执行网页自动化任务。浏览器实例提供网页浏览、元素操作、截图录制等功能。
 *
 * @param request CreateBrowserRequest
 * @return CreateBrowserResponse
 */
async function createBrowser(request: CreateBrowserRequest): CreateBrowserResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return createBrowserWithOptions(request, headers, runtime);
}

model CreateCodeInterpreterRequest {
  body?: CreateCodeInterpreterInput(name='body', description='This parameter is required.'),
}

model CreateCodeInterpreterResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CodeInterpreterResult  
}

/**
 * @summary 创建代码解释器
 *
 * @description 创建一个新的代码解释器实例，用于执行代码解释和运行任务。代码解释器提供Python代码执行、数据处理、机器学习等功能。
 *
 * @param request CreateCodeInterpreterRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateCodeInterpreterResponse
 */
@context("createCodeInterpreterWithContext")
async function createCodeInterpreterWithOptions(request: CreateCodeInterpreterRequest, headers: map[string]string, runtime: $RuntimeOptions): CreateCodeInterpreterResponse {
  request.validate();
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(request.body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateCodeInterpreter',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/code-interpreters`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 创建代码解释器
 *
 * @description 创建一个新的代码解释器实例，用于执行代码解释和运行任务。代码解释器提供Python代码执行、数据处理、机器学习等功能。
 *
 * @param request CreateCodeInterpreterRequest
 * @return CreateCodeInterpreterResponse
 */
async function createCodeInterpreter(request: CreateCodeInterpreterRequest): CreateCodeInterpreterResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return createCodeInterpreterWithOptions(request, headers, runtime);
}

model CreateCredentialRequest {
  body?: CreateCredentialInput(name='body'),
}

model CreateCredentialResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CredentialResult  
}

/**
 * @summary Create a credential
 *
 * @param request CreateCredentialRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateCredentialResponse
 */
@context("createCredentialWithContext")
async function createCredentialWithOptions(request: CreateCredentialRequest, headers: map[string]string, runtime: $RuntimeOptions): CreateCredentialResponse {
  request.validate();
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(request.body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateCredential',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/credentials`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Create a credential
 *
 * @param request CreateCredentialRequest
 * @return CreateCredentialResponse
 */
async function createCredential(request: CreateCredentialRequest): CreateCredentialResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return createCredentialWithOptions(request, headers, runtime);
}

model CreateCustomDomainRequest {
  body?: CreateCustomDomainInput(name='body'),
}

model CreateCustomDomainResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CustomDomainResult  
}

/**
 * @summary 创建自定义域名
 *
 * @param request CreateCustomDomainRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateCustomDomainResponse
 */
@context("createCustomDomainWithContext")
async function createCustomDomainWithOptions(request: CreateCustomDomainRequest, headers: map[string]string, runtime: $RuntimeOptions): CreateCustomDomainResponse {
  request.validate();
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(request.body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateCustomDomain',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/custom-domains`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 创建自定义域名
 *
 * @param request CreateCustomDomainRequest
 * @return CreateCustomDomainResponse
 */
async function createCustomDomain(request: CreateCustomDomainRequest): CreateCustomDomainResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return createCustomDomainWithOptions(request, headers, runtime);
}

model CreateKnowledgeBaseRequest {
  body?: CreateKnowledgeBaseInput(name='body'),
}

model CreateKnowledgeBaseResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: KnowledgeBaseResult  
}

/**
 * @summary 创建知识库
 *
 * @param request CreateKnowledgeBaseRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateKnowledgeBaseResponse
 */
@context("createKnowledgeBaseWithContext")
async function createKnowledgeBaseWithOptions(request: CreateKnowledgeBaseRequest, headers: map[string]string, runtime: $RuntimeOptions): CreateKnowledgeBaseResponse {
  request.validate();
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(request.body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateKnowledgeBase',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/knowledgebases`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 创建知识库
 *
 * @param request CreateKnowledgeBaseRequest
 * @return CreateKnowledgeBaseResponse
 */
async function createKnowledgeBase(request: CreateKnowledgeBaseRequest): CreateKnowledgeBaseResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return createKnowledgeBaseWithOptions(request, headers, runtime);
}

model CreateMemoryCollectionRequest {
  body?: CreateMemoryCollectionInput(name='body'),
}

model CreateMemoryCollectionResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: MemoryCollectionResult  
}

/**
 * @summary 添加记忆存储
 *
 * @param request CreateMemoryCollectionRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateMemoryCollectionResponse
 */
@context("createMemoryCollectionWithContext")
async function createMemoryCollectionWithOptions(request: CreateMemoryCollectionRequest, headers: map[string]string, runtime: $RuntimeOptions): CreateMemoryCollectionResponse {
  request.validate();
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(request.body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateMemoryCollection',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/memory-collections`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 添加记忆存储
 *
 * @param request CreateMemoryCollectionRequest
 * @return CreateMemoryCollectionResponse
 */
async function createMemoryCollection(request: CreateMemoryCollectionRequest): CreateMemoryCollectionResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return createMemoryCollectionWithOptions(request, headers, runtime);
}

model CreateModelProxyRequest {
  body?: CreateModelProxyInput(name='body'),
}

model CreateModelProxyResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ModelProxyResult  
}

/**
 * @summary 新增模型
 *
 * @param request CreateModelProxyRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateModelProxyResponse
 */
@context("createModelProxyWithContext")
async function createModelProxyWithOptions(request: CreateModelProxyRequest, headers: map[string]string, runtime: $RuntimeOptions): CreateModelProxyResponse {
  request.validate();
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(request.body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateModelProxy',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/model-proxies`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 新增模型
 *
 * @param request CreateModelProxyRequest
 * @return CreateModelProxyResponse
 */
async function createModelProxy(request: CreateModelProxyRequest): CreateModelProxyResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return createModelProxyWithOptions(request, headers, runtime);
}

model CreateModelServiceRequest {
  body?: CreateModelServiceInput(name='body'),
}

model CreateModelServiceResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ModelServiceResult  
}

/**
 * @summary 新增模型
 *
 * @param request CreateModelServiceRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateModelServiceResponse
 */
@context("createModelServiceWithContext")
async function createModelServiceWithOptions(request: CreateModelServiceRequest, headers: map[string]string, runtime: $RuntimeOptions): CreateModelServiceResponse {
  request.validate();
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(request.body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateModelService',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/model-services`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 新增模型
 *
 * @param request CreateModelServiceRequest
 * @return CreateModelServiceResponse
 */
async function createModelService(request: CreateModelServiceRequest): CreateModelServiceResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return createModelServiceWithOptions(request, headers, runtime);
}

model CreateSandboxRequest {
  body?: CreateSandboxInput(name='body', description='创建沙箱所需的配置信息，包括模板名称、沙箱名称等

This parameter is required.'),
}

model CreateSandboxResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: SandboxResult  
}

/**
 * @summary 创建沙箱
 *
 * @description 根据模板创建一个新的沙箱实例。沙箱是运行时的执行环境，可以执行代码或运行浏览器。
 *
 * @param request CreateSandboxRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateSandboxResponse
 */
@context("createSandboxWithContext")
async function createSandboxWithOptions(request: CreateSandboxRequest, headers: map[string]string, runtime: $RuntimeOptions): CreateSandboxResponse {
  request.validate();
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(request.body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateSandbox',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/sandboxes`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 创建沙箱
 *
 * @description 根据模板创建一个新的沙箱实例。沙箱是运行时的执行环境，可以执行代码或运行浏览器。
 *
 * @param request CreateSandboxRequest
 * @return CreateSandboxResponse
 */
async function createSandbox(request: CreateSandboxRequest): CreateSandboxResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return createSandboxWithOptions(request, headers, runtime);
}

model CreateTemplateRequest {
  body?: CreateTemplateInput(name='body', description='创建模板所需的完整配置信息，包括模板名称、类型、资源规格、网络配置等

This parameter is required.'),
}

model CreateTemplateResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: TemplateResult  
}

/**
 * @summary 创建模板
 *
 * @description 创建一个新的模板，用于后续创建沙箱。模板定义了沙箱的运行时环境、资源配置等。
 *
 * @param request CreateTemplateRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateTemplateResponse
 */
@context("createTemplateWithContext")
async function createTemplateWithOptions(request: CreateTemplateRequest, headers: map[string]string, runtime: $RuntimeOptions): CreateTemplateResponse {
  request.validate();
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(request.body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateTemplate',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/templates`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 创建模板
 *
 * @description 创建一个新的模板，用于后续创建沙箱。模板定义了沙箱的运行时环境、资源配置等。
 *
 * @param request CreateTemplateRequest
 * @return CreateTemplateResponse
 */
async function createTemplate(request: CreateTemplateRequest): CreateTemplateResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return createTemplateWithOptions(request, headers, runtime);
}

model DeleteAgentRuntimeResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: AgentRuntimeResult  
}

/**
 * @summary 删除智能体运行时
 *
 * @description 删除指定的智能体运行时实例，包括其所有相关资源和数据。删除操作不可逆，请谨慎操作。
 *
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteAgentRuntimeResponse
 */
@context("deleteAgentRuntimeWithContext")
async function deleteAgentRuntimeWithOptions(agentRuntimeId: string, headers: map[string]string, runtime: $RuntimeOptions): DeleteAgentRuntimeResponse {
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
  };
  var params = new OpenApiUtil.Params{
    action = 'DeleteAgentRuntime',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/runtimes/${$URL.percentEncode(agentRuntimeId)}`,
    method = 'DELETE',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 删除智能体运行时
 *
 * @description 删除指定的智能体运行时实例，包括其所有相关资源和数据。删除操作不可逆，请谨慎操作。
 *
 * @return DeleteAgentRuntimeResponse
 */
async function deleteAgentRuntime(agentRuntimeId: string): DeleteAgentRuntimeResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return deleteAgentRuntimeWithOptions(agentRuntimeId, headers, runtime);
}

model DeleteAgentRuntimeEndpointResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: AgentRuntimeEndpointResult  
}

/**
 * @summary Delete an agent runtime endpoint
 *
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteAgentRuntimeEndpointResponse
 */
@context("deleteAgentRuntimeEndpointWithContext")
async function deleteAgentRuntimeEndpointWithOptions(agentRuntimeId: string, agentRuntimeEndpointId: string, headers: map[string]string, runtime: $RuntimeOptions): DeleteAgentRuntimeEndpointResponse {
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
  };
  var params = new OpenApiUtil.Params{
    action = 'DeleteAgentRuntimeEndpoint',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/runtimes/${$URL.percentEncode(agentRuntimeId)}/endpoints/${$URL.percentEncode(agentRuntimeEndpointId)}`,
    method = 'DELETE',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Delete an agent runtime endpoint
 *
 * @return DeleteAgentRuntimeEndpointResponse
 */
async function deleteAgentRuntimeEndpoint(agentRuntimeId: string, agentRuntimeEndpointId: string): DeleteAgentRuntimeEndpointResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return deleteAgentRuntimeEndpointWithOptions(agentRuntimeId, agentRuntimeEndpointId, headers, runtime);
}

model DeleteBrowserResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DeleteBrowserResult  
}

/**
 * @summary 删除浏览器实例
 *
 * @description 删除指定的浏览器实例，包括其所有相关资源和数据。删除操作不可逆，请谨慎操作。
 *
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteBrowserResponse
 */
@context("deleteBrowserWithContext")
async function deleteBrowserWithOptions(browserId: string, headers: map[string]string, runtime: $RuntimeOptions): DeleteBrowserResponse {
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
  };
  var params = new OpenApiUtil.Params{
    action = 'DeleteBrowser',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/browsers/${$URL.percentEncode(browserId)}`,
    method = 'DELETE',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 删除浏览器实例
 *
 * @description 删除指定的浏览器实例，包括其所有相关资源和数据。删除操作不可逆，请谨慎操作。
 *
 * @return DeleteBrowserResponse
 */
async function deleteBrowser(browserId: string): DeleteBrowserResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return deleteBrowserWithOptions(browserId, headers, runtime);
}

model DeleteCodeInterpreterResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DeleteCodeInterpreterResult  
}

/**
 * @summary 删除代码解释器
 *
 * @description 删除指定的代码解释器实例，包括其所有相关资源和数据。删除操作不可逆，请谨慎操作。
 *
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteCodeInterpreterResponse
 */
@context("deleteCodeInterpreterWithContext")
async function deleteCodeInterpreterWithOptions(codeInterpreterId: string, headers: map[string]string, runtime: $RuntimeOptions): DeleteCodeInterpreterResponse {
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
  };
  var params = new OpenApiUtil.Params{
    action = 'DeleteCodeInterpreter',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/code-interpreters/${$URL.percentEncode(codeInterpreterId)}`,
    method = 'DELETE',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 删除代码解释器
 *
 * @description 删除指定的代码解释器实例，包括其所有相关资源和数据。删除操作不可逆，请谨慎操作。
 *
 * @return DeleteCodeInterpreterResponse
 */
async function deleteCodeInterpreter(codeInterpreterId: string): DeleteCodeInterpreterResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return deleteCodeInterpreterWithOptions(codeInterpreterId, headers, runtime);
}

model DeleteCredentialResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CredentialResult  
}

/**
 * @summary Delete a credential
 *
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteCredentialResponse
 */
@context("deleteCredentialWithContext")
async function deleteCredentialWithOptions(credentialName: string, headers: map[string]string, runtime: $RuntimeOptions): DeleteCredentialResponse {
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
  };
  var params = new OpenApiUtil.Params{
    action = 'DeleteCredential',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/credentials/${$URL.percentEncode(credentialName)}`,
    method = 'DELETE',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Delete a credential
 *
 * @return DeleteCredentialResponse
 */
async function deleteCredential(credentialName: string): DeleteCredentialResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return deleteCredentialWithOptions(credentialName, headers, runtime);
}

model DeleteCustomDomainResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CustomDomainResult  
}

/**
 * @summary Delete a custom domain
 *
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteCustomDomainResponse
 */
@context("deleteCustomDomainWithContext")
async function deleteCustomDomainWithOptions(domainName: string, headers: map[string]string, runtime: $RuntimeOptions): DeleteCustomDomainResponse {
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
  };
  var params = new OpenApiUtil.Params{
    action = 'DeleteCustomDomain',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/custom-domains/${$URL.percentEncode(domainName)}`,
    method = 'DELETE',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Delete a custom domain
 *
 * @return DeleteCustomDomainResponse
 */
async function deleteCustomDomain(domainName: string): DeleteCustomDomainResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return deleteCustomDomainWithOptions(domainName, headers, runtime);
}

model DeleteKnowledgeBaseResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: KnowledgeBaseResult  
}

/**
 * @summary 删除知识库
 *
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteKnowledgeBaseResponse
 */
@context("deleteKnowledgeBaseWithContext")
async function deleteKnowledgeBaseWithOptions(knowledgeBaseName: string, headers: map[string]string, runtime: $RuntimeOptions): DeleteKnowledgeBaseResponse {
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
  };
  var params = new OpenApiUtil.Params{
    action = 'DeleteKnowledgeBase',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/knowledgebases/${$URL.percentEncode(knowledgeBaseName)}`,
    method = 'DELETE',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 删除知识库
 *
 * @return DeleteKnowledgeBaseResponse
 */
async function deleteKnowledgeBase(knowledgeBaseName: string): DeleteKnowledgeBaseResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return deleteKnowledgeBaseWithOptions(knowledgeBaseName, headers, runtime);
}

model DeleteMemoryCollectionResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: MemoryCollectionResult  
}

/**
 * @summary 删除记忆存储
 *
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteMemoryCollectionResponse
 */
@context("deleteMemoryCollectionWithContext")
async function deleteMemoryCollectionWithOptions(memoryCollectionName: string, headers: map[string]string, runtime: $RuntimeOptions): DeleteMemoryCollectionResponse {
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
  };
  var params = new OpenApiUtil.Params{
    action = 'DeleteMemoryCollection',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/memory-collections/${$URL.percentEncode(memoryCollectionName)}`,
    method = 'DELETE',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 删除记忆存储
 *
 * @return DeleteMemoryCollectionResponse
 */
async function deleteMemoryCollection(memoryCollectionName: string): DeleteMemoryCollectionResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return deleteMemoryCollectionWithOptions(memoryCollectionName, headers, runtime);
}

model DeleteModelProxyResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DeleteModelProxyResult  
}

/**
 * @summary 删除模型
 *
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteModelProxyResponse
 */
@context("deleteModelProxyWithContext")
async function deleteModelProxyWithOptions(modelProxyName: string, headers: map[string]string, runtime: $RuntimeOptions): DeleteModelProxyResponse {
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
  };
  var params = new OpenApiUtil.Params{
    action = 'DeleteModelProxy',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/model-proxies/${$URL.percentEncode(modelProxyName)}`,
    method = 'DELETE',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 删除模型
 *
 * @return DeleteModelProxyResponse
 */
async function deleteModelProxy(modelProxyName: string): DeleteModelProxyResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return deleteModelProxyWithOptions(modelProxyName, headers, runtime);
}

model DeleteModelServiceResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DeleteModelServiceResult  
}

/**
 * @summary 删除模型
 *
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteModelServiceResponse
 */
@context("deleteModelServiceWithContext")
async function deleteModelServiceWithOptions(modelServiceName: string, headers: map[string]string, runtime: $RuntimeOptions): DeleteModelServiceResponse {
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
  };
  var params = new OpenApiUtil.Params{
    action = 'DeleteModelService',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/model-services/${$URL.percentEncode(modelServiceName)}`,
    method = 'DELETE',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 删除模型
 *
 * @return DeleteModelServiceResponse
 */
async function deleteModelService(modelServiceName: string): DeleteModelServiceResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return deleteModelServiceWithOptions(modelServiceName, headers, runtime);
}

model DeleteSandboxResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DeleteSandboxResult  
}

/**
 * @summary 删除Sandbox
 *
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteSandboxResponse
 */
@context("deleteSandboxWithContext")
async function deleteSandboxWithOptions(sandboxId: string, headers: map[string]string, runtime: $RuntimeOptions): DeleteSandboxResponse {
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
  };
  var params = new OpenApiUtil.Params{
    action = 'DeleteSandbox',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/sandboxes/${$URL.percentEncode(sandboxId)}`,
    method = 'DELETE',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 删除Sandbox
 *
 * @return DeleteSandboxResponse
 */
async function deleteSandbox(sandboxId: string): DeleteSandboxResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return deleteSandboxWithOptions(sandboxId, headers, runtime);
}

model DeleteTemplateResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DeleteTemplateResult  
}

/**
 * @summary 删除模板
 *
 * @description 删除指定的模板。删除后，该模板将无法再用于创建新的沙箱。
 *
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteTemplateResponse
 */
@context("deleteTemplateWithContext")
async function deleteTemplateWithOptions(templateName: string, headers: map[string]string, runtime: $RuntimeOptions): DeleteTemplateResponse {
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
  };
  var params = new OpenApiUtil.Params{
    action = 'DeleteTemplate',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/templates/${$URL.percentEncode(templateName)}`,
    method = 'DELETE',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 删除模板
 *
 * @description 删除指定的模板。删除后，该模板将无法再用于创建新的沙箱。
 *
 * @return DeleteTemplateResponse
 */
async function deleteTemplate(templateName: string): DeleteTemplateResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return deleteTemplateWithOptions(templateName, headers, runtime);
}

model GetAccessTokenRequest {
  resourceId?: string(name='resourceId', example='mod-kw1750tjqs94n9k37o5hjk'),
  resourceName?: string(name='resourceName', example='COUPON_OPERATION_CALLBACK'),
  resourceType?: string(name='resourceType', example='SceneTestingTask'),
}

model GetAccessTokenResponseBody = {
  code?: string(name='code', example='SUCCESS'),
  data?: {
    accessToken?: string(name='accessToken', example='accessToken'),
  }(name='data'),
  requestId?: string(name='requestId', example='F8A0F5F3-0C3E-4C82-9D4F-5E4B6A7C8D9E'),
}

model GetAccessTokenResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetAccessTokenResponseBody(name='body'),
}

/**
 * @summary Get access token for a resource
 *
 * @param request GetAccessTokenRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetAccessTokenResponse
 */
@context("getAccessTokenWithContext")
async function getAccessTokenWithOptions(request: GetAccessTokenRequest, headers: map[string]string, runtime: $RuntimeOptions): GetAccessTokenResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.resourceId)) {
    query['resourceId'] = request.resourceId;
  }
  if (!$isNull(request.resourceName)) {
    query['resourceName'] = request.resourceName;
  }
  if (!$isNull(request.resourceType)) {
    query['resourceType'] = request.resourceType;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetAccessToken',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/accessToken`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Get access token for a resource
 *
 * @param request GetAccessTokenRequest
 * @return GetAccessTokenResponse
 */
async function getAccessToken(request: GetAccessTokenRequest): GetAccessTokenResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getAccessTokenWithOptions(request, headers, runtime);
}

model GetAgentRuntimeRequest {
  agentRuntimeVersion?: string(name='agentRuntimeVersion', description='指定要获取的智能体运行时版本，如果不指定则返回最新版本', example='v1.0.0'),
}

model GetAgentRuntimeResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: AgentRuntimeResult  
}

/**
 * @summary 获取智能体运行时详情
 *
 * @description 根据智能体运行时ID获取指定智能体运行时的详细信息，包括配置、状态、资源使用情况等。
 *
 * @param request GetAgentRuntimeRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetAgentRuntimeResponse
 */
@context("getAgentRuntimeWithContext")
async function getAgentRuntimeWithOptions(agentRuntimeId: string, request: GetAgentRuntimeRequest, headers: map[string]string, runtime: $RuntimeOptions): GetAgentRuntimeResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.agentRuntimeVersion)) {
    query['agentRuntimeVersion'] = request.agentRuntimeVersion;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetAgentRuntime',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/runtimes/${$URL.percentEncode(agentRuntimeId)}`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 获取智能体运行时详情
 *
 * @description 根据智能体运行时ID获取指定智能体运行时的详细信息，包括配置、状态、资源使用情况等。
 *
 * @param request GetAgentRuntimeRequest
 * @return GetAgentRuntimeResponse
 */
async function getAgentRuntime(agentRuntimeId: string, request: GetAgentRuntimeRequest): GetAgentRuntimeResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getAgentRuntimeWithOptions(agentRuntimeId, request, headers, runtime);
}

model GetAgentRuntimeEndpointResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: AgentRuntimeEndpointResult  
}

/**
 * @summary Get an agent runtime endpoint
 *
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetAgentRuntimeEndpointResponse
 */
@context("getAgentRuntimeEndpointWithContext")
async function getAgentRuntimeEndpointWithOptions(agentRuntimeId: string, agentRuntimeEndpointId: string, headers: map[string]string, runtime: $RuntimeOptions): GetAgentRuntimeEndpointResponse {
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
  };
  var params = new OpenApiUtil.Params{
    action = 'GetAgentRuntimeEndpoint',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/runtimes/${$URL.percentEncode(agentRuntimeId)}/endpoints/${$URL.percentEncode(agentRuntimeEndpointId)}`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Get an agent runtime endpoint
 *
 * @return GetAgentRuntimeEndpointResponse
 */
async function getAgentRuntimeEndpoint(agentRuntimeId: string, agentRuntimeEndpointId: string): GetAgentRuntimeEndpointResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getAgentRuntimeEndpointWithOptions(agentRuntimeId, agentRuntimeEndpointId, headers, runtime);
}

model GetBrowserResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: BrowserResult  
}

/**
 * @summary 获取浏览器实例详情
 *
 * @description 根据浏览器ID获取指定浏览器实例的详细信息，包括配置、状态、资源使用情况等。
 *
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetBrowserResponse
 */
@context("getBrowserWithContext")
async function getBrowserWithOptions(browserId: string, headers: map[string]string, runtime: $RuntimeOptions): GetBrowserResponse {
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
  };
  var params = new OpenApiUtil.Params{
    action = 'GetBrowser',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/browsers/${$URL.percentEncode(browserId)}`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 获取浏览器实例详情
 *
 * @description 根据浏览器ID获取指定浏览器实例的详细信息，包括配置、状态、资源使用情况等。
 *
 * @return GetBrowserResponse
 */
async function getBrowser(browserId: string): GetBrowserResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getBrowserWithOptions(browserId, headers, runtime);
}

model GetCodeInterpreterResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CodeInterpreterResult  
}

/**
 * @summary 获取代码解释器详情
 *
 * @description 根据代码解释器ID获取指定代码解释器实例的详细信息，包括配置、状态、资源使用情况等。
 *
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetCodeInterpreterResponse
 */
@context("getCodeInterpreterWithContext")
async function getCodeInterpreterWithOptions(codeInterpreterId: string, headers: map[string]string, runtime: $RuntimeOptions): GetCodeInterpreterResponse {
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
  };
  var params = new OpenApiUtil.Params{
    action = 'GetCodeInterpreter',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/code-interpreters/${$URL.percentEncode(codeInterpreterId)}`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 获取代码解释器详情
 *
 * @description 根据代码解释器ID获取指定代码解释器实例的详细信息，包括配置、状态、资源使用情况等。
 *
 * @return GetCodeInterpreterResponse
 */
async function getCodeInterpreter(codeInterpreterId: string): GetCodeInterpreterResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getCodeInterpreterWithOptions(codeInterpreterId, headers, runtime);
}

model GetCredentialResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CredentialResult  
}

/**
 * @summary Get a credential
 *
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetCredentialResponse
 */
@context("getCredentialWithContext")
async function getCredentialWithOptions(credentialName: string, headers: map[string]string, runtime: $RuntimeOptions): GetCredentialResponse {
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
  };
  var params = new OpenApiUtil.Params{
    action = 'GetCredential',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/credentials/${$URL.percentEncode(credentialName)}`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Get a credential
 *
 * @return GetCredentialResponse
 */
async function getCredential(credentialName: string): GetCredentialResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getCredentialWithOptions(credentialName, headers, runtime);
}

model GetCustomDomainResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CustomDomainResult  
}

/**
 * @summary 获取自定义域名详情
 *
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetCustomDomainResponse
 */
@context("getCustomDomainWithContext")
async function getCustomDomainWithOptions(domainName: string, headers: map[string]string, runtime: $RuntimeOptions): GetCustomDomainResponse {
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
  };
  var params = new OpenApiUtil.Params{
    action = 'GetCustomDomain',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/custom-domains/${$URL.percentEncode(domainName)}`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 获取自定义域名详情
 *
 * @return GetCustomDomainResponse
 */
async function getCustomDomain(domainName: string): GetCustomDomainResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getCustomDomainWithOptions(domainName, headers, runtime);
}

model GetKnowledgeBaseResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: KnowledgeBaseResult  
}

/**
 * @summary 获取知识库
 *
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetKnowledgeBaseResponse
 */
@context("getKnowledgeBaseWithContext")
async function getKnowledgeBaseWithOptions(knowledgeBaseName: string, headers: map[string]string, runtime: $RuntimeOptions): GetKnowledgeBaseResponse {
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
  };
  var params = new OpenApiUtil.Params{
    action = 'GetKnowledgeBase',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/knowledgebases/${$URL.percentEncode(knowledgeBaseName)}`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 获取知识库
 *
 * @return GetKnowledgeBaseResponse
 */
async function getKnowledgeBase(knowledgeBaseName: string): GetKnowledgeBaseResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getKnowledgeBaseWithOptions(knowledgeBaseName, headers, runtime);
}

model GetMemoryCollectionResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: MemoryCollectionResult  
}

/**
 * @summary 查询记忆存储详情
 *
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetMemoryCollectionResponse
 */
@context("getMemoryCollectionWithContext")
async function getMemoryCollectionWithOptions(memoryCollectionName: string, headers: map[string]string, runtime: $RuntimeOptions): GetMemoryCollectionResponse {
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
  };
  var params = new OpenApiUtil.Params{
    action = 'GetMemoryCollection',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/memory-collections/${$URL.percentEncode(memoryCollectionName)}`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 查询记忆存储详情
 *
 * @return GetMemoryCollectionResponse
 */
async function getMemoryCollection(memoryCollectionName: string): GetMemoryCollectionResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getMemoryCollectionWithOptions(memoryCollectionName, headers, runtime);
}

model GetModelProxyResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ModelProxyResult  
}

/**
 * @summary 查看model
 *
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetModelProxyResponse
 */
@context("getModelProxyWithContext")
async function getModelProxyWithOptions(modelProxyName: string, headers: map[string]string, runtime: $RuntimeOptions): GetModelProxyResponse {
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
  };
  var params = new OpenApiUtil.Params{
    action = 'GetModelProxy',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/model-proxies/${$URL.percentEncode(modelProxyName)}`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 查看model
 *
 * @return GetModelProxyResponse
 */
async function getModelProxy(modelProxyName: string): GetModelProxyResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getModelProxyWithOptions(modelProxyName, headers, runtime);
}

model GetModelServiceResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ModelServiceResult  
}

/**
 * @summary 查看model
 *
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetModelServiceResponse
 */
@context("getModelServiceWithContext")
async function getModelServiceWithOptions(modelServiceName: string, headers: map[string]string, runtime: $RuntimeOptions): GetModelServiceResponse {
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
  };
  var params = new OpenApiUtil.Params{
    action = 'GetModelService',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/model-services/${$URL.percentEncode(modelServiceName)}`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 查看model
 *
 * @return GetModelServiceResponse
 */
async function getModelService(modelServiceName: string): GetModelServiceResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getModelServiceWithOptions(modelServiceName, headers, runtime);
}

model GetSandboxResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: SandboxResult  
}

/**
 * @summary 获取沙箱
 *
 * @description 根据沙箱ID获取指定沙箱的详细信息，包括状态、配置等。
 *
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetSandboxResponse
 */
@context("getSandboxWithContext")
async function getSandboxWithOptions(sandboxId: string, headers: map[string]string, runtime: $RuntimeOptions): GetSandboxResponse {
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
  };
  var params = new OpenApiUtil.Params{
    action = 'GetSandbox',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/sandboxes/${$URL.percentEncode(sandboxId)}`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 获取沙箱
 *
 * @description 根据沙箱ID获取指定沙箱的详细信息，包括状态、配置等。
 *
 * @return GetSandboxResponse
 */
async function getSandbox(sandboxId: string): GetSandboxResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getSandboxWithOptions(sandboxId, headers, runtime);
}

model GetTemplateResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: TemplateResult  
}

/**
 * @summary 获取模板
 *
 * @description 根据模板名称获取指定模板的详细信息，包括配置、状态等。
 *
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetTemplateResponse
 */
@context("getTemplateWithContext")
async function getTemplateWithOptions(templateName: string, headers: map[string]string, runtime: $RuntimeOptions): GetTemplateResponse {
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
  };
  var params = new OpenApiUtil.Params{
    action = 'GetTemplate',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/templates/${$URL.percentEncode(templateName)}`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 获取模板
 *
 * @description 根据模板名称获取指定模板的详细信息，包括配置、状态等。
 *
 * @return GetTemplateResponse
 */
async function getTemplate(templateName: string): GetTemplateResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getTemplateWithOptions(templateName, headers, runtime);
}

model ListAgentRuntimeEndpointsRequest {
  endpointName?: string(name='endpointName', description='根据端点名称进行模糊匹配过滤', example='my-endpoint'),
  pageNumber?: int32(name='pageNumber', description='当前页码，从1开始计数', example='1'),
  pageSize?: int32(name='pageSize', description='每页返回的记录数量', example='10'),
  searchMode?: string(name='searchMode', description='查询模式，支持精确查询和模糊查询', example='fuzzy'),
}

model ListAgentRuntimeEndpointsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListAgentRuntimeEndpointsResult  
}

/**
 * @summary 列出智能体运行时端点
 *
 * @description 获取指定智能体运行时的所有端点列表，支持按名称过滤和分页查询。端点用于外部系统访问智能体运行时服务。
 *
 * @param request ListAgentRuntimeEndpointsRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListAgentRuntimeEndpointsResponse
 */
@context("listAgentRuntimeEndpointsWithContext")
async function listAgentRuntimeEndpointsWithOptions(agentRuntimeId: string, request: ListAgentRuntimeEndpointsRequest, headers: map[string]string, runtime: $RuntimeOptions): ListAgentRuntimeEndpointsResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.endpointName)) {
    query['endpointName'] = request.endpointName;
  }
  if (!$isNull(request.pageNumber)) {
    query['pageNumber'] = request.pageNumber;
  }
  if (!$isNull(request.pageSize)) {
    query['pageSize'] = request.pageSize;
  }
  if (!$isNull(request.searchMode)) {
    query['searchMode'] = request.searchMode;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListAgentRuntimeEndpoints',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/runtimes/${$URL.percentEncode(agentRuntimeId)}/endpoints`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 列出智能体运行时端点
 *
 * @description 获取指定智能体运行时的所有端点列表，支持按名称过滤和分页查询。端点用于外部系统访问智能体运行时服务。
 *
 * @param request ListAgentRuntimeEndpointsRequest
 * @return ListAgentRuntimeEndpointsResponse
 */
async function listAgentRuntimeEndpoints(agentRuntimeId: string, request: ListAgentRuntimeEndpointsRequest): ListAgentRuntimeEndpointsResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listAgentRuntimeEndpointsWithOptions(agentRuntimeId, request, headers, runtime);
}

model ListAgentRuntimeVersionsRequest {
  pageNumber?: int32(name='pageNumber', description='当前页码，从1开始计数', example='1'),
  pageSize?: int32(name='pageSize', description='每页返回的记录数量', example='10'),
}

model ListAgentRuntimeVersionsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListAgentRuntimeVersionsResult  
}

/**
 * @summary List agent runtime versions
 *
 * @param request ListAgentRuntimeVersionsRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListAgentRuntimeVersionsResponse
 */
@context("listAgentRuntimeVersionsWithContext")
async function listAgentRuntimeVersionsWithOptions(agentRuntimeId: string, request: ListAgentRuntimeVersionsRequest, headers: map[string]string, runtime: $RuntimeOptions): ListAgentRuntimeVersionsResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.pageNumber)) {
    query['pageNumber'] = request.pageNumber;
  }
  if (!$isNull(request.pageSize)) {
    query['pageSize'] = request.pageSize;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListAgentRuntimeVersions',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/runtimes/${$URL.percentEncode(agentRuntimeId)}/versions`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary List agent runtime versions
 *
 * @param request ListAgentRuntimeVersionsRequest
 * @return ListAgentRuntimeVersionsResponse
 */
async function listAgentRuntimeVersions(agentRuntimeId: string, request: ListAgentRuntimeVersionsRequest): ListAgentRuntimeVersionsResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listAgentRuntimeVersionsWithOptions(agentRuntimeId, request, headers, runtime);
}

model ListAgentRuntimesRequest {
  agentRuntimeName?: string(name='agentRuntimeName', description='根据智能体运行时名称进行模糊匹配过滤', example='my-runtime'),
  pageNumber?: int32(name='pageNumber', description='当前页码，从1开始计数', example='1'),
  pageSize?: int32(name='pageSize', description='每页返回的记录数量', example='10'),
  searchMode?: string(name='searchMode', description='查询模式，支持精确查询和模糊查询', example='fuzzy'),
  status?: string(name='status', description='根据状态进行过滤，多个状态用逗号分隔，支持精确匹配', example='READY,CREATING'),
}

model ListAgentRuntimesResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListAgentRuntimesResult  
}

/**
 * @summary 列出智能体运行时
 *
 * @description 获取当前用户的所有智能体运行时列表，支持按名称、标签等条件过滤，支持分页查询。
 *
 * @param request ListAgentRuntimesRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListAgentRuntimesResponse
 */
@context("listAgentRuntimesWithContext")
async function listAgentRuntimesWithOptions(request: ListAgentRuntimesRequest, headers: map[string]string, runtime: $RuntimeOptions): ListAgentRuntimesResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.agentRuntimeName)) {
    query['agentRuntimeName'] = request.agentRuntimeName;
  }
  if (!$isNull(request.pageNumber)) {
    query['pageNumber'] = request.pageNumber;
  }
  if (!$isNull(request.pageSize)) {
    query['pageSize'] = request.pageSize;
  }
  if (!$isNull(request.searchMode)) {
    query['searchMode'] = request.searchMode;
  }
  if (!$isNull(request.status)) {
    query['status'] = request.status;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListAgentRuntimes',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/runtimes`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 列出智能体运行时
 *
 * @description 获取当前用户的所有智能体运行时列表，支持按名称、标签等条件过滤，支持分页查询。
 *
 * @param request ListAgentRuntimesRequest
 * @return ListAgentRuntimesResponse
 */
async function listAgentRuntimes(request: ListAgentRuntimesRequest): ListAgentRuntimesResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listAgentRuntimesWithOptions(request, headers, runtime);
}

model ListBrowsersRequest {
  browserName?: string(name='browserName', description='根据浏览器实例名称进行模糊匹配过滤', example='browser'),
  pageNumber?: int32(name='pageNumber', description='当前页码，从1开始计数', example='10'),
  pageSize?: int32(name='pageSize', description='每页返回的记录数量', example='1'),
  status?: string(name='status', description='根据浏览器实例的运行状态进行过滤，可选值：CREATING、READY、DELETING等', example='CREATING', nullable=true),
}

model ListBrowsersResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListBrowsersResult  
}

/**
 * @summary 列出浏览器实例
 *
 * @description 获取当前用户的所有浏览器实例列表，支持按名称、状态等条件过滤，支持分页查询。
 *
 * @param request ListBrowsersRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListBrowsersResponse
 */
@context("listBrowsersWithContext")
async function listBrowsersWithOptions(request: ListBrowsersRequest, headers: map[string]string, runtime: $RuntimeOptions): ListBrowsersResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.browserName)) {
    query['browserName'] = request.browserName;
  }
  if (!$isNull(request.pageNumber)) {
    query['pageNumber'] = request.pageNumber;
  }
  if (!$isNull(request.pageSize)) {
    query['pageSize'] = request.pageSize;
  }
  if (!$isNull(request.status)) {
    query['status'] = request.status;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListBrowsers',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/browsers`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 列出浏览器实例
 *
 * @description 获取当前用户的所有浏览器实例列表，支持按名称、状态等条件过滤，支持分页查询。
 *
 * @param request ListBrowsersRequest
 * @return ListBrowsersResponse
 */
async function listBrowsers(request: ListBrowsersRequest): ListBrowsersResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listBrowsersWithOptions(request, headers, runtime);
}

model ListCodeInterpretersRequest {
  codeInterpreterName?: string(name='codeInterpreterName', description='根据代码解释器实例名称进行模糊匹配过滤', example='code'),
  pageNumber?: int32(name='pageNumber', description='当前页码，从1开始计数', example='1'),
  pageSize?: int32(name='pageSize', description='每页返回的记录数量', example='10'),
}

model ListCodeInterpretersResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListCodeInterpretersResult  
}

/**
 * @summary 列出代码解释器
 *
 * @description 获取当前用户的所有代码解释器实例列表，支持按名称等条件过滤，支持分页查询。
 *
 * @param request ListCodeInterpretersRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListCodeInterpretersResponse
 */
@context("listCodeInterpretersWithContext")
async function listCodeInterpretersWithOptions(request: ListCodeInterpretersRequest, headers: map[string]string, runtime: $RuntimeOptions): ListCodeInterpretersResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.codeInterpreterName)) {
    query['codeInterpreterName'] = request.codeInterpreterName;
  }
  if (!$isNull(request.pageNumber)) {
    query['pageNumber'] = request.pageNumber;
  }
  if (!$isNull(request.pageSize)) {
    query['pageSize'] = request.pageSize;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListCodeInterpreters',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/code-interpreters`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 列出代码解释器
 *
 * @description 获取当前用户的所有代码解释器实例列表，支持按名称等条件过滤，支持分页查询。
 *
 * @param request ListCodeInterpretersRequest
 * @return ListCodeInterpretersResponse
 */
async function listCodeInterpreters(request: ListCodeInterpretersRequest): ListCodeInterpretersResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listCodeInterpretersWithOptions(request, headers, runtime);
}

model ListCredentialsRequest {
  credentialAuthType?: string(name='credentialAuthType', description='credentialAuthType', example='credentialAuthType'),
  credentialName?: string(name='credentialName', description='credentialName', example='credentialName'),
  credentialSourceType?: string(name='credentialSourceType', description='credentialSourceType', example='credentialSourceType'),
  enabled?: boolean(name='enabled', example='False'),
  pageNumber?: int32(name='pageNumber', example='1'),
  pageSize?: int32(name='pageSize', example='0'),
  provider?: string(name='provider', example='Aliyun'),
}

model ListCredentialsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListCredentialsResult  
}

/**
 * @summary List credentials
 *
 * @param request ListCredentialsRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListCredentialsResponse
 */
@context("listCredentialsWithContext")
async function listCredentialsWithOptions(request: ListCredentialsRequest, headers: map[string]string, runtime: $RuntimeOptions): ListCredentialsResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.credentialAuthType)) {
    query['credentialAuthType'] = request.credentialAuthType;
  }
  if (!$isNull(request.credentialName)) {
    query['credentialName'] = request.credentialName;
  }
  if (!$isNull(request.credentialSourceType)) {
    query['credentialSourceType'] = request.credentialSourceType;
  }
  if (!$isNull(request.enabled)) {
    query['enabled'] = request.enabled;
  }
  if (!$isNull(request.pageNumber)) {
    query['pageNumber'] = request.pageNumber;
  }
  if (!$isNull(request.pageSize)) {
    query['pageSize'] = request.pageSize;
  }
  if (!$isNull(request.provider)) {
    query['provider'] = request.provider;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListCredentials',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/credentials`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary List credentials
 *
 * @param request ListCredentialsRequest
 * @return ListCredentialsResponse
 */
async function listCredentials(request: ListCredentialsRequest): ListCredentialsResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listCredentialsWithOptions(request, headers, runtime);
}

model ListCustomDomainsRequest {
  domainName?: string(name='domainName', example='mydomain.com'),
  pageNumber?: int32(name='pageNumber', example='1'),
  pageSize?: int32(name='pageSize', example='20'),
  resourceName?: string(name='resourceName', example='agent-1'),
  resourceType?: string(name='resourceType', example='runtime'),
}

model ListCustomDomainsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListCustomDomainsResult  
}

/**
 * @summary 自定义域名列表
 *
 * @param request ListCustomDomainsRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListCustomDomainsResponse
 */
@context("listCustomDomainsWithContext")
async function listCustomDomainsWithOptions(request: ListCustomDomainsRequest, headers: map[string]string, runtime: $RuntimeOptions): ListCustomDomainsResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.domainName)) {
    query['domainName'] = request.domainName;
  }
  if (!$isNull(request.pageNumber)) {
    query['pageNumber'] = request.pageNumber;
  }
  if (!$isNull(request.pageSize)) {
    query['pageSize'] = request.pageSize;
  }
  if (!$isNull(request.resourceName)) {
    query['resourceName'] = request.resourceName;
  }
  if (!$isNull(request.resourceType)) {
    query['resourceType'] = request.resourceType;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListCustomDomains',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/custom-domains`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 自定义域名列表
 *
 * @param request ListCustomDomainsRequest
 * @return ListCustomDomainsResponse
 */
async function listCustomDomains(request: ListCustomDomainsRequest): ListCustomDomainsResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listCustomDomainsWithOptions(request, headers, runtime);
}

model ListKnowledgeBasesRequest {
  pageNumber?: int32(name='pageNumber', example='1'),
  pageSize?: int32(name='pageSize', example='20'),
  provider?: string(name='provider', example='bailian'),
}

model ListKnowledgeBasesResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListKnowledgeBasesResult  
}

/**
 * @summary 列出知识库
 *
 * @param request ListKnowledgeBasesRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListKnowledgeBasesResponse
 */
@context("listKnowledgeBasesWithContext")
async function listKnowledgeBasesWithOptions(request: ListKnowledgeBasesRequest, headers: map[string]string, runtime: $RuntimeOptions): ListKnowledgeBasesResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.pageNumber)) {
    query['pageNumber'] = request.pageNumber;
  }
  if (!$isNull(request.pageSize)) {
    query['pageSize'] = request.pageSize;
  }
  if (!$isNull(request.provider)) {
    query['provider'] = request.provider;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListKnowledgeBases',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/knowledgebases`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 列出知识库
 *
 * @param request ListKnowledgeBasesRequest
 * @return ListKnowledgeBasesResponse
 */
async function listKnowledgeBases(request: ListKnowledgeBasesRequest): ListKnowledgeBasesResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listKnowledgeBasesWithOptions(request, headers, runtime);
}

model ListMemoryCollectionsRequest {
  memoryCollectionName?: string(name='memoryCollectionName', example='my-memory-1'),
  pageNumber?: int32(name='pageNumber', example='1'),
  pageSize?: int32(name='pageSize', example='20'),
  status?: string(name='status', example='READY'),
  type?: string(name='type', example='mem0'),
}

model ListMemoryCollectionsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListMemoryCollectionsResult  
}

/**
 * @summary 查询记忆存储列表
 *
 * @param request ListMemoryCollectionsRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListMemoryCollectionsResponse
 */
@context("listMemoryCollectionsWithContext")
async function listMemoryCollectionsWithOptions(request: ListMemoryCollectionsRequest, headers: map[string]string, runtime: $RuntimeOptions): ListMemoryCollectionsResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.memoryCollectionName)) {
    query['memoryCollectionName'] = request.memoryCollectionName;
  }
  if (!$isNull(request.pageNumber)) {
    query['pageNumber'] = request.pageNumber;
  }
  if (!$isNull(request.pageSize)) {
    query['pageSize'] = request.pageSize;
  }
  if (!$isNull(request.status)) {
    query['status'] = request.status;
  }
  if (!$isNull(request.type)) {
    query['type'] = request.type;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListMemoryCollections',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/memory-collections`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 查询记忆存储列表
 *
 * @param request ListMemoryCollectionsRequest
 * @return ListMemoryCollectionsResponse
 */
async function listMemoryCollections(request: ListMemoryCollectionsRequest): ListMemoryCollectionsResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listMemoryCollectionsWithOptions(request, headers, runtime);
}

model ListModelProvidersRequest {
  modelName?: string(name='modelName', example='aa'),
  modelType?: string(name='modelType', example='pop'),
  pageNumber?: string(name='pageNumber', example='1'),
  pageSize?: string(name='pageSize', example='10'),
  provider?: string(name='provider', example='Aliyun'),
}

model ListModelProvidersResponseBody = {
  code?: string(name='code', example='SUCCESS'),
  data?: {
    items?: [ 
      {
        baseUrl?: string(name='baseUrl', description='baseUrl', example='baseUrl'),
        modelInfoConfig?: ModelInfoConfig(name='modelInfoConfig', description='modelInfoConfig'),
        modelType?: string(name='modelType', example='native'),
        models?: [ string ](name='models'),
        providerName?: string(name='providerName', example='providerName'),
      }
    ](name='items'),
    pageNumber?: int32(name='pageNumber', example='1'),
    pageSize?: int32(name='pageSize', example='10'),
    total?: long(name='total', example='211'),
  }(name='data'),
  requestId?: string(name='requestId', example='F8A0F5F3-0C3E-4C82-9D4F-5E4B6A7C8D9E'),
}

model ListModelProvidersResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListModelProvidersResponseBody(name='body'),
}

/**
 * @summary 查询支持的模型提供商及其模型
 *
 * @param request ListModelProvidersRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListModelProvidersResponse
 */
@context("listModelProvidersWithContext")
async function listModelProvidersWithOptions(request: ListModelProvidersRequest, headers: map[string]string, runtime: $RuntimeOptions): ListModelProvidersResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.modelName)) {
    query['modelName'] = request.modelName;
  }
  if (!$isNull(request.modelType)) {
    query['modelType'] = request.modelType;
  }
  if (!$isNull(request.pageNumber)) {
    query['pageNumber'] = request.pageNumber;
  }
  if (!$isNull(request.pageSize)) {
    query['pageSize'] = request.pageSize;
  }
  if (!$isNull(request.provider)) {
    query['provider'] = request.provider;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListModelProviders',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/model-providers`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 查询支持的模型提供商及其模型
 *
 * @param request ListModelProvidersRequest
 * @return ListModelProvidersResponse
 */
async function listModelProviders(request: ListModelProvidersRequest): ListModelProvidersResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listModelProvidersWithOptions(request, headers, runtime);
}

model ListModelProxiesRequest {
  pageNumber?: int32(name='pageNumber', example='1'),
  pageSize?: int32(name='pageSize', example='10'),
  proxyMode?: string(name='proxyMode', description='proxyMode', example='proxyMode'),
  status?: string(name='status', example='CREATING'),
}

model ListModelProxiesResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListModelProxiesResult  
}

/**
 * @summary model列表
 *
 * @param request ListModelProxiesRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListModelProxiesResponse
 */
@context("listModelProxiesWithContext")
async function listModelProxiesWithOptions(request: ListModelProxiesRequest, headers: map[string]string, runtime: $RuntimeOptions): ListModelProxiesResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.pageNumber)) {
    query['pageNumber'] = request.pageNumber;
  }
  if (!$isNull(request.pageSize)) {
    query['pageSize'] = request.pageSize;
  }
  if (!$isNull(request.proxyMode)) {
    query['proxyMode'] = request.proxyMode;
  }
  if (!$isNull(request.status)) {
    query['status'] = request.status;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListModelProxies',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/model-proxies`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary model列表
 *
 * @param request ListModelProxiesRequest
 * @return ListModelProxiesResponse
 */
async function listModelProxies(request: ListModelProxiesRequest): ListModelProxiesResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listModelProxiesWithOptions(request, headers, runtime);
}

model ListModelServicesRequest {
  modelType?: string(name='modelType', description='This parameter is required.', example='system'),
  pageNumber?: int32(name='pageNumber', example='10'),
  pageSize?: int32(name='pageSize', example='10'),
  provider?: string(name='provider', example='Aliyun'),
  providerType?: string(name='providerType', example='providerType'),
}

model ListModelServicesResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListModelServicesResult  
}

/**
 * @summary model列表
 *
 * @param request ListModelServicesRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListModelServicesResponse
 */
@context("listModelServicesWithContext")
async function listModelServicesWithOptions(request: ListModelServicesRequest, headers: map[string]string, runtime: $RuntimeOptions): ListModelServicesResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.modelType)) {
    query['modelType'] = request.modelType;
  }
  if (!$isNull(request.pageNumber)) {
    query['pageNumber'] = request.pageNumber;
  }
  if (!$isNull(request.pageSize)) {
    query['pageSize'] = request.pageSize;
  }
  if (!$isNull(request.provider)) {
    query['provider'] = request.provider;
  }
  if (!$isNull(request.providerType)) {
    query['providerType'] = request.providerType;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListModelServices',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/model-services`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary model列表
 *
 * @param request ListModelServicesRequest
 * @return ListModelServicesResponse
 */
async function listModelServices(request: ListModelServicesRequest): ListModelServicesResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listModelServicesWithOptions(request, headers, runtime);
}

model ListSandboxesRequest {
  maxResults?: int32(name='maxResults', description='当前页码，从1开始计数', example='1000'),
  nextToken?: string(name='nextToken', example='dnLkmeaJc9vHgbzREh2l0J4dD+2BRJj42DLT6GrZysw='),
  status?: string(name='status', example='CREATING'),
  templateName?: string(name='templateName', description='按模板名称过滤', example='templateName'),
  templateType?: string(name='templateType', example='TASK'),
}

model ListSandboxesResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListSandboxesResult  
}

/**
 * @summary 列出沙箱
 *
 * @description 获取当前用户的所有沙箱列表，支持按模板名称过滤，支持分页查询。
 *
 * @param request ListSandboxesRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListSandboxesResponse
 */
@context("listSandboxesWithContext")
async function listSandboxesWithOptions(request: ListSandboxesRequest, headers: map[string]string, runtime: $RuntimeOptions): ListSandboxesResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.maxResults)) {
    query['maxResults'] = request.maxResults;
  }
  if (!$isNull(request.nextToken)) {
    query['nextToken'] = request.nextToken;
  }
  if (!$isNull(request.status)) {
    query['status'] = request.status;
  }
  if (!$isNull(request.templateName)) {
    query['templateName'] = request.templateName;
  }
  if (!$isNull(request.templateType)) {
    query['templateType'] = request.templateType;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListSandboxes',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/sandboxes`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 列出沙箱
 *
 * @description 获取当前用户的所有沙箱列表，支持按模板名称过滤，支持分页查询。
 *
 * @param request ListSandboxesRequest
 * @return ListSandboxesResponse
 */
async function listSandboxes(request: ListSandboxesRequest): ListSandboxesResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listSandboxesWithOptions(request, headers, runtime);
}

model ListTemplatesRequest {
  pageNumber?: int32(name='pageNumber', description='当前页码，从1开始计数', example='1'),
  pageSize?: int32(name='pageSize', description='每页返回的记录数量', example='20'),
  status?: string(name='status', example='READY'),
  templateName?: string(name='templateName', example='temp-abc'),
  templateType?: string(name='templateType', description='按模板类型过滤', example='Browser'),
}

model ListTemplatesResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListTemplatesResult  
}

/**
 * @summary 列出模板
 *
 * @description 获取当前用户的所有模板列表，支持按模板类型过滤，支持分页查询。
 *
 * @param request ListTemplatesRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListTemplatesResponse
 */
@context("listTemplatesWithContext")
async function listTemplatesWithOptions(request: ListTemplatesRequest, headers: map[string]string, runtime: $RuntimeOptions): ListTemplatesResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.pageNumber)) {
    query['pageNumber'] = request.pageNumber;
  }
  if (!$isNull(request.pageSize)) {
    query['pageSize'] = request.pageSize;
  }
  if (!$isNull(request.status)) {
    query['status'] = request.status;
  }
  if (!$isNull(request.templateName)) {
    query['templateName'] = request.templateName;
  }
  if (!$isNull(request.templateType)) {
    query['templateType'] = request.templateType;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListTemplates',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/templates`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 列出模板
 *
 * @description 获取当前用户的所有模板列表，支持按模板类型过滤，支持分页查询。
 *
 * @param request ListTemplatesRequest
 * @return ListTemplatesResponse
 */
async function listTemplates(request: ListTemplatesRequest): ListTemplatesResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listTemplatesWithOptions(request, headers, runtime);
}

model PublishRuntimeVersionRequest {
  body?: PublishRuntimeVersionInput(name='body'),
}

model PublishRuntimeVersionResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: AgentRuntimeVersionResult  
}

/**
 * @summary 发布运行时版本
 *
 * @description 为指定的智能体运行时发布新版本，用于版本管理和部署。新版本可以包含代码更新、配置变更等内容。
 *
 * @param request PublishRuntimeVersionRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return PublishRuntimeVersionResponse
 */
@context("publishRuntimeVersionWithContext")
async function publishRuntimeVersionWithOptions(agentRuntimeId: string, request: PublishRuntimeVersionRequest, headers: map[string]string, runtime: $RuntimeOptions): PublishRuntimeVersionResponse {
  request.validate();
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(request.body),
  };
  var params = new OpenApiUtil.Params{
    action = 'PublishRuntimeVersion',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/runtimes/${$URL.percentEncode(agentRuntimeId)}/versions`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 发布运行时版本
 *
 * @description 为指定的智能体运行时发布新版本，用于版本管理和部署。新版本可以包含代码更新、配置变更等内容。
 *
 * @param request PublishRuntimeVersionRequest
 * @return PublishRuntimeVersionResponse
 */
async function publishRuntimeVersion(agentRuntimeId: string, request: PublishRuntimeVersionRequest): PublishRuntimeVersionResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return publishRuntimeVersionWithOptions(agentRuntimeId, request, headers, runtime);
}

model StopSandboxResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: StopSandboxResult  
}

/**
 * @summary 删除沙箱
 *
 * @description 停止指定的沙箱实例。停止后，沙箱将进入TERMINATED状态。
 *
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return StopSandboxResponse
 */
@context("stopSandboxWithContext")
async function stopSandboxWithOptions(sandboxId: string, headers: map[string]string, runtime: $RuntimeOptions): StopSandboxResponse {
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
  };
  var params = new OpenApiUtil.Params{
    action = 'StopSandbox',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/sandboxes/${$URL.percentEncode(sandboxId)}/stop`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 删除沙箱
 *
 * @description 停止指定的沙箱实例。停止后，沙箱将进入TERMINATED状态。
 *
 * @return StopSandboxResponse
 */
async function stopSandbox(sandboxId: string): StopSandboxResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return stopSandboxWithOptions(sandboxId, headers, runtime);
}

model StopTemplateMCPResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: TemplateResult  
}

/**
 * @summary 停止模板的MCP服务器
 *
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return StopTemplateMCPResponse
 */
@context("stopTemplateMCPWithContext")
async function stopTemplateMCPWithOptions(templateName: string, headers: map[string]string, runtime: $RuntimeOptions): StopTemplateMCPResponse {
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
  };
  var params = new OpenApiUtil.Params{
    action = 'StopTemplateMCP',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/templates/${$URL.percentEncode(templateName)}/mcp/stop`,
    method = 'PATCH',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 停止模板的MCP服务器
 *
 * @return StopTemplateMCPResponse
 */
async function stopTemplateMCP(templateName: string): StopTemplateMCPResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return stopTemplateMCPWithOptions(templateName, headers, runtime);
}

model UpdateAgentRuntimeRequest {
  body?: UpdateAgentRuntimeInput(name='body', description='包含要更新的智能体运行时配置信息的请求体

This parameter is required.'),
}

model UpdateAgentRuntimeResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: AgentRuntimeResult  
}

/**
 * @summary 更新智能体运行时
 *
 * @description 更新指定智能体运行时的配置信息，包括资源分配、网络配置、环境变量等。更新操作会触发运行时重启。
 *
 * @param request UpdateAgentRuntimeRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateAgentRuntimeResponse
 */
@context("updateAgentRuntimeWithContext")
async function updateAgentRuntimeWithOptions(agentRuntimeId: string, request: UpdateAgentRuntimeRequest, headers: map[string]string, runtime: $RuntimeOptions): UpdateAgentRuntimeResponse {
  request.validate();
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(request.body),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpdateAgentRuntime',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/runtimes/${$URL.percentEncode(agentRuntimeId)}`,
    method = 'PUT',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 更新智能体运行时
 *
 * @description 更新指定智能体运行时的配置信息，包括资源分配、网络配置、环境变量等。更新操作会触发运行时重启。
 *
 * @param request UpdateAgentRuntimeRequest
 * @return UpdateAgentRuntimeResponse
 */
async function updateAgentRuntime(agentRuntimeId: string, request: UpdateAgentRuntimeRequest): UpdateAgentRuntimeResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return updateAgentRuntimeWithOptions(agentRuntimeId, request, headers, runtime);
}

model UpdateAgentRuntimeEndpointRequest {
  body?: UpdateAgentRuntimeEndpointInput(name='body', description='包含要更新的智能体运行时端点配置信息的请求体

This parameter is required.'),
}

model UpdateAgentRuntimeEndpointResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: AgentRuntimeEndpointResult  
}

/**
 * @summary Update an agent runtime endpoint
 *
 * @param request UpdateAgentRuntimeEndpointRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateAgentRuntimeEndpointResponse
 */
@context("updateAgentRuntimeEndpointWithContext")
async function updateAgentRuntimeEndpointWithOptions(agentRuntimeId: string, agentRuntimeEndpointId: string, request: UpdateAgentRuntimeEndpointRequest, headers: map[string]string, runtime: $RuntimeOptions): UpdateAgentRuntimeEndpointResponse {
  request.validate();
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(request.body),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpdateAgentRuntimeEndpoint',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/runtimes/${$URL.percentEncode(agentRuntimeId)}/endpoints/${$URL.percentEncode(agentRuntimeEndpointId)}`,
    method = 'PUT',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Update an agent runtime endpoint
 *
 * @param request UpdateAgentRuntimeEndpointRequest
 * @return UpdateAgentRuntimeEndpointResponse
 */
async function updateAgentRuntimeEndpoint(agentRuntimeId: string, agentRuntimeEndpointId: string, request: UpdateAgentRuntimeEndpointRequest): UpdateAgentRuntimeEndpointResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return updateAgentRuntimeEndpointWithOptions(agentRuntimeId, agentRuntimeEndpointId, request, headers, runtime);
}

model UpdateCredentialRequest {
  body?: UpdateCredentialInput(name='body'),
}

model UpdateCredentialResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CredentialResult  
}

/**
 * @summary Update a credential
 *
 * @param request UpdateCredentialRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateCredentialResponse
 */
@context("updateCredentialWithContext")
async function updateCredentialWithOptions(credentialName: string, request: UpdateCredentialRequest, headers: map[string]string, runtime: $RuntimeOptions): UpdateCredentialResponse {
  request.validate();
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(request.body),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpdateCredential',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/credentials/${$URL.percentEncode(credentialName)}`,
    method = 'PUT',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Update a credential
 *
 * @param request UpdateCredentialRequest
 * @return UpdateCredentialResponse
 */
async function updateCredential(credentialName: string, request: UpdateCredentialRequest): UpdateCredentialResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return updateCredentialWithOptions(credentialName, request, headers, runtime);
}

model UpdateCustomDomainRequest {
  body?: UpdateCustomDomainInput(name='body'),
}

model UpdateCustomDomainResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CustomDomainResult  
}

/**
 * @summary 更新自定义域名
 *
 * @param request UpdateCustomDomainRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateCustomDomainResponse
 */
@context("updateCustomDomainWithContext")
async function updateCustomDomainWithOptions(domainName: string, request: UpdateCustomDomainRequest, headers: map[string]string, runtime: $RuntimeOptions): UpdateCustomDomainResponse {
  request.validate();
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(request.body),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpdateCustomDomain',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/custom-domains/${$URL.percentEncode(domainName)}`,
    method = 'PUT',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 更新自定义域名
 *
 * @param request UpdateCustomDomainRequest
 * @return UpdateCustomDomainResponse
 */
async function updateCustomDomain(domainName: string, request: UpdateCustomDomainRequest): UpdateCustomDomainResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return updateCustomDomainWithOptions(domainName, request, headers, runtime);
}

model UpdateKnowledgeBaseRequest {
  body?: UpdateKnowledgeBaseInput(name='body'),
}

model UpdateKnowledgeBaseResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: KnowledgeBaseResult  
}

/**
 * @summary 更新知识库
 *
 * @param request UpdateKnowledgeBaseRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateKnowledgeBaseResponse
 */
@context("updateKnowledgeBaseWithContext")
async function updateKnowledgeBaseWithOptions(knowledgeBaseName: string, request: UpdateKnowledgeBaseRequest, headers: map[string]string, runtime: $RuntimeOptions): UpdateKnowledgeBaseResponse {
  request.validate();
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(request.body),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpdateKnowledgeBase',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/knowledgebases/${$URL.percentEncode(knowledgeBaseName)}`,
    method = 'PUT',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 更新知识库
 *
 * @param request UpdateKnowledgeBaseRequest
 * @return UpdateKnowledgeBaseResponse
 */
async function updateKnowledgeBase(knowledgeBaseName: string, request: UpdateKnowledgeBaseRequest): UpdateKnowledgeBaseResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return updateKnowledgeBaseWithOptions(knowledgeBaseName, request, headers, runtime);
}

model UpdateMemoryCollectionRequest {
  body?: UpdateMemoryCollectionInput(name='body'),
}

model UpdateMemoryCollectionResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: MemoryCollectionResult  
}

/**
 * @summary 修改记忆存储信息
 *
 * @param request UpdateMemoryCollectionRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateMemoryCollectionResponse
 */
@context("updateMemoryCollectionWithContext")
async function updateMemoryCollectionWithOptions(memoryCollectionName: string, request: UpdateMemoryCollectionRequest, headers: map[string]string, runtime: $RuntimeOptions): UpdateMemoryCollectionResponse {
  request.validate();
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(request.body),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpdateMemoryCollection',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/memory-collections/${$URL.percentEncode(memoryCollectionName)}`,
    method = 'PUT',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 修改记忆存储信息
 *
 * @param request UpdateMemoryCollectionRequest
 * @return UpdateMemoryCollectionResponse
 */
async function updateMemoryCollection(memoryCollectionName: string, request: UpdateMemoryCollectionRequest): UpdateMemoryCollectionResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return updateMemoryCollectionWithOptions(memoryCollectionName, request, headers, runtime);
}

model UpdateModelProxyRequest {
  body?: UpdateModelProxyInput(name='body'),
}

model UpdateModelProxyResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ModelProxyResult  
}

/**
 * @summary 更新模型
 *
 * @param request UpdateModelProxyRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateModelProxyResponse
 */
@context("updateModelProxyWithContext")
async function updateModelProxyWithOptions(modelProxyName: string, request: UpdateModelProxyRequest, headers: map[string]string, runtime: $RuntimeOptions): UpdateModelProxyResponse {
  request.validate();
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(request.body),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpdateModelProxy',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/model-proxies/${$URL.percentEncode(modelProxyName)}`,
    method = 'PUT',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 更新模型
 *
 * @param request UpdateModelProxyRequest
 * @return UpdateModelProxyResponse
 */
async function updateModelProxy(modelProxyName: string, request: UpdateModelProxyRequest): UpdateModelProxyResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return updateModelProxyWithOptions(modelProxyName, request, headers, runtime);
}

model UpdateModelServiceRequest {
  body?: UpdateModelServiceInput(name='body'),
}

model UpdateModelServiceResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ModelServiceResult  
}

/**
 * @summary 更新模型
 *
 * @param request UpdateModelServiceRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateModelServiceResponse
 */
@context("updateModelServiceWithContext")
async function updateModelServiceWithOptions(modelServiceName: string, request: UpdateModelServiceRequest, headers: map[string]string, runtime: $RuntimeOptions): UpdateModelServiceResponse {
  request.validate();
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(request.body),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpdateModelService',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/agents/model-services/${$URL.percentEncode(modelServiceName)}`,
    method = 'PUT',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 更新模型
 *
 * @param request UpdateModelServiceRequest
 * @return UpdateModelServiceResponse
 */
async function updateModelService(modelServiceName: string, request: UpdateModelServiceRequest): UpdateModelServiceResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return updateModelServiceWithOptions(modelServiceName, request, headers, runtime);
}

model UpdateTemplateRequest {
  body?: UpdateTemplateInput(name='body', description='更新模板所需的配置信息

This parameter is required.'),
  clientToken?: string(name='clientToken', description='用于确保请求幂等性的唯一标识符', example='clientToken'),
}

model UpdateTemplateResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: TemplateResult  
}

/**
 * @summary 更新模板
 *
 * @description 更新指定模板的配置信息，包括资源配置、网络配置、环境变量等。
 *
 * @param request UpdateTemplateRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateTemplateResponse
 */
@context("updateTemplateWithContext")
async function updateTemplateWithOptions(templateName: string, request: UpdateTemplateRequest, headers: map[string]string, runtime: $RuntimeOptions): UpdateTemplateResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.clientToken)) {
    query['clientToken'] = request.clientToken;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(request.body),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpdateTemplate',
    version = '2025-09-10',
    protocol = 'HTTPS',
    pathname = `/2025-09-10/templates/${$URL.percentEncode(templateName)}`,
    method = 'PUT',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 更新模板
 *
 * @description 更新指定模板的配置信息，包括资源配置、网络配置、环境变量等。
 *
 * @param request UpdateTemplateRequest
 * @return UpdateTemplateResponse
 */
async function updateTemplate(templateName: string, request: UpdateTemplateRequest): UpdateTemplateResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return updateTemplateWithOptions(templateName, request, headers, runtime);
}

