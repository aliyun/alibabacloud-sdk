/**
 *
 */
import OpenApi;
import OpenApi.OpenApiUtil;

extends OpenApi;


init(config: OpenApiUtil.Config){
  super(config);
  @endpointRule = 'central';
  @endpointMap = {
    'ap-southeast-1' = 'openapi-mcp.ap-southeast-1.aliyuncs.com',
    'cn-hangzhou' = 'openapi-mcp.cn-hangzhou.aliyuncs.com',
  };

  checkConfig(config);
  @endpoint = getEndpoint('openapiexplorer', @regionId, @endpointRule, @network, @suffix, @endpointMap, @endpoint);
}

function getEndpoint(productId: string, regionId: string, endpointRule: string, network: string, suffix: string, endpointMap: map[string]string, endpoint: string) throws: string{
  if (!$isNull(endpoint)) {
    return endpoint;
  }
  
  if (!$isNull(endpointMap) && !$isNull(endpointMap[regionId])) {
    return endpointMap[regionId];
  }
  return OpenApiUtil.getEndpointRules(productId, regionId, endpointRule, network, suffix);
}

model ApiMcpServerValidateHclRequest {
  code?: string(name='code', description='The content of the Terraform HCL code. For more information, see [HCL language overview](https://www.alibabacloud.com/help/en/terraform/terraform-configuration-and-hcl-language-overview).', example='variable "name" {
  default = "terraform-example"
}

provider "alicloud" {
  region = "cn-beijing"
}

resource "alicloud_vpc" "default" {
  ipv6_isp    = "BGP"
  description = "test"
  cidr_block  = "10.0.0.0/8"
  vpc_name    = var.name
  enable_ipv6 = true
}'),
}

model ApiMcpServerValidateHclResponseBody = {
  diagnosticReport?: any(name='diagnosticReport', description='The diagnostic report of the code.', example='Argument or block definition required: An argument or block definition is required here.'),
  errors?: [ string ](name='errors', description='The list of error messages.'),
  hash?: string(name='hash', description='The unique identifier of the Terraform HCL code.', example='0628e13692023222bef9d6377dd03da3304b689e1b2df60f584ea27b4163bf07'),
  isValid?: boolean(name='isValid', description='Indicates whether the code is valid.', example='true'),
  parameters?: [ any ](name='parameters', description='The list of parameters.'),
  requestId?: string(name='requestId', description='The request ID.', example='9BFC4AC1-6BE4-5405-BDEC-CA288D404812'),
  warnings?: [ string ](name='warnings', description='The list of warning messages.'),
}

model ApiMcpServerValidateHclResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ApiMcpServerValidateHclResponseBody(name='body'),
}

/**
 * @summary Validates the syntax of HashiCorp Configuration Language (HCL) for Terraform.
 *
 * @param request ApiMcpServerValidateHclRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ApiMcpServerValidateHclResponse
 */
@context("apiMcpServerValidateHclWithContext")
async function apiMcpServerValidateHclWithOptions(request: ApiMcpServerValidateHclRequest, headers: map[string]string, runtime: $RuntimeOptions): ApiMcpServerValidateHclResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.code)) {
    body['code'] = request.code;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'ApiMcpServerValidateHcl',
    version = '2024-11-30',
    protocol = 'HTTPS',
    pathname = `/terraform/validate`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Validates the syntax of HashiCorp Configuration Language (HCL) for Terraform.
 *
 * @param request ApiMcpServerValidateHclRequest
 * @return ApiMcpServerValidateHclResponse
 */
async function apiMcpServerValidateHcl(request: ApiMcpServerValidateHclRequest): ApiMcpServerValidateHclResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return apiMcpServerValidateHclWithOptions(request, headers, runtime);
}

model CreateApiMcpServerRequest {
  additionalApiDescriptions?: [ 
    {
      apiName?: string(name='apiName', description='The API name.', example='DescribeRegions'),
      apiOverrideJson?: string(name='apiOverrideJson', description='The API metadata in JSON format. For more information about the format, see https\\\\://api.aliyun.com/meta/v1/products/Ecs/versions/2014-05-26/apis/DescribeInstances/api.json. You can overwrite the summary and parameters.', example='{
  "summary": "本接口支持根据不同请求条件查询实例列表，并关联查询实例的详细信息。"
}'),
      apiVersion?: string(name='apiVersion', description='The POP version of the API that is exposed to the MCP server.', example='2014-05-26'),
      constParameters?: [ 
        {
          key?: string(name='key', description='The parameter name. Only first-level parameter names are supported. For ROA-style APIs, you can set the parameter to body.xx. You cannot set values that exceed the top-level parameters.', example='InstanceId'),
          value?: any(name='value', description='The value of the parameter.', example='1234'),
        }
      ](name='constParameters', description='The list of constant input parameters. These parameters are not included in the output during API parameter parsing.'),
      enableOutputSchema?: boolean(name='enableOutputSchema', description='Specifies whether to return the schema of the response parameters. Returning the schema increases the size of the entire API MCP server. The default value is null, which means the schema is not returned.', example='true'),
      executeCliCommand?: boolean(name='executeCliCommand', description='Specifies whether to return the command-line interface (CLI) command for execution. In this mode, the API call is not executed. Instead, the corresponding CLI command is returned. This is suitable for long-running tasks that need to be executed using Alibaba Cloud CLI.', example='true'),
      product?: string(name='product', description='The product code.

- Call the GetRequestLog operation to obtain the product code from the response.

- Find the product code from the URL of the product in OpenAPI Portal. For example, the URL of Short Message Service in OpenAPI Portal is https\\\\://api.alibabacloud.com/product/Dysmsapi. The product code is Dysmsapi.', example='Ecs'),
    }
  ](name='additionalApiDescriptions', description='The list of supplementary API descriptions.'),
  apis?: [ 
    {
      apiVersion?: string(name='apiVersion', description='The POP version of the API that is exposed to the MCP server.

This parameter is required.', example='2014-05-26'),
      product?: string(name='product', description='The product code.

- Call the GetRequestLog operation to obtain the product code from the response.

- Find the product code from the URL of the product in OpenAPI Portal. For example, the URL of Short Message Service in OpenAPI Portal is https\\\\://api.alibabacloud.com/product/Dysmsapi. The product code is Dysmsapi.

This parameter is required.', example='Ecs'),
      selectors?: [ string ](name='selectors', description='The list of API name matching rules. This parameter cannot be empty.

This parameter is required.'),
    }
  ](name='apis', description='The list of APIs to add. This parameter cannot be empty.

This parameter is required.'),
  assumeRoleExtraPolicy?: string(name='assumeRoleExtraPolicy', description='An additional policy for role assumption when multi-account access is enabled. If this policy exists, the permissions for role assumption are based on this policy, which overwrites the permissions defined for the role itself.', example='{
  "Version": "1",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": [
        "ecs:Describe*",
        "vpc:Describe*",
        "vpc:List*"
      ],
      "Resource": "*"
    }
  ]
}'),
  assumeRoleName?: string(name='assumeRoleName', description='The name of the RAM role in the destination account that is assumed for cross-account operations when multi-account access is enabled.', example='test'),
  clientToken?: string(name='clientToken', description='Ensures the idempotence of the request. Generate a parameter value from your client to make sure that the value is unique among different requests. The client token can contain only ASCII characters and cannot exceed 64 characters in length. Use a universally unique identifier (UUID). The token expires in three days.', example='123e4567-e89b-12d3-a456-426655440000'),
  description?: string(name='description', description='The description of the API MCP service.', example='这是一个API MCP服务。'),
  enableAssumeRole?: boolean(name='enableAssumeRole', description='Specifies whether to enable multi-account access.', example='true'),
  enableCustomVpcWhitelist?: boolean(name='enableCustomVpcWhitelist', description='Specifies whether to enable a custom VPC whitelist. If not enabled, the account-level configuration is used.', example='true'),
  instructions?: string(name='instructions', description='The MCP instruction. It prompts the large language model on how to use the MCP. The client must support the Instructions field of the standard MCP protocol.', example='test'),
  language?: string(name='language', description='The language of the API documentation for the API MCP service. You can select Chinese or English API documentation. The prompts in different languages may affect the AI\\\\"s response.', example='ZH_CN'),
  name?: string(name='name', description='The name of the MCP server. The name must be 3 to 64 characters in length and can contain only lowercase letters, digits, underscores (_), and hyphens (-). It cannot start with a digit. The name must be unique within the same Alibaba Cloud account.

This parameter is required.', example='mcp-demo'),
  oauthClientId?: string(name='oauthClientId', description='The custom OAuth client ID when you select a custom OAuth configuration.

`Only web and native applications are supported. The OAuth scope must include /acs/mcp-server.`', example='403*************370'),
  prompts?: [ 
    {
      arguments?: [ 
        {
          description?: string(name='description', description='The description of the parameter.', example='argument description'),
          name?: string(name='name', description='The parameter name.', example='test'),
          required?: boolean(name='required', description='Specifies whether the parameter is required.', example='true'),
        }
      ](name='arguments', description='The list of parameters supported by the prompt.'),
      content?: string(name='content', description='The content of the prompt. Variables are specified in the {{xxx}} format. xxx is a variable that must be defined in the arguments parameter.', example='prompt正文，{{name}}'),
      description?: string(name='description', description='The description.', example='prompt description'),
      name?: string(name='name', description='The prompt name.', example='test'),
    }
  ](name='prompts', description='The list of prompt configurations.'),
  publicAccess?: string(name='publicAccess', description='Specifies whether to enable public network access.', example='on'),
  systemTools?: [ string ](name='systemTools', description='The list of system tools.'),
  terraformTools?: [ 
    {
      async?: boolean(name='async', description='Specifies whether to execute the task asynchronously. If set to true, the system immediately proceeds to the next task after initiating a task execution, without waiting for each resource operation to complete.', example='true'),
      code?: string(name='code', description='The code of the Terraform tool. For more information, see [HCL language overview](https://www.alibabacloud.com/help/en/terraform/terraform-configuration-and-hcl-language-overview).', example='variable "name" {
  default = "terraform-example"
}

provider "alicloud" {
  region = "cn-beijing"
}

resource "alicloud_vpc" "default" {
  ipv6_isp    = "BGP"
  description = "test"
  cidr_block  = "10.0.0.0/8"
  vpc_name    = var.name
  enable_ipv6 = true
}'),
      description?: string(name='description', description='The description of the Terraform tool.', example='terraform tool description'),
      destroyPolicy?: string(name='destroyPolicy', description='The deletion policy. After a task is executed, the system applies the following cleanup policy to temporary resources based on the task execution status.

- NEVER: Does not delete any created resources, regardless of whether the task execution succeeds or fails.

- ALWAYS: Immediately destroys all related resources after the task is executed, regardless of whether the execution succeeds or fails.

- ON_FAILURE: Deletes related resources only when the task execution fails. If the task execution succeeds, the resources are retained.', example='ON_FAILURE'),
      name?: string(name='name', description='The name of the Terraform tool.', example='test'),
    }
  ](name='terraformTools', description='The list of Terraform tools.'),
  vpcWhitelists?: [ string ](name='vpcWhitelists', description='The VPC whitelist that restricts the source of access after public network access is disabled. If you do not set this parameter or leave it empty, the source is not restricted.'),
}

model CreateApiMcpServerResponseBody = {
  id?: string(name='id', description='The ID of the successfully created API MCP service.', example='v6ZZ7ftCzEILW***'),
  requestId?: string(name='requestId', description='The request ID.', example='9BFC4AC1-6BE4-5405-BDEC-CA288D404812'),
  urls?: {
    mcp?: string(name='mcp', description='The connection information for the streamable HTTP protocol. This is the recommended protocol.', example='https://mcpserverinner-pre.cn-zhangjiakou.aliyuncs.com/accounts/xxxx/custom/xxx/id/xxxx/mcp'),
    sse?: string(name='sse', description='The connection information for the Server-Sent Events (SSE) protocol.', example='https://mcpserverinner-pre.cn-zhangjiakou.aliyuncs.com/accounts/xxxx/custom/xxx/id/xxxx/sse'),
    vpcMcp?: string(name='vpcMcp', description='The endpoint of the streamable HTTP protocol in a VPC.', example='https://openapi-mcp-cn.vpc-proxy.aliyuncs.com/accounts/xxxx/custom/xxx/id/xxxx/mcp'),
    vpcSse?: string(name='vpcSse', description='The endpoint of the SSE protocol in a VPC.', example='https://openapi-mcp-cn.vpc-proxy.aliyuncs.com/accounts/xxxx/custom/xxx/id/xxxx/sse'),
  }(name='urls', description='The connection information for the API MCP service.'),
}

model CreateApiMcpServerResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateApiMcpServerResponseBody(name='body'),
}

/**
 * @summary Creates an API MCP service.
 *
 * @param request CreateApiMcpServerRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateApiMcpServerResponse
 */
@context("createApiMcpServerWithContext")
async function createApiMcpServerWithOptions(request: CreateApiMcpServerRequest, headers: map[string]string, runtime: $RuntimeOptions): CreateApiMcpServerResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.additionalApiDescriptions)) {
    body['additionalApiDescriptions'] = request.additionalApiDescriptions;
  }
  if (!$isNull(request.apis)) {
    body['apis'] = request.apis;
  }
  if (!$isNull(request.assumeRoleExtraPolicy)) {
    body['assumeRoleExtraPolicy'] = request.assumeRoleExtraPolicy;
  }
  if (!$isNull(request.assumeRoleName)) {
    body['assumeRoleName'] = request.assumeRoleName;
  }
  if (!$isNull(request.clientToken)) {
    body['clientToken'] = request.clientToken;
  }
  if (!$isNull(request.description)) {
    body['description'] = request.description;
  }
  if (!$isNull(request.enableAssumeRole)) {
    body['enableAssumeRole'] = request.enableAssumeRole;
  }
  if (!$isNull(request.enableCustomVpcWhitelist)) {
    body['enableCustomVpcWhitelist'] = request.enableCustomVpcWhitelist;
  }
  if (!$isNull(request.instructions)) {
    body['instructions'] = request.instructions;
  }
  if (!$isNull(request.language)) {
    body['language'] = request.language;
  }
  if (!$isNull(request.name)) {
    body['name'] = request.name;
  }
  if (!$isNull(request.oauthClientId)) {
    body['oauthClientId'] = request.oauthClientId;
  }
  if (!$isNull(request.prompts)) {
    body['prompts'] = request.prompts;
  }
  if (!$isNull(request.publicAccess)) {
    body['publicAccess'] = request.publicAccess;
  }
  if (!$isNull(request.systemTools)) {
    body['systemTools'] = request.systemTools;
  }
  if (!$isNull(request.terraformTools)) {
    body['terraformTools'] = request.terraformTools;
  }
  if (!$isNull(request.vpcWhitelists)) {
    body['vpcWhitelists'] = request.vpcWhitelists;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateApiMcpServer',
    version = '2024-11-30',
    protocol = 'HTTPS',
    pathname = `/apimcpserver`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates an API MCP service.
 *
 * @param request CreateApiMcpServerRequest
 * @return CreateApiMcpServerResponse
 */
async function createApiMcpServer(request: CreateApiMcpServerRequest): CreateApiMcpServerResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return createApiMcpServerWithOptions(request, headers, runtime);
}

model DeleteApiMcpServerRequest {
  clientToken?: string(name='clientToken', description='The client token used to ensure the idempotence of the request. Generate this value on your client and make sure that it is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length. Use a universally unique identifier (UUID). The token is valid for three days.', example='123e4567-e89b-12d3-a456-426655440000'),
  id?: string(name='id', description='The ID of the API MCP service.

This parameter is required.', example='v6ZZ7ftCzEILW***'),
}

model DeleteApiMcpServerResponseBody = {
  requestId?: string(name='requestId', description='The request ID.', example='9BFC4AC1-6BE4-5405-BDEC-CA288D404812'),
}

model DeleteApiMcpServerResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DeleteApiMcpServerResponseBody(name='body'),
}

/**
 * @summary Deletes an API MCP service.
 *
 * @param request DeleteApiMcpServerRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteApiMcpServerResponse
 */
@context("deleteApiMcpServerWithContext")
async function deleteApiMcpServerWithOptions(request: DeleteApiMcpServerRequest, headers: map[string]string, runtime: $RuntimeOptions): DeleteApiMcpServerResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.clientToken)) {
    query['clientToken'] = request.clientToken;
  }
  if (!$isNull(request.id)) {
    query['id'] = request.id;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DeleteApiMcpServer',
    version = '2024-11-30',
    protocol = 'HTTPS',
    pathname = `/apimcpserver`,
    method = 'DELETE',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Deletes an API MCP service.
 *
 * @param request DeleteApiMcpServerRequest
 * @return DeleteApiMcpServerResponse
 */
async function deleteApiMcpServer(request: DeleteApiMcpServerRequest): DeleteApiMcpServerResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return deleteApiMcpServerWithOptions(request, headers, runtime);
}

model GenerateCLICommandRequest {
  aggregatePagination?: boolean(name='aggregatePagination', description='Enable aggregation. If enabled, the CLI automatically reads full data using pagination and aggregates the results.

>Warning: 

You can use this option only with List operations that support pagination.



- true: Enable

- false: Disable', example='true'),
  api?: string(name='api', description='API name.

This parameter is required.', example='DescribeRegions'),
  apiParams?: map[string]any(name='apiParams', description='Request parameters.', deprecated=true),
  apiVersion?: string(name='apiVersion', description='API version.

This parameter is required.', example='2014-05-26'),
  jsonApiParams?: string(name='jsonApiParams', description='API input parameters in JSON format. This parameter has lower priority than apiParams. If you set apiParams, this parameter is ignored.', example='{
    "InstanceChargeType": "PostPaid",
    "ResourceType": "instance",
    "AcceptLanguage": "en-US"
}'),
  product?: string(name='product', description='Product code.

- Call the GetRequestLog operation and get the product code from the response.

- Find the product code in the OpenAPI portal URL. For example, the OpenAPI portal URL for Short Message Service is https\\\\://api.aliyun.com/product/Dysmsapi. The product code for Short Message Service is Dysmsapi. In international regions, the URL is https\\\\://api.alibabacloud.com/product/Dysmsapi. The product code remains Dysmsapi.

This parameter is required.', example='Ecs'),
  regionId?: string(name='regionId', description='Region ID.', example='cn-hangzhou'),
}

model GenerateCLICommandShrinkRequest {
  aggregatePagination?: boolean(name='aggregatePagination', description='Enable aggregation. If enabled, the CLI automatically reads full data using pagination and aggregates the results.

>Warning: 

You can use this option only with List operations that support pagination.



- true: Enable

- false: Disable', example='true'),
  api?: string(name='api', description='API name.

This parameter is required.', example='DescribeRegions'),
  apiParamsShrink?: string(name='apiParams', description='Request parameters.', deprecated=true),
  apiVersion?: string(name='apiVersion', description='API version.

This parameter is required.', example='2014-05-26'),
  jsonApiParams?: string(name='jsonApiParams', description='API input parameters in JSON format. This parameter has lower priority than apiParams. If you set apiParams, this parameter is ignored.', example='{
    "InstanceChargeType": "PostPaid",
    "ResourceType": "instance",
    "AcceptLanguage": "en-US"
}'),
  product?: string(name='product', description='Product code.

- Call the GetRequestLog operation and get the product code from the response.

- Find the product code in the OpenAPI portal URL. For example, the OpenAPI portal URL for Short Message Service is https\\\\://api.aliyun.com/product/Dysmsapi. The product code for Short Message Service is Dysmsapi. In international regions, the URL is https\\\\://api.alibabacloud.com/product/Dysmsapi. The product code remains Dysmsapi.

This parameter is required.', example='Ecs'),
  regionId?: string(name='regionId', description='Region ID.', example='cn-hangzhou'),
}

model GenerateCLICommandResponseBody = {
  cli?: string(name='cli', description='CLI command.', example='aliyun ecs DescribeRegions --ResourceType instance'),
  requestId?: string(name='requestId', description='Request ID.', example='A707AFA8-1A4C-5B2A-A165-8436C1EA38DB'),
  unifiedCli?: string(name='unifiedCli'),
}

model GenerateCLICommandResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GenerateCLICommandResponseBody(name='body'),
}

/**
 * @summary Generates an Alibaba Cloud CLI command dynamically.
 *
 * @param tmpReq GenerateCLICommandRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GenerateCLICommandResponse
 */
@context("generateCLICommandWithContext")
async function generateCLICommandWithOptions(tmpReq: GenerateCLICommandRequest, headers: map[string]string, runtime: $RuntimeOptions): GenerateCLICommandResponse {
  tmpReq.validate();
  var request = new GenerateCLICommandShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.apiParams)) {
    request.apiParamsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.apiParams, 'apiParams', 'json');
  }
  var body : map[string]any = {};
  if (!$isNull(request.aggregatePagination)) {
    body['aggregatePagination'] = request.aggregatePagination;
  }
  if (!$isNull(request.api)) {
    body['api'] = request.api;
  }
  if (!$isNull(request.apiParamsShrink)) {
    body['apiParams'] = request.apiParamsShrink;
  }
  if (!$isNull(request.apiVersion)) {
    body['apiVersion'] = request.apiVersion;
  }
  if (!$isNull(request.jsonApiParams)) {
    body['jsonApiParams'] = request.jsonApiParams;
  }
  if (!$isNull(request.product)) {
    body['product'] = request.product;
  }
  if (!$isNull(request.regionId)) {
    body['regionId'] = request.regionId;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'GenerateCLICommand',
    version = '2024-11-30',
    protocol = 'HTTPS',
    pathname = `/api/cli/makeCode`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Generates an Alibaba Cloud CLI command dynamically.
 *
 * @param request GenerateCLICommandRequest
 * @return GenerateCLICommandResponse
 */
async function generateCLICommand(request: GenerateCLICommandRequest): GenerateCLICommandResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return generateCLICommandWithOptions(request, headers, runtime);
}

model GetApiDefinitionRequest {
  api?: string(name='api', description='The API name.

This parameter is required.', example='DescribeRegions'),
  apiVersion?: string(name='apiVersion', description='The API version.

This parameter is required.', example='2014-05-26'),
  product?: string(name='product', description='The product code.

- Call the GetRequestLog operation to obtain the product code from the response.

- Find the product code in the product\\\\"s OpenAPI Portal URL. <props="china">For example, the OpenAPI Portal URL for Short Message Service is https\\\\://api.aliyun.com/product/Dysmsapi. The product code for Short Message Service is Dysmsapi.
  <props="intl">For example, the OpenAPI Portal URL for Short Message Service is https\\\\://api.alibabacloud.com/product/Dysmsapi. The product code for Short Message Service is Dysmsapi.

This parameter is required.', example='Ecs'),
}

model GetApiDefinitionResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: object(name='body'),
}

/**
 * @summary Retrieves the OpenAPI metadata for an API in a product.
 *
 * @param request GetApiDefinitionRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetApiDefinitionResponse
 */
@context("getApiDefinitionWithContext")
async function getApiDefinitionWithOptions(request: GetApiDefinitionRequest, headers: map[string]string, runtime: $RuntimeOptions): GetApiDefinitionResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.api)) {
    query['api'] = request.api;
  }
  if (!$isNull(request.apiVersion)) {
    query['apiVersion'] = request.apiVersion;
  }
  if (!$isNull(request.product)) {
    query['product'] = request.product;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetApiDefinition',
    version = '2024-11-30',
    protocol = 'HTTPS',
    pathname = `/api/definition`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves the OpenAPI metadata for an API in a product.
 *
 * @param request GetApiDefinitionRequest
 * @return GetApiDefinitionResponse
 */
async function getApiDefinition(request: GetApiDefinitionRequest): GetApiDefinitionResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getApiDefinitionWithOptions(request, headers, runtime);
}

model GetApiMcpServerRequest {
  id?: string(name='id', description='The ID of the API MCP service.

This parameter is required.', example='v6ZZ7ftCzEILW***'),
}

model GetApiMcpServerResponseBody = {
  additionalApiDescriptions?: [ 
    {
      apiName?: string(name='apiName', description='The API name.', example='DescribeRegions'),
      apiOverrideJson?: string(name='apiOverrideJson', description='The API metadata in JSON format. For an example of the format, see https\\\\://api.aliyun.com/meta/v1/products/Ecs/versions/2014-05-26/apis/DescribeInstances/api.json. You can overwrite the summary and parameters fields.', example='{
  "summary": "本接口支持根据不同请求条件查询实例列表，并关联查询实例的详细信息。"
}'),
      apiVersion?: string(name='apiVersion', description='The POP version of the API that is exposed to the MCP server.', example='2014-05-26'),
      constParameters?: [ 
        {
          key?: string(name='key', description='The parameter name. Only first-level parameter names are supported. For ROA-style APIs, you can set parameters such as body.xx. You cannot set values that exceed the top-level parameters.', example='InstanceId'),
          value?: any(name='value', description='The value of the parameter.', example='1234'),
        }
      ](name='constParameters', description='A list of constant input parameters. These parameters are not included in the output during API parameter parsing.'),
      enableOutputSchema?: boolean(name='enableOutputSchema', description='Indicates whether to return the schema of the response parameters. Returning the schema increases the size of the entire API MCP server. The default value is null, which means the schema is not returned.', example='true'),
      executeCliCommand?: boolean(name='executeCliCommand', description='Indicates whether to return the command-line interface (CLI) command. In this mode, the API is not called. Instead, the corresponding CLI command is returned. This is suitable for long-running tasks that need to be executed using Alibaba Cloud CLI.', example='true'),
      product?: string(name='product', description='The product code.

- Call the GetRequestLog operation to obtain the product code from the response.

<props="intl">

- Find the corresponding product code from the URL of the OpenAPI Portal. For example, the URL of the OpenAPI Portal for Short Message Service is https\\\\://api.alibabacloud.com/product/Dysmsapi. The product code for Short Message Service is Dysmsapi.', example='Ecs'),
    }
  ](name='additionalApiDescriptions', description='A list of supplementary API descriptions.'),
  apiInfos?: [ 
    {
      apiName?: string(name='apiName', description='The API name.', example='DescribeRegions'),
      apiVersion?: string(name='apiVersion', description='The POP version of the API that is exposed to the MCP server.', example='2014-05-26'),
      product?: string(name='product', description='The product code.

- Call the GetRequestLog operation to obtain the product code from the response.

<props="intl">

- Find the corresponding product code from the URL of the OpenAPI Portal. For example, the URL of the OpenAPI Portal for Short Message Service is https\\\\://api.alibabacloud.com/product/Dysmsapi. The product code for Short Message Service is Dysmsapi.', example='Ecs'),
    }
  ](name='apiInfos', description='A list of API information for the API MCP service.'),
  apis?: [ 
    {
      apiVersion?: string(name='apiVersion', description='The POP version of the API that is exposed to the MCP server.', example='2014-05-26'),
      product?: string(name='product', description='The product code.

- Call the GetRequestLog operation to obtain the product code from the response.

<props="intl">

- Find the corresponding product code from the URL of the OpenAPI Portal. For example, the URL of the OpenAPI Portal for Short Message Service is https\\\\://api.alibabacloud.com/product/Dysmsapi. The product code for Short Message Service is Dysmsapi.', example='Ecs'),
      selectors?: [ string ](name='selectors', description='A list of API name matching rules.'),
    }
  ](name='apis', description='A list of API information.'),
  assumeRoleExtraPolicy?: string(name='assumeRoleExtraPolicy', description='The extra policy for role assumption when multi-account access is enabled. If this policy is specified, the permissions for the role assumption are based on this policy and overwrite the permissions that are defined for the role.', example='{
  "Version": "1",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": [
        "ecs:Describe*",
        "vpc:Describe*",
        "vpc:List*"
      ],
      "Resource": "*"
    }
  ]
}'),
  assumeRoleName?: string(name='assumeRoleName', description='The name of the RAM role of the destination account that is assumed when you perform cross-account operations with multi-account access enabled.', example='test'),
  createTime?: string(name='createTime', description='The time when the API MCP server was created.', example='2025-02-07T02:17:46Z'),
  description?: string(name='description', description='The description of the API MCP server.', example='这是一个API MCP 服务器'),
  enableAssumeRole?: boolean(name='enableAssumeRole', description='Indicates whether to enable multi-account access.', example='true'),
  enableCustomVpcWhitelist?: boolean(name='enableCustomVpcWhitelist', description='Indicates whether to enable a custom VPC whitelist. If this parameter is not enabled, the account-level configuration is used.', example='true'),
  id?: string(name='id', description='The ID of the API MCP service.', example='v6ZZ7ftCzEILW***'),
  instructions?: string(name='instructions', description='The MCP instruction. It prompts the large model on how to use the MCP. The client must support the Instructions field of the standard MCP protocol.', example='test'),
  language?: string(name='language', description='The language of the API documentation for the API MCP service. You can select Chinese or English API documentation. The language of the prompt may affect the AI\\\\"s response.', example='ZH_CN'),
  name?: string(name='name', description='The name of the MCP server. The name must be 3 to 64 characters in length and can contain lowercase letters and digits. It must not start with a digit. The name must be unique within the same Alibaba Cloud account.', example='mcp-demo'),
  oauthClientId?: string(name='oauthClientId', description='The custom OAuth client ID when you select a custom OAuth configuration.

`Only web and native applications are supported. The OAuth scope must include /acs/mcp-server.`', example='403*************370'),
  prompts?: [ 
    {
      arguments?: [ 
        {
          description?: string(name='description', description='The description of the parameter.', example='argument description'),
          name?: string(name='name', description='The parameter name.', example='test'),
          required?: boolean(name='required', description='Indicates whether this parameter is required.'),
        }
      ](name='arguments', description='A list of parameters that the prompt supports.'),
      content?: string(name='content', description='The content of the prompt. Variables are specified in the {{xxx}} format. The xxx variable must be defined in the arguments parameter.', example='prompt正文，{{name}}'),
      description?: string(name='description', description='The description.', example='prompt description'),
      name?: string(name='name', description='The prompt name.', example='test'),
    }
  ](name='prompts', description='A list of prompt configurations.'),
  publicAccess?: string(name='publicAccess', description='Indicates whether to enable public access.', example='on'),
  requestId?: string(name='requestId', description='The request ID.', example='9BFC4AC1-6BE4-5405-BDEC-CA288D404812'),
  requiredRAMPolicy?: string(name='requiredRAMPolicy', description='The RAM access policy that is required by the API MCP service.', example='{
  "Version": "1",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": [
        "ecs:Describe*",
        "vpc:Describe*",
        "vpc:List*"
      ],
      "Resource": "*"
    }
  ]
}'),
  sourceType?: string(name='sourceType', description='The type of the API MCP service.

- custom: a custom service

- system: a system service', example='system'),
  systemMcpServerInfo?: {
    name?: string(name='name', description='The name of the system MCP service.', example='mcp-system'),
    product?: string(name='product', description='The product code.

- Call the GetRequestLog operation to obtain the product code from the response.

<props="intl">

- Find the corresponding product code from the URL of the OpenAPI Portal. For example, the URL of the OpenAPI Portal for Short Message Service is https\\\\://api.alibabacloud.com/product/Dysmsapi. The product code for Short Message Service is Dysmsapi.', example='Ecs'),
  }(name='systemMcpServerInfo', description='A list of system MCP services.'),
  systemTools?: [ string ](name='systemTools', description='A list of system tools.'),
  terraformTools?: [ 
    {
      async?: boolean(name='async', description='Indicates whether to execute tasks asynchronously. If set to true, the system immediately starts the next task after the current task is initiated, without waiting for each resource operation to complete.', example='true'),
      code?: string(name='code', description='The code for the Terraform tool. For more information, see [HCL language overview](https://help.aliyun.com/zh/terraform/terraform-configuration-and-hcl-language-overview).', example='variable "name" {
  default = "terraform-example"
}

provider "alicloud" {
  region = "cn-beijing"
}

resource "alicloud_vpc" "default" {
  ipv6_isp    = "BGP"
  description = "test"
  cidr_block  = "10.0.0.0/8"
  vpc_name    = var.name
  enable_ipv6 = true
}'),
      description?: string(name='description', description='The description of the Terraform tool.', example='Terraform Tool description'),
      destroyPolicy?: string(name='destroyPolicy', description='The destroy policy. After a task is complete, the system applies the following cleanup policy to temporary resources based on the task execution status.

- NEVER: All created resources are retained, regardless of whether the task succeeds or fails.

- ALWAYS: All related resources are immediately destroyed after the task is complete, regardless of whether the task succeeds or fails.

- ON_FAILURE: Related resources are deleted only if the task fails. If the task succeeds, the resources are retained.', example='ON_FAILURE'),
      name?: string(name='name', description='The name of the Terraform tool.', example='test'),
    }
  ](name='terraformTools', description='A list of Terraform tools.'),
  updateTime?: string(name='updateTime', description='The time when the API MCP server was last modified.', example='2025-02-05T02:26:04Z'),
  urls?: {
    mcp?: string(name='mcp', description='The connection information for the streamable HTTP protocol. This protocol is recommended.', example='https://openapi-mcp.cn-hangzhou.aliyuncs.com/accounts/xxxx/custom/xxx/id/xxxx/mcp'),
    sse?: string(name='sse', description='The connection information for the Server-Sent Events (SSE) protocol.', example='https://openapi-mcp.cn-hangzhou.aliyuncs.com/accounts/xxxx/custom/xxx/id/xxxx/sse'),
    vpcMcp?: string(name='vpcMcp', description='The endpoint of the streamable HTTP protocol in a VPC.', example='https://openapi-mcp-cn.vpc-proxy.aliyuncs.com/accounts/xxxx/custom/xxx/id/xxxx/mcp'),
    vpcSse?: string(name='vpcSse', description='The endpoint of the SSE protocol in a VPC.', example='https://openapi-mcp-cn.vpc-proxy.aliyuncs.com/accounts/xxxx/custom/xxx/id/xxxx/sse'),
  }(name='urls', description='The connection information for the MCP server.'),
  vpcWhitelists?: [ string ](name='vpcWhitelists', description='The VPC whitelist that specifies the allowed source VPCs after public access is disabled. If you do not set this parameter or leave it empty, access from all sources is allowed.'),
}

model GetApiMcpServerResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetApiMcpServerResponseBody(name='body'),
}

/**
 * @summary Retrieves the details of a specific API MCP service.
 *
 * @param request GetApiMcpServerRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetApiMcpServerResponse
 */
@context("getApiMcpServerWithContext")
async function getApiMcpServerWithOptions(request: GetApiMcpServerRequest, headers: map[string]string, runtime: $RuntimeOptions): GetApiMcpServerResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.id)) {
    query['id'] = request.id;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetApiMcpServer',
    version = '2024-11-30',
    protocol = 'HTTPS',
    pathname = `/apimcpserver`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves the details of a specific API MCP service.
 *
 * @param request GetApiMcpServerRequest
 * @return GetApiMcpServerResponse
 */
async function getApiMcpServer(request: GetApiMcpServerRequest): GetApiMcpServerResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getApiMcpServerWithOptions(request, headers, runtime);
}

model GetApiMcpServerUserConfigResponseBody = {
  accountId?: string(name='accountId', description='The ID of your Alibaba Cloud account.', example='162302724684579*'),
  enablePublicAccess?: boolean(name='enablePublicAccess', description='Specifies whether to enable public network access for all API MCP Servers, including system MCP Servers, under your account. By default, this feature is enabled. If you disable it, you can access the servers only through VPC domain names.', example='true'),
  gmtCreate?: string(name='gmtCreate', description='The time when the configuration was created.', example='2025-11-10T06:58:39Z'),
  gmtModified?: string(name='gmtModified', description='The time when the configuration was last updated.', example='2025-11-10T06:58:39Z'),
  requestId?: string(name='requestId', description='The request ID.', example='A707AFA8-1A4C-5B2A-A165-8436C1EA38DB'),
  vpcWhitelists?: [ string ](name='vpcWhitelists', description='The whitelist of source VPCs that are allowed to send requests after public network access is disabled. If you do not set this parameter or leave it empty, requests from all sources are allowed.'),
}

model GetApiMcpServerUserConfigResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetApiMcpServerUserConfigResponseBody(name='body'),
}

/**
 * @summary Queries the global API MCP Server configuration for a user. The configuration includes settings for public network access and the whitelist of source VPC IDs for requests from a VPC.
 *
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetApiMcpServerUserConfigResponse
 */
@context("getApiMcpServerUserConfigWithContext")
async function getApiMcpServerUserConfigWithOptions(headers: map[string]string, runtime: $RuntimeOptions): GetApiMcpServerUserConfigResponse {
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
  };
  var params = new OpenApiUtil.Params{
    action = 'GetApiMcpServerUserConfig',
    version = '2024-11-30',
    protocol = 'HTTPS',
    pathname = `/userconfig/get`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the global API MCP Server configuration for a user. The configuration includes settings for public network access and the whitelist of source VPC IDs for requests from a VPC.
 *
 * @return GetApiMcpServerUserConfigResponse
 */
async function getApiMcpServerUserConfig(): GetApiMcpServerUserConfigResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getApiMcpServerUserConfigWithOptions(headers, runtime);
}

model GetErrorCodeSolutionsRequest {
  acceptLanguage?: string(name='acceptLanguage', description='The language of the solution. Valid values: zh-CN and en-US. Not all of the solutions are available in English. If you set this parameter to en-US, but the corresponding solution is actually not available in English, no response is returned.', example='zh-CN'),
  errorCode?: string(name='errorCode', description='The error code based on which you want to query a solution.

This parameter is required.', example='IncorrectStatus.TransitRouter'),
  errorMessage?: string(name='errorMessage', description='The error message for which you want to query a solution. This parameter must be configured together with the errorCode parameter.', example='The resource is not in a valid state for the operation.'),
  product?: string(name='product', description='The product code. You can use one of the following methods to query a product code:

- Call the GetRequestLog operation to query a product code from the response.

- Query the code of a product in the OpenAPI Explorer URL of the product. For example, the OpenAPI Explorer URL of Short Message Service (SMS) is https\\\\://api.alibabacloud.com/product/Dysmsapi. Therefore, the product code of SMS is Dysmsapi.', example='Ecs'),
}

model GetErrorCodeSolutionsResponseBody = {
  requestId?: string(name='requestId', description='The request ID.', example='A707AFA8-1A4C-5B2A-A165-8436C1EA38DB'),
  solutions?: [ 
    {
      content?: string(name='content', description='The content of the solution, which is in the markdown format.', example='Unsupported transit router status\\\\n====\\\\n\\\\nCause\\\\n----\\\\n\\\\nThe transit router is in an unstable state.\\\\n\\\\n* * *\\\\n\\\\nSolution\\\\n----\\\\n\\\\nTry again after the transit router enters a stable state. You can check whether the transit router is in a stable state based on the following information.\\\\n\\\\nExample\\\\n------\\\\n\\\\nBefore you proceed, make sure that the transit route is in a stable state.\\\\n\\\\n1. Go to the console. If the status of the transit router in the Transit Router list is Available, the transit router is in a stable state.\\\\n\\\\n2. Call the ListTransitRouters operation. If Active is returned for Status, the transit router is in a stable state.'),
      errorCode?: string(name='errorCode', description='The error code.', example='IncorrectStatus.TransitRouter'),
      errorMessage?: string(name='errorMessage', description='The error message.', example='The resource is not in a valid state for the operation.'),
      product?: string(name='product', description='The code of the product to which the solution belongs.', example='Ecs'),
      productName?: string(name='productName', description='The name of the product to which the solution belongs.', example='Elastic Compute Service'),
      solutionId?: string(name='solutionId', description='The solution ID.', example='0017-00000502'),
    }
  ](name='solutions', description='The solutions. Not all error codes have corresponding solutions. You can submit a ticket or use OpenAPI Explorer to contact technical support if necessary.'),
}

model GetErrorCodeSolutionsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetErrorCodeSolutionsResponseBody(name='body'),
}

/**
 * @summary Queries an error solution by error code.
 *
 * @description You can call this API operation to query public information instead of special information, such as the account ownership. Permissions on this API operation cannot be granted to other members.
 *
 * @param request GetErrorCodeSolutionsRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetErrorCodeSolutionsResponse
 */
@context("getErrorCodeSolutionsWithContext")
async function getErrorCodeSolutionsWithOptions(request: GetErrorCodeSolutionsRequest, headers: map[string]string, runtime: $RuntimeOptions): GetErrorCodeSolutionsResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.acceptLanguage)) {
    query['acceptLanguage'] = request.acceptLanguage;
  }
  if (!$isNull(request.errorCode)) {
    query['errorCode'] = request.errorCode;
  }
  if (!$isNull(request.errorMessage)) {
    query['errorMessage'] = request.errorMessage;
  }
  if (!$isNull(request.product)) {
    query['product'] = request.product;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetErrorCodeSolutions',
    version = '2024-11-30',
    protocol = 'HTTPS',
    pathname = `/getErrorCodeSolutions`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries an error solution by error code.
 *
 * @description You can call this API operation to query public information instead of special information, such as the account ownership. Permissions on this API operation cannot be granted to other members.
 *
 * @param request GetErrorCodeSolutionsRequest
 * @return GetErrorCodeSolutionsResponse
 */
async function getErrorCodeSolutions(request: GetErrorCodeSolutionsRequest): GetErrorCodeSolutionsResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getErrorCodeSolutionsWithOptions(request, headers, runtime);
}

model GetOwnRequestLogRequest {
  logRequestId?: string(name='logRequestId', description='The request ID of the log to query. The ID is the unique identifier (UUID) of an API request and must be in uppercase.

This parameter is required.', example='123E4567-E89B-12D3-A456-426614174000'),
}

model GetOwnRequestLogResponseBody = {
  logInfo?: {
    authenticationInfo?: {
      authenticationType?: string(name='authenticationType', description='The authentication type. Valid values:

- \\\\`AK\\\\`: an AccessKey, which can be a permanent AccessKey, a temporary AccessKey, or an STS token.

- \\\\`PRIVATEKEY\\\\`: an AccessKey for asymmetric key encryption.

- \\\\`BEARERTOKEN\\\\`: an authentication mechanism that is widely used in the OAuth 2.0 framework and cloud services.

- \\\\`CUSTOM_SPI\\\\`: an efficient and secure authentication method that is suitable for the delivery and management of Software as a Service (SaaS) products in Alibaba Cloud Marketplace.

- \\\\`Anonymous\\\\`: anonymous access.

- \\\\`DPS\\\\`: similar to an AccessKey, but uses a product-specific signature algorithm that is different from the official Alibaba Cloud algorithm.', example='AK'),
      signatureMethod?: string(name='signatureMethod', description='The signature algorithm. Valid values:

- \\\\`HMAC-SHA1\\\\`: The request is signed using the HMAC-SHA1 algorithm.

- \\\\`HMAC-SHA256\\\\`: The request is signed using the HMAC-SHA256 algorithm.', example='HMAC-SHA256'),
      signatureVersion?: string(name='signatureVersion', description='The signature version.', example='unknown'),
    }(name='authenticationInfo', description='The authentication information.'),
    basicInfo?: {
      accessDeniedDetail?: {
        authAction?: string(name='authAction', description='The specific operation that caused the permission error.', example='openapiexplorer:GetRequestLog'),
        authPrincipalDisplayName?: string(name='authPrincipalDisplayName', description='The identity.', example='205618123456123456'),
        authPrincipalOwnerId?: string(name='authPrincipalOwnerId', description='The UID of the Alibaba Cloud account to which the current identity belongs.', example='1001234561234567'),
        authPrincipalType?: string(name='authPrincipalType', description='The identity type of the operator.', example='SubUser'),
        encodedDiagnosticMessage?: string(name='encodedDiagnosticMessage', description='The encoded diagnostic information. Call the \\\\`DecodeDiagnosticMessage\\\\` operation of RAM to obtain more diagnostic information.', example='-'),
        noPermissionType?: string(name='noPermissionType', description='The reason why the permission was denied.', example='ImplicitDeny'),
        policyType?: string(name='policyType', description='The type of the policy that caused the permission error.', example='AccountLevelIdentityBasedPolicy'),
      }(name='accessDeniedDetail', description='The details of the permission error.'),
      api?: string(name='api', description='The name of the API in the queried log.', example='RunInstances'),
      apiDoc?: {
        alibabacloudSite?: string(name='alibabacloudSite', description='The URL of the API reference for the international site (alibabacloud.com).', example='https://api.alibabacloud.com/document/Ecs/2014-05-26/RunInstances'),
        aliyunSite?: string(name='aliyunSite', description='The URL of the API reference for the China site (aliyun.com).', example='https://api.aliyun.com/document/Ecs/2014-05-26/RunInstances'),
      }(name='apiDoc', description='Information about the API reference.'),
      apiStyle?: string(name='apiStyle', description='The API style. Valid values: \\\\`ROA\\\\` and \\\\`RPC\\\\`.', example='rpc'),
      apiVersion?: string(name='apiVersion', description='The API version.', example='2014-05-26'),
      endpoint?: string(name='endpoint', description='The endpoint of the service in the region.', example='ecs.cn-hangzhou.aliyuncs.com'),
      errorCode?: string(name='errorCode', description='The error code in the queried log. This parameter is empty if no error is reported for the request.', example='IncorrectStatus.TransitRouter'),
      errorMessage?: string(name='errorMessage', description='The error message in the queried log. This parameter is empty if no error is reported for the request.', example='The resource is not in a valid state for the operation.'),
      gatewayProcessTime?: string(name='gatewayProcessTime', description='The time when the gateway received the request. The time is in the \\\\`yyyy-MM-ddTHH:mm:ssZ\\\\` format and is in UTC.', example='2025-01-21T07:43:06Z'),
      httpMethod?: string(name='httpMethod', description='The HTTP request method.', example='GET'),
      httpStatusCode?: string(name='httpStatusCode', description='The HTTP status code in the queried log.', example='400'),
      logRequestId?: string(name='logRequestId', description='The request ID that is passed in the request.', example='123E4567-E89B-12D3-A456-426614174000'),
      product?: string(name='product', description='The product code.', example='Ecs'),
      productName?: {
        cnName?: string(name='cnName', description='The Chinese name of the product.', example='云服务器 ECS'),
        enName?: string(name='enName', description='The English name of the product.', example='Elastic Compute Service'),
      }(name='productName', description='The name of the product in Chinese and English.'),
      regionId?: string(name='regionId', description='The ID of the region.', example='cn-hangzhou'),
      requestDuration?: string(name='requestDuration', description='The time that elapses from when the gateway receives the request to when the gateway returns the response. Unit: ms.', example='188'),
      sdkRequestTime?: string(name='sdkRequestTime', description='The time when the request was initiated. The time is in the \\\\`yyyy-MM-ddTHH:mm:ssZ\\\\` format and is in UTC.', example='2025-01-21T07:43:06Z'),
      throttlingResult?: string(name='throttlingResult', description='The result of the throttling check.\\\\`FC.PASS\\\\`: The request was not blocked by throttling.\\\\`FC.DENY\\\\`: The request was blocked by throttling.', example='FC.PASS'),
    }(name='basicInfo', description='The basic information about the API request.'),
    callerInfo?: {
      callerAccountId?: string(name='callerAccountId', description='The ID of the caller\\\\"s account.', example='241009849925897811'),
      callerIp?: string(name='callerIp', description='The IP address of the caller.', example='100.68.xxx.xxx'),
      callerType?: string(name='callerType', description='The type of the caller. Valid values:

1. customer: An Alibaba Cloud account.

2. sub: A RAM user.

3. AssumedRoleUser: A temporary identity that uses a Security Token Service (STS) token.', example='sub'),
      masterAccountId?: string(name='masterAccountId', description='The ID of the Alibaba Cloud account.', example='1973374733454118'),
      userAgent?: string(name='userAgent', description='The user agent.', example='AlibabaCloud API Workbench'),
    }(name='callerInfo', description='The information about the caller.'),
    parameters?: [ 
      {
        name?: string(name='name', description='The name of the request parameter.', example='InstanceType'),
        required?: boolean(name='required', description='Specifies whether the request parameter is required.', example='false'),
        type?: string(name='type', description='The type of the request parameter.', example='string'),
        value?: any(name='value', description='The value of the request parameter.', example='ecs.g6.large'),
      }
    ](name='parameters', description='The request parameters.'),
    responses?: {
      responseBody?: string(name='responseBody', description='The returned information.', example='-'),
      responseBodyFormat?: string(name='responseBodyFormat', description='The format of the response body. Valid values: \\\\`JSON\\\\`, \\\\`XML\\\\`, and \\\\`HTML\\\\`.', example='JSON'),
    }(name='responses', description='The information returned for the request.'),
  }(name='logInfo', description='The details of the API request log.'),
  requestId?: string(name='requestId', description='The ID of the request.', example='9BFC4AC1-6BE4-5405-BDEC-CA288D404812'),
}

model GetOwnRequestLogResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetOwnRequestLogResponseBody(name='body'),
}

/**
 * @summary Queries the details of an OpenAPI call log for your account using the request ID returned from an API call. This operation is used for troubleshooting.
 *
 * @description This API does not support authorization.
 *
 * @param request GetOwnRequestLogRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetOwnRequestLogResponse
 */
@context("getOwnRequestLogWithContext")
async function getOwnRequestLogWithOptions(request: GetOwnRequestLogRequest, headers: map[string]string, runtime: $RuntimeOptions): GetOwnRequestLogResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.logRequestId)) {
    query['logRequestId'] = request.logRequestId;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetOwnRequestLog',
    version = '2024-11-30',
    protocol = 'HTTPS',
    pathname = `/getOwnRequestLog`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the details of an OpenAPI call log for your account using the request ID returned from an API call. This operation is used for troubleshooting.
 *
 * @description This API does not support authorization.
 *
 * @param request GetOwnRequestLogRequest
 * @return GetOwnRequestLogResponse
 */
async function getOwnRequestLog(request: GetOwnRequestLogRequest): GetOwnRequestLogResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getOwnRequestLogWithOptions(request, headers, runtime);
}

model GetProductEndpointsRequest {
  product?: string(name='product', description='The product code.

- Call the GetRequestLog operation and find the product code in the response.

- Find the product code in the URL of the OpenAPI Portal page for the product. For example, <props="china">the URL for the Short Message Service (SMS) OpenAPI Portal page is https\\\\://api.aliyun.com/product/Dysmsapi. The product code is Dysmsapi.
  <props="intl">the URL for the Short Message Service (SMS) OpenAPI Portal page is https\\\\://api.alibabacloud.com/product/Dysmsapi. The product code is Dysmsapi.

This parameter is required.', example='Ecs'),
}

model GetProductEndpointsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: object(name='body'),
}

/**
 * @summary Retrieves the endpoint information for a product.
 *
 * @param request GetProductEndpointsRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetProductEndpointsResponse
 */
@context("getProductEndpointsWithContext")
async function getProductEndpointsWithOptions(request: GetProductEndpointsRequest, headers: map[string]string, runtime: $RuntimeOptions): GetProductEndpointsResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.product)) {
    query['product'] = request.product;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetProductEndpoints',
    version = '2024-11-30',
    protocol = 'HTTPS',
    pathname = `/product/endpoints`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves the endpoint information for a product.
 *
 * @param request GetProductEndpointsRequest
 * @return GetProductEndpointsResponse
 */
async function getProductEndpoints(request: GetProductEndpointsRequest): GetProductEndpointsResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getProductEndpointsWithOptions(request, headers, runtime);
}

model GetRequestLogRequest {
  logRequestId?: string(name='logRequestId', description='The request ID of the log to query. The request ID is the unique identifier of an API request. The ID must be in the UUID format and in uppercase.

This parameter is required.', example='123E4567-E89B-12D3-A456-426614174000'),
}

model GetRequestLogResponseBody = {
  logInfo?: {
    authenticationInfo?: {
      authenticationType?: string(name='authenticationType', description='The authentication type.

- AK: An AccessKey pair, which can be a permanent AccessKey pair, a temporary AccessKey pair, or an STS token.

- PRIVATEKEY: An AccessKey pair that uses asymmetric key encryption.

- BEARERTOKEN: An identity verification mechanism that is widely used in the OAuth 2.0 framework and cloud services.

- CUSTOM_SPI: An efficient and secure authentication method that is used for the delivery and management of Software as a Service (SaaS) products in Alibaba Cloud Marketplace.

- Anonymous: Anonymous access.

- DPS: Similar to an AccessKey pair, but uses a signature algorithm that is different from the official Alibaba Cloud algorithm. This method is specific to certain products.', example='AK'),
      signatureMethod?: string(name='signatureMethod', description='The signature method.

- HMAC-SHA1: The request is signed using the HMAC-SHA1 algorithm.

- HMAC-SHA256: The request is signed using the HMAC-SHA256 algorithm.', example='HMAC-SHA256'),
      signatureVersion?: string(name='signatureVersion', description='The signature version.', example='unknown'),
    }(name='authenticationInfo', description='The authentication information.'),
    basicInfo?: {
      accessDeniedDetail?: {
        authAction?: string(name='authAction', description='The specific operation that is denied.', example='openapiexplorer:GetRequestLog'),
        authPrincipalDisplayName?: string(name='authPrincipalDisplayName', description='The identity.', example='205618123456123456'),
        authPrincipalOwnerId?: string(name='authPrincipalOwnerId', description='The UID of the Alibaba Cloud account to which the current identity belongs.', example='1001234561234567'),
        authPrincipalType?: string(name='authPrincipalType', description='The identity type of the operator.', example='SubUser'),
        encodedDiagnosticMessage?: string(name='encodedDiagnosticMessage', description='The encoded diagnostic message. To obtain more diagnostic information, call the DecodeDiagnosticMessage operation of RAM.', example='-'),
        noPermissionType?: string(name='noPermissionType', description='The reason why the access is denied.', example='ImplicitDeny'),
        policyType?: string(name='policyType', description='The type of the policy that denies the access.', example='AccountLevelIdentityBasedPolicy'),
      }(name='accessDeniedDetail', description='The details of the access denied error. This field is returned only if an authentication error occurs for the specified request ID.'),
      api?: string(name='api', description='The name of the API that was called.', example='RunInstances'),
      apiDoc?: {
        alibabacloudSite?: string(name='alibabacloudSite', description='The link to the API reference on the Alibaba Cloud International Website (www\\\\.alibabacloud.com).', example='https://api.alibabacloud.com/document/Ecs/2014-05-26/RunInstances'),
        aliyunSite?: string(name='aliyunSite', description='The link to the API reference on the Alibaba Cloud China Website (www\\\\.aliyun.com).', example='https://api.aliyun.com/document/Ecs/2014-05-26/RunInstances'),
      }(name='apiDoc', description='The API reference information.'),
      apiStyle?: string(name='apiStyle', description='The API style. Valid values: ROA and RPC.', example='roa'),
      apiVersion?: string(name='apiVersion', description='The API version.', example='2014-05-26'),
      endpoint?: string(name='endpoint', description='The endpoint of the area where the endpoint is deployed.', example='ecs.cn-hangzhou.aliyuncs.com'),
      errorCode?: string(name='errorCode', description='The error code in the log. This field is empty if no error is reported for the request.', example='IncorrectStatus.TransitRouter'),
      errorMessage?: string(name='errorMessage', description='The error message in the log. This field is empty if no error is reported for the request.', example='The resource is not in a valid state for the operation.'),
      gatewayProcessTime?: string(name='gatewayProcessTime', description='The time when the gateway received the request. The time is in the yyyy-MM-ddTHH:mm:ssZ format. The time is displayed in UTC.', example='2025-01-21T07:43:06Z'),
      httpMethod?: string(name='httpMethod', description='The HTTP request method. Examples: GET, PUT, and POST.', example='GET'),
      httpStatusCode?: string(name='httpStatusCode', description='The HTTP status code in the log.', example='404'),
      logRequestId?: string(name='logRequestId', description='The request ID that you specified.', example='123E4567-E89B-12D3-A456-426614174000'),
      product?: string(name='product', description='The product code.', example='Ecs'),
      productName?: {
        cnName?: string(name='cnName', description='The Chinese name of the product.', example='云服务器 ECS'),
        enName?: string(name='enName', description='The English name of the product.', example='Elastic Compute Service'),
      }(name='productName', description='The product name, which includes the Chinese and English names.'),
      regionId?: string(name='regionId', description='The ID of the area where the endpoint is deployed.', example='cn-hangzhou'),
      requestDuration?: string(name='requestDuration', description='The time that elapses from when the gateway receives a request to when it returns a response. Unit: milliseconds (ms).', example='188'),
      sdkRequestTime?: string(name='sdkRequestTime', description='The time when the request was initiated. The time is in the yyyy-MM-ddTHH:mm:ssZ format. The time is displayed in UTC.', example='2025-01-21T07:43:06Z'),
      throttlingResult?: string(name='throttlingResult', description='The result of the throttling check. FC.PASS: The request was not blocked by the throttling check and no rate limiting occurred. FC.DENY: The request was blocked by the throttling check and rate limiting occurred.', example='FC.PASS'),
    }(name='basicInfo', description='The basic information about the API request.'),
    callerInfo?: {
      callerAccountId?: string(name='callerAccountId', description='The ID of the caller\\\\"s account.', example='241009849925897811'),
      callerIp?: string(name='callerIp', description='The IP address of the caller.', example='100.68.xxx.xxx'),
      callerType?: string(name='callerType', description='The type of the caller.

1. customer: Alibaba Cloud account

2. sub: RAM user

3. AssumedRoleUser: A temporary identity that is created using a Security Token Service (STS) token.', example='sub'),
      masterAccountId?: string(name='masterAccountId', description='The ID of the Alibaba Cloud account.', example='1973374733454118'),
      userAgent?: string(name='userAgent', description='The user agent.', example='AlibabaCloud API Workbench'),
    }(name='callerInfo', description='The information about the caller.'),
    parameters?: [ 
      {
        name?: string(name='name', description='The name of the request parameter.', example='InstanceType'),
        required?: boolean(name='required', description='Indicates whether the request parameter is required.', example='false'),
        type?: string(name='type', description='The type of the request parameter.', example='string'),
        value?: any(name='value', description='The value of the request parameter.', example='ecs.g6.large'),
      }
    ](name='parameters', description='The request parameter information.'),
    responses?: {
      responseBody?: string(name='responseBody', description='The response information.', example='-'),
      responseBodyFormat?: string(name='responseBodyFormat', description='The type of the response body. Valid values: JSON, XML, and HTML.', example='JSON'),
    }(name='responses', description='The response information that corresponds to the request.'),
  }(name='logInfo', description='The details of the API request log.'),
  requestId?: string(name='requestId', description='The ID of the request.', example='9BFC4AC1-6BE4-5405-BDEC-CA288D404812'),
}

model GetRequestLogResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetRequestLogResponseBody(name='body'),
}

/**
 * @summary Queries the details of a request log using the request ID that is returned from an API call. This operation is useful for troubleshooting.
 *
 * @description You can query call logs across different RAM users or Alibaba Cloud accounts using RAM authorization or role assumption. For more information, see [Cross-account API error diagnosis authorization](https://help.aliyun.com/document_detail/2868101.html).
 *
 * @param request GetRequestLogRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetRequestLogResponse
 */
@context("getRequestLogWithContext")
async function getRequestLogWithOptions(request: GetRequestLogRequest, headers: map[string]string, runtime: $RuntimeOptions): GetRequestLogResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.logRequestId)) {
    query['logRequestId'] = request.logRequestId;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetRequestLog',
    version = '2024-11-30',
    protocol = 'HTTPS',
    pathname = `/getRequestLog`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the details of a request log using the request ID that is returned from an API call. This operation is useful for troubleshooting.
 *
 * @description You can query call logs across different RAM users or Alibaba Cloud accounts using RAM authorization or role assumption. For more information, see [Cross-account API error diagnosis authorization](https://help.aliyun.com/document_detail/2868101.html).
 *
 * @param request GetRequestLogRequest
 * @return GetRequestLogResponse
 */
async function getRequestLog(request: GetRequestLogRequest): GetRequestLogResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getRequestLogWithOptions(request, headers, runtime);
}

model ListApiDefinitionsRequest {
  apiVersion?: string(name='apiVersion', description='The version of the API.

This parameter is required.', example='2014-05-26'),
  product?: string(name='product', description='The product code.

- Call the GetRequestLog operation to obtain the product code from the response.

- Find the product code in the URL of the OpenAPI Portal. For example, <props="china">the URL of the OpenAPI Portal for Short Message Service is https\\\\://api.aliyun.com/product/Dysmsapi. The product code for Short Message Service is Dysmsapi.
  <props="intl">the URL of the OpenAPI Portal for Short Message Service is https\\\\://api.alibabacloud.com/product/Dysmsapi. The product code for Short Message Service is Dysmsapi.

This parameter is required.', example='Ecs'),
}

model ListApiDefinitionsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: object(name='body'),
}

/**
 * @summary Retrieves the OpenAPI metadata for a product.
 *
 * @param request ListApiDefinitionsRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListApiDefinitionsResponse
 */
@context("listApiDefinitionsWithContext")
async function listApiDefinitionsWithOptions(request: ListApiDefinitionsRequest, headers: map[string]string, runtime: $RuntimeOptions): ListApiDefinitionsResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.apiVersion)) {
    query['apiVersion'] = request.apiVersion;
  }
  if (!$isNull(request.product)) {
    query['product'] = request.product;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListApiDefinitions',
    version = '2024-11-30',
    protocol = 'HTTPS',
    pathname = `/api/definitions`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves the OpenAPI metadata for a product.
 *
 * @param request ListApiDefinitionsRequest
 * @return ListApiDefinitionsResponse
 */
async function listApiDefinitions(request: ListApiDefinitionsRequest): ListApiDefinitionsResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listApiDefinitionsWithOptions(request, headers, runtime);
}

model ListApiMcpServerSystemToolsRequest {
  maxResults?: int32(name='maxResults', description='The maximum number of entries to return on each page. The maximum value is 100. The default value is 20.', example='20'),
  nextToken?: string(name='nextToken', description='The token to start the next query. Set this parameter to the NextToken value returned by the last API call.

> Do not specify this parameter for the first query. If a query does not return all results, pass the NextToken value from the previous response to the next query to continue.', example='AAAAAZjtYxxxxxxxx'),
  skip?: int32(name='skip', description='The number of entries to skip.', example='5'),
}

model ListApiMcpServerSystemToolsResponseBody = {
  maxResults?: int32(name='maxResults', description='The maximum number of entries to return on each page. The maximum value is 100. The default value is 20.', example='20'),
  nextToken?: string(name='nextToken', description='The NextToken value. Use this value to retrieve more results.

> If this parameter is not returned, no more results are available.', example='AAAAAZjtYxxxxxxxx'),
  requestId?: string(name='requestId', description='The request ID.', example='9BFC4AC1-6BE4-5405-BDEC-CA288D404812'),
  systemTools?: [ 
    {
      description?: string(name='description', description='The description of the system tool.', example='system tool description'),
      name?: string(name='name', description='The name of the system tool.', example='mcp-system'),
    }
  ](name='systemTools', description='The list of system tools.'),
  totalCount?: int32(name='totalCount', description='The total number of records.', example='22'),
}

model ListApiMcpServerSystemToolsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListApiMcpServerSystemToolsResponseBody(name='body'),
}

/**
 * @summary Lists the system tools supported by the MCP service.
 *
 * @param request ListApiMcpServerSystemToolsRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListApiMcpServerSystemToolsResponse
 */
@context("listApiMcpServerSystemToolsWithContext")
async function listApiMcpServerSystemToolsWithOptions(request: ListApiMcpServerSystemToolsRequest, headers: map[string]string, runtime: $RuntimeOptions): ListApiMcpServerSystemToolsResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.maxResults)) {
    query['maxResults'] = request.maxResults;
  }
  if (!$isNull(request.nextToken)) {
    query['nextToken'] = request.nextToken;
  }
  if (!$isNull(request.skip)) {
    query['skip'] = request.skip;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListApiMcpServerSystemTools',
    version = '2024-11-30',
    protocol = 'HTTPS',
    pathname = `/mcpSystemTools`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Lists the system tools supported by the MCP service.
 *
 * @param request ListApiMcpServerSystemToolsRequest
 * @return ListApiMcpServerSystemToolsResponse
 */
async function listApiMcpServerSystemTools(request: ListApiMcpServerSystemToolsRequest): ListApiMcpServerSystemToolsResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listApiMcpServerSystemToolsWithOptions(request, headers, runtime);
}

model ListApiMcpServersRequest {
  createTime?: string(name='createTime', description='The time when the API MCP server was created.', example='2024-10-30T02:10:13Z'),
  description?: string(name='description', description='The description of the API MCP service.', example='这是一个API MCP服务器。'),
  id?: string(name='id', description='The ID of the API MCP service.', example='v6ZZ7ftCzEILW***'),
  keyword?: string(name='keyword', description='The search keyword. Supports fuzzy search by API name and exact search by API ID.', example='oss'),
  language?: string(name='language', description='The language of the API reference for the API MCP service. The language of the prompt can affect the response from the AI. Valid values: \\\\`ZH_CN\\\\`, \\\\`EN_US\\\\`.', example='ZH_CN'),
  maxResults?: int32(name='maxResults', description='The maximum number of entries to return on each page for a paged query. The maximum value is 100. The default value is 20.', example='10'),
  nextToken?: string(name='nextToken', description='The token that is used to start the next query. Set this parameter to the \\\\`nextToken\\\\` value that was returned from the previous API call.

> This parameter is not required for the first query. If a query does not return all results, pass the \\\\`nextToken\\\\` value from the previous query to continue.', example='AAAAAZjtYxxxxxxxx'),
  skip?: int32(name='skip', description='The number of data entries to skip.', example='5'),
  sourceType?: string(name='sourceType', description='The type of the API MCP service.

- custom: a custom service

- system: a system service', example='system'),
  updateTime?: string(name='updateTime', description='The time when the API MCP server was last updated.', example='2024-06-05T02:27:39Z'),
}

model ListApiMcpServersResponseBody = {
  apiMcpServers?: [ 
    {
      additionalApiDescriptions?: [ 
        {
          apiName?: string(name='apiName', description='The API name.', example='DescribeRegions'),
          apiOverrideJson?: string(name='apiOverrideJson', description='The API metadata in JSON format, which can be used to overwrite the \\\\`summary\\\\` and \\\\`parameters\\\\` fields. For an example of the format, see https\\\\://api.alibabacloud.com/meta/v1/products/Ecs/versions/2014-05-26/apis/DescribeInstances/api.json.', example='{
  "summary": "本接口支持根据不同请求条件查询实例列表，并关联查询实例的详细信息。"
}'),
          apiVersion?: string(name='apiVersion', description='The POP version of the API that is exposed to the MCP server.', example='2014-05-26'),
          constParameters?: [ 
            {
              key?: string(name='key', description='The parameter name. Only top-level parameter names are supported. For ROA-style APIs, you can specify parameters such as \\\\`body.xx\\\\`. You cannot set values for nested parameters.', example='InstanceId'),
              value?: any(name='value', description='The value of the parameter.', example='1234'),
            }
          ](name='constParameters', description='A list of constant input parameters. These parameters are not included in the output during API parameter parsing.'),
          enableOutputSchema?: boolean(name='enableOutputSchema', description='Specifies whether to return the schema of the response parameters. Returning the schema increases the size of the API MCP server. The default value is null, which means the schema is not returned.', example='true'),
          executeCliCommand?: boolean(name='executeCliCommand', description='Specifies whether to return the command-line interface (CLI) command for execution. In this mode, the API call is not executed. Instead, the corresponding CLI command is returned. This mode is suitable for long-running tasks executed using the Alibaba Cloud CLI.', example='true'),
          product?: string(name='product', description='The product code.

- Call the GetRequestLog operation and obtain the product code from the response.

- Find the product code from the URL of the OpenAPI Portal. For example, the URL for the OpenAPI Portal of Short Message Service is https\\\\://api.alibabacloud.com/product/Dysmsapi. The product code is Dysmsapi.', example='Ecs'),
        }
      ](name='additionalApiDescriptions', description='A list of supplementary API descriptions.'),
      apis?: [ 
        {
          apiVersion?: string(name='apiVersion', description='The POP version of the API that is exposed to the MCP server.', example='2014-05-26'),
          product?: string(name='product', description='The product code.

- Call the GetRequestLog operation and obtain the product code from the response.

- Find the product code from the URL of the OpenAPI Portal. For example, the URL for the OpenAPI Portal of Short Message Service is https\\\\://api.alibabacloud.com/product/Dysmsapi. The product code is Dysmsapi.', example='Ecs'),
          selectors?: [ string ](name='selectors', description='A list of API name matching rules.'),
        }
      ](name='apis', description='A list of API information.'),
      assumeRoleExtraPolicy?: string(name='assumeRoleExtraPolicy', description='The extra policy for role assumption when multi-account access is enabled. If this policy is specified, the permissions for the role assumption are based on this policy and overwrite the permissions that are defined for the role.', example='{
  "Version": "1",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": [
        "ecs:Describe*",
        "vpc:Describe*",
        "vpc:List*"
      ],
      "Resource": "*"
    }
  ]
}'),
      assumeRoleName?: string(name='assumeRoleName', description='The name of the RAM role in the destination account that is assumed for cross-account operations when multi-account access is enabled.', example='test'),
      createTime?: string(name='createTime', description='The time when the API MCP server was created.', example='2024-12-10T03:20:21Z'),
      description?: string(name='description', description='The description of the API MCP server.', example='这是一个API MCP服务器。'),
      enableAssumeRole?: boolean(name='enableAssumeRole', description='Specifies whether to enable multi-account access.', example='true'),
      enableCustomVpcWhitelist?: boolean(name='enableCustomVpcWhitelist', description='Specifies whether to enable a custom VPC whitelist. If this parameter is disabled, the account-level configuration is used.', example='true'),
      id?: string(name='id', description='The ID of the API MCP service.', example='v6ZZ7ftCzEILW***'),
      instructions?: string(name='instructions', description='The MCP instruction. It prompts the large language model on how to use the MCP. The client must support the \\\\`Instructions\\\\` field of the standard MCP protocol.', example='test'),
      language?: string(name='language', description='The language of the API reference for the API MCP service. You can select Chinese or English. The language of the prompt can affect the response from the AI.', example='ZH_CN'),
      name?: string(name='name', description='The name of the MCP server. The name must be 3 to 64 characters in length. It can contain lowercase letters and digits, and cannot start with a digit. The name must be unique within the same Alibaba Cloud account.', example='mcp-demo'),
      oauthClientId?: string(name='oauthClientId', description='The custom OAuth client ID that is used for a custom OAuth configuration.

``Only web and native applications are supported. The OAuth scope must include `/acs/mcp-server`.``', example='403*************370'),
      prompts?: [ 
        {
          arguments?: [ 
            {
              description?: string(name='description', description='The description of the parameter.', example='argument description'),
              name?: string(name='name', description='The parameter name.', example='test'),
              required?: boolean(name='required', description='Specifies whether the parameter is required.', example='true'),
            }
          ](name='arguments', description='A list of parameters that the prompt supports.'),
          content?: string(name='content', description='The content of the prompt. Variables are specified in the \\\\`{{xxx}}\\\\` format. \\\\`xxx\\\\` is a variable that must be defined in the \\\\`arguments\\\\` parameter.', example='prompt正文，{{name}}'),
          description?: string(name='description', description='The description of the prompt.', example='prompt description'),
          name?: string(name='name', description='The name of the prompt.', example='test'),
        }
      ](name='prompts', description='A list of prompt configurations.'),
      publicAccess?: string(name='publicAccess', description='Specifies whether to enable access over the Internet.', example='on'),
      sourceType?: string(name='sourceType', description='The type of the API MCP service.

- custom: a custom service

- system: a system service', example='system'),
      systemMcpServerInfo?: {
        name?: string(name='name', description='The name of the system MCP service.', example='mcp-system'),
        product?: string(name='product', description='The product code.

- Call the GetRequestLog operation and obtain the product code from the response.

- Find the product code from the URL of the OpenAPI Portal. For example, the URL for the OpenAPI Portal of Short Message Service is https\\\\://api.alibabacloud.com/product/Dysmsapi. The product code is Dysmsapi.', example='Ecs'),
      }(name='systemMcpServerInfo', description='A list of system MCP services.'),
      systemTools?: [ string ](name='systemTools', description='A list of system tools.'),
      terraformTools?: [ 
        {
          async?: boolean(name='async', description='Specifies whether to execute tasks asynchronously. If this parameter is set to true, the system immediately proceeds to the next task after a task is initiated, without waiting for each resource operation to complete.', example='true'),
          code?: string(name='code', description='The code for the Terraform tool. For more information, see [HCL language overview](https://www.alibabacloud.com/help/en/terraform/terraform-configuration-and-hcl-language-overview).', example='variable "name" {
  default = "terraform-example"
}

provider "alicloud" {
  region = "cn-beijing"
}

resource "alicloud_vpc" "default" {
  ipv6_isp    = "BGP"
  description = "test"
  cidr_block  = "10.0.0.0/8"
  vpc_name    = var.name
  enable_ipv6 = true
}'),
          description?: string(name='description', description='The description of the Terraform tool.', example='Terraform Tool description'),
          destroyPolicy?: string(name='destroyPolicy', description='The deletion policy. After a task is executed, the system applies one of the following cleanup policies to temporary resources based on the task execution status.

- NEVER: Does not delete any created resources, regardless of whether the task succeeds or fails.

- ALWAYS: Immediately destroys all related resources after execution, regardless of whether the task succeeds or fails.

- ON_FAILURE: Deletes related resources only if the task fails. If the task succeeds, the resources are retained.', example='ON_FAILURE'),
          name?: string(name='name', description='The name of the Terraform tool.', example='test'),
        }
      ](name='terraformTools', description='A list of Terraform tools.'),
      updateTime?: string(name='updateTime', description='The time when the API MCP server was last updated.', example='2025-01-10T02:11:43Z'),
      urls?: {
        mcp?: string(name='mcp', description='The connection information for the streamable HTTP protocol. This protocol is recommended.', example='https://openapi-mcp.cn-hangzhou.aliyuncs.com/accounts/xxxx/custom/xxx/id/xxxx/mcp'),
        sse?: string(name='sse', description='The connection information for the Server-Sent Events (SSE) protocol.', example='https://openapi-mcp.cn-hangzhou.aliyuncs.com/accounts/xxxx/custom/xxx/id/xxxx/sse'),
        vpcMcp?: string(name='vpcMcp', description='The endpoint for the streamable HTTP protocol in a VPC.', example='https://openapi-mcp-cn.vpc-proxy.aliyuncs.com/accounts/xxxx/custom/xxx/id/xxxx/mcp'),
        vpcSse?: string(name='vpcSse', description='The endpoint for the SSE protocol in a VPC.', example='https://openapi-mcp-cn.vpc-proxy.aliyuncs.com/accounts/xxxx/custom/xxx/id/xxxx/sse'),
      }(name='urls', description='The connection information for the API MCP service.'),
      vpcWhitelists?: [ string ](name='vpcWhitelists', description='The VPC whitelist that specifies the allowed source VPCs after Internet access is disabled. If this parameter is not set or is left empty, the source is not restricted.'),
    }
  ](name='apiMcpServers', description='The list of queried API MCP servers.'),
  maxResults?: int32(name='maxResults', description='The maximum number of entries returned per page.', example='30'),
  nextToken?: string(name='nextToken', description='The paging cursor.

> If this parameter is not empty, more data is available.', example='AAAAAZjtYxxxxxxxx'),
  requestId?: string(name='requestId', description='The request ID.', example='9BFC4AC1-6BE4-5405-BDEC-CA288D404812'),
  totalCount?: int32(name='totalCount', description='The total number of entries.', example='5'),
}

model ListApiMcpServersResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListApiMcpServersResponseBody(name='body'),
}

/**
 * @summary This operation queries a list of all API MCP servers in an Alibaba Cloud account.
 *
 * @param request ListApiMcpServersRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListApiMcpServersResponse
 */
@context("listApiMcpServersWithContext")
async function listApiMcpServersWithOptions(request: ListApiMcpServersRequest, headers: map[string]string, runtime: $RuntimeOptions): ListApiMcpServersResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.createTime)) {
    query['createTime'] = request.createTime;
  }
  if (!$isNull(request.description)) {
    query['description'] = request.description;
  }
  if (!$isNull(request.id)) {
    query['id'] = request.id;
  }
  if (!$isNull(request.keyword)) {
    query['keyword'] = request.keyword;
  }
  if (!$isNull(request.language)) {
    query['language'] = request.language;
  }
  if (!$isNull(request.maxResults)) {
    query['maxResults'] = request.maxResults;
  }
  if (!$isNull(request.nextToken)) {
    query['nextToken'] = request.nextToken;
  }
  if (!$isNull(request.skip)) {
    query['skip'] = request.skip;
  }
  if (!$isNull(request.sourceType)) {
    query['sourceType'] = request.sourceType;
  }
  if (!$isNull(request.updateTime)) {
    query['updateTime'] = request.updateTime;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListApiMcpServers',
    version = '2024-11-30',
    protocol = 'HTTPS',
    pathname = `/apimcpservers`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary This operation queries a list of all API MCP servers in an Alibaba Cloud account.
 *
 * @param request ListApiMcpServersRequest
 * @return ListApiMcpServersResponse
 */
async function listApiMcpServers(request: ListApiMcpServersRequest): ListApiMcpServersResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listApiMcpServersWithOptions(request, headers, runtime);
}

model UpdateApiMcpServerRequest {
  additionalApiDescriptions?: [ 
    {
      apiName?: string(name='apiName', description='The API name.', example='DescribeRegions'),
      apiOverrideJson?: string(name='apiOverrideJson', description='The API metadata in JSON format. For more information about the format, see https\\\\://api.aliyun.com/meta/v1/products/Ecs/versions/2014-05-26/apis/DescribeInstances/api.json. You can overwrite the summary and parameters fields.', example='{
  "summary": "本接口支持根据不同请求条件查询实例列表，并关联查询实例的详细信息。"
}'),
      apiVersion?: string(name='apiVersion', description='The POP version of the API that is exposed to the MCP server.', example='2014-05-26'),
      constParameters?: [ 
        {
          key?: string(name='key', description='The parameter name. Only first-level parameter names are supported. For ROA-style APIs, you can set parameters such as body.xx. You cannot set values for parameters beyond the top level.', example='InstanceId'),
          value?: any(name='value', description='The value of the parameter.', example='1234'),
        }
      ](name='constParameters', description='A list of constant input parameters. These parameters are not included in the output during API parameter parsing.'),
      enableOutputSchema?: boolean(name='enableOutputSchema', description='Specifies whether to return the schema of the response parameters. Returning the response parameter schema increases the overall size of the API MCP server. The default value is null, which means the response parameter schema is not returned.', example='true'),
      executeCliCommand?: boolean(name='executeCliCommand', description='Specifies whether to return the command-line interface (CLI) command for execution. In this mode, the API call is not actually executed. Instead, the corresponding CLI command is returned. This is suitable for long-running tasks that need to be executed using Alibaba Cloud CLI.', example='true'),
      product?: string(name='product', description='The product code.

- Call the GetRequestLog operation to obtain the product code from the response.

- Find the product code from the URL of the product in OpenAPI Explorer. For example, the URL for Short Message Service is https\\\\://api.alibabacloud.com/product/Dysmsapi. The product code is \\\\`Dysmsapi\\\\`.', example='Ecs'),
    }
  ](name='additionalApiDescriptions', description='A list of supplementary API descriptions.'),
  apis?: [ 
    {
      apiVersion?: string(name='apiVersion', description='The POP version of the API that is exposed to the MCP server.', example='2014-05-26'),
      product?: string(name='product', description='The product code.

- Call the GetRequestLog operation to obtain the product code from the response.

- Find the product code from the URL of the product in OpenAPI Explorer. For example, the URL for Short Message Service is https\\\\://api.alibabacloud.com/product/Dysmsapi. The product code is \\\\`Dysmsapi\\\\`.', example='Ecs'),
      selectors?: [ string ](name='selectors', description='A list of API name matching rules.'),
    }
  ](name='apis', description='The list of APIs to modify.'),
  assumeRoleExtraPolicy?: string(name='assumeRoleExtraPolicy', description='An additional policy for role assumption when multi-account access is enabled. If this policy exists, the permissions for the role assumption are based on this policy, which overwrites the permissions defined for the role itself.', example='{
  "Version": "1",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": [
        "ecs:Describe*",
        "vpc:Describe*",
        "vpc:List*"
      ],
      "Resource": "*"
    }
  ]
}'),
  assumeRoleName?: string(name='assumeRoleName', description='The name of the RAM role in the destination account that is assumed for cross-account operations when multi-account access is enabled.', example='test'),
  description?: string(name='description', description='The description of the API MCP server.', example='这是一个API MCP服务器。'),
  enableAssumeRole?: boolean(name='enableAssumeRole', description='Specifies whether to enable multi-account access.', example='true'),
  enableCustomVpcWhitelist?: boolean(name='enableCustomVpcWhitelist', description='Specifies whether to enable a custom VPC whitelist. If this is disabled, the account-level configuration is used.', example='true'),
  instructions?: string(name='instructions', description='The MCP instruction. It prompts the large language model on how to use this MCP. The client must support the Instructions field of the standard MCP protocol.', example='test'),
  language?: string(name='language', description='The language of the API documentation for the API MCP service. You can select Chinese or English. Different language prompts may affect the AI\\\\"s response.', example='ZH_CN'),
  oauthClientId?: string(name='oauthClientId', description='The custom OAuth client ID to use when you select a custom OAuth configuration.

`Only web and native applications are supported. The OAuth scope must include /acs/mcp-server.`', example='403*************370'),
  prompts?: [ 
    {
      arguments?: [ 
        {
          description?: string(name='description', description='The description of the parameter.', example='argument description'),
          name?: string(name='name', description='The parameter name.', example='test'),
          required?: boolean(name='required', description='Specifies whether the parameter is required.', example='true'),
        }
      ](name='arguments', description='A list of parameters that the prompt supports.'),
      content?: string(name='content', description='The content of the prompt. Variables are specified in the {{xxx}} format. The xxx variable must be defined in the arguments parameter.', example='prompt正文，{{name}}'),
      description?: string(name='description', description='The description.', example='prompt description'),
      name?: string(name='name', description='The prompt name.', example='test'),
    }
  ](name='prompts', description='A list of prompt configurations.'),
  publicAccess?: string(name='publicAccess', description='Specifies whether to enable public network access.', example='on'),
  systemTools?: [ string ](name='systemTools', description='A list of system tools.'),
  terraformTools?: [ 
    {
      async?: boolean(name='async', description='Specifies whether to execute the task asynchronously. If this is enabled, the system immediately proceeds to the next task after it initiates a task execution, without waiting for each resource operation to complete.', example='true'),
      code?: string(name='code', description='The Terraform tool code. For more information, see [HCL language overview](https://www.alibabacloud.com/help/en/terraform/latest/hcl-language-overview).', example='variable "name" {
  default = "terraform-example"
}

provider "alicloud" {
  region = "cn-beijing"
}

resource "alicloud_vpc" "default" {
  ipv6_isp    = "BGP"
  description = "test"
  cidr_block  = "10.0.0.0/8"
  vpc_name    = var.name
  enable_ipv6 = true
}'),
      description?: string(name='description', description='The description of the Terraform tool.', example='Terraform Tool  description'),
      destroyPolicy?: string(name='destroyPolicy', description='The deletion policy. After a task is executed, the system applies one of the following cleanup policies to the temporary resources based on the task execution status.

- NEVER: Does not delete any created resources, regardless of whether the task succeeds or fails.

- ALWAYS: Immediately destroys all related resources after execution, regardless of whether the task succeeds or fails.

- ON_FAILURE: Deletes related resources only if the task fails. If the task succeeds, the resources are retained.', example='ON_FAILURE'),
      name?: string(name='name', description='The name of the Terraform tool.', example='test'),
    }
  ](name='terraformTools', description='A list of Terraform tools.'),
  vpcWhitelists?: [ string ](name='vpcWhitelists', description='The VPC whitelist that specifies the allowed source VPCs after public network access is disabled. If you do not set this parameter or leave it empty, access from all sources is allowed.'),
  clientToken?: string(name='clientToken', description='A client token that you can use to ensure the idempotence of the request. Generate a unique value from your client. The token can contain only ASCII characters and must be no more than 64 characters long. We recommend that you use a UUID. The token is valid for three days.', example='123e4567-e89b-12d3-a456-426655440000'),
  id?: string(name='id', description='The ID of the API MCP service.

This parameter is required.', example='v6ZZ7ftCzEILW***'),
}

model UpdateApiMcpServerResponseBody = {
  requestId?: string(name='requestId', description='The request ID.', example='9BFC4AC1-6BE4-5405-BDEC-CA288D404812'),
}

model UpdateApiMcpServerResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateApiMcpServerResponseBody(name='body'),
}

/**
 * @summary Updates an API MCP service.
 *
 * @param request UpdateApiMcpServerRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateApiMcpServerResponse
 */
@context("updateApiMcpServerWithContext")
async function updateApiMcpServerWithOptions(request: UpdateApiMcpServerRequest, headers: map[string]string, runtime: $RuntimeOptions): UpdateApiMcpServerResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.clientToken)) {
    query['clientToken'] = request.clientToken;
  }
  if (!$isNull(request.id)) {
    query['id'] = request.id;
  }

  var body : map[string]any = {};
  if (!$isNull(request.additionalApiDescriptions)) {
    body['additionalApiDescriptions'] = request.additionalApiDescriptions;
  }
  if (!$isNull(request.apis)) {
    body['apis'] = request.apis;
  }
  if (!$isNull(request.assumeRoleExtraPolicy)) {
    body['assumeRoleExtraPolicy'] = request.assumeRoleExtraPolicy;
  }
  if (!$isNull(request.assumeRoleName)) {
    body['assumeRoleName'] = request.assumeRoleName;
  }
  if (!$isNull(request.description)) {
    body['description'] = request.description;
  }
  if (!$isNull(request.enableAssumeRole)) {
    body['enableAssumeRole'] = request.enableAssumeRole;
  }
  if (!$isNull(request.enableCustomVpcWhitelist)) {
    body['enableCustomVpcWhitelist'] = request.enableCustomVpcWhitelist;
  }
  if (!$isNull(request.instructions)) {
    body['instructions'] = request.instructions;
  }
  if (!$isNull(request.language)) {
    body['language'] = request.language;
  }
  if (!$isNull(request.oauthClientId)) {
    body['oauthClientId'] = request.oauthClientId;
  }
  if (!$isNull(request.prompts)) {
    body['prompts'] = request.prompts;
  }
  if (!$isNull(request.publicAccess)) {
    body['publicAccess'] = request.publicAccess;
  }
  if (!$isNull(request.systemTools)) {
    body['systemTools'] = request.systemTools;
  }
  if (!$isNull(request.terraformTools)) {
    body['terraformTools'] = request.terraformTools;
  }
  if (!$isNull(request.vpcWhitelists)) {
    body['vpcWhitelists'] = request.vpcWhitelists;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpdateApiMcpServer',
    version = '2024-11-30',
    protocol = 'HTTPS',
    pathname = `/apimcpserver`,
    method = 'PATCH',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Updates an API MCP service.
 *
 * @param request UpdateApiMcpServerRequest
 * @return UpdateApiMcpServerResponse
 */
async function updateApiMcpServer(request: UpdateApiMcpServerRequest): UpdateApiMcpServerResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return updateApiMcpServerWithOptions(request, headers, runtime);
}

model UpdateApiMcpServerUserConfigRequest {
  enablePublicAccess?: boolean(name='enablePublicAccess', description='Specifies whether to enable public network access for all API MCP Servers in your account, including the system MCP Server. By default, public network access is enabled. If you disable it, you can only access the servers through their VPC domain names.', example='true'),
  vpcWhitelists?: [ string ](name='vpcWhitelists', description='The VPC whitelist. After disabling public network access, use this parameter to specify allowed source VPCs. If you do not set this parameter or leave it empty, all source VPCs are allowed.'),
}

model UpdateApiMcpServerUserConfigResponseBody = {
  requestId?: string(name='requestId', description='The request ID.', example='9BFC4AC1-6BE4-5405-BDEC-CA288D404812'),
}

model UpdateApiMcpServerUserConfigResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateApiMcpServerUserConfigResponseBody(name='body'),
}

/**
 * @summary Updates the global API MCP Server configuration for a user.
 *
 * @param request UpdateApiMcpServerUserConfigRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateApiMcpServerUserConfigResponse
 */
@context("updateApiMcpServerUserConfigWithContext")
async function updateApiMcpServerUserConfigWithOptions(request: UpdateApiMcpServerUserConfigRequest, headers: map[string]string, runtime: $RuntimeOptions): UpdateApiMcpServerUserConfigResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.enablePublicAccess)) {
    body['enablePublicAccess'] = request.enablePublicAccess;
  }
  if (!$isNull(request.vpcWhitelists)) {
    body['vpcWhitelists'] = request.vpcWhitelists;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpdateApiMcpServerUserConfig',
    version = '2024-11-30',
    protocol = 'HTTPS',
    pathname = `/userconfig/update`,
    method = 'PATCH',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Updates the global API MCP Server configuration for a user.
 *
 * @param request UpdateApiMcpServerUserConfigRequest
 * @return UpdateApiMcpServerUserConfigResponse
 */
async function updateApiMcpServerUserConfig(request: UpdateApiMcpServerUserConfigRequest): UpdateApiMcpServerUserConfigResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return updateApiMcpServerUserConfigWithOptions(request, headers, runtime);
}

