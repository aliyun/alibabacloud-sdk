/**
 *
 */
import Util;
import XML;
import Credential;
import FileForm;
import OpenApi;
import OpenApiUtil;
import EndpointUtil;

extends OpenApi;


init(config: OpenApi.Config){
  super(config);
  @endpointRule = 'regional';
  
  checkConfig(config);
  @endpoint = getEndpoint('winnexo', @regionId, @endpointRule, @network, @suffix, @endpointMap, @endpoint);
}

function getEndpoint(productId: string, regionId: string, endpointRule: string, network: string, suffix: string, endpointMap: map[string]string, endpoint: string) throws: string{
  if (!Util.empty(endpoint)) {
    return endpoint;
  }
  
  if (!Util.isUnset(endpointMap) && !Util.empty(endpointMap[regionId])) {
    return endpointMap[regionId];
  }
  return EndpointUtil.getEndpointRules(productId, regionId, endpointRule, network, suffix);
}

model CheckHealthRequest {
  tenantId?: string(name='tenantId', description='The tenant ID.', example='692318833855074'),
}

model CheckHealthResponseBody = {
  authSource?: string(name='authSource', description='The authentication source: bearer / aliyun_gateway.', example='aliyun_gateway'),
  callerType?: string(name='callerType', description='The caller type: user / aliyun_main / aliyun_ram / service.', example='aliyun_main'),
  code?: string(name='code', description='The response status code.', example='200'),
  digitalEmployeeName?: string(name='digitalEmployeeName', description='The name of the currently effective digital employee. This value is empty if not configured.', example='pcitc-magent'),
  message?: string(name='message', description='The status code description.', example='ok'),
  requestId?: string(name='requestId', description='The request ID.', example='F4A9EB1C-6952-5CCC-B1DC-355576FC82A7'),
  tenantId?: long(name='tenantId', description='The effective tenant ID.', example='21577'),
  userId?: long(name='userId', description='The platform user ID.', example='10001'),
}

model CheckHealthResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CheckHealthResponseBody(name='body'),
}

/**
 * @summary Performs a service health check.
 *
 * @param request CheckHealthRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CheckHealthResponse
 */
async function checkHealthWithOptions(request: CheckHealthRequest, headers: map[string]string, runtime: Util.RuntimeOptions): CheckHealthResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'CheckHealth',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/checkHealth`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Performs a service health check.
 *
 * @param request CheckHealthRequest
 * @return CheckHealthResponse
 */
async function checkHealth(request: CheckHealthRequest): CheckHealthResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return checkHealthWithOptions(request, headers, runtime);
}

model CreateConversationRequest {
  metadata?: string(name='metadata', description='A reserved field.', example='string_value'),
  objectId?: string(name='objectId', description='The primary key ID of the associated variable.', example='2676'),
  operatingObjectName?: [ any ](name='operatingObjectName', description='The operating object name.', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID.', example='10001'),
}

model CreateConversationShrinkRequest {
  metadata?: string(name='metadata', description='A reserved field.', example='string_value'),
  objectId?: string(name='objectId', description='The primary key ID of the associated variable.', example='2676'),
  operatingObjectNameShrink?: string(name='operatingObjectName', description='The operating object name.', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID.', example='10001'),
}

model CreateConversationResponseBody = {
  code?: string(name='code', description='The error code.', example='200'),
  conversationId?: string(name='conversationId', description='Id of the request', example='3a59769c-e631-4b48-84f3-c9bf3a8ae940'),
  createdAt?: string(name='createdAt', description='The time when the share was created.', example='1784513941206'),
  message?: string(name='message', description='The status code description.', example='The current zone list is illegal.'),
  metadata?: map[string]any(name='metadata', description='A reserved field for extension use.'),
  requestId?: string(name='requestId', description='The request ID.', example='A5241B90-8FF4-565C-977A-0CE1842AED72'),
  title?: string(name='title', description='The appointment title.', example='Incident RCA: alert-0885feb7-3d4b-4da5-90f0-0119dfbbf555:up0shc25tp0kueo0afeobvhk81'),
}

model CreateConversationResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateConversationResponseBody(name='body'),
}

/**
 * @summary Creates a session.
 *
 * @param tmpReq CreateConversationRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateConversationResponse
 */
async function createConversationWithOptions(tmpReq: CreateConversationRequest, headers: map[string]string, runtime: Util.RuntimeOptions): CreateConversationResponse {
  Util.validateModel(tmpReq);
  var request = new CreateConversationShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!Util.isUnset(tmpReq.operatingObjectName)) {
    request.operatingObjectNameShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.operatingObjectName, 'operatingObjectName', 'json');
  }
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.metadata)) {
    body['metadata'] = request.metadata;
  }
  if (!Util.isUnset(request.objectId)) {
    body['objectId'] = request.objectId;
  }
  if (!Util.isUnset(request.operatingObjectNameShrink)) {
    body['operatingObjectName'] = request.operatingObjectNameShrink;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'CreateConversation',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/createConversation`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates a session.
 *
 * @param request CreateConversationRequest
 * @return CreateConversationResponse
 */
async function createConversation(request: CreateConversationRequest): CreateConversationResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return createConversationWithOptions(request, headers, runtime);
}

model CreateCustomOrgRequest {
  corpId?: string(name='corpId', description='The corpId of the activated enterprise.

This parameter is required.', example='exampleCorpId'),
  corpName?: string(name='corpName', description='The organization name.', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID.', example='692318833855074'),
}

model CreateCustomOrgResponseBody = {
  code?: string(name='code', description='The response status code.', example='200'),
  corpId?: string(name='corpId', description='The enterprise ID.', example='exampleCorpId'),
  corpName?: string(name='corpName', description='The organization name.', example='string_value'),
  message?: string(name='message', description='The prompt message.', example='successful'),
  platformType?: string(name='platformType', description='The platform type.', example='string_value'),
  requestId?: string(name='requestId', description='The request ID.', example='72D1EC35-B174-5595-891F-2F0B3BFBE56F'),
}

model CreateCustomOrgResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateCustomOrgResponseBody(name='body'),
}

/**
 * @summary Registers a custom-only organization.
 *
 * @description Registers a custom-only organization for subsequent department tree push through syncOrgStructure.
 *     Registration logic:
 *     1. Validates the corpId format (must start with a lowercase letter or digit, 3-64 characters, hyphens allowed).
 *     2. Delegates to OrgSyncAuthorizedService to execute registration (includes permission verification and tenant-level uniqueness check).
 *     3. Returns the registration result.
 *     Note: Custom-only organizations support only department tree synchronization. Member relationship synchronization is not supported.
 *
 * @param request CreateCustomOrgRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateCustomOrgResponse
 */
async function createCustomOrgWithOptions(request: CreateCustomOrgRequest, headers: map[string]string, runtime: Util.RuntimeOptions): CreateCustomOrgResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.corpId)) {
    body['corpId'] = request.corpId;
  }
  if (!Util.isUnset(request.corpName)) {
    body['corpName'] = request.corpName;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'CreateCustomOrg',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/createCustomOrg`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Registers a custom-only organization.
 *
 * @description Registers a custom-only organization for subsequent department tree push through syncOrgStructure.
 *     Registration logic:
 *     1. Validates the corpId format (must start with a lowercase letter or digit, 3-64 characters, hyphens allowed).
 *     2. Delegates to OrgSyncAuthorizedService to execute registration (includes permission verification and tenant-level uniqueness check).
 *     3. Returns the registration result.
 *     Note: Custom-only organizations support only department tree synchronization. Member relationship synchronization is not supported.
 *
 * @param request CreateCustomOrgRequest
 * @return CreateCustomOrgResponse
 */
async function createCustomOrg(request: CreateCustomOrgRequest): CreateCustomOrgResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return createCustomOrgWithOptions(request, headers, runtime);
}

model CreateGroupAliDingChatRequest {
  chatId?: string(name='chatId', description='The session ID, typically used for JSSDK.

This parameter is required.', example='cidxxxxxxxx'),
  chatName?: string(name='chatName', description='The group chat name.', example='CustomerProjectGroup'),
  description?: string(name='description', description='The description of the AI assistant.', example='Customer group chat history'),
  directoryId?: string(name='directoryId', description='The directory ID.', example='exampleDirectoryId'),
  groupId?: string(name='groupId', description='The group ID.

This parameter is required.', example='exampleGroupId'),
  historyStartTime?: string(name='historyStartTime', description='The start time for collecting chat history.

This parameter is required.', example='2026-08-01'),
  notes?: string(name='notes', description='The meeting notes content (optional). The notes are used for auxiliary analysis.', example='Focus on identifying customer demands and to-do items'),
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital employee (operating object name, optional).', example='my-agent'),
  sourceTags?: string(name='sourceTags', description='The resource tags (optional, a JSON string list, such as ["tagA","tagB"]).', example='["Customer","GroupChat"]'),
  tenantId?: string(name='tenantId', description='The tenant ID.', example='10000'),
  updateFrequency?: {
    cron?: string(name='cron', description='The cron expression for timed scheduling.', example='0 2 * * *'),
    enabled?: boolean(name='enabled', description='Specifies whether the throttling rule is enabled. Valid values:
- true: Enabled.
- false: Disabled.', example='true'),
    preset?: string(name='preset', description='The preset mode. You can ignore this parameter.', example='hourly'),
  }(name='updateFrequency', description='The feature update frequency.'),
}

model CreateGroupAliDingChatShrinkRequest {
  chatId?: string(name='chatId', description='The session ID, typically used for JSSDK.

This parameter is required.', example='cidxxxxxxxx'),
  chatName?: string(name='chatName', description='The group chat name.', example='CustomerProjectGroup'),
  description?: string(name='description', description='The description of the AI assistant.', example='Customer group chat history'),
  directoryId?: string(name='directoryId', description='The directory ID.', example='exampleDirectoryId'),
  groupId?: string(name='groupId', description='The group ID.

This parameter is required.', example='exampleGroupId'),
  historyStartTime?: string(name='historyStartTime', description='The start time for collecting chat history.

This parameter is required.', example='2026-08-01'),
  notes?: string(name='notes', description='The meeting notes content (optional). The notes are used for auxiliary analysis.', example='Focus on identifying customer demands and to-do items'),
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital employee (operating object name, optional).', example='my-agent'),
  sourceTags?: string(name='sourceTags', description='The resource tags (optional, a JSON string list, such as ["tagA","tagB"]).', example='["Customer","GroupChat"]'),
  tenantId?: string(name='tenantId', description='The tenant ID.', example='10000'),
  updateFrequencyShrink?: string(name='updateFrequency', description='The feature update frequency.'),
}

model CreateGroupAliDingChatResponseBody = {
  chatId?: string(name='chatId', description='The session ID, typically used for JSSDK.', example='cidxxxxxxxx'),
  code?: string(name='code', description='The response code.', example='200'),
  directoryId?: string(name='directoryId', description='The directory ID.', example='exampleDirectoryId'),
  gmtCreate?: string(name='gmtCreate', description='The creation time.', example='2026-08-18T10:00:00Z'),
  groupId?: string(name='groupId', description='The project group ID.', example='exampleGroupId'),
  message?: string(name='message', description='The error details.', example='ok'),
  name?: string(name='name', description='The bot name.', example='CustomerProjectGroup'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  scope?: string(name='scope', description='The permission scope.', example='GROUP'),
  sourceId?: string(name='sourceId', description='The unique identifier on the business system side, that is, the business ID.', example='exampleSourceId'),
  status?: string(name='status', description='The signing status. Valid values:
- CREATED: Created but not signed.
- SUCCESS: Signed.
- STOP: Terminated.', example='PENDING'),
}

model CreateGroupAliDingChatResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateGroupAliDingChatResponseBody(name='body'),
}

/**
 * @summary Creates a DingTalk group chat knowledge resource in a group.
 *
 * @description ## Operation description
 * - Connects a specified DingTalk group chat to a group knowledge base that the caller has joined.
 * - The resource type is fixed to ALI_DING, the scope is fixed to GROUP, and the owning user is resolved from the gateway authentication identity.
 * - groupId, chatId, and historyStartTime are required.
 * - updateFrequency can be configured by using a preset or a five-field cron expression for subsequent synchronization frequency.
 * - The server verifies the caller\\"s group membership, the target group directory permissions, and the uniqueness of chatId within the scope.
 *
 * @param tmpReq CreateGroupAliDingChatRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateGroupAliDingChatResponse
 */
async function createGroupAliDingChatWithOptions(tmpReq: CreateGroupAliDingChatRequest, headers: map[string]string, runtime: Util.RuntimeOptions): CreateGroupAliDingChatResponse {
  Util.validateModel(tmpReq);
  var request = new CreateGroupAliDingChatShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!Util.isUnset(tmpReq.updateFrequency)) {
    request.updateFrequencyShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.updateFrequency, 'updateFrequency', 'json');
  }
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.chatId)) {
    body['chatId'] = request.chatId;
  }
  if (!Util.isUnset(request.chatName)) {
    body['chatName'] = request.chatName;
  }
  if (!Util.isUnset(request.description)) {
    body['description'] = request.description;
  }
  if (!Util.isUnset(request.directoryId)) {
    body['directoryId'] = request.directoryId;
  }
  if (!Util.isUnset(request.groupId)) {
    body['groupId'] = request.groupId;
  }
  if (!Util.isUnset(request.historyStartTime)) {
    body['historyStartTime'] = request.historyStartTime;
  }
  if (!Util.isUnset(request.notes)) {
    body['notes'] = request.notes;
  }
  if (!Util.isUnset(request.operatingObjectName)) {
    body['operatingObjectName'] = request.operatingObjectName;
  }
  if (!Util.isUnset(request.sourceTags)) {
    body['sourceTags'] = request.sourceTags;
  }
  if (!Util.isUnset(request.updateFrequencyShrink)) {
    body['updateFrequency'] = request.updateFrequencyShrink;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'CreateGroupAliDingChat',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/createGroupAliDingChat`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates a DingTalk group chat knowledge resource in a group.
 *
 * @description ## Operation description
 * - Connects a specified DingTalk group chat to a group knowledge base that the caller has joined.
 * - The resource type is fixed to ALI_DING, the scope is fixed to GROUP, and the owning user is resolved from the gateway authentication identity.
 * - groupId, chatId, and historyStartTime are required.
 * - updateFrequency can be configured by using a preset or a five-field cron expression for subsequent synchronization frequency.
 * - The server verifies the caller\\"s group membership, the target group directory permissions, and the uniqueness of chatId within the scope.
 *
 * @param request CreateGroupAliDingChatRequest
 * @return CreateGroupAliDingChatResponse
 */
async function createGroupAliDingChat(request: CreateGroupAliDingChatRequest): CreateGroupAliDingChatResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return createGroupAliDingChatWithOptions(request, headers, runtime);
}

model CreateKnowledgeBaseAliDingDocRequest {
  description?: string(name='description', description='The description of the alias.', example='created by eventbridge'),
  directoryId?: string(name='directoryId', description='The directory ID.', example='exampleDirectoryId'),
  filePublicUrl?: string(name='filePublicUrl', description='The publicly accessible URL of the AliDing online document.

This parameter is required.', example='https://example.com/winnexo/resource'),
  knowledgeId?: string(name='knowledgeId', description='Not supported. This parameter is ignored.', example='exampleKnowledgeId'),
  name?: string(name='name', description='The name.

This parameter is required.', example='p-toolset-4dd55d81-f98f-4806-8596-43f1c95f1ff0'),
  operatingObjectName?: string(name='operatingObjectName', description='The digital employee name (operating object name, optional).', example='string_value'),
  sourceTags?: string(name='sourceTags', description='The resource tags (optional, a JSON string list, such as ["tagA","tagB"]).', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID.', example='520539530998273'),
}

model CreateKnowledgeBaseAliDingDocResponseBody = {
  code?: string(name='code', description='The result code.', example='200'),
  directoryId?: string(name='directoryId', description='The directory ID.', example='exampleDirectoryId'),
  filePublicUrl?: string(name='filePublicUrl', description='The public URL of the document (echoes the input parameter).', example='https://example.com/winnexo/resource'),
  gmtCreate?: string(name='gmtCreate', description='The creation time.', example='2026-05-22 16:03:27'),
  message?: string(name='message', description='The error details.', example='success'),
  name?: string(name='name', description='The name.', example='p-toolset-b8a1de80-e9f5-49f3-8a12-873d378889c6'),
  requestId?: string(name='requestId', description='Id of the request', example='F42FC60B-C54D-5DFB-A8EC-04625BFFF1F7'),
  scope?: string(name='scope', description='The permission scope.', example='repo'),
  sourceId?: string(name='sourceId', description='The unique identifier on the business system side, which is the business ID.', example='8'),
  status?: string(name='status', description='The refund status. Query this field to confirm the refund status during processing. Valid values:
- SUCCESS: All refunds are successful.
- FAIL: The refund failed.
- WAIT_PAY: Waiting for refund.
- EXPIRE: The refund has expired.
- PAYING: The refund is being processed.
- TERMINATE: The refund is terminated.', example='{\\\\"observedGeneration\\\\": 7, \\\\"servicesInstances\\\\": {}, \\\\"observedTime\\\\": \\\\"2025-12-17T11:57:07Z\\\\", \\\\"servicesWithPendingChanges\\\\": [], \\\\"latestEnvironmentDeploymentName\\\\": \\\\"manual-1765972627273-k7GZvr\\\\"}'),
}

model CreateKnowledgeBaseAliDingDocResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateKnowledgeBaseAliDingDocResponseBody(name='body'),
}

/**
 * @summary Uploads an AliDing online document to the enterprise knowledge base. Management permissions are required.
 *
 * @description ## Request description
 * - This operation adds an AliDing online document to a specified enterprise knowledge base.
 * - The caller must have the `DEVELOPMENT_KB_MANAGE` permission.
 * - `source_type` is fixed to `ONLINE_DOC`, `platform` is fixed to `ALI_DING`, and `scope` is fixed to `TENANT`.
 * - If `directoryId` is not provided, the document is bound to the root directory of the current digital employee by default. If provided, it must be a valid directory ID under the current tenant.
 * - The `filePublicUrl` parameter is required and specifies the publicly accessible URL of the AliDing online document to upload.
 * - Optional parameters include `operatingObjectName` (digital employee name), `description` (resource description), `knowledgeId` (knowledge base ID), and `sourceTags` (resource tags).
 * - A successful response returns information about the newly created resource, such as `sourceId`, `name`, `status`, `directoryId`, and creation time.
 *
 * @param request CreateKnowledgeBaseAliDingDocRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateKnowledgeBaseAliDingDocResponse
 */
async function createKnowledgeBaseAliDingDocWithOptions(request: CreateKnowledgeBaseAliDingDocRequest, headers: map[string]string, runtime: Util.RuntimeOptions): CreateKnowledgeBaseAliDingDocResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.description)) {
    body['description'] = request.description;
  }
  if (!Util.isUnset(request.directoryId)) {
    body['directoryId'] = request.directoryId;
  }
  if (!Util.isUnset(request.filePublicUrl)) {
    body['filePublicUrl'] = request.filePublicUrl;
  }
  if (!Util.isUnset(request.knowledgeId)) {
    body['knowledgeId'] = request.knowledgeId;
  }
  if (!Util.isUnset(request.name)) {
    body['name'] = request.name;
  }
  if (!Util.isUnset(request.operatingObjectName)) {
    body['operatingObjectName'] = request.operatingObjectName;
  }
  if (!Util.isUnset(request.sourceTags)) {
    body['sourceTags'] = request.sourceTags;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'CreateKnowledgeBaseAliDingDoc',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/createKnowledgeBaseAlidingDoc`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Uploads an AliDing online document to the enterprise knowledge base. Management permissions are required.
 *
 * @description ## Request description
 * - This operation adds an AliDing online document to a specified enterprise knowledge base.
 * - The caller must have the `DEVELOPMENT_KB_MANAGE` permission.
 * - `source_type` is fixed to `ONLINE_DOC`, `platform` is fixed to `ALI_DING`, and `scope` is fixed to `TENANT`.
 * - If `directoryId` is not provided, the document is bound to the root directory of the current digital employee by default. If provided, it must be a valid directory ID under the current tenant.
 * - The `filePublicUrl` parameter is required and specifies the publicly accessible URL of the AliDing online document to upload.
 * - Optional parameters include `operatingObjectName` (digital employee name), `description` (resource description), `knowledgeId` (knowledge base ID), and `sourceTags` (resource tags).
 * - A successful response returns information about the newly created resource, such as `sourceId`, `name`, `status`, `directoryId`, and creation time.
 *
 * @param request CreateKnowledgeBaseAliDingDocRequest
 * @return CreateKnowledgeBaseAliDingDocResponse
 */
async function createKnowledgeBaseAliDingDoc(request: CreateKnowledgeBaseAliDingDocRequest): CreateKnowledgeBaseAliDingDocResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return createKnowledgeBaseAliDingDocWithOptions(request, headers, runtime);
}

model CreateKnowledgeBaseDirectoryRequest {
  description?: string(name='description', description='The description of the AI assistant.', example='recorder function'),
  name?: string(name='name', description='The image name.

This parameter is required.', example='oklabs_tongyici'),
  parentDirectoryId?: string(name='parentDirectoryId', description='The directory ID.', example='wd-lxykjnnw4lyl9eq'),
  tenantId?: string(name='tenantId', description='The tenant ID.', example='692318833855074'),
}

model CreateKnowledgeBaseDirectoryResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  description?: string(name='description', description='The description of the to-do card type.', example='This is default function description by fc-deploy component'),
  directoryId?: string(name='directoryId', description='The directory ID.', example='exampleDirectoryId'),
  directoryKind?: string(name='directoryKind', description='The directory type.', example='string_value'),
  gmtCreate?: long(name='gmtCreate', description='The creation time.', example='2025-11-14T02:18:27Z'),
  gmtModified?: long(name='gmtModified', description='The last modification time.', example='2026-01-19T01:48:56Z'),
  message?: string(name='message', description='The description of the status code.', example='successful'),
  name?: string(name='name', description='The name.', example='p-toolset-89550434-4e20-4e4e-bcac-9ab81b82c5b3'),
  parentDirectoryId?: string(name='parentDirectoryId', description='The directory ID.', example='wd-lxykjnnw4lyl9eq'),
  path?: string(name='path', description='The path of the node.', example='UVCIpI0siUski9iw'),
  requestId?: string(name='requestId', description='The request ID.', example='C474BFC7-7B11-5D92-971E-74AA82EC495B'),
}

model CreateKnowledgeBaseDirectoryResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateKnowledgeBaseDirectoryResponseBody(name='body'),
}

/**
 * @summary Creates a category directory in the enterprise knowledge base.
 *
 * @description ## Request description
 * - This operation allows users with the `DEVELOPMENT_KB_MANAGE` permission to create a category for the enterprise knowledge base.
 * - You can specify a parent category ID when creating a category. If no parent category ID is specified, the new category is mounted directly under the root directory of the enterprise knowledge base.
 * - The system automatically checks for name conflicts and directory depth limits.
 * - `tenant_id` and `user_id` are obtained only through authentication. These parameters are ignored even if they are provided in the request body.
 * - Ensure that the specified `parentDirectoryId` (if any) belongs to the current tenant.
 *
 * @param request CreateKnowledgeBaseDirectoryRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateKnowledgeBaseDirectoryResponse
 */
async function createKnowledgeBaseDirectoryWithOptions(request: CreateKnowledgeBaseDirectoryRequest, headers: map[string]string, runtime: Util.RuntimeOptions): CreateKnowledgeBaseDirectoryResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.description)) {
    body['description'] = request.description;
  }
  if (!Util.isUnset(request.name)) {
    body['name'] = request.name;
  }
  if (!Util.isUnset(request.parentDirectoryId)) {
    body['parentDirectoryId'] = request.parentDirectoryId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'CreateKnowledgeBaseDirectory',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/createKnowledgeBaseDirectory`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates a category directory in the enterprise knowledge base.
 *
 * @description ## Request description
 * - This operation allows users with the `DEVELOPMENT_KB_MANAGE` permission to create a category for the enterprise knowledge base.
 * - You can specify a parent category ID when creating a category. If no parent category ID is specified, the new category is mounted directly under the root directory of the enterprise knowledge base.
 * - The system automatically checks for name conflicts and directory depth limits.
 * - `tenant_id` and `user_id` are obtained only through authentication. These parameters are ignored even if they are provided in the request body.
 * - Ensure that the specified `parentDirectoryId` (if any) belongs to the current tenant.
 *
 * @param request CreateKnowledgeBaseDirectoryRequest
 * @return CreateKnowledgeBaseDirectoryResponse
 */
async function createKnowledgeBaseDirectory(request: CreateKnowledgeBaseDirectoryRequest): CreateKnowledgeBaseDirectoryResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return createKnowledgeBaseDirectoryWithOptions(request, headers, runtime);
}

model CreateKnowledgeBaseFileRequest {
  description?: string(name='description', description='The description of the alias.', example='hangzhou-release-version-3-eventbridge-numeric-queue-fix-20260529'),
  directoryId?: string(name='directoryId', description='The directory ID.', example='exampleDirectoryId'),
  fileExt?: string(name='fileExt', description='The file name extension. This parameter is optional. Examples: pdf and docx.', example='string_value'),
  fileName?: string(name='fileName', description='The file name.', example='BasicSimilarityScorer.cava'),
  filePath?: string(name='filePath', description='The file path.

This parameter is required.', example='bi/batch-query-service.app.yaml'),
  filePublicUrl?: string(name='filePublicUrl', description='The publicly accessible URL of the DingTalk online document.', example='https://example.com/winnexo/resource'),
  fileRecordId?: string(name='fileRecordId', description='The file record ID. This parameter is optional and corresponds to settings.file_record_id.', example='exampleFileRecordId'),
  knowledgeId?: string(name='knowledgeId', description='Not supported. Ignore this parameter.', example='exampleKnowledgeId'),
  name?: string(name='name', description='The name of the AI assistant.

This parameter is required.', example='oklabs_tongyici'),
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital employee (operating object name). This parameter is optional.', example='string_value'),
  sourceTags?: string(name='sourceTags', description='The resource labels. This parameter is optional. Specify a JSON string list, such as ["tagA","tagB"].', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID.', example='692318833855074'),
}

model CreateKnowledgeBaseFileResponseBody = {
  code?: string(name='code', description='The response status code.', example='200'),
  directoryId?: string(name='directoryId', description='The directory ID.', example='exampleDirectoryId'),
  gmtCreate?: string(name='gmtCreate', description='The creation time in ISO 8601 format.', example='2025-11-12T03:08:56Z'),
  message?: string(name='message', description='The response message.', example='success'),
  name?: string(name='name', description='The image name.', example='oklabs_tongyici'),
  requestId?: string(name='requestId', description='The request ID.', example='911656E1-9A09-5C77-BAAD-915EB4958D68'),
  scope?: string(name='scope', description='The export scope. Valid values:
- ALL: all.
- SELECT: selected rows.', example='user_info projects pull_requests hook gists emails'),
  sourceId?: string(name='sourceId', description='The unique identifier on the business system side, which is the business ID.', example='2001549'),
  status?: string(name='status', description='The store status.', example='200'),
}

model CreateKnowledgeBaseFileResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateKnowledgeBaseFileResponseBody(name='body'),
}

/**
 * @summary Uploads a specified file to the enterprise knowledge base. Management permissions are required.
 *
 * @description ## Operation description
 * - This operation uploads a file to the enterprise knowledge base.
 * - The `DEVELOPMENT_KB_MANAGE` permission is required to call this operation.
 * - You must provide the Object Storage Service (OSS) persistent address (`filePath`) of the file when uploading.
 * - Optional parameters include the public access URL and original file name to enhance the completeness of file information.
 * - If `directoryId` is specified, the file is placed in the corresponding enterprise knowledge base directory. Otherwise, the file is bound to the default root directory of the current digital employee.
 * - You can use `sourceTags` to add labels to resources for subsequent management and retrieval.
 * - This operation initiates a billing item (UNSTRUCTURED_PARSE). Ensure that your account balance is sufficient.
 *
 * @param request CreateKnowledgeBaseFileRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateKnowledgeBaseFileResponse
 */
async function createKnowledgeBaseFileWithOptions(request: CreateKnowledgeBaseFileRequest, headers: map[string]string, runtime: Util.RuntimeOptions): CreateKnowledgeBaseFileResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.description)) {
    body['description'] = request.description;
  }
  if (!Util.isUnset(request.directoryId)) {
    body['directoryId'] = request.directoryId;
  }
  if (!Util.isUnset(request.fileExt)) {
    body['fileExt'] = request.fileExt;
  }
  if (!Util.isUnset(request.fileName)) {
    body['fileName'] = request.fileName;
  }
  if (!Util.isUnset(request.filePath)) {
    body['filePath'] = request.filePath;
  }
  if (!Util.isUnset(request.filePublicUrl)) {
    body['filePublicUrl'] = request.filePublicUrl;
  }
  if (!Util.isUnset(request.fileRecordId)) {
    body['fileRecordId'] = request.fileRecordId;
  }
  if (!Util.isUnset(request.knowledgeId)) {
    body['knowledgeId'] = request.knowledgeId;
  }
  if (!Util.isUnset(request.name)) {
    body['name'] = request.name;
  }
  if (!Util.isUnset(request.operatingObjectName)) {
    body['operatingObjectName'] = request.operatingObjectName;
  }
  if (!Util.isUnset(request.sourceTags)) {
    body['sourceTags'] = request.sourceTags;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'CreateKnowledgeBaseFile',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/createKnowledgeBaseFile`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Uploads a specified file to the enterprise knowledge base. Management permissions are required.
 *
 * @description ## Operation description
 * - This operation uploads a file to the enterprise knowledge base.
 * - The `DEVELOPMENT_KB_MANAGE` permission is required to call this operation.
 * - You must provide the Object Storage Service (OSS) persistent address (`filePath`) of the file when uploading.
 * - Optional parameters include the public access URL and original file name to enhance the completeness of file information.
 * - If `directoryId` is specified, the file is placed in the corresponding enterprise knowledge base directory. Otherwise, the file is bound to the default root directory of the current digital employee.
 * - You can use `sourceTags` to add labels to resources for subsequent management and retrieval.
 * - This operation initiates a billing item (UNSTRUCTURED_PARSE). Ensure that your account balance is sufficient.
 *
 * @param request CreateKnowledgeBaseFileRequest
 * @return CreateKnowledgeBaseFileResponse
 */
async function createKnowledgeBaseFile(request: CreateKnowledgeBaseFileRequest): CreateKnowledgeBaseFileResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return createKnowledgeBaseFileWithOptions(request, headers, runtime);
}

model CreateKnowledgeBaseTextRequest {
  description?: string(name='description', description='The description of the alias.', example='InterviewMaster operations and health check service'),
  directoryId?: string(name='directoryId', description='The folder ID.', example='exampleDirectoryId'),
  knowledgeId?: string(name='knowledgeId', description='Not supported. Ignore this parameter.', example='exampleKnowledgeId'),
  name?: string(name='name', description='The image name.

This parameter is required.', example='KL_tongyici'),
  operatingObjectName?: string(name='operatingObjectName', description='The name of the operating object.', example='string_value'),
  sourceTags?: string(name='sourceTags', description='The source tags.', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID.', example='10001'),
  textContent?: string(name='textContent', description='The message content for text messages.

This parameter is required.', example='string_value'),
}

model CreateKnowledgeBaseTextResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  directoryId?: string(name='directoryId', description='The folder ID.', example='exampleDirectoryId'),
  gmtCreate?: string(name='gmtCreate', description='The creation time.', example='2026-04-22T07:10:40.000+00:00'),
  message?: string(name='message', description='The error message returned when the request fails.', example='The current zone list is illegal.'),
  name?: string(name='name', description='The image name.', example='p-default-af484147-e026-487b-a9eb-bd25464f0667'),
  requestId?: string(name='requestId', description='The request ID.', example='9005F6D0-748F-559D-ABDA-F4F31B983316'),
  scope?: string(name='scope', description='The permission scope.', example='read:user,read:repo,write:repo,read:org,read:group'),
  sourceId?: string(name='sourceId', description='The data source ID.', example='8'),
  status?: string(name='status', description='The status.', example='200'),
}

model CreateKnowledgeBaseTextResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateKnowledgeBaseTextResponseBody(name='body'),
}

/**
 * @summary Adds plain text content to a specified enterprise knowledge base.
 *
 * @description ## Operation description
 * - This API is used to upload plain text information to an enterprise knowledge base. The caller must have the required management permissions.
 * - The `textContent` field is required and represents the plain text content to upload.
 * - Optional parameters include the digital employee name (`operatingObjectName`) and resource description (`description`), which allow users to customize additional details.
 * - If `directoryId` is provided, the uploaded text is attached to the specified knowledge base folder. If not provided, the text is attached to the root folder of the current digital employee by default.
 * - You can use `sourceTags` to add labels to resources for easier management and retrieval.
 * - Before invoking this operation, make sure that you have correctly configured the authentication method (AK, BearerToken, and APP authentication are supported) and have the `DEVELOPMENT_KB_MANAGE` permission.
 *
 * @param request CreateKnowledgeBaseTextRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateKnowledgeBaseTextResponse
 */
async function createKnowledgeBaseTextWithOptions(request: CreateKnowledgeBaseTextRequest, headers: map[string]string, runtime: Util.RuntimeOptions): CreateKnowledgeBaseTextResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.description)) {
    body['description'] = request.description;
  }
  if (!Util.isUnset(request.directoryId)) {
    body['directoryId'] = request.directoryId;
  }
  if (!Util.isUnset(request.knowledgeId)) {
    body['knowledgeId'] = request.knowledgeId;
  }
  if (!Util.isUnset(request.name)) {
    body['name'] = request.name;
  }
  if (!Util.isUnset(request.operatingObjectName)) {
    body['operatingObjectName'] = request.operatingObjectName;
  }
  if (!Util.isUnset(request.sourceTags)) {
    body['sourceTags'] = request.sourceTags;
  }
  if (!Util.isUnset(request.textContent)) {
    body['textContent'] = request.textContent;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'CreateKnowledgeBaseText',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/createKnowledgeBaseText`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Adds plain text content to a specified enterprise knowledge base.
 *
 * @description ## Operation description
 * - This API is used to upload plain text information to an enterprise knowledge base. The caller must have the required management permissions.
 * - The `textContent` field is required and represents the plain text content to upload.
 * - Optional parameters include the digital employee name (`operatingObjectName`) and resource description (`description`), which allow users to customize additional details.
 * - If `directoryId` is provided, the uploaded text is attached to the specified knowledge base folder. If not provided, the text is attached to the root folder of the current digital employee by default.
 * - You can use `sourceTags` to add labels to resources for easier management and retrieval.
 * - Before invoking this operation, make sure that you have correctly configured the authentication method (AK, BearerToken, and APP authentication are supported) and have the `DEVELOPMENT_KB_MANAGE` permission.
 *
 * @param request CreateKnowledgeBaseTextRequest
 * @return CreateKnowledgeBaseTextResponse
 */
async function createKnowledgeBaseText(request: CreateKnowledgeBaseTextRequest): CreateKnowledgeBaseTextResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return createKnowledgeBaseTextWithOptions(request, headers, runtime);
}

model CreatePersonalAliDingChatRequest {
  chatId?: string(name='chatId', description='The DingTalk group chat session ID.

This parameter is required.', example='cidxxxxxxxx'),
  chatName?: string(name='chatName', description='The group chat name.', example='CustomerProjectGroup'),
  description?: string(name='description', description='The pipeline description.', example='Customer group chat history'),
  directoryId?: string(name='directoryId', description='The folder ID.', example='exampleDirectoryId'),
  historyStartTime?: string(name='historyStartTime', description='The start time for collecting chat history.

This parameter is required.', example='2026-08-01'),
  notes?: string(name='notes', description='The meeting notes content (optional). This participates in auxiliary analysis.', example='Focus on identifying customer demands and to-do items'),
  operatingObjectName?: string(name='operatingObjectName', description='The digital employee name (operating object name, optional).', example='my-agent'),
  sourceTags?: string(name='sourceTags', description='The resource tags (optional, a JSON string list such as ["tagA","tagB"]).', example='["Customer","GroupChat"]'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. The winnexo-cli passes this value explicitly by using --tenant-id.', example='10000'),
  updateFrequency?: {
    cron?: string(name='cron', description='The cron expression for timed scheduling.', example='0 2 * * *'),
    enabled?: boolean(name='enabled', description='Specifies whether to enable or disable the feature.', example='true'),
    preset?: string(name='preset', description='The preset mode (can be ignored).', example='hourly'),
  }(name='updateFrequency', description='The feature update frequency.'),
}

model CreatePersonalAliDingChatShrinkRequest {
  chatId?: string(name='chatId', description='The DingTalk group chat session ID.

This parameter is required.', example='cidxxxxxxxx'),
  chatName?: string(name='chatName', description='The group chat name.', example='CustomerProjectGroup'),
  description?: string(name='description', description='The pipeline description.', example='Customer group chat history'),
  directoryId?: string(name='directoryId', description='The folder ID.', example='exampleDirectoryId'),
  historyStartTime?: string(name='historyStartTime', description='The start time for collecting chat history.

This parameter is required.', example='2026-08-01'),
  notes?: string(name='notes', description='The meeting notes content (optional). This participates in auxiliary analysis.', example='Focus on identifying customer demands and to-do items'),
  operatingObjectName?: string(name='operatingObjectName', description='The digital employee name (operating object name, optional).', example='my-agent'),
  sourceTags?: string(name='sourceTags', description='The resource tags (optional, a JSON string list such as ["tagA","tagB"]).', example='["Customer","GroupChat"]'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. The winnexo-cli passes this value explicitly by using --tenant-id.', example='10000'),
  updateFrequencyShrink?: string(name='updateFrequency', description='The feature update frequency.'),
}

model CreatePersonalAliDingChatResponseBody = {
  chatId?: string(name='chatId', description='The DingTalk group chat session ID.', example='cidxxxxxxxx'),
  code?: string(name='code', description='The status code.', example='200'),
  directoryId?: string(name='directoryId', description='The folder ID.', example='exampleDirectoryId'),
  gmtCreate?: string(name='gmtCreate', description='The creation time.', example='2026-08-18T10:00:00Z'),
  message?: string(name='message', description='The response message.', example='ok'),
  name?: string(name='name', description='The skill name.', example='CustomerProjectGroup'),
  requestId?: string(name='requestId', description='The request trace ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  scope?: string(name='scope', description='The permission scope.', example='PERSONAL'),
  sourceId?: string(name='sourceId', description='The original project ID.', example='exampleSourceId'),
  status?: string(name='status', description='The call status. Valid values:
- **PENDING**: Waiting for acknowledgment.
- **SUCCESS**: Succeeded.
- **FAILED**: Failed.
- **TIMEOUT**: Timed out.', example='PENDING'),
}

model CreatePersonalAliDingChatResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreatePersonalAliDingChatResponseBody(name='body'),
}

/**
 * @summary Creates a personal AliDing group chat knowledge resource.
 *
 * @description ## Request description
 * - Connects the specified AliDing group chat to the current user\\"s personal knowledge base.
 * - The resource type is fixed to ALI_DING, the scope is fixed to PERSONAL, and the owning user is parsed from the gateway authentication identity.
 * - historyStartTime is required and supports YYYY-MM-DD or YYYY-MM-DD HH:MM:SS format.
 * - updateFrequency can be configured with a preset or a five-field cron expression for subsequent synchronization frequency.
 * - chatId must be unique within the target personal scope.
 *
 * @param tmpReq CreatePersonalAliDingChatRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreatePersonalAliDingChatResponse
 */
async function createPersonalAliDingChatWithOptions(tmpReq: CreatePersonalAliDingChatRequest, headers: map[string]string, runtime: Util.RuntimeOptions): CreatePersonalAliDingChatResponse {
  Util.validateModel(tmpReq);
  var request = new CreatePersonalAliDingChatShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!Util.isUnset(tmpReq.updateFrequency)) {
    request.updateFrequencyShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.updateFrequency, 'updateFrequency', 'json');
  }
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.chatId)) {
    body['chatId'] = request.chatId;
  }
  if (!Util.isUnset(request.chatName)) {
    body['chatName'] = request.chatName;
  }
  if (!Util.isUnset(request.description)) {
    body['description'] = request.description;
  }
  if (!Util.isUnset(request.directoryId)) {
    body['directoryId'] = request.directoryId;
  }
  if (!Util.isUnset(request.historyStartTime)) {
    body['historyStartTime'] = request.historyStartTime;
  }
  if (!Util.isUnset(request.notes)) {
    body['notes'] = request.notes;
  }
  if (!Util.isUnset(request.operatingObjectName)) {
    body['operatingObjectName'] = request.operatingObjectName;
  }
  if (!Util.isUnset(request.sourceTags)) {
    body['sourceTags'] = request.sourceTags;
  }
  if (!Util.isUnset(request.updateFrequencyShrink)) {
    body['updateFrequency'] = request.updateFrequencyShrink;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'CreatePersonalAliDingChat',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/createPersonalAliDingChat`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates a personal AliDing group chat knowledge resource.
 *
 * @description ## Request description
 * - Connects the specified AliDing group chat to the current user\\"s personal knowledge base.
 * - The resource type is fixed to ALI_DING, the scope is fixed to PERSONAL, and the owning user is parsed from the gateway authentication identity.
 * - historyStartTime is required and supports YYYY-MM-DD or YYYY-MM-DD HH:MM:SS format.
 * - updateFrequency can be configured with a preset or a five-field cron expression for subsequent synchronization frequency.
 * - chatId must be unique within the target personal scope.
 *
 * @param request CreatePersonalAliDingChatRequest
 * @return CreatePersonalAliDingChatResponse
 */
async function createPersonalAliDingChat(request: CreatePersonalAliDingChatRequest): CreatePersonalAliDingChatResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return createPersonalAliDingChatWithOptions(request, headers, runtime);
}

model CreatePersonalAliDingMeetingRequest {
  description?: string(name='description', description='The description of the AI assistant.', example='controll service user'),
  directoryId?: string(name='directoryId', description='The directory ID.', example='exampleDirectoryId'),
  name?: string(name='name', description='The name of the image-trained digital human.

This parameter is required.', example='p-toolset-3dcef7ca-31b9-4d1c-8692-1ef03099cad3'),
  notes?: string(name='notes', description='The meeting notes (optional). The notes are used for auxiliary analysis.', example='string_value'),
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital employee (operating object name, optional).', example='string_value'),
  shanjiUrl?: string(name='shanjiUrl', description='The original Shanji link (required).

This parameter is required.', example='https://example.com/winnexo/resource'),
  tenantId?: string(name='tenantId', description='The tenant ID.', example='549003315603714'),
}

model CreatePersonalAliDingMeetingResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  directoryId?: string(name='directoryId', description='The directory ID.', example='exampleDirectoryId'),
  gmtCreate?: string(name='gmtCreate', description='The creation time.', example='2025-11-12T03:08:56Z'),
  message?: string(name='message', description='The response message.', example='success'),
  name?: string(name='name', description='The name of the bot.', example='p-toolset-80a4520e-b35c-4e8b-acf7-3a01c7307522'),
  requestId?: string(name='requestId', description='The request ID.', example='247FBC97-433C-544A-BB29-98F572C06E9F'),
  scope?: string(name='scope', description='The permission scope.', example='user_info projects pull_requests hook gists emails'),
  sourceId?: string(name='sourceId', description='The unique identifier on the business system side, that is, the business ID.', example='2000358'),
  status?: string(name='status', description='The signing status. Valid values:

- CREATED: Created but not signed.
- SUCCESS: Signed.
- STOP: Terminated.', example='{\\\\"observedGeneration\\\\": 2, \\\\"servicesInstances\\\\": {}, \\\\"observedTime\\\\": \\\\"2026-03-05T16:00:09Z\\\\", \\\\"servicesWithPendingChanges\\\\": [], \\\\"latestEnvironmentDeploymentName\\\\": \\\\"manual-1772726409137-lmvsqr\\\\"}'),
}

model CreatePersonalAliDingMeetingResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreatePersonalAliDingMeetingResponseBody(name='body'),
}

/**
 * @summary Uploads an AliDing meeting file to the personal resource library of the current digital employee.
 *
 * @description ## Request description
 * - This API uploads AliDing meeting materials (such as audio/video files and Shanji links) to the "My Resources" section of a specified digital employee.
 * - The `source_type` is fixed to `ALI_DING_MEETING`, and the `scope` is fixed to `PERSONAL`.
 * - You must provide a public audio/video OSS URL (`ossUrl`) and the original Shanji link (`shanjiUrl`).
 * - Optionally, you can specify a target personal directory ID (`directoryId`). If not specified, the resource is automatically bound to the default root directory of the current digital employee.
 * - You can add a resource description (`description`) and meeting notes (`notes`). The meeting notes can be used for auxiliary analysis.
 * - This operation requires authentication. AK, BearerToken, and APP authentication methods are supported.
 *
 * @param request CreatePersonalAliDingMeetingRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreatePersonalAliDingMeetingResponse
 */
async function createPersonalAliDingMeetingWithOptions(request: CreatePersonalAliDingMeetingRequest, headers: map[string]string, runtime: Util.RuntimeOptions): CreatePersonalAliDingMeetingResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.description)) {
    body['description'] = request.description;
  }
  if (!Util.isUnset(request.directoryId)) {
    body['directoryId'] = request.directoryId;
  }
  if (!Util.isUnset(request.name)) {
    body['name'] = request.name;
  }
  if (!Util.isUnset(request.notes)) {
    body['notes'] = request.notes;
  }
  if (!Util.isUnset(request.operatingObjectName)) {
    body['operatingObjectName'] = request.operatingObjectName;
  }
  if (!Util.isUnset(request.shanjiUrl)) {
    body['shanjiUrl'] = request.shanjiUrl;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'CreatePersonalAliDingMeeting',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/createPersonalAliDingMeeting`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Uploads an AliDing meeting file to the personal resource library of the current digital employee.
 *
 * @description ## Request description
 * - This API uploads AliDing meeting materials (such as audio/video files and Shanji links) to the "My Resources" section of a specified digital employee.
 * - The `source_type` is fixed to `ALI_DING_MEETING`, and the `scope` is fixed to `PERSONAL`.
 * - You must provide a public audio/video OSS URL (`ossUrl`) and the original Shanji link (`shanjiUrl`).
 * - Optionally, you can specify a target personal directory ID (`directoryId`). If not specified, the resource is automatically bound to the default root directory of the current digital employee.
 * - You can add a resource description (`description`) and meeting notes (`notes`). The meeting notes can be used for auxiliary analysis.
 * - This operation requires authentication. AK, BearerToken, and APP authentication methods are supported.
 *
 * @param request CreatePersonalAliDingMeetingRequest
 * @return CreatePersonalAliDingMeetingResponse
 */
async function createPersonalAliDingMeeting(request: CreatePersonalAliDingMeetingRequest): CreatePersonalAliDingMeetingResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return createPersonalAliDingMeetingWithOptions(request, headers, runtime);
}

model CreatePersonalAlidingDocRequest {
  description?: string(name='description', description='The pipeline description.', example='controll service user'),
  directoryId?: string(name='directoryId', description='The folder ID.', example='exampleDirectoryId'),
  filePublicUrl?: string(name='filePublicUrl', description='The publicly accessible URL of the AliDing online document.

This parameter is required.', example='https://example.com/winnexo/resource'),
  name?: string(name='name', description='The customer group name.

This parameter is required.', example='issue_research'),
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital employee (operating object name, optional).', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID.', example='1729094555111072'),
}

model CreatePersonalAlidingDocResponseBody = {
  code?: string(name='code', description='The response code.', example='200'),
  directoryId?: string(name='directoryId', description='The folder ID.', example='exampleDirectoryId'),
  filePublicUrl?: string(name='filePublicUrl', description='The publicly accessible URL of the AliDing online document.', example='https://example.com/winnexo/resource'),
  gmtCreate?: string(name='gmtCreate', description='The timestamp when the customer group was created. Unit: milliseconds.', example='2026-04-22T08:15:28.000+00:00'),
  message?: string(name='message', description='The response message.', example='successful'),
  name?: string(name='name', description='The pipeline name.', example='user_paswd_104'),
  requestId?: string(name='requestId', description='The request ID.', example='E68654BD-F7BA-5837-8686-5645D739A47C'),
  scope?: string(name='scope', description='The permission scope.', example='user_info projects pull_requests hook gists emails'),
  sourceId?: string(name='sourceId', description='The unique identifier on the business system side, that is, the business ID.', example='2000413'),
  status?: string(name='status', description='The call status. Valid values:
- **PENDING**: Waiting for receipt.
- **SUCCESS**: Succeeded.
- **FAILED**: Failed.
- **TIMEOUT**: Timed out.', example='{\\\\"servicesInstances\\\\": {}, \\\\"servicesWithPendingChanges\\\\": []}'),
}

model CreatePersonalAlidingDocResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreatePersonalAlidingDocResponseBody(name='body'),
}

/**
 * @summary Uploads an AliDing online document to the personal resources of the current digital employee.
 *
 * @description ## Request description
 * - This API is used to add an AliDing online document to the "My Resources" section of a specified digital employee.
 * - Fixed parameters include `source_type=ONLINE_DOC`, `platform=ALI_DING`, and `scope=PERSONAL`.
 * - If `directoryId` is not provided, the document is attached to the root folder of the current digital employee by default. If provided, ensure that the folder belongs to the current user and exists under the current digital employee.
 * - During the invoke process, metering is started and related operation logs are recorded.
 * - For security purposes, `tenant_id` and `user_id` are obtained only from the authentication identity. Values provided by the caller for these fields are ignored.
 * - Any validation or execute failure is thrown as an exception by the service and transformed into a POP error code returned to the caller.
 *
 * @param request CreatePersonalAlidingDocRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreatePersonalAlidingDocResponse
 */
async function createPersonalAlidingDocWithOptions(request: CreatePersonalAlidingDocRequest, headers: map[string]string, runtime: Util.RuntimeOptions): CreatePersonalAlidingDocResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.description)) {
    body['description'] = request.description;
  }
  if (!Util.isUnset(request.directoryId)) {
    body['directoryId'] = request.directoryId;
  }
  if (!Util.isUnset(request.filePublicUrl)) {
    body['filePublicUrl'] = request.filePublicUrl;
  }
  if (!Util.isUnset(request.name)) {
    body['name'] = request.name;
  }
  if (!Util.isUnset(request.operatingObjectName)) {
    body['operatingObjectName'] = request.operatingObjectName;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'CreatePersonalAlidingDoc',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/createPersonalAliDingDoc`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Uploads an AliDing online document to the personal resources of the current digital employee.
 *
 * @description ## Request description
 * - This API is used to add an AliDing online document to the "My Resources" section of a specified digital employee.
 * - Fixed parameters include `source_type=ONLINE_DOC`, `platform=ALI_DING`, and `scope=PERSONAL`.
 * - If `directoryId` is not provided, the document is attached to the root folder of the current digital employee by default. If provided, ensure that the folder belongs to the current user and exists under the current digital employee.
 * - During the invoke process, metering is started and related operation logs are recorded.
 * - For security purposes, `tenant_id` and `user_id` are obtained only from the authentication identity. Values provided by the caller for these fields are ignored.
 * - Any validation or execute failure is thrown as an exception by the service and transformed into a POP error code returned to the caller.
 *
 * @param request CreatePersonalAlidingDocRequest
 * @return CreatePersonalAlidingDocResponse
 */
async function createPersonalAlidingDoc(request: CreatePersonalAlidingDocRequest): CreatePersonalAlidingDocResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return createPersonalAlidingDocWithOptions(request, headers, runtime);
}

model CreatePersonalAlidingKnowledgeBaseRequest {
  directoryId?: string(name='directoryId', description='The directory ID.', example='exampleDirectoryId'),
  kbName?: string(name='kbName', description='The display name of the knowledge base. If not provided, the name is populated from the root node name pulled from the remote source.', example='string_value'),
  kbUrl?: string(name='kbUrl', description='The publicly accessible URL of the AliDing knowledge base.

This parameter is required.', example='https://example.com/winnexo/resource'),
  objectBindings?: [ 
    {
      objectId?: string(name='objectId', description='The ID of the recommended item, which can be a **feedId** or a micro-application ID.', example='2676'),
      objectType?: string(name='objectType', description='The advanced field type.', example='table'),
    }
  ](name='objectBindings', description='The object bindings.'),
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital employee (operating object name, optional).', example='string_value'),
  syncConfig?: {
    cron?: string(name='cron', description='The cron expression for timed scheduling.', example='string_value'),
    enabled?: boolean(name='enabled', description='Specifies whether to enable synchronization.', example='False'),
  }(name='syncConfig', description='The synchronization settings.'),
  tenantId?: string(name='tenantId', description='The tenant ID.', example='PiPklI1iSRTm6VFFqlY9VzbgiEiE'),
}

model CreatePersonalAlidingKnowledgeBaseShrinkRequest {
  directoryId?: string(name='directoryId', description='The directory ID.', example='exampleDirectoryId'),
  kbName?: string(name='kbName', description='The display name of the knowledge base. If not provided, the name is populated from the root node name pulled from the remote source.', example='string_value'),
  kbUrl?: string(name='kbUrl', description='The publicly accessible URL of the AliDing knowledge base.

This parameter is required.', example='https://example.com/winnexo/resource'),
  objectBindingsShrink?: string(name='objectBindings', description='The object bindings.'),
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital employee (operating object name, optional).', example='string_value'),
  syncConfigShrink?: string(name='syncConfig', description='The synchronization settings.'),
  tenantId?: string(name='tenantId', description='The tenant ID.', example='PiPklI1iSRTm6VFFqlY9VzbgiEiE'),
}

model CreatePersonalAlidingKnowledgeBaseResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  directoryId?: string(name='directoryId', description='The directory ID.', example='exampleDirectoryId'),
  gmtCreate?: string(name='gmtCreate', description='The creation time in ISO 8601 format.', example='2025-11-14T02:18:27Z'),
  kbUrl?: string(name='kbUrl', description='The knowledge base URL (echoed from the request parameter for caller alignment).', example='https://example.com/winnexo/resource'),
  message?: string(name='message', description='The response message.', example='ok'),
  name?: string(name='name', description='The name of the AI assistant.', example='p-toolset-3dcef7ca-31b9-4d1c-8692-1ef03099cad3'),
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital employee (operating object name, optional).', example='string_value'),
  requestId?: string(name='requestId', description='The request ID.', example='E68654BD-F7BA-5837-8686-5645D739A47C'),
  status?: string(name='status', description='The status. Valid values:

- 200: Success.
- 500: Failure.', example='200'),
}

model CreatePersonalAlidingKnowledgeBaseResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreatePersonalAlidingKnowledgeBaseResponseBody(name='body'),
}

/**
 * @summary Adds an AliDing knowledge base to the personal resources of the current digital employee.
 *
 * @description ## Request description
 * - This API creates an AliDing knowledge base and mounts it to the personal resource directory of the specified digital employee.
 * - `platform` is fixed to `ALI_DING`, and `directory_type` is fixed to `PERSONAL`.
 * - If `directoryId` is provided, the system verifies that the directory exists and belongs to the current tenant and is of the personal type.
 * - During creation, the knowledge base root directory is initialized (with the status set to `RUNNING`), and background tasks are dispatched based on the provided synchronization configuration to pull the remote directory tree and create child nodes.
 * - For security purposes, `tenant_id` and `user_id` are obtained only from the authenticated identity. These fields in the request body are ignored.
 * - The synchronization configuration is optional. If enabled, a cron expression must be provided. If not provided or disabled, scheduled synchronization is not performed by default.
 * - The knowledge base name can be customized. If not provided, it is automatically populated after background synchronization.
 * - Multi-value object binding is supported. Related information is serialized and stored in the knowledge base metadata.
 *
 * @param tmpReq CreatePersonalAlidingKnowledgeBaseRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreatePersonalAlidingKnowledgeBaseResponse
 */
async function createPersonalAlidingKnowledgeBaseWithOptions(tmpReq: CreatePersonalAlidingKnowledgeBaseRequest, headers: map[string]string, runtime: Util.RuntimeOptions): CreatePersonalAlidingKnowledgeBaseResponse {
  Util.validateModel(tmpReq);
  var request = new CreatePersonalAlidingKnowledgeBaseShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!Util.isUnset(tmpReq.objectBindings)) {
    request.objectBindingsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.objectBindings, 'objectBindings', 'json');
  }
  if (!Util.isUnset(tmpReq.syncConfig)) {
    request.syncConfigShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.syncConfig, 'syncConfig', 'json');
  }
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.directoryId)) {
    body['directoryId'] = request.directoryId;
  }
  if (!Util.isUnset(request.kbName)) {
    body['kbName'] = request.kbName;
  }
  if (!Util.isUnset(request.kbUrl)) {
    body['kbUrl'] = request.kbUrl;
  }
  if (!Util.isUnset(request.objectBindingsShrink)) {
    body['objectBindings'] = request.objectBindingsShrink;
  }
  if (!Util.isUnset(request.operatingObjectName)) {
    body['operatingObjectName'] = request.operatingObjectName;
  }
  if (!Util.isUnset(request.syncConfigShrink)) {
    body['syncConfig'] = request.syncConfigShrink;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'CreatePersonalAlidingKnowledgeBase',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/createPersonalAliDingKnowledgeBase`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Adds an AliDing knowledge base to the personal resources of the current digital employee.
 *
 * @description ## Request description
 * - This API creates an AliDing knowledge base and mounts it to the personal resource directory of the specified digital employee.
 * - `platform` is fixed to `ALI_DING`, and `directory_type` is fixed to `PERSONAL`.
 * - If `directoryId` is provided, the system verifies that the directory exists and belongs to the current tenant and is of the personal type.
 * - During creation, the knowledge base root directory is initialized (with the status set to `RUNNING`), and background tasks are dispatched based on the provided synchronization configuration to pull the remote directory tree and create child nodes.
 * - For security purposes, `tenant_id` and `user_id` are obtained only from the authenticated identity. These fields in the request body are ignored.
 * - The synchronization configuration is optional. If enabled, a cron expression must be provided. If not provided or disabled, scheduled synchronization is not performed by default.
 * - The knowledge base name can be customized. If not provided, it is automatically populated after background synchronization.
 * - Multi-value object binding is supported. Related information is serialized and stored in the knowledge base metadata.
 *
 * @param request CreatePersonalAlidingKnowledgeBaseRequest
 * @return CreatePersonalAlidingKnowledgeBaseResponse
 */
async function createPersonalAlidingKnowledgeBase(request: CreatePersonalAlidingKnowledgeBaseRequest): CreatePersonalAlidingKnowledgeBaseResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return createPersonalAlidingKnowledgeBaseWithOptions(request, headers, runtime);
}

model CreatePersonalDingtalkMeetingRequest {
  credentialId?: string(name='credentialId', description='The credential ID.', example='exampleCredentialId'),
  description?: string(name='description', description='The pipeline description.', example='Watchlist Monitor Layer'),
  directoryId?: string(name='directoryId', description='The directory ID.', example='exampleDirectoryId'),
  name?: string(name='name', description='The image name.

This parameter is required.', example='p-default-007735a2-58f5-47a5-9e37-ea3fd64e0899'),
  notes?: string(name='notes', description='The meeting notes content (optional). The notes are used for auxiliary analysis.', example='string_value'),
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital employee (operating object name, optional).', example='string_value'),
  roomCode?: string(name='roomCode', description='The meeting code.

This parameter is required.', example='356 776 973'),
  tenantId?: string(name='tenantId', description='The tenant ID.', example='520539530998273'),
}

model CreatePersonalDingtalkMeetingResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  directoryId?: string(name='directoryId', description='The directory ID.', example='exampleDirectoryId'),
  gmtCreate?: string(name='gmtCreate', description='The creation time.', example='2025-11-14T02:18:27Z'),
  message?: string(name='message', description='The response message.', example='ok'),
  name?: string(name='name', description='The skill name.', example='oklabs_tongyici'),
  requestId?: string(name='requestId', description='Id of the request', example='B49109FE-5BB1-593C-915D-F5A99D9F5435'),
  scope?: string(name='scope', description='The permission scope.', example='user_info projects pull_requests hook gists emails'),
  sourceId?: string(name='sourceId', description='The unique identifier on the business system side, which is the business ID.', example='2000398'),
  status?: string(name='status', description='The task running status.', example='updated'),
}

model CreatePersonalDingtalkMeetingResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreatePersonalDingtalkMeetingResponseBody(name='body'),
}

/**
 * @deprecated OpenAPI CreatePersonalDingtalkMeeting is deprecated
 *
 * @summary Uploads a DingTalk meeting to the personal resource library of the current digital employee.
 *
 * @description ## Request description
 * - This operation uploads a DingTalk meeting as a resource to the "My Resources" section of a specified digital employee.
 * - `source_type` is fixed to `DINGTALK_MEETING`, and `scope` is fixed to `PERSONAL`.
 * - If `credentialId` is not provided, the system default configurations are used.
 * - If `directoryId` is not specified, the resource is automatically attached to the default root folder of the current digital employee. If specified, it must be an existing personal folder of the invoker under the digital employee.
 * - The optional parameters `description` and `notes` are used to describe the resource and record meeting notes, respectively. The `notes` value is used for auxiliary analysis.
 *
 * @param request CreatePersonalDingtalkMeetingRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreatePersonalDingtalkMeetingResponse
 */
// Deprecated
async function createPersonalDingtalkMeetingWithOptions(request: CreatePersonalDingtalkMeetingRequest, headers: map[string]string, runtime: Util.RuntimeOptions): CreatePersonalDingtalkMeetingResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.credentialId)) {
    body['credentialId'] = request.credentialId;
  }
  if (!Util.isUnset(request.description)) {
    body['description'] = request.description;
  }
  if (!Util.isUnset(request.directoryId)) {
    body['directoryId'] = request.directoryId;
  }
  if (!Util.isUnset(request.name)) {
    body['name'] = request.name;
  }
  if (!Util.isUnset(request.notes)) {
    body['notes'] = request.notes;
  }
  if (!Util.isUnset(request.operatingObjectName)) {
    body['operatingObjectName'] = request.operatingObjectName;
  }
  if (!Util.isUnset(request.roomCode)) {
    body['roomCode'] = request.roomCode;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'CreatePersonalDingtalkMeeting',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/createPersonalDingtalkMeeting`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @deprecated OpenAPI CreatePersonalDingtalkMeeting is deprecated
 *
 * @summary Uploads a DingTalk meeting to the personal resource library of the current digital employee.
 *
 * @description ## Request description
 * - This operation uploads a DingTalk meeting as a resource to the "My Resources" section of a specified digital employee.
 * - `source_type` is fixed to `DINGTALK_MEETING`, and `scope` is fixed to `PERSONAL`.
 * - If `credentialId` is not provided, the system default configurations are used.
 * - If `directoryId` is not specified, the resource is automatically attached to the default root folder of the current digital employee. If specified, it must be an existing personal folder of the invoker under the digital employee.
 * - The optional parameters `description` and `notes` are used to describe the resource and record meeting notes, respectively. The `notes` value is used for auxiliary analysis.
 *
 * @param request CreatePersonalDingtalkMeetingRequest
 * @return CreatePersonalDingtalkMeetingResponse
 */
// Deprecated
async function createPersonalDingtalkMeeting(request: CreatePersonalDingtalkMeetingRequest): CreatePersonalDingtalkMeetingResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return createPersonalDingtalkMeetingWithOptions(request, headers, runtime);
}

model CreatePersonalDingtalkMinutesRequest {
  description?: string(name='description', description='The description of the pipeline.', example='Sample description'),
  directoryId?: string(name='directoryId', description='The directory ID.', example='exampleDirectoryId'),
  name?: string(name='name', description='The name of the worksheet.

This parameter is required.', example='SampleName.pdf'),
  notes?: string(name='notes', description='The meeting notes content (optional). The notes are used for auxiliary analysis.', example='string_value'),
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital employee (operating object name, optional).', example='string_value'),
  shanjiUrl?: string(name='shanjiUrl', description='The original Shanji link (required).

This parameter is required.', example='https://example.com/winnexo/resource'),
  tenantId?: string(name='tenantId', description='The tenant ID.', example='692318833855074'),
}

model CreatePersonalDingtalkMinutesResponseBody = {
  code?: string(name='code', description='The error code.', example='200'),
  directoryId?: string(name='directoryId', description='The directory ID.', example='exampleDirectoryId'),
  gmtCreate?: string(name='gmtCreate', description='The creation time.', example='2025-11-12T03:08:56Z'),
  message?: string(name='message', description='The response message.', example='The current zone list is illegal.'),
  name?: string(name='name', description='The name of the AI assistant.', example='SampleName.pdf'),
  requestId?: string(name='requestId', description='The request ID.', example='72D1EC35-B174-5595-891F-2F0B3BFBE56F'),
  scope?: string(name='scope', description='The permission scope.', example='read:user,read:repo,write:repo,read:org,read:group'),
  sourceId?: string(name='sourceId', description='The unique identifier on the business system side, which is the business ID.', example='exampleSourceId'),
  status?: string(name='status', description='The status.', example='200'),
}

model CreatePersonalDingtalkMinutesResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreatePersonalDingtalkMinutesResponseBody(name='body'),
}

/**
 * @summary Uploads a meeting to the current user\\"s personal knowledge base by using a standard DingTalk Shanji URL.
 *
 * @description ## Request description
 * - This API creates a meeting resource by using a standard DingTalk Shanji link. The collection method is fixed to the DWS corresponding to personal OAuth.
 * - `source_type` is fixed to `DINGTALK_MEETING`, and `scope` is fixed to `PERSONAL`.
 * - You must provide a standard DingTalk Shanji link or taskUuid (`shanjiUrl`).
 * - Optionally specify a target personal directory ID (`directoryId`). If not specified, the default root directory of the current digital employee is used.
 * - You can add a resource description (`description`) and meeting notes (`notes`).
 * - This operation supports one of the following authentication methods: AK, BearerToken, or APP.
 *
 * @param request CreatePersonalDingtalkMinutesRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreatePersonalDingtalkMinutesResponse
 */
async function createPersonalDingtalkMinutesWithOptions(request: CreatePersonalDingtalkMinutesRequest, headers: map[string]string, runtime: Util.RuntimeOptions): CreatePersonalDingtalkMinutesResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.description)) {
    body['description'] = request.description;
  }
  if (!Util.isUnset(request.directoryId)) {
    body['directoryId'] = request.directoryId;
  }
  if (!Util.isUnset(request.name)) {
    body['name'] = request.name;
  }
  if (!Util.isUnset(request.notes)) {
    body['notes'] = request.notes;
  }
  if (!Util.isUnset(request.operatingObjectName)) {
    body['operatingObjectName'] = request.operatingObjectName;
  }
  if (!Util.isUnset(request.shanjiUrl)) {
    body['shanjiUrl'] = request.shanjiUrl;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'CreatePersonalDingtalkMinutes',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/createPersonalDingtalkMinutes`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Uploads a meeting to the current user\\"s personal knowledge base by using a standard DingTalk Shanji URL.
 *
 * @description ## Request description
 * - This API creates a meeting resource by using a standard DingTalk Shanji link. The collection method is fixed to the DWS corresponding to personal OAuth.
 * - `source_type` is fixed to `DINGTALK_MEETING`, and `scope` is fixed to `PERSONAL`.
 * - You must provide a standard DingTalk Shanji link or taskUuid (`shanjiUrl`).
 * - Optionally specify a target personal directory ID (`directoryId`). If not specified, the default root directory of the current digital employee is used.
 * - You can add a resource description (`description`) and meeting notes (`notes`).
 * - This operation supports one of the following authentication methods: AK, BearerToken, or APP.
 *
 * @param request CreatePersonalDingtalkMinutesRequest
 * @return CreatePersonalDingtalkMinutesResponse
 */
async function createPersonalDingtalkMinutes(request: CreatePersonalDingtalkMinutesRequest): CreatePersonalDingtalkMinutesResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return createPersonalDingtalkMinutesWithOptions(request, headers, runtime);
}

model CreatePersonalDirectoryRequest {
  description?: string(name='description', description='The workspace description.', example='hangzhou-release-version-3-eventbridge-numeric-queue-fix-20260529'),
  name?: string(name='name', description='The name of the digital human.

This parameter is required.', example='sandbox-conversation-webpage-github-default-p32JG2'),
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital human (operating object name, optional).', example='string_value'),
  parentDirectoryId?: string(name='parentDirectoryId', description='The folder ID.', example='wd-lxykjnnw4lyl9eq'),
  tenantId?: string(name='tenantId', description='The tenant ID.', example='235454102432001'),
}

model CreatePersonalDirectoryResponseBody = {
  code?: string(name='code', description='SUCCESS indicates success. In case of failure, the corresponding error type is returned, such as ERR_BAD_REQUEST, ERR_VALIDATION_FAILED, or ERR_INTERNAL_SERVER_ERROR.', example='200'),
  description?: string(name='description', description='The description.', example='PublicApplication'),
  directoryId?: string(name='directoryId', description='The folder ID.', example='exampleDirectoryId'),
  directoryKind?: string(name='directoryKind', description='The folder type.', example='string_value'),
  gmtCreate?: long(name='gmtCreate', description='The creation time.', example='2026-03-04 13:54:52'),
  gmtModified?: long(name='gmtModified', description='The last modification time.', example='2025-11-14T02:18:27Z'),
  message?: string(name='message', description='The response message.', example='ok'),
  name?: string(name='name', description='The name of the worksheet.', example='conn_ip_101'),
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital human (operating object name, optional).', example='string_value'),
  parentDirectoryId?: string(name='parentDirectoryId', description='The folder ID.', example='wd-lxykjnnw4lyl9eq'),
  path?: string(name='path', description='The path.', example='oss://clg-paimon-4a00f1ac43464714b86fb54ca41a84c9/db-abc73646-6a08-4b96-820f-3d1d547a1e3b.db/tbl-c8a33522-5398-4f8e-9a2a-fba1efad94d1'),
  requestId?: string(name='requestId', description='Id of the request', example='375701FC-2FB9-5782-BE8F-A3F5E2F2158C'),
}

model CreatePersonalDirectoryResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreatePersonalDirectoryResponseBody(name='body'),
}

/**
 * @summary Creates a personal folder (category) under My Resources.
 *
 * @description ## Request description
 * - This API is used to create a personal folder (category) under "My Resources".
 * - If `parentDirectoryId` is not specified, the system automatically uses or creates the default root folder of the current digital human as the parent folder.
 * - If `parentDirectoryId` is specified, it must be an existing personal folder of the current user under the current digital human.
 * - `tenant_id` and `user_id` are derived from the authentication identity only. These fields are ignored if included in the request body.
 *
 * @param request CreatePersonalDirectoryRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreatePersonalDirectoryResponse
 */
async function createPersonalDirectoryWithOptions(request: CreatePersonalDirectoryRequest, headers: map[string]string, runtime: Util.RuntimeOptions): CreatePersonalDirectoryResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.description)) {
    body['description'] = request.description;
  }
  if (!Util.isUnset(request.name)) {
    body['name'] = request.name;
  }
  if (!Util.isUnset(request.operatingObjectName)) {
    body['operatingObjectName'] = request.operatingObjectName;
  }
  if (!Util.isUnset(request.parentDirectoryId)) {
    body['parentDirectoryId'] = request.parentDirectoryId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'CreatePersonalDirectory',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/createPersonalDirectory`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates a personal folder (category) under My Resources.
 *
 * @description ## Request description
 * - This API is used to create a personal folder (category) under "My Resources".
 * - If `parentDirectoryId` is not specified, the system automatically uses or creates the default root folder of the current digital human as the parent folder.
 * - If `parentDirectoryId` is specified, it must be an existing personal folder of the current user under the current digital human.
 * - `tenant_id` and `user_id` are derived from the authentication identity only. These fields are ignored if included in the request body.
 *
 * @param request CreatePersonalDirectoryRequest
 * @return CreatePersonalDirectoryResponse
 */
async function createPersonalDirectory(request: CreatePersonalDirectoryRequest): CreatePersonalDirectoryResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return createPersonalDirectoryWithOptions(request, headers, runtime);
}

model CreatePersonalFeishuChatRequest {
  chatId?: string(name='chatId', description='The group chat session ID.

This parameter is required.', example='oc_abc123'),
  description?: string(name='description', description='The description of the source.', example='Product R&D group chat records'),
  directoryId?: string(name='directoryId', description='The directory ID.', example='dir_personal_1'),
  historyStartTime?: string(name='historyStartTime', description='The start time for historical messages. Supports YYYY-MM-DD or YYYY-MM-DD HH:MM:SS. If not specified, all visible history is pulled.', example='2026-08-01 00:00:00'),
  notes?: string(name='notes', description='The meeting notes content (optional). Used for auxiliary analysis.', example='Focus on extracting decisions and action items'),
  operatingObjectName?: string(name='operatingObjectName', description='The digital employee name (operating object name, optional).', example='R&D Assistant'),
  sourceTags?: string(name='sourceTags', description='The source tags.', example='["R&D"]'),
  tenantId?: string(name='tenantId', description='The tenant ID to take effect.', example='10000'),
  updateFrequency?: {
    cron?: string(name='cron', description='The cron expression for the timed scheduling node.', example='0 * * * *'),
    enabled?: boolean(name='enabled', description='Specifies whether to enable the scheduled synchronization.', example='true'),
    preset?: string(name='preset', description='The synchronization preset: hourly or daily_2am.', example='hourly'),
  }(name='updateFrequency', description='The update frequency.'),
}

model CreatePersonalFeishuChatShrinkRequest {
  chatId?: string(name='chatId', description='The group chat session ID.

This parameter is required.', example='oc_abc123'),
  description?: string(name='description', description='The description of the source.', example='Product R&D group chat records'),
  directoryId?: string(name='directoryId', description='The directory ID.', example='dir_personal_1'),
  historyStartTime?: string(name='historyStartTime', description='The start time for historical messages. Supports YYYY-MM-DD or YYYY-MM-DD HH:MM:SS. If not specified, all visible history is pulled.', example='2026-08-01 00:00:00'),
  notes?: string(name='notes', description='The meeting notes content (optional). Used for auxiliary analysis.', example='Focus on extracting decisions and action items'),
  operatingObjectName?: string(name='operatingObjectName', description='The digital employee name (operating object name, optional).', example='R&D Assistant'),
  sourceTags?: string(name='sourceTags', description='The source tags.', example='["R&D"]'),
  tenantId?: string(name='tenantId', description='The tenant ID to take effect.', example='10000'),
  updateFrequencyShrink?: string(name='updateFrequency', description='The update frequency.'),
}

model CreatePersonalFeishuChatResponseBody = {
  chatId?: string(name='chatId', description='The group chat session ID.', example='oc_abc123'),
  code?: string(name='code', description='The status code.', example='200'),
  directoryId?: string(name='directoryId', description='The directory ID.', example='dir_personal_1'),
  gmtCreate?: string(name='gmtCreate', description='The creation time.', example='2026-08-18T10:30:00+08:00'),
  message?: string(name='message', description='The status code description.', example='successful'),
  name?: string(name='name', description='The skill name.', example='Product R&D Group'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  scope?: string(name='scope', description='The permission scope.', example='PERSONAL'),
  sourceId?: string(name='sourceId', description='The source ID.', example='src_feishu_1'),
  status?: string(name='status', description='The signing status. Valid values:
- CREATED: Created but not signed.
- SUCCESS: Signed successfully.
- STOP: Terminated.', example='RUNNING'),
}

model CreatePersonalFeishuChatResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreatePersonalFeishuChatResponseBody(name='body'),
}

/**
 * @summary Creates a personal knowledge resource from a single Lark group chat using the current user\\"s Lark authorization.
 *
 * @description ## Request description
 * This API uses the Lark application connection managed by the user corresponding to the current OpenAPI identity. It pulls the name and historical messages of the specified group chat through the built-in CLI of the project and creates a knowledge resource in the user\\"s personal knowledge base.
 * - `chatId`: The Lark group chat ID. Must start with `oc_`.
 * - `directoryId` (optional): The target personal directory ID. If omitted, the current user\\"s default personal root directory is used.
 * - `historyStartTime` (optional): The start time for historical messages. Supports `YYYY-MM-DD` or `YYYY-MM-DD HH:MM:SS`.
 * - `updateFrequency` (optional): The Source-level scheduled synchronization configuration. Supports preset frequencies or five-field cron expressions.
 * - `description`, `operatingObjectName`, `notes`, `sourceTags`: Optional Source metadata.
 * Security constraints: The Source Type is fixed to FEISHU, and the knowledge scope is fixed to PERSONAL. The Lark connector user is determined by the POP trusted identity. Credentials or user IDs passed by the caller are not accepted.
 *
 * @param tmpReq CreatePersonalFeishuChatRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreatePersonalFeishuChatResponse
 */
async function createPersonalFeishuChatWithOptions(tmpReq: CreatePersonalFeishuChatRequest, headers: map[string]string, runtime: Util.RuntimeOptions): CreatePersonalFeishuChatResponse {
  Util.validateModel(tmpReq);
  var request = new CreatePersonalFeishuChatShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!Util.isUnset(tmpReq.updateFrequency)) {
    request.updateFrequencyShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.updateFrequency, 'updateFrequency', 'json');
  }
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.chatId)) {
    body['chatId'] = request.chatId;
  }
  if (!Util.isUnset(request.description)) {
    body['description'] = request.description;
  }
  if (!Util.isUnset(request.directoryId)) {
    body['directoryId'] = request.directoryId;
  }
  if (!Util.isUnset(request.historyStartTime)) {
    body['historyStartTime'] = request.historyStartTime;
  }
  if (!Util.isUnset(request.notes)) {
    body['notes'] = request.notes;
  }
  if (!Util.isUnset(request.operatingObjectName)) {
    body['operatingObjectName'] = request.operatingObjectName;
  }
  if (!Util.isUnset(request.sourceTags)) {
    body['sourceTags'] = request.sourceTags;
  }
  if (!Util.isUnset(request.updateFrequencyShrink)) {
    body['updateFrequency'] = request.updateFrequencyShrink;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'CreatePersonalFeishuChat',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/createPersonalFeishuChat`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates a personal knowledge resource from a single Lark group chat using the current user\\"s Lark authorization.
 *
 * @description ## Request description
 * This API uses the Lark application connection managed by the user corresponding to the current OpenAPI identity. It pulls the name and historical messages of the specified group chat through the built-in CLI of the project and creates a knowledge resource in the user\\"s personal knowledge base.
 * - `chatId`: The Lark group chat ID. Must start with `oc_`.
 * - `directoryId` (optional): The target personal directory ID. If omitted, the current user\\"s default personal root directory is used.
 * - `historyStartTime` (optional): The start time for historical messages. Supports `YYYY-MM-DD` or `YYYY-MM-DD HH:MM:SS`.
 * - `updateFrequency` (optional): The Source-level scheduled synchronization configuration. Supports preset frequencies or five-field cron expressions.
 * - `description`, `operatingObjectName`, `notes`, `sourceTags`: Optional Source metadata.
 * Security constraints: The Source Type is fixed to FEISHU, and the knowledge scope is fixed to PERSONAL. The Lark connector user is determined by the POP trusted identity. Credentials or user IDs passed by the caller are not accepted.
 *
 * @param request CreatePersonalFeishuChatRequest
 * @return CreatePersonalFeishuChatResponse
 */
async function createPersonalFeishuChat(request: CreatePersonalFeishuChatRequest): CreatePersonalFeishuChatResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return createPersonalFeishuChatWithOptions(request, headers, runtime);
}

model CreatePersonalFeishuMinuteRequest {
  credentialId?: string(name='credentialId', description='The credential ID.

This parameter is required.', example='exampleCredentialId'),
  description?: string(name='description', description='The resource description.', example='created by eventbridge'),
  directoryId?: string(name='directoryId', description='The directory ID.', example='exampleDirectoryId'),
  minuteToken?: string(name='minuteToken', description='The Lark Minutes token (unique identifier of the minutes record, required).

This parameter is required.', example='string_value'),
  name?: string(name='name', description='The resource name.

This parameter is required.', example='_DevsAF_19df1a74-a740-449a-bd7a-9acb39e00f25'),
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital employee (operating object name, optional).', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID.', example='1iSgnW4pARwoPUd5D5nuCNwiEiE'),
}

model CreatePersonalFeishuMinuteResponseBody = {
  code?: string(name='code', description='SUCCESS indicates success. In case of failure, the corresponding error type is returned, such as ERR_BAD_REQUEST, ERR_VALIDATION_FAILED, or ERR_INTERNAL_SERVER_ERROR.', example='200'),
  directoryId?: string(name='directoryId', description='The directory ID.', example='exampleDirectoryId'),
  gmtCreate?: string(name='gmtCreate', description='The creation time.', example='2026-03-04 11:12:03'),
  message?: string(name='message', description='The operation message.', example='success'),
  name?: string(name='name', description='The resource name.', example='issue_research'),
  requestId?: string(name='requestId', description='The request ID.', example='019FEE93-17FB-5369-BB65-1188C3A14B0A'),
  scope?: string(name='scope', description='The permission scope.', example='read:user,read:repo,write:repo,read:org,read:group'),
  sourceId?: string(name='sourceId', description='The original project ID.', example='2001086'),
  status?: string(name='status', description='The status.', example='200'),
}

model CreatePersonalFeishuMinuteResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreatePersonalFeishuMinuteResponseBody(name='body'),
}

/**
 * @summary Uploads a Lark Minutes meeting file to the personal resource library of the current digital employee.
 *
 * @description ## Request description
 * This API uploads a meeting record from Lark Minutes to the "My Resources" section of a specified digital employee. By providing the required parameters, such as the unique identifier of the Lark Minutes record (`minuteToken`) and the credential ID (`credentialId`), you can migrate and save meeting content. If no target directory is specified, the resource is bound to the root directory of the current digital employee by default.
 * - `operatingObjectName`: The name of the digital employee that performs the operation.
 * - `name`: The display name of the uploaded resource in the system.
 * - `minuteToken`: The unique identifier of the meeting from the Lark Minutes platform.
 * - `credentialId`: The ID associated with specific authentication information, used to verify the validity of the request.
 * - `directoryId` (optional): The ID of the target personal directory where the resource is stored. If this field is omitted, the resource is automatically placed in the default location.
 * - `description` (optional): A brief description or note about the uploaded resource.
 * Precautions:
 * - Ensure that the provided `minuteToken` and `credentialId` are valid.
 * - If `directoryId` is specified, confirm that it belongs to one of the available personal directories of the caller in the current digital employee environment.
 *
 * @param request CreatePersonalFeishuMinuteRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreatePersonalFeishuMinuteResponse
 */
async function createPersonalFeishuMinuteWithOptions(request: CreatePersonalFeishuMinuteRequest, headers: map[string]string, runtime: Util.RuntimeOptions): CreatePersonalFeishuMinuteResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.credentialId)) {
    body['credentialId'] = request.credentialId;
  }
  if (!Util.isUnset(request.description)) {
    body['description'] = request.description;
  }
  if (!Util.isUnset(request.directoryId)) {
    body['directoryId'] = request.directoryId;
  }
  if (!Util.isUnset(request.minuteToken)) {
    body['minuteToken'] = request.minuteToken;
  }
  if (!Util.isUnset(request.name)) {
    body['name'] = request.name;
  }
  if (!Util.isUnset(request.operatingObjectName)) {
    body['operatingObjectName'] = request.operatingObjectName;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'CreatePersonalFeishuMinute',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/createPersonalFeishuMinute`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Uploads a Lark Minutes meeting file to the personal resource library of the current digital employee.
 *
 * @description ## Request description
 * This API uploads a meeting record from Lark Minutes to the "My Resources" section of a specified digital employee. By providing the required parameters, such as the unique identifier of the Lark Minutes record (`minuteToken`) and the credential ID (`credentialId`), you can migrate and save meeting content. If no target directory is specified, the resource is bound to the root directory of the current digital employee by default.
 * - `operatingObjectName`: The name of the digital employee that performs the operation.
 * - `name`: The display name of the uploaded resource in the system.
 * - `minuteToken`: The unique identifier of the meeting from the Lark Minutes platform.
 * - `credentialId`: The ID associated with specific authentication information, used to verify the validity of the request.
 * - `directoryId` (optional): The ID of the target personal directory where the resource is stored. If this field is omitted, the resource is automatically placed in the default location.
 * - `description` (optional): A brief description or note about the uploaded resource.
 * Precautions:
 * - Ensure that the provided `minuteToken` and `credentialId` are valid.
 * - If `directoryId` is specified, confirm that it belongs to one of the available personal directories of the caller in the current digital employee environment.
 *
 * @param request CreatePersonalFeishuMinuteRequest
 * @return CreatePersonalFeishuMinuteResponse
 */
async function createPersonalFeishuMinute(request: CreatePersonalFeishuMinuteRequest): CreatePersonalFeishuMinuteResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return createPersonalFeishuMinuteWithOptions(request, headers, runtime);
}

model CreatePersonalFileRequest {
  description?: string(name='description', description='The pipeline description.', example='created by eventbridge'),
  directoryId?: string(name='directoryId', description='The directory ID.', example='exampleDirectoryId'),
  fileExt?: string(name='fileExt', description='The file extension (optional, such as pdf or docx).', example='string_value'),
  fileName?: string(name='fileName', description='The file name.', example='0250705120003-2026-04-28-19-22-20.wav'),
  filePath?: string(name='filePath', description='The file path.

This parameter is required.', example='bi/batch-query-service.app.yaml'),
  filePublicUrl?: string(name='filePublicUrl', description='The publicly accessible URL of the Alibaba DingTalk online document.', example='https://example.com/winnexo/resource'),
  fileRecordId?: string(name='fileRecordId', description='The file record ID (optional, corresponding to settings.file_record_id).', example='exampleFileRecordId'),
  name?: string(name='name', description='The pipeline name.

This parameter is required.', example='sys_first_new_v3_b'),
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital employee (operating object name, optional).', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID.', example='1798284341201499'),
}

model CreatePersonalFileResponseBody = {
  code?: string(name='code', description='The response status code.', example='200'),
  directoryId?: string(name='directoryId', description='The directory ID.', example='exampleDirectoryId'),
  gmtCreate?: string(name='gmtCreate', description='The creation time.', example='2025-11-14T02:18:27Z'),
  message?: string(name='message', description='The error details.', example='success'),
  name?: string(name='name', description='The updated filter view name.', example='ha-cn-36z45q4xg06_qrs'),
  requestId?: string(name='requestId', description='The request ID.', example='3DAC4165-2401-543B-B5E7-A86AA151E517'),
  scope?: string(name='scope', description='The export scope. Valid values:
- ALL: all.
- SELECT: selected rows.', example='read:user,read:repo,write:repo,read:org,read:group'),
  sourceId?: string(name='sourceId', description='The source ID.', example='2001086'),
  status?: string(name='status', description='The status.', example='200'),
}

model CreatePersonalFileResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreatePersonalFileResponseBody(name='body'),
}

/**
 * @summary Uploads a file to the personal resource library of the current digital employee.
 *
 * @description ## Request description
 * - This API is used to upload a file to the "My Resources" section of a specified digital employee.
 * - `source_type` is fixed to `FILE`, `scope` is fixed to `PERSONAL`, and `platform` is fixed to `LOCAL`.
 * - The file must include an OSS persistent address (`filePath`). Other information such as the public access URL and original file name is optional.
 * - If the target folder ID (`directoryId`) is not specified, the file is automatically attached to the default root folder of the current digital employee. If specified, ensure that the folder belongs to the personal folder of the caller.
 * - Security authentication is supported through multiple authentication methods (AK, BearerToken, and APP) to authenticate requests.
 * - The operation type is write (`write`), and operation logs are recorded for subsequent auditing.
 * To invoke this operation, you can use AK, BearerToken, or APP authentication.
 *
 * @param request CreatePersonalFileRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreatePersonalFileResponse
 */
async function createPersonalFileWithOptions(request: CreatePersonalFileRequest, headers: map[string]string, runtime: Util.RuntimeOptions): CreatePersonalFileResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.description)) {
    body['description'] = request.description;
  }
  if (!Util.isUnset(request.directoryId)) {
    body['directoryId'] = request.directoryId;
  }
  if (!Util.isUnset(request.fileExt)) {
    body['fileExt'] = request.fileExt;
  }
  if (!Util.isUnset(request.fileName)) {
    body['fileName'] = request.fileName;
  }
  if (!Util.isUnset(request.filePath)) {
    body['filePath'] = request.filePath;
  }
  if (!Util.isUnset(request.filePublicUrl)) {
    body['filePublicUrl'] = request.filePublicUrl;
  }
  if (!Util.isUnset(request.fileRecordId)) {
    body['fileRecordId'] = request.fileRecordId;
  }
  if (!Util.isUnset(request.name)) {
    body['name'] = request.name;
  }
  if (!Util.isUnset(request.operatingObjectName)) {
    body['operatingObjectName'] = request.operatingObjectName;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'CreatePersonalFile',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/createPersonalFile`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Uploads a file to the personal resource library of the current digital employee.
 *
 * @description ## Request description
 * - This API is used to upload a file to the "My Resources" section of a specified digital employee.
 * - `source_type` is fixed to `FILE`, `scope` is fixed to `PERSONAL`, and `platform` is fixed to `LOCAL`.
 * - The file must include an OSS persistent address (`filePath`). Other information such as the public access URL and original file name is optional.
 * - If the target folder ID (`directoryId`) is not specified, the file is automatically attached to the default root folder of the current digital employee. If specified, ensure that the folder belongs to the personal folder of the caller.
 * - Security authentication is supported through multiple authentication methods (AK, BearerToken, and APP) to authenticate requests.
 * - The operation type is write (`write`), and operation logs are recorded for subsequent auditing.
 * To invoke this operation, you can use AK, BearerToken, or APP authentication.
 *
 * @param request CreatePersonalFileRequest
 * @return CreatePersonalFileResponse
 */
async function createPersonalFile(request: CreatePersonalFileRequest): CreatePersonalFileResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return createPersonalFileWithOptions(request, headers, runtime);
}

model CreatePersonalTextRequest {
  description?: string(name='description', description='The pipeline description.', example='PublicApplication'),
  directoryId?: string(name='directoryId', description='The folder ID.', example='exampleDirectoryId'),
  name?: string(name='name', description='The image name.

This parameter is required.', example='rds_mysql_10_34_4_255_6306_password'),
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital employee (operating object name, optional).', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID.', example='3668'),
  textContent?: string(name='textContent', description='The message content for text messages.

This parameter is required.', example='string_value'),
}

model CreatePersonalTextResponseBody = {
  code?: string(name='code', description='SUCCESS indicates success. In case of failure, the corresponding error type is returned, such as ERR_BAD_REQUEST, ERR_VALIDATION_FAILED, or ERR_INTERNAL_SERVER_ERROR.', example='200'),
  directoryId?: string(name='directoryId', description='The folder ID.', example='exampleDirectoryId'),
  gmtCreate?: string(name='gmtCreate', description='The creation time in ISO 8601 format.', example='2026-05-22 18:18:56'),
  message?: string(name='message', description='The response message.', example='Instance i-0jl6hlcbtuo4eqg7puni not found'),
  name?: string(name='name', description='The updated filter view name.', example='p-toolset-3dcef7ca-31b9-4d1c-8692-1ef03099cad3'),
  requestId?: string(name='requestId', description='The request ID.', example='04EE99E6-A0D9-5B04-81D1-7BEC0CB0AFDF'),
  scope?: string(name='scope', description='The travel scale of the integration partner.', example='read:user,read:repo,write:repo,read:org,read:group'),
  sourceId?: string(name='sourceId', description='The source ID.', example='2000398'),
  status?: string(name='status', description='The filter status.

- 0: All

- 1: Unconfirmed

- 3: Ignored

- 4: Rejected', example='{\\\\"observedGeneration\\\\": 4, \\\\"servicesInstances\\\\": {}, \\\\"observedTime\\\\": \\\\"2025-10-31T03:48:27Z\\\\", \\\\"servicesWithPendingChanges\\\\": [], \\\\"latestEnvironmentDeploymentName\\\\": \\\\"manual-1761882507097-Eu1vIP\\\\"}'),
}

model CreatePersonalTextResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreatePersonalTextResponseBody(name='body'),
}

/**
 * @summary Uploads plain text content to the personal resource library of the current digital employee.
 *
 * @description ## Operation description
 * - This API is used to add plain text content to the personal resources of a specified digital employee.
 * - `source_type` is fixed to `TEXT`, and `scope` is fixed to `PERSONAL`.
 * - If `directoryId` is not provided, the content is bound to the root directory of the current digital employee by default. If provided, it must be an existing personal directory of the caller under the digital employee.
 * - `tenant_id` and `user_id` can only be obtained from the authentication identity information. These parameters are ignored if passed in the request body.
 * - The call initiates metering and generates a corresponding `billing_id`.
 * - The text content is written to `unstructured_docs`, and an initial resource record is generated.
 * - Any validation or execution failure throws a `RobjectException`, which is converted to a POP error code by the global middleware and returned to the caller.
 *
 * @param request CreatePersonalTextRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreatePersonalTextResponse
 */
async function createPersonalTextWithOptions(request: CreatePersonalTextRequest, headers: map[string]string, runtime: Util.RuntimeOptions): CreatePersonalTextResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.description)) {
    body['description'] = request.description;
  }
  if (!Util.isUnset(request.directoryId)) {
    body['directoryId'] = request.directoryId;
  }
  if (!Util.isUnset(request.name)) {
    body['name'] = request.name;
  }
  if (!Util.isUnset(request.operatingObjectName)) {
    body['operatingObjectName'] = request.operatingObjectName;
  }
  if (!Util.isUnset(request.textContent)) {
    body['textContent'] = request.textContent;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'CreatePersonalText',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/createPersonalText`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Uploads plain text content to the personal resource library of the current digital employee.
 *
 * @description ## Operation description
 * - This API is used to add plain text content to the personal resources of a specified digital employee.
 * - `source_type` is fixed to `TEXT`, and `scope` is fixed to `PERSONAL`.
 * - If `directoryId` is not provided, the content is bound to the root directory of the current digital employee by default. If provided, it must be an existing personal directory of the caller under the digital employee.
 * - `tenant_id` and `user_id` can only be obtained from the authentication identity information. These parameters are ignored if passed in the request body.
 * - The call initiates metering and generates a corresponding `billing_id`.
 * - The text content is written to `unstructured_docs`, and an initial resource record is generated.
 * - Any validation or execution failure throws a `RobjectException`, which is converted to a POP error code by the global middleware and returned to the caller.
 *
 * @param request CreatePersonalTextRequest
 * @return CreatePersonalTextResponse
 */
async function createPersonalText(request: CreatePersonalTextRequest): CreatePersonalTextResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return createPersonalTextWithOptions(request, headers, runtime);
}

model CreatePersonalVoiceMeetingHeaders {
  commonHeaders?: map[string]string,
  requestId?: string(name='requestId', description='Id of the request

This parameter is required.', example='0B6F0F99-EB17-51D5-AAC8-AD78A26E18DD'),
}

model CreatePersonalVoiceMeetingRequest {
  description?: string(name='description', description='The pipeline description.', example='update-JNQ9csEc6ArPPSXANH7O'),
  directoryId?: string(name='directoryId', description='The directory ID.', example='exampleDirectoryId'),
  fileUrl?: string(name='fileUrl', description='The Yida attachment URL.

This parameter is required.', example='https://emas-devops-cdn.aliyuncs.com/job/PJ859733644657824768/apk/release/com.czmp.vitanexusoff_release_v331.178.14-signed.apk'),
  name?: string(name='name', description='The image name.

This parameter is required.', example='p-toolset-2a1461ff-59c1-4baa-9e19-966ec7c00004'),
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital employee (operating object name, optional).', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID.', example='474379246158592'),
}

model CreatePersonalVoiceMeetingResponseBody = {
  code?: string(name='code', description='The response code.', example='200'),
  directoryId?: string(name='directoryId', description='The bound directory ID. This value echoes the directoryId provided in the request body. If no directoryId is specified, the value is null because the default root directory is used.', example='exampleDirectoryId'),
  gmtCreate?: string(name='gmtCreate', description='The creation time in ISO 8601 format.', example='2026-04-22T08:15:28.000+00:00'),
  message?: string(name='message', description='The description of the status code.', example='successful'),
  name?: string(name='name', description='The knowledge base name.', example='p-toolset-e95d1287-3d40-487a-bcce-6e6252c7a793'),
  requestId?: string(name='requestId', description='The request ID.', example='A5241B90-8FF4-565C-977A-0CE1842AED72'),
  scope?: string(name='scope', description='The resource scope. The value is fixed to PERSONAL.', example='user_info projects pull_requests hook gists emails'),
  sourceId?: string(name='sourceId', description='The unique identifier on the business system side, that is, the business ID.', example='8'),
  status?: string(name='status', description='The resource status. The initial status during the creation process is typically PENDING. If the on_create process fails, the status is FAILED.', example='{\\\\"observedGeneration\\\\": 4, \\\\"servicesInstances\\\\": {}, \\\\"observedTime\\\\": \\\\"2025-10-31T03:48:27Z\\\\", \\\\"servicesWithPendingChanges\\\\": [], \\\\"latestEnvironmentDeploymentName\\\\": \\\\"manual-1761882507097-Eu1vIP\\\\"}'),
}

model CreatePersonalVoiceMeetingResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreatePersonalVoiceMeetingResponseBody(name='body'),
}

/**
 * @summary Uploads an offline meeting audio file to the personal resources of the current digital employee.
 *
 * @description ## Operation description
 * - This API operation uploads an offline meeting audio file to the "My Resources" section of a specified digital employee.
 * - `source_type` is fixed to `VOICE_MEETING`, `scope` is fixed to `PERSONAL`, and `voice_meeting_type` is fixed to `OFFLINE`.
 * - If `directoryId` is not provided in the request body, the resource is automatically bound to the default root directory. If `directoryId` is provided, it must be an existing personal directory of the current user under the current digital employee.
 * - Calling this operation starts a background process to transcribe the audio file and returns information about the newly created resource.
 * - For security purposes, `tenant_id` and `user_id` are obtained only from the authenticated identity. These fields are ignored even if they are included in the request body.
 * - Any validation or execution failure throws a `RobjectException`, which is converted to a POP error code through the global middleware.
 *
 * @param request CreatePersonalVoiceMeetingRequest
 * @param headers CreatePersonalVoiceMeetingHeaders
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreatePersonalVoiceMeetingResponse
 */
async function createPersonalVoiceMeetingWithOptions(request: CreatePersonalVoiceMeetingRequest, headers: CreatePersonalVoiceMeetingHeaders, runtime: Util.RuntimeOptions): CreatePersonalVoiceMeetingResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.description)) {
    body['description'] = request.description;
  }
  if (!Util.isUnset(request.directoryId)) {
    body['directoryId'] = request.directoryId;
  }
  if (!Util.isUnset(request.fileUrl)) {
    body['fileUrl'] = request.fileUrl;
  }
  if (!Util.isUnset(request.name)) {
    body['name'] = request.name;
  }
  if (!Util.isUnset(request.operatingObjectName)) {
    body['operatingObjectName'] = request.operatingObjectName;
  }

  var realHeaders : map[string]string = {};
  if (!Util.isUnset(headers.commonHeaders)) {
    realHeaders = headers.commonHeaders;
  }
  if (!Util.isUnset(headers.requestId)) {
    realHeaders['requestId'] = Util.toJSONString(headers.requestId);
  }
  var req = new OpenApi.OpenApiRequest{ 
    headers = realHeaders,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'CreatePersonalVoiceMeeting',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/createPersonalVoiceMeeting`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Uploads an offline meeting audio file to the personal resources of the current digital employee.
 *
 * @description ## Operation description
 * - This API operation uploads an offline meeting audio file to the "My Resources" section of a specified digital employee.
 * - `source_type` is fixed to `VOICE_MEETING`, `scope` is fixed to `PERSONAL`, and `voice_meeting_type` is fixed to `OFFLINE`.
 * - If `directoryId` is not provided in the request body, the resource is automatically bound to the default root directory. If `directoryId` is provided, it must be an existing personal directory of the current user under the current digital employee.
 * - Calling this operation starts a background process to transcribe the audio file and returns information about the newly created resource.
 * - For security purposes, `tenant_id` and `user_id` are obtained only from the authenticated identity. These fields are ignored even if they are included in the request body.
 * - Any validation or execution failure throws a `RobjectException`, which is converted to a POP error code through the global middleware.
 *
 * @param request CreatePersonalVoiceMeetingRequest
 * @return CreatePersonalVoiceMeetingResponse
 */
async function createPersonalVoiceMeeting(request: CreatePersonalVoiceMeetingRequest): CreatePersonalVoiceMeetingResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers = new CreatePersonalVoiceMeetingHeaders{};
  return createPersonalVoiceMeetingWithOptions(request, headers, runtime);
}

model CreateScheduledTaskRequest {
  collaborationGroupId?: string(name='collaborationGroupId', description='The ID of the collaboration group (such as cg_101). If specified, a group space task is created (the caller must be a valid group member). If empty, a personal task is created.', example='exampleCollaborationGroupId'),
  description?: [ 
    {
      content?: string(name='content', description='The streaming output message.', example='Sample content'),
      enabled?: boolean(name='enabled', description='Specifies whether the throttling rule is enabled. A value of true indicates enabled, and a value of false indicates disabled.', example='true'),
      name?: string(name='name', description='The name.', example='SampleName.pdf'),
      objectId?: string(name='objectId', description='The object ID. Pass the project task ID.

- For internal enterprise applications, use the taskId obtained by calling the [Create a project task](https://open.dingtalk.com/document/orgapp-server/create-a-project-task) operation.

- For third-party enterprise applications, use the taskId obtained by calling the [Create a project task](https://open.dingtalk.com/document/isvapp-server/create-a-project-task) operation.', example='exampleObjectId'),
      objectType?: string(name='objectType', description='The object type. Fixed value: task, indicating a project task.', example='string_value'),
      skillCode?: string(name='skillCode', description='The skill code. This parameter has a value when type is set to skill.', example='string_value'),
      type?: string(name='type', description='The HTTP API type. Valid values: Http (standard HTTP API), Rest (RESTful API), WebSocket (WebSocket API), HttpIngress (HTTP API accessed through Ingress), LLM (large language model API), and Agent (Agent proxy API).', example='text'),
    }
  ](name='description', description='The description of the to-do card type.'),
  digitalEmployeeName?: [ string ](name='digitalEmployeeName', description='The name of the current effective digital employee. This parameter is empty if not configured.', example='string_value'),
  isOpen?: boolean(name='isOpen', description='Specifies whether public access is enabled.', example='true'),
  model?: string(name='model', description='The large model used by the assistant. An empty value indicates that DingTalk automatically selects the model.', example='quick'),
  name?: string(name='name', description='The name.

This parameter is required.', example='SampleName.pdf'),
  segments?: [ 
    {
      content?: string(name='content', description='The card callback content.', example='Sample content'),
      enabled?: boolean(name='enabled', description='Specifies whether to enable this feature.', example='true'),
      name?: string(name='name', description='The name.', example='SampleName.pdf'),
      objectId?: string(name='objectId', description='The ID of the recommended item, which can be a **feedId** or a micro-application ID.', example='exampleObjectId'),
      objectType?: string(name='objectType', description='The customer type to save.', example='string_value'),
      skillCode?: string(name='skillCode', description='The skill code. This parameter has a value when type is set to skill.', example='string_value'),
      type?: string(name='type', description='The billing type. Only fixed is supported.', example='text'),
    }
  ](name='segments', description='The site ID.'),
  taskDetail?: {
    relatedObjects?: [ 
      {
        mentionType?: string(name='mentionType', description='The mention type, such as objects.', example='string_value'),
        name?: string(name='name', description='The name.', example='SampleName.pdf'),
        objectId?: string(name='objectId', description='The object ID. Pass the project task ID.

- For internal enterprise applications, use the taskId obtained by calling the [Create a project task](https://open.dingtalk.com/document/orgapp-server/create-a-project-task) operation.

- For third-party enterprise applications, use the taskId obtained by calling the [Create a project task](https://open.dingtalk.com/document/isvapp-server/create-a-project-task) operation.', example='exampleObjectId'),
        objectType?: string(name='objectType', description='The relationship type. Valid values:
- crm_customer: enterprise customer.
- crm_customer_personal: individual customer.', example='string_value'),
      }
    ](name='relatedObjects', description='The related objects.'),
    relatedSemantics?: [ 
      {
        attributes?: string(name='attributes', description='The file extension information.', example='{"level": "VIP"}'),
        entity?: string(name='entity', description='The semantic entity name, such as customer or opportunity.', example='customer'),
      }
    ](name='relatedSemantics', description='The related semantics.'),
    relatedSkills?: [ 
      {
        displayName?: string(name='displayName', description='The display name.', example='string_value'),
        name?: string(name='name', description='The name.', example='SampleName.pdf'),
        skillCode?: string(name='skillCode', description='The skill code.', example='string_value'),
        sourceIds?: [ string ](name='sourceIds', description='sourceIds', example='string_value'),
      }
    ](name='relatedSkills', description='The related skills.'),
    taskUnderstand?: string(name='taskUnderstand', description='The task understanding description polished by the LLM.', example='string_value'),
  }(name='taskDetail', description='The task details.'),
  tenantId?: string(name='tenantId', description='The ID of the effective tenant.', example='10000'),
  triggerConfig?: {
    cron?: string(name='cron', description='The periodic training information in cron syntax (Minutes Hours DayofMonth Month DayofWeek). An empty value indicates that periodic training is not performed (default). In DayofWeek, 0 indicates Sunday.', example='string_value'),
    language?: string(name='language', description='The language. Valid values:

- zh_CN: Chinese (default)
- en_US: English', example='zh-CN'),
    pushConfig?: [ 
      {
        channelType?: string(name='channelType', description='The notification method. Valid values:

- **hdm_alarm_sms**: SMS.
- **dingtalk**: DingTalk chatbot.
- **hdm_alarm_sms_and_email**: SMS and email.
- **hdm_alarm_sms,dingtalk**: SMS and DingTalk chatbot.', example='DINGTALK'),
        contentScope?: string(name='contentScope', description='The push content scope. Default value: all_replies.', example='all_replies'),
        deliveryMethod?: string(name='deliveryMethod', description='The push method. Default value: channel_bot.', example='channel_bot'),
        enabled?: boolean(name='enabled', description='Specifies whether the credential is enabled. Valid values:

- true: Enabled.
- false: Disabled.', example='true'),
        fileFormat?: string(name='fileFormat', description='The file format. Valid values: Excel and CSV.', example='file'),
        operatingObjectName?: string(name='operatingObjectName', description='The digital employee name (operating object name, optional).', example='string_value'),
        receiverType?: string(name='receiverType', description='The file receiver type. Valid values:

- 0: One-on-one chat.

- 1: Group chat.

- 2: DingTalk Drive.

- 3: Document.', example='string_value'),
      }
    ](name='pushConfig', description='The list of task push channels. No push is performed if the list is empty or no channel is enabled.'),
    timezone?: string(name='timezone', description='The time zone.', example='Asia/Shanghai'),
    triggerMode?: string(name='triggerMode', description='The trigger mode.
 
  1: Manual trigger
   
  2: Scheduled trigger 

  3: Code commit trigger
 
  5: Pipeline trigger

  6: WEBHOOK trigger', example='manual'),
  }(name='triggerConfig', description='The trigger configuration. The configuration varies depending on the trigger type. For the specific format, refer to the following data structures:

  - OSS trigger: See [OSSTriggerConfig](https://help.aliyun.com/document_detail/415697.html).
  - Simple Log Service trigger: See [LogTriggerConfig](https://help.aliyun.com/document_detail/415694.html).
  - Time trigger: See [TimeTriggerConfig](https://help.aliyun.com/document_detail/415712.html).
  - HTTP trigger: See [HTTPTriggerConfig](https://help.aliyun.com/document_detail/415685.html).
  - Tablestore trigger: You only need to specify the complete **SourceArn** parameter. No additional configuration is required. Set the value to an empty object {}.
  - CDN event trigger: See [CDNEventsTriggerConfig](https://help.aliyun.com/document_detail/415674.html).
  - MNS topic trigger: See [MnsTopicTriggerConfig](https://help.aliyun.com/document_detail/415695.html).
  - EventBridge trigger: See [EventBridgeTriggerConfig](https://help.aliyun.com/document_detail/2508622.html).'),
  visibility?: string(name='visibility', description='The visibility scope of the group task. Valid values: PRIVATE (visible only to the creator and group owner), COLLABORATIVE (visible to specified collaborators), and PUBLIC (visible to all group members). Default value for group tasks: PRIVATE. This parameter is ignored for personal tasks.', example='PRIVATE'),
  visibleMemberUserIds?: [ string ](name='visibleMemberUserIds', description='The list of collaborator user IDs. This parameter takes effect only when visibility is set to COLLABORATIVE. It is ignored for other visibility levels. A maximum of 1000 IDs are supported. The task creator and group creator do not need to be included (covered by the authentication layer). This parameter is ignored for personal tasks.', example='string_value'),
}

model CreateScheduledTaskShrinkRequest {
  collaborationGroupId?: string(name='collaborationGroupId', description='The ID of the collaboration group (such as cg_101). If specified, a group space task is created (the caller must be a valid group member). If empty, a personal task is created.', example='exampleCollaborationGroupId'),
  descriptionShrink?: string(name='description', description='The description of the to-do card type.'),
  digitalEmployeeNameShrink?: string(name='digitalEmployeeName', description='The name of the current effective digital employee. This parameter is empty if not configured.', example='string_value'),
  isOpen?: boolean(name='isOpen', description='Specifies whether public access is enabled.', example='true'),
  model?: string(name='model', description='The large model used by the assistant. An empty value indicates that DingTalk automatically selects the model.', example='quick'),
  name?: string(name='name', description='The name.

This parameter is required.', example='SampleName.pdf'),
  segmentsShrink?: string(name='segments', description='The site ID.'),
  taskDetailShrink?: string(name='taskDetail', description='The task details.'),
  tenantId?: string(name='tenantId', description='The ID of the effective tenant.', example='10000'),
  triggerConfigShrink?: string(name='triggerConfig', description='The trigger configuration. The configuration varies depending on the trigger type. For the specific format, refer to the following data structures:

  - OSS trigger: See [OSSTriggerConfig](https://help.aliyun.com/document_detail/415697.html).
  - Simple Log Service trigger: See [LogTriggerConfig](https://help.aliyun.com/document_detail/415694.html).
  - Time trigger: See [TimeTriggerConfig](https://help.aliyun.com/document_detail/415712.html).
  - HTTP trigger: See [HTTPTriggerConfig](https://help.aliyun.com/document_detail/415685.html).
  - Tablestore trigger: You only need to specify the complete **SourceArn** parameter. No additional configuration is required. Set the value to an empty object {}.
  - CDN event trigger: See [CDNEventsTriggerConfig](https://help.aliyun.com/document_detail/415674.html).
  - MNS topic trigger: See [MnsTopicTriggerConfig](https://help.aliyun.com/document_detail/415695.html).
  - EventBridge trigger: See [EventBridgeTriggerConfig](https://help.aliyun.com/document_detail/2508622.html).'),
  visibility?: string(name='visibility', description='The visibility scope of the group task. Valid values: PRIVATE (visible only to the creator and group owner), COLLABORATIVE (visible to specified collaborators), and PUBLIC (visible to all group members). Default value for group tasks: PRIVATE. This parameter is ignored for personal tasks.', example='PRIVATE'),
  visibleMemberUserIdsShrink?: string(name='visibleMemberUserIds', description='The list of collaborator user IDs. This parameter takes effect only when visibility is set to COLLABORATIVE. It is ignored for other visibility levels. A maximum of 1000 IDs are supported. The task creator and group creator do not need to be included (covered by the authentication layer). This parameter is ignored for personal tasks.', example='string_value'),
}

model CreateScheduledTaskResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  message?: string(name='message', description='The description of the status code.', example='successful'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  taskId?: string(name='taskId', description='The task ID.', example='exampleTaskId'),
}

model CreateScheduledTaskResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateScheduledTaskResponseBody(name='body'),
}

/**
 * @summary Creates a scheduled task.
 *
 * @description ## Operation description
 * - This operation is used to upload files to an enterprise knowledge base.
 * - You must have the `DEVELOPMENT_KB_MANAGE` permission to call this API operation.
 * - You must provide the OSS persistent address (`filePath`) of the file when uploading.
 * - Optional parameters include the public access URL of the file and the original file name to enhance the completeness of file information.
 * - If `directoryId` is specified, the file is placed in the corresponding enterprise knowledge base directory. Otherwise, the file is bound to the default root directory of the current digital employee by default.
 * - You can add tags to resources by using `sourceTags` for subsequent management and retrieval.
 * - This operation initiates a billing item (UNSTRUCTURED_PARSE). Make sure that your account balance is sufficient.
 *
 * @param tmpReq CreateScheduledTaskRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateScheduledTaskResponse
 */
async function createScheduledTaskWithOptions(tmpReq: CreateScheduledTaskRequest, headers: map[string]string, runtime: Util.RuntimeOptions): CreateScheduledTaskResponse {
  Util.validateModel(tmpReq);
  var request = new CreateScheduledTaskShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!Util.isUnset(tmpReq.description)) {
    request.descriptionShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.description, 'description', 'json');
  }
  if (!Util.isUnset(tmpReq.digitalEmployeeName)) {
    request.digitalEmployeeNameShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.digitalEmployeeName, 'digitalEmployeeName', 'json');
  }
  if (!Util.isUnset(tmpReq.segments)) {
    request.segmentsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.segments, 'segments', 'json');
  }
  if (!Util.isUnset(tmpReq.taskDetail)) {
    request.taskDetailShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.taskDetail, 'taskDetail', 'json');
  }
  if (!Util.isUnset(tmpReq.triggerConfig)) {
    request.triggerConfigShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.triggerConfig, 'triggerConfig', 'json');
  }
  if (!Util.isUnset(tmpReq.visibleMemberUserIds)) {
    request.visibleMemberUserIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.visibleMemberUserIds, 'visibleMemberUserIds', 'json');
  }
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.collaborationGroupId)) {
    body['collaborationGroupId'] = request.collaborationGroupId;
  }
  if (!Util.isUnset(request.descriptionShrink)) {
    body['description'] = request.descriptionShrink;
  }
  if (!Util.isUnset(request.digitalEmployeeNameShrink)) {
    body['digitalEmployeeName'] = request.digitalEmployeeNameShrink;
  }
  if (!Util.isUnset(request.isOpen)) {
    body['isOpen'] = request.isOpen;
  }
  if (!Util.isUnset(request.model)) {
    body['model'] = request.model;
  }
  if (!Util.isUnset(request.name)) {
    body['name'] = request.name;
  }
  if (!Util.isUnset(request.segmentsShrink)) {
    body['segments'] = request.segmentsShrink;
  }
  if (!Util.isUnset(request.taskDetailShrink)) {
    body['taskDetail'] = request.taskDetailShrink;
  }
  if (!Util.isUnset(request.triggerConfigShrink)) {
    body['triggerConfig'] = request.triggerConfigShrink;
  }
  if (!Util.isUnset(request.visibility)) {
    body['visibility'] = request.visibility;
  }
  if (!Util.isUnset(request.visibleMemberUserIdsShrink)) {
    body['visibleMemberUserIds'] = request.visibleMemberUserIdsShrink;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'CreateScheduledTask',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/createScheduledTask`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates a scheduled task.
 *
 * @description ## Operation description
 * - This operation is used to upload files to an enterprise knowledge base.
 * - You must have the `DEVELOPMENT_KB_MANAGE` permission to call this API operation.
 * - You must provide the OSS persistent address (`filePath`) of the file when uploading.
 * - Optional parameters include the public access URL of the file and the original file name to enhance the completeness of file information.
 * - If `directoryId` is specified, the file is placed in the corresponding enterprise knowledge base directory. Otherwise, the file is bound to the default root directory of the current digital employee by default.
 * - You can add tags to resources by using `sourceTags` for subsequent management and retrieval.
 * - This operation initiates a billing item (UNSTRUCTURED_PARSE). Make sure that your account balance is sufficient.
 *
 * @param request CreateScheduledTaskRequest
 * @return CreateScheduledTaskResponse
 */
async function createScheduledTask(request: CreateScheduledTaskRequest): CreateScheduledTaskResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return createScheduledTaskWithOptions(request, headers, runtime);
}

model CreateTenantDirectoryRequest {
  description?: string(name='description', description='The description of the to-do card type.', example='Sample description'),
  name?: string(name='name', description='The name.

This parameter is required.', example='SampleName.pdf'),
  parentId?: long(name='parentId', description='The ID of the parent node.', example='1'),
  path?: string(name='path', description='The path of the node.', example='https://example.com/oss/file.pdf'),
  tenantId?: string(name='tenantId', description='The ID of the effective tenant.', example='10000'),
}

model CreateTenantDirectoryResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  description?: string(name='description', description='The description of the to-do card type.', example='Sample description'),
  directoryId?: string(name='directoryId', description='The directory ID.

This parameter is required.', example='exampleDirectoryId'),
  gmtCreate?: long(name='gmtCreate', description='The creation time.', example='1'),
  gmtModified?: long(name='gmtModified', description='The last modification time.', example='1'),
  id?: long(name='id', description='Id of the request

This parameter is required.', example='1'),
  message?: string(name='message', description='The description of the status code.', example='successful'),
  name?: string(name='name', description='The name.

This parameter is required.', example='SampleName.pdf'),
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital employee (the operating object name, optional).', example='string_value'),
  parentId?: long(name='parentId', description='The ID of the parent node.', example='1'),
  path?: string(name='path', description='The path of the node.

This parameter is required.', example='https://example.com/oss/file.pdf'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  tenantId?: long(name='tenantId', description='The ID of the effective tenant.

This parameter is required.', example='10000'),
  userId?: long(name='userId', description='The user ID of the creator.

This parameter is required.', example='1'),
}

model CreateTenantDirectoryResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateTenantDirectoryResponseBody(name='body'),
}

/**
 * @summary Adds a knowledge base and knowledge base categories.
 *
 * @description ## Request description
 * - This API is used to create a new enterprise knowledge base directory under a specified tenant.
 * - You can specify the parent directory of the new directory by setting the `parentId` parameter. If this parameter is not specified, the directory is created as a root directory by default.
 * - The `path` parameter is optional. If this parameter is not specified, the system automatically calculates the path based on the parent directory.
 * - Calling this operation requires the corresponding permissions. Multiple authentication methods are supported, including AK, BearerToken, and APP authentication.
 * - After the directory is created, the related information of the new directory is returned, such as the directory ID and name.
 *
 * @param request CreateTenantDirectoryRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateTenantDirectoryResponse
 */
async function createTenantDirectoryWithOptions(request: CreateTenantDirectoryRequest, headers: map[string]string, runtime: Util.RuntimeOptions): CreateTenantDirectoryResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.description)) {
    body['description'] = request.description;
  }
  if (!Util.isUnset(request.name)) {
    body['name'] = request.name;
  }
  if (!Util.isUnset(request.parentId)) {
    body['parentId'] = request.parentId;
  }
  if (!Util.isUnset(request.path)) {
    body['path'] = request.path;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'CreateTenantDirectory',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/createTenantDirectory`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Adds a knowledge base and knowledge base categories.
 *
 * @description ## Request description
 * - This API is used to create a new enterprise knowledge base directory under a specified tenant.
 * - You can specify the parent directory of the new directory by setting the `parentId` parameter. If this parameter is not specified, the directory is created as a root directory by default.
 * - The `path` parameter is optional. If this parameter is not specified, the system automatically calculates the path based on the parent directory.
 * - Calling this operation requires the corresponding permissions. Multiple authentication methods are supported, including AK, BearerToken, and APP authentication.
 * - After the directory is created, the related information of the new directory is returned, such as the directory ID and name.
 *
 * @param request CreateTenantDirectoryRequest
 * @return CreateTenantDirectoryResponse
 */
async function createTenantDirectory(request: CreateTenantDirectoryRequest): CreateTenantDirectoryResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return createTenantDirectoryWithOptions(request, headers, runtime);
}

model CreateUserRequest {
  displayName?: string(name='displayName', description='The cluster name.

This parameter is required.', example='string_value'),
  passwordEncrypted?: string(name='passwordEncrypted', description='The base64-encoded password ciphertext encrypted by RSA-OAEP-SHA256 (required).

This parameter is required.', example='string_value'),
  roleCodes?: [ string ](name='roleCodes', description='The list of new system role codes (full replacement, must contain at least one role). Valid values: SUPER_ADMIN, SYSTEM_ADMIN, SEMANTIC_ADMIN, SKILL_ADMIN, KB_ADMIN, AGENT_ADMIN, and APPLICATION_USER.', example='string_value'),
  tenantId?: string(name='tenantId', description='The ID of the tenant in which the operation takes effect.', example='10000'),
  wnAccountId?: string(name='wnAccountId', description='The WINNEXO logon account (unique identifier, required).

This parameter is required.', example='exampleAccountId'),
}

model CreateUserShrinkRequest {
  displayName?: string(name='displayName', description='The cluster name.

This parameter is required.', example='string_value'),
  passwordEncrypted?: string(name='passwordEncrypted', description='The base64-encoded password ciphertext encrypted by RSA-OAEP-SHA256 (required).

This parameter is required.', example='string_value'),
  roleCodesShrink?: string(name='roleCodes', description='The list of new system role codes (full replacement, must contain at least one role). Valid values: SUPER_ADMIN, SYSTEM_ADMIN, SEMANTIC_ADMIN, SKILL_ADMIN, KB_ADMIN, AGENT_ADMIN, and APPLICATION_USER.', example='string_value'),
  tenantId?: string(name='tenantId', description='The ID of the tenant in which the operation takes effect.', example='10000'),
  wnAccountId?: string(name='wnAccountId', description='The WINNEXO logon account (unique identifier, required).

This parameter is required.', example='exampleAccountId'),
}

model CreateUserResponseBody = {
  accountId?: string(name='accountId', description='The ID of your Alibaba Cloud account.', example='exampleAccountId'),
  code?: string(name='code', description='The error code.', example='200'),
  displayName?: string(name='displayName', description='The cluster name.', example='string_value'),
  isNewUser?: boolean(name='isNewUser', description='Indicates whether the user is newly created. A value of false indicates that an existing user is added to the tenant.', example='true'),
  message?: string(name='message', description='The status code description.', example='successful'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  wnUserId?: string(name='wnUserId', description='The WINNEXO platform user ID.', example='1'),
}

model CreateUserResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateUserResponseBody(name='body'),
}

/**
 * @summary Creates a user and adds the user to a tenant.
 *
 * @description Creates a user by using OpenAPI.
 *     Business orchestration:
 *     1. Parses roleCodes → role_ids (validates against system role enumerations).
 *     2. Checks whether the user already exists (used to return the isNewUser flag).
 *     3. Calls UserManagementService.add_tenant_member to create or add the user (the password must be passed in as an RSA ciphertext by the caller).
 *     4. Returns the creation result (including the isNewUser flag).
 *     Error codes:
 *     - ERR.User.DeactivatedInTenant: The user is deactivated in the tenant. Use updateUser to resume the user.
 *     - ERR.User.AlreadyInTenant: The user is already an active member of the tenant.
 *     - ERR.User.DisplayNameDuplicateInTenant: The display name is duplicate within the tenant.
 *
 * @param tmpReq CreateUserRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateUserResponse
 */
async function createUserWithOptions(tmpReq: CreateUserRequest, headers: map[string]string, runtime: Util.RuntimeOptions): CreateUserResponse {
  Util.validateModel(tmpReq);
  var request = new CreateUserShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!Util.isUnset(tmpReq.roleCodes)) {
    request.roleCodesShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.roleCodes, 'roleCodes', 'json');
  }
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.displayName)) {
    body['displayName'] = request.displayName;
  }
  if (!Util.isUnset(request.passwordEncrypted)) {
    body['passwordEncrypted'] = request.passwordEncrypted;
  }
  if (!Util.isUnset(request.roleCodesShrink)) {
    body['roleCodes'] = request.roleCodesShrink;
  }
  if (!Util.isUnset(request.wnAccountId)) {
    body['wnAccountId'] = request.wnAccountId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'CreateUser',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/createUser`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates a user and adds the user to a tenant.
 *
 * @description Creates a user by using OpenAPI.
 *     Business orchestration:
 *     1. Parses roleCodes → role_ids (validates against system role enumerations).
 *     2. Checks whether the user already exists (used to return the isNewUser flag).
 *     3. Calls UserManagementService.add_tenant_member to create or add the user (the password must be passed in as an RSA ciphertext by the caller).
 *     4. Returns the creation result (including the isNewUser flag).
 *     Error codes:
 *     - ERR.User.DeactivatedInTenant: The user is deactivated in the tenant. Use updateUser to resume the user.
 *     - ERR.User.AlreadyInTenant: The user is already an active member of the tenant.
 *     - ERR.User.DisplayNameDuplicateInTenant: The display name is duplicate within the tenant.
 *
 * @param request CreateUserRequest
 * @return CreateUserResponse
 */
async function createUser(request: CreateUserRequest): CreateUserResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return createUserWithOptions(request, headers, runtime);
}

model DeleteChatSessionRequest {
  sessionId?: string(name='sessionId', description='The ID of the session to delete.

This parameter is required.', example='exampleSessionId'),
  tenantId?: string(name='tenantId', description='The ID of the effective tenant.', example='10000'),
}

model DeleteChatSessionResponseBody = {
  code?: string(name='code', description='The error code.', example='200'),
  deleted?: boolean(name='deleted', description='Indicates whether the session is deleted.', example='true'),
  hardDelete?: boolean(name='hardDelete', description='Indicates whether the session is hard-deleted.', example='true'),
  message?: string(name='message', description='The status code description.', example='ok'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  sessionId?: string(name='sessionId', description='The unique identifier of the function session.', example='exampleSessionId'),
}

model DeleteChatSessionResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DeleteChatSessionResponseBody(name='body'),
}

/**
 * @summary Deletes a session.
 *
 * @description ## Request description
 * - This API is used to upload a file to the "My Resources" section of a specified digital employee.
 * - `source_type` is fixed to `FILE`, `scope` is fixed to `PERSONAL`, and `platform` is fixed to `LOCAL`.
 * - The file must include an OSS persistent address (`filePath`). Other information such as the public access URL and original file name is optional.
 * - If the target directory ID (`directoryId`) is not specified, the file is automatically attached to the default root directory of the current digital employee. If specified, ensure that the directory belongs to the personal directory of the caller.
 * - Security authentication is supported through multiple methods (AK, BearerToken, APP).
 * - The operation type is write (`write`), and operation logs are recorded for subsequent auditing.
 *
 * @param request DeleteChatSessionRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteChatSessionResponse
 */
async function deleteChatSessionWithOptions(request: DeleteChatSessionRequest, headers: map[string]string, runtime: Util.RuntimeOptions): DeleteChatSessionResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.sessionId)) {
    query['sessionId'] = request.sessionId;
  }
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'DeleteChatSession',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/deleteChatSession`,
    method = 'DELETE',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Deletes a session.
 *
 * @description ## Request description
 * - This API is used to upload a file to the "My Resources" section of a specified digital employee.
 * - `source_type` is fixed to `FILE`, `scope` is fixed to `PERSONAL`, and `platform` is fixed to `LOCAL`.
 * - The file must include an OSS persistent address (`filePath`). Other information such as the public access URL and original file name is optional.
 * - If the target directory ID (`directoryId`) is not specified, the file is automatically attached to the default root directory of the current digital employee. If specified, ensure that the directory belongs to the personal directory of the caller.
 * - Security authentication is supported through multiple methods (AK, BearerToken, APP).
 * - The operation type is write (`write`), and operation logs are recorded for subsequent auditing.
 *
 * @param request DeleteChatSessionRequest
 * @return DeleteChatSessionResponse
 */
async function deleteChatSession(request: DeleteChatSessionRequest): DeleteChatSessionResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return deleteChatSessionWithOptions(request, headers, runtime);
}

model DeleteSourceRequest {
  sourceId?: string(name='sourceId', description='The unique identifier on the business system side, that is, the business ID.

This parameter is required.', example='781'),
  tenantId?: string(name='tenantId', description='The tenant ID.', example='21577'),
}

model DeleteSourceResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  deleted?: boolean(name='deleted', description='Indicates whether the deletion is successful.', example='true'),
  message?: string(name='message', description='The description of the status code.', example='successful'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  sourceId?: string(name='sourceId', description='The source ID.', example='exampleSourceId'),
}

model DeleteSourceResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DeleteSourceResponseBody(name='body'),
}

/**
 * @summary Deletes a specified source.
 *
 * @description ## Operation description
 * - `tenantId` is derived from the authenticated identity only. Any value passed by the caller is ignored.
 * - `sourceId` is passed through the request body. The registration path is the flat URI `/openapi/deleteSource` and does not contain a `{sourceId}` path template. Do not append the resource ID as a path segment. The gateway performs exact routing based on the flat URI and returns `InvalidAction.NotFound` if the path does not match.
 * - Deletion is irreversible. The parsing results and bindings associated with the resource are invalidated.
 *
 * @param request DeleteSourceRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteSourceResponse
 */
async function deleteSourceWithOptions(request: DeleteSourceRequest, headers: map[string]string, runtime: Util.RuntimeOptions): DeleteSourceResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.sourceId)) {
    body['sourceId'] = request.sourceId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'DeleteSource',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/deleteSource`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Deletes a specified source.
 *
 * @description ## Operation description
 * - `tenantId` is derived from the authenticated identity only. Any value passed by the caller is ignored.
 * - `sourceId` is passed through the request body. The registration path is the flat URI `/openapi/deleteSource` and does not contain a `{sourceId}` path template. Do not append the resource ID as a path segment. The gateway performs exact routing based on the flat URI and returns `InvalidAction.NotFound` if the path does not match.
 * - Deletion is irreversible. The parsing results and bindings associated with the resource are invalidated.
 *
 * @param request DeleteSourceRequest
 * @return DeleteSourceResponse
 */
async function deleteSource(request: DeleteSourceRequest): DeleteSourceResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return deleteSourceWithOptions(request, headers, runtime);
}

model DeleteTenantDirectoryRequest {
  deleteMode?: string(name='deleteMode', description='The deletion mode: reject / recursive / move_to_root.', example='reject'),
  directoryId?: string(name='directoryId', description='The directory ID.', example='exampleDirectoryId'),
  tenantId?: string(name='tenantId', description='The tenant ID that takes effect.', example='10000'),
}

model DeleteTenantDirectoryResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  deleteMode?: string(name='deleteMode', description='The deletion mode that is actually used.', example='reject'),
  directoryId?: string(name='directoryId', description='The directory ID.', example='exampleDirectoryId'),
  message?: string(name='message', description='The description of the status code.', example='ok'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
}

model DeleteTenantDirectoryResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DeleteTenantDirectoryResponseBody(name='body'),
}

/**
 * @summary Deletes an enterprise knowledge base and its subdirectories.
 *
 * @description ## Request description
 * - This API allows you to delete a specific enterprise knowledge base directory.
 * - Set the `deleteMode` parameter to select different deletion strategies, including reject deletion (reject), recursive deletion (recursive), or move the directory to the root directory (move_to_root).
 * - If `deleteMode` is not provided, the default behavior is to reject deletion.
 * - The enterprise directory boundary is validated before the deletion operation.
 *
 * @param request DeleteTenantDirectoryRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteTenantDirectoryResponse
 */
async function deleteTenantDirectoryWithOptions(request: DeleteTenantDirectoryRequest, headers: map[string]string, runtime: Util.RuntimeOptions): DeleteTenantDirectoryResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.deleteMode)) {
    body['deleteMode'] = request.deleteMode;
  }
  if (!Util.isUnset(request.directoryId)) {
    body['directoryId'] = request.directoryId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'DeleteTenantDirectory',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/deleteTenantDirectory`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Deletes an enterprise knowledge base and its subdirectories.
 *
 * @description ## Request description
 * - This API allows you to delete a specific enterprise knowledge base directory.
 * - Set the `deleteMode` parameter to select different deletion strategies, including reject deletion (reject), recursive deletion (recursive), or move the directory to the root directory (move_to_root).
 * - If `deleteMode` is not provided, the default behavior is to reject deletion.
 * - The enterprise directory boundary is validated before the deletion operation.
 *
 * @param request DeleteTenantDirectoryRequest
 * @return DeleteTenantDirectoryResponse
 */
async function deleteTenantDirectory(request: DeleteTenantDirectoryRequest): DeleteTenantDirectoryResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return deleteTenantDirectoryWithOptions(request, headers, runtime);
}

model DisableTokenRequest {
  tenantId?: string(name='tenantId', description='The ID of the effective tenant.', example='10000'),
  wnUserId?: string(name='wnUserId', description='The ID of the target user (WINNEXO platform user ID). If left empty, the operation is performed on the caller. Administrators can specify another user\\\\"s ID to perform the operation on their behalf.', example='1'),
}

model DisableTokenResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  disabled?: boolean(name='disabled', description='Indicates whether the token is disabled. Valid values:
- true: Disabled.
- false: Not disabled.', example='true'),
  message?: string(name='message', description='The description of the status code.', example='successful'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
}

model DisableTokenResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DisableTokenResponseBody(name='body'),
}

/**
 * @summary Disables an API token.
 *
 * @description Disables the INSTANCE token of a user.
 *     Business logic:
 *     1. Retrieves user_id from identity (caller_type=user is enforced).
 *     2. Constructs an AuthContext and delegates permission verification to UserTokenAuthorizedService.
 *     3. Calls disable_token (ACTIVE → INACTIVE).
 *     4. Returns disabled=True.
 *     Idempotence: If no ACTIVE token exists, deactivate_all affects 0 rows and does not return an error.
 *
 * @param request DisableTokenRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return DisableTokenResponse
 */
async function disableTokenWithOptions(request: DisableTokenRequest, headers: map[string]string, runtime: Util.RuntimeOptions): DisableTokenResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.wnUserId)) {
    body['wnUserId'] = request.wnUserId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'DisableToken',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/disableToken`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Disables an API token.
 *
 * @description Disables the INSTANCE token of a user.
 *     Business logic:
 *     1. Retrieves user_id from identity (caller_type=user is enforced).
 *     2. Constructs an AuthContext and delegates permission verification to UserTokenAuthorizedService.
 *     3. Calls disable_token (ACTIVE → INACTIVE).
 *     4. Returns disabled=True.
 *     Idempotence: If no ACTIVE token exists, deactivate_all affects 0 rows and does not return an error.
 *
 * @param request DisableTokenRequest
 * @return DisableTokenResponse
 */
async function disableToken(request: DisableTokenRequest): DisableTokenResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return disableTokenWithOptions(request, headers, runtime);
}

model EnableTokenRequest {
  tenantId?: string(name='tenantId', description='The ID of the effective tenant.', example='10000'),
  wnUserId?: string(name='wnUserId', description='The ID of the target user (WINNEXO platform user ID). If this parameter is left empty, the operation is performed on the caller. Administrators can specify another user\\\\"s ID to perform the operation on behalf of that user.', example='1'),
}

model EnableTokenResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  message?: string(name='message', description='The description of the status code.', example='ok'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  token?: string(name='token', description='The temporary access credential for the data catalog.', example='example_token_value'),
  tokenMasked?: string(name='tokenMasked', description='The masked token value.', example='string_value'),
}

model EnableTokenResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: EnableTokenResponseBody(name='body'),
}

/**
 * @summary Enables an API token.
 *
 * @description Enables the INSTANCE token for a user (idempotent).
 *     Business logic:
 *     1. Retrieves user_id from identity (caller_type=user is required).
 *     2. Constructs an AuthContext and delegates permission verification to UserTokenAuthorizedService.
 *     3. Calls enable_token:
 *        - If an ACTIVE token exists, returns idempotently (only the masked value is returned, and the plaintext is not issued again).
 *        - If an INACTIVE token exists, reactivates it (returns the plaintext).
 *        - If no token exists, creates one (returns the plaintext).
 *     Security constraint: The token plaintext is returned only once when the token is first enabled. Subsequent idempotent calls do not return the plaintext.
 *
 * @param request EnableTokenRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return EnableTokenResponse
 */
async function enableTokenWithOptions(request: EnableTokenRequest, headers: map[string]string, runtime: Util.RuntimeOptions): EnableTokenResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.wnUserId)) {
    body['wnUserId'] = request.wnUserId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'EnableToken',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/enableToken`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Enables an API token.
 *
 * @description Enables the INSTANCE token for a user (idempotent).
 *     Business logic:
 *     1. Retrieves user_id from identity (caller_type=user is required).
 *     2. Constructs an AuthContext and delegates permission verification to UserTokenAuthorizedService.
 *     3. Calls enable_token:
 *        - If an ACTIVE token exists, returns idempotently (only the masked value is returned, and the plaintext is not issued again).
 *        - If an INACTIVE token exists, reactivates it (returns the plaintext).
 *        - If no token exists, creates one (returns the plaintext).
 *     Security constraint: The token plaintext is returned only once when the token is first enabled. Subsequent idempotent calls do not return the plaintext.
 *
 * @param request EnableTokenRequest
 * @return EnableTokenResponse
 */
async function enableToken(request: EnableTokenRequest): EnableTokenResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return enableTokenWithOptions(request, headers, runtime);
}

model GetChatSessionRequest {
  limit?: int32(name='limit', description='The maximum number of resources to return. If not specified, the default value is 20. The maximum value is 100. The actual number of returned results may be less than the specified value but will not exceed it.', example='100'),
  sessionId?: string(name='sessionId', description='The SessionId value to filter by. If specified, all Active/Expired status information associated with this session is returned.

This parameter is required.', example='exampleSessionId'),
  tenantId?: string(name='tenantId', description='The effective tenant ID.', example='10000'),
}

model GetChatSessionResponseBody = {
  code?: string(name='code', description='The error code.', example='200'),
  message?: string(name='message', description='The status code description.', example='successful'),
  messages?: [ 
    {
      content?: string(name='content', description='The message content.', example='Sample content'),
      contextCleared?: boolean(name='contextCleared', description='Indicates whether the LLM context has been cleared after this message.', example='true'),
      fromShare?: boolean(name='fromShare', description='Indicates whether the message is copied from a shared conversation.', example='true'),
      id?: string(name='id', description='The message ID.', example='exampleId'),
      metadata?: map[string]any(name='metadata', description='The message metadata.', example='1'),
      object?: string(name='object', description='The type.', example='string_value'),
      role?: string(name='role', description='The role.', example='string_value'),
      shareUserName?: string(name='shareUserName', description='The username of the sharing source. This parameter has a value only when from_share=True.', example='string_value'),
      status?: string(name='status', description='The message status.', example='READY'),
      traceId?: string(name='traceId', description='The trace ID.', example='exampleTraceId'),
      updateAt?: long(name='updateAt', description='The update time.', example='20240101'),
      userFeedback?: string(name='userFeedback', description='The user feedback type: LIKE | DISLIKE | CANCEL.', example='string_value'),
    }
  ](name='messages', description='The message data detail structure.'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  session?: {
    createdAt?: long(name='createdAt', description='The creation time.', example='1'),
    id?: string(name='id', description='The message ID.', example='exampleId'),
    isExpired?: boolean(name='isExpired', description='Indicates whether the creation time exceeds 30 days.', example='true'),
    metadata?: map[string]any(name='metadata', description='The session metadata.', example='exampleObjectId'),
    model?: string(name='model', description='The abstract model name used by the session (quick/standard/flagship).', example='string_value'),
    object?: string(name='object', description='The type.', example='string_value'),
    objectId?: string(name='objectId', description='The associated object ID.', example='2676'),
    operatingObjectName?: [ string ](name='operatingObjectName', description='The list of digital employee names.', example='string_value'),
    title?: string(name='title', description='The title.', example='Sample title'),
    updatedAt?: long(name='updatedAt', description='The update time.', example='1'),
  }(name='session', description='The session ID.'),
}

model GetChatSessionResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetChatSessionResponseBody(name='body'),
}

/**
 * @summary Retrieves session details.
 *
 * @description ## Request description
 * - This API uploads a file to the "My Resources" section of a specified digital employee.
 * - `source_type` is fixed to `FILE`, `scope` is fixed to `PERSONAL`, and `platform` is fixed to `LOCAL`.
 * - The file must include an OSS persistent address (`filePath`). Other information such as the public access URL and original file name is optional.
 * - If no target folder ID (`directoryId`) is specified, the file is automatically attached to the default root folder of the current digital employee. If specified, ensure that the folder belongs to the invoker\\"s personal folder.
 * - Security verification is supported through multiple authenticate methods (AK, BearerToken, APP).
 * - The operation type is write (`write`), and operation logs are recorded for subsequent auditing.
 *
 * @param request GetChatSessionRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetChatSessionResponse
 */
async function getChatSessionWithOptions(request: GetChatSessionRequest, headers: map[string]string, runtime: Util.RuntimeOptions): GetChatSessionResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.limit)) {
    query['limit'] = request.limit;
  }
  if (!Util.isUnset(request.sessionId)) {
    query['sessionId'] = request.sessionId;
  }
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'GetChatSession',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/getChatSession`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves session details.
 *
 * @description ## Request description
 * - This API uploads a file to the "My Resources" section of a specified digital employee.
 * - `source_type` is fixed to `FILE`, `scope` is fixed to `PERSONAL`, and `platform` is fixed to `LOCAL`.
 * - The file must include an OSS persistent address (`filePath`). Other information such as the public access URL and original file name is optional.
 * - If no target folder ID (`directoryId`) is specified, the file is automatically attached to the default root folder of the current digital employee. If specified, ensure that the folder belongs to the invoker\\"s personal folder.
 * - Security verification is supported through multiple authenticate methods (AK, BearerToken, APP).
 * - The operation type is write (`write`), and operation logs are recorded for subsequent auditing.
 *
 * @param request GetChatSessionRequest
 * @return GetChatSessionResponse
 */
async function getChatSession(request: GetChatSessionRequest): GetChatSessionResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return getChatSessionWithOptions(request, headers, runtime);
}

model GetGraphSchemaRequest {
  graphName?: string(name='graphName', description='The knowledge graph name. Call listGraphs to obtain this value.

This parameter is required.', example='string_value'),
  tenantId?: string(name='tenantId', description='The effective tenant ID.

This parameter is required.', example='21577'),
}

model GetGraphSchemaResponseBody = {
  code?: string(name='code', description='The response status code.', example='200'),
  graphName?: string(name='graphName', description='The knowledge graph name.

This parameter is required.', example='string_value'),
  message?: string(name='message', description='The status code description.', example='ok'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  schemaVersion?: string(name='schemaVersion', description='The version.

This parameter is required.', example='string_value'),
  yamlEdit?: string(name='yamlEdit', description='The raw YAML text of the Graph Schema trimmed by READ permissions, retaining $ref references within the authorized subgraph.

This parameter is required.', example='string_value'),
}

model GetGraphSchemaResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetGraphSchemaResponseBody(name='body'),
}

/**
 * @summary Retrieves the active Graph Schema that is readable by the current user.
 *
 * @description Reads the active schema_content and securely trims it based on the token user\\"s semantic resource READ permissions.
 *
 * @param request GetGraphSchemaRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetGraphSchemaResponse
 */
async function getGraphSchemaWithOptions(request: GetGraphSchemaRequest, headers: map[string]string, runtime: Util.RuntimeOptions): GetGraphSchemaResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.graphName)) {
    body['graphName'] = request.graphName;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'GetGraphSchema',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/getGraphSchema`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves the active Graph Schema that is readable by the current user.
 *
 * @description Reads the active schema_content and securely trims it based on the token user\\"s semantic resource READ permissions.
 *
 * @param request GetGraphSchemaRequest
 * @return GetGraphSchemaResponse
 */
async function getGraphSchema(request: GetGraphSchemaRequest): GetGraphSchemaResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return getGraphSchemaWithOptions(request, headers, runtime);
}

model GetInstanceExpireTimeRequest {
  tenantId?: string(name='tenantId', description='The effective tenant ID.', example='10000'),
}

model GetInstanceExpireTimeResponseBody = {
  code?: string(name='code', description='The response status code.', example='200'),
  found?: boolean(name='found', description='Indicates whether a standard package instance is found.', example='true'),
  instanceExpireTime?: string(name='instanceExpireTime', description='The expiration time of the instance in ISO format.', example='2023-10-01T12:00:00Z'),
  instanceId?: string(name='instanceId', description='The instance ID. This parameter is required.', example='exampleInstanceId'),
  instanceStatus?: string(name='instanceStatus', description='The instance status. Valid values:
- RUNNING: Running.
- TERMINATED: Terminated.
- COMPLETED: Completed.
- ERROR: Error.', example='string_value'),
  message?: string(name='message', description='The prompt message.', example='ok'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  tenantId?: long(name='tenantId', description='The effective tenant ID.', example='10000'),
}

model GetInstanceExpireTimeResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetInstanceExpireTimeResponseBody(name='body'),
}

/**
 * @summary Queries the expiration time of the most recently created standard package instance for a tenant.
 *
 * @description ## Operation description
 * - This API operation queries the expiration time of the most recently created standard package instance for a specified tenant.
 * - If no standard package instance is found, the `found` field returns `False`.
 * - You can use the `tenantId` parameter to specify the tenant ID. By default, the tenant ID of the caller is used.
 * - The request method is POST and must be called over HTTPS.
 * - Valid authentication information (such as AK, BearerToken, or APP) is required to complete the request.
 *
 * @param request GetInstanceExpireTimeRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetInstanceExpireTimeResponse
 */
async function getInstanceExpireTimeWithOptions(request: GetInstanceExpireTimeRequest, headers: map[string]string, runtime: Util.RuntimeOptions): GetInstanceExpireTimeResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'GetInstanceExpireTime',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/getInstanceExpireTime`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the expiration time of the most recently created standard package instance for a tenant.
 *
 * @description ## Operation description
 * - This API operation queries the expiration time of the most recently created standard package instance for a specified tenant.
 * - If no standard package instance is found, the `found` field returns `False`.
 * - You can use the `tenantId` parameter to specify the tenant ID. By default, the tenant ID of the caller is used.
 * - The request method is POST and must be called over HTTPS.
 * - Valid authentication information (such as AK, BearerToken, or APP) is required to complete the request.
 *
 * @param request GetInstanceExpireTimeRequest
 * @return GetInstanceExpireTimeResponse
 */
async function getInstanceExpireTime(request: GetInstanceExpireTimeRequest): GetInstanceExpireTimeResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return getInstanceExpireTimeWithOptions(request, headers, runtime);
}

model GetKnowledgeBaseSourceRequest {
  sourceId?: string(name='sourceId', description='The unique identifier on the business system side, that is, the business ID.

This parameter is required.', example='exampleSourceId'),
  tenantId?: string(name='tenantId', description='The tenant ID.', example='676577544219585'),
}

model GetKnowledgeBaseSourceResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  description?: string(name='description', description='The description of the to-do card type.', example='Sample description'),
  directoryId?: string(name='directoryId', description='The folder ID.', example='exampleDirectoryId'),
  directoryPath?: string(name='directoryPath', description='The full path of the category to which the knowledge item belongs.', example='string_value'),
  gmtCreate?: long(name='gmtCreate', description='The creation time.', example='1'),
  gmtModified?: long(name='gmtModified', description='The last modification time.', example='1'),
  message?: string(name='message', description='The description of the status code.', example='ok'),
  name?: string(name='name', description='The name.', example='SampleName.pdf'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  sourceId?: string(name='sourceId', description='The unique identifier on the business system side, that is, the business ID.', example='exampleSourceId'),
  sourceKind?: string(name='sourceKind', description='The knowledge base ownership type. Valid values: aliding_kb_doc (DingTalk knowledge base document) and normal (common knowledge).', example='string_value'),
  sourceTags?: string(name='sourceTags', description='The resource tags. This is optional and is a JSON string list, such as ["tagA","tagB"].', example='string_value'),
  sourceType?: string(name='sourceType', description='The type of the resource source. Valid values:

- ExportTaskId: the resource export ID.

- TaskId: the Module execution task ID.

- StatePath: the OSS path where the resource state is stored.', example='string_value'),
  status?: string(name='status', description='The data source status. Valid values:
- **1**: online.
- **0**: offline.', example='READY'),
  statusMessage?: string(name='statusMessage', description='The status message.', example='string_value'),
}

model GetKnowledgeBaseSourceResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetKnowledgeBaseSourceResponseBody(name='body'),
}

/**
 * @summary Queries the details of a specified knowledge item in the enterprise knowledge base.
 *
 * @description ## Operation description
 * - This API operation retrieves the details of a specific knowledge item in the enterprise knowledge base.
 * - Calling this operation requires the `DEVELOPMENT_KB_VIEW` feature permission.
 * - Knowledge details include but are not limited to the knowledge type, name, and description.
 * - The `sourceId` parameter is required to identify the knowledge item to query.
 * - `tenantId` is an optional parameter. The tenant ID of the caller is used by default.
 * - Authentication is supported through `AK`, `BearerToken`, or `APP` methods.
 * - Security constraint: `tenant_id` and `user_id` can only be derived from the authenticated identity.
 *
 * @param request GetKnowledgeBaseSourceRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetKnowledgeBaseSourceResponse
 */
async function getKnowledgeBaseSourceWithOptions(request: GetKnowledgeBaseSourceRequest, headers: map[string]string, runtime: Util.RuntimeOptions): GetKnowledgeBaseSourceResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.sourceId)) {
    body['sourceId'] = request.sourceId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'GetKnowledgeBaseSource',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/getKnowledgeBaseSource`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the details of a specified knowledge item in the enterprise knowledge base.
 *
 * @description ## Operation description
 * - This API operation retrieves the details of a specific knowledge item in the enterprise knowledge base.
 * - Calling this operation requires the `DEVELOPMENT_KB_VIEW` feature permission.
 * - Knowledge details include but are not limited to the knowledge type, name, and description.
 * - The `sourceId` parameter is required to identify the knowledge item to query.
 * - `tenantId` is an optional parameter. The tenant ID of the caller is used by default.
 * - Authentication is supported through `AK`, `BearerToken`, or `APP` methods.
 * - Security constraint: `tenant_id` and `user_id` can only be derived from the authenticated identity.
 *
 * @param request GetKnowledgeBaseSourceRequest
 * @return GetKnowledgeBaseSourceResponse
 */
async function getKnowledgeBaseSource(request: GetKnowledgeBaseSourceRequest): GetKnowledgeBaseSourceResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return getKnowledgeBaseSourceWithOptions(request, headers, runtime);
}

model GetScheduledTaskExecutionDetailRequest {
  executionId?: string(name='executionId', description='The execution record ID.

This parameter is required.', example='exampleExecutionId'),
  tenantId?: string(name='tenantId', description='The tenant ID that takes effect.', example='10000'),
}

model GetScheduledTaskExecutionDetailResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  completedAt?: string(name='completedAt', description='The completion time in ISO 8601 format.', example='string_value'),
  content?: string(name='content', description='The full execution content.', example='Sample content'),
  creator?: string(name='creator', description='The creator.', example='string_value'),
  digitalEmployeeName?: [ string ](name='digitalEmployeeName', description='The list of digital employee names.', example='string_value'),
  errorMessage?: string(name='errorMessage', description='The error message.', example='string_value'),
  executionId?: string(name='executionId', description='The execution ID.', example='exampleExecutionId'),
  files?: [ 
    {
      name?: string(name='name', description='The file name.', example='SampleName.pdf'),
      path?: string(name='path', description='The OSS URL of the file.', example='https://example.com/oss/file.pdf'),
    }
  ](name='files', description='The list of output files.'),
  gmtCreate?: string(name='gmtCreate', description='The creation time in ISO 8601 format.', example='string_value'),
  message?: string(name='message', description='The status code description.', example='ok'),
  metadata?: {
    sessionId?: string(name='sessionId', description='The session ID.', example='exampleSessionId'),
    usage?: map[string]any(name='usage', description='The token usage information.', example='string_value'),
  }(name='metadata', description='The extended metadata.'),
  outputContent?: string(name='outputContent', description='The structured output content.', example='string_value'),
  pushResult?: string(name='pushResult', description='The push status of the execution result.', example='succuss'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  skillCodes?: [ string ](name='skillCodes', description='The list of associated skill codes.', example='string_value'),
  startedAt?: string(name='startedAt', description='The start time in ISO 8601 format.', example='string_value'),
  status?: string(name='status', description='The execution status.', example='READY'),
  taskId?: string(name='taskId', description='The task ID.', example='exampleTaskId'),
  title?: string(name='title', description='The execution result title.', example='Sample title'),
  triggerInfo?: {
    triggeredBy?: string(name='triggeredBy', description='The user identifier that triggered the execution.', example='user_10001'),
  }(name='triggerInfo', description='The trigger information.'),
  triggerType?: string(name='triggerType', description='The trigger type.', example='string_value'),
  visibility?: string(name='visibility', description='The visibility scope of the execution record, which is always equal to the visibility scope of the associated task. Valid values: PRIVATE, COLLABORATIVE, and PUBLIC. This field is empty for personal task executions.', example='COLLABORATIVE'),
}

model GetScheduledTaskExecutionDetailResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetScheduledTaskExecutionDetailResponseBody(name='body'),
}

/**
 * @summary Retrieves the execution details of a scheduled task.
 *
 * @description ## Operation description
 * - This operation uploads a file to an enterprise knowledge base.
 * - You must have the `DEVELOPMENT_KB_MANAGE` permission to call this API operation.
 * - You must provide the OSS persistent address (`filePath`) of the file when uploading.
 * - Optional parameters include the public access URL and original file name to enhance the completeness of file information.
 * - If `directoryId` is specified, the file is placed in the corresponding enterprise knowledge base directory. Otherwise, the file is bound to the default root directory of the current digital employee.
 * - You can add tags to the resource by using `sourceTags` for subsequent management and retrieval.
 * - This operation initiates a billing item (UNSTRUCTURED_PARSE). Make sure your account balance is sufficient.
 *
 * @param request GetScheduledTaskExecutionDetailRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetScheduledTaskExecutionDetailResponse
 */
async function getScheduledTaskExecutionDetailWithOptions(request: GetScheduledTaskExecutionDetailRequest, headers: map[string]string, runtime: Util.RuntimeOptions): GetScheduledTaskExecutionDetailResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.executionId)) {
    query['executionId'] = request.executionId;
  }
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'GetScheduledTaskExecutionDetail',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/getScheduledTaskExecutionDetail`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves the execution details of a scheduled task.
 *
 * @description ## Operation description
 * - This operation uploads a file to an enterprise knowledge base.
 * - You must have the `DEVELOPMENT_KB_MANAGE` permission to call this API operation.
 * - You must provide the OSS persistent address (`filePath`) of the file when uploading.
 * - Optional parameters include the public access URL and original file name to enhance the completeness of file information.
 * - If `directoryId` is specified, the file is placed in the corresponding enterprise knowledge base directory. Otherwise, the file is bound to the default root directory of the current digital employee.
 * - You can add tags to the resource by using `sourceTags` for subsequent management and retrieval.
 * - This operation initiates a billing item (UNSTRUCTURED_PARSE). Make sure your account balance is sufficient.
 *
 * @param request GetScheduledTaskExecutionDetailRequest
 * @return GetScheduledTaskExecutionDetailResponse
 */
async function getScheduledTaskExecutionDetail(request: GetScheduledTaskExecutionDetailRequest): GetScheduledTaskExecutionDetailResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return getScheduledTaskExecutionDetailWithOptions(request, headers, runtime);
}

model GetScheduledTaskExecutionRecordsRequest {
  collaborationGroupId?: string(name='collaborationGroupId', description='The ID of the collaboration group to which the task belongs (such as cg_101). If specified, a group space task is created (the caller must be a valid group member). If left empty, a personal task is created.', example='1112'),
  initiatorUserId?: string(name='initiatorUserId', description='Filters by initiator (platform user ID). The scope is the executor of the record (manual execution = the person who triggered it, automatic execution = the task creator). To view only tasks initiated by yourself, pass the current user ID.', example='5'),
  page?: int32(name='page', description='The page number. Default value: 1. Minimum value: 1. Maximum value: 200.', example='exampleCollaborationGroupId'),
  pageSize?: int32(name='pageSize', description='The number of records per page.', example='10'),
  status?: string(name='status', description='Filters by execution status (lowercase). Valid values:
- pending: queued.
- running: in progress.
- success: succeeded.
- failed: failed.
- timeout: timed out.
- cancelled: terminated.

If not specified, no status filter is applied. If specified, future planned items are no longer generated.', example='failed'),
  taskId?: string(name='taskId', description='Filters by a single task ID. If not specified, execution records of all visible tasks are returned.', example='exampleTaskId'),
  tenantId?: string(name='tenantId', description='The ID of the effective tenant.', example='10000'),
}

model GetScheduledTaskExecutionRecordsResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  hasMore?: boolean(name='hasMore', description='Indicates whether more data is available.', example='true'),
  message?: string(name='message', description='The description of the status code.', example='The current zone list is illegal.'),
  page?: int32(name='page', description='The current page number.', example='1'),
  pageSize?: int32(name='pageSize', description='The number of tasks per page.', example='20'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  tasks?: [ 
    {
      collaborationGroupId?: string(name='collaborationGroupId', description='The ID of the collaboration group to which the task belongs. If empty, the task is a personal task.', example='exampleCollaborationGroupId'),
      cronExpression?: string(name='cronExpression', description='The cron expression.', example='string_value'),
      description?: string(name='description', description='The description of the to-do card type.', example='Sample description'),
      isOpen?: boolean(name='isOpen', description='Indicates whether public access is enabled.', example='true'),
      model?: string(name='model', description='The execution model tier. Valid values:
- flagship: flagship.
- standard: standard.
- quick: lightweight.', example='standard'),
      name?: string(name='name', description='The name.', example='SampleName.pdf'),
      taskId?: string(name='taskId', description='The task ID.', example='exampleTaskId'),
      timeline?: [ 
        {
          actualTime?: string(name='actualTime', description='The actual working hours, in hours.', example='2023-10-01T12:00:00Z'),
          displayName?: string(name='displayName', description='The name of the schedule location.', example='string_value'),
          errorMessage?: string(name='errorMessage', description='The error message.', example='string_value'),
          executionId?: string(name='executionId', description='The execution record ID.', example='exampleExecutionId'),
          isExpired?: boolean(name='isExpired', description='Indicates whether the execution record has been archived due to expiration.', example='false'),
          outputContent?: string(name='outputContent', description='The execution output content (historical records only).', example='string_value'),
          scheduledTime?: string(name='scheduledTime', description='The timed scheduling time.', example='2023-10-01T12:00:00Z'),
          status?: string(name='status', description='The final status of the message.', example='READY'),
        }
      ](name='timeline', description='The timeline.'),
      timezone?: string(name='timezone', description='The time zone.

> Default value: UTC+8.', example='Asia/Shanghai'),
      triggerType?: string(name='triggerType', description='The trigger type. Valid values:
- Manual: manually executed.
- Cron: triggered by a schedule.', example='string_value'),
    }
  ](name='tasks', description='The task list.'),
  total?: long(name='total', description='The total number of tasks.', example='1'),
}

model GetScheduledTaskExecutionRecordsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetScheduledTaskExecutionRecordsResponseBody(name='body'),
}

/**
 * @summary Retrieves execution records of scheduled tasks.
 *
 * @description ## Operation description
 * - This operation uploads a file to the enterprise knowledge base.
 * - The `DEVELOPMENT_KB_MANAGE` feature permission is required to call this API.
 * - You must provide the OSS persistent address (`filePath`) of the file when uploading.
 * - Optional parameters include the public access URL and original file name to enhance the completeness of file information.
 * - If `directoryId` is specified, the file is placed in the corresponding enterprise knowledge base directory. Otherwise, the file is bound to the default root directory of the current digital employee.
 * - You can add tags to the resource by using `sourceTags` for subsequent management and retrieval.
 * - This operation initiates a billing item (UNSTRUCTURED_PARSE). Ensure that your account balance is sufficient.
 *
 * @param request GetScheduledTaskExecutionRecordsRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetScheduledTaskExecutionRecordsResponse
 */
async function getScheduledTaskExecutionRecordsWithOptions(request: GetScheduledTaskExecutionRecordsRequest, headers: map[string]string, runtime: Util.RuntimeOptions): GetScheduledTaskExecutionRecordsResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.collaborationGroupId)) {
    query['collaborationGroupId'] = request.collaborationGroupId;
  }
  if (!Util.isUnset(request.initiatorUserId)) {
    query['initiatorUserId'] = request.initiatorUserId;
  }
  if (!Util.isUnset(request.page)) {
    query['page'] = request.page;
  }
  if (!Util.isUnset(request.pageSize)) {
    query['pageSize'] = request.pageSize;
  }
  if (!Util.isUnset(request.status)) {
    query['status'] = request.status;
  }
  if (!Util.isUnset(request.taskId)) {
    query['taskId'] = request.taskId;
  }
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'GetScheduledTaskExecutionRecords',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/getScheduledTaskExecutionRecords`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves execution records of scheduled tasks.
 *
 * @description ## Operation description
 * - This operation uploads a file to the enterprise knowledge base.
 * - The `DEVELOPMENT_KB_MANAGE` feature permission is required to call this API.
 * - You must provide the OSS persistent address (`filePath`) of the file when uploading.
 * - Optional parameters include the public access URL and original file name to enhance the completeness of file information.
 * - If `directoryId` is specified, the file is placed in the corresponding enterprise knowledge base directory. Otherwise, the file is bound to the default root directory of the current digital employee.
 * - You can add tags to the resource by using `sourceTags` for subsequent management and retrieval.
 * - This operation initiates a billing item (UNSTRUCTURED_PARSE). Ensure that your account balance is sufficient.
 *
 * @param request GetScheduledTaskExecutionRecordsRequest
 * @return GetScheduledTaskExecutionRecordsResponse
 */
async function getScheduledTaskExecutionRecords(request: GetScheduledTaskExecutionRecordsRequest): GetScheduledTaskExecutionRecordsResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return getScheduledTaskExecutionRecordsWithOptions(request, headers, runtime);
}

model GetScheduledTaskUnderstandDetailRequest {
  collaborationGroupId?: string(name='collaborationGroupId', description='The ID of the collaboration group to which the task belongs (such as cg_101). If this parameter is specified, a group space task is created (the caller must be a valid group member). If this parameter is left empty, a personal task is created.', example='exampleCollaborationGroupId'),
  digitalEmployeeName?: [ string ](name='digitalEmployeeName', description='The name of the current effective digital employee. This parameter is empty if not configured.

This parameter is required.', example='string_value'),
  segments?: [ 
    {
      content?: string(name='content', description='The text generated by the model.', example='Sample content'),
      enabled?: string(name='enabled', description='Specifies whether to enable the backfill policy. If this parameter is not specified or is set to true, the policy is enabled. If this parameter is set to false, the policy is disabled but the configuration is retained.', example='true'),
      name?: string(name='name', description='The name.', example='SampleName.pdf'),
      objectId?: string(name='objectId', description='The ID of the recommended item, which can be a **feedId** or a mini-app ID.', example='exampleObjectId'),
      objectType?: string(name='objectType', description='The object type, such as customer. This parameter has a value when type is set to mention.', example='string_value'),
      skillCode?: string(name='skillCode', description='The skill code. This parameter has a value when type is set to skill.', example='string_value'),
      type?: string(name='type', description='The element type: text|web_search|mention|skill.

This parameter is required.', example='text'),
    }
  ](name='segments', description='The segments.'),
  tenantId?: string(name='tenantId', description='The ID of the effective tenant.', example='10000'),
  userInput?: string(name='userInput', description='The natural language task description.', example='string_value'),
}

model GetScheduledTaskUnderstandDetailShrinkRequest {
  collaborationGroupId?: string(name='collaborationGroupId', description='The ID of the collaboration group to which the task belongs (such as cg_101). If this parameter is specified, a group space task is created (the caller must be a valid group member). If this parameter is left empty, a personal task is created.', example='exampleCollaborationGroupId'),
  digitalEmployeeNameShrink?: string(name='digitalEmployeeName', description='The name of the current effective digital employee. This parameter is empty if not configured.

This parameter is required.', example='string_value'),
  segmentsShrink?: string(name='segments', description='The segments.'),
  tenantId?: string(name='tenantId', description='The ID of the effective tenant.', example='10000'),
  userInput?: string(name='userInput', description='The natural language task description.', example='string_value'),
}

model GetScheduledTaskUnderstandDetailResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  message?: string(name='message', description='The description of the status code.', example='ok'),
  relatedObjects?: [ 
    {
      mentionType?: string(name='mentionType', description='The mention type, such as objects.', example='string_value'),
      name?: string(name='name', description='The name.', example='SampleName.pdf'),
      objectId?: string(name='objectId', description='The object ID. Pass the project task ID.

- For internal enterprise applications, this is the taskId obtained by calling the [Create a project task](https://open.dingtalk.com/document/orgapp-server/create-a-project-task) operation.

- For third-party enterprise applications, this is the taskId obtained by calling the [Create a project task](https://open.dingtalk.com/document/isvapp-server/create-a-project-task) operation.', example='exampleObjectId'),
      objectType?: string(name='objectType', description='The object type, such as customer. This parameter has a value when type is set to mention.', example='string_value'),
    }
  ](name='relatedObjects', description='The related objects.'),
  relatedSemantics?: [ 
    {
      attributes?: string(name='attributes', description='The information type.', example='{"level": "VIP"}'),
      entity?: string(name='entity', description='The semantic entity name, such as customer or opportunity.', example='customer'),
    }
  ](name='relatedSemantics', description='The related semantics.'),
  relatedSkills?: [ 
    {
      displayName?: string(name='displayName', description='The display name of the MCP service.', example='string_value'),
      name?: string(name='name', description='The name.', example='SampleName.pdf'),
      skillCode?: string(name='skillCode', description='The skill code.', example='string_value'),
      sourceIds?: [ string ](name='sourceIds', description='sourceIds', example='string_value'),
    }
  ](name='relatedSkills', description='The related skills.'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  taskUnderstand?: string(name='taskUnderstand', description='The task understanding description polished by the LLM.', example='string_value'),
}

model GetScheduledTaskUnderstandDetailResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetScheduledTaskUnderstandDetailResponseBody(name='body'),
}

/**
 * @deprecated OpenAPI GetScheduledTaskUnderstandDetail is deprecated
 *
 * @summary Retrieves the details of scheduled task understanding.
 *
 * @description ## Request description
 * - This operation uploads a file to the enterprise knowledge base.
 * - You must have the `DEVELOPMENT_KB_MANAGE` permission to call this API operation.
 * - You must provide the OSS persistent address (`filePath`) of the file when uploading.
 * - Optional parameters include the public access URL of the file and the original file name to enhance the completeness of file information.
 * - If `directoryId` is specified, the file is placed in the corresponding enterprise knowledge base directory. Otherwise, the file is bound to the default root directory of the current digital employee by default.
 * - You can add tags to the resource by using `sourceTags` for subsequent management and retrieval.
 * - This operation initiates a billing item (UNSTRUCTURED_PARSE). Ensure that your account balance is sufficient.
 *
 * @param tmpReq GetScheduledTaskUnderstandDetailRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetScheduledTaskUnderstandDetailResponse
 */
// Deprecated
async function getScheduledTaskUnderstandDetailWithOptions(tmpReq: GetScheduledTaskUnderstandDetailRequest, headers: map[string]string, runtime: Util.RuntimeOptions): GetScheduledTaskUnderstandDetailResponse {
  Util.validateModel(tmpReq);
  var request = new GetScheduledTaskUnderstandDetailShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!Util.isUnset(tmpReq.digitalEmployeeName)) {
    request.digitalEmployeeNameShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.digitalEmployeeName, 'digitalEmployeeName', 'json');
  }
  if (!Util.isUnset(tmpReq.segments)) {
    request.segmentsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.segments, 'segments', 'json');
  }
  var query : map[string]any = {};
  if (!Util.isUnset(request.collaborationGroupId)) {
    query['collaborationGroupId'] = request.collaborationGroupId;
  }
  if (!Util.isUnset(request.digitalEmployeeNameShrink)) {
    query['digitalEmployeeName'] = request.digitalEmployeeNameShrink;
  }
  if (!Util.isUnset(request.segmentsShrink)) {
    query['segments'] = request.segmentsShrink;
  }
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }
  if (!Util.isUnset(request.userInput)) {
    query['userInput'] = request.userInput;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'GetScheduledTaskUnderstandDetail',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/getScheduledTaskUnderstandDetail`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @deprecated OpenAPI GetScheduledTaskUnderstandDetail is deprecated
 *
 * @summary Retrieves the details of scheduled task understanding.
 *
 * @description ## Request description
 * - This operation uploads a file to the enterprise knowledge base.
 * - You must have the `DEVELOPMENT_KB_MANAGE` permission to call this API operation.
 * - You must provide the OSS persistent address (`filePath`) of the file when uploading.
 * - Optional parameters include the public access URL of the file and the original file name to enhance the completeness of file information.
 * - If `directoryId` is specified, the file is placed in the corresponding enterprise knowledge base directory. Otherwise, the file is bound to the default root directory of the current digital employee by default.
 * - You can add tags to the resource by using `sourceTags` for subsequent management and retrieval.
 * - This operation initiates a billing item (UNSTRUCTURED_PARSE). Ensure that your account balance is sufficient.
 *
 * @param request GetScheduledTaskUnderstandDetailRequest
 * @return GetScheduledTaskUnderstandDetailResponse
 */
// Deprecated
async function getScheduledTaskUnderstandDetail(request: GetScheduledTaskUnderstandDetailRequest): GetScheduledTaskUnderstandDetailResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return getScheduledTaskUnderstandDetailWithOptions(request, headers, runtime);
}

model GetSkillRequest {
  includeSkillFiles?: boolean(name='includeSkillFiles', description='Specifies whether to return the complete file tree. Default value: False. This avoids large response payloads.', example='false'),
  skillCode?: string(name='skillCode', description='The skill code. This parameter has a value when type is set to skill.', example='string_value'),
  skillName?: string(name='skillName', description='The skill name.', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID to which the task belongs.', example='10000'),
  viewMode?: string(name='viewMode', description='The view mode. Valid values: draft (draft/editing view) or published (published view, default).', example='draft'),
}

model GetSkillResponseBody = {
  arguments?: [ 
    {
      default?: string(name='default', description='The default value.', example='string_value'),
      description?: string(name='description', description='The parameter description.', example='Sample description'),
      enum?: [ string ](name='enum', description='The list of valid values, which is available when the options are limited.', example='string_value'),
      name?: string(name='name', description='The parameter name, which corresponds to the key in runSkill.arguments.', example='SampleName.pdf'),
      required?: boolean(name='required', description='Indicates whether the parameter is required.', example='true'),
      type?: string(name='type', description='The parameter type. Valid values: string, number, boolean, and array.', example='text'),
    }
  ](name='arguments', description='The simplified parameter schema that describes what needs to be passed in runSkill.arguments. This is automatically extracted from inputConfigFormatted.'),
  code?: string(name='code', description='The response status code.', example='200'),
  createdTime?: string(name='createdTime', description='The creation time in ISO 8601 format.', example='2023-10-01T12:00:00Z'),
  description?: string(name='description', description='The skill description, which has been resolved through internationalization (i18n).', example='Sample description'),
  detailLogic?: string(name='detailLogic', description='The detailed logic of the skill.', example='string_value'),
  displayName?: string(name='displayName', description='The display name.', example='string_value'),
  executeMode?: string(name='executeMode', description='The execution mode.', example='string_value'),
  globalAccess?: boolean(name='globalAccess', description='Indicates whether the skill is globally accessible.', example='true'),
  hasDraftChanges?: boolean(name='hasDraftChanges', description='Indicates whether unpublished draft changes exist.', example='true'),
  inputConfig?: string(name='inputConfig', description='The raw input parameter configuration.', example='string_value'),
  inputConfigFormatted?: [  map[string]any ](name='inputConfigFormatted', description='The structured input parameter schema array.'),
  message?: string(name='message', description='The status code description.', example='ok'),
  name?: string(name='name', description='The skill name, which has been resolved through internationalization (i18n).', example='SampleName.pdf'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  skillCode?: string(name='skillCode', description='The skill code, which is globally unique.', example='string_value'),
  skillFiles?: [  map[string]any ](name='skillFiles', description='The nested tree of skill files. This is returned only when includeSkillFiles is set to True.'),
  skillHubDefinitionId?: long(name='skillHubDefinitionId', description='The skill definition ID.', example='1'),
  skillMdSummary?: string(name='skillMdSummary', description='The SKILL.md summary, which is generated by a large language model (LLM).', example='string_value'),
  sourceType?: string(name='sourceType', description='The source type. Valid values: BUILTIN and CUSTOM.', example='string_value'),
  status?: string(name='status', description='The skill status. Valid values: ACTIVE and DRAFT.', example='READY'),
  tags?: [ string ](name='tags', description='The tag list, which has been resolved through internationalization (i18n).', example='string_value'),
  updatedTime?: string(name='updatedTime', description='The modification time in ISO 8601 format.', example='2023-10-01T12:00:00Z'),
  versionCount?: long(name='versionCount', description='The total number of versions.', example='1'),
  versionNumber?: string(name='versionNumber', description='The version number.', example='string_value'),
}

model GetSkillResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetSkillResponseBody(name='body'),
}

/**
 * @summary Retrieves skill details.
 *
 * @description ## Request description
 * Queries skill details by SkillCode or SkillName, including metadata, input parameter schema, and SKILL.md summary.
 * - **TenantId**: Optional common parameter passed through by the gateway to the backend header. If not specified, the default tenant of the current caller is used.
 * - **SkillCode**: Mutually exclusive with SkillName. If both are specified, SkillCode takes precedence.
 * - **SkillName**: Mutually exclusive with SkillCode. If the name is not unique within the tenant, `ERR.SkillHub.SkillNameAmbiguous` is returned.
 * - **ViewMode**: Optional. Valid values: `draft` (draft/editing view) or `published` (published view, default).
 * - **IncludeSkillFiles**: Optional. Specifies whether to return the complete skill file tree (SKILL.md / scripts / templates). Default value: `false`.
 *
 * @param request GetSkillRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetSkillResponse
 */
async function getSkillWithOptions(request: GetSkillRequest, headers: map[string]string, runtime: Util.RuntimeOptions): GetSkillResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.includeSkillFiles)) {
    body['includeSkillFiles'] = request.includeSkillFiles;
  }
  if (!Util.isUnset(request.skillCode)) {
    body['skillCode'] = request.skillCode;
  }
  if (!Util.isUnset(request.skillName)) {
    body['skillName'] = request.skillName;
  }
  if (!Util.isUnset(request.viewMode)) {
    body['viewMode'] = request.viewMode;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'GetSkill',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/getSkill`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves skill details.
 *
 * @description ## Request description
 * Queries skill details by SkillCode or SkillName, including metadata, input parameter schema, and SKILL.md summary.
 * - **TenantId**: Optional common parameter passed through by the gateway to the backend header. If not specified, the default tenant of the current caller is used.
 * - **SkillCode**: Mutually exclusive with SkillName. If both are specified, SkillCode takes precedence.
 * - **SkillName**: Mutually exclusive with SkillCode. If the name is not unique within the tenant, `ERR.SkillHub.SkillNameAmbiguous` is returned.
 * - **ViewMode**: Optional. Valid values: `draft` (draft/editing view) or `published` (published view, default).
 * - **IncludeSkillFiles**: Optional. Specifies whether to return the complete skill file tree (SKILL.md / scripts / templates). Default value: `false`.
 *
 * @param request GetSkillRequest
 * @return GetSkillResponse
 */
async function getSkill(request: GetSkillRequest): GetSkillResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return getSkillWithOptions(request, headers, runtime);
}

model GetSkillRunRequest {
  includeLogs?: boolean(name='includeLogs', description='Specifies whether to include execution logs. Default value: false. Enable this parameter only for troubleshooting.', example='false'),
  runId?: string(name='runId', description='The evaluation run ID.

This parameter is required.', example='exampleRunId'),
  tenantId?: string(name='tenantId', description='The tenant ID to which the task belongs.', example='10000'),
}

model GetSkillRunResponseBody = {
  code?: string(name='code', description='The response status code.', example='200'),
  createdAt?: string(name='createdAt', description='The task creation time in ISO 8601 format.', example='string_value'),
  errorCode?: string(name='errorCode', description='The error code. This parameter is returned only when the status is Failed.', example='string_value'),
  errorMessage?: string(name='errorMessage', description='The error description. This parameter is returned only when the status is Failed.', example='string_value'),
  finishedAt?: string(name='finishedAt', description='The task end time in ISO 8601 format. This parameter has a value only in desired states (Succeeded, Failed, or Cancelled).', example='string_value'),
  logs?: [  map[string]any ](name='logs', description='The execution log list. This parameter is returned only when IncludeLogs is set to true.'),
  message?: string(name='message', description='The status code description.', example='successful'),
  progress?: long(name='progress', description='The progress percentage. This parameter is meaningful only when the status is Running.', example='1'),
  progressMessage?: string(name='progressMessage', description='The progress description.', example='string_value'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  result?: map[string]any(name='result', description='The execution result. This parameter is returned only when the status is Succeeded. It contains a content list.'),
  runId?: string(name='runId', description='The asynchronous task ID.', example='exampleRunId'),
  skillCode?: string(name='skillCode', description='The skill code.', example='string_value'),
  skillName?: string(name='skillName', description='The skill name.', example='string_value'),
  startedAt?: string(name='startedAt', description='The task execution start time in ISO 8601 format.', example='string_value'),
  status?: string(name='status', description='The execution status. Valid values: Running, Succeeded, Failed, and Cancelled.', example='READY'),
  usage?: map[string]any(name='usage', description='The LLM token usage statistics. This parameter is returned only when the status is Succeeded.'),
}

model GetSkillRunResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetSkillRunResponseBody(name='body'),
}

/**
 * @summary Queries the execution result of a skill.
 *
 * @description ## Request description
 * Queries the current status and result of an asynchronous task by `RunId`.
 * - **State machine**: Running (PENDING/RUNNING) → Succeeded / Failed / Cancelled
 * - **TenantId**: An optional common parameter passed through by the gateway. The backend verifies that the RunId belongs to the current tenant. Otherwise, `ERR.SkillHub.RunNotFound` is returned to avoid exposing existence information.
 * - **IncludeLogs**: Optional. Specifies whether to return execution logs. Default value: `false`.
 * When execution succeeds, `Result.Content[]` is an MCP-style Content block array (Text / File / Image).
 *
 * @param request GetSkillRunRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetSkillRunResponse
 */
async function getSkillRunWithOptions(request: GetSkillRunRequest, headers: map[string]string, runtime: Util.RuntimeOptions): GetSkillRunResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.includeLogs)) {
    body['includeLogs'] = request.includeLogs;
  }
  if (!Util.isUnset(request.runId)) {
    body['runId'] = request.runId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'GetSkillRun',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/getSkillRun`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the execution result of a skill.
 *
 * @description ## Request description
 * Queries the current status and result of an asynchronous task by `RunId`.
 * - **State machine**: Running (PENDING/RUNNING) → Succeeded / Failed / Cancelled
 * - **TenantId**: An optional common parameter passed through by the gateway. The backend verifies that the RunId belongs to the current tenant. Otherwise, `ERR.SkillHub.RunNotFound` is returned to avoid exposing existence information.
 * - **IncludeLogs**: Optional. Specifies whether to return execution logs. Default value: `false`.
 * When execution succeeds, `Result.Content[]` is an MCP-style Content block array (Text / File / Image).
 *
 * @param request GetSkillRunRequest
 * @return GetSkillRunResponse
 */
async function getSkillRun(request: GetSkillRunRequest): GetSkillRunResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return getSkillRunWithOptions(request, headers, runtime);
}

model GetSourceRequest {
  includeDetails?: boolean(name='includeDetails', description='Specifies whether to return large detail fields (settings / notes / structuredTables / unstructuredDocs). Default value: False, which returns only metadata.', example='false'),
  sourceId?: string(name='sourceId', description='The primary ID of the resource.

This parameter is required.', example='exampleSourceId'),
  tenantId?: string(name='tenantId', description='The tenant ID to which the task belongs.', example='10000'),
}

model GetSourceResponseBody = {
  code?: string(name='code', description='The error code.', example='200'),
  completionTime?: string(name='completionTime', description='The completion time, in milliseconds.', example='2023-10-01T12:00:00Z'),
  description?: string(name='description', description='The description of the to-do card type.', example='Sample description'),
  gmtCreate?: string(name='gmtCreate', description='The creation time.', example='string_value'),
  gmtModified?: string(name='gmtModified', description='The last modification time.', example='string_value'),
  hasNotes?: boolean(name='hasNotes', description='Indicates whether notes exist.', example='true'),
  hasSettings?: boolean(name='hasSettings', description='Indicates whether settings configuration exists.', example='true'),
  hasStructuredTables?: boolean(name='hasStructuredTables', description='Indicates whether structured tables exist.', example='true'),
  hasUnstructuredDocs?: boolean(name='hasUnstructuredDocs', description='Indicates whether unstructured documents exist.', example='true'),
  message?: string(name='message', description='The status code description.', example='ok'),
  name?: string(name='name', description='The username.', example='SampleName.pdf'),
  notes?: string(name='notes', description='The meeting notes content (optional), which participates in auxiliary analysis.', example='string_value'),
  objectBindings?: [ 
    {
      graphName?: string(name='graphName', description='The semantic graph name to which the object belongs.', example='string_value'),
      objectId?: string(name='objectId', description='The ID of the recommended item, which can be a **feedId** or a micro-application ID.', example='exampleObjectId'),
      objectType?: string(name='objectType', description='The object type, such as customer. This field has a value when type is set to mention.', example='string_value'),
    }
  ](name='objectBindings', description='The object bindings.'),
  objectId?: string(name='objectId', description='The associated variable ID.', example='exampleObjectId'),
  objectType?: string(name='objectType', description='The object type, such as customer. This field has a value when type is set to mention.', example='string_value'),
  operatingObjectName?: string(name='operatingObjectName', description='The digital employee name (operating object name, optional).', example='string_value'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  scope?: string(name='scope', description='The task scope.', example='PERSONAL'),
  settings?: map[string]any(name='settings', description='The user profile card settings.'),
  skillOutputId?: string(name='skillOutputId', description='The skill output ID (carried when the output is saved as a resource).', example='exampleSkillOutputId'),
  sourceId?: string(name='sourceId', description='The data source ID.', example='exampleSourceId'),
  sourceKind?: string(name='sourceKind', description='The knowledge base affiliation type. Valid values:
- aliding_kb_doc: DingTalk knowledge base document.
- normal: common knowledge.', example='string_value'),
  sourceTags?: string(name='sourceTags', description='The resource tags (optional, a JSON string list such as ["tagA","tagB"]).', example='string_value'),
  sourceType?: string(name='sourceType', description='The resource type.', example='string_value'),
  status?: string(name='status', description='The final status of the message.', example='READY'),
  structuredTables?: [ string ](name='structuredTables', description='structuredTables', example='string_value'),
  unstructuredDocs?: [ 
    {
      completionTime?: string(name='completionTime', description='The completion time, in milliseconds.', example='2023-10-01T12:00:00Z'),
      fileName?: string(name='fileName', description='The file name, including the file name extension.', example='example.pdf'),
      fileRecordId?: string(name='fileRecordId', description='The file record ID (optional, corresponding to settings.file_record_id).', example='exampleFileRecordId'),
      fileType?: string(name='fileType', description='The file type. Valid values:
- **file**: file.
- **folder**: folder.', example='pdf'),
      ossUrl?: string(name='ossUrl', description='The session analysis result in OSS URL format. The URL expires in one hour.', example='https://example.com/winnexo/resource'),
      semanticsId?: string(name='semanticsId', description='The DocumentAgent semantic ID.', example='exampleSemanticsId'),
    }
  ](name='unstructuredDocs', description='The unstructured documents.'),
}

model GetSourceResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetSourceResponseBody(name='body'),
}

/**
 * @summary Queries the details of a specified resource (knowledge), with support for returning large detail fields on demand.
 *
 * @description ## Operation description
 * - `tenant_id` is derived from the authenticated identity only. Any value passed in the body is ignored.
 * - Response parameters do not expose audit fields such as `creator` or `modifier`. The `unstructured_docs[ ].content` field is not returned by default to avoid large responses.
 * - Set the `includeDetails` parameter to `True` to retrieve additional details including `settings`, `notes`, `structuredTables`, and `unstructuredDocs`.
 *
 * @param request GetSourceRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetSourceResponse
 */
async function getSourceWithOptions(request: GetSourceRequest, headers: map[string]string, runtime: Util.RuntimeOptions): GetSourceResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.includeDetails)) {
    body['includeDetails'] = request.includeDetails;
  }
  if (!Util.isUnset(request.sourceId)) {
    body['sourceId'] = request.sourceId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'GetSource',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/getSource`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the details of a specified resource (knowledge), with support for returning large detail fields on demand.
 *
 * @description ## Operation description
 * - `tenant_id` is derived from the authenticated identity only. Any value passed in the body is ignored.
 * - Response parameters do not expose audit fields such as `creator` or `modifier`. The `unstructured_docs[ ].content` field is not returned by default to avoid large responses.
 * - Set the `includeDetails` parameter to `True` to retrieve additional details including `settings`, `notes`, `structuredTables`, and `unstructuredDocs`.
 *
 * @param request GetSourceRequest
 * @return GetSourceResponse
 */
async function getSource(request: GetSourceRequest): GetSourceResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return getSourceWithOptions(request, headers, runtime);
}

model GetSourceUploadSignatureRequest {
  contentType?: string(name='contentType', description='The content type. Valid values: Text and Markdown.', example='string_value'),
  expires?: long(name='expires', description='The expiration time of the signed URL, in seconds. Default value: 3600.', example='3600'),
  filename?: string(name='filename', description='The file name.

This parameter is required.', example='example.pdf'),
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital employee (operating object name). This parameter is optional.', example='string_value'),
  scope?: string(name='scope', description='The permission scope.', example='source'),
  tenantId?: string(name='tenantId', description='The tenant ID to which the task belongs.', example='10000'),
}

model GetSourceUploadSignatureResponseBody = {
  code?: string(name='code', description='The response status code.', example='200'),
  contentType?: string(name='contentType', description='The content type. Valid values: Text and Markdown.', example='string_value'),
  expiresIn?: long(name='expiresIn', description='The validity period of the task, in seconds.', example='1'),
  filePublicUrl?: string(name='filePublicUrl', description='The publicly accessible URL of the DingTalk online document.', example='https://example.com/winnexo/resource'),
  fileRecordId?: string(name='fileRecordId', description='The file record ID. This parameter is optional and corresponds to settings.file_record_id.', example='exampleFileRecordId'),
  fileUrl?: string(name='fileUrl', description='The Yida attachment URL.', example='https://example.com/winnexo/resource'),
  message?: string(name='message', description='The prompt message.', example='The current zone list is illegal.'),
  method?: string(name='method', description='The method.', example='string_value'),
  objectName?: string(name='objectName', description='The object name.', example='string_value'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  uploadSignatureUrl?: string(name='uploadSignatureUrl', description='The signed URL.', example='https://example.com/winnexo/resource'),
}

model GetSourceUploadSignatureResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetSourceUploadSignatureResponseBody(name='body'),
}

/**
 * @summary Generates a signed URL for directly uploading files to OSS.
 *
 * @description ## Operation description
 * This API allows callers to obtain a signed URL for directly uploading files to Alibaba Cloud Object Storage Service (OSS) based on the provided file name and other information. With this URL, users can upload files directly to the specified OSS location without routing through an intermediate server, which improves efficiency and security.
 * - **Security constraint**: `tenant_id`/`user_id` are derived only from the authenticated identity. Values provided in the request body are ignored.
 * - **Default value**: If the `expires` parameter is not specified, the default expiration time is 3600 seconds (1 hour).
 * - **Content-Type**: If `contentType` is not provided, the system attempts to automatically infer the file type.
 * - **Scope**: The `scope` parameter defines whether the data source belongs to a personal or enterprise knowledge base. In most cases, this does not need to be set.
 *
 * @param request GetSourceUploadSignatureRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetSourceUploadSignatureResponse
 */
async function getSourceUploadSignatureWithOptions(request: GetSourceUploadSignatureRequest, headers: map[string]string, runtime: Util.RuntimeOptions): GetSourceUploadSignatureResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.contentType)) {
    body['contentType'] = request.contentType;
  }
  if (!Util.isUnset(request.expires)) {
    body['expires'] = request.expires;
  }
  if (!Util.isUnset(request.filename)) {
    body['filename'] = request.filename;
  }
  if (!Util.isUnset(request.operatingObjectName)) {
    body['operatingObjectName'] = request.operatingObjectName;
  }
  if (!Util.isUnset(request.scope)) {
    body['scope'] = request.scope;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'GetSourceUploadSignature',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/getSourceUploadSignature`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Generates a signed URL for directly uploading files to OSS.
 *
 * @description ## Operation description
 * This API allows callers to obtain a signed URL for directly uploading files to Alibaba Cloud Object Storage Service (OSS) based on the provided file name and other information. With this URL, users can upload files directly to the specified OSS location without routing through an intermediate server, which improves efficiency and security.
 * - **Security constraint**: `tenant_id`/`user_id` are derived only from the authenticated identity. Values provided in the request body are ignored.
 * - **Default value**: If the `expires` parameter is not specified, the default expiration time is 3600 seconds (1 hour).
 * - **Content-Type**: If `contentType` is not provided, the system attempts to automatically infer the file type.
 * - **Scope**: The `scope` parameter defines whether the data source belongs to a personal or enterprise knowledge base. In most cases, this does not need to be set.
 *
 * @param request GetSourceUploadSignatureRequest
 * @return GetSourceUploadSignatureResponse
 */
async function getSourceUploadSignature(request: GetSourceUploadSignatureRequest): GetSourceUploadSignatureResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return getSourceUploadSignatureWithOptions(request, headers, runtime);
}

model GetTokenInfoRequest {
  tenantId?: string(name='tenantId', description='The ID of the tenant to which the task belongs.', example='10000'),
  wnUserId?: string(name='wnUserId', description='The ID of the target user (WINNEXO platform user ID). If left empty, the operation is performed on the caller. Administrators can specify another user\\\\"s ID to perform the operation on their behalf.', example='1'),
}

model GetTokenInfoResponseBody = {
  code?: string(name='code', description='The error code.', example='200'),
  enabled?: boolean(name='enabled', description='Indicates whether the token is enabled.', example='true'),
  gmtCreate?: string(name='gmtCreate', description='The creation time.', example='string_value'),
  message?: string(name='message', description='The description of the status code.', example='ok'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  tokenMasked?: string(name='tokenMasked', description='The masked token value.', example='string_value'),
}

model GetTokenInfoResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetTokenInfoResponseBody(name='body'),
}

/**
 * @summary Queries the token status of a user.
 *
 * @description Queries the INSTANCE token status of a user.
 *     Business logic:
 *     1. Retrieves user_id from identity (caller_type=user is required).
 *     2. Constructs an AuthContext and delegates permission verification to UserTokenAuthorizedService.
 *     3. Queries the ACTIVE INSTANCE token.
 *     4. If the token exists, returns enabled=True with the masked value and creation time.
 *     5. If the token does not exist, returns enabled=False.
 *
 * @param request GetTokenInfoRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetTokenInfoResponse
 */
async function getTokenInfoWithOptions(request: GetTokenInfoRequest, headers: map[string]string, runtime: Util.RuntimeOptions): GetTokenInfoResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.wnUserId)) {
    body['wnUserId'] = request.wnUserId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'GetTokenInfo',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/getTokenInfo`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the token status of a user.
 *
 * @description Queries the INSTANCE token status of a user.
 *     Business logic:
 *     1. Retrieves user_id from identity (caller_type=user is required).
 *     2. Constructs an AuthContext and delegates permission verification to UserTokenAuthorizedService.
 *     3. Queries the ACTIVE INSTANCE token.
 *     4. If the token exists, returns enabled=True with the masked value and creation time.
 *     5. If the token does not exist, returns enabled=False.
 *
 * @param request GetTokenInfoRequest
 * @return GetTokenInfoResponse
 */
async function getTokenInfo(request: GetTokenInfoRequest): GetTokenInfoResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return getTokenInfoWithOptions(request, headers, runtime);
}

model GetUserRequest {
  tenantId?: string(name='tenantId', description='The ID of the tenant to which the task belongs.', example='10000'),
  wnAccountId?: string(name='wnAccountId', description='The WINNEXO logon account. This is a unique identifier and cannot be empty.', example='exampleAccountId'),
  wnUserId?: string(name='wnUserId', description='The WINNEXO platform user ID. Specify either this parameter or accountId.', example='1'),
}

model GetUserResponseBody = {
  accountId?: string(name='accountId', description='The account ID.', example='exampleAccountId'),
  code?: string(name='code', description='The error code.', example='200'),
  displayName?: string(name='displayName', description='The display name of the account.', example='string_value'),
  gmtCreate?: string(name='gmtCreate', description='The creation time.', example='string_value'),
  isActive?: boolean(name='isActive', description='Indicates whether the account is activated. Valid values:
 - **true**: Activated.
- **false**: Not activated.', example='true'),
  lastLoginTime?: string(name='lastLoginTime', description='The last logon time.', example='2023-10-01T12:00:00Z'),
  message?: string(name='message', description='The status code description.', example='ok'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  roleCodes?: [ string ](name='roleCodes', description='The list of system role codes (full replacement, must contain at least one role). Valid values: SUPER_ADMIN, SYSTEM_ADMIN, SEMANTIC_ADMIN, SKILL_ADMIN, KB_ADMIN, AGENT_ADMIN, and APPLICATION_USER.', example='string_value'),
  userGroupIds?: [ string ](name='userGroupIds', description='The list of user group IDs to which the user belongs.', example='string_value'),
  wnUserId?: string(name='wnUserId', description='The WINNEXO platform user ID.', example='1'),
}

model GetUserResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetUserResponseBody(name='body'),
}

/**
 * @summary Queries user details.
 *
 * @description Queries user details through OpenAPI.
 *     Business orchestration:
 *     1. Locate the user by wnUserId or accountId.
 *     2. Query the user mapping information in the current tenant (status, join time, and last logon time).
 *     3. Query the role list of the user in the current tenant.
 *     4. Query the user group list of the user in the current tenant.
 *     5. Assemble the response.
 *     Error codes:
 *     - ERR.User.NotFound: The user does not exist.
 *     - ERR.User.NotInTenant: The user does not belong to the current tenant.
 *
 * @param request GetUserRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetUserResponse
 */
async function getUserWithOptions(request: GetUserRequest, headers: map[string]string, runtime: Util.RuntimeOptions): GetUserResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }
  if (!Util.isUnset(request.wnAccountId)) {
    query['wnAccountId'] = request.wnAccountId;
  }
  if (!Util.isUnset(request.wnUserId)) {
    query['wnUserId'] = request.wnUserId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'GetUser',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/getUser`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries user details.
 *
 * @description Queries user details through OpenAPI.
 *     Business orchestration:
 *     1. Locate the user by wnUserId or accountId.
 *     2. Query the user mapping information in the current tenant (status, join time, and last logon time).
 *     3. Query the role list of the user in the current tenant.
 *     4. Query the user group list of the user in the current tenant.
 *     5. Assemble the response.
 *     Error codes:
 *     - ERR.User.NotFound: The user does not exist.
 *     - ERR.User.NotInTenant: The user does not belong to the current tenant.
 *
 * @param request GetUserRequest
 * @return GetUserResponse
 */
async function getUser(request: GetUserRequest): GetUserResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return getUserWithOptions(request, headers, runtime);
}

model GetUserCreditUsageRequest {
  tenantId?: string(name='tenantId', description='The ID of the tenant to which the task belongs.', example='10000'),
}

model GetUserCreditUsageResponseBody = {
  code?: string(name='code', description='The error code.', example='200'),
  creditLimit?: string(name='creditLimit', description='The credit limit of the user.', example='string_value'),
  message?: string(name='message', description='The status code description.', example='The current zone list is illegal.'),
  remainingCredits?: string(name='remainingCredits', description='The remaining credits in real time.', example='string_value'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  shadowCreditLimit?: string(name='shadowCreditLimit', description='The shadow credit limit.', example='string_value'),
  shadowRemainingCredits?: string(name='shadowRemainingCredits', description='The remaining shadow credits in real time.', example='string_value'),
  shadowUsedCredits?: string(name='shadowUsedCredits', description='The consumed shadow credits in real time.', example='string_value'),
  tenantId?: long(name='tenantId', description='The ID of the tenant to which the task belongs.', example='10000'),
  usedCredits?: string(name='usedCredits', description='The consumed credits in real time.', example='string_value'),
  userId?: long(name='userId', description='The user ID.', example='1'),
}

model GetUserCreditUsageResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetUserCreditUsageResponseBody(name='body'),
}

/**
 * @summary Queries the real-time credit consumption, limit, and remaining balance of the current logged-on user.
 *
 * @description ## Request description
 * - This API is used to retrieve the credit usage details of the current logged-on user, including the credit limit, consumed credits, and remaining credits.
 * - Data is sourced from a real-time Redis cache, ensuring information immediacy.
 * - You can specify a tenant ID to query the credit usage of a user under a specific tenant. By default, the caller\\"s default tenant is used.
 * - You can optionally provide a `RequestId` as a request identifier, but this is not required.
 *
 * @param request GetUserCreditUsageRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetUserCreditUsageResponse
 */
async function getUserCreditUsageWithOptions(request: GetUserCreditUsageRequest, headers: map[string]string, runtime: Util.RuntimeOptions): GetUserCreditUsageResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'GetUserCreditUsage',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/getUserCreditUsage`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the real-time credit consumption, limit, and remaining balance of the current logged-on user.
 *
 * @description ## Request description
 * - This API is used to retrieve the credit usage details of the current logged-on user, including the credit limit, consumed credits, and remaining credits.
 * - Data is sourced from a real-time Redis cache, ensuring information immediacy.
 * - You can specify a tenant ID to query the credit usage of a user under a specific tenant. By default, the caller\\"s default tenant is used.
 * - You can optionally provide a `RequestId` as a request identifier, but this is not required.
 *
 * @param request GetUserCreditUsageRequest
 * @return GetUserCreditUsageResponse
 */
async function getUserCreditUsage(request: GetUserCreditUsageRequest): GetUserCreditUsageResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return getUserCreditUsageWithOptions(request, headers, runtime);
}

model GetUserInfoRequest {
  tenantId?: string(name='tenantId', description='The effective tenant ID.', example='10000'),
}

model GetUserInfoResponseBody = {
  avatar?: string(name='avatar', description='The profile picture URL.', example='https://example.com/avatar.png'),
  code?: string(name='code', description='The error code.', example='200'),
  crmType?: string(name='crmType', description='The CRM type.', example='standard'),
  isAdmin?: boolean(name='isAdmin', description='Indicates whether the user is an enterprise administrator.', example='true'),
  isSystemTenant?: boolean(name='isSystemTenant', description='Indicates whether the current logon tenant is the system tenant (tenantId=10000).', example='true'),
  languagePreference?: string(name='languagePreference', description='The language preference.', example='string_value'),
  message?: string(name='message', description='The status code description.', example='ok'),
  name?: string(name='name', description='The username.', example='SampleName.pdf'),
  offering?: string(name='offering', description='The user service description. Maximum length: 1000 characters.', example='string_value'),
  parsedOffering?: string(name='parsedOffering', description='The parsed result of the user service (JSON format).', example='string_value'),
  profileRole?: string(name='profileRole', description='The user role.', example='string_value'),
  profileRoleInfo?: string(name='profileRoleInfo', description='The personal profile.', example='string_value'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  selfIntroduction?: string(name='selfIntroduction', description='The user self-introduction. Maximum length: 1000 characters.', example='string_value'),
  tenantId?: long(name='tenantId', description='The effective tenant ID.', example='10000'),
  tenantList?: [ 
    {
      crmType?: string(name='crmType', description='The CRM type.', example='standard'),
      tenantId?: long(name='tenantId', description='The ID of the tenant to which the task belongs.', example='10000'),
      tenantName?: string(name='tenantName', description='The tenant name.', example='string_value'),
      tenantType?: string(name='tenantType', description='The tenant type. Valid values:
- user: individual.
- org: enterprise.
- group: group.', example='normal'),
    }
  ](name='tenantList', description='The tenant list.'),
  tenantName?: string(name='tenantName', description='The current tenant name.', example='string_value'),
  tenantType?: string(name='tenantType', description='The tenant type. Valid values:
- user: individual.
- org: enterprise.
- group: group.', example='normal'),
  userCode?: string(name='userCode', description='The user code.', example='string_value'),
  userId?: long(name='userId', description='The user ID.', example='1'),
}

model GetUserInfoResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetUserInfoResponseBody(name='body'),
}

/**
 * @summary Retrieves the complete information of the authenticated user through OpenAPI, including basic information and tenant list.
 *
 * @description ## Request description
 * - This operation returns the detailed information of the current authenticated user.
 * - If the tenant information is invalid, the corresponding error message is returned.
 * - `tenantId` is an optional parameter. If not provided, the default tenant ID of the caller is used.
 * - Multiple authentication methods are supported: AK, BearerToken, and APP authentication.
 * - The returned data includes the user profile (such as username and profile picture URL), role preference settings, and details of all tenants to which the user belongs.
 * - If the current logon tenant is the system tenant (that is, `tenantId=10000`), this is explicitly indicated in the response.
 *
 * @param request GetUserInfoRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetUserInfoResponse
 */
async function getUserInfoWithOptions(request: GetUserInfoRequest, headers: map[string]string, runtime: Util.RuntimeOptions): GetUserInfoResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'GetUserInfo',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/getUserInfo`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves the complete information of the authenticated user through OpenAPI, including basic information and tenant list.
 *
 * @description ## Request description
 * - This operation returns the detailed information of the current authenticated user.
 * - If the tenant information is invalid, the corresponding error message is returned.
 * - `tenantId` is an optional parameter. If not provided, the default tenant ID of the caller is used.
 * - Multiple authentication methods are supported: AK, BearerToken, and APP authentication.
 * - The returned data includes the user profile (such as username and profile picture URL), role preference settings, and details of all tenants to which the user belongs.
 * - If the current logon tenant is the system tenant (that is, `tenantId=10000`), this is explicitly indicated in the response.
 *
 * @param request GetUserInfoRequest
 * @return GetUserInfoResponse
 */
async function getUserInfo(request: GetUserInfoRequest): GetUserInfoResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return getUserInfoWithOptions(request, headers, runtime);
}

model GrantAgentUsersRequest {
  expireDate?: long(name='expireDate', description='The authorization expiration timestamp in milliseconds. If this parameter is not specified, the authorization never expires.', example='1'),
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital human.

This parameter is required.', example='string_value'),
  permissions?: [ string ](name='permissions', description='The permission items.', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID.', example='676577544219585'),
  userGroupIds?: [ string ](name='userGroupIds', description='The list of user group IDs.', example='string_value'),
  userIds?: [ string ](name='userIds', description='The list of user IDs to be authorized.', example='1'),
}

model GrantAgentUsersShrinkRequest {
  expireDate?: long(name='expireDate', description='The authorization expiration timestamp in milliseconds. If this parameter is not specified, the authorization never expires.', example='1'),
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital human.

This parameter is required.', example='string_value'),
  permissionsShrink?: string(name='permissions', description='The permission items.', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID.', example='676577544219585'),
  userGroupIdsShrink?: string(name='userGroupIds', description='The list of user group IDs.', example='string_value'),
  userIdsShrink?: string(name='userIds', description='The list of user IDs to be authorized.', example='1'),
}

model GrantAgentUsersResponseBody = {
  code?: string(name='code', description='The error code.', example='200'),
  grantedCount?: long(name='grantedCount', description='The number of authorization records processed in this request, including both newly created and updated records.', example='1'),
  message?: string(name='message', description='The description of the status code.', example='successful'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
}

model GrantAgentUsersResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GrantAgentUsersResponseBody(name='body'),
}

/**
 * @summary Grants authorization to authorized users or user groups to use a digital human.
 *
 * @description Grants authorization to authorized users or user groups to use a specified digital human.
 *     Business logic:
 *     1. Constructs an AuthContext from identity.
 *     2. Performs mutual exclusion validation on the request body: specify either userIds or userGroupIds.
 *     3. Delegates to AgentAuthorizationAuthorizedService.grant_authorization to execute.
 *     4. Pre-validation: verifies MANAGE permission and agent existence (performed at the AuthorizedService layer, which performs authentication first before it exposes existence).
 *     5. Existing authorization records are updated (expire_date / permissions).
 *
 * @param tmpReq GrantAgentUsersRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GrantAgentUsersResponse
 */
async function grantAgentUsersWithOptions(tmpReq: GrantAgentUsersRequest, headers: map[string]string, runtime: Util.RuntimeOptions): GrantAgentUsersResponse {
  Util.validateModel(tmpReq);
  var request = new GrantAgentUsersShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!Util.isUnset(tmpReq.permissions)) {
    request.permissionsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.permissions, 'permissions', 'json');
  }
  if (!Util.isUnset(tmpReq.userGroupIds)) {
    request.userGroupIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.userGroupIds, 'userGroupIds', 'json');
  }
  if (!Util.isUnset(tmpReq.userIds)) {
    request.userIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.userIds, 'userIds', 'json');
  }
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.expireDate)) {
    body['expireDate'] = request.expireDate;
  }
  if (!Util.isUnset(request.operatingObjectName)) {
    body['operatingObjectName'] = request.operatingObjectName;
  }
  if (!Util.isUnset(request.permissionsShrink)) {
    body['permissions'] = request.permissionsShrink;
  }
  if (!Util.isUnset(request.userGroupIdsShrink)) {
    body['userGroupIds'] = request.userGroupIdsShrink;
  }
  if (!Util.isUnset(request.userIdsShrink)) {
    body['userIds'] = request.userIdsShrink;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'GrantAgentUsers',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/grantAgentUsers`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Grants authorization to authorized users or user groups to use a digital human.
 *
 * @description Grants authorization to authorized users or user groups to use a specified digital human.
 *     Business logic:
 *     1. Constructs an AuthContext from identity.
 *     2. Performs mutual exclusion validation on the request body: specify either userIds or userGroupIds.
 *     3. Delegates to AgentAuthorizationAuthorizedService.grant_authorization to execute.
 *     4. Pre-validation: verifies MANAGE permission and agent existence (performed at the AuthorizedService layer, which performs authentication first before it exposes existence).
 *     5. Existing authorization records are updated (expire_date / permissions).
 *
 * @param request GrantAgentUsersRequest
 * @return GrantAgentUsersResponse
 */
async function grantAgentUsers(request: GrantAgentUsersRequest): GrantAgentUsersResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return grantAgentUsersWithOptions(request, headers, runtime);
}

model ListAdminKnowledgeBasesRequest {
  directoryId?: string(name='directoryId', description='The directory ID.', example='exampleDirectoryId'),
  keyword?: string(name='keyword', description='The keyword for fuzzy search on form component data.', example='SampleKeyword'),
  page?: long(name='page', description='The page number. Default value: 1. Pages start from page 1.', example='1'),
  pageSize?: long(name='pageSize', description='The number of entries per page.', example='20'),
  sortField?: string(name='sortField', description='The field by which the results are sorted. Valid values:

- event_time: the event creation time.
- event_execute_start_time: the event execution time.
- event_execute_finish_time: the event completion time.', example='name'),
  sortOrder?: string(name='sortOrder', description='The sort order. This parameter takes effect only when sortBy is specified. Valid values: ASC, DESC (case-insensitive).', example='asc'),
  sourceTypes?: [ string ](name='sourceTypes', description='The list of source types.', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID to which the task belongs.', example='10000'),
}

model ListAdminKnowledgeBasesShrinkRequest {
  directoryId?: string(name='directoryId', description='The directory ID.', example='exampleDirectoryId'),
  keyword?: string(name='keyword', description='The keyword for fuzzy search on form component data.', example='SampleKeyword'),
  page?: long(name='page', description='The page number. Default value: 1. Pages start from page 1.', example='1'),
  pageSize?: long(name='pageSize', description='The number of entries per page.', example='20'),
  sortField?: string(name='sortField', description='The field by which the results are sorted. Valid values:

- event_time: the event creation time.
- event_execute_start_time: the event execution time.
- event_execute_finish_time: the event completion time.', example='name'),
  sortOrder?: string(name='sortOrder', description='The sort order. This parameter takes effect only when sortBy is specified. Valid values: ASC, DESC (case-insensitive).', example='asc'),
  sourceTypesShrink?: string(name='sourceTypes', description='The list of source types.', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID to which the task belongs.', example='10000'),
}

model ListAdminKnowledgeBasesResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  items?: [ 
    {
      creatorName?: string(name='creatorName', description='The name of the creator.', example='string_value'),
      description?: string(name='description', description='The description of the to-do card type.', example='Sample description'),
      directoryKind?: string(name='directoryKind', description='The directory type.', example='string_value'),
      gmtCreate?: long(name='gmtCreate', description='The creation time.', example='1'),
      gmtModified?: long(name='gmtModified', description='The last modification time.', example='1'),
      itemId?: string(name='itemId', description='The ID of the data item. When tabId and orgId are the same, itemId uniquely identifies a data item. The maximum length is 128 characters.', example='exampleItemId'),
      itemType?: string(name='itemType', description='The data type (group, user, or role).', example='string_value'),
      name?: string(name='name', description='The name.', example='SampleName.pdf'),
      objectBindings?: [ 
        {
          graphName?: string(name='graphName', description='The semantic graph name to which the object belongs. The object_id is unique within this graph.', example='string_value'),
          objectId?: string(name='objectId', description='The ID of the recommended item, which can be a feedId or a micro-application ID.', example='exampleObjectId'),
          objectName?: string(name='objectName', description='The object name.', example='string_value'),
          objectType?: string(name='objectType', description='The object type, such as customer. This field has a value only when type is mention.', example='string_value'),
          objectTypeName?: string(name='objectTypeName', description='The display name of the object type (such as "Customer"), parsed from the graph schema. The value is null when the cache is missed.', example='string_value'),
        }
      ](name='objectBindings', description='The object bindings.'),
      sourceFailedCount?: long(name='sourceFailedCount', description='The number of resources with FAILED status. This field is returned only for the top-level knowledge base directory list.', example='1'),
      sourceKind?: string(name='sourceKind', description='The knowledge base ownership type. Valid values: aliding_kb_doc (DingTalk knowledge base document) and normal (common knowledge).', example='string_value'),
      sourceReadyCount?: long(name='sourceReadyCount', description='The number of resources with READY status. This field is returned only for the top-level knowledge base directory list.', example='1'),
      sourceStatus?: string(name='sourceStatus', description='The resource status. This field has a value only when itemType is resource.', example='string_value'),
      sourceTotalCount?: long(name='sourceTotalCount', description='The total number of resources in the directory and its subdirectories. This field is returned only for the top-level knowledge base directory list.', example='1'),
      sourceType?: string(name='sourceType', description='The source type.', example='string_value'),
    }
  ](name='items', description='The list of MCP cards.'),
  message?: string(name='message', description='The status code description.', example='ok'),
  page?: long(name='page', description='The page number. Default value: 1.', example='1'),
  pageSize?: long(name='pageSize', description='The page size.', example='20'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  total?: long(name='total', description='The maximum number of records returned in this request.', example='1'),
}

model ListAdminKnowledgeBasesResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListAdminKnowledgeBasesResponseBody(name='body'),
}

/**
 * @summary Queries or drills down into the enterprise knowledge base list of a tenant.
 *
 * @description ## Operation description
 * - This API supports two modes: when `directoryId` is empty or set to \\"root\\", the top-level knowledge base list is returned. When `directoryId` has a specific value, a drill-down operation is performed to return subdirectories and resources under the specified directory.
 * - `tenantId` is a common parameter. If not provided, the caller\\"s tenant ID is used by default.
 * - In drill-down mode (when `directoryId` is not empty), use the `sourceTypes` parameter to filter resources by specific types.
 * - The sort field (`sortField`) and sort order (`sortOrder`) can be customized. Invalid values are reset to default settings.
 * - The search feature is only effective when retrieving the top-level list and supports only fuzzy matching on names or descriptions.
 * - For security purposes, `tenant_id` is strictly obtained from the authenticated identity and cannot be passed through the request body.
 *
 * @param tmpReq ListAdminKnowledgeBasesRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListAdminKnowledgeBasesResponse
 */
async function listAdminKnowledgeBasesWithOptions(tmpReq: ListAdminKnowledgeBasesRequest, headers: map[string]string, runtime: Util.RuntimeOptions): ListAdminKnowledgeBasesResponse {
  Util.validateModel(tmpReq);
  var request = new ListAdminKnowledgeBasesShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!Util.isUnset(tmpReq.sourceTypes)) {
    request.sourceTypesShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.sourceTypes, 'sourceTypes', 'json');
  }
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.directoryId)) {
    body['directoryId'] = request.directoryId;
  }
  if (!Util.isUnset(request.keyword)) {
    body['keyword'] = request.keyword;
  }
  if (!Util.isUnset(request.page)) {
    body['page'] = request.page;
  }
  if (!Util.isUnset(request.pageSize)) {
    body['pageSize'] = request.pageSize;
  }
  if (!Util.isUnset(request.sortField)) {
    body['sortField'] = request.sortField;
  }
  if (!Util.isUnset(request.sortOrder)) {
    body['sortOrder'] = request.sortOrder;
  }
  if (!Util.isUnset(request.sourceTypesShrink)) {
    body['sourceTypes'] = request.sourceTypesShrink;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'ListAdminKnowledgeBases',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/listAdminKnowledgeBases`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries or drills down into the enterprise knowledge base list of a tenant.
 *
 * @description ## Operation description
 * - This API supports two modes: when `directoryId` is empty or set to \\"root\\", the top-level knowledge base list is returned. When `directoryId` has a specific value, a drill-down operation is performed to return subdirectories and resources under the specified directory.
 * - `tenantId` is a common parameter. If not provided, the caller\\"s tenant ID is used by default.
 * - In drill-down mode (when `directoryId` is not empty), use the `sourceTypes` parameter to filter resources by specific types.
 * - The sort field (`sortField`) and sort order (`sortOrder`) can be customized. Invalid values are reset to default settings.
 * - The search feature is only effective when retrieving the top-level list and supports only fuzzy matching on names or descriptions.
 * - For security purposes, `tenant_id` is strictly obtained from the authenticated identity and cannot be passed through the request body.
 *
 * @param request ListAdminKnowledgeBasesRequest
 * @return ListAdminKnowledgeBasesResponse
 */
async function listAdminKnowledgeBases(request: ListAdminKnowledgeBasesRequest): ListAdminKnowledgeBasesResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return listAdminKnowledgeBasesWithOptions(request, headers, runtime);
}

model ListAgentsRequest {
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. If not specified, the default tenant of the caller is used.', example='10000'),
}

model ListAgentsResponseBody = {
  code?: string(name='code', description='The response status code.', example='200'),
  items?: [ 
    {
      authMode?: string(name='authMode', description='The authentication mode.', example='string_value'),
      displayName?: string(name='displayName', description='The display name of the tool.', example='string_value'),
      isActive?: boolean(name='isActive', description='Indicates whether the account is activated.', example='true'),
      operatingObjectName?: string(name='operatingObjectName', description='The name of the digital employee.', example='string_value'),
    }
  ](name='items', description='The list of skill cards.'),
  message?: string(name='message', description='The prompt message.', example='ok'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
}

model ListAgentsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListAgentsResponseBody(name='body'),
}

/**
 * @summary Queries the full list of digital employees for a tenant.
 *
 * @description Queries the full list of digital employees under a tenant, including deactivated ones.
 *     Business logic:
 *     1. Constructs AuthContext from identity.
 *     2. Delegates to AgentAuthorizationAuthorizedService.list_agents to complete permission verification (APPLICATION_AGENT_VIEW).
 *     3. Returns rich fields for all digital employees of the tenant (operatingObjectName / displayName / authMode / isActive).
 *     4. System-level tokens are automatically allowed through ctx.skip_permission.
 *     Difference from listAuthorizedAgents: This operation returns all digital employees of the tenant (including deactivated ones, without authorization filtering) and includes rich fields such as displayName and isActive for management console display.
 *
 * @param request ListAgentsRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListAgentsResponse
 */
async function listAgentsWithOptions(request: ListAgentsRequest, headers: map[string]string, runtime: Util.RuntimeOptions): ListAgentsResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'ListAgents',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/listAgents`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the full list of digital employees for a tenant.
 *
 * @description Queries the full list of digital employees under a tenant, including deactivated ones.
 *     Business logic:
 *     1. Constructs AuthContext from identity.
 *     2. Delegates to AgentAuthorizationAuthorizedService.list_agents to complete permission verification (APPLICATION_AGENT_VIEW).
 *     3. Returns rich fields for all digital employees of the tenant (operatingObjectName / displayName / authMode / isActive).
 *     4. System-level tokens are automatically allowed through ctx.skip_permission.
 *     Difference from listAuthorizedAgents: This operation returns all digital employees of the tenant (including deactivated ones, without authorization filtering) and includes rich fields such as displayName and isActive for management console display.
 *
 * @param request ListAgentsRequest
 * @return ListAgentsResponse
 */
async function listAgents(request: ListAgentsRequest): ListAgentsResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return listAgentsWithOptions(request, headers, runtime);
}

model ListAuthorizedAgentsRequest {
  permission?: string(name='permission', description='The userId of the responsible user.', example='USE'),
  targetUserId?: long(name='targetUserId', description='The target user ID.', example='1'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. If not specified, the default tenant of the caller is used.', example='10000'),
}

model ListAuthorizedAgentsResponseBody = {
  agentNames?: [ string ](name='agentNames', description='The agent names.', example='string_value'),
  code?: string(name='code', description='The status code.', example='200'),
  message?: string(name='message', description='The description of the status code.', example='ok'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
}

model ListAuthorizedAgentsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListAuthorizedAgentsResponseBody(name='body'),
}

/**
 * @summary Queries the list of digital human names for which the caller has specified permissions.
 *
 * @description Queries the list of digital human names for which the current caller (or a specified target user) has specified permissions (USE/MANAGE).
 *     Business logic:
 *     1. Constructs an AuthContext from the identity.
 *     2. Delegates to AgentAuthorizationAuthorizedService.list_authorized_agents to execute the query.
 *     3. When skip_permission=True, returns all active agents for the tenant.
 *     4. Regular users are filtered based on authorization records and auth_mode.
 *     5. When targetUserId is specified (querying on behalf of another user), the APPLICATION_AGENT_VIEW gate is required, and the query is restricted to the current tenant. If the target user is not a member of the current tenant, a USER_NOT_IN_TENANT error is thrown (an empty list is not silently returned).
 *
 * @param request ListAuthorizedAgentsRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListAuthorizedAgentsResponse
 */
async function listAuthorizedAgentsWithOptions(request: ListAuthorizedAgentsRequest, headers: map[string]string, runtime: Util.RuntimeOptions): ListAuthorizedAgentsResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.permission)) {
    body['permission'] = request.permission;
  }
  if (!Util.isUnset(request.targetUserId)) {
    body['targetUserId'] = request.targetUserId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'ListAuthorizedAgents',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/listAuthorizedAgents`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the list of digital human names for which the caller has specified permissions.
 *
 * @description Queries the list of digital human names for which the current caller (or a specified target user) has specified permissions (USE/MANAGE).
 *     Business logic:
 *     1. Constructs an AuthContext from the identity.
 *     2. Delegates to AgentAuthorizationAuthorizedService.list_authorized_agents to execute the query.
 *     3. When skip_permission=True, returns all active agents for the tenant.
 *     4. Regular users are filtered based on authorization records and auth_mode.
 *     5. When targetUserId is specified (querying on behalf of another user), the APPLICATION_AGENT_VIEW gate is required, and the query is restricted to the current tenant. If the target user is not a member of the current tenant, a USER_NOT_IN_TENANT error is thrown (an empty list is not silently returned).
 *
 * @param request ListAuthorizedAgentsRequest
 * @return ListAuthorizedAgentsResponse
 */
async function listAuthorizedAgents(request: ListAuthorizedAgentsRequest): ListAuthorizedAgentsResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return listAuthorizedAgentsWithOptions(request, headers, runtime);
}

model ListAuthorizedUsersRequest {
  granteeType?: string(name='granteeType', description='The filter type. Valid values: USER, USER_GROUP. If not specified, all types are returned.', example='USER'),
  keyword?: string(name='keyword', description='The search keyword.', example='SampleKeyword'),
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital employee.

This parameter is required.', example='string_value'),
  permission?: string(name='permission', description='Permission', example='USE'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. If not specified, the default tenant of the caller is used.', example='10000'),
}

model ListAuthorizedUsersResponseBody = {
  authMode?: string(name='authMode', description='The authentication mode.', example='string_value'),
  code?: string(name='code', description='The status code.', example='200'),
  items?: [ 
    {
      expireDate?: long(name='expireDate', description='The authorization expiration timestamp in milliseconds. If not specified, the authorization never expires.', example='1'),
      gmtCreate?: string(name='gmtCreate', description='The creation time.', example='string_value'),
      gmtModified?: string(name='gmtModified', description='The last update time.', example='string_value'),
      grantedBy?: long(name='grantedBy', description='The user ID of the person who granted the authorization.', example='1'),
      granteeId?: string(name='granteeId', description='The ID of the authorized object.', example='exampleGranteeId'),
      granteeType?: string(name='granteeType', description='The authorization object type. Valid values: USER, USER_GROUP.', example='string_value'),
      id?: long(name='id', description='The Operation logs ID.', example='1'),
      memberCount?: long(name='memberCount', description='The number of members.', example='1'),
      permissions?: [ string ](name='permissions', description='The permission member type. Valid values:

- **ORG**: Enterprise.

- **DEPT**: Department.

- **TAG**: Custom tag.

- **CONVERSATION**: Conversation.

- **USER**: User.', example='string_value'),
      userGroupId?: string(name='userGroupId', description='The user group ID. This parameter has a value only when granteeType is set to USER_GROUP.', example='exampleUserGroupId'),
      userGroupName?: string(name='userGroupName', description='The user group name.', example='string_value'),
      userId?: long(name='userId', description='The user ID. This parameter has a value only when granteeType is set to USER.', example='1'),
      userName?: string(name='userName', description='The username.', example='string_value'),
    }
  ](name='items', description='The details.'),
  message?: string(name='message', description='The description of the status code.', example='successful'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  total?: long(name='total', description='The total number of records.', example='1'),
}

model ListAuthorizedUsersResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListAuthorizedUsersResponseBody(name='body'),
}

/**
 * @summary Queries the list of authorized users or user groups for a digital employee.
 *
 * @description Queries the list of authorized users or user groups for a specified digital employee.
 *     Business logic:
 *     1. Constructs an AuthContext from the identity.
 *     2. Delegates to AgentAuthorizationAuthorizedService.list_authorized_users to execute the query.
 *     3. Permission verification is performed at the AuthorizedService layer by @require_permission(APPLICATION_AGENT_VIEW).
 *     4. When auth_mode=ALL_USERS, only records with MANAGE permissions are displayed.
 *
 * @param request ListAuthorizedUsersRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListAuthorizedUsersResponse
 */
async function listAuthorizedUsersWithOptions(request: ListAuthorizedUsersRequest, headers: map[string]string, runtime: Util.RuntimeOptions): ListAuthorizedUsersResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.granteeType)) {
    body['granteeType'] = request.granteeType;
  }
  if (!Util.isUnset(request.keyword)) {
    body['keyword'] = request.keyword;
  }
  if (!Util.isUnset(request.operatingObjectName)) {
    body['operatingObjectName'] = request.operatingObjectName;
  }
  if (!Util.isUnset(request.permission)) {
    body['permission'] = request.permission;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'ListAuthorizedUsers',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/listAuthorizedUsers`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the list of authorized users or user groups for a digital employee.
 *
 * @description Queries the list of authorized users or user groups for a specified digital employee.
 *     Business logic:
 *     1. Constructs an AuthContext from the identity.
 *     2. Delegates to AgentAuthorizationAuthorizedService.list_authorized_users to execute the query.
 *     3. Permission verification is performed at the AuthorizedService layer by @require_permission(APPLICATION_AGENT_VIEW).
 *     4. When auth_mode=ALL_USERS, only records with MANAGE permissions are displayed.
 *
 * @param request ListAuthorizedUsersRequest
 * @return ListAuthorizedUsersResponse
 */
async function listAuthorizedUsers(request: ListAuthorizedUsersRequest): ListAuthorizedUsersResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return listAuthorizedUsersWithOptions(request, headers, runtime);
}

model ListAvailableConfigsRequest {
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. Pass it explicitly through --tenant-id in winnexo-cli.', example='10000'),
}

model ListAvailableConfigsResponseBody = {
  code?: string(name='code', description='The error code.', example='200'),
  configs?: [ 
    {
      corpId?: string(name='corpId', description='The enterprise ID.', example='exampleCorpId'),
      corpName?: string(name='corpName', description='The organization name.', example='string_value'),
      platformType?: string(name='platformType', description='The platform type.', example='string_value'),
      ssoSettingsId?: string(name='ssoSettingsId', description='The SSO configuration ID. This field has a value only for SAML, OAuth2, or WeCom types. The value is null for custom types.', example='exampleSsoSettingsId'),
      ssoSettingsName?: string(name='ssoSettingsName', description='The SSO configuration name. This field has a value only for SAML, OAuth2, or WeCom types. The value is null for custom types.', example='string_value'),
    }
  ](name='configs', description='The list of queried Logtail configurations.'),
  message?: string(name='message', description='The status code description.', example='ok'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
}

model ListAvailableConfigsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListAvailableConfigsResponseBody(name='body'),
}

/**
 * @summary Enumerates available organization synchronization configurations.
 *
 * @description Enumerates all available organization synchronization configurations under the current tenant.
 *     Returns a unified configs list covering four platform types:
 *     - **wecom**: Retrieves active WeCom SSO configurations from SsoProviderRegistry.
 *     - **saml**: Retrieves active SAML SSO configurations from SsoProviderRegistry. The corpId is set to idpEntityId.
 *     - **oauth2**: Retrieves active OAuth2 SSO configurations from SsoProviderRegistry. The corpId is set to clientId.
 *     - **custom**: Queries the database for pure custom organizations registered under the tenant.
 *     The client distinguishes processing logic based on the returned platformType. The corpId is a required parameter for subsequent synchronization operations.
 *
 * @param request ListAvailableConfigsRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListAvailableConfigsResponse
 */
async function listAvailableConfigsWithOptions(request: ListAvailableConfigsRequest, headers: map[string]string, runtime: Util.RuntimeOptions): ListAvailableConfigsResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'ListAvailableConfigs',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/listAvailableConfigs`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Enumerates available organization synchronization configurations.
 *
 * @description Enumerates all available organization synchronization configurations under the current tenant.
 *     Returns a unified configs list covering four platform types:
 *     - **wecom**: Retrieves active WeCom SSO configurations from SsoProviderRegistry.
 *     - **saml**: Retrieves active SAML SSO configurations from SsoProviderRegistry. The corpId is set to idpEntityId.
 *     - **oauth2**: Retrieves active OAuth2 SSO configurations from SsoProviderRegistry. The corpId is set to clientId.
 *     - **custom**: Queries the database for pure custom organizations registered under the tenant.
 *     The client distinguishes processing logic based on the returned platformType. The corpId is a required parameter for subsequent synchronization operations.
 *
 * @param request ListAvailableConfigsRequest
 * @return ListAvailableConfigsResponse
 */
async function listAvailableConfigs(request: ListAvailableConfigsRequest): ListAvailableConfigsResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return listAvailableConfigsWithOptions(request, headers, runtime);
}

model ListBillingRequest {
  bizId?: string(name='bizId', description='The unique business identifier. When bizType is set to LibraryChat, bizId refers to the document library ID.', example='exampleBizId'),
  bizType?: string(name='bizType', description='The business type. Currently supported values: model Q&A (LlmChat) and document library Q&A (LibraryChat).', example='string_value'),
  endTime?: string(name='endTime', description='The actual end timestamp of the live stream, in milliseconds.', example='2023-10-01T12:00:00Z'),
  ignoreZero?: boolean(name='ignoreZero', description='Specifies whether to filter out bills with zero credit consumption. Default value: true (filtered).', example='true'),
  operation?: string(name='operation', description='The operation type. Valid values:

- start: indicates node creation. This is the default value and does not need to be explicitly set in most cases.
- stop: stops a real-time meeting task. This corresponds to the creation of a real-time meeting. Set this to stop after the meeting ends to trigger the call. This is used in real-time meeting scenarios.

Note: When ending a real-time recording, you must set this parameter to stop.', example='string_value'),
  page?: long(name='page', description='The current page number.', example='1'),
  pageSize?: long(name='pageSize', description='The number of entries per page. Default value: 20. Minimum value: 1. Maximum value: 50.', example='20'),
  startTime?: string(name='startTime', description='The query start time. This is a UNIX timestamp in seconds.', example='2023-10-01T12:00:00Z'),
  status?: string(name='status', description='The task status. Running is returned upon submission.', example='READY'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. In winnexo-cli, pass it explicitly with --tenant-id.', example='10000'),
  wnUserId?: string(name='wnUserId', description='The user ID (WINNEXO platform user ID, optional filter).', example='1'),
}

model ListBillingResponseBody = {
  code?: string(name='code', description='The response status code.', example='200'),
  list?: [ 
    {
      billingId?: string(name='billingId', description='The billing ID.', example='exampleBillingId'),
      bizId?: string(name='bizId', description='The file ID.', example='exampleBizId'),
      bizType?: string(name='bizType', description='The business type.', example='string_value'),
      costSource?: [ string ](name='costSource', description='costSource', example='string_value'),
      costSourceDisplayName?: [ string ](name='costSourceDisplayName', description='costSourceDisplayName', example='string_value'),
      endTime?: string(name='endTime', description='The end timestamp, in milliseconds.', example='2023-10-01T12:00:00Z'),
      entryObjectId?: string(name='entryObjectId', description='The entry object ID.', example='exampleEntryObjectId'),
      entryObjectType?: string(name='entryObjectType', description='The entry object type.', example='string_value'),
      isShadow?: boolean(name='isShadow', description='Indicates whether the bill is a shadow bill.', example='true'),
      operation?: string(name='operation', description='The operation type.', example='string_value'),
      operationDisplayName?: string(name='operationDisplayName', description='The display name of the operation.', example='string_value'),
      startTime?: string(name='startTime', description='The start timestamp, in milliseconds.', example='2023-10-01T12:00:00Z'),
      status?: string(name='status', description='The task status. Running is returned upon submission.', example='READY'),
      statusDisplayName?: string(name='statusDisplayName', description='The display name of the status.', example='string_value'),
      tenantId?: long(name='tenantId', description='The tenant ID. This is a common parameter. If not specified, the default tenant of the caller is used.', example='10000'),
      totalCreditCost?: string(name='totalCreditCost', description='The total credit consumption.', example='string_value'),
      wnUserId?: string(name='wnUserId', description='The WINNEXO platform user ID.', example='1'),
    }
  ](name='list', description='The details of the list.'),
  message?: string(name='message', description='The prompt message.', example='successful'),
  page?: long(name='page', description='The page number.', example='1'),
  pageSize?: long(name='pageSize', description='The number of entries per page.', example='20'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  total?: long(name='total', description='The total number of bills.', example='1'),
}

model ListBillingResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListBillingResponseBody(name='body'),
}

/**
 * @summary Queries and filters the bill list through OpenAPI with support for multiple filter conditions.
 *
 * @description ## Operation description
 * - This operation queries the bill list based on specified conditions.
 * - Supports filtering by tenant, user, operation type, status, time range, business source, and other conditions.
 * - Returns bill data in pages. The default page size is 20 records.
 * - You can choose whether to filter out bills with zero credit consumption. By default, such bills are filtered out.
 * - Authentication information (such as AK, BearerToken, or APP authentication) is required in the request.
 *
 * @param request ListBillingRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListBillingResponse
 */
async function listBillingWithOptions(request: ListBillingRequest, headers: map[string]string, runtime: Util.RuntimeOptions): ListBillingResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.bizId)) {
    body['bizId'] = request.bizId;
  }
  if (!Util.isUnset(request.bizType)) {
    body['bizType'] = request.bizType;
  }
  if (!Util.isUnset(request.endTime)) {
    body['endTime'] = request.endTime;
  }
  if (!Util.isUnset(request.ignoreZero)) {
    body['ignoreZero'] = request.ignoreZero;
  }
  if (!Util.isUnset(request.operation)) {
    body['operation'] = request.operation;
  }
  if (!Util.isUnset(request.page)) {
    body['page'] = request.page;
  }
  if (!Util.isUnset(request.pageSize)) {
    body['pageSize'] = request.pageSize;
  }
  if (!Util.isUnset(request.startTime)) {
    body['startTime'] = request.startTime;
  }
  if (!Util.isUnset(request.status)) {
    body['status'] = request.status;
  }
  if (!Util.isUnset(request.wnUserId)) {
    body['wnUserId'] = request.wnUserId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'ListBilling',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/listBilling`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries and filters the bill list through OpenAPI with support for multiple filter conditions.
 *
 * @description ## Operation description
 * - This operation queries the bill list based on specified conditions.
 * - Supports filtering by tenant, user, operation type, status, time range, business source, and other conditions.
 * - Returns bill data in pages. The default page size is 20 records.
 * - You can choose whether to filter out bills with zero credit consumption. By default, such bills are filtered out.
 * - Authentication information (such as AK, BearerToken, or APP authentication) is required in the request.
 *
 * @param request ListBillingRequest
 * @return ListBillingResponse
 */
async function listBilling(request: ListBillingRequest): ListBillingResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return listBillingWithOptions(request, headers, runtime);
}

model ListChatSessionsRequest {
  digitalEmployeeName?: string(name='digitalEmployeeName', description='The list of digital employee names. A single string can be passed for backward compatibility with the legacy format.', example='string_value'),
  keyword?: string(name='keyword', description='The rule name keyword for fuzzy match.', example='SampleKeyword'),
  page?: int32(name='page', description='The page number. Default value: 1. Pages start from page 1.', example='1'),
  pageSize?: int32(name='pageSize', description='The maximum number of data records to read in this request.', example='10'),
  tenantId?: string(name='tenantId', description='The tenant ID to which the task belongs.', example='10000'),
}

model ListChatSessionsResponseBody = {
  code?: string(name='code', description='The error code.', example='200'),
  hasMore?: boolean(name='hasMore', description='Indicates whether there is a next page.', example='true'),
  message?: string(name='message', description='The status code description.', example='ok'),
  page?: int32(name='page', description='The current page number.', example='1'),
  pageSize?: string(name='pageSize', description='The number of entries per page. Default value: 10.', example='20'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  sessions?: [ any ](name='sessions', description='The list of sessions.', example='{}'),
  tenantId?: string(name='tenantId', description='The effective tenant ID.', example='10000'),
  total?: int32(name='total', description='The total number of records.', example='1159'),
}

model ListChatSessionsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListChatSessionsResponseBody(name='body'),
}

/**
 * @summary Lists chat sessions of the current user in reverse chronological order by creation time.
 *
 * @description ## Operation description
 * - This API supports filtering and sorting by multiple parameters, including tenant ID, page size, pagination token, keyword search, digital employee name, and update time range.
 * - By default, results are sorted in descending order by the `UpdatedAt` field.
 * - If an invalid `NextToken` is provided or `PageSize` exceeds the allowed range (1-100), the API returns a 400 error.
 *
 * @param request ListChatSessionsRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListChatSessionsResponse
 */
async function listChatSessionsWithOptions(request: ListChatSessionsRequest, headers: map[string]string, runtime: Util.RuntimeOptions): ListChatSessionsResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.digitalEmployeeName)) {
    query['digitalEmployeeName'] = request.digitalEmployeeName;
  }
  if (!Util.isUnset(request.keyword)) {
    query['keyword'] = request.keyword;
  }
  if (!Util.isUnset(request.page)) {
    query['page'] = request.page;
  }
  if (!Util.isUnset(request.pageSize)) {
    query['pageSize'] = request.pageSize;
  }
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'ListChatSessions',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/listChatSessions`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Lists chat sessions of the current user in reverse chronological order by creation time.
 *
 * @description ## Operation description
 * - This API supports filtering and sorting by multiple parameters, including tenant ID, page size, pagination token, keyword search, digital employee name, and update time range.
 * - By default, results are sorted in descending order by the `UpdatedAt` field.
 * - If an invalid `NextToken` is provided or `PageSize` exceeds the allowed range (1-100), the API returns a 400 error.
 *
 * @param request ListChatSessionsRequest
 * @return ListChatSessionsResponse
 */
async function listChatSessions(request: ListChatSessionsRequest): ListChatSessionsResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return listChatSessionsWithOptions(request, headers, runtime);
}

model ListGraphsRequest {
  tenantId?: string(name='tenantId', description='The ID of the tenant to which the node belongs.

This parameter is required.', example='21577'),
}

model ListGraphsResponseBody = {
  code?: string(name='code', description='The response status code.', example='200'),
  items?: [ 
    {
      businessProfile?: string(name='businessProfile', description='The business description of the knowledge graph. An empty string is returned if not configured.

This parameter is required.', example='string_value'),
      displayName?: string(name='displayName', description='The tool display name.

This parameter is required.', example='string_value'),
      graphName?: string(name='graphName', description='The knowledge graph name.

This parameter is required.', example='string_value'),
      isDefault?: boolean(name='isDefault', description='Indicates whether this is the default group.

This parameter is required.', example='true'),
    }
  ](name='items', description='The list of MCP cards.

This parameter is required.'),
  message?: string(name='message', description='The prompt message.', example='successful'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
}

model ListGraphsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListGraphsResponseBody(name='body'),
}

/**
 * @summary Queries the list of knowledge graphs available for semantic queries under a tenant.
 *
 * @description Lists published knowledge graphs under an identity tenant.
 *     CLI mapping: ``winnexo graph list``. ``tenantId`` is a required common parameter and is not included in the request body.
 *     The returned ``graphName`` can be used directly in ``querySemanticKnowledge``. This query is consistent with the existing frontend knowledge graph list and does not apply digital worker permission filtering. Specific semantic queries still verify agent USE permissions.
 *     Database exceptions go directly into unified 5xx error handling and are not disguised as a successful empty list.
 *
 * @param request ListGraphsRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListGraphsResponse
 */
async function listGraphsWithOptions(request: ListGraphsRequest, headers: map[string]string, runtime: Util.RuntimeOptions): ListGraphsResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'ListGraphs',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/listGraphs`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the list of knowledge graphs available for semantic queries under a tenant.
 *
 * @description Lists published knowledge graphs under an identity tenant.
 *     CLI mapping: ``winnexo graph list``. ``tenantId`` is a required common parameter and is not included in the request body.
 *     The returned ``graphName`` can be used directly in ``querySemanticKnowledge``. This query is consistent with the existing frontend knowledge graph list and does not apply digital worker permission filtering. Specific semantic queries still verify agent USE permissions.
 *     Database exceptions go directly into unified 5xx error handling and are not disguised as a successful empty list.
 *
 * @param request ListGraphsRequest
 * @return ListGraphsResponse
 */
async function listGraphs(request: ListGraphsRequest): ListGraphsResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return listGraphsWithOptions(request, headers, runtime);
}

model ListKnowledgeBaseDirectoriesRequest {
  directoryId?: string(name='directoryId', description='The directory ID.', example='exampleDirectoryId'),
  sortField?: string(name='sortField', description='The field by which the results are sorted. Valid values:

- event_time: event creation time
- event_execute_start_time: event execution time
- event_execute_finish_time: event completion time', example='name'),
  sortOrder?: string(name='sortOrder', description='The sort order. This parameter takes effect only when sortBy is specified. Valid values: ASC, DESC (case-insensitive).', example='asc'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. You can pass it explicitly by using --tenant-id in winnexo-cli.', example='10000'),
}

model ListKnowledgeBaseDirectoriesResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  directories?: [ any ](name='directories', description='The directory titles.', example='string_value'),
  message?: string(name='message', description='The description of the status code.', example='ok'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  totalCount?: long(name='totalCount', description='The total number of results.', example='1'),
}

model ListKnowledgeBaseDirectoriesResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListKnowledgeBaseDirectoriesResponseBody(name='body'),
}

/**
 * @summary Queries the category directory tree of an enterprise knowledge base, with support for sorting by a specified field.
 *
 * @description ## Request description
 * - This API retrieves the category list (subdirectory tree) of an enterprise knowledge base. You must have the knowledge base view permission.
 * - If the `directoryId` parameter is not provided, the API returns all category trees under the root directory of the enterprise knowledge base. If `directoryId` is provided, the API returns the subdirectory tree rooted at the specified directory.
 * - You can sort results by using the `sortField` and `sortOrder` parameters. By default, results are sorted by creation time in descending order.
 * - Security constraints: `tenant_id` and `user_id` are derived only from the authenticated identity, and the caller must have the `DEVELOPMENT_KB_VIEW` feature permission.
 *
 * @param request ListKnowledgeBaseDirectoriesRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListKnowledgeBaseDirectoriesResponse
 */
async function listKnowledgeBaseDirectoriesWithOptions(request: ListKnowledgeBaseDirectoriesRequest, headers: map[string]string, runtime: Util.RuntimeOptions): ListKnowledgeBaseDirectoriesResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.directoryId)) {
    body['directoryId'] = request.directoryId;
  }
  if (!Util.isUnset(request.sortField)) {
    body['sortField'] = request.sortField;
  }
  if (!Util.isUnset(request.sortOrder)) {
    body['sortOrder'] = request.sortOrder;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'ListKnowledgeBaseDirectories',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/listKnowledgeBaseDirectories`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the category directory tree of an enterprise knowledge base, with support for sorting by a specified field.
 *
 * @description ## Request description
 * - This API retrieves the category list (subdirectory tree) of an enterprise knowledge base. You must have the knowledge base view permission.
 * - If the `directoryId` parameter is not provided, the API returns all category trees under the root directory of the enterprise knowledge base. If `directoryId` is provided, the API returns the subdirectory tree rooted at the specified directory.
 * - You can sort results by using the `sortField` and `sortOrder` parameters. By default, results are sorted by creation time in descending order.
 * - Security constraints: `tenant_id` and `user_id` are derived only from the authenticated identity, and the caller must have the `DEVELOPMENT_KB_VIEW` feature permission.
 *
 * @param request ListKnowledgeBaseDirectoriesRequest
 * @return ListKnowledgeBaseDirectoriesResponse
 */
async function listKnowledgeBaseDirectories(request: ListKnowledgeBaseDirectoriesRequest): ListKnowledgeBaseDirectoriesResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return listKnowledgeBaseDirectoriesWithOptions(request, headers, runtime);
}

model ListOutputFilesRequest {
  itemType?: string(name='itemType', description='The type of the output item. Valid values: ppt, html, document, picture, slides, video, audio, email, and others.', example='ppt'),
  keyword?: string(name='keyword', description='The keyword for searching. Matches output titles or item names.', example='string_value'),
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital employee (operating object). Used to filter results by name.', example='string_value'),
  page?: long(name='page', description='The page number, starting from 1.', example='1'),
  pageSize?: long(name='pageSize', description='The number of entries per page. Valid values: 1 to 100.', example='20'),
  sharedOnly?: boolean(name='sharedOnly', description='Specifies whether to display only outputs and output items that have sharing enabled.', example='False'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. In winnexo-cli, pass it explicitly with --tenant-id.', example='10000'),
}

model ListOutputFilesResponseBody = {
  code?: string(name='code', description='The response status code.', example='200'),
  items?: [ 
    {
      conversationId?: string(name='conversationId', description='The conversation ID.', example='exampleConversationId'),
      gmtCreate?: string(name='gmtCreate', description='The creation time in ISO 8601 format.', example='string_value'),
      gmtModified?: string(name='gmtModified', description='The update time in ISO 8601 format.', example='string_value'),
      name?: string(name='name', description='The output name.', example='SampleName.pdf'),
      operatingObjectName?: string(name='operatingObjectName', description='The name of the digital employee (operating object).', example='string_value'),
      outputId?: string(name='outputId', description='The output ID.', example='exampleOutputId'),
      outputItems?: [ 
        {
          createTime?: string(name='createTime', description='The creation time in ISO 8601 format.', example='2023-10-01T12:00:00Z'),
          emailInfo?: {
            body?: string(name='body', description='The email body.', example='string_value'),
            contentType?: string(name='contentType', description='The content type, such as MARKDOWN/JSONML/HTML.', example='string_value'),
            recipients?: [ string ](name='recipients', description='The recipient list.', example='string_value'),
            subject?: string(name='subject', description='The email subject.', example='string_value'),
          }(name='emailInfo', description='The email information. This field is present when the output type is email.'),
          fileInfo?: {
            description?: string(name='description', description='The file description.', example='Sample description'),
            name?: string(name='name', description='The file name.', example='SampleName.pdf'),
            path?: string(name='path', description='The file path (OSS object key).', example='https://example.com/oss/file.pdf'),
            type?: string(name='type', description='The file type, such as .pdf or .md.', example='text'),
          }(name='fileInfo', description='The file information. This field is present when the output type is file.'),
          gmtCreate?: string(name='gmtCreate', description='The database creation time in ISO 8601 format.', example='2023-10-01T12:00:00Z'),
          gmtModified?: string(name='gmtModified', description='The database update time in ISO 8601 format.', example='2023-10-01T12:00:00Z'),
          itemName?: string(name='itemName', description='The output name.', example='exampleItemName'),
          itemType?: string(name='itemType', description='The type of the output item. Valid values: ppt, html, document, picture, slides, video, audio, email, and others.', example='ppt'),
          itemTypeDisplayName?: string(name='itemTypeDisplayName', description='The internationalized display name of the output detail type.', example='string_value'),
          messageId?: string(name='messageId', description='The message ID.', example='exampleMessageId'),
          outputItemId?: string(name='outputItemId', description='The output detail ID.', example='string_example_value'),
          shareEnabled?: boolean(name='shareEnabled', description='Indicates whether sharing is enabled.', example='true'),
          shareToken?: string(name='shareToken', description='The share token, which is present when sharing is enabled. You can use this token to access the public share preview API.', example='example_share_token'),
          skillOutputId?: string(name='skillOutputId', description='The skill output ID.', example='exampleSkillOutputId'),
          slidesInfo?: {
            completedSlides?: long(name='completedSlides', description='The number of completed slides.', example='1'),
            pptId?: string(name='pptId', description='PPT ID', example='examplePptId'),
            pptName?: string(name='pptName', description='The PPT name.', example='string_value'),
            totalSlides?: long(name='totalSlides', description='The total number of slides.', example='1'),
          }(name='slidesInfo', description='The slides information. This field is present when the output type is slides.'),
          taskExecutionId?: string(name='taskExecutionId', description='The task execution ID.', example='exampleTaskExecutionId'),
        }
      ](name='outputItems', description='The output detail list.'),
      outputType?: string(name='outputType', description='The output type: `conversation/skill/task`.', example='conversation'),
      outputTypeDisplayName?: string(name='outputTypeDisplayName', description='The internationalized display name of the output type.', example='string_value'),
      skillOutputId?: string(name='skillOutputId', description='The skill output ID.', example='exampleSkillOutputId'),
      taskId?: string(name='taskId', description='The task ID.', example='string_example_value'),
    }
  ](name='items', description='The output list.'),
  message?: string(name='message', description='The prompt message.', example='ok'),
  page?: long(name='page', description='The current page number.', example='1'),
  pageSize?: long(name='pageSize', description='The number of entries per page.', example='20'),
  requestId?: string(name='requestId', description='The request trace ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  total?: long(name='total', description='The total number of outputs that match the specified conditions.', example='1'),
}

model ListOutputFilesResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListOutputFilesResponseBody(name='body'),
}

/**
 * @summary Queries the output list of the current user with support for conditional filtering and pagination.
 *
 * @description ## Operation description
 * - This API operation queries the output list of the current logon user.
 * - `tenantId` is a common parameter. If this parameter is not specified, the default tenant of the caller is used.
 * - You can filter results by using parameters such as `operatingObjectName`, `itemType`, and `keyword`.
 * - Set `sharedOnly` to `true` to display only shared outputs.
 * - Pagination is controlled by `page` (page number) and `pageSize` (number of entries per page). By default, the first page is returned with 20 records per page.
 * - Results are sorted by update time in descending order by default.
 * - The `tenant_id` or `user_id` values passed in the request body are ignored. This information is obtained only from the authenticated identity.
 *
 * @param request ListOutputFilesRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListOutputFilesResponse
 */
async function listOutputFilesWithOptions(request: ListOutputFilesRequest, headers: map[string]string, runtime: Util.RuntimeOptions): ListOutputFilesResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.itemType)) {
    body['itemType'] = request.itemType;
  }
  if (!Util.isUnset(request.keyword)) {
    body['keyword'] = request.keyword;
  }
  if (!Util.isUnset(request.operatingObjectName)) {
    body['operatingObjectName'] = request.operatingObjectName;
  }
  if (!Util.isUnset(request.page)) {
    body['page'] = request.page;
  }
  if (!Util.isUnset(request.pageSize)) {
    body['pageSize'] = request.pageSize;
  }
  if (!Util.isUnset(request.sharedOnly)) {
    body['sharedOnly'] = request.sharedOnly;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'ListOutputFiles',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/listOutputFiles`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the output list of the current user with support for conditional filtering and pagination.
 *
 * @description ## Operation description
 * - This API operation queries the output list of the current logon user.
 * - `tenantId` is a common parameter. If this parameter is not specified, the default tenant of the caller is used.
 * - You can filter results by using parameters such as `operatingObjectName`, `itemType`, and `keyword`.
 * - Set `sharedOnly` to `true` to display only shared outputs.
 * - Pagination is controlled by `page` (page number) and `pageSize` (number of entries per page). By default, the first page is returned with 20 records per page.
 * - Results are sorted by update time in descending order by default.
 * - The `tenant_id` or `user_id` values passed in the request body are ignored. This information is obtained only from the authenticated identity.
 *
 * @param request ListOutputFilesRequest
 * @return ListOutputFilesResponse
 */
async function listOutputFiles(request: ListOutputFilesRequest): ListOutputFilesResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return listOutputFilesWithOptions(request, headers, runtime);
}

model ListPersonalDirectoryContentsRequest {
  directoryId?: string(name='directoryId', description='The directory ID.

This parameter is required.', example='exampleDirectoryId'),
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital employee.', example='string_value'),
  page?: long(name='page', description='The page number. Default value: 1. Pages start from page 1.', example='1'),
  pageSize?: long(name='pageSize', description='The number of entries per page.', example='20'),
  sortField?: string(name='sortField', description='The field by which the results are sorted. Valid values:

- event_time: event creation time
- event_execute_start_time: event execution time
- event_execute_finish_time: event completion time', example='name'),
  sortOrder?: string(name='sortOrder', description='The sort order. This parameter takes effect when sortBy is specified. Valid values: ASC, DESC (case-insensitive).', example='asc'),
  sourceTypes?: [ string ](name='sourceTypes', description='The list of service source types.', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. If not specified, the default tenant of the caller is used.', example='10000'),
}

model ListPersonalDirectoryContentsShrinkRequest {
  directoryId?: string(name='directoryId', description='The directory ID.

This parameter is required.', example='exampleDirectoryId'),
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital employee.', example='string_value'),
  page?: long(name='page', description='The page number. Default value: 1. Pages start from page 1.', example='1'),
  pageSize?: long(name='pageSize', description='The number of entries per page.', example='20'),
  sortField?: string(name='sortField', description='The field by which the results are sorted. Valid values:

- event_time: event creation time
- event_execute_start_time: event execution time
- event_execute_finish_time: event completion time', example='name'),
  sortOrder?: string(name='sortOrder', description='The sort order. This parameter takes effect when sortBy is specified. Valid values: ASC, DESC (case-insensitive).', example='asc'),
  sourceTypesShrink?: string(name='sourceTypes', description='The list of service source types.', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. If not specified, the default tenant of the caller is used.', example='10000'),
}

model ListPersonalDirectoryContentsResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  items?: [ 
    {
      creatorName?: string(name='creatorName', description='The name of the creator.', example='string_value'),
      description?: string(name='description', description='The description of the to-do card type.', example='Sample description'),
      directoryKind?: string(name='directoryKind', description='The directory type.', example='string_value'),
      gmtCreate?: long(name='gmtCreate', description='The creation time.', example='1'),
      gmtModified?: long(name='gmtModified', description='The last modification time.', example='1'),
      itemId?: string(name='itemId', description='The signing record ID.', example='exampleItemId'),
      itemType?: string(name='itemType', description='The item type.', example='string_value'),
      name?: string(name='name', description='The name.', example='SampleName.pdf'),
      objectBindings?: [ 
        {
          objectId?: string(name='objectId', description='The bound object ID.', example='exampleObjectId'),
          objectName?: string(name='objectName', description='The object name.', example='string_value'),
          objectType?: string(name='objectType', description='The bound object type, such as customer or project.', example='string_value'),
          objectTypeName?: string(name='objectTypeName', description='The display name of the object type, such as "Customer". This value is resolved from the graph schema. The value is null when the cache is missed.', example='string_value'),
        }
      ](name='objectBindings', description='The object bindings.'),
      sourceFailedCount?: long(name='sourceFailedCount', description='The number of resources in the FAILED state. This field is returned only when the top-level KB directory list is queried.', example='1'),
      sourceKind?: string(name='sourceKind', description='The knowledge base affiliation type. Valid values: aliding_kb_doc (DingTalk knowledge base document), normal (common knowledge).', example='string_value'),
      sourceReadyCount?: long(name='sourceReadyCount', description='The number of resources in the READY state. This field is returned only when the top-level KB directory list is queried.', example='1'),
      sourceStatus?: string(name='sourceStatus', description='The resource status. This field has a value only when itemType is resource.', example='string_value'),
      sourceTotalCount?: long(name='sourceTotalCount', description='The total number of resources under the directory and its subdirectories. This field is returned only when the top-level KB directory list is queried.', example='1'),
      sourceType?: string(name='sourceType', description='The data source type.', example='string_value'),
    }
  ](name='items', description='The file information.'),
  message?: string(name='message', description='The status code description.', example='The current zone list is illegal.'),
  page?: long(name='page', description='The page number.', example='1'),
  pageSize?: long(name='pageSize', description='The number of entries returned per page. Default value: 10.', example='20'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  total?: long(name='total', description='The total number of records.', example='1'),
}

model ListPersonalDirectoryContentsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListPersonalDirectoryContentsResponseBody(name='body'),
}

/**
 * @summary Queries subdirectories and resources under a specified digital employee resource directory.
 *
 * @description ## Operation description
 * - This API is used to drill down and query subdirectories and resources under the "My Resources" directory.
 * - When `directoryId` is set to \\"root\\", the service automatically resolves and returns the content under the current digital employee\\"s default root directory. If a specific directory ID is provided, the subdirectories and resources under that directory are returned.
 * - Security constraint: `tenant_id` and `user_id` can only come from the authenticated identity information. These fields provided by the caller in the request body are ignored.
 * - You can use the `sourceTypes` parameter to filter resources of specific types. When this parameter has a value, only resources that match the type condition are returned, and subdirectories are not included.
 * - Sorting supports ascending or descending order by name (`name`), creation time (`gmt_create`), or modification time (`gmt_modified`).
 * - The pagination feature allows you to customize the number of items displayed per page (maximum 100) and the current page number.
 *
 * @param tmpReq ListPersonalDirectoryContentsRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListPersonalDirectoryContentsResponse
 */
async function listPersonalDirectoryContentsWithOptions(tmpReq: ListPersonalDirectoryContentsRequest, headers: map[string]string, runtime: Util.RuntimeOptions): ListPersonalDirectoryContentsResponse {
  Util.validateModel(tmpReq);
  var request = new ListPersonalDirectoryContentsShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!Util.isUnset(tmpReq.sourceTypes)) {
    request.sourceTypesShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.sourceTypes, 'sourceTypes', 'json');
  }
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.directoryId)) {
    body['directoryId'] = request.directoryId;
  }
  if (!Util.isUnset(request.operatingObjectName)) {
    body['operatingObjectName'] = request.operatingObjectName;
  }
  if (!Util.isUnset(request.page)) {
    body['page'] = request.page;
  }
  if (!Util.isUnset(request.pageSize)) {
    body['pageSize'] = request.pageSize;
  }
  if (!Util.isUnset(request.sortField)) {
    body['sortField'] = request.sortField;
  }
  if (!Util.isUnset(request.sortOrder)) {
    body['sortOrder'] = request.sortOrder;
  }
  if (!Util.isUnset(request.sourceTypesShrink)) {
    body['sourceTypes'] = request.sourceTypesShrink;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'ListPersonalDirectoryContents',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/listPersonalDirectoryContents`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries subdirectories and resources under a specified digital employee resource directory.
 *
 * @description ## Operation description
 * - This API is used to drill down and query subdirectories and resources under the "My Resources" directory.
 * - When `directoryId` is set to \\"root\\", the service automatically resolves and returns the content under the current digital employee\\"s default root directory. If a specific directory ID is provided, the subdirectories and resources under that directory are returned.
 * - Security constraint: `tenant_id` and `user_id` can only come from the authenticated identity information. These fields provided by the caller in the request body are ignored.
 * - You can use the `sourceTypes` parameter to filter resources of specific types. When this parameter has a value, only resources that match the type condition are returned, and subdirectories are not included.
 * - Sorting supports ascending or descending order by name (`name`), creation time (`gmt_create`), or modification time (`gmt_modified`).
 * - The pagination feature allows you to customize the number of items displayed per page (maximum 100) and the current page number.
 *
 * @param request ListPersonalDirectoryContentsRequest
 * @return ListPersonalDirectoryContentsResponse
 */
async function listPersonalDirectoryContents(request: ListPersonalDirectoryContentsRequest): ListPersonalDirectoryContentsResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return listPersonalDirectoryContentsWithOptions(request, headers, runtime);
}

model ListRolesRequest {
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. You can pass it explicitly by using --tenant-id in winnexo-cli.', example='10000'),
}

model ListRolesResponseBody = {
  code?: string(name='code', description='The response status code.', example='200'),
  items?: [ 
    {
      description?: string(name='description', description='The description of the to-do card type.', example='Sample description'),
      roleCode?: string(name='roleCode', description='The role code.', example='string_value'),
      roleName?: string(name='roleName', description='The role name.', example='string_value'),
      toggleable?: boolean(name='toggleable', description='Indicates whether the enable/disable operation is allowed. Super administrators and application users cannot be switched.', example='true'),
    }
  ](name='items', description='The MCP card list.'),
  message?: string(name='message', description='The prompt message.', example='ok'),
  requestId?: string(name='requestId', description='The request trace ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
}

model ListRolesResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListRolesResponseBody(name='body'),
}

/**
 * @summary Queries the list of system built-in roles.
 *
 * @description Queries the list of system built-in roles.
 *     Business logic:
 *     1. Constructs AuthContext from identity.
 *     2. Delegates to UserManagementAuthorizedService.list_system_roles for permission verification (PLATFORM_USER_VIEW).
 *     3. Renders role names and descriptions based on the request Accept-Language header.
 *     4. Returns a fixed set of 7 system built-in roles.
 *     The returned roleCode field can be directly used as the roleCodes parameter for createUser or updateUser.
 *
 * @param request ListRolesRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListRolesResponse
 */
async function listRolesWithOptions(request: ListRolesRequest, headers: map[string]string, runtime: Util.RuntimeOptions): ListRolesResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'ListRoles',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/listRoles`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the list of system built-in roles.
 *
 * @description Queries the list of system built-in roles.
 *     Business logic:
 *     1. Constructs AuthContext from identity.
 *     2. Delegates to UserManagementAuthorizedService.list_system_roles for permission verification (PLATFORM_USER_VIEW).
 *     3. Renders role names and descriptions based on the request Accept-Language header.
 *     4. Returns a fixed set of 7 system built-in roles.
 *     The returned roleCode field can be directly used as the roleCodes parameter for createUser or updateUser.
 *
 * @param request ListRolesRequest
 * @return ListRolesResponse
 */
async function listRoles(request: ListRolesRequest): ListRolesResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return listRolesWithOptions(request, headers, runtime);
}

model ListScheduledTasksRequest {
  collaborationGroupId?: string(name='collaborationGroupId', description='The ID of the collaboration group (such as cg_101). If specified, a group task is created (the caller must be a valid group member). If left empty, a personal task is created.', example='exampleCollaborationGroupId'),
  creatorOnly?: boolean(name='creatorOnly', description='Specifies whether to return only tasks created by the caller. This parameter takes effect only in the group dimension (in the personal dimension, only the caller\\\\"s own tasks are returned). If not specified, no filtering is applied.', example='true'),
  keyword?: string(name='keyword', description='The keyword of the rule name, used for fuzzy match.', example='SampleKeyword'),
  maxResults?: int32(name='maxResults', description='The maximum number of entries returned in this request.', example='string_value'),
  nextToken?: string(name='nextToken', description='The pagination token for the next page.', example='eHiB8vca1XDyBT0cNAmThA=='),
  page?: long(name='page', description='The page number. Default value: 1.', example='1'),
  pageSize?: long(name='pageSize', description='The number of entries per page.

> The maximum number of entries per page is 30.', example='20'),
  tenantId?: string(name='tenantId', description='The tenant ID that takes effect.', example='10000'),
  visibilities?: [ string ](name='visibilities', description='Filters by visibility. Valid values:
- PRIVATE: visible only to the creator and group owner.
- COLLABORATIVE: visible to specified collaborators.
- PUBLIC: visible to all group members.

If not specified or an empty list is passed, no filtering is applied. This parameter takes effect only in the group dimension (when collaborationGroupId is specified) and is ignored in the personal dimension.', example='PRIVATE'),
}

model ListScheduledTasksShrinkRequest {
  collaborationGroupId?: string(name='collaborationGroupId', description='The ID of the collaboration group (such as cg_101). If specified, a group task is created (the caller must be a valid group member). If left empty, a personal task is created.', example='exampleCollaborationGroupId'),
  creatorOnly?: boolean(name='creatorOnly', description='Specifies whether to return only tasks created by the caller. This parameter takes effect only in the group dimension (in the personal dimension, only the caller\\\\"s own tasks are returned). If not specified, no filtering is applied.', example='true'),
  keyword?: string(name='keyword', description='The keyword of the rule name, used for fuzzy match.', example='SampleKeyword'),
  maxResults?: int32(name='maxResults', description='The maximum number of entries returned in this request.', example='string_value'),
  nextToken?: string(name='nextToken', description='The pagination token for the next page.', example='eHiB8vca1XDyBT0cNAmThA=='),
  page?: long(name='page', description='The page number. Default value: 1.', example='1'),
  pageSize?: long(name='pageSize', description='The number of entries per page.

> The maximum number of entries per page is 30.', example='20'),
  tenantId?: string(name='tenantId', description='The tenant ID that takes effect.', example='10000'),
  visibilitiesShrink?: string(name='visibilities', description='Filters by visibility. Valid values:
- PRIVATE: visible only to the creator and group owner.
- COLLABORATIVE: visible to specified collaborators.
- PUBLIC: visible to all group members.

If not specified or an empty list is passed, no filtering is applied. This parameter takes effect only in the group dimension (when collaborationGroupId is specified) and is ignored in the personal dimension.', example='PRIVATE'),
}

model ListScheduledTasksResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  hasMore?: boolean(name='hasMore', description='Indicates whether more data is available. Valid values:
- true: More data is available.
- false: No more data is available.', example='true'),
  items?: [ 
    {
      abnormalReason?: string(name='abnormalReason', description='The reason for the abnormality. This field has a value only when status is abnormal.', example='string_value'),
      canDelete?: boolean(name='canDelete', description='Indicates whether the current caller can delete the task (only the task creator and group owner can do so). Always returns true for personal tasks.', example='true'),
      canEdit?: boolean(name='canEdit', description='Indicates whether the task can be edited or deleted.', example='true'),
      canExecute?: boolean(name='canExecute', description='Indicates whether the current caller can immediately execute the task (anyone with visibility can operate. Returns false for abnormal tasks). Always returns true for personal tasks.', example='true'),
      canToggle?: boolean(name='canToggle', description='Indicates whether the current caller can start or stop the task (only the task creator and group owner can do so. Returns false for abnormal tasks). Always returns true for personal tasks.', example='true'),
      collaborationGroupId?: string(name='collaborationGroupId', description='The ID of the collaboration group (such as cg_101). If specified, a group task is created (the caller must be a valid group member). If left empty, a personal task is created.', example='exampleCollaborationGroupId'),
      creator?: string(name='creator', description='The creator.', example='string_value'),
      creatorName?: string(name='creatorName', description='The creator.', example='string_value'),
      cronExpression?: string(name='cronExpression', description='The cron expression.', example='string_value'),
      description?: string(name='description', description='The description of the to-do card type.', example='Sample description'),
      digitalEmployeeName?: [ string ](name='digitalEmployeeName', description='The list of digital employee names.', example='string_value'),
      executionCount?: long(name='executionCount', description='The cumulative number of executions.', example='1'),
      gmtCreate?: string(name='gmtCreate', description='The creation time.', example='string_value'),
      gmtModified?: string(name='gmtModified', description='The last modification time.', example='string_value'),
      isOpen?: boolean(name='isOpen', description='Indicates whether public access is enabled.', example='true'),
      model?: string(name='model', description='The execution model tier. If not specified, the value is not updated.', example='standard'),
      name?: string(name='name', description='The name.', example='SampleName.pdf'),
      status?: string(name='status', description='The task status. Running is returned upon submission.', example='active'),
      taskId?: string(name='taskId', description='The task ID.', example='exampleTaskId'),
      triggerType?: string(name='triggerType', description='The trigger type.', example='string_value'),
      visibility?: string(name='visibility', description='The visibility of the group task. Valid values:
- PRIVATE: visible only to the creator and group owner.
- COLLABORATIVE: visible to specified collaborators.
- PUBLIC: visible to all group members.

If not specified for a group task, the default value is PRIVATE. This field is ignored for personal tasks.', example='PRIVATE'),
      visibleMemberUserIds?: [ string ](name='visibleMemberUserIds', description='The list of collaborator user IDs (excluding the task creator and group creator, who are covered by the authentication layer). This field is returned only for group tasks. An empty list is returned for PRIVATE or PUBLIC visibility.', example='string_value'),
    }
  ](name='items', description='The list of skill cards.'),
  maxResults?: int32(name='maxResults', description='The maximum number of entries returned in this request.', example='20'),
  message?: string(name='message', description='The status code description.', example='ok'),
  nextToken?: string(name='nextToken', description='The pagination token.', example='1763604514518000_531300'),
  page?: long(name='page', description='The page number. Default value: 1.', example='1'),
  pageSize?: long(name='pageSize', description='The number of entries per page.', example='20'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  total?: long(name='total', description='The total number of tasks.', example='1'),
}

model ListScheduledTasksResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListScheduledTasksResponseBody(name='body'),
}

/**
 * @summary Retrieves a list of scheduled tasks.
 *
 * @description ## Operation description
 * - This operation uploads a file to an enterprise knowledge base.
 * - The `DEVELOPMENT_KB_MANAGE` permission is required to call this API.
 * - You must provide the OSS persistent address (`filePath`) of the file when uploading.
 * - Optional parameters include the public access URL and original file name to enhance the completeness of file information.
 * - If `directoryId` is specified, the file is placed in the corresponding enterprise knowledge base directory. Otherwise, the file is bound to the default root directory of the current digital employee.
 * - You can add tags to the resource by using `sourceTags` for subsequent management and retrieval.
 * - This operation initiates a billing item (UNSTRUCTURED_PARSE). Make sure your account balance is sufficient.
 *
 * @param tmpReq ListScheduledTasksRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListScheduledTasksResponse
 */
async function listScheduledTasksWithOptions(tmpReq: ListScheduledTasksRequest, headers: map[string]string, runtime: Util.RuntimeOptions): ListScheduledTasksResponse {
  Util.validateModel(tmpReq);
  var request = new ListScheduledTasksShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!Util.isUnset(tmpReq.visibilities)) {
    request.visibilitiesShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.visibilities, 'visibilities', 'json');
  }
  var query : map[string]any = {};
  if (!Util.isUnset(request.collaborationGroupId)) {
    query['collaborationGroupId'] = request.collaborationGroupId;
  }
  if (!Util.isUnset(request.creatorOnly)) {
    query['creatorOnly'] = request.creatorOnly;
  }
  if (!Util.isUnset(request.keyword)) {
    query['keyword'] = request.keyword;
  }
  if (!Util.isUnset(request.maxResults)) {
    query['maxResults'] = request.maxResults;
  }
  if (!Util.isUnset(request.nextToken)) {
    query['nextToken'] = request.nextToken;
  }
  if (!Util.isUnset(request.page)) {
    query['page'] = request.page;
  }
  if (!Util.isUnset(request.pageSize)) {
    query['pageSize'] = request.pageSize;
  }
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }
  if (!Util.isUnset(request.visibilitiesShrink)) {
    query['visibilities'] = request.visibilitiesShrink;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'ListScheduledTasks',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/listScheduledTasks`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves a list of scheduled tasks.
 *
 * @description ## Operation description
 * - This operation uploads a file to an enterprise knowledge base.
 * - The `DEVELOPMENT_KB_MANAGE` permission is required to call this API.
 * - You must provide the OSS persistent address (`filePath`) of the file when uploading.
 * - Optional parameters include the public access URL and original file name to enhance the completeness of file information.
 * - If `directoryId` is specified, the file is placed in the corresponding enterprise knowledge base directory. Otherwise, the file is bound to the default root directory of the current digital employee.
 * - You can add tags to the resource by using `sourceTags` for subsequent management and retrieval.
 * - This operation initiates a billing item (UNSTRUCTURED_PARSE). Make sure your account balance is sufficient.
 *
 * @param request ListScheduledTasksRequest
 * @return ListScheduledTasksResponse
 */
async function listScheduledTasks(request: ListScheduledTasksRequest): ListScheduledTasksResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return listScheduledTasksWithOptions(request, headers, runtime);
}

model ListSkillsRequest {
  bindStatus?: string(name='bindStatus', description='The binding status. Valid values: BOUND (bound) and UNBOUND (unbound global skills). Must be specified together with operatingObjectName.', example='BOUND'),
  filterType?: string(name='filterType', description='The filter expression type.

- SQL: SQL-based filtering.
- TAG: Tag-based filtering.', example='ALL'),
  keyword?: string(name='keyword', description='The search keyword. Supports fuzzy search by API name or exact search by API ID.', example='SampleKeyword'),
  operatingObjectName?: string(name='operatingObjectName', description='The digital employee name. Used to calculate the CodeAgent allowedSkills whitelist based on binding relationships.', example='11111'),
  page?: int32(name='page', description='The current page number.', example='string_value'),
  pageSize?: int32(name='pageSize', description='The number of entries per page.', example='10'),
  tags?: [ string ](name='tags', description='The tag filtering parameter.', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. If not specified, the default tenant of the caller is used.', example='10000'),
}

model ListSkillsShrinkRequest {
  bindStatus?: string(name='bindStatus', description='The binding status. Valid values: BOUND (bound) and UNBOUND (unbound global skills). Must be specified together with operatingObjectName.', example='BOUND'),
  filterType?: string(name='filterType', description='The filter expression type.

- SQL: SQL-based filtering.
- TAG: Tag-based filtering.', example='ALL'),
  keyword?: string(name='keyword', description='The search keyword. Supports fuzzy search by API name or exact search by API ID.', example='SampleKeyword'),
  operatingObjectName?: string(name='operatingObjectName', description='The digital employee name. Used to calculate the CodeAgent allowedSkills whitelist based on binding relationships.', example='11111'),
  page?: int32(name='page', description='The current page number.', example='string_value'),
  pageSize?: int32(name='pageSize', description='The number of entries per page.', example='10'),
  tagsShrink?: string(name='tags', description='The tag filtering parameter.', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. If not specified, the default tenant of the caller is used.', example='10000'),
}

model ListSkillsResponseBody = {
  code?: string(name='code', description='The response status code.', example='200'),
  items?: [ 
    {
      createdTime?: string(name='createdTime', description='The creation time.', example='2023-10-01T12:00:00Z'),
      description?: string(name='description', description='The description of the to-do card type.', example='Sample description'),
      executeMode?: string(name='executeMode', description='The execution mode, such as CODE_AGENT or SYSTEM.', example='string_value'),
      globalAccess?: boolean(name='globalAccess', description='Indicates whether the skill is globally accessible.', example='true'),
      hasDraftChanges?: boolean(name='hasDraftChanges', description='Indicates whether unpublished draft modifications exist.', example='true'),
      name?: string(name='name', description='The file name.', example='SampleName.pdf'),
      skillCode?: string(name='skillCode', description='The actual skill code for execution.', example='string_value'),
      skillHubDefinitionId?: long(name='skillHubDefinitionId', description='The skill definition ID.', example='1'),
      sourceType?: string(name='sourceType', description='The data source type. Fixed value: FILE.', example='BUILTIN'),
      status?: string(name='status', description='The task status. Returns Running upon submission.', example='READY'),
      tags?: [ string ](name='tags', description='The keys.', example='string_value'),
      updatedTime?: string(name='updatedTime', description='The update timestamp, in milliseconds.', example='2023-10-01T12:00:00Z'),
      versionNumber?: string(name='versionNumber', description='The workflow definition version number.', example='string_value'),
    }
  ](name='items', description='The list of skill cards.'),
  message?: string(name='message', description='The prompt message.', example='successful'),
  page?: int32(name='page', description='The page number.', example='1'),
  pageSize?: int32(name='pageSize', description='The number of entries per page. Default value: 20. Minimum value: 1. Maximum value: 50.', example='10'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  total?: long(name='total', description='The total number of entries.', example='1'),
}

model ListSkillsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListSkillsResponseBody(name='body'),
}

/**
 * @summary Lists the skills visible to the current tenant.
 *
 * @description ## Request description
 * This API retrieves all visible skills under the current tenant. It supports filtering by digital employee binding relationship, skill source, tags, keywords, and other conditions, and supports pagination.
 * ### Request parameters
 * - **TenantId**: Optional. A common parameter passed through by the gateway to the backend header. If not specified, the default tenant of the current caller is used.
 * - **FilterType**: Optional. The skill filtering dimension. Valid values: `ALL` (all published), `BUILTIN` (built-in published), `CUSTOM` (custom published), `DRAFT` (drafts, including published skills with unpublished modifications). Default value: `ALL`.
 * - **Tags**: Optional. Filters by tags. A match occurs if any tag in the array is hit.
 * - **Keyword**: Optional. Performs fuzzy matching by skill name or description.
 * - **Page**: Optional. The page number. Minimum value: 1. Default value: 1.
 * - **PageSize**: Optional. The number of entries per page. Value range: 1 to 100. Default value: 20.
 * - **OperatingObjectName**: Optional. The digital employee name. If specified, filters by binding relationship. Must be used together with `BindStatus`.
 * - **BindStatus**: Optional. The binding status. Valid values: `BOUND` (bound), `UNBOUND` (unbound global skills).
 * ### Response parameters
 * The response contains the skill list `items`, total count `total`, current page `page`, and page size `pageSize`.
 *
 * @param tmpReq ListSkillsRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListSkillsResponse
 */
async function listSkillsWithOptions(tmpReq: ListSkillsRequest, headers: map[string]string, runtime: Util.RuntimeOptions): ListSkillsResponse {
  Util.validateModel(tmpReq);
  var request = new ListSkillsShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!Util.isUnset(tmpReq.tags)) {
    request.tagsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.tags, 'tags', 'json');
  }
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.bindStatus)) {
    body['bindStatus'] = request.bindStatus;
  }
  if (!Util.isUnset(request.filterType)) {
    body['filterType'] = request.filterType;
  }
  if (!Util.isUnset(request.keyword)) {
    body['keyword'] = request.keyword;
  }
  if (!Util.isUnset(request.operatingObjectName)) {
    body['operatingObjectName'] = request.operatingObjectName;
  }
  if (!Util.isUnset(request.page)) {
    body['page'] = request.page;
  }
  if (!Util.isUnset(request.pageSize)) {
    body['pageSize'] = request.pageSize;
  }
  if (!Util.isUnset(request.tagsShrink)) {
    body['tags'] = request.tagsShrink;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'ListSkills',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/listSkills`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Lists the skills visible to the current tenant.
 *
 * @description ## Request description
 * This API retrieves all visible skills under the current tenant. It supports filtering by digital employee binding relationship, skill source, tags, keywords, and other conditions, and supports pagination.
 * ### Request parameters
 * - **TenantId**: Optional. A common parameter passed through by the gateway to the backend header. If not specified, the default tenant of the current caller is used.
 * - **FilterType**: Optional. The skill filtering dimension. Valid values: `ALL` (all published), `BUILTIN` (built-in published), `CUSTOM` (custom published), `DRAFT` (drafts, including published skills with unpublished modifications). Default value: `ALL`.
 * - **Tags**: Optional. Filters by tags. A match occurs if any tag in the array is hit.
 * - **Keyword**: Optional. Performs fuzzy matching by skill name or description.
 * - **Page**: Optional. The page number. Minimum value: 1. Default value: 1.
 * - **PageSize**: Optional. The number of entries per page. Value range: 1 to 100. Default value: 20.
 * - **OperatingObjectName**: Optional. The digital employee name. If specified, filters by binding relationship. Must be used together with `BindStatus`.
 * - **BindStatus**: Optional. The binding status. Valid values: `BOUND` (bound), `UNBOUND` (unbound global skills).
 * ### Response parameters
 * The response contains the skill list `items`, total count `total`, current page `page`, and page size `pageSize`.
 *
 * @param request ListSkillsRequest
 * @return ListSkillsResponse
 */
async function listSkills(request: ListSkillsRequest): ListSkillsResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return listSkillsWithOptions(request, headers, runtime);
}

model ListTenantDirectoryRequest {
  directoryId?: string(name='directoryId', description='The folder ID.', example='exampleDirectoryId'),
  page?: long(name='page', description='The current page number.', example='1'),
  pageSize?: long(name='pageSize', description='The number of entries per page. Default value: 100. Maximum value: 500.', example='20'),
  sortField?: string(name='sortField', description='The field by which the results are sorted. Valid values:

- event_time: the event creation time.
- event_execute_start_time: the event execution time.
- event_execute_finish_time: the event completion time.', example='name'),
  sortOrder?: string(name='sortOrder', description='The sort order. This parameter takes effect only when sortBy is specified. Valid values: ASC, DESC (case-insensitive).', example='asc'),
  sourceTypes?: string(name='sourceTypes', description='The list of source types.', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. If not specified, the default tenant of the caller is used.', example='10000'),
}

model ListTenantDirectoryResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  items?: [ 
    {
      creatorName?: string(name='creatorName', description='The nickname of the creator.', example='string_value'),
      description?: string(name='description', description='The description of the to-do card type.', example='Sample description'),
      gmtCreate?: long(name='gmtCreate', description='The creation time.', example='1'),
      gmtModified?: long(name='gmtModified', description='The last modification time.', example='1'),
      itemId?: string(name='itemId', description='The signing record ID.', example='exampleItemId'),
      itemType?: string(name='itemType', description='The data type (group, user, or role).', example='string_value'),
      name?: string(name='name', description='The name.', example='SampleName.pdf'),
      objectBindings?: [  map[string]any ](name='objectBindings', description='The object bindings.'),
      sourceFailedCount?: long(name='sourceFailedCount', description='The number of resources with the FAILED status. This field is returned only when the top-level directory list of the knowledge base is queried.', example='1'),
      sourceReadyCount?: long(name='sourceReadyCount', description='The number of resources with the READY status. This field is returned only when the top-level directory list of the knowledge base is queried.', example='1'),
      sourceStatus?: string(name='sourceStatus', description='The resource status. This field has a value only when itemType is set to resource.', example='string_value'),
      sourceTotalCount?: long(name='sourceTotalCount', description='The total number of resources in the directory and its subdirectories. This field is returned only when the top-level directory list of the knowledge base is queried.', example='1'),
      sourceType?: string(name='sourceType', description='The data source type.', example='string_value'),
    }
  ](name='items', description='The file information.'),
  message?: string(name='message', description='The description of the status code.', example='successful'),
  page?: long(name='page', description='The page number. Default value: 1. Minimum value: 1. Maximum value: 200.', example='1'),
  pageSize?: long(name='pageSize', description='The number of entries per page. Default value: 100. Maximum value: 500.', example='20'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  totalCount?: long(name='totalCount', description='The total number of entries.', example='1'),
}

model ListTenantDirectoryResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListTenantDirectoryResponseBody(name='body'),
}

/**
 * @summary Retrieves the list of knowledge bases.
 *
 * @description ## Request description
 * - This API is used to perform a paging query on the folder content and resources in an enterprise knowledge base.
 * - Multiple parameters are supported for filtering and sorting, such as `directoryId`, `page`, `pageSize`, `sortField`, `sortOrder`, and others.
 * - The `sourceTypes` parameter allows you to filter by resource type. Separate multiple types with commas.
 * - When `directoryId` is not specified or set to `root`, the root folder list of the knowledge base is queried by default.
 * - The default sort field is `name`, and the default sort order is ascending (`asc`).
 *
 * @param request ListTenantDirectoryRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListTenantDirectoryResponse
 */
async function listTenantDirectoryWithOptions(request: ListTenantDirectoryRequest, headers: map[string]string, runtime: Util.RuntimeOptions): ListTenantDirectoryResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.directoryId)) {
    body['directoryId'] = request.directoryId;
  }
  if (!Util.isUnset(request.page)) {
    body['page'] = request.page;
  }
  if (!Util.isUnset(request.pageSize)) {
    body['pageSize'] = request.pageSize;
  }
  if (!Util.isUnset(request.sortField)) {
    body['sortField'] = request.sortField;
  }
  if (!Util.isUnset(request.sortOrder)) {
    body['sortOrder'] = request.sortOrder;
  }
  if (!Util.isUnset(request.sourceTypes)) {
    body['sourceTypes'] = request.sourceTypes;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'ListTenantDirectory',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/listTenantDirectory`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves the list of knowledge bases.
 *
 * @description ## Request description
 * - This API is used to perform a paging query on the folder content and resources in an enterprise knowledge base.
 * - Multiple parameters are supported for filtering and sorting, such as `directoryId`, `page`, `pageSize`, `sortField`, `sortOrder`, and others.
 * - The `sourceTypes` parameter allows you to filter by resource type. Separate multiple types with commas.
 * - When `directoryId` is not specified or set to `root`, the root folder list of the knowledge base is queried by default.
 * - The default sort field is `name`, and the default sort order is ascending (`asc`).
 *
 * @param request ListTenantDirectoryRequest
 * @return ListTenantDirectoryResponse
 */
async function listTenantDirectory(request: ListTenantDirectoryRequest): ListTenantDirectoryResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return listTenantDirectoryWithOptions(request, headers, runtime);
}

model ListUserVisibleKnowledgeBaseContentsRequest {
  directoryId?: string(name='directoryId', description='The directory ID.

This parameter is required.', example='dir_example'),
  keyword?: string(name='keyword', description='The search keyword. Supports fuzzy match on version names.', example='Product description'),
  page?: long(name='page', description='The page number.', example='1'),
  pageSize?: long(name='pageSize', description='The page size.', example='20'),
  sortField?: string(name='sortField', description='The field by which the results are sorted. Valid values:

- event_time: the event creation time.
- event_execute_start_time: the event execution time.
- event_execute_finish_time: the event completion time.', example='name'),
  sortOrder?: string(name='sortOrder', description='The sort order. This parameter takes effect when sortBy is specified. Valid values: ASC, DESC (case-insensitive).', example='asc'),
  sourceTypes?: string(name='sourceTypes', description='The list of service source types.', example='FILE,WEB_PAGE'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. Pass it explicitly through --tenant-id in winnexo-cli.', example='10000'),
}

model ListUserVisibleKnowledgeBaseContentsResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  items?: [ 
    {
      creatorName?: string(name='creatorName', description='The name of the creator.', example='John'),
      description?: string(name='description', description='The description of the to-do card type.', example='Product materials and user guide'),
      directoryKind?: string(name='directoryKind', description='The directory type.', example='normal'),
      directoryType?: string(name='directoryType', description='The directory type.', example='TENANT'),
      gmtCreate?: long(name='gmtCreate', description='The creation time.', example='1'),
      gmtModified?: long(name='gmtModified', description='The last modification time.', example='1'),
      itemId?: string(name='itemId', description='The item ID.', example='source_example'),
      itemType?: string(name='itemType', description='The item type.', example='resource'),
      kbSubmissionPending?: boolean(name='kbSubmissionPending', description='Indicates whether the resource has a pending knowledge base submission record.', example='false'),
      modifierName?: string(name='modifierName', description='The name of the modifier.', example='Jane'),
      name?: string(name='name', description='The name.', example='Product description.pdf'),
      objectBindings?: [  map[string]any ](name='objectBindings', description='The object bindings.'),
      ooVisibilityMode?: string(name='ooVisibilityMode', description='The visibility mode of the knowledge base to digital employees.', example='PUBLIC'),
      readOnly?: boolean(name='readOnly', description='Indicates whether the item is read-only.', example='false'),
      shareInfos?: [ 
        {
          submissionId?: string(name='submissionId', description='The Ray Job ID.', example='submission_example'),
          submitterId?: long(name='submitterId', description='The user ID of the submitter.', example='1'),
          submitterName?: string(name='submitterName', description='The submitter name.', example='John'),
          targetDirectoryId?: string(name='targetDirectoryId', description='The target directory ID.', example='dir_target'),
          targetDirectoryName?: string(name='targetDirectoryName', description='The target directory name.', example='Shared directory'),
          targetKbRootDirectoryId?: string(name='targetKbRootDirectoryId', description='The root directory ID of the target enterprise knowledge base.', example='dir_kb_root'),
          targetKbRootDirectoryName?: string(name='targetKbRootDirectoryName', description='The name of the target enterprise knowledge base.', example='Product knowledge base'),
        }
      ](name='shareInfos', description='The sharing information.'),
      shared?: boolean(name='shared', description='Indicates whether shared access is allowed.', example='false'),
      sourceFailedCount?: long(name='sourceFailedCount', description='The number of resources in FAILED status. Returned only when listing top-level KB directories.', example='0'),
      sourceKind?: string(name='sourceKind', description='The knowledge base affiliation type. Valid values: aliding_kb_doc (DingTalk knowledge base document), normal (common knowledge).', example='normal'),
      sourceReadyCount?: long(name='sourceReadyCount', description='The number of resources in READY status. Returned only when listing top-level KB directories.', example='1'),
      sourceStatus?: string(name='sourceStatus', description='The resource status. This field has a value only when itemType is resource.', example='READY'),
      sourceTotalCount?: long(name='sourceTotalCount', description='The total number of resources under the directory and its subdirectories. Returned only when listing top-level KB directories.', example='1'),
      sourceType?: string(name='sourceType', description='The data source type.', example='FILE'),
    }
  ](name='items', description='The list of skill cards.'),
  message?: string(name='message', description='The status code description.', example='successful'),
  page?: long(name='page', description='The page number. Default value: 1. Pages start from page 1.', example='1'),
  pageSize?: long(name='pageSize', description='The page size.', example='20'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  totalCount?: long(name='totalCount', description='The total number of records.', example='1'),
}

model ListUserVisibleKnowledgeBaseContentsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListUserVisibleKnowledgeBaseContentsResponseBody(name='body'),
}

/**
 * @summary Queries the knowledge base directory content visible to the current OpenAPI user.
 *
 * @description ## Operation description
 * - This operation returns subdirectories and READY resources under the specified directory based on the enterprise knowledge base frontend scope.
 * - The user identity and directory visibility scope are derived from the OpenAPI authentication context.
 * - When `sourceTypes` has a value, only resources are returned. `keyword` searches only the current directory level.
 *
 * @param request ListUserVisibleKnowledgeBaseContentsRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListUserVisibleKnowledgeBaseContentsResponse
 */
async function listUserVisibleKnowledgeBaseContentsWithOptions(request: ListUserVisibleKnowledgeBaseContentsRequest, headers: map[string]string, runtime: Util.RuntimeOptions): ListUserVisibleKnowledgeBaseContentsResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.directoryId)) {
    body['directoryId'] = request.directoryId;
  }
  if (!Util.isUnset(request.keyword)) {
    body['keyword'] = request.keyword;
  }
  if (!Util.isUnset(request.page)) {
    body['page'] = request.page;
  }
  if (!Util.isUnset(request.pageSize)) {
    body['pageSize'] = request.pageSize;
  }
  if (!Util.isUnset(request.sortField)) {
    body['sortField'] = request.sortField;
  }
  if (!Util.isUnset(request.sortOrder)) {
    body['sortOrder'] = request.sortOrder;
  }
  if (!Util.isUnset(request.sourceTypes)) {
    body['sourceTypes'] = request.sourceTypes;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'ListUserVisibleKnowledgeBaseContents',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/listUserVisibleKnowledgeBaseContents`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the knowledge base directory content visible to the current OpenAPI user.
 *
 * @description ## Operation description
 * - This operation returns subdirectories and READY resources under the specified directory based on the enterprise knowledge base frontend scope.
 * - The user identity and directory visibility scope are derived from the OpenAPI authentication context.
 * - When `sourceTypes` has a value, only resources are returned. `keyword` searches only the current directory level.
 *
 * @param request ListUserVisibleKnowledgeBaseContentsRequest
 * @return ListUserVisibleKnowledgeBaseContentsResponse
 */
async function listUserVisibleKnowledgeBaseContents(request: ListUserVisibleKnowledgeBaseContentsRequest): ListUserVisibleKnowledgeBaseContentsResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return listUserVisibleKnowledgeBaseContentsWithOptions(request, headers, runtime);
}

model ListUserVisibleKnowledgeBasesRequest {
  keyword?: string(name='keyword', description='The keyword for fuzzy match on knowledge base name or description.', example='Product knowledge'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. In winnexo-cli, pass it explicitly with --tenant-id.', example='10000'),
}

model ListUserVisibleKnowledgeBasesResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  items?: [ 
    {
      creatorId?: long(name='creatorId', description='The user ID of the creator.', example='1'),
      creatorName?: string(name='creatorName', description='The name of the creator.', example='John'),
      description?: string(name='description', description='The description of the knowledge base.', example='Product materials and usage instructions'),
      directoryId?: string(name='directoryId', description='The directory ID of the enterprise knowledge base.', example='dir_example'),
      directoryKind?: string(name='directoryKind', description='The directory type.', example='normal'),
      directoryType?: string(name='directoryType', description='The directory type.', example='TENANT'),
      gmtCreate?: long(name='gmtCreate', description='The creation time.', example='1'),
      gmtModified?: long(name='gmtModified', description='The last modification time.', example='1'),
      name?: string(name='name', description='The file name.', example='Product Knowledge Base'),
    }
  ](name='items', description='The result list.
Maximum size:
	50'),
  message?: string(name='message', description='The description of the status code.', example='The current zone list is illegal.'),
  requestId?: string(name='requestId', description='The request trace ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  totalCount?: long(name='totalCount', description='The total number of entries.', example='1'),
}

model ListUserVisibleKnowledgeBasesResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListUserVisibleKnowledgeBasesResponseBody(name='body'),
}

/**
 * @summary Queries the list of enterprise knowledge bases visible to the current OpenAPI user.
 *
 * @description ## Operation description
 * - This operation queries the enterprise knowledge bases visible to the platform user mapped from the OpenAPI authentication identity.
 * - Both the tenant and user identities are determined by the authentication context. Callers cannot expand the visible scope through business parameters.
 * - `tenantId` is an optional common parameter. `keyword` can filter by knowledge base name or description.
 *
 * @param request ListUserVisibleKnowledgeBasesRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListUserVisibleKnowledgeBasesResponse
 */
async function listUserVisibleKnowledgeBasesWithOptions(request: ListUserVisibleKnowledgeBasesRequest, headers: map[string]string, runtime: Util.RuntimeOptions): ListUserVisibleKnowledgeBasesResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.keyword)) {
    body['keyword'] = request.keyword;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'ListUserVisibleKnowledgeBases',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/listUserVisibleKnowledgeBases`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the list of enterprise knowledge bases visible to the current OpenAPI user.
 *
 * @description ## Operation description
 * - This operation queries the enterprise knowledge bases visible to the platform user mapped from the OpenAPI authentication identity.
 * - Both the tenant and user identities are determined by the authentication context. Callers cannot expand the visible scope through business parameters.
 * - `tenantId` is an optional common parameter. `keyword` can filter by knowledge base name or description.
 *
 * @param request ListUserVisibleKnowledgeBasesRequest
 * @return ListUserVisibleKnowledgeBasesResponse
 */
async function listUserVisibleKnowledgeBases(request: ListUserVisibleKnowledgeBasesRequest): ListUserVisibleKnowledgeBasesResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return listUserVisibleKnowledgeBasesWithOptions(request, headers, runtime);
}

model ListUsersRequest {
  accountIds?: [ string ](name='accountIds', description='The list of Alibaba Cloud account IDs.', example='string_value'),
  isActive?: boolean(name='isActive', description='Specifies whether the account is activated.
 - **true**: Activated.
- **false**: Not activated.', example='true'),
  keyword?: string(name='keyword', description='The keyword for searching products. Fuzzy match is supported.', example='SampleKeyword'),
  page?: long(name='page', description='The page number.', example='1'),
  pageSize?: long(name='pageSize', description='The number of entries per page.

> The maximum number of entries per page is 30.', example='20'),
  roleCodes?: [ string ](name='roleCodes', description='The list of new system role codes (full replacement, at least one role must be included). Valid values: SUPER_ADMIN / SYSTEM_ADMIN / SEMANTIC_ADMIN / SKILL_ADMIN / KB_ADMIN / AGENT_ADMIN / APPLICATION_USER.', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. The winnexo-cli passes this parameter explicitly by using --tenant-id.', example='10000'),
}

model ListUsersShrinkRequest {
  accountIdsShrink?: string(name='accountIds', description='The list of Alibaba Cloud account IDs.', example='string_value'),
  isActive?: boolean(name='isActive', description='Specifies whether the account is activated.
 - **true**: Activated.
- **false**: Not activated.', example='true'),
  keyword?: string(name='keyword', description='The keyword for searching products. Fuzzy match is supported.', example='SampleKeyword'),
  page?: long(name='page', description='The page number.', example='1'),
  pageSize?: long(name='pageSize', description='The number of entries per page.

> The maximum number of entries per page is 30.', example='20'),
  roleCodesShrink?: string(name='roleCodes', description='The list of new system role codes (full replacement, at least one role must be included). Valid values: SUPER_ADMIN / SYSTEM_ADMIN / SEMANTIC_ADMIN / SKILL_ADMIN / KB_ADMIN / AGENT_ADMIN / APPLICATION_USER.', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. The winnexo-cli passes this parameter explicitly by using --tenant-id.', example='10000'),
}

model ListUsersResponseBody = {
  code?: string(name='code', description='The error code.', example='200'),
  items?: [ 
    {
      accountId?: string(name='accountId', description='The account ID.', example='exampleAccountId'),
      displayName?: string(name='displayName', description='The display name of the tool.', example='string_value'),
      gmtCreate?: string(name='gmtCreate', description='The creation time.', example='string_value'),
      isActive?: boolean(name='isActive', description='Indicates whether the account is activated:

- 1: Activated.
- 0: Not activated.', example='true'),
      lastLoginTime?: string(name='lastLoginTime', description='The last logon time.', example='2023-10-01T12:00:00Z'),
      roleCodes?: [ string ](name='roleCodes', description='The list of new system role codes (full replacement, at least one role must be included). Valid values: SUPER_ADMIN / SYSTEM_ADMIN / SEMANTIC_ADMIN / SKILL_ADMIN / KB_ADMIN / AGENT_ADMIN / APPLICATION_USER.', example='string_value'),
      userId?: long(name='userId', description='The user ID.', example='1'),
    }
  ](name='items', description='The user information.'),
  message?: string(name='message', description='The description of the status code.', example='ok'),
  page?: long(name='page', description='The current page number.', example='1'),
  pageSize?: long(name='pageSize', description='The number of entries per page.', example='20'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  total?: long(name='total', description='The total number of data entries in the project.', example='1'),
}

model ListUsersResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListUsersResponseBody(name='body'),
}

/**
 * @summary Queries tenant members by paging.
 *
 * @description Queries a paged list of tenant members by using OpenAPI.
 *     Business orchestration:
 *     1. Parse filter conditions (roleCodes → role_ids).
 *     2. Call UserTenantMappingRepository.query_paged_tenant_members to perform a paged query.
 *     3. Convert role_id in the results to roleCode and assemble the response.
 *     Error codes:
 *     - An error is thrown when an invalid roleCode parameter is specified.
 *
 * @param tmpReq ListUsersRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListUsersResponse
 */
async function listUsersWithOptions(tmpReq: ListUsersRequest, headers: map[string]string, runtime: Util.RuntimeOptions): ListUsersResponse {
  Util.validateModel(tmpReq);
  var request = new ListUsersShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!Util.isUnset(tmpReq.accountIds)) {
    request.accountIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.accountIds, 'accountIds', 'json');
  }
  if (!Util.isUnset(tmpReq.roleCodes)) {
    request.roleCodesShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.roleCodes, 'roleCodes', 'json');
  }
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.accountIdsShrink)) {
    body['accountIds'] = request.accountIdsShrink;
  }
  if (!Util.isUnset(request.isActive)) {
    body['isActive'] = request.isActive;
  }
  if (!Util.isUnset(request.keyword)) {
    body['keyword'] = request.keyword;
  }
  if (!Util.isUnset(request.page)) {
    body['page'] = request.page;
  }
  if (!Util.isUnset(request.pageSize)) {
    body['pageSize'] = request.pageSize;
  }
  if (!Util.isUnset(request.roleCodesShrink)) {
    body['roleCodes'] = request.roleCodesShrink;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'ListUsers',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/listUsers`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries tenant members by paging.
 *
 * @description Queries a paged list of tenant members by using OpenAPI.
 *     Business orchestration:
 *     1. Parse filter conditions (roleCodes → role_ids).
 *     2. Call UserTenantMappingRepository.query_paged_tenant_members to perform a paged query.
 *     3. Convert role_id in the results to roleCode and assemble the response.
 *     Error codes:
 *     - An error is thrown when an invalid roleCode parameter is specified.
 *
 * @param request ListUsersRequest
 * @return ListUsersResponse
 */
async function listUsers(request: ListUsersRequest): ListUsersResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return listUsersWithOptions(request, headers, runtime);
}

model ListVisibleKnowledgeBaseContentsRequest {
  directoryId?: string(name='directoryId', description='The directory ID.

This parameter is required.', example='exampleDirectoryId'),
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital employee (operating object name).

This parameter is required.', example='string_value'),
  page?: long(name='page', description='The page number of the results to return. Default value: 1. Minimum value: 1. Maximum value: 200.', example='1'),
  pageSize?: long(name='pageSize', description='The page size. Maximum value: 100.', example='10'),
  sortField?: string(name='sortField', description='The field by which to sort the results. Valid values:

- event_time: event creation time
- event_execute_start_time: event execution time
- event_execute_finish_time: event completion time', example='name'),
  sortOrder?: string(name='sortOrder', description='The sort order. This parameter takes effect only when sortBy is specified. Valid values: ASC, DESC (case-insensitive).', example='desc'),
  sourceTypes?: [ string ](name='sourceTypes', description='The list of service source types.', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID.', example='PiPklI1iSRTm6VFFqlY9VzbgiEiE'),
}

model ListVisibleKnowledgeBaseContentsShrinkRequest {
  directoryId?: string(name='directoryId', description='The directory ID.

This parameter is required.', example='exampleDirectoryId'),
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital employee (operating object name).

This parameter is required.', example='string_value'),
  page?: long(name='page', description='The page number of the results to return. Default value: 1. Minimum value: 1. Maximum value: 200.', example='1'),
  pageSize?: long(name='pageSize', description='The page size. Maximum value: 100.', example='10'),
  sortField?: string(name='sortField', description='The field by which to sort the results. Valid values:

- event_time: event creation time
- event_execute_start_time: event execution time
- event_execute_finish_time: event completion time', example='name'),
  sortOrder?: string(name='sortOrder', description='The sort order. This parameter takes effect only when sortBy is specified. Valid values: ASC, DESC (case-insensitive).', example='desc'),
  sourceTypesShrink?: string(name='sourceTypes', description='The list of service source types.', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID.', example='PiPklI1iSRTm6VFFqlY9VzbgiEiE'),
}

model ListVisibleKnowledgeBaseContentsResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  items?: [ 
    {
      creatorName?: string(name='creatorName', description='The name of the creator.', example='admin'),
      description?: string(name='description', description='The description.', example='Created by taishan-module-recovery'),
      directoryKind?: string(name='directoryKind', description='The directory type.', example='string_value'),
      gmtCreate?: long(name='gmtCreate', description='The creation time.', example='2025-11-14T02:18:27Z'),
      gmtModified?: long(name='gmtModified', description='The modification time.', example='2025-11-26T08:46:25Z'),
      itemId?: string(name='itemId', description='The ID of the data item. When tabId and orgId are the same, itemId uniquely identifies a data item. The maximum length is 128 characters.', example='8525934734583554048_prod'),
      itemType?: string(name='itemType', description='The item type.', example='item'),
      name?: string(name='name', description='The skill name.', example='cs-default-umodel-1782181212383_k8s.metric.k8s_csi_node_pv_node_cn-heyuan-acdr-1/c80cf3a4f9d6c496781591bd17d006c6f'),
      objectBindings?: [ 
        {
          graphName?: string(name='graphName', description='The semantic graph name to which the object belongs. The object_id is unique within this graph.', example='product'),
          objectId?: string(name='objectId', description='The ID of the recommended item, which can be a **feedId** or a micro-application ID.', example='2676'),
          objectName?: string(name='objectName', description='The object name.', example='0bf4cf71-a55d-43f7-9d1e-3f9a6110ae6b'),
          objectType?: string(name='objectType', description='The data type.', example='table'),
          objectTypeName?: string(name='objectTypeName', description='The display name of the object type (such as "Customer"), parsed from the graph schema. The value is null when the cache is missed.', example='string_value'),
        }
      ](name='objectBindings', description='The object bindings.'),
      sourceFailedCount?: long(name='sourceFailedCount', description='The number of resources in the FAILED state. This field is returned only when listing top-level knowledge base directories.', example='1'),
      sourceKind?: string(name='sourceKind', description='The knowledge base affiliation type. Valid values: aliding_kb_doc (DingTalk knowledge base document) and normal (common knowledge).', example='string_value'),
      sourceReadyCount?: long(name='sourceReadyCount', description='The number of resources in the READY state. This field is returned only when listing top-level knowledge base directories.', example='1'),
      sourceStatus?: string(name='sourceStatus', description='The resource status. This field has a value only when itemType is resource.', example='string_value'),
      sourceTotalCount?: long(name='sourceTotalCount', description='The total number of resources under the directory and its subdirectories. This field is returned only when listing top-level knowledge base directories.', example='1'),
      sourceType?: string(name='sourceType', description='The source type.', example='AGENT'),
    }
  ](name='items', description='The list of MCP cards.'),
  message?: string(name='message', description='The status code description.', example='successful'),
  page?: long(name='page', description='The current page number.', example='1'),
  pageSize?: long(name='pageSize', description='The page size.', example='10'),
  requestId?: string(name='requestId', description='The request ID.', example='C474BFC7-7B11-5D92-971E-74AA82EC495B'),
  total?: long(name='total', description='The total number of context libraries that match the query conditions.', example='3'),
}

model ListVisibleKnowledgeBaseContentsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListVisibleKnowledgeBaseContentsResponseBody(name='body'),
}

/**
 * @summary Drills down to query subdirectories and resources under a specified knowledge base directory visible to a specified digital employee.
 *
 * @description ## Operation description
 * - This operation queries all subdirectories and resources under a specified knowledge base directory for a specific digital employee.
 * - The user must have the USE permission on the target digital employee, and the digital employee must have access to the directory and its subdirectories specified in the request.
 * - You must provide the digital employee name (`operatingObjectName`) and the directory ID (`directoryId`) to query. Other parameters such as pagination information and sorting method are optional.
 * - The response includes the list of subdirectories and resources under the directory, and supports pagination.
 * - The `sourceStatus` field filters only resources in the `READY` state.
 * - For security purposes, `tenant_id` and `user_id` are obtained only from the authenticated identity. Values passed in the request body by the caller are ignored.
 *
 * @param tmpReq ListVisibleKnowledgeBaseContentsRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListVisibleKnowledgeBaseContentsResponse
 */
async function listVisibleKnowledgeBaseContentsWithOptions(tmpReq: ListVisibleKnowledgeBaseContentsRequest, headers: map[string]string, runtime: Util.RuntimeOptions): ListVisibleKnowledgeBaseContentsResponse {
  Util.validateModel(tmpReq);
  var request = new ListVisibleKnowledgeBaseContentsShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!Util.isUnset(tmpReq.sourceTypes)) {
    request.sourceTypesShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.sourceTypes, 'sourceTypes', 'json');
  }
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.directoryId)) {
    body['directoryId'] = request.directoryId;
  }
  if (!Util.isUnset(request.operatingObjectName)) {
    body['operatingObjectName'] = request.operatingObjectName;
  }
  if (!Util.isUnset(request.page)) {
    body['page'] = request.page;
  }
  if (!Util.isUnset(request.pageSize)) {
    body['pageSize'] = request.pageSize;
  }
  if (!Util.isUnset(request.sortField)) {
    body['sortField'] = request.sortField;
  }
  if (!Util.isUnset(request.sortOrder)) {
    body['sortOrder'] = request.sortOrder;
  }
  if (!Util.isUnset(request.sourceTypesShrink)) {
    body['sourceTypes'] = request.sourceTypesShrink;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'ListVisibleKnowledgeBaseContents',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/listVisibleKnowledgeBaseContents`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Drills down to query subdirectories and resources under a specified knowledge base directory visible to a specified digital employee.
 *
 * @description ## Operation description
 * - This operation queries all subdirectories and resources under a specified knowledge base directory for a specific digital employee.
 * - The user must have the USE permission on the target digital employee, and the digital employee must have access to the directory and its subdirectories specified in the request.
 * - You must provide the digital employee name (`operatingObjectName`) and the directory ID (`directoryId`) to query. Other parameters such as pagination information and sorting method are optional.
 * - The response includes the list of subdirectories and resources under the directory, and supports pagination.
 * - The `sourceStatus` field filters only resources in the `READY` state.
 * - For security purposes, `tenant_id` and `user_id` are obtained only from the authenticated identity. Values passed in the request body by the caller are ignored.
 *
 * @param request ListVisibleKnowledgeBaseContentsRequest
 * @return ListVisibleKnowledgeBaseContentsResponse
 */
async function listVisibleKnowledgeBaseContents(request: ListVisibleKnowledgeBaseContentsRequest): ListVisibleKnowledgeBaseContentsResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return listVisibleKnowledgeBaseContentsWithOptions(request, headers, runtime);
}

model ListVisibleKnowledgeBasesHeaders {
  commonHeaders?: map[string]string,
  requestId?: string(name='requestId', description='Id of the request', example='F892C03F-7E12-5F37-A506-1FC3B065EAC6'),
}

model ListVisibleKnowledgeBasesRequest {
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital employee (operating object name).

This parameter is required.', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID.', example='1729094555111072'),
}

model ListVisibleKnowledgeBasesResponseBody = {
  code?: string(name='code', description='The response status code.', example='200'),
  items?: [ 
    {
      creatorName?: string(name='creatorName', description='The creator.', example='admin'),
      description?: string(name='description', description='The description.', example='{{7*7}}'),
      directoryId?: string(name='directoryId', description='The directory ID. You can obtain this value by calling the API operation for retrieving the knowledge base directory.', example='exampleDirectoryId'),
      gmtCreate?: long(name='gmtCreate', description='The creation time. This value is a timestamp in milliseconds.', example='2025-11-14T02:18:27Z'),
      gmtModified?: long(name='gmtModified', description='The modification time.', example='2026-03-26T13:57:42Z'),
      name?: string(name='name', description='The skill name.', example='cs-default-umodel-1785637207863_k8s.metric.k8s_csi_node_pv_node_cn-heyuan-acdr-1/c80cf3a4f9d6c496781591bd17d006c6f'),
      path?: string(name='path', description='The file directory information.', example='/test-folder-path1_1773194924773'),
      sourceFailedCount?: long(name='sourceFailedCount', description='The number of resources in the FAILED state. This parameter is returned only when listing top-level knowledge base directories.', example='1'),
      sourceReadyCount?: long(name='sourceReadyCount', description='The number of resources in the READY state. This parameter is returned only when listing top-level knowledge base directories.', example='1'),
      sourceTotalCount?: long(name='sourceTotalCount', description='The total number of resources in the directory and its subdirectories. This parameter is returned only when listing top-level knowledge base directories.', example='1'),
    }
  ](name='items', description='The file information.'),
  message?: string(name='message', description='The description of the status code.', example='successful'),
  requestId?: string(name='requestId', description='Id of the request', example='190F5425-A145-5BBA-980F-082ADB0CA6AF'),
  total?: long(name='total', description='The total number of records.', example='3'),
}

model ListVisibleKnowledgeBasesResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListVisibleKnowledgeBasesResponseBody(name='body'),
}

/**
 * @summary Queries the top-level directories of enterprise knowledge bases accessible to a digital employee.
 *
 * @description ## Request description
 * - This API operation retrieves the list of top-level knowledge base directories visible to a specified digital employee (operating object) within the enterprise.
 *
 * @param request ListVisibleKnowledgeBasesRequest
 * @param headers ListVisibleKnowledgeBasesHeaders
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListVisibleKnowledgeBasesResponse
 */
async function listVisibleKnowledgeBasesWithOptions(request: ListVisibleKnowledgeBasesRequest, headers: ListVisibleKnowledgeBasesHeaders, runtime: Util.RuntimeOptions): ListVisibleKnowledgeBasesResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.operatingObjectName)) {
    body['operatingObjectName'] = request.operatingObjectName;
  }

  var realHeaders : map[string]string = {};
  if (!Util.isUnset(headers.commonHeaders)) {
    realHeaders = headers.commonHeaders;
  }
  if (!Util.isUnset(headers.requestId)) {
    realHeaders['requestId'] = Util.toJSONString(headers.requestId);
  }
  var req = new OpenApi.OpenApiRequest{ 
    headers = realHeaders,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'ListVisibleKnowledgeBases',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/listVisibleKnowledgeBases`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the top-level directories of enterprise knowledge bases accessible to a digital employee.
 *
 * @description ## Request description
 * - This API operation retrieves the list of top-level knowledge base directories visible to a specified digital employee (operating object) within the enterprise.
 *
 * @param request ListVisibleKnowledgeBasesRequest
 * @return ListVisibleKnowledgeBasesResponse
 */
async function listVisibleKnowledgeBases(request: ListVisibleKnowledgeBasesRequest): ListVisibleKnowledgeBasesResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers = new ListVisibleKnowledgeBasesHeaders{};
  return listVisibleKnowledgeBasesWithOptions(request, headers, runtime);
}

model MoveKnowledgeBaseResourceRequest {
  knowledgeId?: string(name='knowledgeId', description='Not supported. This parameter is ignored.

This parameter is required.', example='exampleKnowledgeId'),
  sourceDirectoryId?: string(name='sourceDirectoryId', description='The source directory ID. This is the enterprise knowledge base directory where the resource currently resides.

This parameter is required.', example='exampleSourceDirectoryId'),
  sourceId?: string(name='sourceId', description='The data source ID.

This parameter is required.', example='2000627'),
  targetDirectoryId?: string(name='targetDirectoryId', description='The target directory ID.

This parameter is required.', example='exampleTargetDirectoryId'),
  tenantId?: string(name='tenantId', description='The tenant ID.', example='1729094555111072'),
}

model MoveKnowledgeBaseResourceResponseBody = {
  code?: string(name='code', description='The response code.', example='200'),
  knowledgeId?: string(name='knowledgeId', description='The target knowledge base ID. This value is echoed from the request parameter.', example='exampleKnowledgeId'),
  message?: string(name='message', description='The description of the status code.', example='ok'),
  requestId?: string(name='requestId', description='The request ID.', example='375701FC-2FB9-5782-BE8F-A3F5E2F2158C'),
  sourceDirectoryId?: string(name='sourceDirectoryId', description='The source directory ID. This value is echoed from the request parameter.', example='exampleSourceDirectoryId'),
  sourceId?: string(name='sourceId', description='The unique identifier on the business system side, that is, the business ID.', example='2000358'),
  targetDirectoryId?: string(name='targetDirectoryId', description='The target directory ID. This value is echoed from the request parameter.', example='exampleTargetDirectoryId'),
}

model MoveKnowledgeBaseResourceResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: MoveKnowledgeBaseResourceResponseBody(name='body'),
}

/**
 * @summary Moves a specified resource between enterprise knowledge base directories. Management permissions are required.
 *
 * @description ## Operation description
 * - **Authentication flow**:
 *   1. Basic authentication is performed by the root router (`request.state.openapi_identity`).
 *   2. This handler checks the `DEVELOPMENT_KB_MANAGE` feature permission.
 * - **Procedure**:
 *   1. Check that the source directory and target directory are not the same.
 *   2. Confirm that the target directory exists.
 *   3. Verify that the resource to be moved is in the source directory.
 *   4. Update the directory binding of the resource.
 *   5. Best-effort update of `source.settings["knowledge_id"]` to the target knowledge base ID.
 *   6. Best-effort notification to DocumentAgent to sync `knowledge_id` and `update_time`.
 * - **Security constraints**:
 *   - `tenant_id` and `user_id` must come from the authenticated identity.
 *   - The caller must have KB management permissions.
 *
 * @param request MoveKnowledgeBaseResourceRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return MoveKnowledgeBaseResourceResponse
 */
async function moveKnowledgeBaseResourceWithOptions(request: MoveKnowledgeBaseResourceRequest, headers: map[string]string, runtime: Util.RuntimeOptions): MoveKnowledgeBaseResourceResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.knowledgeId)) {
    body['knowledgeId'] = request.knowledgeId;
  }
  if (!Util.isUnset(request.sourceDirectoryId)) {
    body['sourceDirectoryId'] = request.sourceDirectoryId;
  }
  if (!Util.isUnset(request.sourceId)) {
    body['sourceId'] = request.sourceId;
  }
  if (!Util.isUnset(request.targetDirectoryId)) {
    body['targetDirectoryId'] = request.targetDirectoryId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'MoveKnowledgeBaseResource',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/moveKnowledgeBaseResource`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Moves a specified resource between enterprise knowledge base directories. Management permissions are required.
 *
 * @description ## Operation description
 * - **Authentication flow**:
 *   1. Basic authentication is performed by the root router (`request.state.openapi_identity`).
 *   2. This handler checks the `DEVELOPMENT_KB_MANAGE` feature permission.
 * - **Procedure**:
 *   1. Check that the source directory and target directory are not the same.
 *   2. Confirm that the target directory exists.
 *   3. Verify that the resource to be moved is in the source directory.
 *   4. Update the directory binding of the resource.
 *   5. Best-effort update of `source.settings["knowledge_id"]` to the target knowledge base ID.
 *   6. Best-effort notification to DocumentAgent to sync `knowledge_id` and `update_time`.
 * - **Security constraints**:
 *   - `tenant_id` and `user_id` must come from the authenticated identity.
 *   - The caller must have KB management permissions.
 *
 * @param request MoveKnowledgeBaseResourceRequest
 * @return MoveKnowledgeBaseResourceResponse
 */
async function moveKnowledgeBaseResource(request: MoveKnowledgeBaseResourceRequest): MoveKnowledgeBaseResourceResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return moveKnowledgeBaseResourceWithOptions(request, headers, runtime);
}

model MoveResourceRequest {
  sourceDirectoryId?: string(name='sourceDirectoryId', description='The source directory ID, which is the personal directory where the resource currently resides.

This parameter is required.', example='exampleSourceDirectoryId'),
  sourceId?: string(name='sourceId', description='The ID of the resource to be moved.

This parameter is required.', example='8'),
  targetDirectoryId?: string(name='targetDirectoryId', description='The target directory ID, which is the personal directory to which the resource will be moved.

This parameter is required.', example='exampleTargetDirectoryId'),
  tenantId?: string(name='tenantId', description='The tenant ID. You can view the tenant ID by logging on to the MaxCompute console and choosing **Tenant Management** > **Tenant Properties** in the left-side navigation pane.', example='PiPklI1iSRTm6VFFqlY9VzbgiEiE'),
}

model MoveResourceResponseBody = {
  code?: string(name='code', description='The response status code.', example='200'),
  message?: string(name='message', description='The response message.', example='ok'),
  requestId?: string(name='requestId', description='The request ID.', example='E68654BD-F7BA-5837-8686-5645D739A47C'),
  sourceDirectoryId?: string(name='sourceDirectoryId', description='The source directory ID, which echoes the input parameter.', example='exampleSourceDirectoryId'),
  sourceId?: string(name='sourceId', description='The resource ID, which echoes the input parameter.', example='2000627'),
  success?: boolean(name='success', description='Indicates whether the request was successful.', example='True'),
  targetDirectoryId?: string(name='targetDirectoryId', description='The target directory ID.', example='exampleTargetDirectoryId'),
}

model MoveResourceResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: MoveResourceResponseBody(name='body'),
}

/**
 * @summary Moves a specified resource between personal directories of a user.
 *
 * @description ## Request description
 * - **The source directory and target directory cannot be the same**. Otherwise, the `ERR.Robject.UserDirectory.InvalidOperation` error is returned.
 * - **The target directory must exist**. If it does not exist, the `ERR.Robject.UserDirectory.DirectoryNotFound` error is returned.
 * - **The resource to be moved must exist in the source directory**. If it is not in the source directory, the `ERR.Robject.UserDirectory.ResourceNotInDirectory` error is returned.
 * - After a successful move, the system attempts to notify DocumentAgent to update the new path (`source_path`) of the resource. This step is best-effort. Even if it fails, the overall operation success status is not affected. Only an error log is recorded.
 * - For security purposes, the value of `tenant_id` can only be derived from the authenticated identity information.
 *
 * @param request MoveResourceRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return MoveResourceResponse
 */
async function moveResourceWithOptions(request: MoveResourceRequest, headers: map[string]string, runtime: Util.RuntimeOptions): MoveResourceResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.sourceDirectoryId)) {
    body['sourceDirectoryId'] = request.sourceDirectoryId;
  }
  if (!Util.isUnset(request.sourceId)) {
    body['sourceId'] = request.sourceId;
  }
  if (!Util.isUnset(request.targetDirectoryId)) {
    body['targetDirectoryId'] = request.targetDirectoryId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'MoveResource',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/moveResource`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Moves a specified resource between personal directories of a user.
 *
 * @description ## Request description
 * - **The source directory and target directory cannot be the same**. Otherwise, the `ERR.Robject.UserDirectory.InvalidOperation` error is returned.
 * - **The target directory must exist**. If it does not exist, the `ERR.Robject.UserDirectory.DirectoryNotFound` error is returned.
 * - **The resource to be moved must exist in the source directory**. If it is not in the source directory, the `ERR.Robject.UserDirectory.ResourceNotInDirectory` error is returned.
 * - After a successful move, the system attempts to notify DocumentAgent to update the new path (`source_path`) of the resource. This step is best-effort. Even if it fails, the overall operation success status is not affected. Only an error log is recorded.
 * - For security purposes, the value of `tenant_id` can only be derived from the authenticated identity information.
 *
 * @param request MoveResourceRequest
 * @return MoveResourceResponse
 */
async function moveResource(request: MoveResourceRequest): MoveResourceResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return moveResourceWithOptions(request, headers, runtime);
}

model PreviewKnowledgeBaseSourceRequest {
  sourceId?: string(name='sourceId', description='The data source ID, which is unique within the tenant.

This parameter is required.', example='exampleSourceId'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. In winnexo-cli, pass it explicitly with --tenant-id.', example='10000'),
}

model PreviewKnowledgeBaseSourceResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  content?: string(name='content', description='The text content. This is used for the CONTENT type.', example='Sample content'),
  fileExt?: string(name='fileExt', description='The file name extension. This is used for the OSS_IMM type.', example='string_value'),
  fileName?: string(name='fileName', description='The file name. This is used for the OSS_IMM type.', example='example.pdf'),
  message?: string(name='message', description='The description of the status code.', example='successful'),
  previewType?: string(name='previewType', description='The preview type. Valid values: OSS_IMM, IMAGE, AUDIO, VIDEO, HTML, DING_TALK, VOICE_MEETING, CONTENT.', example='OSS_IMM'),
  previewUrl?: string(name='previewUrl', description='The preview URL. This is used for the OSS_IMM, DING_TALK, and VOICE_MEETING types.', example='https://example.com/winnexo/resource'),
  publicUrl?: string(name='publicUrl', description='The public download URL of the file.', example='https://example.com/winnexo/resource'),
  requestId?: string(name='requestId', description='The request trace ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
}

model PreviewKnowledgeBaseSourceResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: PreviewKnowledgeBaseSourceResponseBody(name='body'),
}

/**
 * @summary Previews the knowledge content in a specified enterprise knowledge base.
 *
 * @description ## Operation description
 * - This operation previews the content of a specified knowledge entry in an enterprise knowledge base.
 * - The `DEVELOPMENT_KB_VIEW` permission is required to call this API.
 * - `sourceId` is a required parameter that identifies the knowledge entry to preview.
 * - The optional parameter `tenantId` specifies the tenant ID. If not provided, the default tenant ID of the caller is used.
 * - Multiple preview types are supported, including but not limited to images, audio, video, and text.
 *
 * @param request PreviewKnowledgeBaseSourceRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return PreviewKnowledgeBaseSourceResponse
 */
async function previewKnowledgeBaseSourceWithOptions(request: PreviewKnowledgeBaseSourceRequest, headers: map[string]string, runtime: Util.RuntimeOptions): PreviewKnowledgeBaseSourceResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.sourceId)) {
    body['sourceId'] = request.sourceId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'PreviewKnowledgeBaseSource',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/previewKnowledgeBaseSource`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Previews the knowledge content in a specified enterprise knowledge base.
 *
 * @description ## Operation description
 * - This operation previews the content of a specified knowledge entry in an enterprise knowledge base.
 * - The `DEVELOPMENT_KB_VIEW` permission is required to call this API.
 * - `sourceId` is a required parameter that identifies the knowledge entry to preview.
 * - The optional parameter `tenantId` specifies the tenant ID. If not provided, the default tenant ID of the caller is used.
 * - Multiple preview types are supported, including but not limited to images, audio, video, and text.
 *
 * @param request PreviewKnowledgeBaseSourceRequest
 * @return PreviewKnowledgeBaseSourceResponse
 */
async function previewKnowledgeBaseSource(request: PreviewKnowledgeBaseSourceRequest): PreviewKnowledgeBaseSourceResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return previewKnowledgeBaseSourceWithOptions(request, headers, runtime);
}

model PreviewPersonalSourceHeaders {
  commonHeaders?: map[string]string,
  requestId?: string(name='requestId', description='The request trace ID.

This parameter is required.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
}

model PreviewPersonalSourceRequest {
  sourceId?: string(name='sourceId', description='The data source ID, which is unique within the tenant.

This parameter is required.', example='exampleSourceId'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. The winnexo-cli passes this parameter explicitly by using --tenant-id.', example='10000'),
}

model PreviewPersonalSourceResponseBody = {
  code?: string(name='code', description='The error code.', example='200'),
  content?: string(name='content', description='The text content. This parameter is used for the CONTENT type.', example='Sample content'),
  fileExt?: string(name='fileExt', description='The file name extension. This parameter is used for the OSS_IMM type.', example='string_value'),
  fileName?: string(name='fileName', description='The file name. This parameter is used for the OSS_IMM type.', example='example.pdf'),
  message?: string(name='message', description='The status code description.', example='successful'),
  previewType?: string(name='previewType', description='The preview type. Valid values: OSS_IMM, IMAGE, AUDIO, VIDEO, HTML, DING_TALK, VOICE_MEETING, and CONTENT.', example='OSS_IMM'),
  previewUrl?: string(name='previewUrl', description='The preview URL. This parameter is used for the OSS_IMM, DING_TALK, and VOICE_MEETING types.', example='https://example.com/winnexo/resource'),
  publicUrl?: string(name='publicUrl', description='The public download URL of the file.', example='https://example.com/winnexo/resource'),
  requestId?: string(name='requestId', description='The request trace ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
}

model PreviewPersonalSourceResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: PreviewPersonalSourceResponseBody(name='body'),
}

/**
 * @summary Allows a user to preview specified knowledge content in their personal directory.
 *
 * @description ## Request description
 * - This operation only allows a user to preview resources in their own personal directory.
 * - The authentication process includes basic authentication and data source ownership verification to ensure that the requester can only access knowledge in their personal directory.
 * - You must provide the unique identifier `sourceId` of the knowledge content in the request. The system queries and returns the corresponding preview information based on this ID and the user\\"s tenant information.
 * - Multiple preview types are supported, such as image, audio, and video. The system returns the corresponding preview URL or direct content display based on the type.
 *
 * @param request PreviewPersonalSourceRequest
 * @param headers PreviewPersonalSourceHeaders
 * @param runtime runtime options for this request RuntimeOptions
 * @return PreviewPersonalSourceResponse
 */
async function previewPersonalSourceWithOptions(request: PreviewPersonalSourceRequest, headers: PreviewPersonalSourceHeaders, runtime: Util.RuntimeOptions): PreviewPersonalSourceResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.sourceId)) {
    body['sourceId'] = request.sourceId;
  }

  var realHeaders : map[string]string = {};
  if (!Util.isUnset(headers.commonHeaders)) {
    realHeaders = headers.commonHeaders;
  }
  if (!Util.isUnset(headers.requestId)) {
    realHeaders['requestId'] = Util.toJSONString(headers.requestId);
  }
  var req = new OpenApi.OpenApiRequest{ 
    headers = realHeaders,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'PreviewPersonalSource',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/previewPersonalSource`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Allows a user to preview specified knowledge content in their personal directory.
 *
 * @description ## Request description
 * - This operation only allows a user to preview resources in their own personal directory.
 * - The authentication process includes basic authentication and data source ownership verification to ensure that the requester can only access knowledge in their personal directory.
 * - You must provide the unique identifier `sourceId` of the knowledge content in the request. The system queries and returns the corresponding preview information based on this ID and the user\\"s tenant information.
 * - Multiple preview types are supported, such as image, audio, and video. The system returns the corresponding preview URL or direct content display based on the type.
 *
 * @param request PreviewPersonalSourceRequest
 * @return PreviewPersonalSourceResponse
 */
async function previewPersonalSource(request: PreviewPersonalSourceRequest): PreviewPersonalSourceResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers = new PreviewPersonalSourceHeaders{};
  return previewPersonalSourceWithOptions(request, headers, runtime);
}

model QueryPrimaryObjectDataRequest {
  keyword?: string(name='keyword', description='The keyword for search. The keyword is matched against the name field. If the schema defines a description field, the keyword is also matched against the description field.', example='SampleKeyword'),
  onlyFavorites?: boolean(name='onlyFavorites', description='Specifies whether to return only favorited primary objects. If this parameter is set to false or not specified, all objects are returned with the isFavorited flag.', example='false'),
  operatingObjectName?: string(name='operatingObjectName', description='The operating object name (such as customer_1).

This parameter is required.', example='string_value'),
  page?: long(name='page', description='The page number (starting from 1).', example='1'),
  pageSize?: long(name='pageSize', description='The number of entries per page. Valid values: 1 to 100.', example='20'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. In winnexo-cli, pass this value explicitly by using --tenant-id.', example='10000'),
}

model QueryPrimaryObjectDataResponseBody = {
  code?: string(name='code', description='The business status code. A value of 200 indicates success. A failure returns a backend error code (ERR.* / InvalidParameter.*).', example='200'),
  items?: [ map[string]string ](name='items', description='The incremental information list.', example='string_value'),
  message?: string(name='message', description='The error description. This value is empty when the request succeeds.', example='ok'),
  page?: long(name='page', description='The current page number.', example='1'),
  pageSize?: long(name='pageSize', description='The number of entries per page.', example='100'),
  requestId?: string(name='requestId', description='The request trace ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  total?: long(name='total', description='The total number of entries.', example='0'),
}

model QueryPrimaryObjectDataResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: QueryPrimaryObjectDataResponseBody(name='body'),
}

/**
 * @summary Queries primary object data by operating object name with paging support, including filtering and search.
 *
 * @description ## Operation description
 * - This API queries primary object data by a specified operating object name (such as `customer_1`) with paging.
 * - Keyword-based search is supported. You can set whether to return only objects marked as favorites in Settings.
 * - Complex filter conditions can be used to further narrow results, including but not limited to equal to, not equal to, greater than, and less than operators.
 * - If no primary object type is configured, an empty result set is returned.
 * - Data in the request undergoes authentication and filtering to ensure security and accuracy.
 *
 * @param request QueryPrimaryObjectDataRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return QueryPrimaryObjectDataResponse
 */
async function queryPrimaryObjectDataWithOptions(request: QueryPrimaryObjectDataRequest, headers: map[string]string, runtime: Util.RuntimeOptions): QueryPrimaryObjectDataResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.keyword)) {
    body['keyword'] = request.keyword;
  }
  if (!Util.isUnset(request.onlyFavorites)) {
    body['onlyFavorites'] = request.onlyFavorites;
  }
  if (!Util.isUnset(request.operatingObjectName)) {
    body['operatingObjectName'] = request.operatingObjectName;
  }
  if (!Util.isUnset(request.page)) {
    body['page'] = request.page;
  }
  if (!Util.isUnset(request.pageSize)) {
    body['pageSize'] = request.pageSize;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'QueryPrimaryObjectData',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/queryPrimaryObjectData`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries primary object data by operating object name with paging support, including filtering and search.
 *
 * @description ## Operation description
 * - This API queries primary object data by a specified operating object name (such as `customer_1`) with paging.
 * - Keyword-based search is supported. You can set whether to return only objects marked as favorites in Settings.
 * - Complex filter conditions can be used to further narrow results, including but not limited to equal to, not equal to, greater than, and less than operators.
 * - If no primary object type is configured, an empty result set is returned.
 * - Data in the request undergoes authentication and filtering to ensure security and accuracy.
 *
 * @param request QueryPrimaryObjectDataRequest
 * @return QueryPrimaryObjectDataResponse
 */
async function queryPrimaryObjectData(request: QueryPrimaryObjectDataRequest): QueryPrimaryObjectDataResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return queryPrimaryObjectDataWithOptions(request, headers, runtime);
}

model QuerySemanticKnowledgeRequest {
  agentName?: string(name='agentName', description='The digital human name. Call listAuthorizedAgents first to retrieve the list of USE permissions.

This parameter is required.', example='SampleDigitalHuman'),
  graphName?: string(name='graphName', description='The knowledge graph name. Call listGraphs first to retrieve available graphs.

This parameter is required.', example='string_value'),
  query?: string(name='query', description='The natural language query question.

This parameter is required.', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. In winnexo-cli, pass it explicitly with --tenant-id.

This parameter is required.', example='21577'),
}

model QuerySemanticKnowledgeResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  message?: string(name='message', description='The status code description.', example='successful'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  schemaKnowledge?: string(name='schemaKnowledge', description='The semantic knowledge text related to the query.

This parameter is required.', example='string_value'),
}

model QuerySemanticKnowledgeResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: QuerySemanticKnowledgeResponseBody(name='body'),
}

/**
 * @summary Queries semantic knowledge related to a user question.
 *
 * @description Exposes only the schema_knowledge semantic recall capability of smart-query.
 *     CLI mapping: ``winnexo semantic query``. ``tenantId`` is passed through common parameters. ``userId``
 *     is read only from the Token identity and cannot be overridden by the request body. The service validates
 *     the ownership of ``graphName + agentName``, active graph status, digital human enablement status, and
 *     the current user\\"s USE permission. A cross-graph agent with the same name will fail and be closed.
 *     Then ``outputs=[schema_knowledge]`` is fixed.
 *
 * @param request QuerySemanticKnowledgeRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return QuerySemanticKnowledgeResponse
 */
async function querySemanticKnowledgeWithOptions(request: QuerySemanticKnowledgeRequest, headers: map[string]string, runtime: Util.RuntimeOptions): QuerySemanticKnowledgeResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.agentName)) {
    body['agentName'] = request.agentName;
  }
  if (!Util.isUnset(request.graphName)) {
    body['graphName'] = request.graphName;
  }
  if (!Util.isUnset(request.query)) {
    body['query'] = request.query;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'QuerySemanticKnowledge',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/querySemanticKnowledge`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries semantic knowledge related to a user question.
 *
 * @description Exposes only the schema_knowledge semantic recall capability of smart-query.
 *     CLI mapping: ``winnexo semantic query``. ``tenantId`` is passed through common parameters. ``userId``
 *     is read only from the Token identity and cannot be overridden by the request body. The service validates
 *     the ownership of ``graphName + agentName``, active graph status, digital human enablement status, and
 *     the current user\\"s USE permission. A cross-graph agent with the same name will fail and be closed.
 *     Then ``outputs=[schema_knowledge]`` is fixed.
 *
 * @param request QuerySemanticKnowledgeRequest
 * @return QuerySemanticKnowledgeResponse
 */
async function querySemanticKnowledge(request: QuerySemanticKnowledgeRequest): QuerySemanticKnowledgeResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return querySemanticKnowledgeWithOptions(request, headers, runtime);
}

model QuerySyncResultRequest {
  taskId?: long(name='taskId', description='The task ID.

This parameter is required.', example='1'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. If not specified, the default tenant of the caller is used.', example='10000'),
}

model QuerySyncResultResponseBody = {
  code?: string(name='code', description='The response status code.', example='200'),
  completedAt?: string(name='completedAt', description='The time when the task was completed (ISO 8601 format).', example='string_value'),
  corpId?: string(name='corpId', description='The enterprise identifier.', example='exampleCorpId'),
  deptStats?: {
    created?: long(name='created', description='The total number of external departments.', example='1'),
    deleted?: long(name='deleted', description='The number of user groups marked for deletion.', example='1'),
    moved?: long(name='moved', description='The number of moved user groups.', example='1'),
    renamed?: long(name='renamed', description='The number of renamed user groups.', example='1'),
    skipped?: long(name='skipped', description='The number of skipped user groups.', example='1'),
    totalExternal?: long(name='totalExternal', description='The total number of external departments.', example='1'),
  }(name='deptStats', description='The department synchronization statistics. This field has a value when the task is completed.'),
  durationSeconds?: long(name='durationSeconds', description='The execution duration, in seconds.', example='1'),
  errorMessage?: string(name='errorMessage', description='The error message.', example='string_value'),
  memberStats?: {
    failed?: long(name='failed', description='The number of failed members.', example='1'),
    relationshipAdded?: long(name='relationshipAdded', description='The number of added member relationships.', example='1'),
    relationshipRemoved?: long(name='relationshipRemoved', description='The number of removed member relationships.', example='1'),
    totalExternal?: long(name='totalExternal', description='The total number of external members.', example='1'),
    unchanged?: long(name='unchanged', description='The number of unchanged member relationships.', example='1'),
  }(name='memberStats', description='The member synchronization statistics. This field has a value when syncMembers is set to true and the task is completed.'),
  message?: string(name='message', description='The description of the status code.', example='ok'),
  platformType?: string(name='platformType', description='The platform type.', example='string_value'),
  requestId?: string(name='requestId', description='The request trace ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  startedAt?: string(name='startedAt', description='The time when the task started (ISO 8601 format).', example='string_value'),
  status?: string(name='status', description='The task status. Valid values: PENDING, RUNNING, COMPLETED, FAILED, TIMEOUT, and CANCELED.', example='READY'),
  submittedAt?: string(name='submittedAt', description='The time when the task was submitted (ISO 8601 format).', example='string_value'),
  summary?: string(name='summary', description='The intelligent meeting summary content.', example='string_value'),
  taskId?: long(name='taskId', description='The task ID.', example='1'),
}

model QuerySyncResultResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: QuerySyncResultResponseBody(name='body'),
}

/**
 * @summary Queries the result of an organization synchronization task.
 *
 * @description Queries the execution status and result of an organization synchronization task based on the task ID.
 *     Task status transitions: PENDING → RUNNING → COMPLETED / FAILED / TIMEOUT / CANCELED
 *     Recommended client polling interval: 3 to 5 seconds.
 *
 * @param request QuerySyncResultRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return QuerySyncResultResponse
 */
async function querySyncResultWithOptions(request: QuerySyncResultRequest, headers: map[string]string, runtime: Util.RuntimeOptions): QuerySyncResultResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.taskId)) {
    body['taskId'] = request.taskId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'QuerySyncResult',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/querySyncResult`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the result of an organization synchronization task.
 *
 * @description Queries the execution status and result of an organization synchronization task based on the task ID.
 *     Task status transitions: PENDING → RUNNING → COMPLETED / FAILED / TIMEOUT / CANCELED
 *     Recommended client polling interval: 3 to 5 seconds.
 *
 * @param request QuerySyncResultRequest
 * @return QuerySyncResultResponse
 */
async function querySyncResult(request: QuerySyncResultRequest): QuerySyncResultResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return querySyncResultWithOptions(request, headers, runtime);
}

model RecommendNextActionsRequest {
  customPrompt?: string(name='customPrompt', description='The extraction instruction.', example='Only recommend next steps related to data analysis'),
  outputType?: string(name='outputType', description='The output type: `conversation/skill/task`.', example='followUpOnly'),
  recentMessageCount?: long(name='recentMessageCount', description='The number of recent messages used to assemble contextual information.', example='10'),
  sessionId?: string(name='sessionId', description='The session ID to filter by. If specified, returns all Active/Expired status information associated with this session.

This parameter is required.', example='exampleSessionId'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. In winnexo-cli, pass this explicitly with --tenant-id.', example='10000'),
}

model RecommendNextActionsResponseBody = {
  code?: string(name='code', description='The business status code. 200 indicates success. A failure returns a backend error code (ERR.* / InvalidParameter.*).', example='200'),
  message?: string(name='message', description='The status code description.', example='successful'),
  nextActions?: [ 
    {
      actionTitle?: string(name='actionTitle', description='The recommendation title.', example='Continue analyzing this metric'),
      skillCode?: string(name='skillCode', description='The skill code.', example='exampleSkillCode'),
      skillName?: string(name='skillName', description='The skill name.', example='Sample skill'),
      type?: string(name='type', description='The recommendation type.', example='recommend_reply'),
    }
  ](name='nextActions', description='The next-step recommendations.'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  title?: string(name='title', description='The meeting reservation title.', example='Sample session title'),
}

model RecommendNextActionsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: RecommendNextActionsResponseBody(name='body'),
}

/**
 * @summary Generates next-step recommendations for a session.
 *
 * @description ## Request description
 * Based on the most recent N messages in a session and the skills attached to the agent, this operation invokes an LLM to generate 0 to 3 next-step recommendations (follow-up questions or recommended skills to execute).
 * - `sessionId`: The session ID. Required. Only sessions that the currently authenticated user has permission to access are allowed.
 * - `recentMessageCount`: The number of recent messages used to assemble contextual information. Valid values: 1 to 30. Default value: 10 (approximately 5 rounds of user+assistant conversation).
 * - `customPrompt`: A custom recommendation instruction (up to 10,000 characters). This is injected into the default recommendation template as a custom instruction (before the output format constraints). The output is still subject to the JSON format and type constraints of the template.
 * - `outputType`: The output type filter. followUpOnly = follow-up recommendations only (default). skillOnly = skill recommendations only. both = generate both types.
 * Unlike internal endpoints, API calls are not restricted by the next-step recommendation toggle in user personal settings and always execute recommendation generation.
 *
 * @param request RecommendNextActionsRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return RecommendNextActionsResponse
 */
async function recommendNextActionsWithOptions(request: RecommendNextActionsRequest, headers: map[string]string, runtime: Util.RuntimeOptions): RecommendNextActionsResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.customPrompt)) {
    body['customPrompt'] = request.customPrompt;
  }
  if (!Util.isUnset(request.outputType)) {
    body['outputType'] = request.outputType;
  }
  if (!Util.isUnset(request.recentMessageCount)) {
    body['recentMessageCount'] = request.recentMessageCount;
  }
  if (!Util.isUnset(request.sessionId)) {
    body['sessionId'] = request.sessionId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'RecommendNextActions',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/recommendNextActions`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Generates next-step recommendations for a session.
 *
 * @description ## Request description
 * Based on the most recent N messages in a session and the skills attached to the agent, this operation invokes an LLM to generate 0 to 3 next-step recommendations (follow-up questions or recommended skills to execute).
 * - `sessionId`: The session ID. Required. Only sessions that the currently authenticated user has permission to access are allowed.
 * - `recentMessageCount`: The number of recent messages used to assemble contextual information. Valid values: 1 to 30. Default value: 10 (approximately 5 rounds of user+assistant conversation).
 * - `customPrompt`: A custom recommendation instruction (up to 10,000 characters). This is injected into the default recommendation template as a custom instruction (before the output format constraints). The output is still subject to the JSON format and type constraints of the template.
 * - `outputType`: The output type filter. followUpOnly = follow-up recommendations only (default). skillOnly = skill recommendations only. both = generate both types.
 * Unlike internal endpoints, API calls are not restricted by the next-step recommendation toggle in user personal settings and always execute recommendation generation.
 *
 * @param request RecommendNextActionsRequest
 * @return RecommendNextActionsResponse
 */
async function recommendNextActions(request: RecommendNextActionsRequest): RecommendNextActionsResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return recommendNextActionsWithOptions(request, headers, runtime);
}

model RemoveUserRequest {
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. If not specified, the default tenant of the caller is used.', example='10000'),
  wnUserId?: string(name='wnUserId', description='The ID of the target user (WINNEXO platform user ID).', example='1'),
}

model RemoveUserResponseBody = {
  code?: string(name='code', description='The error code.', example='200'),
  message?: string(name='message', description='The status code description.', example='successful'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  wnUserId?: string(name='wnUserId', description='The WINNEXO platform user ID.', example='1'),
}

model RemoveUserResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: RemoveUserResponseBody(name='body'),
}

/**
 * @summary Removes a user from a tenant.
 *
 * @description Removes a user from a tenant.
 *     Business orchestration:
 *     1. Obtains tenant_id from identity.
 *     2. Calls delete_user_from_tenant (includes last admin protection).
 *     3. Returns success.
 *     This operation:
 *     - Removes all role associations of the user under the tenant.
 *     - Removes all user group associations of the user under the tenant.
 *     - Revokes all digital employee usage authorizations of the user under the tenant.
 *     - Deletes the user-tenant mapping.
 *
 * @param request RemoveUserRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return RemoveUserResponse
 */
async function removeUserWithOptions(request: RemoveUserRequest, headers: map[string]string, runtime: Util.RuntimeOptions): RemoveUserResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }
  if (!Util.isUnset(request.wnUserId)) {
    query['wnUserId'] = request.wnUserId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'RemoveUser',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/removeUser`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Removes a user from a tenant.
 *
 * @description Removes a user from a tenant.
 *     Business orchestration:
 *     1. Obtains tenant_id from identity.
 *     2. Calls delete_user_from_tenant (includes last admin protection).
 *     3. Returns success.
 *     This operation:
 *     - Removes all role associations of the user under the tenant.
 *     - Removes all user group associations of the user under the tenant.
 *     - Revokes all digital employee usage authorizations of the user under the tenant.
 *     - Deletes the user-tenant mapping.
 *
 * @param request RemoveUserRequest
 * @return RemoveUserResponse
 */
async function removeUser(request: RemoveUserRequest): RemoveUserResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return removeUserWithOptions(request, headers, runtime);
}

model RenameKnowledgeBaseSourceRequest {
  newName?: string(name='newName', description='The new name of the data source.

This parameter is required.', example='string_value'),
  sourceId?: string(name='sourceId', description='The data source ID, which is unique within the tenant.

This parameter is required.', example='exampleSourceId'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. In winnexo-cli, pass this value explicitly by using --tenant-id.', example='10000'),
}

model RenameKnowledgeBaseSourceResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  gmtModified?: string(name='gmtModified', description='The last modified time.', example='string_value'),
  message?: string(name='message', description='The description of the status code.', example='successful'),
  name?: string(name='name', description='The file name.', example='SampleName.pdf'),
  requestId?: string(name='requestId', description='The request trace ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  sourceId?: string(name='sourceId', description='The data source ID.', example='exampleSourceId'),
  status?: string(name='status', description='The data source status after re-parsing.', example='READY'),
}

model RenameKnowledgeBaseSourceResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: RenameKnowledgeBaseSourceResponseBody(name='body'),
}

/**
 * @summary Renames a data source in a specified enterprise knowledge base.
 *
 * @description ## Request description
 * - This operation allows users with the required permissions to modify the name of a specific data source in an enterprise knowledge base.
 * - You must provide the ID of the data source to be renamed (sourceId) and the new name (newName).
 * - The rename operation only updates the name field of the data source and does not trigger other processing flows.
 * - After successful execution, the system publishes a `SOURCE_CHANGED` event for frontend display refresh and attempts to notify DocumentAgent to synchronize the latest source_name information. However, if this step fails, it does not affect the completion status of the main flow.
 * - If the specified sourceId does not exist, the error code `ERR.Robject.Source.NotFound` is returned.
 * - To invoke this API, you must have the `DEVELOPMENT_KB_MANAGE` feature permission.
 * - Identity verification is supported through AccessKey, BearerToken, or APP methods to authenticate requests.
 *
 * @param request RenameKnowledgeBaseSourceRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return RenameKnowledgeBaseSourceResponse
 */
async function renameKnowledgeBaseSourceWithOptions(request: RenameKnowledgeBaseSourceRequest, headers: map[string]string, runtime: Util.RuntimeOptions): RenameKnowledgeBaseSourceResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.newName)) {
    body['newName'] = request.newName;
  }
  if (!Util.isUnset(request.sourceId)) {
    body['sourceId'] = request.sourceId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'RenameKnowledgeBaseSource',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/renameKnowledgeBaseSource`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Renames a data source in a specified enterprise knowledge base.
 *
 * @description ## Request description
 * - This operation allows users with the required permissions to modify the name of a specific data source in an enterprise knowledge base.
 * - You must provide the ID of the data source to be renamed (sourceId) and the new name (newName).
 * - The rename operation only updates the name field of the data source and does not trigger other processing flows.
 * - After successful execution, the system publishes a `SOURCE_CHANGED` event for frontend display refresh and attempts to notify DocumentAgent to synchronize the latest source_name information. However, if this step fails, it does not affect the completion status of the main flow.
 * - If the specified sourceId does not exist, the error code `ERR.Robject.Source.NotFound` is returned.
 * - To invoke this API, you must have the `DEVELOPMENT_KB_MANAGE` feature permission.
 * - Identity verification is supported through AccessKey, BearerToken, or APP methods to authenticate requests.
 *
 * @param request RenameKnowledgeBaseSourceRequest
 * @return RenameKnowledgeBaseSourceResponse
 */
async function renameKnowledgeBaseSource(request: RenameKnowledgeBaseSourceRequest): RenameKnowledgeBaseSourceResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return renameKnowledgeBaseSourceWithOptions(request, headers, runtime);
}

model RenameSourceRequest {
  newName?: string(name='newName', description='The new name of the data source.', example='string_value'),
  sourceId?: string(name='sourceId', description='The data source ID, which is unique within the tenant.', example='exampleSourceId'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. You can pass this parameter explicitly by using --tenant-id in winnexo-cli.', example='10000'),
}

model RenameSourceResponseBody = {
  code?: string(name='code', description='The response status code.', example='200'),
  gmtModified?: string(name='gmtModified', description='The last modified time in ISO 8601 format.', example='string_value'),
  message?: string(name='message', description='The description of the status code.', example='successful'),
  name?: string(name='name', description='The file name.', example='SampleName.pdf'),
  requestId?: string(name='requestId', description='The request trace ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  sourceId?: string(name='sourceId', description='The data source ID.', example='exampleSourceId'),
  status?: string(name='status', description='The data source status.', example='READY'),
}

model RenameSourceResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: RenameSourceResponseBody(name='body'),
}

/**
 * @summary Renames a specified data source. This is a lightweight operation.
 *
 * @description ## Operation description
 * - This API updates only the `name` field of the data source and does not trigger `process_source`.
 * - After a successful update, a `SOURCE_CHANGED` event is published for the frontend to refresh the display.
 * - The system makes a best-effort attempt to notify DocumentAgent to sync the new `source_name`. Even if the sync fails, the main process is not blocked.
 * - If the specified data source does not exist, the `ERR.Robject.Source.NotFound` error is returned. The global middleware converts this error into a POP error code.
 * - Security constraint: `tenant_id` and `user_id` must be derived from the authenticated identity.
 *
 * @param request RenameSourceRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return RenameSourceResponse
 */
async function renameSourceWithOptions(request: RenameSourceRequest, headers: map[string]string, runtime: Util.RuntimeOptions): RenameSourceResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.newName)) {
    body['newName'] = request.newName;
  }
  if (!Util.isUnset(request.sourceId)) {
    body['sourceId'] = request.sourceId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'RenameSource',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/renameSource`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Renames a specified data source. This is a lightweight operation.
 *
 * @description ## Operation description
 * - This API updates only the `name` field of the data source and does not trigger `process_source`.
 * - After a successful update, a `SOURCE_CHANGED` event is published for the frontend to refresh the display.
 * - The system makes a best-effort attempt to notify DocumentAgent to sync the new `source_name`. Even if the sync fails, the main process is not blocked.
 * - If the specified data source does not exist, the `ERR.Robject.Source.NotFound` error is returned. The global middleware converts this error into a POP error code.
 * - Security constraint: `tenant_id` and `user_id` must be derived from the authenticated identity.
 *
 * @param request RenameSourceRequest
 * @return RenameSourceResponse
 */
async function renameSource(request: RenameSourceRequest): RenameSourceResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return renameSourceWithOptions(request, headers, runtime);
}

model ReparseSourceRequest {
  forceSync?: boolean(name='forceSync', description='Specifies whether to synchronously wait for the re-parsing to complete. Default value: false, which indicates asynchronous queuing.', example='false'),
  sourceId?: string(name='sourceId', description='The ID of the data source to re-parse. This ID is unique within the tenant.

This parameter is required.', example='exampleSourceId'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. In winnexo-cli, pass this value explicitly by using --tenant-id.', example='10000'),
}

model ReparseSourceResponseBody = {
  code?: string(name='code', description='The error code.', example='200'),
  message?: string(name='message', description='The status code description.', example='ok'),
  name?: string(name='name', description='The file name.

This parameter is required.', example='SampleName.pdf'),
  requestId?: string(name='requestId', description='The request trace ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  sourceId?: string(name='sourceId', description='The data source ID.

This parameter is required.', example='exampleSourceId'),
  sourceType?: string(name='sourceType', description='The data source type.

This parameter is required.', example='string_value'),
  status?: string(name='status', description='The data source status after re-parsing.

This parameter is required.', example='READY'),
}

model ReparseSourceResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ReparseSourceResponseBody(name='body'),
}

/**
 * @summary Re-parses a resource.
 *
 * @description ## Operation description
 * This API operation re-parses a specified data source. You can choose synchronous or asynchronous execution. You must provide the data source ID in the request. You can optionally specify whether to synchronously wait for parsing to complete. By default, the request is processed asynchronously by being added to a queue. You can also use the `tenantId` parameter to specify a tenant ID, but this parameter is optional.
 * - **forceSync**: If set to `true`, the operation synchronously waits for the re-parsing to complete. Default value: `false`, which indicates that the request is processed asynchronously.
 * - When the service returns `None`, it is converted to a `SourceNotFound` exception. Other exceptions are handled by the OpenAPI global exception chain.
 *
 * @param request ReparseSourceRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ReparseSourceResponse
 */
async function reparseSourceWithOptions(request: ReparseSourceRequest, headers: map[string]string, runtime: Util.RuntimeOptions): ReparseSourceResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.forceSync)) {
    body['forceSync'] = request.forceSync;
  }
  if (!Util.isUnset(request.sourceId)) {
    body['sourceId'] = request.sourceId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'ReparseSource',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/reparseSource`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Re-parses a resource.
 *
 * @description ## Operation description
 * This API operation re-parses a specified data source. You can choose synchronous or asynchronous execution. You must provide the data source ID in the request. You can optionally specify whether to synchronously wait for parsing to complete. By default, the request is processed asynchronously by being added to a queue. You can also use the `tenantId` parameter to specify a tenant ID, but this parameter is optional.
 * - **forceSync**: If set to `true`, the operation synchronously waits for the re-parsing to complete. Default value: `false`, which indicates that the request is processed asynchronously.
 * - When the service returns `None`, it is converted to a `SourceNotFound` exception. Other exceptions are handled by the OpenAPI global exception chain.
 *
 * @param request ReparseSourceRequest
 * @return ReparseSourceResponse
 */
async function reparseSource(request: ReparseSourceRequest): ReparseSourceResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return reparseSourceWithOptions(request, headers, runtime);
}

model ReplaceKnowledgeBaseSourceFileRequest {
  fileName?: string(name='fileName', description='The new file name. This parameter is optional. If this parameter is not provided or set to an empty string, the original file name is retained.', example='example.pdf'),
  filePath?: string(name='filePath', description='The OSS persistent storage address of the replacement file.

This parameter is required.', example='string_value'),
  filePublicUrl?: string(name='filePublicUrl', description='The public access URL of the new file. The URL may contain a temporary signature.

This parameter is required.', example='https://example.com/winnexo/resource'),
  fileRecordId?: string(name='fileRecordId', description='The file record ID of the replacement file.

This parameter is required.', example='exampleFileRecordId'),
  forceSync?: boolean(name='forceSync', description='Specifies whether to synchronously wait for re-parsing to complete. Default value: false, which indicates asynchronous queuing.', example='false'),
  sourceId?: string(name='sourceId', description='The ID of the FILE data source in the enterprise knowledge base to be replaced.

This parameter is required.', example='exampleSourceId'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. In winnexo-cli, pass this parameter explicitly by using --tenant-id.', example='10000'),
}

model ReplaceKnowledgeBaseSourceFileResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  filePath?: string(name='filePath', description='The OSS persistent storage address of the replacement file.', example='string_value'),
  filePublicUrl?: string(name='filePublicUrl', description='The OSS persistent storage address of the replacement file.', example='https://example.com/winnexo/resource'),
  fileRecordId?: string(name='fileRecordId', description='The file record ID of the replacement file.', example='exampleFileRecordId'),
  message?: string(name='message', description='The description of the status code.', example='ok'),
  name?: string(name='name', description='The file name.', example='SampleName.pdf'),
  requestId?: string(name='requestId', description='The request trace ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  sourceId?: string(name='sourceId', description='The data source ID.', example='exampleSourceId'),
  sourceType?: string(name='sourceType', description='The data source type.', example='string_value'),
  status?: string(name='status', description='The task status. The value Running is returned upon submission.', example='READY'),
}

model ReplaceKnowledgeBaseSourceFileResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ReplaceKnowledgeBaseSourceFileResponseBody(name='body'),
}

/**
 * @summary Replaces a FILE resource in a specified enterprise knowledge base and triggers re-parsing.
 *
 * @description ## Request description
 * This API allows you to update a specific FILE-type data source in a self-built enterprise knowledge base and trigger the system to re-parse the data source by providing a new file path and public access URL. Operations can be performed in synchronous or asynchronous mode. In synchronous mode, the client waits until the parsing process is complete.
 * - The **forceSync** parameter controls whether the request is processed synchronously. The default value is `false`, which indicates asynchronous processing.
 * - If **fileName** is not provided or its value is empty, the newly uploaded file retains the original file name.
 * - Ensure that the provided **filePath** and **filePublicUrl** are valid and point to the same file entity.
 *
 * @param request ReplaceKnowledgeBaseSourceFileRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ReplaceKnowledgeBaseSourceFileResponse
 */
async function replaceKnowledgeBaseSourceFileWithOptions(request: ReplaceKnowledgeBaseSourceFileRequest, headers: map[string]string, runtime: Util.RuntimeOptions): ReplaceKnowledgeBaseSourceFileResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.fileName)) {
    body['fileName'] = request.fileName;
  }
  if (!Util.isUnset(request.filePath)) {
    body['filePath'] = request.filePath;
  }
  if (!Util.isUnset(request.filePublicUrl)) {
    body['filePublicUrl'] = request.filePublicUrl;
  }
  if (!Util.isUnset(request.fileRecordId)) {
    body['fileRecordId'] = request.fileRecordId;
  }
  if (!Util.isUnset(request.forceSync)) {
    body['forceSync'] = request.forceSync;
  }
  if (!Util.isUnset(request.sourceId)) {
    body['sourceId'] = request.sourceId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'ReplaceKnowledgeBaseSourceFile',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/replaceKnowledgeBaseSourceFile`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Replaces a FILE resource in a specified enterprise knowledge base and triggers re-parsing.
 *
 * @description ## Request description
 * This API allows you to update a specific FILE-type data source in a self-built enterprise knowledge base and trigger the system to re-parse the data source by providing a new file path and public access URL. Operations can be performed in synchronous or asynchronous mode. In synchronous mode, the client waits until the parsing process is complete.
 * - The **forceSync** parameter controls whether the request is processed synchronously. The default value is `false`, which indicates asynchronous processing.
 * - If **fileName** is not provided or its value is empty, the newly uploaded file retains the original file name.
 * - Ensure that the provided **filePath** and **filePublicUrl** are valid and point to the same file entity.
 *
 * @param request ReplaceKnowledgeBaseSourceFileRequest
 * @return ReplaceKnowledgeBaseSourceFileResponse
 */
async function replaceKnowledgeBaseSourceFile(request: ReplaceKnowledgeBaseSourceFileRequest): ReplaceKnowledgeBaseSourceFileResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return replaceKnowledgeBaseSourceFileWithOptions(request, headers, runtime);
}

model ReplaceObjectBindingsRequest {
  objectBindings?: [ 
    {
      graphName?: string(name='graphName', description='The semantic graph name to which the binding object belongs (object_id is unique within this graph. Required).', example='string_value'),
      objectId?: string(name='objectId', description='The binding object ID.', example='exampleObjectId'),
      objectType?: string(name='objectType', description='The binding object type (such as customer or project).', example='string_value'),
    }
  ](name='objectBindings', description='The new list of object bindings (full replacement. Pass an empty list to clear all bindings).

This parameter is required.'),
  sourceId?: string(name='sourceId', description='The ID of the personal FILE data source to be replaced (unique within the tenant).

This parameter is required.', example='exampleSourceId'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. Pass it explicitly through winnexo-cli using --tenant-id.', example='10000'),
}

model ReplaceObjectBindingsShrinkRequest {
  objectBindingsShrink?: string(name='objectBindings', description='The new list of object bindings (full replacement. Pass an empty list to clear all bindings).

This parameter is required.'),
  sourceId?: string(name='sourceId', description='The ID of the personal FILE data source to be replaced (unique within the tenant).

This parameter is required.', example='exampleSourceId'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. Pass it explicitly through winnexo-cli using --tenant-id.', example='10000'),
}

model ReplaceObjectBindingsResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  message?: string(name='message', description='The description of the status code.', example='successful'),
  objectBindings?: [ 
    {
      graphName?: string(name='graphName', description='The semantic graph name to which the binding object belongs (object_id is unique within this graph. Required).', example='string_value'),
      objectId?: string(name='objectId', description='The binding object ID.', example='exampleObjectId'),
      objectType?: string(name='objectType', description='The binding object type (such as customer or project).', example='string_value'),
    }
  ](name='objectBindings', description='The object binding.'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  sourceId?: string(name='sourceId', description='The ID of the personal FILE data source to be replaced (unique within the tenant).', example='exampleSourceId'),
}

model ReplaceObjectBindingsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ReplaceObjectBindingsResponseBody(name='body'),
}

/**
 * @summary Replaces all object binding information under a specified data source.
 *
 * @description ## Operation description
 * This API performs a full replacement of object bindings for a specified data source (deletes existing bindings first, then inserts new bindings). If an empty list is passed, all bindings are cleared.
 * - **Security constraints**: `tenant_id` and `user_id` must come from the authenticated identity.
 * - **Error handling**: If the specified data source does not exist, an `ERR.Robject.InvalidParameter` error is thrown and converted to a POP error code by the global middleware.
 * - **Synchronous notification**: After a successful replacement, the system makes a best-effort synchronous notification to DocumentAgent to update `semantics.object_bindings`. However, failures are only logged and do not block the main process.
 *
 * @param tmpReq ReplaceObjectBindingsRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ReplaceObjectBindingsResponse
 */
async function replaceObjectBindingsWithOptions(tmpReq: ReplaceObjectBindingsRequest, headers: map[string]string, runtime: Util.RuntimeOptions): ReplaceObjectBindingsResponse {
  Util.validateModel(tmpReq);
  var request = new ReplaceObjectBindingsShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!Util.isUnset(tmpReq.objectBindings)) {
    request.objectBindingsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.objectBindings, 'objectBindings', 'json');
  }
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.objectBindingsShrink)) {
    body['objectBindings'] = request.objectBindingsShrink;
  }
  if (!Util.isUnset(request.sourceId)) {
    body['sourceId'] = request.sourceId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'ReplaceObjectBindings',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/replaceObjectBindings`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Replaces all object binding information under a specified data source.
 *
 * @description ## Operation description
 * This API performs a full replacement of object bindings for a specified data source (deletes existing bindings first, then inserts new bindings). If an empty list is passed, all bindings are cleared.
 * - **Security constraints**: `tenant_id` and `user_id` must come from the authenticated identity.
 * - **Error handling**: If the specified data source does not exist, an `ERR.Robject.InvalidParameter` error is thrown and converted to a POP error code by the global middleware.
 * - **Synchronous notification**: After a successful replacement, the system makes a best-effort synchronous notification to DocumentAgent to update `semantics.object_bindings`. However, failures are only logged and do not block the main process.
 *
 * @param request ReplaceObjectBindingsRequest
 * @return ReplaceObjectBindingsResponse
 */
async function replaceObjectBindings(request: ReplaceObjectBindingsRequest): ReplaceObjectBindingsResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return replaceObjectBindingsWithOptions(request, headers, runtime);
}

model ReplaceSourceFileRequest {
  fileName?: string(name='fileName', description='The new file name. This parameter is optional. If you do not specify this parameter or set it to an empty string, the original file name is retained.', example='example.pdf'),
  filePath?: string(name='filePath', description='The OSS persistent address of the new file, returned by the upload signing operation.

This parameter is required.', example='string_value'),
  filePublicUrl?: string(name='filePublicUrl', description='The public access URL of the new file. The URL may contain a temporary signature.

This parameter is required.', example='https://example.com/winnexo/resource'),
  fileRecordId?: string(name='fileRecordId', description='The file record ID of the new file.

This parameter is required.', example='exampleFileRecordId'),
  forceSync?: boolean(name='forceSync', description='Specifies whether to synchronously wait for re-parsing to complete. Default value: false, which indicates asynchronous queuing.', example='false'),
  sourceId?: string(name='sourceId', description='The ID of the personal FILE data source to be replaced. This ID is unique within the tenant.

This parameter is required.', example='exampleSourceId'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. In winnexo-cli, pass this value explicitly by using --tenant-id.', example='10000'),
}

model ReplaceSourceFileResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  filePath?: string(name='filePath', description='The OSS persistent address of the file after replacement.', example='string_value'),
  filePublicUrl?: string(name='filePublicUrl', description='The OSS persistent address of the file after replacement.', example='https://example.com/winnexo/resource'),
  fileRecordId?: string(name='fileRecordId', description='The file record ID after replacement.', example='exampleFileRecordId'),
  message?: string(name='message', description='The description of the status code.', example='ok'),
  name?: string(name='name', description='The file name.

This parameter is required.', example='SampleName.pdf'),
  requestId?: string(name='requestId', description='The request trace ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  sourceId?: string(name='sourceId', description='The data source ID.

This parameter is required.', example='exampleSourceId'),
  sourceType?: string(name='sourceType', description='The data source type. The value is fixed to FILE.', example='string_value'),
  status?: string(name='status', description='The data source status after re-parsing.', example='READY'),
}

model ReplaceSourceFileResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ReplaceSourceFileResponseBody(name='body'),
}

/**
 * @summary Allows a user to replace a personal file resource that they created and triggers the system to re-parse the file.
 *
 * @description ## Operation description
 * - This API operation replaces a personal FILE resource created by the current platform user and triggers the system to re-parse the file.
 * - The `tenant_id`, operator, and creator constraints are read only from the authenticated identity. Requests without a platform user are rejected to prevent bypassing ownership verification.
 * - If the server returns `None`, it is converted to a `NotFound` exception. Other exceptions are handled by the OpenAPI global exception chain.
 * - This operation supports synchronous or asynchronous waiting for re-parsing to complete. The default behavior is asynchronous queuing (controlled by the `forceSync` parameter).
 *
 * @param request ReplaceSourceFileRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ReplaceSourceFileResponse
 */
async function replaceSourceFileWithOptions(request: ReplaceSourceFileRequest, headers: map[string]string, runtime: Util.RuntimeOptions): ReplaceSourceFileResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.fileName)) {
    body['fileName'] = request.fileName;
  }
  if (!Util.isUnset(request.filePath)) {
    body['filePath'] = request.filePath;
  }
  if (!Util.isUnset(request.filePublicUrl)) {
    body['filePublicUrl'] = request.filePublicUrl;
  }
  if (!Util.isUnset(request.fileRecordId)) {
    body['fileRecordId'] = request.fileRecordId;
  }
  if (!Util.isUnset(request.forceSync)) {
    body['forceSync'] = request.forceSync;
  }
  if (!Util.isUnset(request.sourceId)) {
    body['sourceId'] = request.sourceId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'ReplaceSourceFile',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/replaceSourceFile`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Allows a user to replace a personal file resource that they created and triggers the system to re-parse the file.
 *
 * @description ## Operation description
 * - This API operation replaces a personal FILE resource created by the current platform user and triggers the system to re-parse the file.
 * - The `tenant_id`, operator, and creator constraints are read only from the authenticated identity. Requests without a platform user are rejected to prevent bypassing ownership verification.
 * - If the server returns `None`, it is converted to a `NotFound` exception. Other exceptions are handled by the OpenAPI global exception chain.
 * - This operation supports synchronous or asynchronous waiting for re-parsing to complete. The default behavior is asynchronous queuing (controlled by the `forceSync` parameter).
 *
 * @param request ReplaceSourceFileRequest
 * @return ReplaceSourceFileResponse
 */
async function replaceSourceFile(request: ReplaceSourceFileRequest): ReplaceSourceFileResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return replaceSourceFileWithOptions(request, headers, runtime);
}

model ResetPasswordRequest {
  passwordEncrypted?: string(name='passwordEncrypted', description='The base64-encoded password ciphertext encrypted with RSA-OAEP-SHA256. This parameter is required and cannot be empty.

This parameter is required.', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID.', example='21577'),
  wnUserId?: string(name='wnUserId', description='The ID of the target user (WINNEXO platform user ID).

This parameter is required.', example='1'),
}

model ResetPasswordResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  message?: string(name='message', description='The description of the status code.', example='ok'),
  requestId?: string(name='requestId', description='The request trace ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  wnUserId?: string(name='wnUserId', description='The WINNEXO platform user ID.', example='1'),
}

model ResetPasswordResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ResetPasswordResponseBody(name='body'),
}

/**
 * @summary Resets the password of a user.
 *
 * @description Resets the password of a user through OpenAPI.
 *     Business orchestration:
 *     1. Call UserManagementService.reset_member_password with password_encrypted (required).
 *        The service internally performs RSA decryption, complexity validation, bcrypt hashing, and writes the result.
 *     2. Returns the reset result.
 *     Error codes:
 *     - ERR.User.NotFound: The user does not exist.
 *     - ERR.User.NotInTenant: The user does not belong to the current tenant.
 *     - ERR.User.WinnexoPasswordRequired: The user does not have password credentials (non-WINNEXO type).
 *
 * @param request ResetPasswordRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ResetPasswordResponse
 */
async function resetPasswordWithOptions(request: ResetPasswordRequest, headers: map[string]string, runtime: Util.RuntimeOptions): ResetPasswordResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.passwordEncrypted)) {
    body['passwordEncrypted'] = request.passwordEncrypted;
  }
  if (!Util.isUnset(request.wnUserId)) {
    body['wnUserId'] = request.wnUserId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'ResetPassword',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/resetPassword`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Resets the password of a user.
 *
 * @description Resets the password of a user through OpenAPI.
 *     Business orchestration:
 *     1. Call UserManagementService.reset_member_password with password_encrypted (required).
 *        The service internally performs RSA decryption, complexity validation, bcrypt hashing, and writes the result.
 *     2. Returns the reset result.
 *     Error codes:
 *     - ERR.User.NotFound: The user does not exist.
 *     - ERR.User.NotInTenant: The user does not belong to the current tenant.
 *     - ERR.User.WinnexoPasswordRequired: The user does not have password credentials (non-WINNEXO type).
 *
 * @param request ResetPasswordRequest
 * @return ResetPasswordResponse
 */
async function resetPassword(request: ResetPasswordRequest): ResetPasswordResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return resetPasswordWithOptions(request, headers, runtime);
}

model ResetTokenRequest {
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. If this parameter is not specified, the default tenant of the caller is used.', example='10000'),
  wnUserId?: string(name='wnUserId', description='The ID of the target user on the WINNEXO platform. If this parameter is left empty, the operation is performed on the caller. Administrators can specify the ID of another user to perform the operation on behalf of that user.', example='1'),
}

model ResetTokenResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  message?: string(name='message', description='The description of the status code.', example='ok'),
  requestId?: string(name='requestId', description='The request trace ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  token?: string(name='token', description='The new token in plaintext. This value is returned only in this response. Store it securely.', example='example_token_value'),
  tokenMasked?: string(name='tokenMasked', description='The masked token value.', example='string_value'),
}

model ResetTokenResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ResetTokenResponseBody(name='body'),
}

/**
 * @summary Resets an API token.
 *
 * @description Resets the token of a user.
 *     Business logic:
 *     1. Retrieves user_id from identity (caller_type=user is required).
 *     2. Constructs an AuthContext and delegates permission verification to UserTokenAuthorizedService.
 *     3. Calls reset_token:
 *        - Changes the old ACTIVE token to RESET (permanently invalidated).
 *        - Generates a new ACTIVE token.
 *     4. Returns the new token in plaintext and the masked value.
 *     Note: After the reset, the old token is permanently invalidated and cannot be recovered. The new token in plaintext is returned only in this response.
 *
 * @param request ResetTokenRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ResetTokenResponse
 */
async function resetTokenWithOptions(request: ResetTokenRequest, headers: map[string]string, runtime: Util.RuntimeOptions): ResetTokenResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.wnUserId)) {
    body['wnUserId'] = request.wnUserId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'ResetToken',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/resetToken`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Resets an API token.
 *
 * @description Resets the token of a user.
 *     Business logic:
 *     1. Retrieves user_id from identity (caller_type=user is required).
 *     2. Constructs an AuthContext and delegates permission verification to UserTokenAuthorizedService.
 *     3. Calls reset_token:
 *        - Changes the old ACTIVE token to RESET (permanently invalidated).
 *        - Generates a new ACTIVE token.
 *     4. Returns the new token in plaintext and the masked value.
 *     Note: After the reset, the old token is permanently invalidated and cannot be recovered. The new token in plaintext is returned only in this response.
 *
 * @param request ResetTokenRequest
 * @return ResetTokenResponse
 */
async function resetToken(request: ResetTokenRequest): ResetTokenResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return resetTokenWithOptions(request, headers, runtime);
}

model RetryDirectoryFailedSourcesRequest {
  directoryId?: string(name='directoryId', description='The ID of the enterprise knowledge base folder. Failed resources in the folder and its subfolders are included recursively.

This parameter is required.', example='exampleDirectoryId'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. In winnexo-cli, pass this value explicitly by using --tenant-id.', example='10000'),
}

model RetryDirectoryFailedSourcesResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  directoryId?: string(name='directoryId', description='The ID of the enterprise knowledge base folder. Failed resources in the folder and its subfolders are included recursively.', example='exampleDirectoryId'),
  enqueuedCount?: long(name='enqueuedCount', description='The number of resources successfully enqueued for retry.', example='1'),
  enqueuedIds?: [ string ](name='enqueuedIds', description='enqueuedIds。', example='string_value'),
  failedCount?: long(name='failedCount', description='The total number of failed resources in the folder.', example='1'),
  failedSources?: [ 
    {
      name?: string(name='name', description='The file name.', example='SampleName.pdf'),
      sourceId?: string(name='sourceId', description='The data source ID.', example='exampleSourceId'),
      sourceType?: string(name='sourceType', description='The data source type.', example='string_value'),
    }
  ](name='failedSources', description='The list of failed data source IDs.'),
  message?: string(name='message', description='The description of the status code.', example='ok'),
  requestId?: string(name='requestId', description='The request trace ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  skippedCount?: long(name='skippedCount', description='The number of resources skipped because they are not in FAILED status.', example='1'),
}

model RetryDirectoryFailedSourcesResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: RetryDirectoryFailedSourcesResponseBody(name='body'),
}

/**
 * @summary Retries all failed data sources in a specified folder and its subfolders in batch.
 *
 * @description ## Request description
 * This API retrieves and retries data sources with a FAILED status in the specified personal folder of a user (including all subfolders). The request returns immediately, and the actual retry tasks are executed asynchronously in the background. Only resources that the current logged-in user has access to and that were created by the user can be retried.
 * ### Security and permissions
 * - This operation requires appropriate RAM permissions.
 * - You can only operate on resources within the tenant to which the current user belongs.
 * - Ensure that `tenantId` and `userId` come from verified identity information.
 * ### Precautions
 * - `directoryId` is a required parameter that specifies the target folder in which to check and retry failed data sources.
 * - If `tenantId` is not provided, the tenant ID of the caller is used by default.
 * - The API supports multiple authentication methods, including AccessKey, BearerToken, and APP authentication.
 *
 * @param request RetryDirectoryFailedSourcesRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return RetryDirectoryFailedSourcesResponse
 */
async function retryDirectoryFailedSourcesWithOptions(request: RetryDirectoryFailedSourcesRequest, headers: map[string]string, runtime: Util.RuntimeOptions): RetryDirectoryFailedSourcesResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.directoryId)) {
    body['directoryId'] = request.directoryId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'RetryDirectoryFailedSources',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/retryDirectoryFailedSources`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retries all failed data sources in a specified folder and its subfolders in batch.
 *
 * @description ## Request description
 * This API retrieves and retries data sources with a FAILED status in the specified personal folder of a user (including all subfolders). The request returns immediately, and the actual retry tasks are executed asynchronously in the background. Only resources that the current logged-in user has access to and that were created by the user can be retried.
 * ### Security and permissions
 * - This operation requires appropriate RAM permissions.
 * - You can only operate on resources within the tenant to which the current user belongs.
 * - Ensure that `tenantId` and `userId` come from verified identity information.
 * ### Precautions
 * - `directoryId` is a required parameter that specifies the target folder in which to check and retry failed data sources.
 * - If `tenantId` is not provided, the tenant ID of the caller is used by default.
 * - The API supports multiple authentication methods, including AccessKey, BearerToken, and APP authentication.
 *
 * @param request RetryDirectoryFailedSourcesRequest
 * @return RetryDirectoryFailedSourcesResponse
 */
async function retryDirectoryFailedSources(request: RetryDirectoryFailedSourcesRequest): RetryDirectoryFailedSourcesResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return retryDirectoryFailedSourcesWithOptions(request, headers, runtime);
}

model RetryKnowledgeBaseFailedSourcesRequest {
  directoryId?: string(name='directoryId', description='The ID of the enterprise knowledge base directory. Failed resources in subdirectories are recursively included.

This parameter is required.', example='exampleDirectoryId'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. In winnexo-cli, pass this value explicitly by using --tenant-id.', example='10000'),
}

model RetryKnowledgeBaseFailedSourcesResponseBody = {
  code?: string(name='code', description='The business status code. A value of 200 indicates success. A failure returns a backend error code (ERR.* / InvalidParameter.*).', example='200'),
  directoryId?: string(name='directoryId', description='The enterprise knowledge base directory ID.', example='exampleDirectoryId'),
  enqueuedCount?: long(name='enqueuedCount', description='The number of resources successfully enqueued for retry.', example='1'),
  enqueuedIds?: [ string ](name='enqueuedIds', description='enqueuedIds。', example='string_value'),
  failedCount?: long(name='failedCount', description='The total number of failed resources under the directory.', example='1'),
  failedSources?: [ 
    {
      name?: string(name='name', description='The file name.', example='SampleName.pdf'),
      sourceId?: string(name='sourceId', description='The data source ID.', example='exampleSourceId'),
      sourceType?: string(name='sourceType', description='The data source type.', example='string_value'),
    }
  ](name='failedSources', description='The list of failed data sources.'),
  message?: string(name='message', description='The error description. This value is empty on success.', example='ok'),
  requestId?: string(name='requestId', description='The request trace ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  skippedCount?: long(name='skippedCount', description='The number of resources skipped because they are not in FAILED status.', example='1'),
}

model RetryKnowledgeBaseFailedSourcesResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: RetryKnowledgeBaseFailedSourcesResponseBody(name='body'),
}

/**
 * @summary Retries all data sources in failed status under a specified directory in batch.
 *
 * @description ## Request description
 * This API retrieves and retries all data sources in FAILED status under a specified enterprise knowledge base directory (including its subdirectories). The request returns immediately, and the actual retry operations are executed asynchronously in the background.
 * - **Authentication**: In addition to basic authentication, the `DEVELOPMENT_KB_MANAGE` permission is required.
 * - **Security constraints**: Only callers with the corresponding tenant and user identity are allowed access, and KB management permission is required. Administrators can initiate retries for failed resources of any user.
 * - **Parameters**:
 *   - `directoryId` (required): The ID of the enterprise knowledge base directory for which to check and retry failed data sources.
 *   - `tenantId` (optional): The tenant ID. The default tenant of the caller is used if this parameter is not specified.
 * - **Response**: On success, returns the number of data sources enqueued for retry and related details.
 *
 * @param request RetryKnowledgeBaseFailedSourcesRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return RetryKnowledgeBaseFailedSourcesResponse
 */
async function retryKnowledgeBaseFailedSourcesWithOptions(request: RetryKnowledgeBaseFailedSourcesRequest, headers: map[string]string, runtime: Util.RuntimeOptions): RetryKnowledgeBaseFailedSourcesResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.directoryId)) {
    body['directoryId'] = request.directoryId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'RetryKnowledgeBaseFailedSources',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/retryKnowledgeBaseFailedSources`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retries all data sources in failed status under a specified directory in batch.
 *
 * @description ## Request description
 * This API retrieves and retries all data sources in FAILED status under a specified enterprise knowledge base directory (including its subdirectories). The request returns immediately, and the actual retry operations are executed asynchronously in the background.
 * - **Authentication**: In addition to basic authentication, the `DEVELOPMENT_KB_MANAGE` permission is required.
 * - **Security constraints**: Only callers with the corresponding tenant and user identity are allowed access, and KB management permission is required. Administrators can initiate retries for failed resources of any user.
 * - **Parameters**:
 *   - `directoryId` (required): The ID of the enterprise knowledge base directory for which to check and retry failed data sources.
 *   - `tenantId` (optional): The tenant ID. The default tenant of the caller is used if this parameter is not specified.
 * - **Response**: On success, returns the number of data sources enqueued for retry and related details.
 *
 * @param request RetryKnowledgeBaseFailedSourcesRequest
 * @return RetryKnowledgeBaseFailedSourcesResponse
 */
async function retryKnowledgeBaseFailedSources(request: RetryKnowledgeBaseFailedSourcesRequest): RetryKnowledgeBaseFailedSourcesResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return retryKnowledgeBaseFailedSourcesWithOptions(request, headers, runtime);
}

model RevokeAgentUsersRequest {
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital human.

This parameter is required.', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. If not specified, the default tenant of the caller is used.', example='10000'),
  userGroupIds?: [ string ](name='userGroupIds', description='The list of user group IDs to be revoked (16-character hex strings).', example='string_value'),
  userIds?: [ string ](name='userIds', description='The list of user IDs to be revoked.', example='1'),
}

model RevokeAgentUsersShrinkRequest {
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital human.

This parameter is required.', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. If not specified, the default tenant of the caller is used.', example='10000'),
  userGroupIdsShrink?: string(name='userGroupIds', description='The list of user group IDs to be revoked (16-character hex strings).', example='string_value'),
  userIdsShrink?: string(name='userIds', description='The list of user IDs to be revoked.', example='1'),
}

model RevokeAgentUsersResponseBody = {
  code?: string(name='code', description='The business status code. A value of 200 indicates success. A failure returns a backend error code (ERR.* / InvalidParameter.*).', example='200'),
  message?: string(name='message', description='The error description. This is empty when the call succeeds.', example='successful'),
  requestId?: string(name='requestId', description='The request trace ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  revokedCount?: long(name='revokedCount', description='The number of records successfully revoked in this call.', example='1'),
}

model RevokeAgentUsersResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: RevokeAgentUsersResponseBody(name='body'),
}

/**
 * @summary Revokes the usage permissions of a user or user group on a digital human.
 *
 * @description Revokes the usage permissions of a user or user group on a specified digital human.
 *     Business logic:
 *     1. Constructs an AuthContext from identity.
 *     2. Performs mutual exclusion validation on the request body: either userIds or userGroupIds must be specified.
 *     3. Delegates to AgentAuthorizationAuthorizedService.revoke_authorization for execution.
 *     4. Pre-validation: MANAGE permission + agent existence check (performed by the AuthorizedService layer, which authenticates before exposing existence).
 *     5. After direct user authorization is revoked, the user may still have access through user group authorization.
 *
 * @param tmpReq RevokeAgentUsersRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return RevokeAgentUsersResponse
 */
async function revokeAgentUsersWithOptions(tmpReq: RevokeAgentUsersRequest, headers: map[string]string, runtime: Util.RuntimeOptions): RevokeAgentUsersResponse {
  Util.validateModel(tmpReq);
  var request = new RevokeAgentUsersShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!Util.isUnset(tmpReq.userGroupIds)) {
    request.userGroupIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.userGroupIds, 'userGroupIds', 'json');
  }
  if (!Util.isUnset(tmpReq.userIds)) {
    request.userIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.userIds, 'userIds', 'json');
  }
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.operatingObjectName)) {
    body['operatingObjectName'] = request.operatingObjectName;
  }
  if (!Util.isUnset(request.userGroupIdsShrink)) {
    body['userGroupIds'] = request.userGroupIdsShrink;
  }
  if (!Util.isUnset(request.userIdsShrink)) {
    body['userIds'] = request.userIdsShrink;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'RevokeAgentUsers',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/revokeAgentUsers`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Revokes the usage permissions of a user or user group on a digital human.
 *
 * @description Revokes the usage permissions of a user or user group on a specified digital human.
 *     Business logic:
 *     1. Constructs an AuthContext from identity.
 *     2. Performs mutual exclusion validation on the request body: either userIds or userGroupIds must be specified.
 *     3. Delegates to AgentAuthorizationAuthorizedService.revoke_authorization for execution.
 *     4. Pre-validation: MANAGE permission + agent existence check (performed by the AuthorizedService layer, which authenticates before exposing existence).
 *     5. After direct user authorization is revoked, the user may still have access through user group authorization.
 *
 * @param request RevokeAgentUsersRequest
 * @return RevokeAgentUsersResponse
 */
async function revokeAgentUsers(request: RevokeAgentUsersRequest): RevokeAgentUsersResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return revokeAgentUsersWithOptions(request, headers, runtime);
}

model RunSkillRequest {
  arguments?: map[string]any(name='arguments', description='The placeholder variable value that stores the error message for a fault.', example='string_value'),
  clientToken?: string(name='clientToken', description='The idempotency token generated by the caller. In the current version, this value is only recorded in metadata and is not used for deduplication.', example='string_value'),
  model?: string(name='model', description='The abstract model name (model tier). Defaults to standard if not specified.', example='quick'),
  operatingObjectName?: string(name='operatingObjectName', description='The digital worker name. Used to calculate the CodeAgent allowedSkills whitelist based on binding relationships.', example='string_value'),
  skillCode?: string(name='skillCode', description='The skill code (globally unique). This parameter takes priority over skillName.', example='string_value'),
  skillName?: string(name='skillName', description='The skill name. Used when skillCode is not specified. The name must be unique within the tenant scope.', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. If not specified, the default tenant of the caller is used.', example='10000'),
}

model RunSkillShrinkRequest {
  argumentsShrink?: string(name='arguments', description='The placeholder variable value that stores the error message for a fault.', example='string_value'),
  clientToken?: string(name='clientToken', description='The idempotency token generated by the caller. In the current version, this value is only recorded in metadata and is not used for deduplication.', example='string_value'),
  model?: string(name='model', description='The abstract model name (model tier). Defaults to standard if not specified.', example='quick'),
  operatingObjectName?: string(name='operatingObjectName', description='The digital worker name. Used to calculate the CodeAgent allowedSkills whitelist based on binding relationships.', example='string_value'),
  skillCode?: string(name='skillCode', description='The skill code (globally unique). This parameter takes priority over skillName.', example='string_value'),
  skillName?: string(name='skillName', description='The skill name. Used when skillCode is not specified. The name must be unique within the tenant scope.', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. If not specified, the default tenant of the caller is used.', example='10000'),
}

model RunSkillResponseBody = {
  code?: string(name='code', description='The response status code.', example='200'),
  createdAt?: string(name='createdAt', description='The task creation time in ISO 8601 UTC format.', example='string_value'),
  message?: string(name='message', description='The prompt message.', example='The current zone list is illegal.'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  runId?: string(name='runId', description='The asynchronous task ID, used for querying with getSkillRun.', example='exampleRunId'),
  skillCode?: string(name='skillCode', description='The skill code that was actually executed.', example='string_value'),
  skillName?: string(name='skillName', description='The skill name.', example='string_value'),
  status?: string(name='status', description='The task status. Returns Running immediately upon submission.', example='READY'),
}

model RunSkillResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: RunSkillResponseBody(name='body'),
}

/**
 * @summary Asynchronously triggers skill execution and immediately returns a RunId.
 *
 * @description ## Request description
 * This operation supports only asynchronous mode. After submission, the operation immediately returns a `RunId` and `Status=Running`. The client polls for the final result by calling `GetSkillRun`.
 * - **TenantId**: An optional common parameter that the gateway passes through to the backend header.
 * - **SkillCode** / **SkillName**: Specify one of the two parameters. SkillCode takes priority. If SkillName is not unique, `ERR.SkillHub.SkillNameAmbiguous` is returned.
 * - **Arguments**: Required. The skill input parameter object. The structure is described by the inputConfig returned by `GetSkill`.
 * - **ClientToken**: An optional idempotency key. In the current version, this value is only recorded in the task metadata and is not used for strict idempotency deduplication.
 * Note: Synchronous mode (Async=false), Stream, and CallbackUrl are not supported in the first release and will be available in later versions.
 *
 * @param tmpReq RunSkillRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return RunSkillResponse
 */
async function runSkillWithOptions(tmpReq: RunSkillRequest, headers: map[string]string, runtime: Util.RuntimeOptions): RunSkillResponse {
  Util.validateModel(tmpReq);
  var request = new RunSkillShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!Util.isUnset(tmpReq.arguments)) {
    request.argumentsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.arguments, 'arguments', 'json');
  }
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.argumentsShrink)) {
    body['arguments'] = request.argumentsShrink;
  }
  if (!Util.isUnset(request.clientToken)) {
    body['clientToken'] = request.clientToken;
  }
  if (!Util.isUnset(request.model)) {
    body['model'] = request.model;
  }
  if (!Util.isUnset(request.operatingObjectName)) {
    body['operatingObjectName'] = request.operatingObjectName;
  }
  if (!Util.isUnset(request.skillCode)) {
    body['skillCode'] = request.skillCode;
  }
  if (!Util.isUnset(request.skillName)) {
    body['skillName'] = request.skillName;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'RunSkill',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/runSkill`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Asynchronously triggers skill execution and immediately returns a RunId.
 *
 * @description ## Request description
 * This operation supports only asynchronous mode. After submission, the operation immediately returns a `RunId` and `Status=Running`. The client polls for the final result by calling `GetSkillRun`.
 * - **TenantId**: An optional common parameter that the gateway passes through to the backend header.
 * - **SkillCode** / **SkillName**: Specify one of the two parameters. SkillCode takes priority. If SkillName is not unique, `ERR.SkillHub.SkillNameAmbiguous` is returned.
 * - **Arguments**: Required. The skill input parameter object. The structure is described by the inputConfig returned by `GetSkill`.
 * - **ClientToken**: An optional idempotency key. In the current version, this value is only recorded in the task metadata and is not used for strict idempotency deduplication.
 * Note: Synchronous mode (Async=false), Stream, and CallbackUrl are not supported in the first release and will be available in later versions.
 *
 * @param request RunSkillRequest
 * @return RunSkillResponse
 */
async function runSkill(request: RunSkillRequest): RunSkillResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return runSkillWithOptions(request, headers, runtime);
}

model SaveOutputFileToResourceRequest {
  directoryId?: string(name='directoryId', description='The ID of the target personal folder. If not specified, the user\\\\"s default folder is automatically resolved.', example='exampleDirectoryId'),
  itemIds?: [ string ](name='itemIds', description='itemIds

This parameter is required.', example='string_value'),
  mode?: string(name='mode', description='The save mode. Valid values:
- link: Links the resource to the output in a 1:1 idempotent manner. Edits to the output are synchronized to the resource.
- copy: Creates a snapshot copy with no limit on the number of copies.', example='link'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. In winnexo-cli, pass it explicitly with --tenant-id.', example='string_value'),
}

model SaveOutputFileToResourceShrinkRequest {
  directoryId?: string(name='directoryId', description='The ID of the target personal folder. If not specified, the user\\\\"s default folder is automatically resolved.', example='exampleDirectoryId'),
  itemIdsShrink?: string(name='itemIds', description='itemIds

This parameter is required.', example='string_value'),
  mode?: string(name='mode', description='The save mode. Valid values:
- link: Links the resource to the output in a 1:1 idempotent manner. Edits to the output are synchronized to the resource.
- copy: Creates a snapshot copy with no limit on the number of copies.', example='link'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. In winnexo-cli, pass it explicitly with --tenant-id.', example='string_value'),
}

model SaveOutputFileToResourceResponseBody = {
  code?: string(name='code', description='The business status code. A value of 200 indicates success. A failure returns a backend error code (ERR.* / InvalidParameter.*).', example='200'),
  message?: string(name='message', description='The error description. This value is empty on success.', example='The current zone list is illegal.'),
  requestId?: string(name='requestId', description='The request trace ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  results?: [ 
    {
      errorCode?: string(name='errorCode', description='The business error code (i18n key). Returned on failure.', example='string_value'),
      errorMessage?: string(name='errorMessage', description='The error description, localized based on the request Accept-Language header. Returned on failure.', example='string_value'),
      itemId?: string(name='itemId', description='The output detail ID.', example='exampleItemId'),
      sourceId?: string(name='sourceId', description='The sourceId of the newly created resource. Returned on success.', example='exampleSourceId'),
      success?: boolean(name='success', description='Indicates whether the operation is successful.', example='true'),
    }
  ](name='results', description='The per-record results in the same order as the input itemIds. A single record failure does not affect other records.'),
}

model SaveOutputFileToResourceResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: SaveOutputFileToResourceResponseBody(name='body'),
}

/**
 * @summary Saves output details in batch as personal resources. Supports link or copy mode.
 *
 * @description ## Operation description
 * - This API saves a batch of output details as personal resources for the user.
 * - Two save modes are supported: `link` and `copy`. When `link` is selected, edits to the output are synchronized to the resource. When `copy` is selected, a snapshot is created with no limit on the number of copies.
 * - `tenant_id` and `user_id` are derived only from the authenticated identity.
 * - If `operating_object` values are inconsistent within the batch and `directoryId` is not specified, the entire batch fails with a pre-check error.
 * - The processing result of a single record does not affect other records. Failure information for individual records is returned in the response.
 * - A maximum of 50 records are supported per batch operation.
 * - Batch-level pre-check failures are returned in a POP-compatible error format by the global exception middleware.
 *
 * @param tmpReq SaveOutputFileToResourceRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return SaveOutputFileToResourceResponse
 */
async function saveOutputFileToResourceWithOptions(tmpReq: SaveOutputFileToResourceRequest, headers: map[string]string, runtime: Util.RuntimeOptions): SaveOutputFileToResourceResponse {
  Util.validateModel(tmpReq);
  var request = new SaveOutputFileToResourceShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!Util.isUnset(tmpReq.itemIds)) {
    request.itemIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.itemIds, 'itemIds', 'json');
  }
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.directoryId)) {
    body['directoryId'] = request.directoryId;
  }
  if (!Util.isUnset(request.itemIdsShrink)) {
    body['itemIds'] = request.itemIdsShrink;
  }
  if (!Util.isUnset(request.mode)) {
    body['mode'] = request.mode;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'SaveOutputFileToResource',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/saveOutputFileToResource`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Saves output details in batch as personal resources. Supports link or copy mode.
 *
 * @description ## Operation description
 * - This API saves a batch of output details as personal resources for the user.
 * - Two save modes are supported: `link` and `copy`. When `link` is selected, edits to the output are synchronized to the resource. When `copy` is selected, a snapshot is created with no limit on the number of copies.
 * - `tenant_id` and `user_id` are derived only from the authenticated identity.
 * - If `operating_object` values are inconsistent within the batch and `directoryId` is not specified, the entire batch fails with a pre-check error.
 * - The processing result of a single record does not affect other records. Failure information for individual records is returned in the response.
 * - A maximum of 50 records are supported per batch operation.
 * - Batch-level pre-check failures are returned in a POP-compatible error format by the global exception middleware.
 *
 * @param request SaveOutputFileToResourceRequest
 * @return SaveOutputFileToResourceResponse
 */
async function saveOutputFileToResource(request: SaveOutputFileToResourceRequest): SaveOutputFileToResourceResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return saveOutputFileToResourceWithOptions(request, headers, runtime);
}

model SendAsyncChatMessageRequest {
  content?: string(name='content', description='The message body from the user.

This parameter is required.', example='Sample content'),
  contentType?: string(name='contentType', description='The message type. Valid values: Text and Markdown.', example='Text'),
  digitalEmployeeName?: [ string ](name='digitalEmployeeName', description='The list of digital employee names. A single string can be passed for backward compatibility with the legacy format.', example='string_value'),
  directChat?: boolean(name='directChat', description='Specifies whether to enable direct chat mode. If set to true, the regular scenario routing is skipped and the direct chat scenario is entered.', example='false'),
  files?: [ 
    {
      fileId?: string(name='fileId', description='The file ID returned by uploadChatFile.

This parameter is required.', example='exampleFileId'),
      type?: string(name='type', description='The file type.', example='text'),
    }
  ](name='files', description='The list of file references. Each item is an object in which fileId is required and is returned by uploadChatFile.'),
  model?: string(name='model', description='The abstract model tier. Valid values: quick, standard, and flagship. If not specified, a new session uses standard, and an existing session retains its current tier.', example='quick'),
  reuseLastSession?: boolean(name='reuseLastSession', description='Specifies whether to reuse the most recent session of the digital employee when sessionId is not specified. This is designed for CLI scenarios. Default value: false, which creates a new session.', example='false'),
  sessionId?: string(name='sessionId', description='The session ID. If not specified, a new session is created.', example='exampleSessionId'),
  stream?: boolean(name='stream', description='Specifies whether to use streaming generation. This operation always generates backend content in streaming mode and writes it to the message stream. The value does not change the response structure.', example='true'),
  taskExecution?: {
    billingId?: string(name='billingId', description='The billing ID.', example='exampleBillingId'),
    enableWebSearch?: boolean(name='enableWebSearch', description='Specifies whether to enable web search.', example='true'),
    executionId?: string(name='executionId', description='The execution record ID.

This parameter is required.', example='exampleExecutionId'),
    operatingObjectName?: string(name='operatingObjectName', description='The digital employee name.', example='string_value'),
    skillCodes?: [ string ](name='skillCodes', description='The list of associated skill codes.', example='string_value'),
    taskId?: string(name='taskId', description='The task ID.

This parameter is required.', example='exampleTaskId'),
    taskName?: string(name='taskName', description='The task name.', example='string_value'),
    taskUnderstand?: string(name='taskUnderstand', description='The task understanding content.', example='string_value'),
    tenantId?: string(name='tenantId', description='The tenant ID to which the task belongs.', example='10000'),
    userId?: string(name='userId', description='The user ID to which the task belongs.', example='exampleUserId'),
  }(name='taskExecution', description='The task execution metadata returned by executeScheduledTask. If specified, the request is processed through the task execution pipeline.'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. If not specified, the default tenant of the caller is used.', example='10000'),
}

model SendAsyncChatMessageShrinkRequest {
  content?: string(name='content', description='The message body from the user.

This parameter is required.', example='Sample content'),
  contentType?: string(name='contentType', description='The message type. Valid values: Text and Markdown.', example='Text'),
  digitalEmployeeNameShrink?: string(name='digitalEmployeeName', description='The list of digital employee names. A single string can be passed for backward compatibility with the legacy format.', example='string_value'),
  directChat?: boolean(name='directChat', description='Specifies whether to enable direct chat mode. If set to true, the regular scenario routing is skipped and the direct chat scenario is entered.', example='false'),
  filesShrink?: string(name='files', description='The list of file references. Each item is an object in which fileId is required and is returned by uploadChatFile.'),
  model?: string(name='model', description='The abstract model tier. Valid values: quick, standard, and flagship. If not specified, a new session uses standard, and an existing session retains its current tier.', example='quick'),
  reuseLastSession?: boolean(name='reuseLastSession', description='Specifies whether to reuse the most recent session of the digital employee when sessionId is not specified. This is designed for CLI scenarios. Default value: false, which creates a new session.', example='false'),
  sessionId?: string(name='sessionId', description='The session ID. If not specified, a new session is created.', example='exampleSessionId'),
  stream?: boolean(name='stream', description='Specifies whether to use streaming generation. This operation always generates backend content in streaming mode and writes it to the message stream. The value does not change the response structure.', example='true'),
  taskExecutionShrink?: string(name='taskExecution', description='The task execution metadata returned by executeScheduledTask. If specified, the request is processed through the task execution pipeline.'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. If not specified, the default tenant of the caller is used.', example='10000'),
}

model SendAsyncChatMessageResponseBody = {
  code?: string(name='code', description='The business status code. A value of 200 indicates success. A failure returns a backend error code (ERR.* or InvalidParameter.*).', example='200'),
  message?: string(name='message', description='The error description. This value is empty when the request succeeds.', example='successful'),
  messageId?: string(name='messageId', description='The assistant message ID. Use this ID to call streamChatMessage to subscribe to the generation results.', example='3cf84d92-f273-4bb7-ab3c-52646d25ec30'),
  requestId?: string(name='requestId', description='The request trace ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  sessionCreated?: boolean(name='sessionCreated', description='Indicates whether a new session was created by this call.', example='true'),
  sessionId?: string(name='sessionId', description='The session ID. For continued sessions, this value matches the input parameter. For new sessions, this value is generated by the server.', example='bd772dcc-afab-44ad-9fb8-bca716726201'),
}

model SendAsyncChatMessageResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: SendAsyncChatMessageResponseBody(name='body'),
}

/**
 * @summary Asynchronously sends a session message.
 *
 * @description Asynchronously sends a session message.
 *
 * @param tmpReq SendAsyncChatMessageRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return SendAsyncChatMessageResponse
 */
async function sendAsyncChatMessageWithOptions(tmpReq: SendAsyncChatMessageRequest, headers: map[string]string, runtime: Util.RuntimeOptions): SendAsyncChatMessageResponse {
  Util.validateModel(tmpReq);
  var request = new SendAsyncChatMessageShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!Util.isUnset(tmpReq.digitalEmployeeName)) {
    request.digitalEmployeeNameShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.digitalEmployeeName, 'digitalEmployeeName', 'json');
  }
  if (!Util.isUnset(tmpReq.files)) {
    request.filesShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.files, 'files', 'json');
  }
  if (!Util.isUnset(tmpReq.taskExecution)) {
    request.taskExecutionShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.taskExecution, 'taskExecution', 'json');
  }
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.content)) {
    body['content'] = request.content;
  }
  if (!Util.isUnset(request.contentType)) {
    body['contentType'] = request.contentType;
  }
  if (!Util.isUnset(request.digitalEmployeeNameShrink)) {
    body['digitalEmployeeName'] = request.digitalEmployeeNameShrink;
  }
  if (!Util.isUnset(request.directChat)) {
    body['directChat'] = request.directChat;
  }
  if (!Util.isUnset(request.filesShrink)) {
    body['files'] = request.filesShrink;
  }
  if (!Util.isUnset(request.model)) {
    body['model'] = request.model;
  }
  if (!Util.isUnset(request.reuseLastSession)) {
    body['reuseLastSession'] = request.reuseLastSession;
  }
  if (!Util.isUnset(request.sessionId)) {
    body['sessionId'] = request.sessionId;
  }
  if (!Util.isUnset(request.stream)) {
    body['stream'] = request.stream;
  }
  if (!Util.isUnset(request.taskExecutionShrink)) {
    body['taskExecution'] = request.taskExecutionShrink;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'SendAsyncChatMessage',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/sendAsyncChatMessage`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Asynchronously sends a session message.
 *
 * @description Asynchronously sends a session message.
 *
 * @param request SendAsyncChatMessageRequest
 * @return SendAsyncChatMessageResponse
 */
async function sendAsyncChatMessage(request: SendAsyncChatMessageRequest): SendAsyncChatMessageResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return sendAsyncChatMessageWithOptions(request, headers, runtime);
}

model SendChatMessageRequest {
  content?: string(name='content', description='The message body from the user.

This parameter is required.', example='Sample content'),
  contentType?: string(name='contentType', description='The message type. Valid values: Text and Markdown.', example='Text'),
  digitalEmployeeName?: [ string ](name='digitalEmployeeName', description='The list of digital employee names. A single string can be passed for backward compatibility with the legacy format.', example='string_value'),
  directChat?: boolean(name='directChat', description='Specifies whether to enable direct connection mode. When set to true, the regular scenario routing is skipped and the direct conversation scenario is entered directly.', example='false'),
  files?: [ 
    {
      fileId?: string(name='fileId', description='The file ID.

This parameter is required.', example='exampleFileId'),
      type?: string(name='type', description='The element type. Valid values: text, web_search, mention, and skill.', example='text'),
    }
  ](name='files', description='The list of file references. Each item is an object, and fileId is required (returned by uploadChatFile).'),
  model?: string(name='model', description='The abstract model tier. Valid values: quick, standard, and flagship. If not specified, new sessions use standard, and existing sessions retain the current session tier.', example='quick'),
  reuseLastSession?: boolean(name='reuseLastSession', description='Specifies whether to reuse the most recent session of the digital employee when sessionId is not provided (CLI scenario). Default value: false, which creates a new session.', example='false'),
  sessionId?: string(name='sessionId', description='The session ID.', example='exampleSessionId'),
  stream?: boolean(name='stream', description='Specifies whether to use streaming output.', example='true'),
  taskExecution?: {
    billingId?: string(name='billingId', description='The billing ID.', example='exampleBillingId'),
    enableWebSearch?: boolean(name='enableWebSearch', description='Specifies whether to enable web search.', example='true'),
    executionId?: string(name='executionId', description='The execution record ID.

This parameter is required.', example='exampleExecutionId'),
    operatingObjectName?: string(name='operatingObjectName', description='The digital employee name.', example='string_value'),
    skillCodes?: [ string ](name='skillCodes', description='The list of associated skill codes.', example='string_value'),
    taskId?: string(name='taskId', description='The task ID.

This parameter is required.', example='exampleTaskId'),
    taskName?: string(name='taskName', description='The task name.', example='string_value'),
    taskUnderstand?: string(name='taskUnderstand', description='The task understanding content.', example='string_value'),
    tenantId?: string(name='tenantId', description='The tenant ID to which the task belongs.', example='10000'),
    userId?: string(name='userId', description='The user ID to which the task belongs.', example='exampleUserId'),
  }(name='taskExecution', description='The task execution metadata returned by executeScheduledTask. When provided, the request is processed through the task execution pipeline.'),
  tenantId?: string(name='tenantId', description='The effective tenant ID.', example='10000'),
}

model SendChatMessageShrinkRequest {
  content?: string(name='content', description='The message body from the user.

This parameter is required.', example='Sample content'),
  contentType?: string(name='contentType', description='The message type. Valid values: Text and Markdown.', example='Text'),
  digitalEmployeeNameShrink?: string(name='digitalEmployeeName', description='The list of digital employee names. A single string can be passed for backward compatibility with the legacy format.', example='string_value'),
  directChat?: boolean(name='directChat', description='Specifies whether to enable direct connection mode. When set to true, the regular scenario routing is skipped and the direct conversation scenario is entered directly.', example='false'),
  filesShrink?: string(name='files', description='The list of file references. Each item is an object, and fileId is required (returned by uploadChatFile).'),
  model?: string(name='model', description='The abstract model tier. Valid values: quick, standard, and flagship. If not specified, new sessions use standard, and existing sessions retain the current session tier.', example='quick'),
  reuseLastSession?: boolean(name='reuseLastSession', description='Specifies whether to reuse the most recent session of the digital employee when sessionId is not provided (CLI scenario). Default value: false, which creates a new session.', example='false'),
  sessionId?: string(name='sessionId', description='The session ID.', example='exampleSessionId'),
  stream?: boolean(name='stream', description='Specifies whether to use streaming output.', example='true'),
  taskExecutionShrink?: string(name='taskExecution', description='The task execution metadata returned by executeScheduledTask. When provided, the request is processed through the task execution pipeline.'),
  tenantId?: string(name='tenantId', description='The effective tenant ID.', example='10000'),
}

model SendChatMessageResponseBody = {
  code?: string(name='code', description='The error code.', example='200', deprecated=true),
  content?: string(name='content', description='The streaming response content.', example='yes，i\\\\"m ready'),
  data?: any(name='data', description='The SSE event stream payload. On success, the response is a text/event-stream raw frame that must be consumed frame by frame in streaming mode.', example='{}'),
  message?: string(name='message', description='The status code description.', example='successful', deprecated=true),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D', deprecated=true),
  type?: string(name='type', description='The event type.', example='think'),
}

model SendChatMessageResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: SendChatMessageResponseBody(name='body'),
}

/**
 * @summary Sends a message.
 *
 * @description ## Operation description
 * - This API is used to upload a file to the "My Resources" section of a specified digital employee.
 * - `source_type` is fixed to `FILE`, `scope` is fixed to `PERSONAL`, and `platform` is fixed to `LOCAL`.
 * - A persistent OSS address (`filePath`) must be provided for the file. Other information such as the public access URL and original file name is optional.
 * - If the target folder ID (`directoryId`) is not specified, the file is automatically attached to the default root folder of the current digital employee. If specified, ensure that the folder belongs to the invoker\\"s personal folder.
 * - Multiple authentication methods (AK, BearerToken, APP) are supported to authenticate requests.
 * - The operation type is write, and operation logs are recorded for subsequent auditing.
 *
 * @param tmpReq SendChatMessageRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return SendChatMessageResponse
 */
async function sendChatMessageWithOptions(tmpReq: SendChatMessageRequest, headers: map[string]string, runtime: Util.RuntimeOptions): SendChatMessageResponse {
  Util.validateModel(tmpReq);
  var request = new SendChatMessageShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!Util.isUnset(tmpReq.digitalEmployeeName)) {
    request.digitalEmployeeNameShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.digitalEmployeeName, 'digitalEmployeeName', 'json');
  }
  if (!Util.isUnset(tmpReq.files)) {
    request.filesShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.files, 'files', 'json');
  }
  if (!Util.isUnset(tmpReq.taskExecution)) {
    request.taskExecutionShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.taskExecution, 'taskExecution', 'json');
  }
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.content)) {
    body['content'] = request.content;
  }
  if (!Util.isUnset(request.contentType)) {
    body['contentType'] = request.contentType;
  }
  if (!Util.isUnset(request.digitalEmployeeNameShrink)) {
    body['digitalEmployeeName'] = request.digitalEmployeeNameShrink;
  }
  if (!Util.isUnset(request.directChat)) {
    body['directChat'] = request.directChat;
  }
  if (!Util.isUnset(request.filesShrink)) {
    body['files'] = request.filesShrink;
  }
  if (!Util.isUnset(request.model)) {
    body['model'] = request.model;
  }
  if (!Util.isUnset(request.reuseLastSession)) {
    body['reuseLastSession'] = request.reuseLastSession;
  }
  if (!Util.isUnset(request.sessionId)) {
    body['sessionId'] = request.sessionId;
  }
  if (!Util.isUnset(request.stream)) {
    body['stream'] = request.stream;
  }
  if (!Util.isUnset(request.taskExecutionShrink)) {
    body['taskExecution'] = request.taskExecutionShrink;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'SendChatMessage',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/sendChatMessage`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Sends a message.
 *
 * @description ## Operation description
 * - This API is used to upload a file to the "My Resources" section of a specified digital employee.
 * - `source_type` is fixed to `FILE`, `scope` is fixed to `PERSONAL`, and `platform` is fixed to `LOCAL`.
 * - A persistent OSS address (`filePath`) must be provided for the file. Other information such as the public access URL and original file name is optional.
 * - If the target folder ID (`directoryId`) is not specified, the file is automatically attached to the default root folder of the current digital employee. If specified, ensure that the folder belongs to the invoker\\"s personal folder.
 * - Multiple authentication methods (AK, BearerToken, APP) are supported to authenticate requests.
 * - The operation type is write, and operation logs are recorded for subsequent auditing.
 *
 * @param request SendChatMessageRequest
 * @return SendChatMessageResponse
 */
async function sendChatMessage(request: SendChatMessageRequest): SendChatMessageResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return sendChatMessageWithOptions(request, headers, runtime);
}

model StopChatMessageRequest {
  sessionId?: string(name='sessionId', description='The session ID.

This parameter is required.', example='exampleSessionId'),
  tenantId?: string(name='tenantId', description='The ID of the effective tenant.', example='10000'),
}

model StopChatMessageResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  finishReason?: string(name='finishReason', description='The reason for stopping.', example='string_value'),
  message?: string(name='message', description='The description of the status code.', example='successful'),
  messageId?: string(name='messageId', description='The message ID.', example='exampleMessageId'),
  partialContent?: string(name='partialContent', description='The partially generated content.', example='string_value'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  sessionId?: string(name='sessionId', description='The session ID.', example='exampleSessionId'),
  status?: string(name='status', description='The final status of the message.', example='READY'),
}

model StopChatMessageResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: StopChatMessageResponseBody(name='body'),
}

/**
 * @summary Stops conversation generation.
 *
 * @param request StopChatMessageRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return StopChatMessageResponse
 */
async function stopChatMessageWithOptions(request: StopChatMessageRequest, headers: map[string]string, runtime: Util.RuntimeOptions): StopChatMessageResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.sessionId)) {
    query['sessionId'] = request.sessionId;
  }
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'StopChatMessage',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/stopChatMessage`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Stops conversation generation.
 *
 * @param request StopChatMessageRequest
 * @return StopChatMessageResponse
 */
async function stopChatMessage(request: StopChatMessageRequest): StopChatMessageResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return stopChatMessageWithOptions(request, headers, runtime);
}

model StreamChatMessageRequest {
  lastEventId?: string(name='lastEventId', description='The last received SSE event ID, used for resuming delivery after a disconnection. If not specified, the full stream is replayed from the beginning.', example='5-1683456789012'),
  tenantId?: string(name='tenantId', description='The ID of the effective tenant.', example='10000'),
}

model StreamChatMessageResponseBody = {
  code?: string(name='code', description='The error code.', example='200'),
  data?: any(name='data', description='The SSE event stream payload. On success, the response is in text/event-stream raw frames and must be consumed frame by frame in a streaming manner.', example='{}'),
  message?: string(name='message', description='The status code description.', example='ok'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
}

model StreamChatMessageResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: StreamChatMessageResponseBody(name='body'),
}

/**
 * @summary Subscribes to a conversation message stream.
 *
 * @description Subscribes to a conversation message stream.
 *
 * @param request StreamChatMessageRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return StreamChatMessageResponse
 */
async function streamChatMessageWithOptions(messageId: string, request: StreamChatMessageRequest, headers: map[string]string, runtime: Util.RuntimeOptions): StreamChatMessageResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.lastEventId)) {
    query['lastEventId'] = request.lastEventId;
  }
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'StreamChatMessage',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/streamChatMessage/${OpenApiUtil.getEncodeParam(messageId)}`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Subscribes to a conversation message stream.
 *
 * @description Subscribes to a conversation message stream.
 *
 * @param request StreamChatMessageRequest
 * @return StreamChatMessageResponse
 */
async function streamChatMessage(messageId: string, request: StreamChatMessageRequest): StreamChatMessageResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return streamChatMessageWithOptions(messageId, request, headers, runtime);
}

model SyncOrgStructureRequest {
  corpId?: string(name='corpId', description='The enterprise identifier. This value must match the corpId returned by listAvailableConfigs.

This parameter is required.', example='exampleCorpId'),
  departments?: [ 
    {
      deptId?: string(name='deptId', description='The department ID. This is an external identifier. The client is responsible for ensuring uniqueness.', example='exampleDeptId'),
      deptName?: string(name='deptName', description='The department name.', example='string_value'),
      order?: long(name='order', description='The sort order. A smaller value indicates a higher priority.', example='1'),
      parentDeptId?: string(name='parentDeptId', description='The parent department ID. A value of null indicates a top-level department or root department.', example='exampleParentDeptId'),
    }
  ](name='departments', description='The department list. At least one root department must be included.

This parameter is required.'),
  members?: [ 
    {
      accountId?: string(name='accountId', description='The user identifier. In the SAML scenario, this is an email address or UPN, which must match rbj_user_account.account_id.', example='exampleAccountId'),
      deptId?: string(name='deptId', description='The department ID to which the member belongs. This value must correspond to a deptId in the departments list.', example='exampleDeptId'),
      name?: string(name='name', description='The username for display purposes. This parameter is optional.', example='SampleName.pdf'),
    }
  ](name='members', description='The member list. This parameter is required when syncMembers is set to true.'),
  platformType?: string(name='platformType', description='The platform type. Valid values: saml, oauth2, or custom.

This parameter is required.', example='saml'),
  ssoSettingsId?: string(name='ssoSettingsId', description='The SSO configuration ID. For SAML/OAuth2, this parameter is optional. If not specified, the value is automatically derived based on corpId. If multiple IdPs use the same corpId, you must explicitly specify this parameter. Otherwise, an AMBIGUOUS error is returned. This parameter is not required for custom.', example='exampleSsoSettingsId'),
  syncMembers?: boolean(name='syncMembers', description='Specifies whether to synchronize member relationships. In custom mode, this parameter is forced to false.', example='false'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. If not specified, the default tenant of the caller is used.', example='10000'),
}

model SyncOrgStructureShrinkRequest {
  corpId?: string(name='corpId', description='The enterprise identifier. This value must match the corpId returned by listAvailableConfigs.

This parameter is required.', example='exampleCorpId'),
  departmentsShrink?: string(name='departments', description='The department list. At least one root department must be included.

This parameter is required.'),
  membersShrink?: string(name='members', description='The member list. This parameter is required when syncMembers is set to true.'),
  platformType?: string(name='platformType', description='The platform type. Valid values: saml, oauth2, or custom.

This parameter is required.', example='saml'),
  ssoSettingsId?: string(name='ssoSettingsId', description='The SSO configuration ID. For SAML/OAuth2, this parameter is optional. If not specified, the value is automatically derived based on corpId. If multiple IdPs use the same corpId, you must explicitly specify this parameter. Otherwise, an AMBIGUOUS error is returned. This parameter is not required for custom.', example='exampleSsoSettingsId'),
  syncMembers?: boolean(name='syncMembers', description='Specifies whether to synchronize member relationships. In custom mode, this parameter is forced to false.', example='false'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. If not specified, the default tenant of the caller is used.', example='10000'),
}

model SyncOrgStructureResponseBody = {
  code?: string(name='code', description='The business status code. A value of 200 indicates success. A failure returns a backend error code (ERR.* / InvalidParameter.*).', example='200'),
  message?: string(name='message', description='The error description. This value is empty when the request is successful.', example='successful'),
  requestId?: string(name='requestId', description='The request trace ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  status?: string(name='status', description='The initial status of the task.', example='READY'),
  taskId?: long(name='taskId', description='The asynchronous synchronization task ID, used for polling the status through querySyncResult.', example='1'),
}

model SyncOrgStructureResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: SyncOrgStructureResponseBody(name='body'),
}

/**
 * @summary Pushes organizational structure synchronization.
 *
 * @description Accepts a department tree and member relationships pushed from the client and creates an asynchronous synchronization task.
 *     Processing flow:
 *     1. Validates platformType (only saml, oauth2, or custom are allowed).
 *     2. Validates data volume limits (departments + members <= 50000).
 *     3. Validates the compatibility between syncMembers and platformType.
 *     4. SAML/OAuth2 scenario: Parses or automatically derives ssoSettingsId.
 *     5. Custom scenario: Validates that corpId has been registered through createCustomOrg.
 *     6. Delegates to OrgSyncAuthorizedService to create the task (which includes permission verification).
 *     7. Returns taskId for polling.
 *
 * @param tmpReq SyncOrgStructureRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return SyncOrgStructureResponse
 */
async function syncOrgStructureWithOptions(tmpReq: SyncOrgStructureRequest, headers: map[string]string, runtime: Util.RuntimeOptions): SyncOrgStructureResponse {
  Util.validateModel(tmpReq);
  var request = new SyncOrgStructureShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!Util.isUnset(tmpReq.departments)) {
    request.departmentsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.departments, 'departments', 'json');
  }
  if (!Util.isUnset(tmpReq.members)) {
    request.membersShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.members, 'members', 'json');
  }
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.corpId)) {
    body['corpId'] = request.corpId;
  }
  if (!Util.isUnset(request.departmentsShrink)) {
    body['departments'] = request.departmentsShrink;
  }
  if (!Util.isUnset(request.membersShrink)) {
    body['members'] = request.membersShrink;
  }
  if (!Util.isUnset(request.platformType)) {
    body['platformType'] = request.platformType;
  }
  if (!Util.isUnset(request.ssoSettingsId)) {
    body['ssoSettingsId'] = request.ssoSettingsId;
  }
  if (!Util.isUnset(request.syncMembers)) {
    body['syncMembers'] = request.syncMembers;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'SyncOrgStructure',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/syncOrgStructure`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Pushes organizational structure synchronization.
 *
 * @description Accepts a department tree and member relationships pushed from the client and creates an asynchronous synchronization task.
 *     Processing flow:
 *     1. Validates platformType (only saml, oauth2, or custom are allowed).
 *     2. Validates data volume limits (departments + members <= 50000).
 *     3. Validates the compatibility between syncMembers and platformType.
 *     4. SAML/OAuth2 scenario: Parses or automatically derives ssoSettingsId.
 *     5. Custom scenario: Validates that corpId has been registered through createCustomOrg.
 *     6. Delegates to OrgSyncAuthorizedService to create the task (which includes permission verification).
 *     7. Returns taskId for polling.
 *
 * @param request SyncOrgStructureRequest
 * @return SyncOrgStructureResponse
 */
async function syncOrgStructure(request: SyncOrgStructureRequest): SyncOrgStructureResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return syncOrgStructureWithOptions(request, headers, runtime);
}

model TogglePrimaryObjectFavoriteRequest {
  action?: string(name='action', description='The operation type. Valid values:

- **1**: Add to whitelist.
- **2**: Remove from whitelist.

This parameter is required.', example='add'),
  objectIds?: [ string ](name='objectIds', description='The list of primary object business IDs.

This parameter is required.', example='string_value'),
  objectType?: string(name='objectType', description='The object type, such as customer. This parameter has a value when type is set to mention.

This parameter is required.', example='string_value'),
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital employee (operating object name, optional).

This parameter is required.', example='string_value'),
  tenantId?: string(name='tenantId', description='The ID of the effective tenant.', example='10000'),
}

model TogglePrimaryObjectFavoriteShrinkRequest {
  action?: string(name='action', description='The operation type. Valid values:

- **1**: Add to whitelist.
- **2**: Remove from whitelist.

This parameter is required.', example='add'),
  objectIdsShrink?: string(name='objectIds', description='The list of primary object business IDs.

This parameter is required.', example='string_value'),
  objectType?: string(name='objectType', description='The object type, such as customer. This parameter has a value when type is set to mention.

This parameter is required.', example='string_value'),
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital employee (operating object name, optional).

This parameter is required.', example='string_value'),
  tenantId?: string(name='tenantId', description='The ID of the effective tenant.', example='10000'),
}

model TogglePrimaryObjectFavoriteResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  favoriteCount?: long(name='favoriteCount', description='The total number of follows by the user for the specified object type.', example='1'),
  message?: string(name='message', description='The description of the status code.', example='ok'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  results?: [ 
    {
      isFavorited?: boolean(name='isFavorited', description='Indicates whether the object is followed after the operation.', example='true'),
      message?: string(name='message', description='The description of the status code.', example='string_value'),
      objectId?: string(name='objectId', description='The ID of the aligned object: target ID or KR ID.', example='exampleObjectId'),
      success?: boolean(name='success', description='Indicates whether the operation is successful.', example='true'),
    }
  ](name='results', description='The list of results.'),
}

model TogglePrimaryObjectFavoriteResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: TogglePrimaryObjectFavoriteResponseBody(name='body'),
}

/**
 * @summary Follows or unfollows a specified primary object.
 *
 * @description ## Request description
 * - **Precheck**:
 *   1. When adding a follow: The system checks whether the primary object is already followed to prevent duplicates, and authenticates that the primary object exists.
 *   2. When unfollowing: This is an idempotent operation. Regardless of whether the user has previously followed the object, `success=true` is returned.
 * - **Security**: Three authentication methods are supported: AK, BearerToken, and APP.
 * - **Request frequency limit**: A maximum of 100 requests can be send per second.
 * - **Response log**: The response log record feature is enabled.
 * - **Tenant relevance**: This API is associated with a specific tenant. The tenant ID of the invoker is used by default.
 * - **Operation type**: Write operation.
 * - **Backend service**: Requests are forwarded to an internal service for processing. The timeout period is 3 seconds.
 *
 * @param tmpReq TogglePrimaryObjectFavoriteRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return TogglePrimaryObjectFavoriteResponse
 */
async function togglePrimaryObjectFavoriteWithOptions(tmpReq: TogglePrimaryObjectFavoriteRequest, headers: map[string]string, runtime: Util.RuntimeOptions): TogglePrimaryObjectFavoriteResponse {
  Util.validateModel(tmpReq);
  var request = new TogglePrimaryObjectFavoriteShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!Util.isUnset(tmpReq.objectIds)) {
    request.objectIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.objectIds, 'objectIds', 'json');
  }
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.action)) {
    body['action'] = request.action;
  }
  if (!Util.isUnset(request.objectIdsShrink)) {
    body['objectIds'] = request.objectIdsShrink;
  }
  if (!Util.isUnset(request.objectType)) {
    body['objectType'] = request.objectType;
  }
  if (!Util.isUnset(request.operatingObjectName)) {
    body['operatingObjectName'] = request.operatingObjectName;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'TogglePrimaryObjectFavorite',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/togglePrimaryObjectFavorite`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Follows or unfollows a specified primary object.
 *
 * @description ## Request description
 * - **Precheck**:
 *   1. When adding a follow: The system checks whether the primary object is already followed to prevent duplicates, and authenticates that the primary object exists.
 *   2. When unfollowing: This is an idempotent operation. Regardless of whether the user has previously followed the object, `success=true` is returned.
 * - **Security**: Three authentication methods are supported: AK, BearerToken, and APP.
 * - **Request frequency limit**: A maximum of 100 requests can be send per second.
 * - **Response log**: The response log record feature is enabled.
 * - **Tenant relevance**: This API is associated with a specific tenant. The tenant ID of the invoker is used by default.
 * - **Operation type**: Write operation.
 * - **Backend service**: Requests are forwarded to an internal service for processing. The timeout period is 3 seconds.
 *
 * @param request TogglePrimaryObjectFavoriteRequest
 * @return TogglePrimaryObjectFavoriteResponse
 */
async function togglePrimaryObjectFavorite(request: TogglePrimaryObjectFavoriteRequest): TogglePrimaryObjectFavoriteResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return togglePrimaryObjectFavoriteWithOptions(request, headers, runtime);
}

model UpdateAgentAuthModeRequest {
  authMode?: string(name='authMode', description='The authentication mode.

This parameter is required.', example='SPECIFIED_USERS'),
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital employee (operating object name, optional).

This parameter is required.', example='string_value'),
  tenantId?: string(name='tenantId', description='The ID of the effective tenant.', example='10000'),
}

model UpdateAgentAuthModeResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  message?: string(name='message', description='The description of the status code.', example='ok'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  updated?: boolean(name='updated', description='The updated item.', example='true'),
}

model UpdateAgentAuthModeResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateAgentAuthModeResponseBody(name='body'),
}

/**
 * @summary Updates the authorization mode for digital employee usage permissions.
 *
 * @description Switches the authorization mode for digital employee usage permissions.
 *     Business logic:
 *     1. Constructs an AuthContext from the identity.
 *     2. Delegates to AgentAuthorizationAuthorizedService.update_auth_mode for execution.
 *     3. Pre-validation: MANAGE permission + agent existence check (performed by the AuthorizedService layer, which authenticates before exposing existence).
 *     4. SPECIFIED_USERS: Explicit authorization is required before usage.
 *     5. ALL_USERS: All users can use the digital employee without authorization (management permissions are not affected).
 *
 * @param request UpdateAgentAuthModeRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateAgentAuthModeResponse
 */
async function updateAgentAuthModeWithOptions(request: UpdateAgentAuthModeRequest, headers: map[string]string, runtime: Util.RuntimeOptions): UpdateAgentAuthModeResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.authMode)) {
    body['authMode'] = request.authMode;
  }
  if (!Util.isUnset(request.operatingObjectName)) {
    body['operatingObjectName'] = request.operatingObjectName;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'UpdateAgentAuthMode',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/updateAgentAuthMode`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Updates the authorization mode for digital employee usage permissions.
 *
 * @description Switches the authorization mode for digital employee usage permissions.
 *     Business logic:
 *     1. Constructs an AuthContext from the identity.
 *     2. Delegates to AgentAuthorizationAuthorizedService.update_auth_mode for execution.
 *     3. Pre-validation: MANAGE permission + agent existence check (performed by the AuthorizedService layer, which authenticates before exposing existence).
 *     4. SPECIFIED_USERS: Explicit authorization is required before usage.
 *     5. ALL_USERS: All users can use the digital employee without authorization (management permissions are not affected).
 *
 * @param request UpdateAgentAuthModeRequest
 * @return UpdateAgentAuthModeResponse
 */
async function updateAgentAuthMode(request: UpdateAgentAuthModeRequest): UpdateAgentAuthModeResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return updateAgentAuthModeWithOptions(request, headers, runtime);
}

model UpdateChatSessionRequest {
  model?: string(name='model', description='The abstract model name (model tier). If not specified, the current model of the session is not modified.', example='quick'),
  sessionId?: string(name='sessionId', description='The session ID.

This parameter is required.', example='exampleSessionId'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. If not specified, the default tenant of the caller is used.', example='10000'),
  title?: string(name='title', description='The new session title.', example='Sample title'),
}

model UpdateChatSessionResponseBody = {
  code?: string(name='code', description='The business status code. A value of 200 indicates success. A failure returns a backend error code (ERR.* / InvalidParameter.*).', example='200'),
  message?: string(name='message', description='The error description. This is empty when the request is successful.', example='successful'),
  requestId?: string(name='requestId', description='The request trace ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  session?: {
    createdAt?: long(name='createdAt', description='The creation time.', example='1'),
    id?: string(name='id', description='The message ID.', example='exampleId'),
    isExpired?: boolean(name='isExpired', description='Indicates whether the creation time exceeds 30 days.', example='true'),
    metadata?: map[string]any(name='metadata', description='The associated object ID.', example='exampleObjectId'),
    model?: string(name='model', description='The abstract model name used by the session (quick/standard/flagship).', example='string_value'),
    object?: string(name='object', description='The type.', example='string_value'),
    objectId?: string(name='objectId', description='The associated object ID.', example='2676'),
    operatingObjectName?: [ string ](name='operatingObjectName', description='The operating object name.', example='string_value'),
    title?: string(name='title', description='The title.', example='Sample title'),
    updatedAt?: long(name='updatedAt', description='The update time.', example='1'),
  }(name='session', description='The session ID.'),
}

model UpdateChatSessionResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateChatSessionResponseBody(name='body'),
}

/**
 * @summary Updates a session.
 *
 * @description Updates a session.
 *
 * @param request UpdateChatSessionRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateChatSessionResponse
 */
async function updateChatSessionWithOptions(request: UpdateChatSessionRequest, headers: map[string]string, runtime: Util.RuntimeOptions): UpdateChatSessionResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.model)) {
    body['model'] = request.model;
  }
  if (!Util.isUnset(request.sessionId)) {
    body['sessionId'] = request.sessionId;
  }
  if (!Util.isUnset(request.title)) {
    body['title'] = request.title;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'UpdateChatSession',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/updateChatSession`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Updates a session.
 *
 * @description Updates a session.
 *
 * @param request UpdateChatSessionRequest
 * @return UpdateChatSessionResponse
 */
async function updateChatSession(request: UpdateChatSessionRequest): UpdateChatSessionResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return updateChatSessionWithOptions(request, headers, runtime);
}

model UpdateDirectoryRequest {
  description?: string(name='description', description='The description of the to-do card type.', example='Sample description'),
  directoryId?: string(name='directoryId', description='The directory ID.

This parameter is required.', example='exampleDirectoryId'),
  name?: string(name='name', description='The name.', example='SampleName.pdf'),
  parentId?: long(name='parentId', description='The ID of the parent node.', example='1'),
  path?: string(name='path', description='The path of the node.', example='https://example.com/oss/file.pdf'),
  tenantId?: string(name='tenantId', description='The tenant ID.', example='10000'),
}

model UpdateDirectoryResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  directoryId?: string(name='directoryId', description='The directory ID.', example='exampleDirectoryId'),
  message?: string(name='message', description='The description of the status code.', example='ok'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  success?: boolean(name='success', description='Indicates whether the API call is successful. Valid values:
- true: The call is successful.
- false: The call fails.', example='true'),
}

model UpdateDirectoryResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateDirectoryResponseBody(name='body'),
}

/**
 * @summary Updates the basic information of a user\\"s personal directory.
 *
 * @description ## Operation description
 * This API operation updates the personal directory information of a specified user, including the name, description, and parent directory. Ensure that the directory corresponding to the provided `directoryId` exists and belongs to the current user. If the `name` or `path` of the directory is changed, the system automatically and recursively updates the paths of all subdirectories to maintain consistency. When adjusting the parent directory, ensure the validity of the new parent directory (that is, it is not the directory itself and does not cause a circular dependency).
 * - **Security constraints**: `tenant_id` and `user_id` must be derived from the authenticated identity.
 * - **Permission requirements**: Corresponding RAM permissions are required to perform this operation.
 * - **Input parameters**:
 *   - `directoryId`: Required. The unique identifier of the directory to update.
 *   - `name`: Optional. The new directory name.
 *   - `description`: Optional. The new directory description.
 *   - `parentId`: Optional. The ID of the new parent directory.
 *   - `path`: Optional. When specified, the system cascades the update to the paths of the current directory and all its subdirectories.
 *
 * @param request UpdateDirectoryRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateDirectoryResponse
 */
async function updateDirectoryWithOptions(request: UpdateDirectoryRequest, headers: map[string]string, runtime: Util.RuntimeOptions): UpdateDirectoryResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.description)) {
    body['description'] = request.description;
  }
  if (!Util.isUnset(request.directoryId)) {
    body['directoryId'] = request.directoryId;
  }
  if (!Util.isUnset(request.name)) {
    body['name'] = request.name;
  }
  if (!Util.isUnset(request.parentId)) {
    body['parentId'] = request.parentId;
  }
  if (!Util.isUnset(request.path)) {
    body['path'] = request.path;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'UpdateDirectory',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/updateDirectory`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Updates the basic information of a user\\"s personal directory.
 *
 * @description ## Operation description
 * This API operation updates the personal directory information of a specified user, including the name, description, and parent directory. Ensure that the directory corresponding to the provided `directoryId` exists and belongs to the current user. If the `name` or `path` of the directory is changed, the system automatically and recursively updates the paths of all subdirectories to maintain consistency. When adjusting the parent directory, ensure the validity of the new parent directory (that is, it is not the directory itself and does not cause a circular dependency).
 * - **Security constraints**: `tenant_id` and `user_id` must be derived from the authenticated identity.
 * - **Permission requirements**: Corresponding RAM permissions are required to perform this operation.
 * - **Input parameters**:
 *   - `directoryId`: Required. The unique identifier of the directory to update.
 *   - `name`: Optional. The new directory name.
 *   - `description`: Optional. The new directory description.
 *   - `parentId`: Optional. The ID of the new parent directory.
 *   - `path`: Optional. When specified, the system cascades the update to the paths of the current directory and all its subdirectories.
 *
 * @param request UpdateDirectoryRequest
 * @return UpdateDirectoryResponse
 */
async function updateDirectory(request: UpdateDirectoryRequest): UpdateDirectoryResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return updateDirectoryWithOptions(request, headers, runtime);
}

model UpdateKnowledgeBaseDirectoryRequest {
  description?: string(name='description', description='The description of the to-do card type.', example='Sample description'),
  directoryId?: string(name='directoryId', description='The directory ID.

This parameter is required.', example='exampleDirectoryId'),
  name?: string(name='name', description='The name.', example='SampleName.pdf'),
  parentDirectoryId?: string(name='parentDirectoryId', description='The directory ID.', example='exampleParentDirectoryId'),
  tenantId?: string(name='tenantId', description='The tenant ID to take effect.', example='10000'),
}

model UpdateKnowledgeBaseDirectoryResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  directoryId?: string(name='directoryId', description='The category ID (echoed from the input parameter).', example='exampleDirectoryId'),
  message?: string(name='message', description='The description of the status code.', example='ok'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
}

model UpdateKnowledgeBaseDirectoryResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateKnowledgeBaseDirectoryResponseBody(name='body'),
}

/**
 * @summary Updates the information of a specified enterprise knowledge base directory, including the name, description, and parent directory.
 *
 * @description ## Operation description
 * - This operation modifies a directory in the enterprise knowledge base.
 * - You must have the `DEVELOPMENT_KB_MANAGE` permission to call this API operation.
 * - The `tenantId` parameter is optional. If not provided, the tenant ID of the caller is used by default.
 * - You must specify the `directoryId` of the directory to modify. The `name`, `description`, and `parentDirectoryId` parameters are optional. If not provided, the corresponding fields remain unchanged.
 * - When a new `parentDirectoryId` is specified, the system checks whether the new parent directory belongs to the current tenant and does not cause a circular dependency.
 * - This API operation supports multiple authentication methods (AK, BearerToken, APP) and has RAM permission control and operation auditing enabled.
 *
 * @param request UpdateKnowledgeBaseDirectoryRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateKnowledgeBaseDirectoryResponse
 */
async function updateKnowledgeBaseDirectoryWithOptions(request: UpdateKnowledgeBaseDirectoryRequest, headers: map[string]string, runtime: Util.RuntimeOptions): UpdateKnowledgeBaseDirectoryResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.description)) {
    body['description'] = request.description;
  }
  if (!Util.isUnset(request.directoryId)) {
    body['directoryId'] = request.directoryId;
  }
  if (!Util.isUnset(request.name)) {
    body['name'] = request.name;
  }
  if (!Util.isUnset(request.parentDirectoryId)) {
    body['parentDirectoryId'] = request.parentDirectoryId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'UpdateKnowledgeBaseDirectory',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/updateKnowledgeBaseDirectory`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Updates the information of a specified enterprise knowledge base directory, including the name, description, and parent directory.
 *
 * @description ## Operation description
 * - This operation modifies a directory in the enterprise knowledge base.
 * - You must have the `DEVELOPMENT_KB_MANAGE` permission to call this API operation.
 * - The `tenantId` parameter is optional. If not provided, the tenant ID of the caller is used by default.
 * - You must specify the `directoryId` of the directory to modify. The `name`, `description`, and `parentDirectoryId` parameters are optional. If not provided, the corresponding fields remain unchanged.
 * - When a new `parentDirectoryId` is specified, the system checks whether the new parent directory belongs to the current tenant and does not cause a circular dependency.
 * - This API operation supports multiple authentication methods (AK, BearerToken, APP) and has RAM permission control and operation auditing enabled.
 *
 * @param request UpdateKnowledgeBaseDirectoryRequest
 * @return UpdateKnowledgeBaseDirectoryResponse
 */
async function updateKnowledgeBaseDirectory(request: UpdateKnowledgeBaseDirectoryRequest): UpdateKnowledgeBaseDirectoryResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return updateKnowledgeBaseDirectoryWithOptions(request, headers, runtime);
}

model UpdateKnowledgeBaseSourceContentRequest {
  content?: string(name='content', description='Specifies whether the operation is successful. A value of true indicates success.

This parameter is required.', example='Sample content'),
  forceSync?: boolean(name='forceSync', description='Specifies whether to force synchronous processing.', example='false'),
  sourceId?: string(name='sourceId', description='The source ID.

This parameter is required.', example='exampleSourceId'),
  tenantId?: string(name='tenantId', description='The tenant ID that takes effect.', example='10000'),
}

model UpdateKnowledgeBaseSourceContentResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  message?: string(name='message', description='The description of the status code.', example='successful'),
  name?: string(name='name', description='The name.

This parameter is required.', example='SampleName.pdf'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  sourceId?: string(name='sourceId', description='The unique identifier on the business system side, that is, the business ID.

This parameter is required.', example='exampleSourceId'),
  sourceType?: string(name='sourceType', description='The data source type.

This parameter is required.', example='string_value'),
  status?: string(name='status', description='The task running status.

This parameter is required.', example='READY'),
}

model UpdateKnowledgeBaseSourceContentResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateKnowledgeBaseSourceContentResponseBody(name='body'),
}

/**
 * @summary Edits the body content of a resource in an enterprise self-built knowledge base and triggers re-parsing.
 *
 * @description ## Request description
 * This operation allows you to update the body content of a specified enterprise knowledge base data source and optionally wait synchronously for parsing to complete. By setting the `forceSync` parameter, you can control whether the parsing process is executed synchronously or asynchronously. The default is asynchronous processing.
 * - **Note**: When the `content` field is an empty string, the original content is cleared.
 * - **Permission requirement**: Calling this operation requires the corresponding RAM action permission (`winnexo:UpdateKnowledgeBaseSourceContent`).
 *
 * @param request UpdateKnowledgeBaseSourceContentRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateKnowledgeBaseSourceContentResponse
 */
async function updateKnowledgeBaseSourceContentWithOptions(request: UpdateKnowledgeBaseSourceContentRequest, headers: map[string]string, runtime: Util.RuntimeOptions): UpdateKnowledgeBaseSourceContentResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.content)) {
    body['content'] = request.content;
  }
  if (!Util.isUnset(request.forceSync)) {
    body['forceSync'] = request.forceSync;
  }
  if (!Util.isUnset(request.sourceId)) {
    body['sourceId'] = request.sourceId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'UpdateKnowledgeBaseSourceContent',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/updateKnowledgeBaseSourceContent`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Edits the body content of a resource in an enterprise self-built knowledge base and triggers re-parsing.
 *
 * @description ## Request description
 * This operation allows you to update the body content of a specified enterprise knowledge base data source and optionally wait synchronously for parsing to complete. By setting the `forceSync` parameter, you can control whether the parsing process is executed synchronously or asynchronously. The default is asynchronous processing.
 * - **Note**: When the `content` field is an empty string, the original content is cleared.
 * - **Permission requirement**: Calling this operation requires the corresponding RAM action permission (`winnexo:UpdateKnowledgeBaseSourceContent`).
 *
 * @param request UpdateKnowledgeBaseSourceContentRequest
 * @return UpdateKnowledgeBaseSourceContentResponse
 */
async function updateKnowledgeBaseSourceContent(request: UpdateKnowledgeBaseSourceContentRequest): UpdateKnowledgeBaseSourceContentResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return updateKnowledgeBaseSourceContentWithOptions(request, headers, runtime);
}

model UpdateKnowledgeBaseSourceTagsRequest {
  sourceId?: string(name='sourceId', description='The unique identifier on the business system side, that is, the business ID.

This parameter is required.', example='exampleSourceId'),
  sourceTags?: string(name='sourceTags', description='The resource tags. This is an optional parameter that accepts a JSON string list, such as ["tagA","tagB"].', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID.', example='10000'),
}

model UpdateKnowledgeBaseSourceTagsResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  gmtModified?: string(name='gmtModified', description='The last modified time in ISO 8601 format.', example='string_value'),
  message?: string(name='message', description='The description of the status code.', example='ok'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  sourceId?: string(name='sourceId', description='The source ID.', example='exampleSourceId'),
  sourceTags?: string(name='sourceTags', description='The updated resource tags.', example='string_value'),
}

model UpdateKnowledgeBaseSourceTagsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateKnowledgeBaseSourceTagsResponseBody(name='body'),
}

/**
 * @summary Updates the resource tags of a specified data source in an enterprise knowledge base.
 *
 * @description ## Request description
 * - This operation updates the labels of a specific data source in an enterprise knowledge base.
 * - You must have knowledge base management permissions to invoke this operation.
 * - The `sourceTags` parameter accepts a JSON character string list, such as `["tagA", "tagB"]`. If you set this parameter to `null`, all existing labels are cleared.
 * - The update operation affects only the `sourceTags` and `gmt_modified` fields and does not trigger the `process_source` workflow.
 * - If the specified data source does not exist, the `ERR.Robject.Source.NotFound` fault is returned.
 * - This operation supports authentication through AccessKey, BearerToken, or APP methods.
 * - When you invoke this operation, make sure that `tenant_id` and `user_id` are from valid authentication identity information.
 *
 * @param request UpdateKnowledgeBaseSourceTagsRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateKnowledgeBaseSourceTagsResponse
 */
async function updateKnowledgeBaseSourceTagsWithOptions(request: UpdateKnowledgeBaseSourceTagsRequest, headers: map[string]string, runtime: Util.RuntimeOptions): UpdateKnowledgeBaseSourceTagsResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.sourceId)) {
    body['sourceId'] = request.sourceId;
  }
  if (!Util.isUnset(request.sourceTags)) {
    body['sourceTags'] = request.sourceTags;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'UpdateKnowledgeBaseSourceTags',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/updateKnowledgeBaseSourceTags`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Updates the resource tags of a specified data source in an enterprise knowledge base.
 *
 * @description ## Request description
 * - This operation updates the labels of a specific data source in an enterprise knowledge base.
 * - You must have knowledge base management permissions to invoke this operation.
 * - The `sourceTags` parameter accepts a JSON character string list, such as `["tagA", "tagB"]`. If you set this parameter to `null`, all existing labels are cleared.
 * - The update operation affects only the `sourceTags` and `gmt_modified` fields and does not trigger the `process_source` workflow.
 * - If the specified data source does not exist, the `ERR.Robject.Source.NotFound` fault is returned.
 * - This operation supports authentication through AccessKey, BearerToken, or APP methods.
 * - When you invoke this operation, make sure that `tenant_id` and `user_id` are from valid authentication identity information.
 *
 * @param request UpdateKnowledgeBaseSourceTagsRequest
 * @return UpdateKnowledgeBaseSourceTagsResponse
 */
async function updateKnowledgeBaseSourceTags(request: UpdateKnowledgeBaseSourceTagsRequest): UpdateKnowledgeBaseSourceTagsResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return updateKnowledgeBaseSourceTagsWithOptions(request, headers, runtime);
}

model UpdateScheduledTaskRequest {
  description?: [ 
    {
      content?: string(name='content', description='The text content. Required when type is set to text.', example='Sample content'),
      enabled?: boolean(name='enabled', description='The feature switch. Optional when type is set to web_search.', example='true'),
      name?: string(name='name', description='The file name.', example='SampleName.pdf'),
      objectId?: string(name='objectId', description='The object ID. This parameter has a value when type is set to mention.', example='exampleObjectId'),
      objectType?: string(name='objectType', description='The object type, such as customer. This parameter has a value when type is set to mention.', example='string_value'),
      skillCode?: string(name='skillCode', description='The skill code. This parameter has a value when type is set to skill.', example='string_value'),
      type?: string(name='type', description='The element type. Valid values: text, web_search, mention, and skill.

This parameter is required.', example='text'),
    }
  ](name='description', description='The description information.'),
  digitalEmployeeName?: [ string ](name='digitalEmployeeName', description='The list of digital human names.', example='string_value'),
  isOpen?: boolean(name='isOpen', description='Specifies whether the task is publicly accessible.', example='true'),
  model?: string(name='model', description='The execution model tier. If not specified, the model tier is not updated.', example='quick'),
  name?: string(name='name', description='The file name.', example='SampleName.pdf'),
  segments?: [ 
    {
      content?: string(name='content', description='The text content. Required when type is set to text.', example='Sample content'),
      enabled?: boolean(name='enabled', description='The feature switch. Optional when type is set to web_search.', example='true'),
      name?: string(name='name', description='The file name.', example='SampleName.pdf'),
      objectId?: string(name='objectId', description='The object ID. This parameter has a value when type is set to mention.', example='exampleObjectId'),
      objectType?: string(name='objectType', description='The object type, such as customer. This parameter has a value when type is set to mention.', example='string_value'),
      skillCode?: string(name='skillCode', description='The skill code. This parameter has a value when type is set to skill.', example='string_value'),
      type?: string(name='type', description='The element type. Valid values: text, web_search, mention, and skill.

This parameter is required.', example='text'),
    }
  ](name='segments', description='The segments.'),
  taskDetail?: {
    relatedObjects?: [ 
      {
        mentionType?: string(name='mentionType', description='The mention type, such as objects.', example='string_value'),
        name?: string(name='name', description='The file name.', example='SampleName.pdf'),
        objectId?: string(name='objectId', description='The object ID. This parameter has a value when an object is mentioned using @.', example='exampleObjectId'),
        objectType?: string(name='objectType', description='The object type, such as customer or company.', example='string_value'),
      }
    ](name='relatedObjects', description='The related objects.'),
    relatedSemantics?: [ 
      {
        attributes?: string(name='attributes', description='The semantic attributes (JSON string) used for filtering during semantic retrieval.', example='{"level": "VIP"}'),
        entity?: string(name='entity', description='The semantic entity name, such as customer or opportunity.', example='customer'),
      }
    ](name='relatedSemantics', description='The related semantics.'),
    relatedSkills?: [ 
      {
        displayName?: string(name='displayName', description='The display name of the skill.', example='string_value'),
        name?: string(name='name', description='The file name.', example='SampleName.pdf'),
        skillCode?: string(name='skillCode', description='The skill code.', example='string_value'),
        sourceIds?: [ string ](name='sourceIds', description='sourceIds', example='string_value'),
      }
    ](name='relatedSkills', description='The related skills.'),
    taskUnderstand?: string(name='taskUnderstand', description='The task understanding description polished by the LLM.', example='string_value'),
  }(name='taskDetail', description='The task details.'),
  taskId?: string(name='taskId', description='The task ID.

This parameter is required.', example='exampleTaskId'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. If not specified, the default tenant of the caller is used.', example='10000'),
  triggerConfig?: {
    cron?: string(name='cron', description='The cron expression. Required when trigger_mode is set to scheduled. Example: \\\\"00 09 * * *\\\\".', example='string_value'),
    language?: string(name='language', description='The language, such as zh-CN or en-US. Automatically injected by the server.', example='zh-CN'),
    pushConfig?: [ 
      {
        channelType?: string(name='channelType', description='The push channel type.', example='DINGTALK'),
        contentScope?: string(name='contentScope', description='The scope of push content. Default value: all_replies.', example='all_replies'),
        deliveryMethod?: string(name='deliveryMethod', description='The push method. Default value: channel_bot.', example='channel_bot'),
        enabled?: boolean(name='enabled', description='Specifies whether to push to this channel. Default value: false.', example='true'),
        fileFormat?: string(name='fileFormat', description='The format for pushing output files. Default value: file.', example='file'),
        operatingObjectName?: string(name='operatingObjectName', description='The digital human to which the sending bot belongs. This parameter is required and cannot be empty.', example='string_value'),
        receiverType?: string(name='receiverType', description='The receiver type. Currently only self is supported.', example='string_value'),
      }
    ](name='pushConfig', description='The list of push channels for the task. No push notifications are sent if the list is empty or no channel is enabled.'),
    timezone?: string(name='timezone', description='The time zone, such as Asia/Shanghai. Automatically injected by the server.', example='Asia/Shanghai'),
    triggerMode?: string(name='triggerMode', description='The trigger mode. Valid values: manual and scheduled.

This parameter is required.', example='manual'),
  }(name='triggerConfig', description='The trigger configuration. The configuration varies depending on the trigger type.'),
  visibility?: string(name='visibility', description='The visibility scope for group tasks. Valid values: PRIVATE (visible only to the creator and group owner), COLLABORATIVE (visible to specified collaborators), and PUBLIC (visible to all group members). If not specified, the visibility is not updated. This parameter is ignored for personal tasks.', example='COLLABORATIVE'),
  visibleMemberUserIds?: [ string ](name='visibleMemberUserIds', description='The full replacement list of collaborator member user IDs. This parameter takes effect only when visibility is set to COLLABORATIVE. The list is cleared when switching away from the COLLABORATIVE tier. A maximum of 1000 members are supported. If not specified, the member list is not updated. The task creator and group creator do not need to be included because they are covered by the authentication layer. This parameter is ignored for personal tasks.', example='string_value'),
}

model UpdateScheduledTaskShrinkRequest {
  descriptionShrink?: string(name='description', description='The description information.'),
  digitalEmployeeNameShrink?: string(name='digitalEmployeeName', description='The list of digital human names.', example='string_value'),
  isOpen?: boolean(name='isOpen', description='Specifies whether the task is publicly accessible.', example='true'),
  model?: string(name='model', description='The execution model tier. If not specified, the model tier is not updated.', example='quick'),
  name?: string(name='name', description='The file name.', example='SampleName.pdf'),
  segmentsShrink?: string(name='segments', description='The segments.'),
  taskDetailShrink?: string(name='taskDetail', description='The task details.'),
  taskId?: string(name='taskId', description='The task ID.

This parameter is required.', example='exampleTaskId'),
  tenantId?: string(name='tenantId', description='The tenant ID. This is a common parameter. If not specified, the default tenant of the caller is used.', example='10000'),
  triggerConfigShrink?: string(name='triggerConfig', description='The trigger configuration. The configuration varies depending on the trigger type.'),
  visibility?: string(name='visibility', description='The visibility scope for group tasks. Valid values: PRIVATE (visible only to the creator and group owner), COLLABORATIVE (visible to specified collaborators), and PUBLIC (visible to all group members). If not specified, the visibility is not updated. This parameter is ignored for personal tasks.', example='COLLABORATIVE'),
  visibleMemberUserIdsShrink?: string(name='visibleMemberUserIds', description='The full replacement list of collaborator member user IDs. This parameter takes effect only when visibility is set to COLLABORATIVE. The list is cleared when switching away from the COLLABORATIVE tier. A maximum of 1000 members are supported. If not specified, the member list is not updated. The task creator and group creator do not need to be included because they are covered by the authentication layer. This parameter is ignored for personal tasks.', example='string_value'),
}

model UpdateScheduledTaskResponseBody = {
  code?: string(name='code', description='The business status code. A value of 200 indicates success. A failure returns a backend error code (ERR.* or InvalidParameter.*).', example='200'),
  message?: string(name='message', description='The error description. Empty when the request is successful.', example='ok'),
  requestId?: string(name='requestId', description='The request trace ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  taskId?: string(name='taskId', description='The task ID (echoed back).', example='exampleTaskId'),
  updated?: boolean(name='updated', description='Indicates whether an actual update was made.', example='true'),
}

model UpdateScheduledTaskResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateScheduledTaskResponseBody(name='body'),
}

/**
 * @summary Updates a scheduled task.
 *
 * @description Updates a scheduled task.
 *
 * @param tmpReq UpdateScheduledTaskRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateScheduledTaskResponse
 */
async function updateScheduledTaskWithOptions(tmpReq: UpdateScheduledTaskRequest, headers: map[string]string, runtime: Util.RuntimeOptions): UpdateScheduledTaskResponse {
  Util.validateModel(tmpReq);
  var request = new UpdateScheduledTaskShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!Util.isUnset(tmpReq.description)) {
    request.descriptionShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.description, 'description', 'json');
  }
  if (!Util.isUnset(tmpReq.digitalEmployeeName)) {
    request.digitalEmployeeNameShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.digitalEmployeeName, 'digitalEmployeeName', 'json');
  }
  if (!Util.isUnset(tmpReq.segments)) {
    request.segmentsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.segments, 'segments', 'json');
  }
  if (!Util.isUnset(tmpReq.taskDetail)) {
    request.taskDetailShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.taskDetail, 'taskDetail', 'json');
  }
  if (!Util.isUnset(tmpReq.triggerConfig)) {
    request.triggerConfigShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.triggerConfig, 'triggerConfig', 'json');
  }
  if (!Util.isUnset(tmpReq.visibleMemberUserIds)) {
    request.visibleMemberUserIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.visibleMemberUserIds, 'visibleMemberUserIds', 'json');
  }
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.descriptionShrink)) {
    body['description'] = request.descriptionShrink;
  }
  if (!Util.isUnset(request.digitalEmployeeNameShrink)) {
    body['digitalEmployeeName'] = request.digitalEmployeeNameShrink;
  }
  if (!Util.isUnset(request.isOpen)) {
    body['isOpen'] = request.isOpen;
  }
  if (!Util.isUnset(request.model)) {
    body['model'] = request.model;
  }
  if (!Util.isUnset(request.name)) {
    body['name'] = request.name;
  }
  if (!Util.isUnset(request.segmentsShrink)) {
    body['segments'] = request.segmentsShrink;
  }
  if (!Util.isUnset(request.taskDetailShrink)) {
    body['taskDetail'] = request.taskDetailShrink;
  }
  if (!Util.isUnset(request.taskId)) {
    body['taskId'] = request.taskId;
  }
  if (!Util.isUnset(request.triggerConfigShrink)) {
    body['triggerConfig'] = request.triggerConfigShrink;
  }
  if (!Util.isUnset(request.visibility)) {
    body['visibility'] = request.visibility;
  }
  if (!Util.isUnset(request.visibleMemberUserIdsShrink)) {
    body['visibleMemberUserIds'] = request.visibleMemberUserIdsShrink;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'UpdateScheduledTask',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/updateScheduledTask`,
    method = 'PUT',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Updates a scheduled task.
 *
 * @description Updates a scheduled task.
 *
 * @param request UpdateScheduledTaskRequest
 * @return UpdateScheduledTaskResponse
 */
async function updateScheduledTask(request: UpdateScheduledTaskRequest): UpdateScheduledTaskResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return updateScheduledTaskWithOptions(request, headers, runtime);
}

model UpdateSourceContentRequest {
  content?: string(name='content', description='The returned content.

This parameter is required.', example='Sample content'),
  forceSync?: boolean(name='forceSync', description='Specifies whether to force synchronous processing.', example='false'),
  sourceId?: string(name='sourceId', description='The ID of the data source.

This parameter is required.', example='exampleSourceId'),
  tenantId?: string(name='tenantId', description='The ID of the effective tenant.', example='10000'),
}

model UpdateSourceContentResponseBody = {
  code?: string(name='code', description='The error code.', example='200'),
  message?: string(name='message', description='The description of the status code.', example='ok'),
  name?: string(name='name', description='The username.

This parameter is required.', example='SampleName.pdf'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  sourceId?: string(name='sourceId', description='The ID of the data source.

This parameter is required.', example='exampleSourceId'),
  sourceType?: string(name='sourceType', description='The source type of the dictionary file. Valid values:
- OSS: Object Storage Service (OSS).
- ORIGIN: retains the previously uploaded dictionary.

This parameter is required.', example='string_value'),
  status?: string(name='status', description='The status.

This parameter is required.', example='READY'),
}

model UpdateSourceContentResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateSourceContentResponseBody(name='body'),
}

/**
 * @summary Updates the content of an editable data source within a tenant and triggers re-parsing.
 *
 * @description ## Operation description
 * - This API operation updates the content of a data source within a specified tenant and triggers synchronous or asynchronous re-parsing of the data source as needed.
 * - `tenant_id` and `user_id` are used only for authentication and are not involved in actual business logic processing.
 * - When the provided content is an empty string, the system performs the operation according to the existing service contract.
 * - If the specified data source does not exist, a standard NotFound error is returned. Other exceptions are handled by the global exception chain.
 * - Set the `forceSync` parameter to determine whether to wait for the parsing process to complete. The default behavior is asynchronous queuing.
 *
 * @param request UpdateSourceContentRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateSourceContentResponse
 */
async function updateSourceContentWithOptions(request: UpdateSourceContentRequest, headers: map[string]string, runtime: Util.RuntimeOptions): UpdateSourceContentResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.content)) {
    body['content'] = request.content;
  }
  if (!Util.isUnset(request.forceSync)) {
    body['forceSync'] = request.forceSync;
  }
  if (!Util.isUnset(request.sourceId)) {
    body['sourceId'] = request.sourceId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'UpdateSourceContent',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/updateSourceContent`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Updates the content of an editable data source within a tenant and triggers re-parsing.
 *
 * @description ## Operation description
 * - This API operation updates the content of a data source within a specified tenant and triggers synchronous or asynchronous re-parsing of the data source as needed.
 * - `tenant_id` and `user_id` are used only for authentication and are not involved in actual business logic processing.
 * - When the provided content is an empty string, the system performs the operation according to the existing service contract.
 * - If the specified data source does not exist, a standard NotFound error is returned. Other exceptions are handled by the global exception chain.
 * - Set the `forceSync` parameter to determine whether to wait for the parsing process to complete. The default behavior is asynchronous queuing.
 *
 * @param request UpdateSourceContentRequest
 * @return UpdateSourceContentResponse
 */
async function updateSourceContent(request: UpdateSourceContentRequest): UpdateSourceContentResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return updateSourceContentWithOptions(request, headers, runtime);
}

model UpdateTenantDirectoryRequest {
  description?: string(name='description', description='The folder description.', example='Sample description'),
  directoryId?: string(name='directoryId', description='The folder ID.', example='exampleDirectoryId'),
  name?: string(name='name', description='The folder name.', example='SampleName.pdf'),
  parentId?: long(name='parentId', description='The parent label ID. Set this parameter to -**1** for the root folder.', example='1'),
  path?: string(name='path', description='The path of the node.', example='https://example.com/oss/file.pdf'),
  tenantId?: string(name='tenantId', description='The ID of the effective tenant.', example='10000'),
}

model UpdateTenantDirectoryResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  directoryId?: string(name='directoryId', description='The folder ID.', example='exampleDirectoryId'),
  message?: string(name='message', description='The description of the status code.', example='ok'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
}

model UpdateTenantDirectoryResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateTenantDirectoryResponseBody(name='body'),
}

/**
 * @summary Modifies knowledge base folder information.
 *
 * @description Modifies knowledge base folder information.
 *
 * @param request UpdateTenantDirectoryRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateTenantDirectoryResponse
 */
async function updateTenantDirectoryWithOptions(request: UpdateTenantDirectoryRequest, headers: map[string]string, runtime: Util.RuntimeOptions): UpdateTenantDirectoryResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.description)) {
    body['description'] = request.description;
  }
  if (!Util.isUnset(request.directoryId)) {
    body['directoryId'] = request.directoryId;
  }
  if (!Util.isUnset(request.name)) {
    body['name'] = request.name;
  }
  if (!Util.isUnset(request.parentId)) {
    body['parentId'] = request.parentId;
  }
  if (!Util.isUnset(request.path)) {
    body['path'] = request.path;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'UpdateTenantDirectory',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/updateTenantDirectory`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Modifies knowledge base folder information.
 *
 * @description Modifies knowledge base folder information.
 *
 * @param request UpdateTenantDirectoryRequest
 * @return UpdateTenantDirectoryResponse
 */
async function updateTenantDirectory(request: UpdateTenantDirectoryRequest): UpdateTenantDirectoryResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return updateTenantDirectoryWithOptions(request, headers, runtime);
}

model UpdateUserRequest {
  displayName?: string(name='displayName', description='The display name of the user.', example='string_value'),
  isActive?: boolean(name='isActive', description='Specifies whether the account is activated. Valid values:
 - **true**: Activated.
- **false**: Not activated.', example='true'),
  roleCodes?: [ string ](name='roleCodes', description='The new list of system role codes (full replacement, must contain at least one role). Valid values: SUPER_ADMIN / SYSTEM_ADMIN / SEMANTIC_ADMIN / SKILL_ADMIN / KB_ADMIN / AGENT_ADMIN / APPLICATION_USER.', example='string_value'),
  tenantId?: string(name='tenantId', description='The ID of the effective tenant.', example='10000'),
  userGroupIds?: [ string ](name='userGroupIds', description='The new list of user group IDs (full replacement. If not specified, the value is not modified).', example='string_value'),
  wnUserId?: string(name='wnUserId', description='The ID of the target user (WINNEXO platform user ID).

This parameter is required.', example='1'),
}

model UpdateUserShrinkRequest {
  displayName?: string(name='displayName', description='The display name of the user.', example='string_value'),
  isActive?: boolean(name='isActive', description='Specifies whether the account is activated. Valid values:
 - **true**: Activated.
- **false**: Not activated.', example='true'),
  roleCodesShrink?: string(name='roleCodes', description='The new list of system role codes (full replacement, must contain at least one role). Valid values: SUPER_ADMIN / SYSTEM_ADMIN / SEMANTIC_ADMIN / SKILL_ADMIN / KB_ADMIN / AGENT_ADMIN / APPLICATION_USER.', example='string_value'),
  tenantId?: string(name='tenantId', description='The ID of the effective tenant.', example='10000'),
  userGroupIdsShrink?: string(name='userGroupIds', description='The new list of user group IDs (full replacement. If not specified, the value is not modified).', example='string_value'),
  wnUserId?: string(name='wnUserId', description='The ID of the target user (WINNEXO platform user ID).

This parameter is required.', example='1'),
}

model UpdateUserResponseBody = {
  code?: string(name='code', description='The error code.', example='200'),
  message?: string(name='message', description='The status code description.', example='ok'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  wnUserId?: string(name='wnUserId', description='The WINNEXO platform user ID.', example='1'),
}

model UpdateUserResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateUserResponseBody(name='body'),
}

/**
 * @summary Modifies user information, including enabling or disabling the user.
 *
 * @description Modifies user information through OpenAPI.
 *     Business orchestration:
 *     1. Parse roleCodes → role_ids
 *     2. If isActive has changed, perform the status switch first (including last super admin protection)
 *     3. Call update_tenant_member to modify other fields (displayName / roleCodes / userGroupIds)
 *     4. Return HTTP 200 if all steps succeed
 *     Execution order notes:
 *     - The isActive status change is performed before other field writes. The two steps are not in the same transaction.
 *     - If validation fails (such as last super admin protection) → an exception is thrown and subsequent steps are not executed.
 *     - If the isActive change has been persisted but a subsequent step fails, the isActive change is not rolled back.
 *
 * @param tmpReq UpdateUserRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateUserResponse
 */
async function updateUserWithOptions(tmpReq: UpdateUserRequest, headers: map[string]string, runtime: Util.RuntimeOptions): UpdateUserResponse {
  Util.validateModel(tmpReq);
  var request = new UpdateUserShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!Util.isUnset(tmpReq.roleCodes)) {
    request.roleCodesShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.roleCodes, 'roleCodes', 'json');
  }
  if (!Util.isUnset(tmpReq.userGroupIds)) {
    request.userGroupIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.userGroupIds, 'userGroupIds', 'json');
  }
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.displayName)) {
    body['displayName'] = request.displayName;
  }
  if (!Util.isUnset(request.isActive)) {
    body['isActive'] = request.isActive;
  }
  if (!Util.isUnset(request.roleCodesShrink)) {
    body['roleCodes'] = request.roleCodesShrink;
  }
  if (!Util.isUnset(request.userGroupIdsShrink)) {
    body['userGroupIds'] = request.userGroupIdsShrink;
  }
  if (!Util.isUnset(request.wnUserId)) {
    body['wnUserId'] = request.wnUserId;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'UpdateUser',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/updateUser`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Modifies user information, including enabling or disabling the user.
 *
 * @description Modifies user information through OpenAPI.
 *     Business orchestration:
 *     1. Parse roleCodes → role_ids
 *     2. If isActive has changed, perform the status switch first (including last super admin protection)
 *     3. Call update_tenant_member to modify other fields (displayName / roleCodes / userGroupIds)
 *     4. Return HTTP 200 if all steps succeed
 *     Execution order notes:
 *     - The isActive status change is performed before other field writes. The two steps are not in the same transaction.
 *     - If validation fails (such as last super admin protection) → an exception is thrown and subsequent steps are not executed.
 *     - If the isActive change has been persisted but a subsequent step fails, the isActive change is not rolled back.
 *
 * @param request UpdateUserRequest
 * @return UpdateUserResponse
 */
async function updateUser(request: UpdateUserRequest): UpdateUserResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return updateUserWithOptions(request, headers, runtime);
}

model UpdateUserInfoRequest {
  avatar?: string(name='avatar', description='The profile picture URL.', example='https://example.com/avatar.png'),
  languagePreference?: string(name='languagePreference', description='The language preference: zh-CN, en-US.', example='string_value'),
  name?: string(name='name', description='The name.', example='SampleName.pdf'),
  offering?: string(name='offering', description='The user service description. Maximum length: 1000 characters.', example='string_value'),
  profileRoleInfo?: string(name='profileRoleInfo', description='The user role description (used when profileRole is set to Others). Maximum length: 100 characters.', example='string_value'),
  selfIntroduction?: string(name='selfIntroduction', description='The user self-introduction. Maximum length: 1000 characters.', example='string_value'),
  tenantId?: string(name='tenantId', description='The effective tenant ID.', example='10000'),
}

model UpdateUserInfoResponseBody = {
  code?: string(name='code', description='The error code.', example='200'),
  message?: string(name='message', description='The status code description.', example='The current zone list is illegal.'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  user?: {
    avatar?: string(name='avatar', description='The user profile picture URL.', example='https://example.com/avatar.png'),
    isAdmin?: boolean(name='isAdmin', description='Indicates whether the user is a super administrator.', example='true'),
    isSystemTenant?: boolean(name='isSystemTenant', description='Indicates whether the current logged-in tenant is a system tenant.', example='true'),
    languagePreference?: string(name='languagePreference', description='The user language preference.', example='string_value'),
    name?: string(name='name', description='The username.', example='SampleName.pdf'),
    offering?: string(name='offering', description='The user service description.', example='string_value'),
    parsedOffering?: string(name='parsedOffering', description='The parsed user service result in JSON format.', example='string_value'),
    profileRole?: string(name='profileRole', description='The user role.', example='string_value'),
    profileRoleInfo?: string(name='profileRoleInfo', description='The user role description.', example='string_value'),
    selfIntroduction?: string(name='selfIntroduction', description='The user self-introduction.', example='string_value'),
    tenantId?: long(name='tenantId', description='The current tenant ID.', example='10000'),
    tenantList?: [ 
      {
        tenantId?: long(name='tenantId', description='The tenant ID.', example='10000'),
        tenantName?: string(name='tenantName', description='The tenant name.', example='string_value'),
      }
    ](name='tenantList', description='The list of tenants to which the user belongs.'),
    tenantName?: string(name='tenantName', description='The current tenant name.', example='string_value'),
    userCode?: string(name='userCode', description='The user code.', example='string_value'),
    userId?: long(name='userId', description='The user ID.', example='1'),
  }(name='user', description='The user information.'),
}

model UpdateUserInfoResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateUserInfoResponseBody(name='body'),
}

/**
 * @summary Updates partial fields of the current user information and returns the complete user information.
 *
 * @description ## Request description
 * - This API allows the caller to update some or all optional fields of a specified user. Fields that are not provided retain their original values.
 * - Use the `tenantId` parameter to specify a tenant ID. If omitted, the default tenant of the caller is used.
 * - After a successful update, the response body contains the complete user information object.
 * - This operation requires authentication and supports AK, BearerToken, and APP security schemes.
 * - The request content type is JSON, and the operation is available only over HTTPS.
 * - Note: The `profileRoleInfo` field is valid only when the user role is set to Others. It describes the specific role information of the user.
 *
 * @param request UpdateUserInfoRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateUserInfoResponse
 */
async function updateUserInfoWithOptions(request: UpdateUserInfoRequest, headers: map[string]string, runtime: Util.RuntimeOptions): UpdateUserInfoResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.avatar)) {
    body['avatar'] = request.avatar;
  }
  if (!Util.isUnset(request.languagePreference)) {
    body['languagePreference'] = request.languagePreference;
  }
  if (!Util.isUnset(request.name)) {
    body['name'] = request.name;
  }
  if (!Util.isUnset(request.offering)) {
    body['offering'] = request.offering;
  }
  if (!Util.isUnset(request.profileRoleInfo)) {
    body['profileRoleInfo'] = request.profileRoleInfo;
  }
  if (!Util.isUnset(request.selfIntroduction)) {
    body['selfIntroduction'] = request.selfIntroduction;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'UpdateUserInfo',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/updateUserInfo`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Updates partial fields of the current user information and returns the complete user information.
 *
 * @description ## Request description
 * - This API allows the caller to update some or all optional fields of a specified user. Fields that are not provided retain their original values.
 * - Use the `tenantId` parameter to specify a tenant ID. If omitted, the default tenant of the caller is used.
 * - After a successful update, the response body contains the complete user information object.
 * - This operation requires authentication and supports AK, BearerToken, and APP security schemes.
 * - The request content type is JSON, and the operation is available only over HTTPS.
 * - Note: The `profileRoleInfo` field is valid only when the user role is set to Others. It describes the specific role information of the user.
 *
 * @param request UpdateUserInfoRequest
 * @return UpdateUserInfoResponse
 */
async function updateUserInfo(request: UpdateUserInfoRequest): UpdateUserInfoResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return updateUserInfoWithOptions(request, headers, runtime);
}

model UploadChatFileRequest {
  contentType?: string(name='contentType', description='The content type of the file. Valid values:

- **image**: image
- **document**: general document
- **alidoc**: Alibaba document
- **text**: text
- **video**: video
- **audio**: audio
- **archive**: archive
- **app**: application
- **link**: shortcut
- **other**: other', example='application/pdf'),
  fileName?: string(name='fileName', description='The full path name of the file.

This parameter is required.', example='report.pdf'),
  fileUrl?: string(name='fileUrl', description='The attachment address.

This parameter is required.', example='http://winnexo-file-transfer.oss-cn-hangzhou.aliyuncs.com/openapi/2026-08-06/9f8c2a1b'),
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital employee (operating object name, optional).', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID.', example='10000'),
}

model UploadChatFileAdvanceRequest {
  contentType?: string(name='contentType', description='The content type of the file. Valid values:

- **image**: image
- **document**: general document
- **alidoc**: Alibaba document
- **text**: text
- **video**: video
- **audio**: audio
- **archive**: archive
- **app**: application
- **link**: shortcut
- **other**: other', example='application/pdf'),
  fileName?: string(name='fileName', description='The full path name of the file.

This parameter is required.', example='report.pdf'),
  fileUrlObject?: readable(name='fileUrl', description='The attachment address.

This parameter is required.', example='http://winnexo-file-transfer.oss-cn-hangzhou.aliyuncs.com/openapi/2026-08-06/9f8c2a1b'),
  operatingObjectName?: string(name='operatingObjectName', description='The name of the digital employee (operating object name, optional).', example='string_value'),
  tenantId?: string(name='tenantId', description='The tenant ID.', example='10000'),
}

model UploadChatFileResponseBody = {
  code?: string(name='code', description='The status code.', example='200'),
  contentType?: string(name='contentType', description='The content type of the file. Valid values:

- **image**: image
- **document**: general document
- **alidoc**: Alibaba document
- **text**: text
- **video**: video
- **audio**: audio
- **archive**: archive
- **app**: application
- **link**: shortcut
- **other**: other', example='string_value'),
  fileName?: string(name='fileName', description='The full path name of the file.', example='example.pdf'),
  filePublicUrl?: string(name='filePublicUrl', description='The publicly accessible URL of the online document.', example='https://example.com/winnexo/resource'),
  fileRecordId?: string(name='fileRecordId', description='The file record ID (optional, corresponding to settings.file_record_id).', example='exampleFileRecordId'),
  fileUrl?: string(name='fileUrl', description='The attachment address.', example='https://example.com/winnexo/resource'),
  message?: string(name='message', description='The description of the status code.', example='ok'),
  objectName?: string(name='objectName', description='The object name.', example='string_value'),
  requestId?: string(name='requestId', description='The request ID.', example='019FF406-1B10-0065-A97D-2D1920C2A03D'),
  uploadSignatureUrl?: string(name='uploadSignatureUrl', description='The signature URL.', example='https://example.com/winnexo/resource'),
}

model UploadChatFileResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UploadChatFileResponseBody(name='body'),
}

/**
 * @summary Uploads a local file in a session.
 *
 * @description ## Operation description
 * This API is used to upload a session temporary file by using the **file transfer upload** mode (`fileTransfer`). The file binary data is not transmitted through the request body of this API. Instead, the file is first uploaded to Object Storage Service (OSS), and then the OSS address is passed to the backend through the `FileUrl` parameter. The backend retrieves the bytes from that address, writes them to its own OSS, and creates a session temporary file record.
 * ### Call methods
 * - **Recommended**: Use the `UploadChatFileAdvance` method generated by the SDK. Pass in the local file stream, and the SDK automatically completes the transfer upload and populates `FileUrl`.
 * - **Direct upload**: Upload the file to an OSS address accessible by the server, and then directly call this API with the `FileUrl` parameter.
 * ### Request parameters
 * - **FileUrl**: Required. The OSS address of the file. When you use the Advance method, the SDK automatically populates this parameter. You do not need to manually assign a value.
 * - **FileName**: Required. The original file name including the extension, such as `report.pdf`. The OSS address generated during transfer does not carry the original file name. The backend uses this parameter to determine the file extension and display name. Therefore, you must explicitly pass in this parameter.
 * - **ContentType**: Optional. The MIME type of the file. If this parameter is not specified, `application/octet-stream` is used.
 * - **OperatingObjectName**: Optional. The Agent namespace identifier that determines the file storage path.
 * ### Response parameters
 * The response includes the OSS object path `objectName`, the storage address `fileUrl`, the public access address `filePublicUrl` (valid for 1 hour), and the file record ID `fileRecordId`. The `uploadSignatureUrl` parameter is always empty in this mode.
 *
 * @param request UploadChatFileRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UploadChatFileResponse
 */
async function uploadChatFileWithOptions(request: UploadChatFileRequest, headers: map[string]string, runtime: Util.RuntimeOptions): UploadChatFileResponse {
  Util.validateModel(request);
  var query : map[string]any = {};
  if (!Util.isUnset(request.tenantId)) {
    query['tenantId'] = request.tenantId;
  }

  var body : map[string]any = {};
  if (!Util.isUnset(request.contentType)) {
    body['contentType'] = request.contentType;
  }
  if (!Util.isUnset(request.fileName)) {
    body['fileName'] = request.fileName;
  }
  if (!Util.isUnset(request.fileUrl)) {
    body['fileUrl'] = request.fileUrl;
  }
  if (!Util.isUnset(request.operatingObjectName)) {
    body['operatingObjectName'] = request.operatingObjectName;
  }

  var req = new OpenApi.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'UploadChatFile',
    version = '2026-05-12',
    protocol = 'HTTPS',
    pathname = `/openapi/uploadChatFile`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Uploads a local file in a session.
 *
 * @description ## Operation description
 * This API is used to upload a session temporary file by using the **file transfer upload** mode (`fileTransfer`). The file binary data is not transmitted through the request body of this API. Instead, the file is first uploaded to Object Storage Service (OSS), and then the OSS address is passed to the backend through the `FileUrl` parameter. The backend retrieves the bytes from that address, writes them to its own OSS, and creates a session temporary file record.
 * ### Call methods
 * - **Recommended**: Use the `UploadChatFileAdvance` method generated by the SDK. Pass in the local file stream, and the SDK automatically completes the transfer upload and populates `FileUrl`.
 * - **Direct upload**: Upload the file to an OSS address accessible by the server, and then directly call this API with the `FileUrl` parameter.
 * ### Request parameters
 * - **FileUrl**: Required. The OSS address of the file. When you use the Advance method, the SDK automatically populates this parameter. You do not need to manually assign a value.
 * - **FileName**: Required. The original file name including the extension, such as `report.pdf`. The OSS address generated during transfer does not carry the original file name. The backend uses this parameter to determine the file extension and display name. Therefore, you must explicitly pass in this parameter.
 * - **ContentType**: Optional. The MIME type of the file. If this parameter is not specified, `application/octet-stream` is used.
 * - **OperatingObjectName**: Optional. The Agent namespace identifier that determines the file storage path.
 * ### Response parameters
 * The response includes the OSS object path `objectName`, the storage address `fileUrl`, the public access address `filePublicUrl` (valid for 1 hour), and the file record ID `fileRecordId`. The `uploadSignatureUrl` parameter is always empty in this mode.
 *
 * @param request UploadChatFileRequest
 * @return UploadChatFileResponse
 */
async function uploadChatFile(request: UploadChatFileRequest): UploadChatFileResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string = {};
  return uploadChatFileWithOptions(request, headers, runtime);
}

async function uploadChatFileAdvance(request: UploadChatFileAdvanceRequest, headers: map[string]string, runtime: Util.RuntimeOptions): UploadChatFileResponse {
  // Step 0: init client
  var credentialModel: Credential.CredentialModel = null;
  if (Util.isUnset(@credential)) {
    throw {
      code = 'InvalidCredentials',
      message = 'Please set up the credentials correctly. If you are setting them through environment variables, please ensure that ALIBABA_CLOUD_ACCESS_KEY_ID and ALIBABA_CLOUD_ACCESS_KEY_SECRET are set correctly. See https://help.aliyun.com/zh/sdk/developer-reference/configure-the-alibaba-cloud-accesskey-environment-variable-on-linux-macos-and-windows-systems for more details.'
    }
  }
  credentialModel = @credential.getCredential();
  var accessKeyId = credentialModel.accessKeyId;
  var accessKeySecret = credentialModel.accessKeySecret;
  var securityToken = credentialModel.securityToken;
  var credentialType = credentialModel.type;
  var openPlatformEndpoint = @openPlatformEndpoint;
  if(Util.empty(openPlatformEndpoint)) {
    openPlatformEndpoint ='openplatform.aliyuncs.com';
  }
  if(Util.isUnset(credentialType)) {
    credentialType ='access_key';
  }
  var authConfig = new OpenApi.Config{
    accessKeyId = accessKeyId,
    accessKeySecret = accessKeySecret,
    securityToken = securityToken,
    type = credentialType,
    endpoint = openPlatformEndpoint,
    protocol = @protocol,
    regionId = @regionId,
  };
  var authClient = new OpenApi(authConfig);
  var authRequest = {
    Product = 'WinNexo',
    RegionId = @regionId,
  };
  var authReq = new OpenApi.OpenApiRequest{
    query = OpenApiUtil.query(authRequest),
  };
  var authParams = new OpenApi.Params{
    action = 'AuthorizeFileUpload',
    version = '2019-12-19',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'GET',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  var authResponse : object = {};
  var fileObj = new FileForm.FileField{};
  var ossHeader: object = {};
  var tmpBody: object = {};
  var useAccelerate: boolean = false;
  var authResponseBody: map[string]string = {};
  var uploadChatFileReq = new UploadChatFileRequest{};
  OpenApiUtil.convert(request, uploadChatFileReq);

  if(!Util.isUnset(request.fileUrlObject)) {
    var tmpResp0: any = authClient.callApi(authParams, authReq, runtime);
    authResponse = Util.assertAsMap(tmpResp0);
    tmpBody = Util.assertAsMap(authResponse['body']);
    useAccelerate = Util.assertAsBoolean(tmpBody['UseAccelerate']);
    authResponseBody = Util.stringifyMapValue(tmpBody);
    fileObj = new FileForm.FileField{
      filename = authResponseBody['ObjectKey'],
      content = request.fileUrlObject,
      contentType = '',
    };
    ossHeader = {
      host = OpenApiUtil.getEndpoint(authResponseBody['Endpoint'], useAccelerate, @endpointType),
      OSSAccessKeyId = authResponseBody['AccessKeyId'],
      policy = authResponseBody['EncodedPolicy'],
      Signature = authResponseBody['Signature'],
      key = authResponseBody['ObjectKey'],
      file = fileObj,
      success_action_status = '201',
    };
    _postOSSObject(authResponseBody['Bucket'], ossHeader, runtime);
    uploadChatFileReq.fileUrl = `http://${authResponseBody['Bucket']}.${authResponseBody['Endpoint']}/${authResponseBody['ObjectKey']}`;
  }
  var uploadChatFileResp = uploadChatFileWithOptions(uploadChatFileReq, headers, runtime);
  return uploadChatFileResp;
}

api _postOSSObject(bucketName: string, data: object, runtime: Util.RuntimeOptions): object {
  var form = Util.assertAsMap(data);
  var boundary = FileForm.getBoundary();
  var tmp = Util.assertAsString(form.host);
  var host = `${bucketName}.${tmp}`;
  __request.protocol = 'HTTPS';
  __request.method = 'POST';
  __request.pathname = `/`;
  __request.headers = {
    host = host,
    date = Util.getDateUTCString(),
    user-agent = Util.getUserAgent(''),
  };
  __request.headers.content-type = `multipart/form-data; boundary=${boundary}`;
  __request.body = FileForm.toFileForm(form, boundary);
} returns {
  var respMap : object = null;
  var bodyStr = Util.readAsString(__response.body);
  if (Util.is4xx(__response.statusCode) || Util.is5xx(__response.statusCode)) {
    respMap = XML.parseXml(bodyStr, null);
    var err = Util.assertAsMap(respMap.Error);
    throw {
      code = err.Code,
      message = err.Message,
      data = {
        httpCode = __response.statusCode,
        requestId = err.RequestId,
        hostId = err.HostId,
      }
    };
  }
  respMap = XML.parseXml(bodyStr, null);
  return {
    ...respMap,
  };
} runtime {
  timeouted = 'retry',
  key = Util.defaultString(runtime.key, @key),
  cert = Util.defaultString(runtime.cert, @cert),
  ca = Util.defaultString(runtime.ca, @ca),
  readTimeout = Util.defaultNumber(runtime.readTimeout, @readTimeout),
  connectTimeout = Util.defaultNumber(runtime.connectTimeout, @connectTimeout),
  httpProxy = Util.defaultString(runtime.httpProxy, @httpProxy),
  httpsProxy = Util.defaultString(runtime.httpsProxy, @httpsProxy),
  noProxy = Util.defaultString(runtime.noProxy, @noProxy),
  socks5Proxy = Util.defaultString(runtime.socks5Proxy, @socks5Proxy),
  socks5NetWork = Util.defaultString(runtime.socks5NetWork, @socks5NetWork),
  maxIdleConns = Util.defaultNumber(runtime.maxIdleConns, @maxIdleConns),
  retry = {
    retryable = runtime.autoretry,
    maxAttempts = Util.defaultNumber(runtime.maxAttempts, 3)
  },
  backoff = {
    policy = Util.defaultString(runtime.backoffPolicy, 'no'),
    period = Util.defaultNumber(runtime.backoffPeriod, 1)
  },
  ignoreSSL = OpenApi.defaultAny(runtime.ignoreSSL, false),
  tlsMinVersion = @tlsMinVersion
}
