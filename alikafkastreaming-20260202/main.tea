/**
 *
 */
import Util;
import OpenApi;
import OpenApiUtil;
import EndpointUtil;

extends OpenApi;


init(config: OpenApi.Config){
  super(config);
  @endpointRule = 'regional';
  
  checkConfig(config);
  @endpoint = getEndpoint('alikafkastreaming', @regionId, @endpointRule, @network, @suffix, @endpointMap, @endpoint);
}

function getEndpoint(productId: string, regionId: string, endpointRule: string, network: string, suffix: string, endpointMap: map[string]string, endpoint: string) throws: string{
  if (!Util.empty(endpoint)) {
    return endpoint;
  }
  
  if (!Util.isUnset(endpointMap) && !Util.empty(endpointMap[regionId])) {
    return endpointMap[regionId];
  }
  return EndpointUtil.getEndpointRules(productId, regionId, endpointRule, network, suffix);
}

model CheckSqlContentRequest {
  instanceId?: string(name='InstanceId', description='This parameter is required.', example='alikafka_streaming-cn-a1b2c3d4'),
  jobName?: string(name='JobName', description='This parameter is required.', example='order_enrichment'),
  regionId?: string(name='RegionId', description='This parameter is required.', example='cn-hangzhou'),
  sqlContent?: string(name='SqlContent', description='This parameter is required.', example='INSERT INTO sink_table SELECT * FROM source_table;'),
}

model CheckSqlContentResponseBody = {
  code?: long(name='Code', example='200'),
  data?: {
    errorList?: [ 
      {
        codeSnippet?: string(name='CodeSnippet', example='SELECT FROM source_table'),
        columnNumber?: int32(name='ColumnNumber', example='8'),
        endColumnNumber?: int32(name='EndColumnNumber', example='12'),
        endLineNumber?: int32(name='EndLineNumber', example='1'),
        errorType?: string(name='ErrorType', example='SYNTAX_ERROR'),
        lineNumber?: int32(name='LineNumber', example='1'),
        message?: string(name='Message', example='SQL parse failed near FROM'),
      }
    ](name='ErrorList'),
    valid?: boolean(name='Valid', example='false'),
  }(name='Data'),
  requestId?: string(name='RequestId', example='062D8E8B-8D47-5DCC-BB12-5A1D93C3A66B'),
  success?: boolean(name='Success', example='true'),
}

model CheckSqlContentResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CheckSqlContentResponseBody(name='body'),
}

/**
 * @summary 检查sql语法
 *
 * @description ## 请求说明
 * - 该接口支持通过 GET 或 POST 方法调用。
 * - 必须提供 `InstanceId`、`JobName` 和 `SqlContent` 参数，其中 `SqlContent` 是待校验的 Flink SQL 语句。
 * - 返回结果中，`Data.Valid` 字段指示 SQL 是否通过校验；若未通过，则错误详情位于 `Data.ErrorList` 中。
 * - 当前版本要求同时传入实例 ID (`InstanceId`) 和作业名称 (`JobName`) 以构建作业上下文。
 * - 接口返回成功仅表示校验流程执行完成，并不直接反映 SQL 的有效性，请检查 `Data.Valid` 字段来确定 SQL 是否有效。
 * - 错误码和异常处理请参考文档中的“错误码”部分。
 *
 * @param request CheckSqlContentRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CheckSqlContentResponse
 */
async function checkSqlContentWithOptions(request: CheckSqlContentRequest, runtime: Util.RuntimeOptions): CheckSqlContentResponse {
  Util.validateModel(request);
  var query = {};
  if (!Util.isUnset(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!Util.isUnset(request.jobName)) {
    query['JobName'] = request.jobName;
  }
  if (!Util.isUnset(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  if (!Util.isUnset(request.sqlContent)) {
    query['SqlContent'] = request.sqlContent;
  }
  var req = new OpenApi.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'CheckSqlContent',
    version = '2026-02-02',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 检查sql语法
 *
 * @description ## 请求说明
 * - 该接口支持通过 GET 或 POST 方法调用。
 * - 必须提供 `InstanceId`、`JobName` 和 `SqlContent` 参数，其中 `SqlContent` 是待校验的 Flink SQL 语句。
 * - 返回结果中，`Data.Valid` 字段指示 SQL 是否通过校验；若未通过，则错误详情位于 `Data.ErrorList` 中。
 * - 当前版本要求同时传入实例 ID (`InstanceId`) 和作业名称 (`JobName`) 以构建作业上下文。
 * - 接口返回成功仅表示校验流程执行完成，并不直接反映 SQL 的有效性，请检查 `Data.Valid` 字段来确定 SQL 是否有效。
 * - 错误码和异常处理请参考文档中的“错误码”部分。
 *
 * @param request CheckSqlContentRequest
 * @return CheckSqlContentResponse
 */
async function checkSqlContent(request: CheckSqlContentRequest): CheckSqlContentResponse {
  var runtime = new Util.RuntimeOptions{};
  return checkSqlContentWithOptions(request, runtime);
}

model CreateComputeInstanceRequest {
  paidType?: long(name='PaidType', description='This parameter is required.', example='5'),
  regionId?: string(name='RegionId', description='This parameter is required.', example='cn-hangzhou'),
  resourceGroupId?: string(name='ResourceGroupId', example='rg-acfm2pz25js****'),
}

model CreateComputeInstanceResponseBody = {
  code?: long(name='Code', example='200'),
  data?: {
    instanceId?: string(name='InstanceId', example='alikafka_streaming-cn-pe333xxxxxx'),
    orderId?: string(name='OrderId', example='1234567890'),
  }(name='Data'),
  requestId?: string(name='RequestId', example='062D8E8B-8D47-5DCC-BB12-5A1D93C3A66B'),
  success?: boolean(name='Success', example='true'),
}

model CreateComputeInstanceResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateComputeInstanceResponseBody(name='body'),
}

/**
 * @summary 创建 流计算实例
 *
 * @description 创建一个计算实例。接口只完成购买阶段；创建成功后需调用 StartComputeInstance 完成网络配置和部署。
 * - API 版本：2026-02-02
 * - Action：CreateComputeInstance
 *
 * @param request CreateComputeInstanceRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateComputeInstanceResponse
 */
async function createComputeInstanceWithOptions(request: CreateComputeInstanceRequest, runtime: Util.RuntimeOptions): CreateComputeInstanceResponse {
  Util.validateModel(request);
  var query = {};
  if (!Util.isUnset(request.paidType)) {
    query['PaidType'] = request.paidType;
  }
  if (!Util.isUnset(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  if (!Util.isUnset(request.resourceGroupId)) {
    query['ResourceGroupId'] = request.resourceGroupId;
  }
  var req = new OpenApi.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'CreateComputeInstance',
    version = '2026-02-02',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 创建 流计算实例
 *
 * @description 创建一个计算实例。接口只完成购买阶段；创建成功后需调用 StartComputeInstance 完成网络配置和部署。
 * - API 版本：2026-02-02
 * - Action：CreateComputeInstance
 *
 * @param request CreateComputeInstanceRequest
 * @return CreateComputeInstanceResponse
 */
async function createComputeInstance(request: CreateComputeInstanceRequest): CreateComputeInstanceResponse {
  var runtime = new Util.RuntimeOptions{};
  return createComputeInstanceWithOptions(request, runtime);
}

model CreateComputeJobRequest {
  cuLimit?: double(name='CuLimit', description='This parameter is required.', example='2.0'),
  cuReserved?: double(name='CuReserved', description='This parameter is required.', example='1.0'),
  instanceId?: string(name='InstanceId', description='This parameter is required.', example='alikafka_streaming-cn-pe333xxxxxx'),
  jobName?: string(name='JobName', description='This parameter is required.', example='order_enrichment'),
  regionId?: string(name='RegionId', description='This parameter is required.', example='cn-hangzhou'),
  remark?: string(name='Remark', example='订单流实时清洗'),
}

model CreateComputeJobResponseBody = {
  code?: long(name='Code', example='200'),
  data?: boolean(name='Data', example='true'),
  requestId?: string(name='RequestId', example='062D8E8B-8D47-5DCC-BB12-5A1D93C3A66B'),
  success?: boolean(name='Success', example='true'),
}

model CreateComputeJobResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateComputeJobResponseBody(name='body'),
}

/**
 * @summary 创建 JOB
 *
 * @description ## 请求说明
 * - 该API用于在指定的运行中的计算实例上创建一个新的Flink SQL作业。
 * - 创建后的作业将处于`INIT`状态。
 * - 用户可以通过设置`CuLimit`和`CuReserved`来控制作业的资源使用情况。
 * - `Remark`字段允许用户为作业添加备注信息，便于管理和识别。
 * - 确保提供的`RegionId`、`InstanceId`以及`JobName`参数准确无误，否则可能导致请求失败。
 * - 如果尝试创建同名作业，则会返回错误提示。
 * - 计算实例必须处于运行状态才能成功创建作业。
 *
 * @param request CreateComputeJobRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateComputeJobResponse
 */
async function createComputeJobWithOptions(request: CreateComputeJobRequest, runtime: Util.RuntimeOptions): CreateComputeJobResponse {
  Util.validateModel(request);
  var query = {};
  if (!Util.isUnset(request.cuLimit)) {
    query['CuLimit'] = request.cuLimit;
  }
  if (!Util.isUnset(request.cuReserved)) {
    query['CuReserved'] = request.cuReserved;
  }
  if (!Util.isUnset(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!Util.isUnset(request.jobName)) {
    query['JobName'] = request.jobName;
  }
  if (!Util.isUnset(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  if (!Util.isUnset(request.remark)) {
    query['Remark'] = request.remark;
  }
  var req = new OpenApi.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'CreateComputeJob',
    version = '2026-02-02',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 创建 JOB
 *
 * @description ## 请求说明
 * - 该API用于在指定的运行中的计算实例上创建一个新的Flink SQL作业。
 * - 创建后的作业将处于`INIT`状态。
 * - 用户可以通过设置`CuLimit`和`CuReserved`来控制作业的资源使用情况。
 * - `Remark`字段允许用户为作业添加备注信息，便于管理和识别。
 * - 确保提供的`RegionId`、`InstanceId`以及`JobName`参数准确无误，否则可能导致请求失败。
 * - 如果尝试创建同名作业，则会返回错误提示。
 * - 计算实例必须处于运行状态才能成功创建作业。
 *
 * @param request CreateComputeJobRequest
 * @return CreateComputeJobResponse
 */
async function createComputeJob(request: CreateComputeJobRequest): CreateComputeJobResponse {
  var runtime = new Util.RuntimeOptions{};
  return createComputeJobWithOptions(request, runtime);
}

model DeleteComputeInstanceRequest {
  instanceId?: string(name='InstanceId', description='This parameter is required.', example='alikafka_streaming-cn-pe333xxxxxx'),
  regionId?: string(name='RegionId', description='This parameter is required.', example='cn-hangzhou'),
}

model DeleteComputeInstanceResponseBody = {
  code?: long(name='Code', example='200'),
  data?: boolean(name='Data', example='true'),
  requestId?: string(name='RequestId', example='2834B42F-FBAD-516B-94FA-95E675A5D6CC'),
  success?: boolean(name='Success', example='true'),
}

model DeleteComputeInstanceResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DeleteComputeInstanceResponseBody(name='body'),
}

/**
 * @summary  删除实例
 *
 * @description 删除处于待部署、已停止或已释放状态的计算实例。
 * - API版本：2026-02-02
 * - Action：DeleteComputeInstance
 *
 * @param request DeleteComputeInstanceRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteComputeInstanceResponse
 */
async function deleteComputeInstanceWithOptions(request: DeleteComputeInstanceRequest, runtime: Util.RuntimeOptions): DeleteComputeInstanceResponse {
  Util.validateModel(request);
  var query = {};
  if (!Util.isUnset(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!Util.isUnset(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  var req = new OpenApi.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'DeleteComputeInstance',
    version = '2026-02-02',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary  删除实例
 *
 * @description 删除处于待部署、已停止或已释放状态的计算实例。
 * - API版本：2026-02-02
 * - Action：DeleteComputeInstance
 *
 * @param request DeleteComputeInstanceRequest
 * @return DeleteComputeInstanceResponse
 */
async function deleteComputeInstance(request: DeleteComputeInstanceRequest): DeleteComputeInstanceResponse {
  var runtime = new Util.RuntimeOptions{};
  return deleteComputeInstanceWithOptions(request, runtime);
}

model DeleteComputeJobRequest {
  instanceId?: string(name='InstanceId', description='This parameter is required.', example='alikafka_streaming-cn-pe333xxxxxx'),
  jobName?: string(name='JobName', description='This parameter is required.', example='order_enrichment'),
  regionId?: string(name='RegionId', description='This parameter is required.', example='cn-hangzhou'),
}

model DeleteComputeJobResponseBody = {
  code?: long(name='Code', example='200'),
  data?: boolean(name='Data', example='true'),
  requestId?: string(name='RequestId', example='062D8E8B-8D47-5DCC-BB12-5A1D93C3A66B'),
  success?: boolean(name='Success', example='true'),
}

model DeleteComputeJobResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DeleteComputeJobResponseBody(name='body'),
}

/**
 * @summary 删除 JOB
 *
 * @description ## 请求说明
 * - 该接口用于删除一个特定的计算作业。
 * - 成功调用此接口仅表示删除请求已被系统接受，并非立即完成删除操作。
 * - 确保提供的`RegionId`、`InstanceId`以及`JobName`参数准确无误，否则可能导致请求失败。
 * - 如果计算实例或作业处于不允许删除的状态（例如：非运行状态），则会返回相应的错误信息。
 * - 删除操作不可逆，请谨慎使用。
 *
 * @param request DeleteComputeJobRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteComputeJobResponse
 */
async function deleteComputeJobWithOptions(request: DeleteComputeJobRequest, runtime: Util.RuntimeOptions): DeleteComputeJobResponse {
  Util.validateModel(request);
  var query = {};
  if (!Util.isUnset(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!Util.isUnset(request.jobName)) {
    query['JobName'] = request.jobName;
  }
  if (!Util.isUnset(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  var req = new OpenApi.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'DeleteComputeJob',
    version = '2026-02-02',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 删除 JOB
 *
 * @description ## 请求说明
 * - 该接口用于删除一个特定的计算作业。
 * - 成功调用此接口仅表示删除请求已被系统接受，并非立即完成删除操作。
 * - 确保提供的`RegionId`、`InstanceId`以及`JobName`参数准确无误，否则可能导致请求失败。
 * - 如果计算实例或作业处于不允许删除的状态（例如：非运行状态），则会返回相应的错误信息。
 * - 删除操作不可逆，请谨慎使用。
 *
 * @param request DeleteComputeJobRequest
 * @return DeleteComputeJobResponse
 */
async function deleteComputeJob(request: DeleteComputeJobRequest): DeleteComputeJobResponse {
  var runtime = new Util.RuntimeOptions{};
  return deleteComputeJobWithOptions(request, runtime);
}

model GetComputeInstanceRequest {
  instanceId?: string(name='InstanceId', description='This parameter is required.', example='alikafka_streaming-cn-xxxxxxx'),
  regionId?: string(name='RegionId', description='This parameter is required.', example='cn-hangzhou'),
}

model GetComputeInstanceResponseBody = {
  code?: long(name='Code', example='200'),
  data?: {
    chargeType?: string(name='ChargeType', example='POST_PAID'),
    createTime?: string(name='CreateTime', description='Use the UTC time format: yyyy-MM-ddTHH:mm:ssZ', example='2026-09-02T16:00:00Z'),
    cuLimitSum?: double(name='CuLimitSum', example='4.0'),
    cuReservedSum?: double(name='CuReservedSum', example='2.0'),
    cuUsedSum?: double(name='CuUsedSum', example='2.5'),
    expireTime?: string(name='ExpireTime', description='Use the UTC time format: yyyy-MM-ddTHH:mm:ssZ', example='2027-09-02T16:00:00Z'),
    instanceId?: string(name='InstanceId', example='alikafka_streaming-cn-pe333xxxxxx'),
    instanceName?: string(name='InstanceName', example='streaming-prod'),
    regionId?: string(name='RegionId', example='cn-hangzhou'),
    resourceGroupId?: string(name='ResourceGroupId', example='rg-aek53bg4tl6fgma'),
    serviceStatus?: string(name='ServiceStatus', example='RUNNING'),
    totalJobs?: long(name='TotalJobs', example='3'),
    totalRunningJobs?: long(name='TotalRunningJobs', example='2'),
    vSwitchIds?: [ string ](name='VSwitchIds'),
    vpcId?: string(name='VpcId', example='vpc-bp1abcdefg'),
  }(name='Data'),
  requestId?: string(name='RequestId', example='019F5939-507B-57C1-B82C-E55821456FA6'),
  success?: boolean(name='Success', example='true'),
}

model GetComputeInstanceResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetComputeInstanceResponseBody(name='body'),
}

/**
 * @summary 查询单个实例
 *
 * @param request GetComputeInstanceRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetComputeInstanceResponse
 */
async function getComputeInstanceWithOptions(request: GetComputeInstanceRequest, runtime: Util.RuntimeOptions): GetComputeInstanceResponse {
  Util.validateModel(request);
  var query = {};
  if (!Util.isUnset(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!Util.isUnset(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  var req = new OpenApi.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'GetComputeInstance',
    version = '2026-02-02',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 查询单个实例
 *
 * @param request GetComputeInstanceRequest
 * @return GetComputeInstanceResponse
 */
async function getComputeInstance(request: GetComputeInstanceRequest): GetComputeInstanceResponse {
  var runtime = new Util.RuntimeOptions{};
  return getComputeInstanceWithOptions(request, runtime);
}

model GetComputeJobRequest {
  instanceId?: string(name='InstanceId', description='This parameter is required.', example='alikafka_streaming-cn-a1b2c3d4'),
  jobName?: string(name='JobName', description='This parameter is required.', example='order_enrichment'),
  regionId?: string(name='RegionId', description='This parameter is required.', example='cn-hangzhou'),
}

model GetComputeJobResponseBody = {
  code?: long(name='Code', example='200'),
  data?: {
    createTime?: string(name='CreateTime', description='Use the UTC time format: yyyy-MM-ddTHH:mm:ssZ', example='2026-09-02T16:00:00Z'),
    cuLimit?: double(name='CuLimit', example='2.0'),
    cuReserved?: double(name='CuReserved', example='1.0'),
    cuUsed?: double(name='CuUsed', example='1.5'),
    debugMode?: int32(name='DebugMode', example='0'),
    deployedSql?: string(name='DeployedSql', example='INSERT INTO sink_table SELECT * FROM source_table;'),
    draftSql?: string(name='DraftSql', example='INSERT INTO sink_table SELECT * FROM source_table;'),
    errorMsg?: string(name='ErrorMsg', example='SQL 校验或编译失败：Column \\\\"xxx\\\\" not found'),
    expirationTime?: string(name='ExpirationTime', description='Use the UTC time format: yyyy-MM-ddTHH:mm:ssZ', example='2026-09-11T04:33:03Z'),
    instanceId?: string(name='InstanceId', example='alikafka_streaming-cn-hangzhou-a1b2c3d4'),
    jobName?: string(name='JobName', example='order_enrichment'),
    owner?: string(name='Owner', example='1234567890123456'),
    regionId?: string(name='RegionId', example='cn-hangzhou'),
    remark?: string(name='Remark', example='订单流实时清洗'),
    status?: string(name='Status', example='RUNNING'),
    upgradeMode?: string(name='UpgradeMode', example='savepoint'),
  }(name='Data'),
  requestId?: string(name='RequestId', example='062D8E8B-8D47-5DCC-BB12-5A1D93C3A66B'),
  success?: boolean(name='Success', example='true'),
}

model GetComputeJobResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetComputeJobResponseBody(name='body'),
}

/**
 * @summary 查询 JOB 详情
 *
 * @description ## 请求说明
 * - 本接口用于查询指定计算作业的详情。
 * - 支持使用 GET 或 POST 方法进行请求。
 * - 所有时间字段以 Unix 时间戳形式返回，单位为毫秒。
 * - 必须提供 `RegionId`、`InstanceId` 和 `JobName` 参数。
 * - 授权操作为 `alikafkastreaming:GetComputeJob`，访问级别为读取（Read）。
 *
 * @param request GetComputeJobRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetComputeJobResponse
 */
async function getComputeJobWithOptions(request: GetComputeJobRequest, runtime: Util.RuntimeOptions): GetComputeJobResponse {
  Util.validateModel(request);
  var query = {};
  if (!Util.isUnset(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!Util.isUnset(request.jobName)) {
    query['JobName'] = request.jobName;
  }
  if (!Util.isUnset(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  var req = new OpenApi.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'GetComputeJob',
    version = '2026-02-02',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 查询 JOB 详情
 *
 * @description ## 请求说明
 * - 本接口用于查询指定计算作业的详情。
 * - 支持使用 GET 或 POST 方法进行请求。
 * - 所有时间字段以 Unix 时间戳形式返回，单位为毫秒。
 * - 必须提供 `RegionId`、`InstanceId` 和 `JobName` 参数。
 * - 授权操作为 `alikafkastreaming:GetComputeJob`，访问级别为读取（Read）。
 *
 * @param request GetComputeJobRequest
 * @return GetComputeJobResponse
 */
async function getComputeJob(request: GetComputeJobRequest): GetComputeJobResponse {
  var runtime = new Util.RuntimeOptions{};
  return getComputeJobWithOptions(request, runtime);
}

model GetJobDebugDataRequest {
  cursor?: string(name='Cursor'),
  endTime?: long(name='EndTime'),
  instanceId?: string(name='InstanceId', description='This parameter is required.'),
  jobName?: string(name='JobName', description='This parameter is required.'),
  limit?: int32(name='Limit'),
  regionId?: string(name='RegionId', description='This parameter is required.'),
  startTime?: long(name='StartTime'),
}

model GetJobDebugDataResponseBody = {
  code?: long(name='Code'),
  data?: {
    dataRows?: [ 
      {
        flinkInstanceId?: string(name='FlinkInstanceId'),
        jobName?: string(name='JobName'),
        offset?: long(name='Offset'),
        partition?: int32(name='Partition'),
        processedValue?: string(name='ProcessedValue'),
        timestamp?: long(name='Timestamp'),
        uuid?: string(name='Uuid'),
      }
    ](name='DataRows'),
    debugField?: string(name='DebugField'),
    hasMore?: boolean(name='HasMore'),
    limit?: string(name='Limit'),
    nextCursor?: string(name='NextCursor'),
  }(name='Data'),
  requestId?: string(name='RequestId'),
  success?: boolean(name='Success'),
}

model GetJobDebugDataResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetJobDebugDataResponseBody(name='body'),
}

/**
 * @summary 获取debug信息
 *
 * @param request GetJobDebugDataRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetJobDebugDataResponse
 */
async function getJobDebugDataWithOptions(request: GetJobDebugDataRequest, runtime: Util.RuntimeOptions): GetJobDebugDataResponse {
  Util.validateModel(request);
  var query = {};
  if (!Util.isUnset(request.cursor)) {
    query['Cursor'] = request.cursor;
  }
  if (!Util.isUnset(request.endTime)) {
    query['EndTime'] = request.endTime;
  }
  if (!Util.isUnset(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!Util.isUnset(request.jobName)) {
    query['JobName'] = request.jobName;
  }
  if (!Util.isUnset(request.limit)) {
    query['Limit'] = request.limit;
  }
  if (!Util.isUnset(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  if (!Util.isUnset(request.startTime)) {
    query['StartTime'] = request.startTime;
  }
  var req = new OpenApi.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'GetJobDebugData',
    version = '2026-02-02',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 获取debug信息
 *
 * @param request GetJobDebugDataRequest
 * @return GetJobDebugDataResponse
 */
async function getJobDebugData(request: GetJobDebugDataRequest): GetJobDebugDataResponse {
  var runtime = new Util.RuntimeOptions{};
  return getJobDebugDataWithOptions(request, runtime);
}

model ListComputeInstancesRequest {
  instanceId?: string(name='InstanceId'),
  instanceIds?: [ string ](name='InstanceIds'),
  maxResults?: int32(name='MaxResults'),
  nextToken?: string(name='NextToken'),
  orderId?: string(name='OrderId'),
  regionId?: string(name='RegionId', description='This parameter is required.'),
}

model ListComputeInstancesShrinkRequest {
  instanceId?: string(name='InstanceId'),
  instanceIdsShrink?: string(name='InstanceIds'),
  maxResults?: int32(name='MaxResults'),
  nextToken?: string(name='NextToken'),
  orderId?: string(name='OrderId'),
  regionId?: string(name='RegionId', description='This parameter is required.'),
}

model ListComputeInstancesResponseBody = {
  code?: long(name='Code'),
  data?: [ 
    {
      createTime?: string(name='CreateTime', description='Use the UTC time format: yyyy-MM-ddTHH:mm:ssZ'),
      cu?: int32(name='Cu'),
      instanceId?: string(name='InstanceId'),
      instanceName?: string(name='InstanceName'),
      serviceStatus?: string(name='ServiceStatus'),
      totalJobs?: long(name='TotalJobs'),
      totalRunningJobs?: long(name='TotalRunningJobs'),
    }
  ](name='Data'),
  maxResults?: int32(name='MaxResults'),
  nextToken?: string(name='NextToken'),
  requestId?: string(name='RequestId'),
  success?: boolean(name='Success'),
}

model ListComputeInstancesResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListComputeInstancesResponseBody(name='body'),
}

/**
 * @summary 查询实例列表（分页）
 *
 * @param tmpReq ListComputeInstancesRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListComputeInstancesResponse
 */
async function listComputeInstancesWithOptions(tmpReq: ListComputeInstancesRequest, runtime: Util.RuntimeOptions): ListComputeInstancesResponse {
  Util.validateModel(tmpReq);
  var request = new ListComputeInstancesShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!Util.isUnset(tmpReq.instanceIds)) {
    request.instanceIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.instanceIds, 'InstanceIds', 'json');
  }
  var query = {};
  if (!Util.isUnset(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!Util.isUnset(request.instanceIdsShrink)) {
    query['InstanceIds'] = request.instanceIdsShrink;
  }
  if (!Util.isUnset(request.maxResults)) {
    query['MaxResults'] = request.maxResults;
  }
  if (!Util.isUnset(request.nextToken)) {
    query['NextToken'] = request.nextToken;
  }
  if (!Util.isUnset(request.orderId)) {
    query['OrderId'] = request.orderId;
  }
  if (!Util.isUnset(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  var req = new OpenApi.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'ListComputeInstances',
    version = '2026-02-02',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 查询实例列表（分页）
 *
 * @param request ListComputeInstancesRequest
 * @return ListComputeInstancesResponse
 */
async function listComputeInstances(request: ListComputeInstancesRequest): ListComputeInstancesResponse {
  var runtime = new Util.RuntimeOptions{};
  return listComputeInstancesWithOptions(request, runtime);
}

model ListComputeInstancesInPageRequest {
  currentPage?: int32(name='CurrentPage', example='1'),
  instanceIds?: [ string ](name='InstanceIds'),
  pageSize?: int32(name='PageSize', example='10'),
  regionId?: string(name='RegionId', description='This parameter is required.', example='cn-hangzhou'),
  resourceGroupId?: string(name='ResourceGroupId', example='rg-acfm2pz25js****'),
}

model ListComputeInstancesInPageShrinkRequest {
  currentPage?: int32(name='CurrentPage', example='1'),
  instanceIdsShrink?: string(name='InstanceIds'),
  pageSize?: int32(name='PageSize', example='10'),
  regionId?: string(name='RegionId', description='This parameter is required.', example='cn-hangzhou'),
  resourceGroupId?: string(name='ResourceGroupId', example='rg-acfm2pz25js****'),
}

model ListComputeInstancesInPageResponseBody = {
  code?: long(name='Code', example='200'),
  currentPage?: int32(name='CurrentPage', example='1'),
  data?: [ 
    {
      autoRenewFlag?: boolean(name='AutoRenewFlag', example='false'),
      chargeType?: string(name='ChargeType', example='POST_PAID'),
      createTime?: string(name='CreateTime', description='Use the UTC time format: yyyy-MM-ddTHH:mm:ssZ', example='2026-09-02T16:00:00Z'),
      cuLimitSum?: double(name='CuLimitSum', example='4.0'),
      cuReservedSum?: double(name='CuReservedSum', example='2.0'),
      cuUsedSum?: double(name='CuUsedSum', example='2.5'),
      expireTime?: string(name='ExpireTime', description='Use the UTC time format: yyyy-MM-ddTHH:mmZ', example='2027-09-02T16:00:00Z'),
      instanceId?: string(name='InstanceId', example='alikafka_streaming-cn-xxxxxxx'),
      instanceName?: string(name='InstanceName', example='streaming-prod'),
      owner?: string(name='Owner', example='1234567890123456'),
      regionId?: string(name='RegionId', example='cn-beijing'),
      serviceStatus?: string(name='ServiceStatus', example='RUNNING'),
      totalJobs?: long(name='TotalJobs', example='3'),
      totalRunningJobs?: long(name='TotalRunningJobs', example='2'),
      vSwitchIds?: [ string ](name='VSwitchIds'),
      vpcId?: string(name='VpcId', example='vpc-bp1abcdefg'),
    }
  ](name='Data'),
  pageSize?: int32(name='PageSize', example='10'),
  requestId?: string(name='RequestId', example='9079DD86-09F1-5303-A64F-D9089F96BC16'),
  success?: boolean(name='Success', example='true'),
  total?: long(name='Total', example='3'),
}

model ListComputeInstancesInPageResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListComputeInstancesInPageResponseBody(name='body'),
}

/**
 * @summary 查询实例列表（分页）
 *
 * @param tmpReq ListComputeInstancesInPageRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListComputeInstancesInPageResponse
 */
async function listComputeInstancesInPageWithOptions(tmpReq: ListComputeInstancesInPageRequest, runtime: Util.RuntimeOptions): ListComputeInstancesInPageResponse {
  Util.validateModel(tmpReq);
  var request = new ListComputeInstancesInPageShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!Util.isUnset(tmpReq.instanceIds)) {
    request.instanceIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.instanceIds, 'InstanceIds', 'json');
  }
  var query = {};
  if (!Util.isUnset(request.currentPage)) {
    query['CurrentPage'] = request.currentPage;
  }
  if (!Util.isUnset(request.instanceIdsShrink)) {
    query['InstanceIds'] = request.instanceIdsShrink;
  }
  if (!Util.isUnset(request.pageSize)) {
    query['PageSize'] = request.pageSize;
  }
  if (!Util.isUnset(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  if (!Util.isUnset(request.resourceGroupId)) {
    query['ResourceGroupId'] = request.resourceGroupId;
  }
  var req = new OpenApi.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'ListComputeInstancesInPage',
    version = '2026-02-02',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 查询实例列表（分页）
 *
 * @param request ListComputeInstancesInPageRequest
 * @return ListComputeInstancesInPageResponse
 */
async function listComputeInstancesInPage(request: ListComputeInstancesInPageRequest): ListComputeInstancesInPageResponse {
  var runtime = new Util.RuntimeOptions{};
  return listComputeInstancesInPageWithOptions(request, runtime);
}

model ListComputeJobsRequest {
  instanceId?: string(name='InstanceId', description='This parameter is required.', example='alikafka_streaming-cn-a1b2c3d4'),
  maxResults?: int32(name='MaxResults', example='20'),
  nextToken?: string(name='NextToken', example='eyJvZmZzZXQiOjIwfQ=='),
  regionId?: string(name='RegionId', description='This parameter is required.', example='cn-hangzhou'),
  search?: string(name='Search', example='order'),
  sortDirection?: string(name='SortDirection', example='DESC'),
  sortField?: string(name='SortField', example='createTime'),
}

model ListComputeJobsResponseBody = {
  code?: long(name='Code', example='200'),
  data?: [ 
    {
      createTime?: string(name='CreateTime', description='Use the UTC time format: yyyy-MM-ddTHH:mm:ssZ', example='2026-09-02T16:00:00Z'),
      cuLimit?: double(name='CuLimit', example='2.0'),
      cuReserved?: double(name='CuReserved', example='1.0'),
      cuUsed?: double(name='CuUsed', example='1.5'),
      debugMode?: int32(name='DebugMode', example='0'),
      instanceId?: string(name='InstanceId', example='alikafka_streaming-cn-a1b2c3d4'),
      jobName?: string(name='JobName', example='order_enrichment'),
      owner?: string(name='Owner', example='1234567890123456'),
      regionId?: string(name='RegionId', example='cn-hangzhou'),
      remark?: string(name='Remark', example='订单流实时清洗'),
      status?: string(name='Status', example='RUNNING'),
    }
  ](name='Data'),
  maxResults?: int32(name='MaxResults', example='20'),
  nextToken?: string(name='NextToken', example='eyJvZmZzZXQiOjIwfQ=='),
  requestId?: string(name='RequestId', example='062D8E8B-8D47-5DCC-BB12-5A1D93C3A66B'),
  success?: boolean(name='Success', example='true'),
  total?: long(name='Total', example='3'),
}

model ListComputeJobsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListComputeJobsResponseBody(name='body'),
}

/**
 * @summary 分页查询 JOB 列表
 *
 * @description ## 请求说明
 * - 该接口支持通过 `MaxResults` 和 `NextToken` 参数进行游标分页查询。
 * - 首次请求时不需要传递 `NextToken`，后续请求需使用上一次响应中返回的 `NextToken` 值。
 * - 支持按作业名称或备注搜索，并可选择不同的排序字段和方向。
 * - 返回的时间字段均为 Unix 时间戳（单位：毫秒）。
 * - 授权操作为 `alikafkastreaming:ListComputeJobs`，访问级别为列出（List），适用于全部资源。
 *
 * @param request ListComputeJobsRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListComputeJobsResponse
 */
async function listComputeJobsWithOptions(request: ListComputeJobsRequest, runtime: Util.RuntimeOptions): ListComputeJobsResponse {
  Util.validateModel(request);
  var query = {};
  if (!Util.isUnset(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!Util.isUnset(request.maxResults)) {
    query['MaxResults'] = request.maxResults;
  }
  if (!Util.isUnset(request.nextToken)) {
    query['NextToken'] = request.nextToken;
  }
  if (!Util.isUnset(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  if (!Util.isUnset(request.search)) {
    query['Search'] = request.search;
  }
  if (!Util.isUnset(request.sortDirection)) {
    query['SortDirection'] = request.sortDirection;
  }
  if (!Util.isUnset(request.sortField)) {
    query['SortField'] = request.sortField;
  }
  var req = new OpenApi.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'ListComputeJobs',
    version = '2026-02-02',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 分页查询 JOB 列表
 *
 * @description ## 请求说明
 * - 该接口支持通过 `MaxResults` 和 `NextToken` 参数进行游标分页查询。
 * - 首次请求时不需要传递 `NextToken`，后续请求需使用上一次响应中返回的 `NextToken` 值。
 * - 支持按作业名称或备注搜索，并可选择不同的排序字段和方向。
 * - 返回的时间字段均为 Unix 时间戳（单位：毫秒）。
 * - 授权操作为 `alikafkastreaming:ListComputeJobs`，访问级别为列出（List），适用于全部资源。
 *
 * @param request ListComputeJobsRequest
 * @return ListComputeJobsResponse
 */
async function listComputeJobs(request: ListComputeJobsRequest): ListComputeJobsResponse {
  var runtime = new Util.RuntimeOptions{};
  return listComputeJobsWithOptions(request, runtime);
}

model ListSupportedConnectorsRequest {
  instanceId?: string(name='InstanceId'),
  regionId?: string(name='RegionId', description='This parameter is required.'),
}

model ListSupportedConnectorsResponseBody = {
  code?: long(name='Code'),
  data?: [ 
    {
      connector?: string(name='Connector'),
      description?: string(name='Description'),
      iconUrl?: string(name='IconUrl'),
      name?: string(name='Name'),
      sinkSql?: string(name='SinkSql'),
      sourceSql?: string(name='SourceSql'),
    }
  ](name='Data'),
  maxResults?: int32(name='MaxResults'),
  nextToken?: string(name='NextToken'),
  requestId?: string(name='RequestId'),
  success?: boolean(name='Success'),
}

model ListSupportedConnectorsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListSupportedConnectorsResponseBody(name='body'),
}

/**
 * @summary 查询创建 SQL 任务时支持的连接器列表
 *
 * @param request ListSupportedConnectorsRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListSupportedConnectorsResponse
 */
async function listSupportedConnectorsWithOptions(request: ListSupportedConnectorsRequest, runtime: Util.RuntimeOptions): ListSupportedConnectorsResponse {
  Util.validateModel(request);
  var query = {};
  if (!Util.isUnset(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!Util.isUnset(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  var req = new OpenApi.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'ListSupportedConnectors',
    version = '2026-02-02',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 查询创建 SQL 任务时支持的连接器列表
 *
 * @param request ListSupportedConnectorsRequest
 * @return ListSupportedConnectorsResponse
 */
async function listSupportedConnectors(request: ListSupportedConnectorsRequest): ListSupportedConnectorsResponse {
  var runtime = new Util.RuntimeOptions{};
  return listSupportedConnectorsWithOptions(request, runtime);
}

model ReopenComputeInstanceRequest {
  instanceId?: string(name='InstanceId', description='This parameter is required.', example='alikafka_streaming-cn-pe333xxxxxx'),
  regionId?: string(name='RegionId', description='This parameter is required.', example='cn-hangzhou'),
}

model ReopenComputeInstanceResponseBody = {
  code?: long(name='Code', example='200'),
  data?: boolean(name='Data', example='true'),
  requestId?: string(name='RequestId', example='019F3F8E-4159-52B9-8C9B-0C0B63D70A4B'),
  success?: boolean(name='Success', example='true'),
}

model ReopenComputeInstanceResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ReopenComputeInstanceResponseBody(name='body'),
}

/**
 * @summary 重新启动后付费实例
 *
 * @description 重新启用一个已停止的后付费计算实例。接口返回成功表示启用请求已受理。
 * - API版本：2026-02-02
 * - Action：ReopenComputeInstance
 *
 * @param request ReopenComputeInstanceRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ReopenComputeInstanceResponse
 */
async function reopenComputeInstanceWithOptions(request: ReopenComputeInstanceRequest, runtime: Util.RuntimeOptions): ReopenComputeInstanceResponse {
  Util.validateModel(request);
  var query = {};
  if (!Util.isUnset(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!Util.isUnset(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  var req = new OpenApi.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'ReopenComputeInstance',
    version = '2026-02-02',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 重新启动后付费实例
 *
 * @description 重新启用一个已停止的后付费计算实例。接口返回成功表示启用请求已受理。
 * - API版本：2026-02-02
 * - Action：ReopenComputeInstance
 *
 * @param request ReopenComputeInstanceRequest
 * @return ReopenComputeInstanceResponse
 */
async function reopenComputeInstance(request: ReopenComputeInstanceRequest): ReopenComputeInstanceResponse {
  var runtime = new Util.RuntimeOptions{};
  return reopenComputeInstanceWithOptions(request, runtime);
}

model RestartComputeJobRequest {
  clientToken?: string(name='ClientToken'),
  instanceId?: string(name='InstanceId', description='This parameter is required.'),
  jobName?: string(name='JobName', description='This parameter is required.'),
  regionId?: string(name='RegionId', description='This parameter is required.'),
}

model RestartComputeJobResponseBody = {
  code?: long(name='Code'),
  data?: boolean(name='Data'),
  requestId?: string(name='RequestId'),
  success?: boolean(name='Success'),
}

model RestartComputeJobResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: RestartComputeJobResponseBody(name='body'),
}

/**
 * @summary 重启 JOB
 *
 * @param request RestartComputeJobRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return RestartComputeJobResponse
 */
async function restartComputeJobWithOptions(request: RestartComputeJobRequest, runtime: Util.RuntimeOptions): RestartComputeJobResponse {
  Util.validateModel(request);
  var query = {};
  if (!Util.isUnset(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!Util.isUnset(request.jobName)) {
    query['JobName'] = request.jobName;
  }
  if (!Util.isUnset(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  var body : map[string]any = {};
  if (!Util.isUnset(request.clientToken)) {
    body['ClientToken'] = request.clientToken;
  }
  var req = new OpenApi.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApi.Params{
    action = 'RestartComputeJob',
    version = '2026-02-02',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 重启 JOB
 *
 * @param request RestartComputeJobRequest
 * @return RestartComputeJobResponse
 */
async function restartComputeJob(request: RestartComputeJobRequest): RestartComputeJobResponse {
  var runtime = new Util.RuntimeOptions{};
  return restartComputeJobWithOptions(request, runtime);
}

model StartComputeInstanceRequest {
  instanceId?: string(name='InstanceId', description='This parameter is required.', example='alikafka_streaming-cn-pe333xxxxxx'),
  instanceName?: string(name='InstanceName', example='streaming-prod'),
  regionId?: string(name='RegionId', description='This parameter is required.', example='cn-hangzhou'),
  vSwitchIds?: [ string ](name='VSwitchIds', description='This parameter is required.'),
  vpcId?: string(name='VpcId', description='This parameter is required.', example='vpc-bp1abcdefg'),
}

model StartComputeInstanceShrinkRequest {
  instanceId?: string(name='InstanceId', description='This parameter is required.', example='alikafka_streaming-cn-pe333xxxxxx'),
  instanceName?: string(name='InstanceName', example='streaming-prod'),
  regionId?: string(name='RegionId', description='This parameter is required.', example='cn-hangzhou'),
  vSwitchIdsShrink?: string(name='VSwitchIds', description='This parameter is required.'),
  vpcId?: string(name='VpcId', description='This parameter is required.', example='vpc-bp1abcdefg'),
}

model StartComputeInstanceResponseBody = {
  code?: long(name='Code', example='200'),
  data?: boolean(name='Data', example='true'),
  requestId?: string(name='RequestId', example='062D8E8B-8D47-5DCC-BB12-5A1D93C3A66B'),
  success?: boolean(name='Success', example='true'),
}

model StartComputeInstanceResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: StartComputeInstanceResponseBody(name='body'),
}

/**
 * @summary 部署实例
 *
 * @description 为处于待部署状态的计算实例配置网络并发起部署。
 * - API 版本：2026-02-02
 * - Action：StartComputeInstance
 *
 * @param tmpReq StartComputeInstanceRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return StartComputeInstanceResponse
 */
async function startComputeInstanceWithOptions(tmpReq: StartComputeInstanceRequest, runtime: Util.RuntimeOptions): StartComputeInstanceResponse {
  Util.validateModel(tmpReq);
  var request = new StartComputeInstanceShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!Util.isUnset(tmpReq.vSwitchIds)) {
    request.vSwitchIdsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.vSwitchIds, 'VSwitchIds', 'json');
  }
  var query = {};
  if (!Util.isUnset(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!Util.isUnset(request.instanceName)) {
    query['InstanceName'] = request.instanceName;
  }
  if (!Util.isUnset(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  if (!Util.isUnset(request.vSwitchIdsShrink)) {
    query['VSwitchIds'] = request.vSwitchIdsShrink;
  }
  if (!Util.isUnset(request.vpcId)) {
    query['VpcId'] = request.vpcId;
  }
  var req = new OpenApi.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'StartComputeInstance',
    version = '2026-02-02',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 部署实例
 *
 * @description 为处于待部署状态的计算实例配置网络并发起部署。
 * - API 版本：2026-02-02
 * - Action：StartComputeInstance
 *
 * @param request StartComputeInstanceRequest
 * @return StartComputeInstanceResponse
 */
async function startComputeInstance(request: StartComputeInstanceRequest): StartComputeInstanceResponse {
  var runtime = new Util.RuntimeOptions{};
  return startComputeInstanceWithOptions(request, runtime);
}

model StartComputeJobRequest {
  cuLimit?: double(name='CuLimit', example='2.0'),
  cuReserved?: double(name='CuReserved', example='1.0'),
  draftSql?: string(name='DraftSql', example='CREATE TEMPORARY TABLE src (id BIGINT) WITH (\\\\"connector\\\\" = \\\\"datagen\\\\"); CREATE TEMPORARY TABLE sink (id BIGINT) WITH (\\\\"connector\\\\" = \\\\"print\\\\"); INSERT INTO sink SELECT id FROM src;'),
  instanceId?: string(name='InstanceId', description='This parameter is required.', example='alikafka_streaming-cn-pe333xxxxxx'),
  jobName?: string(name='JobName', description='This parameter is required.', example='order_enrichment'),
  recoveryMode?: string(name='RecoveryMode', example='savepoint'),
  regionId?: string(name='RegionId', description='This parameter is required.', example='cn-hangzhou'),
}

model StartComputeJobResponseBody = {
  code?: long(name='Code', example='200'),
  data?: boolean(name='Data', example='true'),
  requestId?: string(name='RequestId', example='062D8E8B-8D47-5DCC-BB12-5A1D93C3A66B'),
  success?: boolean(name='Success', example='true'),
}

model StartComputeJobResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: StartComputeJobResponseBody(name='body'),
}

/**
 * @summary 创建 JOB
 *
 * @description ## 请求说明
 * - `RecoveryMode` 支持两种模式：`savepoint` 和 `stateless`。如果选择 `savepoint` 模式但没有可用的 savepoint，则会返回错误。
 * - `CuLimit` 和 `CuReserved` 参数分别用来设定作业的 CU 上限和预留 CU 数量，支持整数或小数形式输入。
 * - 确保提供的 `RegionId`, `InstanceId`, 和 `JobName` 参数值正确且存在，否则将导致请求失败。
 *
 * @param request StartComputeJobRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return StartComputeJobResponse
 */
async function startComputeJobWithOptions(request: StartComputeJobRequest, runtime: Util.RuntimeOptions): StartComputeJobResponse {
  Util.validateModel(request);
  var query = {};
  if (!Util.isUnset(request.cuLimit)) {
    query['CuLimit'] = request.cuLimit;
  }
  if (!Util.isUnset(request.cuReserved)) {
    query['CuReserved'] = request.cuReserved;
  }
  if (!Util.isUnset(request.draftSql)) {
    query['DraftSql'] = request.draftSql;
  }
  if (!Util.isUnset(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!Util.isUnset(request.jobName)) {
    query['JobName'] = request.jobName;
  }
  if (!Util.isUnset(request.recoveryMode)) {
    query['RecoveryMode'] = request.recoveryMode;
  }
  if (!Util.isUnset(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  var req = new OpenApi.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'StartComputeJob',
    version = '2026-02-02',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 创建 JOB
 *
 * @description ## 请求说明
 * - `RecoveryMode` 支持两种模式：`savepoint` 和 `stateless`。如果选择 `savepoint` 模式但没有可用的 savepoint，则会返回错误。
 * - `CuLimit` 和 `CuReserved` 参数分别用来设定作业的 CU 上限和预留 CU 数量，支持整数或小数形式输入。
 * - 确保提供的 `RegionId`, `InstanceId`, 和 `JobName` 参数值正确且存在，否则将导致请求失败。
 *
 * @param request StartComputeJobRequest
 * @return StartComputeJobResponse
 */
async function startComputeJob(request: StartComputeJobRequest): StartComputeJobResponse {
  var runtime = new Util.RuntimeOptions{};
  return startComputeJobWithOptions(request, runtime);
}

model StopComputeInstanceRequest {
  instanceId?: string(name='InstanceId', description='This parameter is required.', example='alikafka_streaming-cn-pe333xxxxxx'),
  regionId?: string(name='RegionId', description='This parameter is required.', example='cn-hangzhou'),
}

model StopComputeInstanceResponseBody = {
  code?: long(name='Code', example='200'),
  data?: boolean(name='Data', example='true'),
  requestId?: string(name='RequestId', example='B353C700-D3F6-5325-8A1C-376CE8E04DC4'),
  success?: boolean(name='Success', example='true'),
}

model StopComputeInstanceResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: StopComputeInstanceResponseBody(name='body'),
}

/**
 * @summary 停用/释放后付费实例
 *
 * @description 停止一个正在运行的后付费计算实例。接口返回成功表示停止请求已受理。
 * - API 版本：2026-02-02
 * - Action：StopComputeInstance
 *
 * @param request StopComputeInstanceRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return StopComputeInstanceResponse
 */
async function stopComputeInstanceWithOptions(request: StopComputeInstanceRequest, runtime: Util.RuntimeOptions): StopComputeInstanceResponse {
  Util.validateModel(request);
  var query = {};
  if (!Util.isUnset(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!Util.isUnset(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  var req = new OpenApi.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'StopComputeInstance',
    version = '2026-02-02',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 停用/释放后付费实例
 *
 * @description 停止一个正在运行的后付费计算实例。接口返回成功表示停止请求已受理。
 * - API 版本：2026-02-02
 * - Action：StopComputeInstance
 *
 * @param request StopComputeInstanceRequest
 * @return StopComputeInstanceResponse
 */
async function stopComputeInstance(request: StopComputeInstanceRequest): StopComputeInstanceResponse {
  var runtime = new Util.RuntimeOptions{};
  return stopComputeInstanceWithOptions(request, runtime);
}

model StopComputeJobRequest {
  instanceId?: string(name='InstanceId', description='This parameter is required.', example='alikafka_streaming-cn-pe333xxxxxx'),
  jobName?: string(name='JobName', description='This parameter is required.', example='order_enrichment'),
  regionId?: string(name='RegionId', description='This parameter is required.', example='cn-hangzhou'),
}

model StopComputeJobResponseBody = {
  code?: long(name='Code', example='200'),
  data?: boolean(name='Data', example='true'),
  requestId?: string(name='RequestId', example='062D8E8B-8D47-5DCC-BB12-5A1D93C3A66B'),
  success?: boolean(name='Success', example='true'),
}

model StopComputeJobResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: StopComputeJobResponseBody(name='body'),
}

/**
 * @summary 停止 JOB
 *
 * @description ## 请求说明
 * - 该接口用于停止指定的计算作业生产或 Debug 运行实例。
 * - 接口返回成功表示停止请求已被受理，但并不意味着作业立即停止。
 *
 * @param request StopComputeJobRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return StopComputeJobResponse
 */
async function stopComputeJobWithOptions(request: StopComputeJobRequest, runtime: Util.RuntimeOptions): StopComputeJobResponse {
  Util.validateModel(request);
  var query = {};
  if (!Util.isUnset(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!Util.isUnset(request.jobName)) {
    query['JobName'] = request.jobName;
  }
  if (!Util.isUnset(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  var req = new OpenApi.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'StopComputeJob',
    version = '2026-02-02',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 停止 JOB
 *
 * @description ## 请求说明
 * - 该接口用于停止指定的计算作业生产或 Debug 运行实例。
 * - 接口返回成功表示停止请求已被受理，但并不意味着作业立即停止。
 *
 * @param request StopComputeJobRequest
 * @return StopComputeJobResponse
 */
async function stopComputeJob(request: StopComputeJobRequest): StopComputeJobResponse {
  var runtime = new Util.RuntimeOptions{};
  return stopComputeJobWithOptions(request, runtime);
}

model UpdateComputeInstanceNameRequest {
  instanceId?: string(name='InstanceId', description='This parameter is required.', example='alikafka_streaming-cn-pe333xxxxxx'),
  instanceName?: string(name='InstanceName', description='This parameter is required.', example='streaming-prod'),
  regionId?: string(name='RegionId', description='This parameter is required.', example='cn-hangzhou'),
}

model UpdateComputeInstanceNameResponseBody = {
  code?: long(name='Code', example='200'),
  data?: boolean(name='Data', example='true'),
  requestId?: string(name='RequestId', example='019F6DD7-A85E-56CF-BF45-BACA73D3FC2F'),
  success?: boolean(name='Success', example='true'),
}

model UpdateComputeInstanceNameResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateComputeInstanceNameResponseBody(name='body'),
}

/**
 * @summary 更新实例名称
 *
 * @description 修改计算实例名称。实例需处于部署准备阶段或运行中状态。
 * - API 版本：2026-02-02
 * - Action：UpdateComputeInstanceName
 *
 * @param request UpdateComputeInstanceNameRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateComputeInstanceNameResponse
 */
async function updateComputeInstanceNameWithOptions(request: UpdateComputeInstanceNameRequest, runtime: Util.RuntimeOptions): UpdateComputeInstanceNameResponse {
  Util.validateModel(request);
  var query = {};
  if (!Util.isUnset(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!Util.isUnset(request.instanceName)) {
    query['InstanceName'] = request.instanceName;
  }
  if (!Util.isUnset(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  var req = new OpenApi.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'UpdateComputeInstanceName',
    version = '2026-02-02',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 更新实例名称
 *
 * @description 修改计算实例名称。实例需处于部署准备阶段或运行中状态。
 * - API 版本：2026-02-02
 * - Action：UpdateComputeInstanceName
 *
 * @param request UpdateComputeInstanceNameRequest
 * @return UpdateComputeInstanceNameResponse
 */
async function updateComputeInstanceName(request: UpdateComputeInstanceNameRequest): UpdateComputeInstanceNameResponse {
  var runtime = new Util.RuntimeOptions{};
  return updateComputeInstanceNameWithOptions(request, runtime);
}

model UpdateComputeJobRequest {
  instanceId?: string(name='InstanceId', description='This parameter is required.', example='alikafka_streaming-cn-a1b2c3d4'),
  jobName?: string(name='JobName', description='This parameter is required.', example='order_enrichment'),
  regionId?: string(name='RegionId', description='This parameter is required.', example='cn-hangzhou'),
  remark?: string(name='Remark', example='订单流实时清洗'),
}

model UpdateComputeJobResponseBody = {
  code?: long(name='Code', example='200'),
  data?: boolean(name='Data', example='true'),
  requestId?: string(name='RequestId', example='062D8E8B-8D47-5DCC-BB12-5A1D93C3A66B'),
  success?: boolean(name='Success', example='true'),
}

model UpdateComputeJobResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateComputeJobResponseBody(name='body'),
}

/**
 * @summary 更新 JOB
 *
 * @description ## 请求说明
 * - 确保提供的 `InstanceId` 和 `JobName` 是有效的，否则将返回错误。
 * - 如果实例状态不在运行中，则不允许执行此操作。
 * - 当前作业状态如果为调试任务正在运行或变更中，则不支持修改。
 *
 * @param request UpdateComputeJobRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateComputeJobResponse
 */
async function updateComputeJobWithOptions(request: UpdateComputeJobRequest, runtime: Util.RuntimeOptions): UpdateComputeJobResponse {
  Util.validateModel(request);
  var query = {};
  if (!Util.isUnset(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!Util.isUnset(request.jobName)) {
    query['JobName'] = request.jobName;
  }
  if (!Util.isUnset(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  if (!Util.isUnset(request.remark)) {
    query['Remark'] = request.remark;
  }
  var req = new OpenApi.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'UpdateComputeJob',
    version = '2026-02-02',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 更新 JOB
 *
 * @description ## 请求说明
 * - 确保提供的 `InstanceId` 和 `JobName` 是有效的，否则将返回错误。
 * - 如果实例状态不在运行中，则不允许执行此操作。
 * - 当前作业状态如果为调试任务正在运行或变更中，则不支持修改。
 *
 * @param request UpdateComputeJobRequest
 * @return UpdateComputeJobResponse
 */
async function updateComputeJob(request: UpdateComputeJobRequest): UpdateComputeJobResponse {
  var runtime = new Util.RuntimeOptions{};
  return updateComputeJobWithOptions(request, runtime);
}

model UpdateComputeJobCuRequest {
  cuLimit?: double(name='CuLimit', description='This parameter is required.', example='2.0'),
  cuReserved?: double(name='CuReserved', description='This parameter is required.', example='1.0'),
  instanceId?: string(name='InstanceId', description='This parameter is required.', example='alikafka_streaming-cn-a1b2c3d4'),
  jobName?: string(name='JobName', description='This parameter is required.', example='order_enrichment'),
  regionId?: string(name='RegionId', description='This parameter is required.', example='cn-hangzhou'),
}

model UpdateComputeJobCuResponseBody = {
  code?: long(name='Code', example='200'),
  data?: boolean(name='Data', example='true'),
  requestId?: string(name='RequestId', example='062D8E8B-8D47-5DCC-BB12-5A1D93C3A66B'),
  success?: boolean(name='Success', example='true'),
}

model UpdateComputeJobCuResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateComputeJobCuResponseBody(name='body'),
}

/**
 * @summary 更新 JOB 的 CU 配额
 *
 * @description ## 请求说明
 * 本API允许用户修改特定计算作业的计算单元（CU）上限和预留CU数量。在调用此接口前，请确保提供的`InstanceId`和`JobName`正确无误，并且实例处于运行状态。此外，注意检查`CuLimit`与`CuReserved`参数的有效性和合理性，避免因超出限制或不符合业务逻辑导致请求失败。
 *
 * @param request UpdateComputeJobCuRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateComputeJobCuResponse
 */
async function updateComputeJobCuWithOptions(request: UpdateComputeJobCuRequest, runtime: Util.RuntimeOptions): UpdateComputeJobCuResponse {
  Util.validateModel(request);
  var query = {};
  if (!Util.isUnset(request.cuLimit)) {
    query['CuLimit'] = request.cuLimit;
  }
  if (!Util.isUnset(request.cuReserved)) {
    query['CuReserved'] = request.cuReserved;
  }
  if (!Util.isUnset(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!Util.isUnset(request.jobName)) {
    query['JobName'] = request.jobName;
  }
  if (!Util.isUnset(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  var req = new OpenApi.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'UpdateComputeJobCu',
    version = '2026-02-02',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 更新 JOB 的 CU 配额
 *
 * @description ## 请求说明
 * 本API允许用户修改特定计算作业的计算单元（CU）上限和预留CU数量。在调用此接口前，请确保提供的`InstanceId`和`JobName`正确无误，并且实例处于运行状态。此外，注意检查`CuLimit`与`CuReserved`参数的有效性和合理性，避免因超出限制或不符合业务逻辑导致请求失败。
 *
 * @param request UpdateComputeJobCuRequest
 * @return UpdateComputeJobCuResponse
 */
async function updateComputeJobCu(request: UpdateComputeJobCuRequest): UpdateComputeJobCuResponse {
  var runtime = new Util.RuntimeOptions{};
  return updateComputeJobCuWithOptions(request, runtime);
}

model UpdateComputeJobDraftSqlRequest {
  draftSql?: string(name='DraftSql', description='This parameter is required.', example='INSERT INTO sink_table SELECT * FROM source_table;'),
  instanceId?: string(name='InstanceId', description='This parameter is required.', example='alikafka_streaming-cn-a1b2c3d4'),
  jobName?: string(name='JobName', description='This parameter is required.', example='order_enrichment'),
  regionId?: string(name='RegionId', description='This parameter is required.', example='cn-hangzhou'),
}

model UpdateComputeJobDraftSqlResponseBody = {
  code?: long(name='Code', example='200'),
  data?: boolean(name='Data', example='true'),
  requestId?: string(name='RequestId', example='062D8E8B-8D47-5DCC-BB12-5A1D93C3A66B'),
  success?: boolean(name='Success', example='true'),
}

model UpdateComputeJobDraftSqlResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateComputeJobDraftSqlResponseBody(name='body'),
}

/**
 * @summary 更新 JOB 的 SQL
 *
 * @description ## 请求说明
 * 本接口用于更新特定计算实例下的某个计算作业所保存的Flink SQL草稿内容。请确保提供的`InstanceId`和`JobName`准确无误，并且该作业当前状态支持进行SQL修改操作。
 * - **注意事项**：
 *   - 确保目标实例处于运行状态。
 *   - 当前作业状态需允许修改SQL，即作业不应处于调试或变更过程中。
 *   - `DraftSql`参数应包含完整的、格式正确的Flink SQL语句。
 *
 * @param request UpdateComputeJobDraftSqlRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateComputeJobDraftSqlResponse
 */
async function updateComputeJobDraftSqlWithOptions(request: UpdateComputeJobDraftSqlRequest, runtime: Util.RuntimeOptions): UpdateComputeJobDraftSqlResponse {
  Util.validateModel(request);
  var query = {};
  if (!Util.isUnset(request.draftSql)) {
    query['DraftSql'] = request.draftSql;
  }
  if (!Util.isUnset(request.instanceId)) {
    query['InstanceId'] = request.instanceId;
  }
  if (!Util.isUnset(request.jobName)) {
    query['JobName'] = request.jobName;
  }
  if (!Util.isUnset(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  var req = new OpenApi.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApi.Params{
    action = 'UpdateComputeJobDraftSql',
    version = '2026-02-02',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 更新 JOB 的 SQL
 *
 * @description ## 请求说明
 * 本接口用于更新特定计算实例下的某个计算作业所保存的Flink SQL草稿内容。请确保提供的`InstanceId`和`JobName`准确无误，并且该作业当前状态支持进行SQL修改操作。
 * - **注意事项**：
 *   - 确保目标实例处于运行状态。
 *   - 当前作业状态需允许修改SQL，即作业不应处于调试或变更过程中。
 *   - `DraftSql`参数应包含完整的、格式正确的Flink SQL语句。
 *
 * @param request UpdateComputeJobDraftSqlRequest
 * @return UpdateComputeJobDraftSqlResponse
 */
async function updateComputeJobDraftSql(request: UpdateComputeJobDraftSqlRequest): UpdateComputeJobDraftSqlResponse {
  var runtime = new Util.RuntimeOptions{};
  return updateComputeJobDraftSqlWithOptions(request, runtime);
}

