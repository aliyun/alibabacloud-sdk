/**
 *
 */
import OpenApi;
import OpenApi.OpenApiUtil;

extends OpenApi;


init(config: OpenApiUtil.Config){
  super(config);
  @endpointRule = 'regional';
  @endpointMap = {
    'cn-hangzhou' = 'sysom.cn-hangzhou.aliyuncs.com',
  };

  checkConfig(config);
  @endpoint = getEndpoint('sysom', @regionId, @endpointRule, @network, @suffix, @endpointMap, @endpoint);
}

function getEndpoint(productId: string, regionId: string, endpointRule: string, network: string, suffix: string, endpointMap: map[string]string, endpoint: string) throws: string{
  if (!$isNull(endpoint)) {
    return endpoint;
  }
  
  if (!$isNull(endpointMap) && !$isNull(endpointMap[regionId])) {
    return endpointMap[regionId];
  }
  return OpenApiUtil.getEndpointRules(productId, regionId, endpointRule, network, suffix);
}

model AuthDiagnosisRequest {
  autoCreateRole?: boolean(name='autoCreateRole', description='Specifies whether to enable automatic creation of the service-linked role.'),
  autoInstallAgent?: boolean(name='autoInstallAgent', description='Specifies whether to automatically install the agent.'),
  instances?: [ 
    {
      instance?: string(name='instance', description='The instance ID.', example='i-wz9b9vucz1iubsz8sjqo'),
      region?: string(name='region', description='The region ID.', example='cn-hangzhou'),
    }
  ](name='instances', description='The list of instances to authorize for diagnostics.'),
}

model AuthDiagnosisResponseBody = {
  code?: string(name='code', description='The status code.
- `code == Success` indicates that the authorization is successful.
- Other status codes indicate that the authorization failed. Check the `message` field for the detailed fault information.', example='Success'),
  data?: any(name='data', description='This operation does not return data.', example='{}'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the error information of the request.', example='SysomOpenAPIAssumeRoleException: EntityNotExist.Role The role not exists: acs:ram::xxxxx:role/aliyunserviceroleforsysom'),
  requestId?: string(name='request_id', description='The request ID.', example='35F91AAB-5FDF-5A22-B211-C7C6B00817D0'),
}

model AuthDiagnosisResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: AuthDiagnosisResponseBody(name='body'),
}

/**
 * @summary Authorizes SysOM to diagnose ECS instances under the current account. You must call this operation to authorize diagnostics for a specific ECS instance before you can call the InvokeDiagnosis operation to initiate diagnostics on it.
 *
 * @description >Notice: The diagnostics feature requires a service-linked role to be created under the Resource Access Management (RAM) user. This operation automatically checks whether the service-linked role exists and creates it if it does not. The RAM user that invokes this operation must have the ram:CreateServiceLinkedRole permission.</notice>
 * Note the following when you invoke this operation to authorize SysOM to diagnose ECS instances:
 * - Each authorization is valid for 7 days. After the authorization expires, invoke this operation again to re-authorize.
 * - If the SysOM service-linked role (AliyunServiceRoleForSysom) does not exist when you invoke this operation, automatic creation is performed. The RAM user that invokes this operation must have the `ram:CreateServiceLinkedRole` permission.
 * - When you invoke this operation to authorize diagnostics for a specific instance, the label `sysom:diagnosis` is automatically associated with the target ECS instance. SysOM only allows diagnostics on instances that have this label.
 *
 * @param request AuthDiagnosisRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return AuthDiagnosisResponse
 */
@context("authDiagnosisWithContext")
async function authDiagnosisWithOptions(request: AuthDiagnosisRequest, headers: map[string]string, runtime: $RuntimeOptions): AuthDiagnosisResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.autoCreateRole)) {
    body['autoCreateRole'] = request.autoCreateRole;
  }
  if (!$isNull(request.autoInstallAgent)) {
    body['autoInstallAgent'] = request.autoInstallAgent;
  }
  if (!$isNull(request.instances)) {
    body['instances'] = request.instances;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'AuthDiagnosis',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/openapi/diagnosis/auth`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Authorizes SysOM to diagnose ECS instances under the current account. You must call this operation to authorize diagnostics for a specific ECS instance before you can call the InvokeDiagnosis operation to initiate diagnostics on it.
 *
 * @description >Notice: The diagnostics feature requires a service-linked role to be created under the Resource Access Management (RAM) user. This operation automatically checks whether the service-linked role exists and creates it if it does not. The RAM user that invokes this operation must have the ram:CreateServiceLinkedRole permission.</notice>
 * Note the following when you invoke this operation to authorize SysOM to diagnose ECS instances:
 * - Each authorization is valid for 7 days. After the authorization expires, invoke this operation again to re-authorize.
 * - If the SysOM service-linked role (AliyunServiceRoleForSysom) does not exist when you invoke this operation, automatic creation is performed. The RAM user that invokes this operation must have the `ram:CreateServiceLinkedRole` permission.
 * - When you invoke this operation to authorize diagnostics for a specific instance, the label `sysom:diagnosis` is automatically associated with the target ECS instance. SysOM only allows diagnostics on instances that have this label.
 *
 * @param request AuthDiagnosisRequest
 * @return AuthDiagnosisResponse
 */
async function authDiagnosis(request: AuthDiagnosisRequest): AuthDiagnosisResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return authDiagnosisWithOptions(request, headers, runtime);
}

model CheckInstanceSupportRequest {
  instances?: [ string ](name='instances', description='The list of instance IDs to check.', example='["i-2zxxxxxx"]'),
  region?: string(name='region', description='The region to which the instances belong. All instance IDs specified in instances must belong to the same region.', example='cn-hangzhou'),
}

model CheckInstanceSupportResponseBody = {
  code?: string(name='code', description='The status code.
- `code == Success` indicates that the authorization is successful.
- Other status codes indicate that the authorization failed. Check the `message` field for the detailed fault information.', example='Success'),
  data?: [ 
    {
      instance?: string(name='instance', description='The ECS instance ID.', example='i-wz9d00ut2ska3mlyhn6j'),
      reason?: string(name='reason', description='The reason why the instance cannot be managed. This field is not empty when success is false.', example='instance not found in ecs'),
      support?: boolean(name='support', description='Indicates whether the instance can be managed by SysOM.

- **true**: The instance can be managed by SysOM.

- **false**: The instance cannot be managed by SysOM.', example='true'),
    }
  ](name='data', description='The returned data.'),
  message?: string(name='message', description='The error message. This field contains the error information when code is not Success.', example='SysomOpenAPIAssumeRoleException: EntityNotExist.Role The role not exists: acs:ram::xxxxx:role/aliyunserviceroleforsysom'),
  requestId?: string(name='requestId', description='Id of the request', example='2D693121-C925-5154-8DF6-C09A8B369822'),
}

model CheckInstanceSupportResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CheckInstanceSupportResponseBody(name='body'),
}

/**
 * @summary Checks whether a target instance is supported by SysOM.
 *
 * @description This operation retrieves the list of instances that are already managed by SysOM. If an ECS instance exists but is not managed by SysOM, it does not appear in the list.
 *
 * @param request CheckInstanceSupportRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CheckInstanceSupportResponse
 */
@context("checkInstanceSupportWithContext")
async function checkInstanceSupportWithOptions(request: CheckInstanceSupportRequest, headers: map[string]string, runtime: $RuntimeOptions): CheckInstanceSupportResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.instances)) {
    body['instances'] = request.instances;
  }
  if (!$isNull(request.region)) {
    body['region'] = request.region;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CheckInstanceSupport',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/am/supportInstanceList/checkInstanceSupport`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Checks whether a target instance is supported by SysOM.
 *
 * @description This operation retrieves the list of instances that are already managed by SysOM. If an ECS instance exists but is not managed by SysOM, it does not appear in the list.
 *
 * @param request CheckInstanceSupportRequest
 * @return CheckInstanceSupportResponse
 */
async function checkInstanceSupport(request: CheckInstanceSupportRequest): CheckInstanceSupportResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return checkInstanceSupportWithOptions(request, headers, runtime);
}

model CpuHighAgentStreamResponseRequest {
  llmParamString?: string(name='llmParamString', description='The input parameter for the CPU High Agent service. Refer to the standard LLM API input parameter dict, convert it to a string, and pass it in the llmParamString field.', example='"llmParamString": "{\\\\"messages\\\\": [{\\\\"role\\\\": \\\\"user\\\\", \\\\"content\\\\": \\\\"用户12345的机器i-67890，最近2分钟CPU使用率高，请结合最近2分钟的火焰图信息，分析原因\\\\"}]}"'),
}

model CpuHighAgentStreamResponseResponseBody = {
  code?: string(name='code', description='The status code.
- If code is Success, the authorization is successful.
- Other status codes indicate authorization failed. Check the message field for the detailed fault information.', example='Success'),
  data?: string(name='data', description='The response data.', example='<SSEResponse>'),
  message?: string(name='message', description='The error message.
- If code is Success, this field is empty.
- Otherwise, this field contains the request error information.', example='SysomOpenAPIAssumeRoleException: EntityNotExist.Role The role not exists: acs:ram::xxxxx:role/aliyunserviceroleforsysom'),
  requestId?: string(name='requestId', description='Id of the request', example='2D693121-C925-5154-8DF6-C09A8B369822'),
}

model CpuHighAgentStreamResponseResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  id?: string(name='id'),
  event?: string(name='event'),
  body?: CpuHighAgentStreamResponseResponseBody(name='body'),
}

/**
 * @summary Calls the CPU High Agent streaming SSE interface.
 *
 * @param request CpuHighAgentStreamResponseRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CpuHighAgentStreamResponseResponse
 */
@sse(true)
@context("cpuHighAgentStreamResponseWithSSECtx")
async function cpuHighAgentStreamResponseWithSSE(request: CpuHighAgentStreamResponseRequest, headers: map[string]string, runtime: $RuntimeOptions): asyncIterator[CpuHighAgentStreamResponseResponse] {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.llmParamString)) {
    body['llmParamString'] = request.llmParamString;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CpuHighAgentStreamResponse',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/highCpuAgent/streamResponse`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  var sseResp = callSSEApi(params, req, runtime);
  for (var resp : sseResp) {
    if (!$isNull(resp.event) && !$isNull(resp.event.data)) {
      var data = $object($JSON.parseJSON(resp.event.data));
      yield {
        statusCode = resp.statusCode,
        headers = resp.headers,
        id = resp.event.id,
        event = resp.event.event,
        body = data
      };
    }
  }
}
/**
 * @summary Calls the CPU High Agent streaming SSE interface.
 *
 * @param request CpuHighAgentStreamResponseRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CpuHighAgentStreamResponseResponse
 */
@context("cpuHighAgentStreamResponseWithContext")
async function cpuHighAgentStreamResponseWithOptions(request: CpuHighAgentStreamResponseRequest, headers: map[string]string, runtime: $RuntimeOptions): CpuHighAgentStreamResponseResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.llmParamString)) {
    body['llmParamString'] = request.llmParamString;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CpuHighAgentStreamResponse',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/highCpuAgent/streamResponse`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Calls the CPU High Agent streaming SSE interface.
 *
 * @param request CpuHighAgentStreamResponseRequest
 * @return CpuHighAgentStreamResponseResponse
 */
async function cpuHighAgentStreamResponse(request: CpuHighAgentStreamResponseRequest): CpuHighAgentStreamResponseResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return cpuHighAgentStreamResponseWithOptions(request, headers, runtime);
}

model CreateAlertDestinationRequest {
  name?: string(name='name', description='The name of the alert contact.', example='test_name'),
  params?: {
    email?: string(name='email', description='The email address.', example='xxx@email.com'),
    phone?: string(name='phone', description='The phone number.', example='1xxx'),
    sec?: string(name='sec', description='The secret key of the chatbot.', example='SECxxx'),
    webhook?: string(name='webhook', description='The webhook URL of the chatbot.', example='https://oapi.dingtalk.com/robot/send?access_token=xxx'),
  }(name='params', description='The configuration parameters of the alert contact.'),
  source?: string(name='source', description='The configuration source.', example='console'),
  target?: string(name='target', description='The notification target. Currently, only DingTalk chatbots are supported.', example='dingtalk'),
}

model CreateAlertDestinationResponseBody = {
  code?: string(name='code', description='The error code.', example='Success'),
  data?: {
    createdAt?: long(name='createdAt', description='The creation time.', example='1753669116286'),
    id?: int32(name='id', description='The policy ID.', example='1'),
    name?: string(name='name', description='The policy name.', example='SysOM'),
    params?: {
      email?: string(name='email', description='The email address.', example='xxx@email.com'),
      phone?: string(name='phone', description='The phone number.', example='1xxx'),
      sec?: string(name='sec', description='The secret key of the chatbot.', example='SECxxx'),
      webhook?: string(name='webhook', description='The webhook URL of the chatbot.', example='https://oapi.dingtalk.com/robot/send?access_token=xxx'),
    }(name='params', description='The configuration parameters of the alert contact.'),
    source?: string(name='source', description='The configuration source.', example='console'),
    target?: string(name='target', description='The notification target. Currently, only DingTalk chatbots are supported.', example='dingtalk'),
    uid?: string(name='uid', description='The user ID.', example='1222933234714935'),
    updatedAt?: long(name='updatedAt', description='The update time.', example='1751254826285'),
  }(name='data', description='The returned data.'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the error message of the request.', example='SysomOpenAPIException: SysomOpenAPI.InvalidParameter Invalid params, should be json string or dict'),
  requestId?: string(name='requestId', description='The request ID.', example='2D693121-C925-5154-8DF6-C09A8B369822'),
}

model CreateAlertDestinationResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateAlertDestinationResponseBody(name='body'),
}

/**
 * @summary Creates a contact for alert notifications.
 *
 * @param request CreateAlertDestinationRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateAlertDestinationResponse
 */
@context("createAlertDestinationWithContext")
async function createAlertDestinationWithOptions(request: CreateAlertDestinationRequest, headers: map[string]string, runtime: $RuntimeOptions): CreateAlertDestinationResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.name)) {
    body['name'] = request.name;
  }
  if (!$isNull(request.params)) {
    body['params'] = request.params;
  }
  if (!$isNull(request.source)) {
    body['source'] = request.source;
  }
  if (!$isNull(request.target)) {
    body['target'] = request.target;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateAlertDestination',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/alertPusher/alert/createDestination`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates a contact for alert notifications.
 *
 * @param request CreateAlertDestinationRequest
 * @return CreateAlertDestinationResponse
 */
async function createAlertDestination(request: CreateAlertDestinationRequest): CreateAlertDestinationResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return createAlertDestinationWithOptions(request, headers, runtime);
}

model CreateAlertStrategyRequest {
  enabled?: boolean(name='enabled', description='Specifies whether the alert strategy is enabled.

This parameter is required.', example='false'),
  k8sLabel?: boolean(name='k8sLabel'),
  name?: string(name='name', description='The name of the alert strategy.

This parameter is required.', example='strategy1'),
  strategy?: {
    clusters?: [ string ](name='clusters', description='The collection of clusters for which alerts are received.'),
    destinations?: [ int32 ](name='destinations'),
    items?: [ string ](name='items', description='The collection of anomaly items for which alerts are received.'),
  }(name='strategy', description='The details of the alert strategy.

This parameter is required.'),
}

model CreateAlertStrategyResponseBody = {
  code?: string(name='code', description='The status code.
- If `code == Success`, the request is successful.
- Other status codes indicate a failed request. If the request fails, check the `message` field for detailed error information.', example='Success'),
  data?: any(name='data', description='The response data.', example='{
            "uid": "uid-1",
            "name": "test-name",
            "strategy": {
                "clusters": ["test-cluster-1","test-cluster-2"],
                "items": ["test-item1","test-item2"]
            },
            "enabled": True,
          
        }'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the error information of the request.', example='创建策略失败'),
  requestId?: string(name='requestId', description='Id of the request', example='2D693121-C925-5154-8DF6-C09A8B369822'),
}

model CreateAlertStrategyResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateAlertStrategyResponseBody(name='body'),
}

/**
 * @summary Creates an alert push strategy.
 *
 * @param request CreateAlertStrategyRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateAlertStrategyResponse
 */
@context("createAlertStrategyWithContext")
async function createAlertStrategyWithOptions(request: CreateAlertStrategyRequest, headers: map[string]string, runtime: $RuntimeOptions): CreateAlertStrategyResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.enabled)) {
    body['enabled'] = request.enabled;
  }
  if (!$isNull(request.k8sLabel)) {
    body['k8sLabel'] = request.k8sLabel;
  }
  if (!$isNull(request.name)) {
    body['name'] = request.name;
  }
  if (!$isNull(request.strategy)) {
    body['strategy'] = request.strategy;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateAlertStrategy',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/alertPusher/alert/createStrategy`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates an alert push strategy.
 *
 * @param request CreateAlertStrategyRequest
 * @return CreateAlertStrategyResponse
 */
async function createAlertStrategy(request: CreateAlertStrategyRequest): CreateAlertStrategyResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return createAlertStrategyWithOptions(request, headers, runtime);
}

model CreateClusterVpcEndpointConnectionRequest {
  clusterId?: string(name='clusterId', description='This parameter is required.', example='ray-bzxw7g2r7301r3f2'),
  dryRun?: boolean(name='dryRun', example='false'),
  region?: string(name='region', description='This parameter is required.', example='cn-hangzhou'),
}

model CreateClusterVpcEndpointConnectionResponseBody = {
  code?: string(name='code', example='Success'),
  data?: {
    endpointConnectionId?: string(name='endpointConnectionId', example='ep-xxx'),
  }(name='data'),
  message?: string(name='message', example='success'),
  requestId?: string(name='requestId', example='2D693121-C925-5154-8DF6-C09A8B369822'),
}

model CreateClusterVpcEndpointConnectionResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateClusterVpcEndpointConnectionResponseBody(name='body'),
}

/**
 * @summary 创建集群Vpc端点连接
 *
 * @description - 需配合aliyun-tea-openapi-inner包的call_sseapi接口使用
 * - 需要按通用LLM服务输入参数填充参数，转为string后赋给llmParamString
 * - 返回数据需将string转为dict后使用，参考通用LLM服务返回格式
 *
 * @param request CreateClusterVpcEndpointConnectionRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateClusterVpcEndpointConnectionResponse
 */
@context("createClusterVpcEndpointConnectionWithContext")
async function createClusterVpcEndpointConnectionWithOptions(request: CreateClusterVpcEndpointConnectionRequest, headers: map[string]string, runtime: $RuntimeOptions): CreateClusterVpcEndpointConnectionResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.clusterId)) {
    body['clusterId'] = request.clusterId;
  }
  if (!$isNull(request.dryRun)) {
    body['dryRun'] = request.dryRun;
  }
  if (!$isNull(request.region)) {
    body['region'] = request.region;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateClusterVpcEndpointConnection',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/k8sProxy/access/createClusterVpcEndpointConnection`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 创建集群Vpc端点连接
 *
 * @description - 需配合aliyun-tea-openapi-inner包的call_sseapi接口使用
 * - 需要按通用LLM服务输入参数填充参数，转为string后赋给llmParamString
 * - 返回数据需将string转为dict后使用，参考通用LLM服务返回格式
 *
 * @param request CreateClusterVpcEndpointConnectionRequest
 * @return CreateClusterVpcEndpointConnectionResponse
 */
async function createClusterVpcEndpointConnection(request: CreateClusterVpcEndpointConnectionRequest): CreateClusterVpcEndpointConnectionResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return createClusterVpcEndpointConnectionWithOptions(request, headers, runtime);
}

model CreateInstanceInspectionRequest {
  instance?: string(name='instance', description='The instance ID.', example='i-2ze5ru5rjurix7f71sxv'),
  items?: [ string ](name='items', description='异常项'),
  metricSource?: string(name='metricSource', description='The metric source.', example='sysom'),
  region?: string(name='region', description='The region to which the instance belongs.', example='cn-shenzhen'),
  source?: string(name='source', description='The source.', example='console'),
}

model CreateInstanceInspectionResponseBody = {
  code?: string(name='code', description='The error code.', example='Success'),
  data?: any(name='data', description='The returned data.', example='reportId'),
  message?: string(name='message', description='The description.', example='success'),
  requestId?: string(name='requestId', description='Id of the request', example='2D693121-C925-5154-8DF6-C09A8B369822'),
}

model CreateInstanceInspectionResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateInstanceInspectionResponseBody(name='body'),
}

/**
 * @summary Creates a SysOM instance inspection.
 *
 * @param request CreateInstanceInspectionRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateInstanceInspectionResponse
 */
@context("createInstanceInspectionWithContext")
async function createInstanceInspectionWithOptions(request: CreateInstanceInspectionRequest, headers: map[string]string, runtime: $RuntimeOptions): CreateInstanceInspectionResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.instance)) {
    body['instance'] = request.instance;
  }
  if (!$isNull(request.items)) {
    body['items'] = request.items;
  }
  if (!$isNull(request.metricSource)) {
    body['metricSource'] = request.metricSource;
  }
  if (!$isNull(request.region)) {
    body['region'] = request.region;
  }
  if (!$isNull(request.source)) {
    body['source'] = request.source;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateInstanceInspection',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/inspection/createInstanceInspection`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates a SysOM instance inspection.
 *
 * @param request CreateInstanceInspectionRequest
 * @return CreateInstanceInspectionResponse
 */
async function createInstanceInspection(request: CreateInstanceInspectionRequest): CreateInstanceInspectionResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return createInstanceInspectionWithOptions(request, headers, runtime);
}

model CreateVmcoreDiagnosisTaskRequest {
  debuginfoCommonUrl?: string(name='debuginfoCommonUrl', description='The download URL of the debuginfo-common file. This parameter is optional when the diagnostic type is vmcore.

For CentOS or Alinux kernels, the corresponding debuginfo-common file is automatically downloaded, and you do not need to specify this parameter. For other distribution kernels, manually provide the download URL of the debuginfo-common file that corresponds to the kernel version.', example='https://bucket-cn-hangzhou.oss-cn-hangzhou.aliyuncs.com/debuginfo-common/file/path'),
  debuginfoUrl?: string(name='debuginfoUrl', description='The download URL of the debuginfo file. This parameter is optional when the diagnostic type is vmcore.

For CentOS or Alinux kernels, the corresponding debuginfo file is automatically downloaded, and you do not need to specify this parameter. For other distribution kernels, manually provide the download URL of the debuginfo file that corresponds to the kernel version.', example='https://bucket-cn-hangzhou.oss-cn-hangzhou.aliyuncs.com/debuginfo/file/path'),
  dmesgUrl?: string(name='dmesgUrl', description='The download URL of the dmesg log file. This parameter is required when the diagnostic type is dmesg.', example='https://bucket-cn-hangzhou.oss-cn-hangzhou.aliyuncs.com/dmesg/file/path'),
  taskType?: string(name='taskType', description='The task type. Valid values:

- vmcore: vmcore file diagnostic task.
- dmesg: dmesg log diagnostic task.

This parameter is required.', example='vmcore'),
  vmcoreUrl?: string(name='vmcoreUrl', description='The download URL of the vmcore file. This parameter is required when the diagnostic type is vmcore.', example='https://bucket-cn-hangzhou.oss-cn-hangzhou.aliyuncs.com/vmcore/file/path'),
}

model CreateVmcoreDiagnosisTaskResponseBody = {
  code?: string(name='code', description='The status code.
- If code is Success, the authorization is successful.
- Other status codes indicate that the authorization has failed. Check the message field for the detailed fault message.', example='Success'),
  data?: {
    taskId?: string(name='taskId', description='The ID of the created diagnostic task.', example='bbe94a98-4192-4172-b856-95777e0a55d7'),
  }(name='data', description='The returned data.'),
  message?: string(name='message', description='The error message.
- If code is Success, this field is empty.
- Otherwise, this field contains the error message for the request.', example='""'),
  requestId?: string(name='requestId', description='The request ID.', example='2D693121-C925-5154-8DF6-C09A8B369822'),
}

model CreateVmcoreDiagnosisTaskResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateVmcoreDiagnosisTaskResponseBody(name='body'),
}

/**
 * @summary Creates an intelligent breakdown diagnostic node that diagnoses the specified vmcore or dmesg log file based on the input parameters.
 *
 * @param request CreateVmcoreDiagnosisTaskRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateVmcoreDiagnosisTaskResponse
 */
@context("createVmcoreDiagnosisTaskWithContext")
async function createVmcoreDiagnosisTaskWithOptions(request: CreateVmcoreDiagnosisTaskRequest, headers: map[string]string, runtime: $RuntimeOptions): CreateVmcoreDiagnosisTaskResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.debuginfoCommonUrl)) {
    body['debuginfoCommonUrl'] = request.debuginfoCommonUrl;
  }
  if (!$isNull(request.debuginfoUrl)) {
    body['debuginfoUrl'] = request.debuginfoUrl;
  }
  if (!$isNull(request.dmesgUrl)) {
    body['dmesgUrl'] = request.dmesgUrl;
  }
  if (!$isNull(request.taskType)) {
    body['taskType'] = request.taskType;
  }
  if (!$isNull(request.vmcoreUrl)) {
    body['vmcoreUrl'] = request.vmcoreUrl;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateVmcoreDiagnosisTask',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/crashAgent/diagnosis/createDiagnosisTask`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates an intelligent breakdown diagnostic node that diagnoses the specified vmcore or dmesg log file based on the input parameters.
 *
 * @param request CreateVmcoreDiagnosisTaskRequest
 * @return CreateVmcoreDiagnosisTaskResponse
 */
async function createVmcoreDiagnosisTask(request: CreateVmcoreDiagnosisTaskRequest): CreateVmcoreDiagnosisTaskResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return createVmcoreDiagnosisTaskWithOptions(request, headers, runtime);
}

model DeleteAlertDestinationRequest {
  id?: int32(name='id', description='The ID of the alert contact.', example='1'),
}

model DeleteAlertDestinationResponseBody = {
  code?: string(name='code', description='The status code.
- If `code == Success`, the authorization is successful.
- Other status codes indicate that the authorization failed. Check the `message` field for the detailed fault information.', example='Success'),
  data?: any(name='data', description='The returned data.', example='{}'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the error message of the request.', example='SysomOpenAPIException: SysomOpenAPI.InvalidParameter Invalid params, should be json string or dict'),
  requestId?: string(name='requestId', description='The request ID.', example='2D693121-C925-5154-8DF6-C09A8B369822'),
}

model DeleteAlertDestinationResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DeleteAlertDestinationResponseBody(name='body'),
}

/**
 * @summary Deletes an alert contact.
 *
 * @param request DeleteAlertDestinationRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteAlertDestinationResponse
 */
@context("deleteAlertDestinationWithContext")
async function deleteAlertDestinationWithOptions(request: DeleteAlertDestinationRequest, headers: map[string]string, runtime: $RuntimeOptions): DeleteAlertDestinationResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.id)) {
    query['id'] = request.id;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DeleteAlertDestination',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/alertPusher/alert/deleteDestination`,
    method = 'DELETE',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Deletes an alert contact.
 *
 * @param request DeleteAlertDestinationRequest
 * @return DeleteAlertDestinationResponse
 */
async function deleteAlertDestination(request: DeleteAlertDestinationRequest): DeleteAlertDestinationResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return deleteAlertDestinationWithOptions(request, headers, runtime);
}

model DeleteAlertStrategyRequest {
  id?: string(name='id', description='The ID of the alert policy.

This parameter is required.', example='1'),
}

model DeleteAlertStrategyResponseBody = {
  code?: string(name='code', description='The status code.
- If `code == Success`, the authorization is successful.
- Other status codes indicate authorization failed. Check the `message` field for the detailed error message.', example='Success'),
  data?: any(name='data', description='The response data.', example='{
    "uid": "1808078950770264",
    "name": "test",
    "strategy": {
      "items": [
        "节点CPU使用率检测"
      ],
      "clusters": [
        "1"
      ]
    },
    "enabled": false,
    "id": 1,
    "created_at": 1753170771,
    "updated_at": 1753170811
  }'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error information.', example='success'),
  requestId?: string(name='requestId', description='Id of the request', example='2D693121-C925-5154-8DF6-C09A8B369822'),
}

model DeleteAlertStrategyResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DeleteAlertStrategyResponseBody(name='body'),
}

/**
 * @summary Deletes an alert policy for push notifications.
 *
 * @param request DeleteAlertStrategyRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteAlertStrategyResponse
 */
@context("deleteAlertStrategyWithContext")
async function deleteAlertStrategyWithOptions(request: DeleteAlertStrategyRequest, headers: map[string]string, runtime: $RuntimeOptions): DeleteAlertStrategyResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.id)) {
    query['id'] = request.id;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DeleteAlertStrategy',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/alertPusher/alert/deleteStrategy`,
    method = 'DELETE',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Deletes an alert policy for push notifications.
 *
 * @param request DeleteAlertStrategyRequest
 * @return DeleteAlertStrategyResponse
 */
async function deleteAlertStrategy(request: DeleteAlertStrategyRequest): DeleteAlertStrategyResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return deleteAlertStrategyWithOptions(request, headers, runtime);
}

model DescribeMetricListRequest {
  endTime?: long(name='endTime', description='The end time as a UNIX timestamp in seconds.', example='1683618245000'),
  instance?: string(name='instance', description='The instance ID.', example='i-wz9d00ut2ska3mlyhn6j'),
  metricName?: string(name='metricName', description='The metric name.', example='sysom_cpu_graph'),
  startTime?: long(name='startTime', description='The start time as a UNIX timestamp in seconds.', example='1709740800000'),
}

model DescribeMetricListResponseBody = {
  code?: string(name='code', description='The status code.
- If `code == Success`, the authorization is successful.
- Other status codes indicate that the authorization failed. Check the `message` field for the detailed fault information.', example='Success'),
  data?: [ 
    {
      labels?: string(name='labels', description='The metric labels.', example='{\\\\"taskExecName\\\\": \\\\"build-and-deploy\\\\", \\\\"pipelineName\\\\": \\\\"pipeline-run-1722909642357\\\\"}'),
      metricName?: string(name='metricName', description='The metric name.', example='sysom_cpu_usage_idle'),
      values?: [[ string ]      ](name='values', description='The metric data.'),
    }
  ](name='data', description='The returned data.'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error message.', example='分析ID: 56dae746-ef55-4f77-8373-cb3594c41457
机器i-bp164ns76pzirbwv0snt分析失败, 失败原因: Not get GPU trace data for \\\\"56dae746-ef55-4f77-8373-cb3594c41457\\\\" \\\\"[\\\\"93811\\\\"]\\\\"!'),
  requestId?: string(name='requestId', description='Id of the request', example='2D693121-C925-5154-8DF6-C09A8B369822'),
}

model DescribeMetricListResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeMetricListResponseBody(name='body'),
}

/**
 * @summary Queries metric data.
 *
 * @description The instance list returned by this operation contains only instances that are managed by SysOM. If an ECS instance exists but is not managed by SysOM, it does not appear in the list.
 *
 * @param request DescribeMetricListRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeMetricListResponse
 */
@context("describeMetricListWithContext")
async function describeMetricListWithOptions(request: DescribeMetricListRequest, headers: map[string]string, runtime: $RuntimeOptions): DescribeMetricListResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.endTime)) {
    query['endTime'] = request.endTime;
  }
  if (!$isNull(request.instance)) {
    query['instance'] = request.instance;
  }
  if (!$isNull(request.metricName)) {
    query['metricName'] = request.metricName;
  }
  if (!$isNull(request.startTime)) {
    query['startTime'] = request.startTime;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeMetricList',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/openapi/proxy/get/describeMetricList`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries metric data.
 *
 * @description The instance list returned by this operation contains only instances that are managed by SysOM. If an ECS instance exists but is not managed by SysOM, it does not appear in the list.
 *
 * @param request DescribeMetricListRequest
 * @return DescribeMetricListResponse
 */
async function describeMetricList(request: DescribeMetricListRequest): DescribeMetricListResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return describeMetricListWithOptions(request, headers, runtime);
}

model GenerateCopilotResponseRequest {
  llmParamString?: string(name='llmParamString', description='Input parameters for the copilot LLM service. Refer to the standard LLM API input parameter dict, which needs to be converted to a string and passed into llmParamString', example='"{\\\\"model\\\\":\\\\"Qwen-7B-Chat\\\\",\\\\"temperature\\\\":0.9,\\\\"max_tokens\\\\":1000,\\\\"top_p\\\\":1,\\\\"frequency_penalty\\\\":0.0,\\\\"presence_penalty\\\\":0.6,\\\\"messages\\\\":[{\\\\"role\\\\":\\\\"user\\\\",\\\\"content\\\\":\\\\"我是os工程师\\\\"},{\\\\"role\\\\":\\\\"assistant\\\\",\\\\"content\\\\":\\\\"您好。我是AI语言模型，很高兴为您服 务。有什么我能帮助您的呢\\\\"},{\\\\"role\\\\":\\\\"user\\\\",\\\\"content\\\\":\\\\"你知道什么是alinux吗\\\\"}]}"'),
}

model GenerateCopilotResponseResponseBody = {
  code?: string(name='code', description='Error code', example='SysomOpenAPI.ServerError'),
  data?: string(name='data', description='Data returned by the LLM service, in string type. If it is a dict, you need to convert and parse it yourself', example='{\\\\"choices\\\\": [{\\\\"finish_reason\\\\": \\\\"stop\\\\", \\\\"index\\\\": 0, \\\\"message\\\\": {\\\\"content\\\\": \\\\"Alinux是一个基于Linux的开源操作系统，它主要由阿里巴巴集团开发和维护，专为大规模云计算和大数据处理环境设计。这个系统针对高并发、低延迟以及资源利用率优化进行了深度定制和改良，以适应阿里内部复杂且庞大的业务需求。由于其在性能和稳定性方面的优秀表现，Alinux也在逐渐被外部的一些企业和研究机构采用，特别是在需要处理高负载场景的领域。与标准Linux发行版相比，Alinux可能包含特定的内核补丁、优化工具集以及其他阿里巴巴内部研发的特性。然而，值得注意的是，尽管名称中的“Ali”可能让人联想到与阿里巴巴的紧密关系，但Alinux作为一个项目，已经于2021年成为开放原子开源基金会（OpenAtom Foundation）下的一个子项目，即 OpenAnolis，这标志着它的社区化和广泛化的进程。\\\\", \\\\"role\\\\": \\\\"assistant\\\\"}}], \\\\"model\\\\": \\\\"Qwen1.5\\\\", \\\\"sentiment\\\\": [], \\\\"status_code\\\\": 200, \\\\"time\\\\": 6.836989402770996, \\\\"usage\\\\": {\\\\"completion_tokens\\\\": 180, \\\\"prompt_tokens\\\\": 176, \\\\"time_dict\\\\": {\\\\"auto_regression_time\\\\": 0.03798324399524265, \\\\"speed\\\\": 26.327398474054736, \\\\"total_generate_time\\\\": 6.836983919143677}, \\\\"total_tokens\\\\": 356}}'),
  massage?: string(name='massage', description='Error code description, empty if no error occurs', example='Requests for llm service failed'),
  requestId?: string(name='requestId', description='Id of the request', example='2D693121-C925-5154-8DF6-C09A8B369822'),
}

model GenerateCopilotResponseResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GenerateCopilotResponseResponseBody(name='body'),
}

/**
 * @summary Get the response result of the copilot service
 *
 * @description - Parameters need to be filled in according to the standard LLM service input parameters, converted to a string, and assigned to llmParamString
 * - The returned data needs to be converted from string to dict before use. Refer to the standard LLM service response format
 *
 * @param request GenerateCopilotResponseRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GenerateCopilotResponseResponse
 */
@context("generateCopilotResponseWithContext")
async function generateCopilotResponseWithOptions(request: GenerateCopilotResponseRequest, headers: map[string]string, runtime: $RuntimeOptions): GenerateCopilotResponseResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.llmParamString)) {
    body['llmParamString'] = request.llmParamString;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'GenerateCopilotResponse',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/copilot/generate_copilot_response`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Get the response result of the copilot service
 *
 * @description - Parameters need to be filled in according to the standard LLM service input parameters, converted to a string, and assigned to llmParamString
 * - The returned data needs to be converted from string to dict before use. Refer to the standard LLM service response format
 *
 * @param request GenerateCopilotResponseRequest
 * @return GenerateCopilotResponseResponse
 */
async function generateCopilotResponse(request: GenerateCopilotResponseRequest): GenerateCopilotResponseResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return generateCopilotResponseWithOptions(request, headers, runtime);
}

model GenerateCopilotStreamResponseRequest {
  llmParamString?: string(name='llmParamString', description='The input parameter for the Copilot LLM service. Refer to the standard LLM operation input parameter dictionary. Convert the dictionary to a string and pass it in llmParamString.', example='"{\\\\"model\\\\":\\\\"Qwen-7B-Chat\\\\",\\\\"temperature\\\\":0.9,\\\\"max_tokens\\\\":1000,\\\\"top_p\\\\":1,\\\\"frequency_penalty\\\\":0.0,\\\\"presence_penalty\\\\":0.6,\\\\"messages\\\\":[{\\\\"role\\\\":\\\\"user\\\\",\\\\"content\\\\":\\\\"我是os工程师\\\\"},{\\\\"role\\\\":\\\\"assistant\\\\",\\\\"content\\\\":\\\\"您好。我是AI语言模型，很高兴为您服 务。有什么我能帮助您的呢\\\\"},{\\\\"role\\\\":\\\\"user\\\\",\\\\"content\\\\":\\\\"你知道什么是alinux吗\\\\"}]}"'),
}

model GenerateCopilotStreamResponseResponseBody = {
  code?: string(name='code', description='The error code.', example='SysomOpenAPI.ServerError'),
  data?: string(name='data', description='The data returned by the LLM service. The value is of the string type. Convert and parse it yourself if the value is a dictionary.', example='{\\\\"choices\\\\": [{\\\\"finish_reason\\\\": \\\\"stop\\\\", \\\\"index\\\\": 0, \\\\"message\\\\": {\\\\"content\\\\": \\\\"Alinux是一个基于Linux的开源操作系统，它主要由阿里巴巴集团开发和维护，专为大规模云计算和大数据处理环境设计。这个系统针对高并发、低延迟以及资源利用率优化进行了深度定制和改良，以适应阿里内部复杂且庞大的业务需求。由于其在性能和稳定性方面的优秀表现，Alinux也在逐渐被外部的一些企业和研究机构采用，特别是在需要处理高负载场景的领域。与标准Linux发行版相比，Alinux可能包含特定的内核补丁、优化工具集以及其他阿里巴巴内部研发的特性。然而，值得注意的是，尽管名称中的“Ali”可能让人联想到与阿里巴巴的紧密关系，但Alinux作为一个项目，已经于2021年成为开放原子开源基金会（OpenAtom Foundation）下的一个子项目，即 OpenAnolis，这标志着它的社区化和广泛化的进程。\\\\", \\\\"role\\\\": \\\\"assistant\\\\"}}], \\\\"model\\\\": \\\\"Qwen1.5\\\\", \\\\"sentiment\\\\": [], \\\\"status_code\\\\": 200, \\\\"time\\\\": 6.836989402770996, \\\\"usage\\\\": {\\\\"completion_tokens\\\\": 180, \\\\"prompt_tokens\\\\": 176, \\\\"time_dict\\\\": {\\\\"auto_regression_time\\\\": 0.03798324399524265, \\\\"speed\\\\": 26.327398474054736, \\\\"total_generate_time\\\\": 6.836983919143677}, \\\\"total_tokens\\\\": 356}}'),
  message?: string(name='message', description='The description of the error code. This value is empty if no error occurs.', example='Requests for llm service failed'),
  requestId?: string(name='requestId', description='Id of the request', example='2D693121-C925-5154-8DF6-C09A8B369822'),
}

model GenerateCopilotStreamResponseResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  id?: string(name='id'),
  event?: string(name='event'),
  body?: GenerateCopilotStreamResponseResponseBody(name='body'),
}

/**
 * @summary Calls the streaming SSE endpoint of the OS Copilot service.
 *
 * @description - Use this operation together with the call_sseapi operation in the aliyun-tea-openapi-inner package.
 * - Populate the parameters based on the standard LLM service input parameters, convert them to a string, and assign the string to llmParamString.
 * - Convert the returned string to a dictionary before use. Refer to the standard LLM service response format.
 *
 * @param request GenerateCopilotStreamResponseRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GenerateCopilotStreamResponseResponse
 */
@sse(true)
@context("generateCopilotStreamResponseWithSSECtx")
async function generateCopilotStreamResponseWithSSE(request: GenerateCopilotStreamResponseRequest, headers: map[string]string, runtime: $RuntimeOptions): asyncIterator[GenerateCopilotStreamResponseResponse] {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.llmParamString)) {
    body['llmParamString'] = request.llmParamString;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'GenerateCopilotStreamResponse',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/copilot/generate_copilot_stream_response`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  var sseResp = callSSEApi(params, req, runtime);
  for (var resp : sseResp) {
    if (!$isNull(resp.event) && !$isNull(resp.event.data)) {
      var data = $object($JSON.parseJSON(resp.event.data));
      yield {
        statusCode = resp.statusCode,
        headers = resp.headers,
        id = resp.event.id,
        event = resp.event.event,
        body = data
      };
    }
  }
}
/**
 * @summary Calls the streaming SSE endpoint of the OS Copilot service.
 *
 * @description - Use this operation together with the call_sseapi operation in the aliyun-tea-openapi-inner package.
 * - Populate the parameters based on the standard LLM service input parameters, convert them to a string, and assign the string to llmParamString.
 * - Convert the returned string to a dictionary before use. Refer to the standard LLM service response format.
 *
 * @param request GenerateCopilotStreamResponseRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GenerateCopilotStreamResponseResponse
 */
@context("generateCopilotStreamResponseWithContext")
async function generateCopilotStreamResponseWithOptions(request: GenerateCopilotStreamResponseRequest, headers: map[string]string, runtime: $RuntimeOptions): GenerateCopilotStreamResponseResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.llmParamString)) {
    body['llmParamString'] = request.llmParamString;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'GenerateCopilotStreamResponse',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/copilot/generate_copilot_stream_response`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Calls the streaming SSE endpoint of the OS Copilot service.
 *
 * @description - Use this operation together with the call_sseapi operation in the aliyun-tea-openapi-inner package.
 * - Populate the parameters based on the standard LLM service input parameters, convert them to a string, and assign the string to llmParamString.
 * - Convert the returned string to a dictionary before use. Refer to the standard LLM service response format.
 *
 * @param request GenerateCopilotStreamResponseRequest
 * @return GenerateCopilotStreamResponseResponse
 */
async function generateCopilotStreamResponse(request: GenerateCopilotStreamResponseRequest): GenerateCopilotStreamResponseResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return generateCopilotStreamResponseWithOptions(request, headers, runtime);
}

model GetAIQueryResultRequest {
  analysisId?: string(name='analysisId', description='The AI analysis ID.', example='16896fa8-37f6-4c70-bb32-67fa9817d426'),
}

model GetAIQueryResultResponseBody = {
  code?: string(name='code', description='The error code.', example='Success'),
  data?: string(name='data', description='The returned results. The data volume can be large.', example='{\\\\"task_id\\\\": \\\\"y4ba8uRV\\\\"}'),
  message?: string(name='message', description='The error message.', example='分析ID: 56dae746-ef55-4f77-8373-cb3594c41457
机器i-bp164ns76pzirbwv0snt分析失败, 失败原因: Not get GPU trace data for \\\\"56dae746-ef55-4f77-8373-cb3594c41457\\\\" \\\\"[\\\\"93811\\\\"]\\\\"!'),
  requestId?: string(name='requestId', description='Id of the request', example='2D693121-C925-5154-8DF6-C09A8B369822'),
}

model GetAIQueryResultResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetAIQueryResultResponseBody(name='body'),
}

/**
 * @summary Queries the AI Infra analysis results.
 *
 * @param request GetAIQueryResultRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetAIQueryResultResponse
 */
@context("getAIQueryResultWithContext")
async function getAIQueryResultWithOptions(request: GetAIQueryResultRequest, headers: map[string]string, runtime: $RuntimeOptions): GetAIQueryResultResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.analysisId)) {
    body['analysisId'] = request.analysisId;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetAIQueryResult',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/app_observ/aiAnalysis/query_result`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the AI Infra analysis results.
 *
 * @param request GetAIQueryResultRequest
 * @return GetAIQueryResultResponse
 */
async function getAIQueryResult(request: GetAIQueryResultRequest): GetAIQueryResultResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getAIQueryResultWithOptions(request, headers, runtime);
}

model GetAbnormalEventsCountRequest {
  cluster?: string(name='cluster', description='Cluster ID.', example='2ijff4be-bf24-4070-89ca-c47c879b0g32'),
  end?: float(name='end', description='End time.', example='1725801327754'),
  instance?: string(name='instance', description='Instance ID.', example='i-wz9d00ut2ska3mlyhn6j'),
  level?: string(name='level', description='Level of abnormal events.', example='potential'),
  namespace?: string(name='namespace', description='The namespace where the Pod resides.', example='default'),
  pod?: string(name='pod', description='Pod name.', example='test-pod'),
  showPod?: int32(name='showPod', description='Whether to display Pod abnormal events.', example='1'),
  start?: float(name='start', description='Start time.', example='1725797727754'),
}

model GetAbnormalEventsCountResponseBody = {
  code?: string(name='code', description='Response code.', example='Success'),
  data?: [ 
    {
      eventList?: [ string ](name='eventList', description='List of abnormal events.'),
      type?: string(name='type', description='Abnormal event type.', example='health'),
      value?: long(name='value', description='Count of abnormal event type.', example='1'),
    }
  ](name='data', description='Response data.'),
  message?: string(name='message', description='When success is false, this value is not empty, indicating a business error message.', example='result: code=1 msg=(Request failed, status_code != 200)'),
}

model GetAbnormalEventsCountResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetAbnormalEventsCountResponseBody(name='body'),
}

/**
 * @summary Get the count of unhandled (undiagnosed) abnormal events of different levels for nodes/Pods
 *
 * @param request GetAbnormalEventsCountRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetAbnormalEventsCountResponse
 */
@context("getAbnormalEventsCountWithContext")
async function getAbnormalEventsCountWithOptions(request: GetAbnormalEventsCountRequest, headers: map[string]string, runtime: $RuntimeOptions): GetAbnormalEventsCountResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.cluster)) {
    query['cluster'] = request.cluster;
  }
  if (!$isNull(request.end)) {
    query['end'] = request.end;
  }
  if (!$isNull(request.instance)) {
    query['instance'] = request.instance;
  }
  if (!$isNull(request.level)) {
    query['level'] = request.level;
  }
  if (!$isNull(request.namespace)) {
    query['namespace'] = request.namespace;
  }
  if (!$isNull(request.pod)) {
    query['pod'] = request.pod;
  }
  if (!$isNull(request.showPod)) {
    query['showPod'] = request.showPod;
  }
  if (!$isNull(request.start)) {
    query['start'] = request.start;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetAbnormalEventsCount',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/openapi/cluster_health/range/abnormaly_events_count`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Get the count of unhandled (undiagnosed) abnormal events of different levels for nodes/Pods
 *
 * @param request GetAbnormalEventsCountRequest
 * @return GetAbnormalEventsCountResponse
 */
async function getAbnormalEventsCount(request: GetAbnormalEventsCountRequest): GetAbnormalEventsCountResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getAbnormalEventsCountWithOptions(request, headers, runtime);
}

model GetAgentRequest {
  agentId?: string(name='agent_id', description='Agent ID', example='74a86327-3170-412c-8e67-da3389ec56a9'),
}

model GetAgentResponseBody = {
  requestId?: string(name='RequestId', description='Request ID, which can be used for end-to-end diagnostics', example='2024-09-14T20:46:08'),
  code?: string(name='code', description='Status code
- `code == Success` indicates the authorization is successful;
- Other status codes indicate authorization failure. Check the `message` field for detailed error information when authorization fails;', example='Success'),
  data?: {
    createdAt?: string(name='created_at', description='Agent creation time', example='2024-09-14T20:46:08'),
    description?: string(name='description', description='Agent description', example='SysOM Agent'),
    id?: string(name='id', description='Agent ID', example='74a86327-3170-412c-8e67-da3389ec56a9'),
    name?: string(name='name', description='Agent name', example='SysOM'),
    supportArch?: string(name='support_arch', description='Supported architectures', example='x86'),
    type?: string(name='type', description='Agent type', example='control'),
    updatedAt?: string(name='updated_at', description='Update time', example='2024-09-14T20:46:08'),
    versions?: [ 
      {
        createdAt?: string(name='created_at', description='Agent version creation time', example='2024-09-14T20:46:08'),
        installScript?: string(name='install_script', description='Installation script for this agent version', example='sysom.sh install'),
        uninstallScript?: string(name='uninstall_script', description='Uninstallation script for this agent version', example='sysom.sh uninstall'),
        updatedAt?: string(name='updated_at', description='Agent version update time', example='2024-09-14T20:46:08'),
        upgradeScript?: string(name='upgrade_script', description='Upgrade script for this agent version', example='sysom.sh upgrade'),
        version?: string(name='version', description='Agent version number', example='3.4.0-1'),
      }
    ](name='versions', description='Agent version information'),
  }(name='data', description='Response data.'),
  message?: string(name='message', description='Error message
- If `code == Success`, this field is empty;
- Otherwise, this field contains the request error message.', example='SysomOpenAPIException: SysomOpenAPI.InvalidParameter Invalid params, should be json string or dict'),
}

model GetAgentResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetAgentResponseBody(name='body'),
}

/**
 * @summary Get details of a specific agent
 *
 * @param request GetAgentRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetAgentResponse
 */
@context("getAgentWithContext")
async function getAgentWithOptions(request: GetAgentRequest, headers: map[string]string, runtime: $RuntimeOptions): GetAgentResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.agentId)) {
    query['agent_id'] = request.agentId;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetAgent',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/am/agent/get_agent`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Get details of a specific agent
 *
 * @param request GetAgentRequest
 * @return GetAgentResponse
 */
async function getAgent(request: GetAgentRequest): GetAgentResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getAgentWithOptions(request, headers, runtime);
}

model GetAgentTaskRequest {
  taskId?: string(name='task_id', description='The task ID.', example='42172120177e4b3abd6fabb3a6b5e2dd'),
}

model GetAgentTaskResponseBody = {
  requestId?: string(name='RequestId', description='The request ID, which can be used for end-to-end diagnostics.', example='2E75336A-0DB2-5263-B201-A6488EC97B50'),
  code?: string(name='code', description='The status code.
- `code == Success` indicates that the authorization is successful.
- Other status codes indicate that the authorization failed. Check the `message` field for the detailed fault message.', example='Success'),
  data?: {
    jobs?: [ 
      {
        error?: string(name='error', description='The cause of the task failure. This field is returned only when the task fails.', example='已废弃（误用）'),
        errorCode?: string(name='errorCode', description='The error code of the subtask failure. Valid values:
* Empty: The task is executed normally.
* INSTANCE_NOT_SUPPORTED: The instance type is not supported.
* INSTANCE_NOT_EXISTS: The instance does not exist.
* INSTANCE_RELEASED: The instance has been released.
* INSTANCE_NOT_RUNNING: The instance is not running.
* INSTANCE_NOT_OWNED: The instance does not belong to the current account.
* AGENT_ALREADY_INSTALLED: The Agent is already installed.
* AGENT_NOT_INSTALLED: The Agent is not installed.
* AGENT_SAME_VERSION: The version is the same.
* HAS_RUNNING_JOB: A running task exists.
* RPM_LOCK_HELD: The RPM lock is held.
* DISK_SPACE_INSUFFICIENT: Insufficient disk space.
* NODE_LOAD_HIGH: The node load is high.
* COMMAND_FAILED: Command execution failed.
* CLIENT_NOT_RUNNING: The Cloud Assistant Agent is not running.
* CLIENT_NOT_RESPONSE: The Cloud Assistant Agent is not responding.
* DELIVERY_TIMEOUT: Command delivery timed out.
* EXECUTION_TIMEOUT: Command execution timed out.
* TASK_CONCURRENCY_LIMIT: The task concurrency limit is reached.', example='DISK_SPACE_INSUFFICIENT'),
        errorMessage?: string(name='errorMessage', description='The detailed description of the subtask failure. Valid values:
* The instance type is not supported.
* The instance does not exist.
* The instance has been released.
* The instance is not running.
* The instance does not belong to the current account.
* The Agent is already installed.
* The Agent is not installed.
* The Agent version is the same. No upgrade is required.
* A running task exists. Try again later.
* The RPM lock is held. Try again later.
* Insufficient disk space.
* The node load is too high. Try again later.
* Command execution failed. Try again later.
* The Cloud Assistant Agent is not running.
* The Cloud Assistant Agent is not responding.
* Command delivery timed out.
* Command execution timed out.
* The task concurrency limit is reached.', example='磁盘空间不足'),
        instance?: string(name='instance', description='The instance ID.', example='i-2zehme0rs1tc090fdl3n'),
        params?: any(name='params', description='The subtask parameters.', example='{
    "agent_version": "3.5.0-beta",
    "opt": "install",
    "agent_id": "74a86327-3170-412c-8fd67-da3389ec56a9",
    "install_type": "InstallAndUpgrade"
}'),
        region?: string(name='region', description='The region ID.', example='cn-hangzhou'),
        result?: string(name='result', description='The subtask execution result.', example='已废弃（误用）'),
        status?: string(name='status', description='The subtask status. Valid values:
- Created: Created.
- Running: Running.
- Success: The task succeeded.
- Fail: The task failed.', example='Running'),
      }
    ](name='jobs', description='The list of subtasks.'),
    status?: string(name='status'),
    taskId?: string(name='task_id', description='The task ID.', example='c41d8e3506224184a714682fea86d22d'),
  }(name='data', description='The returned data.'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error information.', example='SysomOpenAPIException: SysomOpenAPI.InvalidParameter Invalid params, should be json string or dict'),
}

model GetAgentTaskResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetAgentTaskResponseBody(name='body'),
}

/**
 * @summary Retrieves the execution status of an Agent installation task.
 *
 * @param request GetAgentTaskRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetAgentTaskResponse
 */
@context("getAgentTaskWithContext")
async function getAgentTaskWithOptions(request: GetAgentTaskRequest, headers: map[string]string, runtime: $RuntimeOptions): GetAgentTaskResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.taskId)) {
    query['task_id'] = request.taskId;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetAgentTask',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/am/agent/get_agent_task`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves the execution status of an Agent installation task.
 *
 * @param request GetAgentTaskRequest
 * @return GetAgentTaskResponse
 */
async function getAgentTask(request: GetAgentTaskRequest): GetAgentTaskResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getAgentTaskWithOptions(request, headers, runtime);
}

model GetAlertDestinationRequest {
  id?: int32(name='id', description='The alert contact ID.', example='1'),
}

model GetAlertDestinationResponseBody = {
  code?: string(name='code', description='The status code.
- If `code == Success`, the authorization is successful.
- Other status codes indicate authorization failed. Check the `message` field for the detailed fault information.', example='Success or Sysom.ServerError'),
  data?: {
    createdAt?: string(name='createdAt', description='The creation time.', example='1751520976660'),
    id?: int32(name='id', description='The alert contact ID.', example='1'),
    name?: string(name='name', description='The alert contact name.', example='name1'),
    params?: any(name='params', description='The configuration parameters.', example='{
    "webhook":"",
    "sec":"",
}'),
    source?: string(name='source', description='The configuration source.', example='console'),
    target?: string(name='target', description='The alert notification target. Currently, only DingTalk chatbots are supported.', example='dingtalk'),
    uid?: string(name='uid', description='The user ID.', example='1234123412352311'),
    updatedAt?: string(name='updatedAt', description='The update time.', example='1751254826285'),
  }(name='data', description='The returned data.'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error information.', example='SysomOpenAPIAssumeRoleException: EntityNotExist.Role The role not exists: acs:ram::xxxxx:role/aliyunserviceroleforsysom'),
  requestId?: string(name='requestId', description='The request ID.', example='2D693121-C925-5154-8DF6-C09A8B369822'),
}

model GetAlertDestinationResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetAlertDestinationResponseBody(name='body'),
}

/**
 * @summary Retrieves the information of a specified alert contact.
 *
 * @param request GetAlertDestinationRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetAlertDestinationResponse
 */
@context("getAlertDestinationWithContext")
async function getAlertDestinationWithOptions(request: GetAlertDestinationRequest, headers: map[string]string, runtime: $RuntimeOptions): GetAlertDestinationResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.id)) {
    query['id'] = request.id;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetAlertDestination',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/alertPusher/alert/getDestination`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves the information of a specified alert contact.
 *
 * @param request GetAlertDestinationRequest
 * @return GetAlertDestinationResponse
 */
async function getAlertDestination(request: GetAlertDestinationRequest): GetAlertDestinationResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getAlertDestinationWithOptions(request, headers, runtime);
}

model GetAlertStrategyRequest {
  id?: long(name='id', description='The policy ID.', example='1'),
}

model GetAlertStrategyResponseBody = {
  code?: string(name='code', description='The status code.
- If `code == Success`, the authorization is successful.
- Other status codes indicate authorization failed. Check the `message` field for the detailed fault message.', example='Success'),
  data?: {
    createdAt?: long(name='createdAt', description='The creation time.', example='1751520976660'),
    enabled?: boolean(name='enabled', description='Indicates whether the alert policy is enabled.', example='false'),
    id?: long(name='id', description='The alert policy ID.', example='1'),
    k8sLabel?: boolean(name='k8sLabel', description='The Kubernetes label.'),
    name?: string(name='name', description='The policy name.', example='strategy1'),
    strategy?: {
      clusters?: [ string ](name='clusters', description='The collection of clusters for which alerts are received.'),
      destinations?: any(name='destinations'),
      items?: any(name='items', description='接收告警的异常项列表', example='节点CPU使用率检测'),
    }(name='strategy', description='The alert policy details.'),
    uid?: string(name='uid', description='The user ID.', example='1222933234714935'),
    updatedAt?: long(name='updatedAt', description='The update time.', example='1751254826285'),
  }(name='data', description='The response data.'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error message.', example='success'),
  requestId?: string(name='requestId', description='Id of the request', example='2D693121-C925-5154-8DF6-C09A8B369822'),
}

model GetAlertStrategyResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetAlertStrategyResponseBody(name='body'),
}

/**
 * @summary Retrieves an alert for a user based on the policy ID.
 *
 * @param request GetAlertStrategyRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetAlertStrategyResponse
 */
@context("getAlertStrategyWithContext")
async function getAlertStrategyWithOptions(request: GetAlertStrategyRequest, headers: map[string]string, runtime: $RuntimeOptions): GetAlertStrategyResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.id)) {
    query['id'] = request.id;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetAlertStrategy',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/alertPusher/alert/getStrategy`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves an alert for a user based on the policy ID.
 *
 * @param request GetAlertStrategyRequest
 * @return GetAlertStrategyResponse
 */
async function getAlertStrategy(request: GetAlertStrategyRequest): GetAlertStrategyResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getAlertStrategyWithOptions(request, headers, runtime);
}

model GetCopilotHistoryRequest {
  count?: long(name='count', description='The number of historical chat records to retrieve. The value is generally less than 100.

This parameter is required.', example='100'),
}

model GetCopilotHistoryResponseBody = {
  code?: string(name='code', description='The error code.', example='SysomOpenAPI.InvalidParameter'),
  data?: [ 
    {
      content?: string(name='content', description='The text content of the chat message.', example='copilot回复的具体内容'),
      time?: string(name='time', description='The time of the chat record. The value is of the STRING type.', example='2024-09-02 10:02:39'),
      user?: string(name='user', description='The role of the message sender. Valid values:
- user: the user.
- copilot: the bot.', example='user
copilot'),
    }
  ](name='data', description='The list of chat records. The length equals the requested count. If the actual number of chat records is less than the requested count, the actual number is returned. Format: [{"user":"","content":"".question,"time":""},{...}].'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the error message of the request.', example='SysomOpenAPIAssumeRoleException: EntityNotExist.Role The role not exists: acs:ram::xxxxx:role/aliyunserviceroleforsysom'),
  requestId?: string(name='requestId', description='Id of the request', example='2D693121-C925-5154-8DF6-C09A8B369822'),
}

model GetCopilotHistoryResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetCopilotHistoryResponseBody(name='body'),
}

/**
 * @summary Retrieves the chat history of Copilot.
 *
 * @param request GetCopilotHistoryRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetCopilotHistoryResponse
 */
@context("getCopilotHistoryWithContext")
async function getCopilotHistoryWithOptions(request: GetCopilotHistoryRequest, headers: map[string]string, runtime: $RuntimeOptions): GetCopilotHistoryResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.count)) {
    body['count'] = request.count;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetCopilotHistory',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/copilot/get_copilot_history`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves the chat history of Copilot.
 *
 * @param request GetCopilotHistoryRequest
 * @return GetCopilotHistoryResponse
 */
async function getCopilotHistory(request: GetCopilotHistoryRequest): GetCopilotHistoryResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getCopilotHistoryWithOptions(request, headers, runtime);
}

model GetDiagnosisResultRequest {
  taskId?: string(name='task_id', description='The diagnostic task ID.

This parameter is required.', example='quzuYl23'),
}

model GetDiagnosisResultResponseBody = {
  code?: string(name='code', description='The status code.
- `code == Success` indicates that the authorization was successful.
- Other status codes indicate that the authorization failed. Check the `message` field for the detailed fault information.', example='Success'),
  data?: {
    code?: int32(name='code', description='The error code. A value of 0 indicates no error.', example='0'),
    command?: any(name='command', description='The diagnostic command.', example='{
    "jobs":[
        {
            "cmd":"mkdir -p /var/log/sysak && sysak podmem -r 100  -a -j /var/log/sysak/podmem.json > /dev/null 2>&1 && cat /var/log/sysak/podmem.json",
            "instance":"172.20.12.174",
            "fetch_file_list":[

            ]
        }
    ],
    "in_order":true,
    "offline_mode":false,
    "offline_results":[

    ]
}'),
    createdAt?: string(name='created_at', description='The creation time.', example='2025-12-03 16:30:28'),
    errMsg?: string(name='err_msg', description='The error message.', example='Diagnosis failed'),
    params?: any(name='params', description='The diagnostic parameters.', example='{
    "type":"all",
    "value":"",
    "channel":"ssh",
    "instance":"172.1.2.174",
    "service_name":"filecache"
}'),
    result?: any(name='result', description='The diagnostic result.', example='{
    "summary":"  memory cgroup leak",
    "dataMemEvent":{
        "data":[
            {
                "key":"Util",
                "value":20
            },
            {
                "key":"MemLeak",
                "value":"OK"
            },
            {
                "key":"MemcgLeak",
                "value":"NG"
            },
            {
                "key":"MemFrag",
                "value":"OK"
            }
        ]
    },
    "dataMemOverView":{
        "data":[
            {
                "key":"app",
                "value":10937332
            },
            {
                "key":"free",
                "value":806800
            },
            {
                "key":"kernel",
                "value":4527660
            }
        ]
    },
    "dataKerMem":{
        "data":[
            {
                "key":"SReclaimable",
                "value":3411292
            },
            {
                "key":"VmallocUsed",
                "value":30980
            },
            {
                "key":"allocPage",
                "value":177732
            },
            {
                "key":"KernelStack",
                "value":9280
            },
            {
                "key":"PageTables",
                "value":38056
            },
            {
                "key":"SUnreclaim",
                "value":170248
            },
            {
                "key":"reserved",
                "value":690072
            }
        ]
    },
    "dataUserMem":{
        "data":[
            {
                "key":"filecache",
                "value":8010008
            },
            {
                "key":"anon",
                "value":2468608
            },
            {
                "key":"mlock",
                "value":0
            },
            {
                "key":"huge1G",
                "value":0
            },
            {
                "key":"huge2M",
                "value":0
            },
            {
                "key":"buffers",
                "value":458608
            },
            {
                "key":"shmem",
                "value":2284
            }
        ]
    },
    "dataCacheList":{
        "data":[
            {
                "key":0,
                "Name":"/var/lib/mysql/sysom/sys_handler_log.ibd",
                "cached":576764,
                "Task":"mysqld_78575 "
            },
            {
                "key":1,
                "Name":"/var/log/sysom/sysom-migration-access.log",
                "cached":276688,
                "Task":"gunicorn_33647 ,gunicorn_460836 ,gunicorn_559934 ,gunicorn_731758 ,gunicorn_2362682 "
            },
            {
                "key":2,
                "Name":"/var/log/sysom/sysom-rtdemo-access.log",
                "cached":229404,
                "Task":"gunicorn_60718 ,gunicorn_720734 ,gunicorn_722168 "
            },
            {
                "key":3,
                "Name":"/var/log/sysom/sysom-monitor-server-access.log",
                "cached":197368,
                "Task":"gunicorn_33682 ,gunicorn_671155 ,gunicorn_714998 "
            },
            {
                "key":4,
                "Name":"/var/log/sysom/sysom-channel-access.log",
                "cached":180276,
                "Task":"gunicorn_33233 ,gunicorn_499735 ,gunicorn_725497 "
            },
            {
                "key":5,
                "Name":"total cached of close file",
                "cached":3729668,
                "Task":""
            }
        ]
    },
    "dataProcMemList":{
        "data":[
            {
                "key":0,
                "task":"mysqld",
                "MemTotal":240856,
                "RssAnon":218248,
                "RssFile":22608
            },
            {
                "key":1,
                "task":"systemd-journal",
                "MemTotal":150248,
                "RssAnon":74300,
                "RssFile":75944
            },
            {
                "key":2,
                "task":"gunicorn",
                "MemTotal":144640,
                "RssAnon":114200,
                "RssFile":30440
            },
            {
                "key":3,
                "task":"gunicorn",
                "MemTotal":141480,
                "RssAnon":111040,
                "RssFile":30440
            },
            {
                "key":4,
                "task":"grafana-server",
                "MemTotal":103660,
                "RssAnon":42732,
                "RssFile":60928
            },
            {
                "key":5,
                "task":"gunicorn",
                "MemTotal":97444,
                "RssAnon":76256,
                "RssFile":21188
            },
            {
                "key":6,
                "task":"gunicorn",
                "MemTotal":97260,
                "RssAnon":76072,
                "RssFile":21188
            },
            {
                "key":7,
                "task":"prometheus",
                "MemTotal":95356,
                "RssAnon":45376,
                "RssFile":49980
            },
            {
                "key":8,
                "task":"gunicorn",
                "MemTotal":90144,
                "RssAnon":76456,
                "RssFile":13688
            },
            {
                "key":9,
                "task":"gunicorn",
                "MemTotal":89796,
                "RssAnon":76108,
                "RssFile":13688
            }
        ]
    }
}'),
    serviceName?: string(name='service_name', description='The diagnostic type.', example='memgraph'),
    status?: string(name='status', description='The execution status of the diagnostic task.
Valid values:
- **Ready**: ready
- **Running**: running
- **Success**: succeeded
- **Fail**: failed.', example='Running'),
    taskId?: string(name='task_id', description='The task ID.', example='grcuU21a'),
    updatedAt?: string(name='updated_at', description='The update time.', example='2025-12-03 16:30:28'),
    url?: string(name='url', description='The URL of the diagnostic details.', example='/diagnose/detail/qe3Z34sa'),
  }(name='data', description='The response data.'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error information.', example='""'),
  requestId?: string(name='request_id', description='The request ID.', example='9515E5A0-8905-59B0-9BBF-5F0BE568C3A0'),
}

model GetDiagnosisResultResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetDiagnosisResultResponseBody(name='body'),
}

/**
 * @summary Retrieves the diagnostic result.
 *
 * @description The diagnostic process is asynchronous. When you call this operation, the diagnosis may still be in progress. You can check the `data.status` field in the response to determine the status. When `data.status == Success`, the diagnosis is complete and you can read the diagnostic result from `data.result`.
 *
 * @param request GetDiagnosisResultRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetDiagnosisResultResponse
 */
@context("getDiagnosisResultWithContext")
async function getDiagnosisResultWithOptions(request: GetDiagnosisResultRequest, headers: map[string]string, runtime: $RuntimeOptions): GetDiagnosisResultResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.taskId)) {
    query['task_id'] = request.taskId;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetDiagnosisResult',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/openapi/diagnosis/get_diagnosis_results`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves the diagnostic result.
 *
 * @description The diagnostic process is asynchronous. When you call this operation, the diagnosis may still be in progress. You can check the `data.status` field in the response to determine the status. When `data.status == Success`, the diagnosis is complete and you can read the diagnostic result from `data.result`.
 *
 * @param request GetDiagnosisResultRequest
 * @return GetDiagnosisResultResponse
 */
async function getDiagnosisResult(request: GetDiagnosisResultRequest): GetDiagnosisResultResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getDiagnosisResultWithOptions(request, headers, runtime);
}

model GetHealthPercentageRequest {
  cluster?: string(name='cluster', description='The cluster ID.', example='2ijff4be-bf24-4070-89ca-c47c879b0g32'),
  end?: float(name='end', description='The end time.

This parameter is required.', example='1725801327754'),
  instance?: string(name='instance', description='The instance ID.', example='i-wz9d00ut2ska3mlyhn6j'),
  start?: float(name='start', description='The start time.

This parameter is required.', example='1725797727754'),
}

model GetHealthPercentageResponseBody = {
  code?: string(name='code', description='The status code.
- `code == Success` indicates that the authorization was successful.
- Other status codes indicate that the authorization failed. Check the `message` field for the detailed fault message.', example='SysomOpenAPI.ServerError'),
  data?: [ 
    {
      type?: string(name='type', description='The health level of the node or pod.', example='health'),
      value?: long(name='value', description='The quantity.', example='1'),
    }
  ](name='data', description='The returned data.'),
  message?: string(name='message', description='The error details.', example='""'),
}

model GetHealthPercentageResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetHealthPercentageResponseBody(name='body'),
}

/**
 * @summary Retrieves the health status distribution of nodes or pods over a specified time period.
 *
 * @param request GetHealthPercentageRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetHealthPercentageResponse
 */
@context("getHealthPercentageWithContext")
async function getHealthPercentageWithOptions(request: GetHealthPercentageRequest, headers: map[string]string, runtime: $RuntimeOptions): GetHealthPercentageResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.cluster)) {
    query['cluster'] = request.cluster;
  }
  if (!$isNull(request.end)) {
    query['end'] = request.end;
  }
  if (!$isNull(request.instance)) {
    query['instance'] = request.instance;
  }
  if (!$isNull(request.start)) {
    query['start'] = request.start;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetHealthPercentage',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/openapi/cluster_health/range/health_percentage`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves the health status distribution of nodes or pods over a specified time period.
 *
 * @param request GetHealthPercentageRequest
 * @return GetHealthPercentageResponse
 */
async function getHealthPercentage(request: GetHealthPercentageRequest): GetHealthPercentageResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getHealthPercentageWithOptions(request, headers, runtime);
}

model GetHostCountRequest {
  cluster?: string(name='cluster', description='The cluster ID.', example='1808078950770264'),
  end?: float(name='end', description='The end time.', example='1725801327754'),
  instance?: string(name='instance', description='The instance ID.', example='i-wz9d00ut2ska3mlyhn6j'),
  start?: float(name='start', description='The start time.', example='1725797727754'),
}

model GetHostCountResponseBody = {
  code?: string(name='code', description='The status code.
- `code == Success` indicates that the authorization is successful.
- Other status codes indicate that the authorization failed. Check the `message` field for the detailed fault information.', example='Success'),
  data?: [ 
    {
      time?: long(name='time', description='The point in time.', example='1725797727754'),
      value?: int32(name='value', description='The count.', example='5'),
    }
  ](name='data', description='The returned data.'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error information.', example='“”'),
  requestId?: string(name='request_id', description='The request ID.', example='43A910E9-A739-525E-855D-A32C257F1826'),
  total?: long(name='total', description='The total number of entries.', example='3'),
}

model GetHostCountResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetHostCountResponseBody(name='body'),
}

/**
 * @summary Retrieves the number of nodes or the number of Pods on nodes in a cluster.
 *
 * @param request GetHostCountRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetHostCountResponse
 */
@context("getHostCountWithContext")
async function getHostCountWithOptions(request: GetHostCountRequest, headers: map[string]string, runtime: $RuntimeOptions): GetHostCountResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.cluster)) {
    query['cluster'] = request.cluster;
  }
  if (!$isNull(request.end)) {
    query['end'] = request.end;
  }
  if (!$isNull(request.instance)) {
    query['instance'] = request.instance;
  }
  if (!$isNull(request.start)) {
    query['start'] = request.start;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetHostCount',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/openapi/cluster_health/range/host_count`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves the number of nodes or the number of Pods on nodes in a cluster.
 *
 * @param request GetHostCountRequest
 * @return GetHostCountResponse
 */
async function getHostCount(request: GetHostCountRequest): GetHostCountResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getHostCountWithOptions(request, headers, runtime);
}

model GetHotSpotUniqListRequest {
  begEnd?: long(name='beg_end', description='End time

This parameter is required.', example='1735010351000'),
  begStart?: long(name='beg_start', description='Start time

This parameter is required.', example='1735008551000'),
  instance?: string(name='instance', description='Instance ID

This parameter is required.', example='i-bp1g2i0siirefgwnnnvy'),
  pid?: long(name='pid', description='Process ID', example='12345'),
  table?: string(name='table', description='Table name', example='prof_on'),
  uniq?: string(name='uniq', description='Identifier flag

This parameter is required.', example='默认需要为"flag"，待查询的字段'),
}

model GetHotSpotUniqListResponseBody = {
  code?: string(name='code', description='Status code
- `code == Success` indicates that the authorization is successful;
- Other status codes indicate that the authorization has failed. When authorization fails, check the `message` field for detailed error information;', example='Success'),
  data?: {
    columns?: [ string ](name='columns', description='List of data column names'),
    values?: [ string ](name='values', description='List of data values'),
  }(name='data', description='Response data'),
  message?: string(name='message', description='Response message', example='success'),
  requestId?: string(name='requestId', description='Request ID', example='2D693121-C925-5154-8DF6-C09A8B369822'),
  success?: string(name='success', description='true indicates the API call succeeded, false indicates the API call failed', example='true'),
}

model GetHotSpotUniqListResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetHotSpotUniqListResponseBody(name='body'),
}

/**
 * @summary Get the list of a specific field under an instance.
 *
 * @param request GetHotSpotUniqListRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetHotSpotUniqListResponse
 */
@context("getHotSpotUniqListWithContext")
async function getHotSpotUniqListWithOptions(request: GetHotSpotUniqListRequest, headers: map[string]string, runtime: $RuntimeOptions): GetHotSpotUniqListResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.begEnd)) {
    body['beg_end'] = request.begEnd;
  }
  if (!$isNull(request.begStart)) {
    body['beg_start'] = request.begStart;
  }
  if (!$isNull(request.instance)) {
    body['instance'] = request.instance;
  }
  if (!$isNull(request.pid)) {
    body['pid'] = request.pid;
  }
  if (!$isNull(request.table)) {
    body['table'] = request.table;
  }
  if (!$isNull(request.uniq)) {
    body['uniq'] = request.uniq;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetHotSpotUniqList',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/livetrace_proxy/hotspot_uniq_list`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Get the list of a specific field under an instance.
 *
 * @param request GetHotSpotUniqListRequest
 * @return GetHotSpotUniqListResponse
 */
async function getHotSpotUniqList(request: GetHotSpotUniqListRequest): GetHotSpotUniqListResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getHotSpotUniqListWithOptions(request, headers, runtime);
}

model GetHotspotAnalysisRequest {
  appType?: string(name='appType', description='The application type.

This parameter is required.', example='GR'),
  begEnd?: long(name='beg_end', description='The end time.

This parameter is required.', example='1725413948000'),
  begStart?: long(name='beg_start', description='The start time.

This parameter is required.', example='1725410348000'),
  instance?: string(name='instance', description='The instance ID.

This parameter is required.', example='i-2ze5ru5rjurix7f71sxv'),
  pid?: long(name='pid', description='The process ID (PID).', example='1657494'),
  table?: string(name='table', description='The name of the table to query.', example='prof_on'),
}

model GetHotspotAnalysisResponseBody = {
  code?: string(name='code', description='The error code.', example='SysomOpenAPI.ServerError'),
  data?: string(name='data', description='The returned data.', example='"AliYunDun:1657494进程(1657494):根据提供的热点调用栈信息，热点主要集中在`__nanosleep_nocancel`以及相关的系统调用`SyS_nanosleep`上，这表明进程在等待特定的时间间隔。通常这是由于应用中存在长时间的睡眠或高频率的定时操作导致的，比如定时任务、心跳检测等。\\\\n\\\\n应用代码在需要定期执行某些检查或等待外部事件时会使用`nanosleep`来实现精确的延时控制。为了优化这种情况，可以考虑以下方案：\\\\n- 评估是否可以减少定时任务的频率。\\\\n- 使用条件变量替代单纯的睡眠等待，以响应更快的事件触发。\\\\n- 如果是I/O密集型操作等待，考虑优化I/O路径或提升I/O效率。建议使用SysOM平台的IO诊断工具来进一步定位具体的I/O瓶颈。"'),
  message?: string(name='message', description='The response message.', example='success'),
  requestId?: string(name='requestId', description='Id of the request', example='2D693121-C925-5154-8DF6-C09A8B369822'),
  success?: boolean(name='success', description='Indicates whether the call was successful.', example='True'),
}

model GetHotspotAnalysisResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetHotspotAnalysisResponseBody(name='body'),
}

/**
 * @summary Retrieves hot spot analysis results.
 *
 * @param request GetHotspotAnalysisRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetHotspotAnalysisResponse
 */
@context("getHotspotAnalysisWithContext")
async function getHotspotAnalysisWithOptions(request: GetHotspotAnalysisRequest, headers: map[string]string, runtime: $RuntimeOptions): GetHotspotAnalysisResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.appType)) {
    body['appType'] = request.appType;
  }
  if (!$isNull(request.begEnd)) {
    body['beg_end'] = request.begEnd;
  }
  if (!$isNull(request.begStart)) {
    body['beg_start'] = request.begStart;
  }
  if (!$isNull(request.instance)) {
    body['instance'] = request.instance;
  }
  if (!$isNull(request.pid)) {
    body['pid'] = request.pid;
  }
  if (!$isNull(request.table)) {
    body['table'] = request.table;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetHotspotAnalysis',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/openapi/proxy/post/livetrace_hotspot_analysis`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves hot spot analysis results.
 *
 * @param request GetHotspotAnalysisRequest
 * @return GetHotspotAnalysisResponse
 */
async function getHotspotAnalysis(request: GetHotspotAnalysisRequest): GetHotspotAnalysisResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getHotspotAnalysisWithOptions(request, headers, runtime);
}

model GetHotspotCompareRequest {
  beg1End?: long(name='beg1_end', description='End time 1

This parameter is required.', example='1725415774000'),
  beg1Start?: long(name='beg1_start', description='Start time 1

This parameter is required.', example='1725415474000'),
  beg2End?: long(name='beg2_end', description='End time 2

This parameter is required.', example='1725415774000'),
  beg2Start?: long(name='beg2_start', description='Start time 2

This parameter is required.', example='1725415474000'),
  hotType?: string(name='hot_type', description='Hotspot type', example='alloc'),
  instance1?: string(name='instance1', description='Instance 1

This parameter is required.', example='i-2zei55fwj8nnu31h3z46"'),
  instance2?: string(name='instance2', description='Instance 2

This parameter is required.', example='0'),
  pid1?: long(name='pid1', description='Instance 1 process', example='0'),
  pid2?: long(name='pid2', description='Instance 2 process', example='i-2zei55fwj8nnu31h3z46'),
  table?: string(name='table', description='Query table name

This parameter is required.', example='prof_on'),
}

model GetHotspotCompareResponseBody = {
  code?: string(name='code', description='Error code', example='SysomOpenAPI.ServerError'),
  data?: {
    flame?: {
      columns?: [ string ](name='columns', description='Field name list'),
      values?: [[ string ]      ](name='values', description='Flame graph data list'),
    }(name='flame', description='Differential flame graph data'),
    seriesInstance1?: {
      columns?: [ string ](name='columns', description='Field name list'),
      values?: [[ string ]      ](name='values', description='Hotspot time series data list'),
    }(name='series_instance1', description='Hotspot time series 1'),
    seriesInstance2?: {
      columns?: [ string ](name='columns', description='Field name list'),
      values?: [[ string ]      ](name='values', description='Hotspot time series data list'),
    }(name='series_instance2', description='Hotspot time series 2'),
  }(name='data', description='Response data'),
  message?: string(name='message', description='Response message', example='success'),
  requestId?: string(name='requestId', description='Id of the request', example='2D693121-C925-5154-8DF6-C09A8B369822'),
  success?: boolean(name='success', description='true: API call succeeded; false: API call failed', example='true'),
}

model GetHotspotCompareResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetHotspotCompareResponseBody(name='body'),
}

/**
 * @summary Get Hotspot Comparison Tracing Results
 *
 * @param request GetHotspotCompareRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetHotspotCompareResponse
 */
@context("getHotspotCompareWithContext")
async function getHotspotCompareWithOptions(request: GetHotspotCompareRequest, headers: map[string]string, runtime: $RuntimeOptions): GetHotspotCompareResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.beg1End)) {
    body['beg1_end'] = request.beg1End;
  }
  if (!$isNull(request.beg1Start)) {
    body['beg1_start'] = request.beg1Start;
  }
  if (!$isNull(request.beg2End)) {
    body['beg2_end'] = request.beg2End;
  }
  if (!$isNull(request.beg2Start)) {
    body['beg2_start'] = request.beg2Start;
  }
  if (!$isNull(request.hotType)) {
    body['hot_type'] = request.hotType;
  }
  if (!$isNull(request.instance1)) {
    body['instance1'] = request.instance1;
  }
  if (!$isNull(request.instance2)) {
    body['instance2'] = request.instance2;
  }
  if (!$isNull(request.pid1)) {
    body['pid1'] = request.pid1;
  }
  if (!$isNull(request.pid2)) {
    body['pid2'] = request.pid2;
  }
  if (!$isNull(request.table)) {
    body['table'] = request.table;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetHotspotCompare',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/openapi/proxy/post/livetrace_hotspot_compare`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Get Hotspot Comparison Tracing Results
 *
 * @param request GetHotspotCompareRequest
 * @return GetHotspotCompareResponse
 */
async function getHotspotCompare(request: GetHotspotCompareRequest): GetHotspotCompareResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getHotspotCompareWithOptions(request, headers, runtime);
}

model GetHotspotInstanceListRequest {
  begEnd?: long(name='beg_end', description='End time

This parameter is required.', example='1725413947000'),
  begStart?: long(name='beg_start', description='Start time

This parameter is required.', example='1725413647000'),
  table?: string(name='table', description='Query table name

This parameter is required.', example='prof_on'),
}

model GetHotspotInstanceListResponseBody = {
  code?: string(name='code', description='Error code', example='SysomOpenAPI.ServerError'),
  data?: {
    columns?: [ string ](name='columns', description='Field names'),
    values?: [ string ](name='values', description='Instance list'),
  }(name='data', description='Response data.'),
  message?: string(name='message', description='Description', example='success'),
  requestId?: string(name='requestId', description='Id of the request', example='2D693121-C925-5154-8DF6-C09A8B369822'),
}

model GetHotspotInstanceListResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetHotspotInstanceListResponseBody(name='body'),
}

/**
 * @summary Get Hotspot Instance List
 *
 * @param request GetHotspotInstanceListRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetHotspotInstanceListResponse
 */
@context("getHotspotInstanceListWithContext")
async function getHotspotInstanceListWithOptions(request: GetHotspotInstanceListRequest, headers: map[string]string, runtime: $RuntimeOptions): GetHotspotInstanceListResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.begEnd)) {
    body['beg_end'] = request.begEnd;
  }
  if (!$isNull(request.begStart)) {
    body['beg_start'] = request.begStart;
  }
  if (!$isNull(request.table)) {
    body['table'] = request.table;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetHotspotInstanceList',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/openapi/proxy/post/livetrace_hotspot_instance_list`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Get Hotspot Instance List
 *
 * @param request GetHotspotInstanceListRequest
 * @return GetHotspotInstanceListResponse
 */
async function getHotspotInstanceList(request: GetHotspotInstanceListRequest): GetHotspotInstanceListResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getHotspotInstanceListWithOptions(request, headers, runtime);
}

model GetHotspotPidListRequest {
  begEnd?: long(name='beg_end', description='The end time.

This parameter is required.', example='1725413947000'),
  begStart?: long(name='beg_start', description='The start time.

This parameter is required.', example='1725413647000'),
  instance?: string(name='instance', description='The instance ID.

This parameter is required.', example='i-2ze5ru5rjurix7f71sxv'),
  table?: string(name='table', description='The name of the table to query.

This parameter is required.', example='prof_on'),
}

model GetHotspotPidListResponseBody = {
  code?: string(name='code', description='The error code.', example='SysomOpenAPI.InvalidParameter'),
  data?: {
    columns?: [ string ](name='columns', description='The list of field names.'),
    values?: [[ string ]    ](name='values', description='The list of process information.'),
  }(name='data', description='The returned data.'),
  message?: string(name='message', description='The response message.', example='Success'),
  requestId?: string(name='requestId', description='Id of the request', example='2D693121-C925-5154-8DF6-C09A8B369822'),
}

model GetHotspotPidListResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetHotspotPidListResponseBody(name='body'),
}

/**
 * @summary Retrieves the PID list of a specified instance.
 *
 * @param request GetHotspotPidListRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetHotspotPidListResponse
 */
@context("getHotspotPidListWithContext")
async function getHotspotPidListWithOptions(request: GetHotspotPidListRequest, headers: map[string]string, runtime: $RuntimeOptions): GetHotspotPidListResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.begEnd)) {
    body['beg_end'] = request.begEnd;
  }
  if (!$isNull(request.begStart)) {
    body['beg_start'] = request.begStart;
  }
  if (!$isNull(request.instance)) {
    body['instance'] = request.instance;
  }
  if (!$isNull(request.table)) {
    body['table'] = request.table;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetHotspotPidList',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/openapi/proxy/post/livetrace_hotspot_pid_list`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves the PID list of a specified instance.
 *
 * @param request GetHotspotPidListRequest
 * @return GetHotspotPidListResponse
 */
async function getHotspotPidList(request: GetHotspotPidListRequest): GetHotspotPidListResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getHotspotPidListWithOptions(request, headers, runtime);
}

model GetHotspotTrackingRequest {
  begEnd?: long(name='beg_end', description='The end time.

This parameter is required.', example='1725413948000'),
  begStart?: long(name='beg_start', description='The start time.

This parameter is required.', example='1725410348000'),
  hotType?: string(name='hot_type', description='The hot spot type.

This parameter is required.', example='alloc'),
  instance?: string(name='instance', description='The instance ID.

This parameter is required.', example='i-2ze5ru5rjurix7f71sxv'),
  pid?: long(name='pid', description='The process PID.', example='1657494'),
  table?: string(name='table', description='The name of the table to query.

This parameter is required.', example='prof_on'),
}

model GetHotspotTrackingResponseBody = {
  code?: string(name='code', description='The error code.', example='SysomOpenAPI.ServerError'),
  data?: {
    flame?: {
      columns?: [ string ](name='columns', description='The list of field names.'),
      values?: [[ string ]      ](name='values', description='The list of flame graph data.'),
    }(name='flame', description='The flame graph data.'),
    series?: {
      columns?: [ string ](name='columns', description='The list of field names.'),
      values?: [[ string ]      ](name='values', description='The list of hot spot time series data.'),
    }(name='series', description='The data list.'),
  }(name='data', description='The returned data.'),
  message?: string(name='message', description='The response message.', example='Success'),
  requestId?: string(name='requestId', description='The request ID.', example='2D693121-C925-5154-8DF6-C09A8B369822'),
}

model GetHotspotTrackingResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetHotspotTrackingResponseBody(name='body'),
}

/**
 * @summary Retrieves hot spot tracking results.
 *
 * @param request GetHotspotTrackingRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetHotspotTrackingResponse
 */
@context("getHotspotTrackingWithContext")
async function getHotspotTrackingWithOptions(request: GetHotspotTrackingRequest, headers: map[string]string, runtime: $RuntimeOptions): GetHotspotTrackingResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.begEnd)) {
    body['beg_end'] = request.begEnd;
  }
  if (!$isNull(request.begStart)) {
    body['beg_start'] = request.begStart;
  }
  if (!$isNull(request.hotType)) {
    body['hot_type'] = request.hotType;
  }
  if (!$isNull(request.instance)) {
    body['instance'] = request.instance;
  }
  if (!$isNull(request.pid)) {
    body['pid'] = request.pid;
  }
  if (!$isNull(request.table)) {
    body['table'] = request.table;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetHotspotTracking',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/openapi/proxy/post/livetrace_hotspot_tracking`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves hot spot tracking results.
 *
 * @param request GetHotspotTrackingRequest
 * @return GetHotspotTrackingResponse
 */
async function getHotspotTracking(request: GetHotspotTrackingRequest): GetHotspotTrackingResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getHotspotTrackingWithOptions(request, headers, runtime);
}

model GetInspectionReportRequest {
  reportId?: string(name='reportId', description='The inspection report ID.', example='91bec4c5a168494e8128468e3995df87'),
}

model GetInspectionReportResponseBody = {
  code?: string(name='code', description='The status code.
- If `code == Success`, the authorization is successful.
- Other status codes indicate authorization failed. Check the `message` field for the detailed fault information.', example='SysomOpenAPI.InvalidParameter'),
  data?: any(name='data', description='The response data.', example='{}'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error information.', example='SysomOpenAPIException: SysomOpenAPI.InvalidParameter Invalid params, should be json string or dict'),
  requestId?: string(name='requestId', description='Id of the request', example='2D693121-C925-5154-8DF6-C09A8B369822'),
}

model GetInspectionReportResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetInspectionReportResponseBody(name='body'),
}

/**
 * @summary Retrieves a SysOM inspection report.
 *
 * @param request GetInspectionReportRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetInspectionReportResponse
 */
@context("getInspectionReportWithContext")
async function getInspectionReportWithOptions(request: GetInspectionReportRequest, headers: map[string]string, runtime: $RuntimeOptions): GetInspectionReportResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.reportId)) {
    query['reportId'] = request.reportId;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetInspectionReport',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/inspection/getInspectionReport`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves a SysOM inspection report.
 *
 * @param request GetInspectionReportRequest
 * @return GetInspectionReportResponse
 */
async function getInspectionReport(request: GetInspectionReportRequest): GetInspectionReportResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getInspectionReportWithOptions(request, headers, runtime);
}

model GetInstantScoreRequest {
  cluster?: string(name='cluster', description='Cluster ID.', example='2ijff4be-bf24-4070-89ca-c47c879b0g32'),
  instance?: string(name='instance', description='Instance ID.', example='i-wz9d00ut2ska3mlyhn6j'),
}

model GetInstantScoreResponseBody = {
  code?: string(name='code', description='Status code.
- If `code == Success`, the authorization is successful.
- Other status codes indicate authorization failure. Check the `message` field for detailed error information when authorization fails.', example='Success'),
  data?: {
    error?: float(name='error', description='Error score.', example='100'),
    latency?: float(name='latency', description='Latency score.', example='100'),
    load?: float(name='load', description='Load score.', example='100'),
    saturation?: float(name='saturation', description='Saturation score.', example='100'),
    total?: float(name='total', description='Total score.', example='100'),
  }(name='data', description='Response data.'),
  message?: string(name='message', description='Error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error message.', example='Query no data'),
  requestId?: string(name='requestId', description='Id of the request', example='2D693121-C925-5154-8DF6-C09A8B369822'),
}

model GetInstantScoreResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetInstantScoreResponseBody(name='body'),
}

/**
 * @summary Get real-time cluster/node health score
 *
 * @param request GetInstantScoreRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetInstantScoreResponse
 */
@context("getInstantScoreWithContext")
async function getInstantScoreWithOptions(request: GetInstantScoreRequest, headers: map[string]string, runtime: $RuntimeOptions): GetInstantScoreResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.cluster)) {
    query['cluster'] = request.cluster;
  }
  if (!$isNull(request.instance)) {
    query['instance'] = request.instance;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetInstantScore',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/openapi/cluster_health/instant/score`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Get real-time cluster/node health score
 *
 * @param request GetInstantScoreRequest
 * @return GetInstantScoreResponse
 */
async function getInstantScore(request: GetInstantScoreRequest): GetInstantScoreResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getInstantScoreWithOptions(request, headers, runtime);
}

model GetListRecordRequest {
  analysisId?: string(name='analysisId', description='The ID of the analysis record to query the status of.', example='xxxxxxxxx-f37b-478d-87f6-2259ed8766bf'),
  current?: long(name='current', description='The current page number.', example='5'),
  customId?: long(name='customId'),
  pageSize?: long(name='pageSize', description='The number of entries per page.', example='10'),
  region?: string(name='region', description='The region ID.', example='cn-hangzhou'),
}

model GetListRecordResponseBody = {
  code?: string(name='code', description='The status code.', example='Success'),
  data?: [ 
    {
      analysisId?: string(name='analysisId', description='The AI analysis ID.', example='16896fa8-37f6-4c70-bb32-67fa9817d426'),
      analysisTime?: string(name='analysisTime', description='The analysis time.', example='2024-12-24 12:02:05'),
      arguments?: string(name='arguments', description='The analysis parameters of the AI job analysis.', example='timeout=2000 ms'),
      failedLog?: string(name='failedLog', description='The failure log of the analysis.', example='机器i-wz9dej066kii4goqpnze分析失败, 失败原因: Not get GPU trace data for \\\\"e59ce870-dbd4-4c44-a814-174ac6ab5bcf\\\\" \\\\"[\\\\"118534\\\\"]\\\\"!'),
      status?: string(name='status', description='The analysis status.', example='已完成/分析失败'),
    }
  ](name='data', description='The returned data.'),
  message?: string(name='message', description='The description of the status code.', example='""'),
  requestId?: string(name='requestId', description='Id of the request', example='2D693121-C925-5154-8DF6-C09A8B369822'),
  total?: long(name='total', description='The total number of records.', example='19'),
}

model GetListRecordResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetListRecordResponseBody(name='body'),
}

/**
 * @summary Retrieves a list of AI Infra analysis records.
 *
 * @param request GetListRecordRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetListRecordResponse
 */
@context("getListRecordWithContext")
async function getListRecordWithOptions(request: GetListRecordRequest, headers: map[string]string, runtime: $RuntimeOptions): GetListRecordResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.analysisId)) {
    query['analysisId'] = request.analysisId;
  }
  if (!$isNull(request.current)) {
    query['current'] = request.current;
  }
  if (!$isNull(request.customId)) {
    query['customId'] = request.customId;
  }
  if (!$isNull(request.pageSize)) {
    query['pageSize'] = request.pageSize;
  }
  if (!$isNull(request.region)) {
    query['region'] = request.region;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetListRecord',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/app_observ/aiAnalysis/list_record`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves a list of AI Infra analysis records.
 *
 * @param request GetListRecordRequest
 * @return GetListRecordResponse
 */
async function getListRecord(request: GetListRecordRequest): GetListRecordResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getListRecordWithOptions(request, headers, runtime);
}

model GetProblemPercentageRequest {
  cluster?: string(name='cluster', description='Cluster ID.', example='2ijff4be-bf24-4070-89ca-c47c879b0g32'),
  end?: float(name='end', description='End time.

This parameter is required.', example='1725801327754'),
  instance?: string(name='instance', description='Instance ID.', example='i-wz9d00ut2ska3mlyhn6j'),
  start?: float(name='start', description='Start time.

This parameter is required.', example='1725797727754'),
}

model GetProblemPercentageResponseBody = {
  code?: string(name='code', description='Status code.
- If `code == Success`, the authorization is successful.
- Other status codes indicate authorization failure. Check the `message` field for detailed error information.', example='Success'),
  data?: [ 
    {
      type?: string(name='type', description='Abnormal item type.', example='saturation'),
      value?: long(name='value', description='Number of abnormal items.', example='5'),
    }
  ](name='data', description='Response data.'),
  message?: string(name='message', description='Error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error information.', example='result: code=1 msg=(Request failed, status_code != 200)'),
  requestId?: string(name='request_id', description='Request ID.', example='35F91AAB-5FDF-5A22-B211-C7C6B00817D0'),
  total?: long(name='total', description='Total number of data entries.', example='19'),
}

model GetProblemPercentageResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetProblemPercentageResponseBody(name='body'),
}

/**
 * @summary Get the proportion of abnormal issues in cluster nodes/pods within a specified time range
 *
 * @param request GetProblemPercentageRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetProblemPercentageResponse
 */
@context("getProblemPercentageWithContext")
async function getProblemPercentageWithOptions(request: GetProblemPercentageRequest, headers: map[string]string, runtime: $RuntimeOptions): GetProblemPercentageResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.cluster)) {
    query['cluster'] = request.cluster;
  }
  if (!$isNull(request.end)) {
    query['end'] = request.end;
  }
  if (!$isNull(request.instance)) {
    query['instance'] = request.instance;
  }
  if (!$isNull(request.start)) {
    query['start'] = request.start;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetProblemPercentage',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/openapi/cluster_health/range/problem_percentage`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Get the proportion of abnormal issues in cluster nodes/pods within a specified time range
 *
 * @param request GetProblemPercentageRequest
 * @return GetProblemPercentageResponse
 */
async function getProblemPercentage(request: GetProblemPercentageRequest): GetProblemPercentageResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getProblemPercentageWithOptions(request, headers, runtime);
}

model GetRangeScoreRequest {
  cluster?: string(name='cluster', description='The cluster ID.', example='2ijff4be-bf24-4070-89ca-c47c879b0g32'),
  end?: float(name='end', description='The end time.

This parameter is required.', example='1725801327754'),
  instance?: string(name='instance', description='The instance ID.', example='i-wz9d00ut2ska3mlyhn6j'),
  start?: float(name='start', description='The start time.

This parameter is required.', example='1725797727754'),
}

model GetRangeScoreResponseBody = {
  code?: string(name='code', description='The status code.
- If `code == Success`, the authorization is successful.
- Other status codes indicate that the authorization has failed. Check the `message` field for the detailed fault information.', example='Success'),
  data?: [ 
    {
      time?: long(name='time', description='The point in time.', example='1725797727754'),
      type?: string(name='type', description='The metric type.', example='saturation'),
      value?: long(name='value', description='The score.', example='100'),
    }
  ](name='data', description='The returned data.'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error information.', example='""'),
  requestId?: string(name='request_id', description='The request ID.', example='35F91AAB-5FDF-5A22-B211-C7C6B00817D0'),
  total?: float(name='total', description='The total amount of data.', example='2'),
}

model GetRangeScoreResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetRangeScoreResponseBody(name='body'),
}

/**
 * @summary Retrieves the health score trend.
 *
 * @param request GetRangeScoreRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetRangeScoreResponse
 */
@context("getRangeScoreWithContext")
async function getRangeScoreWithOptions(request: GetRangeScoreRequest, headers: map[string]string, runtime: $RuntimeOptions): GetRangeScoreResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.cluster)) {
    query['cluster'] = request.cluster;
  }
  if (!$isNull(request.end)) {
    query['end'] = request.end;
  }
  if (!$isNull(request.instance)) {
    query['instance'] = request.instance;
  }
  if (!$isNull(request.start)) {
    query['start'] = request.start;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetRangeScore',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/openapi/cluster_health/range/score`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves the health score trend.
 *
 * @param request GetRangeScoreRequest
 * @return GetRangeScoreResponse
 */
async function getRangeScore(request: GetRangeScoreRequest): GetRangeScoreResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getRangeScoreWithOptions(request, headers, runtime);
}

model GetResourcesRequest {
  cluster?: string(name='cluster', description='The cluster ID.', example='1808078950770264'),
  instance?: string(name='instance', description='The instance ID.', example='i-wz9d00ut2ska3mlyhn6j'),
  type?: string(name='type', description='The resource type.', example='mem'),
}

model GetResourcesResponseBody = {
  code?: string(name='code', description='The status code.
- `code == Success` indicates that the authorization is successful.
- Other status codes indicate that the authorization failed. Check the `message` field for the detailed fault information.', example='Success'),
  data?: {
    total?: float(name='total', description='The total number of resources.', example='2354'),
    unit?: string(name='unit', description='The unit.', example='Kbytes'),
    usage?: float(name='usage', description='The resource usage.', example='100'),
  }(name='data', description='The returned data.'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error information.', example='result: code=1 msg=(Request failed, status_code != 200)'),
  requestId?: string(name='request_id', description='The request ID.', example='35F91AAB-5FDF-5A22-B211-C7C6B00817D0'),
}

model GetResourcesResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetResourcesResponseBody(name='body'),
}

/**
 * @summary Retrieves the real-time resource usage of a cluster or node.
 *
 * @param request GetResourcesRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetResourcesResponse
 */
@context("getResourcesWithContext")
async function getResourcesWithOptions(request: GetResourcesRequest, headers: map[string]string, runtime: $RuntimeOptions): GetResourcesResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.cluster)) {
    query['cluster'] = request.cluster;
  }
  if (!$isNull(request.instance)) {
    query['instance'] = request.instance;
  }
  if (!$isNull(request.type)) {
    query['type'] = request.type;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetResources',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/openapi/cluster_health/instant/resource`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves the real-time resource usage of a cluster or node.
 *
 * @param request GetResourcesRequest
 * @return GetResourcesResponse
 */
async function getResources(request: GetResourcesRequest): GetResourcesResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getResourcesWithOptions(request, headers, runtime);
}

model GetServiceFuncStatusRequest {
  channel?: string(name='channel', description='The channel name.

This parameter is required.', example='ecs'),
  params?: {
    functionName?: string(name='function_name', description='The feature name.

This parameter is required.', example='mullprof'),
    instance?: string(name='instance', description='The instance ID.', example='i-2zei55fwj8nnu31h3z46'),
    uid?: string(name='uid', description='The user ID.', example='1338904783509062'),
  }(name='params', description='The diagnostic parameters. Different types of diagnostics require different diagnostic parameters. You can use this field to filter records whose parameters match the specified values.

This parameter is required.'),
  serviceName?: string(name='service_name', description='The service name.

This parameter is required.', example='livetrace'),
}

model GetServiceFuncStatusShrinkRequest {
  channel?: string(name='channel', description='The channel name.

This parameter is required.', example='ecs'),
  paramsShrink?: string(name='params', description='The diagnostic parameters. Different types of diagnostics require different diagnostic parameters. You can use this field to filter records whose parameters match the specified values.

This parameter is required.'),
  serviceName?: string(name='service_name', description='The service name.

This parameter is required.', example='livetrace'),
}

model GetServiceFuncStatusResponseBody = {
  code?: string(name='code', description='The status code.
- `code == Success` indicates that the authorization is successful.
- Other status codes indicate that the authorization failed. Check the `message` field for the detailed fault information.', example='Success'),
  data?: {
    args?: {
      addCmd?: string(name='add_cmd', description='The configuration process.', example='java'),
      cpu?: string(name='cpu', description='The ONCPU tracing switch.', example='true'),
      javaStorePath?: string(name='java_store_path', description='The storage path.', example='/tmp/sysom/java-profiler'),
      locks?: string(name='locks', description='The lock tracing switch.', example='true'),
      loop?: int32(name='loop', description='The tracing cycle.', example='-1'),
      mem?: string(name='mem', description='The memory tracing switch.', example='true'),
      systemProfiling?: string(name='system_profiling', description='The system profiling switch.', example='true'),
    }(name='args', description='The configuration parameters.'),
  }(name='data', description='The returned result.'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error information.'),
  requestId?: string(name='requestId', description='Id of the request', example='2D693121-C925-5154-8DF6-C09A8B369822'),
}

model GetServiceFuncStatusResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetServiceFuncStatusResponseBody(name='body'),
}

/**
 * @summary Retrieves the configuration of a feature module.
 *
 * @description Retrieves the service configuration status.
 *
 * @param tmpReq GetServiceFuncStatusRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetServiceFuncStatusResponse
 */
@context("getServiceFuncStatusWithContext")
async function getServiceFuncStatusWithOptions(tmpReq: GetServiceFuncStatusRequest, headers: map[string]string, runtime: $RuntimeOptions): GetServiceFuncStatusResponse {
  tmpReq.validate();
  var request = new GetServiceFuncStatusShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.params)) {
    request.paramsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.params, 'params', 'json');
  }
  var query : map[string]any = {};
  if (!$isNull(request.channel)) {
    query['channel'] = request.channel;
  }
  if (!$isNull(request.paramsShrink)) {
    query['params'] = request.paramsShrink;
  }
  if (!$isNull(request.serviceName)) {
    query['service_name'] = request.serviceName;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetServiceFuncStatus',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/func-switch/get-service-func-status`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves the configuration of a feature module.
 *
 * @description Retrieves the service configuration status.
 *
 * @param request GetServiceFuncStatusRequest
 * @return GetServiceFuncStatusResponse
 */
async function getServiceFuncStatus(request: GetServiceFuncStatusRequest): GetServiceFuncStatusResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getServiceFuncStatusWithOptions(request, headers, runtime);
}

model GetVmcoreDiagnosisTaskRequest {
  taskId?: string(name='taskId', description='The task ID.

This parameter is required.', example='bbe94a98-4192-4172-b856-95777e0a55d7'),
}

model GetVmcoreDiagnosisTaskResponseBody = {
  code?: string(name='code', description='The status code.
- `code == Success` indicates that the authorization is successful.
- Other status codes indicate that the authorization failed. Check the `message` field for the detailed fault information.', example='Success'),
  data?: {
    createdAt?: string(name='createdAt', description='The time when the task was created.', example='2025-12-02T17:36:12'),
    diagnoseResult?: string(name='diagnoseResult', description='The diagnostic result.', example='result'),
    errorMsg?: string(name='errorMsg', description='The diagnostic error message.', example='error message'),
    taskId?: string(name='taskId', description='The task ID.', example='bbe94a98-4192-4172-b856-95777e0a55d7'),
    taskStatus?: string(name='taskStatus', description='The task status.', example='running'),
    taskType?: string(name='taskType', description='The task type.', example='vmcore'),
    urls?: {
      debuginfoCommonUrl?: string(name='debuginfoCommonUrl', description='The download URL of the debuginfo-common RPM package.', example='https://bucket-cn-hangzhou.oss-cn-hangzhou.aliyuncs.com/debuginfo-common/file/path'),
      debuginfoUrl?: string(name='debuginfoUrl', description='The download URL of the debuginfo RPM package.', example='https://bucket-cn-hangzhou.oss-cn-hangzhou.aliyuncs.com/debuginfo/file/path'),
      dmesgUrl?: string(name='dmesgUrl', description='The download URL of the dmesg log.', example='https://bucket-cn-hangzhou.oss-cn-hangzhou.aliyuncs.com/dmesg/file/path'),
      vmcoreUrl?: string(name='vmcoreUrl', description='The download URL of the vmcore file.', example='https://bucket-cn-hangzhou.oss-cn-hangzhou.aliyuncs.com/vmcore/file/path'),
    }(name='urls', description='The download URLs of related files associated with the task.'),
  }(name='data', description='The returned result.'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error information.', example='SysomOpenAPIException: SysomOpenAPI.InvalidParameter Invalid params, should be json string or dict'),
  requestId?: string(name='requestId', description='Id of the request', example='2D693121-C925-5154-8DF6-C09A8B369822'),
}

model GetVmcoreDiagnosisTaskResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetVmcoreDiagnosisTaskResponseBody(name='body'),
}

/**
 * @summary Queries the execution status and diagnostic result of a diagnostic task by task ID.
 *
 * @param request GetVmcoreDiagnosisTaskRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetVmcoreDiagnosisTaskResponse
 */
@context("getVmcoreDiagnosisTaskWithContext")
async function getVmcoreDiagnosisTaskWithOptions(request: GetVmcoreDiagnosisTaskRequest, headers: map[string]string, runtime: $RuntimeOptions): GetVmcoreDiagnosisTaskResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.taskId)) {
    query['taskId'] = request.taskId;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetVmcoreDiagnosisTask',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/crashAgent/diagnosis/queryTask`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the execution status and diagnostic result of a diagnostic task by task ID.
 *
 * @param request GetVmcoreDiagnosisTaskRequest
 * @return GetVmcoreDiagnosisTaskResponse
 */
async function getVmcoreDiagnosisTask(request: GetVmcoreDiagnosisTaskRequest): GetVmcoreDiagnosisTaskResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return getVmcoreDiagnosisTaskWithOptions(request, headers, runtime);
}

model InitialSysomRequest {
  checkOnly?: boolean(name='check_only', description='Specifies whether to only check whether the service-linked role exists.'),
  source?: string(name='source', description='The source. Set this parameter to console.', example='console'),
}

model InitialSysomResponseBody = {
  requestId?: string(name='RequestId', description='The request ID, which can be used for end-to-end diagnostics.', example='3FCA2E38-2A8E-5501-93BD-5CE1BA58F2EF'),
  code?: string(name='code', description='The status code.
- `code == Success`: The authorization is successful.
- Other status codes indicate that the authorization failed. Check the `message` field for the detailed fault information.', example='Success'),
  data?: {
    roleExist?: boolean(name='role_exist', description='Indicates whether the service-linked role exists.', example='true'),
  }(name='data', description='The response data.'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error information.', example='SysomOpenAPIException: SysomOpenAPI.InvalidParameter Invalid params, should be json string or dict'),
}

model InitialSysomResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: InitialSysomResponseBody(name='body'),
}

/**
 * @summary Initializes SysOM to ensure that the service-linked role exists.
 *
 * @description Some SysOM API operations require role assumption based on the `AliyunServiceRoleForSysom` service-linked role. Before using SysOM features, invoke this operation to perform initialization and ensure that the service-linked role has been created.
 * - `check_only`: If this parameter is set to True, the operation only checks whether the service-linked role exists and does not create it. If this parameter is set to False or left empty, the operation performs automatic creation of the service-linked role if it does not exist.
 * > 
 * > Note: When you call this operation to initialize the role through the API, you agree to the user agreement of the operating system console by default. For more information, see [Operating system console overview](https://www.alibabacloud.com/help/en/alinux/product-overview/os-console-overview) and [Alibaba Cloud Service Trial Terms](https://terms.aliyun.com/legal-agreement/terms/suit_bu1_ali_cloud/suit_bu1_ali_cloud202001091714_51956.html).
 *
 * @param request InitialSysomRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return InitialSysomResponse
 */
@context("initialSysomWithContext")
async function initialSysomWithOptions(request: InitialSysomRequest, headers: map[string]string, runtime: $RuntimeOptions): InitialSysomResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.checkOnly)) {
    body['check_only'] = request.checkOnly;
  }
  if (!$isNull(request.source)) {
    body['source'] = request.source;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'InitialSysom',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/openapi/initial`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Initializes SysOM to ensure that the service-linked role exists.
 *
 * @description Some SysOM API operations require role assumption based on the `AliyunServiceRoleForSysom` service-linked role. Before using SysOM features, invoke this operation to perform initialization and ensure that the service-linked role has been created.
 * - `check_only`: If this parameter is set to True, the operation only checks whether the service-linked role exists and does not create it. If this parameter is set to False or left empty, the operation performs automatic creation of the service-linked role if it does not exist.
 * > 
 * > Note: When you call this operation to initialize the role through the API, you agree to the user agreement of the operating system console by default. For more information, see [Operating system console overview](https://www.alibabacloud.com/help/en/alinux/product-overview/os-console-overview) and [Alibaba Cloud Service Trial Terms](https://terms.aliyun.com/legal-agreement/terms/suit_bu1_ali_cloud/suit_bu1_ali_cloud202001091714_51956.html).
 *
 * @param request InitialSysomRequest
 * @return InitialSysomResponse
 */
async function initialSysom(request: InitialSysomRequest): InitialSysomResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return initialSysomWithOptions(request, headers, runtime);
}

model InstallAgentRequest {
  agentId?: string(name='agent_id', description='The ID of the component to install.

This parameter is required.', example='74a86327-3170-412c-8e67-da3389ec56a9'),
  agentVersion?: string(name='agent_version', description='The version of the component to install.

This parameter is required.', example='3.4.0-1'),
  installType?: string(name='install_type', description='The installation type. Valid values:
- InstallAndUpgrade: installs the component if it does not exist, or updates it if it exists.
- OnlyInstallNotHasAgent: installs the component if it does not exist, or takes no action if it exists.
- OnlyUpgradeHasAgent: takes no action if the component does not exist, or updates it if it exists.
- OnlyInstallWithoutStart: installs the component only without starting the service.

This parameter is required.', example='InstallAndUpgrade'),
  instances?: [ 
    {
      instance?: string(name='instance', description='The instance ID.

This parameter is required.', example='i-wz9b9vucz1iubsz8sjqo'),
      region?: string(name='region', description='The region ID.

This parameter is required.', example='cn-hangzhou'),
    }
  ](name='instances', description='The list of instances on which to install the component.

This parameter is required.'),
}

model InstallAgentResponseBody = {
  requestId?: string(name='RequestId', description='The request ID, which can be used for end-to-end diagnostics.', example='1D8887FC-4BDB-5A1C-AB19-135C29A9E481'),
  code?: string(name='code', description='The status code.
- If `code == Success`, the authorization is successful.
- Other status codes indicate authorization failed. Check the `message` field for the detailed fault information.', example='Success'),
  data?: {
    taskId?: string(name='task_id', description='The task ID.

You can use this task ID to call the GetAgentTask operation to retrieve the task execution status.', example='26b3cd97389c43dcad6bc4901c36fcec'),
  }(name='data', description='The response data.'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error information.', example='""'),
}

model InstallAgentResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: InstallAgentResponseBody(name='body'),
}

/**
 * @summary Installs an Agent on a specified instance.
 *
 * @description Calling this operation to install an Agent is asynchronous. After the call, a task_id is returned. You can use this ID to call the GetAgentTask operation to retrieve the task execution status.
 *
 * @param request InstallAgentRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return InstallAgentResponse
 */
@context("installAgentWithContext")
async function installAgentWithOptions(request: InstallAgentRequest, headers: map[string]string, runtime: $RuntimeOptions): InstallAgentResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.agentId)) {
    body['agent_id'] = request.agentId;
  }
  if (!$isNull(request.agentVersion)) {
    body['agent_version'] = request.agentVersion;
  }
  if (!$isNull(request.installType)) {
    body['install_type'] = request.installType;
  }
  if (!$isNull(request.instances)) {
    body['instances'] = request.instances;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'InstallAgent',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/am/agent/install_agent`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Installs an Agent on a specified instance.
 *
 * @description Calling this operation to install an Agent is asynchronous. After the call, a task_id is returned. You can use this ID to call the GetAgentTask operation to retrieve the task execution status.
 *
 * @param request InstallAgentRequest
 * @return InstallAgentResponse
 */
async function installAgent(request: InstallAgentRequest): InstallAgentResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return installAgentWithOptions(request, headers, runtime);
}

model InstallAgentForClusterRequest {
  agentId?: string(name='agent_id', description='The component ID.', example='74a86327-3170-412c-8e67-da3389ec56a9'),
  agentVersion?: string(name='agent_version', description='The component version.', example='3.4.0-1'),
  clusterId?: string(name='cluster_id', description='The cluster ID.

> The cluster ID here must be an ACK cluster ID.', example='c9d7f3fc3d42942afbcb65c1100ffb19d'),
  configId?: string(name='config_id', description='The component configuration ID.', example='8gj86wrt7-3170-412c-8e67-da3389ecg6a9'),
  grayscaleConfig?: string(name='grayscale_config', description='The canary release configuration.', example='{"type":"numeric","config":{"value":2}}'),
}

model InstallAgentForClusterResponseBody = {
  requestId?: string(name='RequestId', description='The request ID, which can be used for end-to-end diagnostics.', example='B149FD9C-ED5C-5765-B3AD-05AA4A4D64D7'),
  code?: string(name='code', description='The status code.
- If `code == Success`, the authorization is successful.
- Any other status code indicates authorization failure. Check the `message` field for detailed error information.', example='SysomOpenAPI.ServerError'),
  data?: {
    taskId?: string(name='task_id', description='The task ID.', example='049ea0609515414b9e19c3389d7ba638'),
  }(name='data', description='The response data.'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error message.', example='success'),
}

model InstallAgentForClusterResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: InstallAgentForClusterResponseBody(name='body'),
}

/**
 * @summary Install component for cluster
 *
 * @description After installing a component for the target ACK cluster:
 * 1. First, when the cluster is managed for the first time, the component will be installed on all ECS instances currently in the cluster. If the cluster has more than 50 nodes, only 50 instances will be covered in the first batch.
 * 2. Then, the SysOM console periodically checks the scaling status of the managed cluster. Once a new ECS instance is added to the cluster, the SysOM console automatically installs the component on it without user intervention.
 *
 * @param request InstallAgentForClusterRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return InstallAgentForClusterResponse
 */
@context("installAgentForClusterWithContext")
async function installAgentForClusterWithOptions(request: InstallAgentForClusterRequest, headers: map[string]string, runtime: $RuntimeOptions): InstallAgentForClusterResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.agentId)) {
    body['agent_id'] = request.agentId;
  }
  if (!$isNull(request.agentVersion)) {
    body['agent_version'] = request.agentVersion;
  }
  if (!$isNull(request.clusterId)) {
    body['cluster_id'] = request.clusterId;
  }
  if (!$isNull(request.configId)) {
    body['config_id'] = request.configId;
  }
  if (!$isNull(request.grayscaleConfig)) {
    body['grayscale_config'] = request.grayscaleConfig;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'InstallAgentForCluster',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/am/agent/install_agent_by_cluster`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Install component for cluster
 *
 * @description After installing a component for the target ACK cluster:
 * 1. First, when the cluster is managed for the first time, the component will be installed on all ECS instances currently in the cluster. If the cluster has more than 50 nodes, only 50 instances will be covered in the first batch.
 * 2. Then, the SysOM console periodically checks the scaling status of the managed cluster. Once a new ECS instance is added to the cluster, the SysOM console automatically installs the component on it without user intervention.
 *
 * @param request InstallAgentForClusterRequest
 * @return InstallAgentForClusterResponse
 */
async function installAgentForCluster(request: InstallAgentForClusterRequest): InstallAgentForClusterResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return installAgentForClusterWithOptions(request, headers, runtime);
}

model InstallAgentWithTypeRequest {
  agentId?: string(name='agentId', description='This parameter is required.', example='74a86327-3170-412c-8e67-da3389ec56a9'),
  agentVersion?: string(name='agentVersion', description='This parameter is required.', example='3.4.0-1'),
  configId?: string(name='configId', example='f0078fbb-4213-11f0-a19b-00163e4ae208'),
  instanceType?: string(name='instanceType', description='This parameter is required.', example='ecs'),
  instances?: [ 
    {
      instance?: string(name='instance', description='This parameter is required.', example='i-wz9b9vucz1iubsz8sjqo'),
      region?: string(name='region', description='This parameter is required.', example='cn-hangzhou'),
    }
  ](name='instances', description='This parameter is required.'),
}

model InstallAgentWithTypeResponseBody = {
  code?: string(name='code', example='Success'),
  data?: {
    taskId?: string(name='taskId', example='26b3cd97389c43dcad6bc4901c36fcec'),
  }(name='data'),
  message?: string(name='message', example='""'),
  requestId?: string(name='requestId', example='1D8887FC-4BDB-5A1C-AB19-135C29A9E481'),
}

model InstallAgentWithTypeResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: InstallAgentWithTypeResponseBody(name='body'),
}

/**
 * @summary 在指定的实例上安装 Agent
 *
 * @description 调用本接口安装 Agent 是异步的，调用接口后会返回一个 task_id，可以凭借该 ID 调用 GetAgentTask 接口获取任务的执行情况。
 *
 * @param request InstallAgentWithTypeRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return InstallAgentWithTypeResponse
 */
@context("installAgentWithTypeWithContext")
async function installAgentWithTypeWithOptions(request: InstallAgentWithTypeRequest, headers: map[string]string, runtime: $RuntimeOptions): InstallAgentWithTypeResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.agentId)) {
    body['agentId'] = request.agentId;
  }
  if (!$isNull(request.agentVersion)) {
    body['agentVersion'] = request.agentVersion;
  }
  if (!$isNull(request.configId)) {
    body['configId'] = request.configId;
  }
  if (!$isNull(request.instanceType)) {
    body['instanceType'] = request.instanceType;
  }
  if (!$isNull(request.instances)) {
    body['instances'] = request.instances;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'InstallAgentWithType',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/am/agent/installAgent`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 在指定的实例上安装 Agent
 *
 * @description 调用本接口安装 Agent 是异步的，调用接口后会返回一个 task_id，可以凭借该 ID 调用 GetAgentTask 接口获取任务的执行情况。
 *
 * @param request InstallAgentWithTypeRequest
 * @return InstallAgentWithTypeResponse
 */
async function installAgentWithType(request: InstallAgentWithTypeRequest): InstallAgentWithTypeResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return installAgentWithTypeWithOptions(request, headers, runtime);
}

model InvokeAnomalyDiagnosisRequest {
  uuid?: string(name='uuid', description='The UUID of the anomaly event.', example='8047d763-5465-4a8c-b1cd-23f5a8ba2594'),
}

model InvokeAnomalyDiagnosisResponseBody = {
  code?: string(name='code', description='The status code.
- If `code == Success`, the authorization is successful.
- Other status codes indicate authorization failed. Check the `message` field for the detailed fault message.', example='Success'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error information.', example='success'),
  requestId?: string(name='requestId', description='Id of the request', example='2D693121-C925-5154-8DF6-C09A8B369822'),
}

model InvokeAnomalyDiagnosisResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: InvokeAnomalyDiagnosisResponseBody(name='body'),
}

/**
 * @summary Initiates an anomaly diagnostics task.
 *
 * @param request InvokeAnomalyDiagnosisRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return InvokeAnomalyDiagnosisResponse
 */
@context("invokeAnomalyDiagnosisWithContext")
async function invokeAnomalyDiagnosisWithOptions(request: InvokeAnomalyDiagnosisRequest, headers: map[string]string, runtime: $RuntimeOptions): InvokeAnomalyDiagnosisResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.uuid)) {
    query['uuid'] = request.uuid;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'InvokeAnomalyDiagnosis',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/openapi/cluster_health/diagnosis/invoke_anomaly_diagnose`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Initiates an anomaly diagnostics task.
 *
 * @param request InvokeAnomalyDiagnosisRequest
 * @return InvokeAnomalyDiagnosisResponse
 */
async function invokeAnomalyDiagnosis(request: InvokeAnomalyDiagnosisRequest): InvokeAnomalyDiagnosisResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return invokeAnomalyDiagnosisWithOptions(request, headers, runtime);
}

model InvokeDiagnosisRequest {
  channel?: string(name='channel', description='Diagnosis channel (currently fixed as the ECS channel).

This parameter is required.', example='ecs'),
  params?: string(name='params', description='Diagnosis parameters. Different types of diagnoses require different diagnosis parameters. For the parameters required by each diagnosis type, refer to the supplementary request parameter description below.

>Notice: Please pass a JSON-formatted string.</notice>

This parameter is required.', example='{
    "instance": "i-wz9gdv7qmdhusamc4dl01",
    "uid": "xxxxxxxxxxxxxx",
    "region": "cn-shenzhen"
}'),
  serviceName?: string(name='service_name', description='Diagnosis type, used to distinguish different types of diagnoses.

This parameter is required.', example='memgraph'),
}

model InvokeDiagnosisResponseBody = {
  code?: string(name='code', description='Status code.
- If `code == Success`, the authorization is successful.
- Other status codes indicate authorization failure. When authorization fails, check the `message` field for detailed error information.', example='Success'),
  data?: {
    taskId?: string(name='task_id', description='Diagnosis task ID. You can use this ID to call the `GetDiagnosisResult` API to query the diagnosis result.', example='ihqhAcrt'),
  }(name='data', description='Response result.'),
  message?: string(name='message', description='Error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error information.', example='SysomOpenAPIAssumeRoleException: EntityNotExist.Role The role not exists: acs:ram::xxxxx:role/aliyunserviceroleforsysom'),
  requestId?: string(name='request_id', description='Request ID.', example='43A910E9-A739-525E-855D-A32C257F1826'),
}

model InvokeDiagnosisResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: InvokeDiagnosisResponseBody(name='body'),
}

/**
 * @summary Initiate Diagnosis.
 *
 * @description The following requirements must be met to diagnose a target ECS instance:
 * - The target ECS instance must be in the Running state.
 * - The Cloud Assistant must be installed on the target ECS instance. If it is not installed, refer to [Install the Cloud Assistant Agent](https://help.aliyun.com/zh/ecs/user-guide/install-the-cloud-assistant-agent) for installation.
 * - You must call the AuthDiagnosis API to authorize SysOM to diagnose the target ECS instance. If authorization is not granted, this API will fail directly.
 * - This API requires that the SysOM service-linked role (AliyunServiceRoleForSysom) has been created. This API does not automatically create the service role. If the service role does not exist, you must first call AuthDiagnosis for authorization, which will create the aforementioned service role.
 *
 * @param request InvokeDiagnosisRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return InvokeDiagnosisResponse
 */
@context("invokeDiagnosisWithContext")
async function invokeDiagnosisWithOptions(request: InvokeDiagnosisRequest, headers: map[string]string, runtime: $RuntimeOptions): InvokeDiagnosisResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.channel)) {
    body['channel'] = request.channel;
  }
  if (!$isNull(request.params)) {
    body['params'] = request.params;
  }
  if (!$isNull(request.serviceName)) {
    body['service_name'] = request.serviceName;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'InvokeDiagnosis',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/openapi/diagnosis/invoke_diagnosis`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Initiate Diagnosis.
 *
 * @description The following requirements must be met to diagnose a target ECS instance:
 * - The target ECS instance must be in the Running state.
 * - The Cloud Assistant must be installed on the target ECS instance. If it is not installed, refer to [Install the Cloud Assistant Agent](https://help.aliyun.com/zh/ecs/user-guide/install-the-cloud-assistant-agent) for installation.
 * - You must call the AuthDiagnosis API to authorize SysOM to diagnose the target ECS instance. If authorization is not granted, this API will fail directly.
 * - This API requires that the SysOM service-linked role (AliyunServiceRoleForSysom) has been created. This API does not automatically create the service role. If the service role does not exist, you must first call AuthDiagnosis for authorization, which will create the aforementioned service role.
 *
 * @param request InvokeDiagnosisRequest
 * @return InvokeDiagnosisResponse
 */
async function invokeDiagnosis(request: InvokeDiagnosisRequest): InvokeDiagnosisResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return invokeDiagnosisWithOptions(request, headers, runtime);
}

model ListAbnormalyEventsRequest {
  cluster?: string(name='cluster', description='The cluster ID.', example='2d33d4be-bf54-4070-82ca-c1dc2d8b1562'),
  current?: int32(name='current', description='The current page number. This parameter is present during paginated queries.', example='1'),
  end?: float(name='end', description='The end time.', example='1725801327754'),
  event?: string(name='event', description='The name of the anomaly event.', example='节点根文件系统使用检测'),
  instance?: string(name='instance', description='The instance ID.', example='i-wz9d00ut2ska3mlyhn6j'),
  level?: string(name='level', description='The level of the anomaly event.', example='potential'),
  namespace?: string(name='namespace', description='The namespace of the pod.', example='default'),
  pageSize?: int32(name='pageSize', description='The number of entries per page. Default value: 5. Valid values: 1 to 100.', example='10'),
  pod?: string(name='pod', description='The pod name.', example='test-pod'),
  showPod?: int32(name='showPod', description='Specifies whether to display pod anomaly events.', example='1'),
  start?: float(name='start', description='The start time.', example='1725797727754'),
}

model ListAbnormalyEventsResponseBody = {
  code?: string(name='code', description='The status code.
- `code == Success` indicates that the authorization was successful.
- Other status codes indicate that the authorization failed. Check the `message` field for the detailed fault information.', example='Success'),
  data?: [ 
    {
      createdAt?: long(name='created_at', description='The creation time.', example='1725801090000'),
      description?: string(name='description', description='The detailed description of the anomaly item.', example='节点发生OOM, 可查看OOM发生原因'),
      diagStatus?: int32(name='diag_status', description='The diagnostic status. Valid values:
- 1: diagnosis ready.
- 2: diagnosis in progress.
- 3: diagnosis completed.
- 4: not diagnosable.
- 5: diagnosis failed.', example='3'),
      endAt?: long(name='end_at', description='The end time of the anomaly event.', example='1725797727754'),
      instance?: string(name='instance', description='The instance ID.', example='i-wz9d00ut2ska3mlyhn6j'),
      item?: string(name='item', description='The name of the anomaly item.', example='节点CPU使用率检测'),
      level?: string(name='level', description='The level of the anomaly item.', example='potential'),
      namespace?: string(name='namespace', description='The namespace of the pod.', example='default'),
      opts?: [ 
        {
          label?: string(name='label', description='The operation type.', example='diagnose'),
          result?: {
            status?: string(name='status', description='The diagnostic status.', example='success'),
            url?: string(name='url', description='The URL of the diagnostic details.', example='/diagnose/result/PhfFg456'),
          }(name='result', description='The diagnostic result of the anomaly item.'),
          type?: string(name='type', description='The diagnostic type.', example='auto'),
        }
      ](name='opts', description='The list of operations for the anomaly item.'),
      pod?: string(name='pod', description='The pod name.', example='test-pod'),
      rawMetrics?: {
        endTime?: float(name='end_time', description='The end time.', example='1761814928'),
        metrics?: [ string ](name='metrics', description='The list of metric values.'),
        startTime?: float(name='start_time', description='The start time.', example='1761814928'),
      }(name='raw_metrics', description='The raw metrics.'),
      regionId?: string(name='region_id', description='The region ID.', example='cn-shanghai'),
      type?: string(name='type', description='The type of the anomaly item.', example='saturation'),
      uuid?: string(name='uuid', description='The UUID of the anomaly event.', example='43f05b46-1034-42e8-a528-6e5ca1108277'),
    }
  ](name='data', description='The returned result.'),
  message?: string(name='message', description='The error code description. This value is empty if no error occurred.', example='Success'),
  total?: int32(name='total', description='The total number of records.', example='4'),
}

model ListAbnormalyEventsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListAbnormalyEventsResponseBody(name='body'),
}

/**
 * @summary Retrieves anomaly event information for a cluster, node, or pod within a specified time range.
 *
 * @param request ListAbnormalyEventsRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListAbnormalyEventsResponse
 */
@context("listAbnormalyEventsWithContext")
async function listAbnormalyEventsWithOptions(request: ListAbnormalyEventsRequest, headers: map[string]string, runtime: $RuntimeOptions): ListAbnormalyEventsResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.cluster)) {
    query['cluster'] = request.cluster;
  }
  if (!$isNull(request.current)) {
    query['current'] = request.current;
  }
  if (!$isNull(request.end)) {
    query['end'] = request.end;
  }
  if (!$isNull(request.event)) {
    query['event'] = request.event;
  }
  if (!$isNull(request.instance)) {
    query['instance'] = request.instance;
  }
  if (!$isNull(request.level)) {
    query['level'] = request.level;
  }
  if (!$isNull(request.namespace)) {
    query['namespace'] = request.namespace;
  }
  if (!$isNull(request.pageSize)) {
    query['pageSize'] = request.pageSize;
  }
  if (!$isNull(request.pod)) {
    query['pod'] = request.pod;
  }
  if (!$isNull(request.showPod)) {
    query['showPod'] = request.showPod;
  }
  if (!$isNull(request.start)) {
    query['start'] = request.start;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListAbnormalyEvents',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/openapi/cluster_health/range/abnormaly_events`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves anomaly event information for a cluster, node, or pod within a specified time range.
 *
 * @param request ListAbnormalyEventsRequest
 * @return ListAbnormalyEventsResponse
 */
async function listAbnormalyEvents(request: ListAbnormalyEventsRequest): ListAbnormalyEventsResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listAbnormalyEventsWithOptions(request, headers, runtime);
}

model ListAgentInstallRecordsRequest {
  current?: long(name='current', description='The current page number. Pages start from page 1.', example='1'),
  instanceId?: string(name='instance_id', description='The ID of the instance. If you specify this parameter, only the Agent installation records for the specified instance are returned.', example='i-bp118piqcio9tiwgh84b'),
  pageSize?: long(name='pageSize', description='The number of entries per page.', example='20'),
  pluginId?: string(name='plugin_id', description='The ID of the Agent. If you specify this parameter, only the installation records for the specified Agent are returned. You can use this parameter together with the plugin_version parameter.', example='74a86327-3170-412c-8e67-da3389ec56a9'),
  pluginVersion?: string(name='plugin_version', description='The version of the Agent. This parameter cannot be used alone. Use this parameter together with the plugin_id parameter to filter installation records for a specific version of the specified Agent.', example='3.4.0-1'),
  region?: string(name='region', description='The region ID.', example='cn-hangzhou'),
  status?: string(name='status', description='Filters component installation records by status.', example='Installed'),
}

model ListAgentInstallRecordsResponseBody = {
  requestId?: string(name='RequestId', description='The request ID, which is active for end-to-end diagnostics.', example='E8CDFBA1-0564-5897-B070-D3C85002AF2F'),
  code?: string(name='code', description='The status code.
- If `code == Success`, the authorization is successful.
- Other status codes indicate that the authorization has failed. Check the `message` field for the detailed fault information.', example='Success'),
  data?: [ 
    {
      createdAt?: string(name='created_at', description='The creation time.', example='2024-11-27T16:37:53'),
      instanceId?: string(name='instance_id', description='The instance ID.', example='i-bp118piqcio9tiwgh84b'),
      pluginId?: string(name='plugin_id', description='The component ID.', example='74a86327-3170-412c-8e67-da3389ec56a9'),
      pluginVersion?: string(name='plugin_version', description='The plug-in version.', example='3.4.0-1'),
      status?: string(name='status', description='The component status.', example='Installed'),
      updatedAt?: string(name='updated_at', description='The update time.', example='2024-11-27T16:37:53'),
    }
  ](name='data', description='The returned results.'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the error message for the request.', example='SysomOpenAPIAssumeRoleException: EntityNotExist.Role The role not exists: acs:ram::xxxxx:role/aliyunserviceroleforsysom'),
  total?: long(name='total', description='The total number of records.', example='64'),
}

model ListAgentInstallRecordsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListAgentInstallRecordsResponseBody(name='body'),
}

/**
 * @summary Lists the installation records of an Agent.
 *
 * @param request ListAgentInstallRecordsRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListAgentInstallRecordsResponse
 */
@context("listAgentInstallRecordsWithContext")
async function listAgentInstallRecordsWithOptions(request: ListAgentInstallRecordsRequest, headers: map[string]string, runtime: $RuntimeOptions): ListAgentInstallRecordsResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.current)) {
    query['current'] = request.current;
  }
  if (!$isNull(request.instanceId)) {
    query['instance_id'] = request.instanceId;
  }
  if (!$isNull(request.pageSize)) {
    query['pageSize'] = request.pageSize;
  }
  if (!$isNull(request.pluginId)) {
    query['plugin_id'] = request.pluginId;
  }
  if (!$isNull(request.pluginVersion)) {
    query['plugin_version'] = request.pluginVersion;
  }
  if (!$isNull(request.region)) {
    query['region'] = request.region;
  }
  if (!$isNull(request.status)) {
    query['status'] = request.status;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListAgentInstallRecords',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/am/agent/list_agent_install_list`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Lists the installation records of an Agent.
 *
 * @param request ListAgentInstallRecordsRequest
 * @return ListAgentInstallRecordsResponse
 */
async function listAgentInstallRecords(request: ListAgentInstallRecordsRequest): ListAgentInstallRecordsResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listAgentInstallRecordsWithOptions(request, headers, runtime);
}

model ListAgentsRequest {
  current?: long(name='current', description='The current page number. Pages start from page 1.', example='1'),
  name?: string(name='name', description='Filters plug-ins by plug-in name.', example='SysOM'),
  pageSize?: long(name='pageSize', description='The number of entries per page.', example='10'),
  type?: string(name='type', description='The agent type used to filter the list. For example, set this parameter to control to retrieve all agents of the control type.', example='control'),
}

model ListAgentsResponseBody = {
  requestId?: string(name='RequestId', description='The request ID, which can be used for end-to-end diagnostics.', example='66EAED72-542B-583B-BCED-64433DC27AD7'),
  code?: string(name='code', description='The status code.
- If code is Success, the authorization is successful.
- Other status codes indicate authorization failed. Check the message field for the detailed fault information.', example='Success'),
  data?: [ 
    {
      createdAt?: string(name='created_at', description='The time when the component was created.', example='2024-09-14T20:46:08'),
      description?: string(name='description', description='The component description.', example='SysOM Agent'),
      id?: string(name='id', description='The component ID.', example='74a86327-3170-412c-8e67-da3389ec56a9'),
      name?: string(name='name', description='The component name.', example='SysOM Agent'),
      supportArch?: string(name='support_arch', description='The supported architectures. Multiple architectures are separated by commas.', example='x86'),
      type?: string(name='type', description='The type of the component. Valid values:
- Control: control-type component.
- AI: AI component.', example='Control'),
      updatedAt?: string(name='updated_at', description='The time when the component was last updated.', example='2024-09-14T20:46:08'),
      versions?: [ 
        {
          createdAt?: string(name='created_at', description='The time when the component version was created.', example='2024-09-14T20:46:08'),
          installScript?: string(name='install_script', description='The installation script of the component.', example='sysom.sh install'),
          uninstallScript?: string(name='uninstall_script', description='The uninstallation script of the component.', example='sysom.sh uninstall'),
          updatedAt?: string(name='updated_at', description='The time when the component version was last updated.', example='2024-09-14T20:46:08'),
          upgradeScript?: string(name='upgrade_script', description='The update script of the component.', example='sysom.sh upgrade'),
          version?: string(name='version', description='The component version number.', example='3.4.0-1'),
        }
      ](name='versions', description='The list of component versions.'),
    }
  ](name='data', description='The returned data.'),
  message?: string(name='message', description='The error message.
- If code is Success, this field is empty.
- Otherwise, this field contains the request error message.', example='SysomOpenAPIAssumeRoleException: EntityNotExist.Role The role not exists: acs:ram::xxxxx:role/aliyunserviceroleforsysom'),
  total?: long(name='total', description='The total number of records.', example='2'),
}

model ListAgentsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListAgentsResponseBody(name='body'),
}

/**
 * @summary Retrieves a list of agents.
 *
 * @param request ListAgentsRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListAgentsResponse
 */
@context("listAgentsWithContext")
async function listAgentsWithOptions(request: ListAgentsRequest, headers: map[string]string, runtime: $RuntimeOptions): ListAgentsResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.current)) {
    query['current'] = request.current;
  }
  if (!$isNull(request.name)) {
    query['name'] = request.name;
  }
  if (!$isNull(request.pageSize)) {
    query['pageSize'] = request.pageSize;
  }
  if (!$isNull(request.type)) {
    query['type'] = request.type;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListAgents',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/am/agent/list_agents`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves a list of agents.
 *
 * @param request ListAgentsRequest
 * @return ListAgentsResponse
 */
async function listAgents(request: ListAgentsRequest): ListAgentsResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listAgentsWithOptions(request, headers, runtime);
}

model ListAlertDestinationsRequest {
  current?: int32(name='current', description='Current page number (starting from 1)', example='1'),
  maxResults?: int32(name='maxResults', description='Maximum number of records to retrieve at a time.', example='10'),
  name?: string(name='name', description='Alert contact name', example='name1'),
  nextToken?: string(name='nextToken', description='Pagination token for the next request.', example='c2f78a783f49457caba6bace6f6f79e4'),
  pageSize?: int32(name='pageSize', description='Page size', example='10'),
}

model ListAlertDestinationsResponseBody = {
  code?: string(name='code', description='Status code
- `code == Success` indicates that the authorization was successful;
- Other status codes indicate authorization failure. When authorization fails, check the `message` field for detailed error information;', example='Success'),
  data?: any(name='data', description='Response data.', example='{}'),
  maxResults?: int32(name='maxResults', description='Maximum number of records to retrieve at a time.', example='10'),
  message?: string(name='message', description='Error message
- If `code == Success`, this field is empty;
- Otherwise, this field contains the request error information.', example='server error'),
  nextToken?: string(name='nextToken', description='Pagination token for the next request.', example='c2f78a783f49457caba6bace6f6f79e4'),
  requestId?: string(name='requestId', description='Request ID.', example='2D693121-C925-5154-8DF6-C09A8B369822'),
  total?: int32(name='total', description='Maximum number of records returned by this request', example='623'),
}

model ListAlertDestinationsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListAlertDestinationsResponseBody(name='body'),
}

/**
 * @summary This API is used to get the list of alert contacts
 *
 * @param request ListAlertDestinationsRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListAlertDestinationsResponse
 */
@context("listAlertDestinationsWithContext")
async function listAlertDestinationsWithOptions(request: ListAlertDestinationsRequest, headers: map[string]string, runtime: $RuntimeOptions): ListAlertDestinationsResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.current)) {
    query['current'] = request.current;
  }
  if (!$isNull(request.maxResults)) {
    query['maxResults'] = request.maxResults;
  }
  if (!$isNull(request.name)) {
    query['name'] = request.name;
  }
  if (!$isNull(request.nextToken)) {
    query['nextToken'] = request.nextToken;
  }
  if (!$isNull(request.pageSize)) {
    query['pageSize'] = request.pageSize;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListAlertDestinations',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/alertPusher/alert/listDestinations`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary This API is used to get the list of alert contacts
 *
 * @param request ListAlertDestinationsRequest
 * @return ListAlertDestinationsResponse
 */
async function listAlertDestinations(request: ListAlertDestinationsRequest): ListAlertDestinationsResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listAlertDestinationsWithOptions(request, headers, runtime);
}

model ListAlertItemsResponseBody = {
  code?: string(name='code', description='The status code.
- `code == Success` indicates that the authorization is successful.
- Other status codes indicate that the authorization failed. Check the `message` field for the detailed fault message.', example='Success'),
  data?: any(name='data', description='The response data.', example='{
    "NODE": {
      "饱和度": [
        "节点CPU使用率检测",
        "节点内核态CPU使用检测",
        "节点软中断CPU使用检测",
        "节点内存使用检测",
        "节点内核内存使用检测",
        "节点文件描述符使用检测",
        "节点根文件系统使用检测",
        "节点cgroup泄漏检测",
        "节点Sockets使用检测",
        "节点TCP内存使用检测"
      ],
      "延时": [
        "节点调度延时检测",
        "节点网络延时检测",
        "节点磁盘写入延迟检测",
        "节点磁盘读取延迟检测"
      ],
      "负载": [
        "节点磁盘IO流量检测",
        "节点load average检测"
      ],
      "错误": [
        "节点网络丢包检测",
        "节点OOM夯机预测及检测"
      ]
    },
    "POD": {
      "饱和度": [
        "Pod内存使用检测",
        "Pod CPU使用率检测"
      ],
      "错误": [
        "Pod CPU限流检测",
        "Pod OOM事件检测"
      ]
    }
}'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error information.', example='success'),
  requestId?: string(name='requestId', description='Id of the request', example='2D693121-C925-5154-8DF6-C09A8B369822'),
}

model ListAlertItemsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListAlertItemsResponseBody(name='body'),
}

/**
 * @summary Retrieves all alert metrics.
 *
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListAlertItemsResponse
 */
@context("listAlertItemsWithContext")
async function listAlertItemsWithOptions(headers: map[string]string, runtime: $RuntimeOptions): ListAlertItemsResponse {
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
  };
  var params = new OpenApiUtil.Params{
    action = 'ListAlertItems',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/alertPusher/alert/listItems`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves all alert metrics.
 *
 * @return ListAlertItemsResponse
 */
async function listAlertItems(): ListAlertItemsResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listAlertItemsWithOptions(headers, runtime);
}

model ListAlertStrategiesRequest {
  current?: int32(name='current', description='The current page number (starting from 1).', example='1'),
  maxResults?: int32(name='maxResults', description='The maximum number of records to retrieve in a single request.', example='20'),
  name?: string(name='name', description='The policy name of the alerting policy.', example='strategy1'),
  nextToken?: string(name='nextToken', description='The pagination token for the next request.', example='c2f78a783f49457caba6bace6f6f79e4'),
  pageSize?: int32(name='pageSize', description='The number of entries per page.', example='10'),
}

model ListAlertStrategiesResponseBody = {
  code?: string(name='code', description='The status code.
- If `code == Success`, the authorization is successful.
- Other status codes indicate that the authorization has failed. Check the `message` field for the detailed fault information.', example='Success'),
  data?: [ 
    {
      createdAt?: string(name='createdAt', description='The creation time.', example='1753669116286'),
      enabled?: boolean(name='enabled', description='Indicates whether the alert policy is enabled.', example='false'),
      id?: long(name='id', description='The policy ID.', example='1'),
      k8sLabel?: boolean(name='k8sLabel'),
      name?: string(name='name', description='The policy name.', example='strategy1'),
      strategy?: {
        clusters?: [ string ](name='clusters', description='The collection of clusters for which alerts are received.'),
        destinations?: [ int32 ](name='destinations'),
        items?: [ string ](name='items', description='接收告警的异常项列表'),
      }(name='strategy', description='The alert policy details.'),
      uid?: string(name='uid', description='The user ID.', example='1880327028143673'),
      updatedAt?: long(name='updatedAt', description='The update time.', example='1753237017710'),
    }
  ](name='data', description='The returned result.'),
  maxResults?: int32(name='maxResults', description='The maximum number of entries returned.', example='20'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the error information for the request.', example='success'),
  nextToken?: string(name='nextToken', description='The pagination token for the next request.', example='c2f78a783f49457caba6bace6f6f79e4'),
  requestId?: string(name='requestId', description='Id of the request', example='2D693121-C925-5154-8DF6-C09A8B369822'),
  total?: long(name='total', description='The total number of records.', example='92'),
}

model ListAlertStrategiesResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListAlertStrategiesResponseBody(name='body'),
}

/**
 * @summary Retrieves all push alert policies for the current user.
 *
 * @param request ListAlertStrategiesRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListAlertStrategiesResponse
 */
@context("listAlertStrategiesWithContext")
async function listAlertStrategiesWithOptions(request: ListAlertStrategiesRequest, headers: map[string]string, runtime: $RuntimeOptions): ListAlertStrategiesResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.current)) {
    query['current'] = request.current;
  }
  if (!$isNull(request.maxResults)) {
    query['maxResults'] = request.maxResults;
  }
  if (!$isNull(request.name)) {
    query['name'] = request.name;
  }
  if (!$isNull(request.nextToken)) {
    query['nextToken'] = request.nextToken;
  }
  if (!$isNull(request.pageSize)) {
    query['pageSize'] = request.pageSize;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListAlertStrategies',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/alertPusher/alert/listStrategies`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves all push alert policies for the current user.
 *
 * @param request ListAlertStrategiesRequest
 * @return ListAlertStrategiesResponse
 */
async function listAlertStrategies(request: ListAlertStrategiesRequest): ListAlertStrategiesResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listAlertStrategiesWithOptions(request, headers, runtime);
}

model ListAllInstancesRequest {
  current?: string(name='current', description='Current page number.', example='1'),
  filters?: string(name='filters', description='List of filter information.', example='{}'),
  instanceType?: string(name='instanceType', description='Instance type.', example='ecs'),
  managedType?: string(name='managedType', description='Management type.', example='managed'),
  maxResults?: int32(name='maxResults', description='Maximum number of records to retrieve at a time.', example='100'),
  nextToken?: string(name='nextToken', description='Pagination token.
> If this parameter is not empty, more data is available.', example='U+w1wv2R4ZWR5oZLXD0+Dp4dD+2BRJj42DLT6GrZysw='),
  pageSize?: string(name='pageSize', description='Number of entries returned per page. Default value: pageSize=10.', example='10'),
  pluginId?: string(name='pluginId', description='Plugin ID.', example='01fc4a0b-f199-4885-9861-b4054a310fe7'),
  region?: string(name='region', description='Region ID.', example='cn-hangzhou'),
}

model ListAllInstancesResponseBody = {
  code?: string(name='code', description='Status code.', example='Success'),
  data?: [ 
    {
      agentConfigId?: string(name='agentConfigId', description='Agent configuration ID.', example='test_id'),
      agentConfigName?: string(name='agentConfigName', description='Agent configuration name.', example='test_name'),
      attributes?: [ 
        {
          infoKey?: string(name='infoKey', description='Information key.', example='sysom'),
          infoType?: string(name='infoType', description='Information type.', example='instance_tag'),
          infoValue?: string(name='infoValue', description='Information value.', example='diagnosis'),
        }
      ](name='attributes', description='Extended information.'),
      clusterId?: string(name='clusterId', description='Cluster ID.', example='3b24a621-acb3-11ef-8c90-00163e1029af'),
      clusterName?: string(name='clusterName', description='Cluster name.', example='zjk_vpc_domain_1'),
      imageId?: string(name='imageId', description='Image ID.', example='aliyun_3_x64_20G_alibase_20250117.vhd'),
      installLevel?: string(name='installLevel', description='Installation level.', example='Cluster'),
      installType?: string(name='installType', description='Installation type.', example='console'),
      instanceId?: string(name='instanceId', description='Instance ID.', example='i-bp17uabeke9v7n30abm2'),
      instanceName?: string(name='instanceName', description='Instance name.', example='test'),
      instanceType?: string(name='instanceType', description='Instance type.', example='ecs'),
      kernelVersion?: string(name='kernelVersion', description='Kernel version.', example='5.10.134-18.al8.x86_64'),
      manageLevel?: string(name='manageLevel', description='Management level.', example='cluster'),
      manageType?: string(name='manageType', description='Management type.', example='managed'),
      osArch?: string(name='osArch', description='Operating system architecture.', example='x86_64'),
      osHealthScore?: int32(name='osHealthScore', description='Operating system health score.', example='100'),
      osName?: string(name='osName', description='Operating system name.', example='alios'),
      privateIp?: string(name='privateIp', description='Private IP address.', example='172.21.172.7'),
      publicIp?: string(name='publicIp', description='Public IP address.', example='47.98.215.58'),
      resourceGroupId?: string(name='resourceGroupId', description='Resource group ID.', example='3b24a621-acb3-11ef-8c90-00163e1029af'),
      resourceGroupName?: string(name='resourceGroupName', description='Resource group name.', example='xxxx'),
      status?: string(name='status', description='Running status of the instance. Valid values:
- **Running**: The instance is running.
- **Offline**: The instance is offline.



> An instance in the Offline status indicates that the heartbeat from the node to the SysOM server is lost. It does not necessarily mean that the corresponding ECS instance is not running.', example='Running'),
    }
  ](name='data', description='Returned data.'),
  maxResults?: int32(name='maxResults', description='Maximum number of results returned.', example='20'),
  message?: string(name='message', description='Error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error message.', example='instance not exists'),
  nextToken?: string(name='nextToken', description='Pagination token.

> A non-empty value indicates that more data is available.', example='c2f78a783f49457caba6bace6f6f79e4'),
  requestId?: string(name='requestId', description='Id of the request', example='2D693121-C925-5154-8DF6-C09A8B369822'),
  total?: long(name='total', description='Total number of query results.', example='2'),
}

model ListAllInstancesResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListAllInstancesResponseBody(name='body'),
}

/**
 * @summary This API is used to retrieve a list of managed/unmanaged instances along with their instance information.
 *
 * @param request ListAllInstancesRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListAllInstancesResponse
 */
@context("listAllInstancesWithContext")
async function listAllInstancesWithOptions(request: ListAllInstancesRequest, headers: map[string]string, runtime: $RuntimeOptions): ListAllInstancesResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.current)) {
    query['current'] = request.current;
  }
  if (!$isNull(request.filters)) {
    query['filters'] = request.filters;
  }
  if (!$isNull(request.instanceType)) {
    query['instanceType'] = request.instanceType;
  }
  if (!$isNull(request.managedType)) {
    query['managedType'] = request.managedType;
  }
  if (!$isNull(request.maxResults)) {
    query['maxResults'] = request.maxResults;
  }
  if (!$isNull(request.nextToken)) {
    query['nextToken'] = request.nextToken;
  }
  if (!$isNull(request.pageSize)) {
    query['pageSize'] = request.pageSize;
  }
  if (!$isNull(request.pluginId)) {
    query['pluginId'] = request.pluginId;
  }
  if (!$isNull(request.region)) {
    query['region'] = request.region;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListAllInstances',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/am/instance/listAllInstances`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary This API is used to retrieve a list of managed/unmanaged instances along with their instance information.
 *
 * @param request ListAllInstancesRequest
 * @return ListAllInstancesResponse
 */
async function listAllInstances(request: ListAllInstancesRequest): ListAllInstancesResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listAllInstancesWithOptions(request, headers, runtime);
}

model ListClusterAgentInstallRecordsRequest {
  agentConfigId?: string(name='agent_config_id'),
  clusterId?: string(name='cluster_id', description='Filter by cluster ID.

> This cluster ID is not the ACK cluster ID, but the `cluster_id` field in the data returned by this API, or the `id` field in the data returned by the ListCluster API.', example='cbd80af02b9d6454ebdc579c5e022d0c8'),
  current?: long(name='current', description='Current page number (starting from 1)', example='1'),
  pageSize?: long(name='pageSize', description='Page size', example='10'),
  pluginId?: string(name='plugin_id', description='Specify this parameter to filter the installation list for a specific agent. Can be used in combination with the plugin_version parameter.', example='74a86327-3170-412c-8e67-da3389ec56a9'),
  pluginVersion?: string(name='plugin_version', description='Cannot be used alone. Use in combination with plugin_id to filter the installation list for a specific agent version.', example='3.4.0-1'),
}

model ListClusterAgentInstallRecordsResponseBody = {
  requestId?: string(name='RequestId', description='Request ID, which can be used for end-to-end diagnostics.', example='B149FD9C-ED5C-5765-B3AD-05AA4A4D64D7'),
  code?: string(name='code', description='Status code.
- `code == Success` indicates that the authorization is successful.
- Other status codes indicate authorization failure. When authorization fails, check the `message` field for detailed error information.', example='Success'),
  data?: [ 
    {
      agentConfigId?: string(name='agent_config_id'),
      agentConfigName?: string(name='agent_config_name'),
      clusterId?: string(name='cluster_id', description='Cluster ID.

> This cluster ID is not the ACK cluster ID.', example='cbf7a37bc905d4682a3338b3744810269'),
      createdAt?: string(name='created_at', description='Creation time.', example='2024-12-25T15:08:19'),
      grayscaleConfig?: string(name='grayscale_config', description='Canary release configuration.', example='{}'),
      pluginId?: string(name='plugin_id', description='Component ID.', example='74a86327-3170-412c-8e67-da3389ec56a9'),
      pluginVersion?: string(name='plugin_version', description='Plugin version.', example='3.4.0-1'),
      updatedAt?: string(name='updated_at', description='Modification time.', example='2024-12-25T15:08:19'),
    }
  ](name='data', description='Response results.'),
  message?: string(name='message', description='Error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error message.', example='success'),
  total?: long(name='total', description='Total number of query results.', example='42'),
}

model ListClusterAgentInstallRecordsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListClusterAgentInstallRecordsResponseBody(name='body'),
}

/**
 * @summary Get cluster component installation records
 *
 * @param request ListClusterAgentInstallRecordsRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListClusterAgentInstallRecordsResponse
 */
@context("listClusterAgentInstallRecordsWithContext")
async function listClusterAgentInstallRecordsWithOptions(request: ListClusterAgentInstallRecordsRequest, headers: map[string]string, runtime: $RuntimeOptions): ListClusterAgentInstallRecordsResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.agentConfigId)) {
    query['agent_config_id'] = request.agentConfigId;
  }
  if (!$isNull(request.clusterId)) {
    query['cluster_id'] = request.clusterId;
  }
  if (!$isNull(request.current)) {
    query['current'] = request.current;
  }
  if (!$isNull(request.pageSize)) {
    query['pageSize'] = request.pageSize;
  }
  if (!$isNull(request.pluginId)) {
    query['plugin_id'] = request.pluginId;
  }
  if (!$isNull(request.pluginVersion)) {
    query['plugin_version'] = request.pluginVersion;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListClusterAgentInstallRecords',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/am/agent/list_cluster_agent_install_list`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Get cluster component installation records
 *
 * @param request ListClusterAgentInstallRecordsRequest
 * @return ListClusterAgentInstallRecordsResponse
 */
async function listClusterAgentInstallRecords(request: ListClusterAgentInstallRecordsRequest): ListClusterAgentInstallRecordsResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listClusterAgentInstallRecordsWithOptions(request, headers, runtime);
}

model ListClustersRequest {
  clusterId?: string(name='cluster_id', description='Filter by cluster ID.

> This cluster ID is not the ACK cluster ID, but the `id` field in the data returned by this API.', example='cb7d4cc26c8f845fb8a8255ffd394820e'),
  clusterStatus?: string(name='cluster_status', description='- `Running`: The cluster is managed normally.
- `Installing`: The cluster has an installation task in progress.
- `Uninstalling`: The cluster has an uninstallation task in progress.
- `Upgrading`: The cluster has an upgrade task in progress.
- `Offline`: The cluster is offline and management is abnormal.', example='Running'),
  clusterType?: string(name='cluster_type', description='- `ACK`: ACK cluster.
- `CUSTOM`: Custom cluster (the default cluster belongs to custom clusters).', example='ACK'),
  current?: long(name='current', description='Current page number (starting from page 1)', example='1'),
  id?: string(name='id', description='This field is deprecated. Use the cluster_id field to filter instead.', example='cb7d4cc26c8f845fb8a8255ffd394820e'),
  name?: string(name='name', description='Filter by plugin name', example='proxy-next-upstream'),
  pageSize?: long(name='pageSize', description='Page size', example='10'),
}

model ListClustersResponseBody = {
  requestId?: string(name='RequestId', description='Request ID, which can be used for end-to-end diagnostics.', example='B149FD9C-ED5C-5765-B3AD-05AA4A4D64D7'),
  code?: string(name='code', description='Status code.
- If `code == Success`, the authorization is successful.
- Other status codes indicate authorization failure. Check the `message` field for detailed error information.', example='Success'),
  data?: [ 
    {
      clusterId?: string(name='cluster_id', description='Actual cluster ID.

> - For `ACK` type clusters, this cluster ID is the ACK cluster ID.
> - For `CUSTOM` type clusters, this cluster ID serves as a unique identifier with no additional meaning.', example='c666d4774f0e2440b979bf917bf100e40'),
      clusterStatus?: string(name='cluster_status', description='- `Running`: The cluster is managed normally.
- `Installing`: The cluster has an installation task in progress.
- `Uninstalling`: The cluster has an uninstallation task in progress.
- `Upgrading`: The cluster has an upgrade task in progress.
- `Offline`: The cluster is offline and management is abnormal.', example='Running'),
      clusterType?: string(name='cluster_type', description='- `ACK`: ACK cluster.
- `CUSTOM`: Custom cluster (the default cluster belongs to custom clusters).', example='ACK'),
      createdAt?: string(name='created_at', description='Creation time.', example='2024-12-25T15:08:19'),
      id?: string(name='id', description='Cluster ID.', example='5389fba5-92a1-4ff4-9b26-773b97828144'),
      name?: string(name='name', description='Cluster name.', example='auto-name-sbvCT'),
      region?: string(name='region', description='Region ID.', example='cn-hangzhou'),
      updatedAt?: string(name='updated_at', description='Update time.', example='2024-12-25T15:08:19'),
    }
  ](name='data', description='Response data.'),
  message?: string(name='message', description='Error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error information.', example='success'),
  total?: long(name='total', description='Total number of records.', example='64'),
}

model ListClustersResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListClustersResponseBody(name='body'),
}

/**
 * @summary Retrieve all managed clusters of the current user
 *
 * @param request ListClustersRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListClustersResponse
 */
@context("listClustersWithContext")
async function listClustersWithOptions(request: ListClustersRequest, headers: map[string]string, runtime: $RuntimeOptions): ListClustersResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.clusterId)) {
    query['cluster_id'] = request.clusterId;
  }
  if (!$isNull(request.clusterStatus)) {
    query['cluster_status'] = request.clusterStatus;
  }
  if (!$isNull(request.clusterType)) {
    query['cluster_type'] = request.clusterType;
  }
  if (!$isNull(request.current)) {
    query['current'] = request.current;
  }
  if (!$isNull(request.id)) {
    query['id'] = request.id;
  }
  if (!$isNull(request.name)) {
    query['name'] = request.name;
  }
  if (!$isNull(request.pageSize)) {
    query['pageSize'] = request.pageSize;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListClusters',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/am/cluster/list_clusters`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieve all managed clusters of the current user
 *
 * @param request ListClustersRequest
 * @return ListClustersResponse
 */
async function listClusters(request: ListClustersRequest): ListClustersResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listClustersWithOptions(request, headers, runtime);
}

model ListDiagnosisRequest {
  current?: long(name='current', description='Current page number', example='1'),
  pageSize?: long(name='pageSize', description='Page size', example='10'),
  params?: string(name='params', description='Diagnostic parameters. Different types of diagnostics require different diagnostic parameters. You can use this field to filter records whose parameters match the specified values.', example='[{\\\\"key\\\\":\\\\"region\\\\",\\\\"value\\\\":\\\\"cn-beijing\\\\"}]'),
  serviceName?: string(name='service_name', description='Diagnostic type', example='memgraph'),
  status?: string(name='status', description='Execution status of the diagnostic task.
Valid values:
- **Ready**: Ready
- **Running**: Running
- **Success**: Execution succeeded
- **Fail**: Execution failed', example='Running'),
}

model ListDiagnosisResponseBody = {
  requestId?: string(name='RequestId', description='Request ID, which can be used for full-link diagnostics', example='44841312-7227-55C9-AE03-D59729BFAE38'),
  code?: string(name='code', description='Status code
- If `code == Success`, the authorization is successful.
- Other status codes indicate authorization failure. When authorization fails, check the `message` field for detailed error information.', example='Success'),
  data?: [ 
    {
      code?: int32(name='code', description='Diagnostic error code. 0 indicates no error.', example='0'),
      command?: any(name='command', description='Diagnostic command', example='{
    "jobs":[
        {
            "cmd":"mkdir -p /var/log/sysak && sysak podmem -r 100  -a -j /var/log/sysak/podmem.json > /dev/null 2>&1 && cat /var/log/sysak/podmem.json",
            "instance":"172.20.12.174",
            "fetch_file_list":[

            ]
        }
    ],
    "in_order":true,
    "offline_mode":false,
    "offline_results":[

    ]
}'),
      createdAt?: string(name='created_at', description='Creation time', example='2024-12-25T15:08:19'),
      errMsg?: string(name='err_msg', description='Error message', example='Diagnosis failed'),
      params?: any(name='params', description='Diagnostic parameters', example='{
    "type":"all",
    "value":"",
    "channel":"ssh",
    "instance":"172.1.2.174",
    "service_name":"filecache"
}'),
      result?: any(name='result', description='Diagnostic result', example='{
    "summary":"  memory cgroup leak",
    "dataMemEvent":{
        "data":[
            {
                "key":"Util",
                "value":20
            },
            {
                "key":"MemLeak",
                "value":"OK"
            },
            {
                "key":"MemcgLeak",
                "value":"NG"
            },
            {
                "key":"MemFrag",
                "value":"OK"
            }
        ]
    },
    "dataMemOverView":{
        "data":[
            {
                "key":"app",
                "value":10937332
            },
            {
                "key":"free",
                "value":806800
            },
            {
                "key":"kernel",
                "value":4527660
            }
        ]
    },
    "dataKerMem":{
        "data":[
            {
                "key":"SReclaimable",
                "value":3411292
            },
            {
                "key":"VmallocUsed",
                "value":30980
            },
            {
                "key":"allocPage",
                "value":177732
            },
            {
                "key":"KernelStack",
                "value":9280
            },
            {
                "key":"PageTables",
                "value":38056
            },
            {
                "key":"SUnreclaim",
                "value":170248
            },
            {
                "key":"reserved",
                "value":690072
            }
        ]
    },
    "dataUserMem":{
        "data":[
            {
                "key":"filecache",
                "value":8010008
            },
            {
                "key":"anon",
                "value":2468608
            },
            {
                "key":"mlock",
                "value":0
            },
            {
                "key":"huge1G",
                "value":0
            },
            {
                "key":"huge2M",
                "value":0
            },
            {
                "key":"buffers",
                "value":458608
            },
            {
                "key":"shmem",
                "value":2284
            }
        ]
    },
    "dataCacheList":{
        "data":[
            {
                "key":0,
                "Name":"/var/lib/mysql/sysom/sys_handler_log.ibd",
                "cached":576764,
                "Task":"mysqld_78575 "
            },
            {
                "key":1,
                "Name":"/var/log/sysom/sysom-migration-access.log",
                "cached":276688,
                "Task":"gunicorn_33647 ,gunicorn_460836 ,gunicorn_559934 ,gunicorn_731758 ,gunicorn_2362682 "
            },
            {
                "key":2,
                "Name":"/var/log/sysom/sysom-rtdemo-access.log",
                "cached":229404,
                "Task":"gunicorn_60718 ,gunicorn_720734 ,gunicorn_722168 "
            },
            {
                "key":3,
                "Name":"/var/log/sysom/sysom-monitor-server-access.log",
                "cached":197368,
                "Task":"gunicorn_33682 ,gunicorn_671155 ,gunicorn_714998 "
            },
            {
                "key":4,
                "Name":"/var/log/sysom/sysom-channel-access.log",
                "cached":180276,
                "Task":"gunicorn_33233 ,gunicorn_499735 ,gunicorn_725497 "
            },
            {
                "key":5,
                "Name":"total cached of close file",
                "cached":3729668,
                "Task":""
            }
        ]
    },
    "dataProcMemList":{
        "data":[
            {
                "key":0,
                "task":"mysqld",
                "MemTotal":240856,
                "RssAnon":218248,
                "RssFile":22608
            },
            {
                "key":1,
                "task":"systemd-journal",
                "MemTotal":150248,
                "RssAnon":74300,
                "RssFile":75944
            },
            {
                "key":2,
                "task":"gunicorn",
                "MemTotal":144640,
                "RssAnon":114200,
                "RssFile":30440
            },
            {
                "key":3,
                "task":"gunicorn",
                "MemTotal":141480,
                "RssAnon":111040,
                "RssFile":30440
            },
            {
                "key":4,
                "task":"grafana-server",
                "MemTotal":103660,
                "RssAnon":42732,
                "RssFile":60928
            },
            {
                "key":5,
                "task":"gunicorn",
                "MemTotal":97444,
                "RssAnon":76256,
                "RssFile":21188
            },
            {
                "key":6,
                "task":"gunicorn",
                "MemTotal":97260,
                "RssAnon":76072,
                "RssFile":21188
            },
            {
                "key":7,
                "task":"prometheus",
                "MemTotal":95356,
                "RssAnon":45376,
                "RssFile":49980
            },
            {
                "key":8,
                "task":"gunicorn",
                "MemTotal":90144,
                "RssAnon":76456,
                "RssFile":13688
            },
            {
                "key":9,
                "task":"gunicorn",
                "MemTotal":89796,
                "RssAnon":76108,
                "RssFile":13688
            }
        ]
    }
}'),
      serviceName?: string(name='service_name', description='Diagnostic type', example='memgraph'),
      status?: string(name='status', description='Execution status of the diagnostic task.
Valid values:
- **Ready**: Ready
- **Running**: Running
- **Success**: Execution succeeded
- **Fail**: Execution failed', example='Running'),
      taskId?: string(name='task_id', description='Task ID.', example='grcuU21a'),
      updatedAt?: string(name='updated_at', description='Update time', example='2024-12-25T15:08:19'),
      url?: string(name='url', description='URL for diagnostic details', example='/diagnose/detail/qe3Z34sa'),
    }
  ](name='data', description='Returned data'),
  message?: string(name='message', description='Error message
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error message.

This parameter is required.', example='SysomOpenAPIAssumeRoleException: EntityNotExist.Role The role not exists: acs:ram::xxxxx:role/aliyunserviceroleforsysom'),
  total?: long(name='total', description='Total', example='319'),
}

model ListDiagnosisResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListDiagnosisResponseBody(name='body'),
}

/**
 * @summary Obtain the list of diagnostic history.
 *
 * @param request ListDiagnosisRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListDiagnosisResponse
 */
@context("listDiagnosisWithContext")
async function listDiagnosisWithOptions(request: ListDiagnosisRequest, headers: map[string]string, runtime: $RuntimeOptions): ListDiagnosisResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.current)) {
    query['current'] = request.current;
  }
  if (!$isNull(request.pageSize)) {
    query['pageSize'] = request.pageSize;
  }
  if (!$isNull(request.params)) {
    query['params'] = request.params;
  }
  if (!$isNull(request.serviceName)) {
    query['service_name'] = request.serviceName;
  }
  if (!$isNull(request.status)) {
    query['status'] = request.status;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListDiagnosis',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/openapi/diagnosis/list_diagnosis`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Obtain the list of diagnostic history.
 *
 * @param request ListDiagnosisRequest
 * @return ListDiagnosisResponse
 */
async function listDiagnosis(request: ListDiagnosisRequest): ListDiagnosisResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listDiagnosisWithOptions(request, headers, runtime);
}

model ListInstanceHealthRequest {
  cluster?: string(name='cluster', description='The cluster ID.', example='2dc6f3be-bf24-4070-8d3a-c03c26850532'),
  current?: int32(name='current', description='The current page number.', example='1'),
  end?: float(name='end', description='The end time.

This parameter is required.', example='1725801327754'),
  instance?: string(name='instance', description='The instance ID.', example='i-wz9d00ut2ska3mlyhn6j'),
  pageSize?: int32(name='pageSize', description='The number of entries per page. Default value: 5. Valid values: 1 to 100.', example='10'),
  start?: float(name='start', description='The start time.

This parameter is required.', example='1725797727754'),
}

model ListInstanceHealthResponseBody = {
  code?: string(name='code', description='The status code.
- If `code == Success`, the authorization is successful.
- Other status codes indicate authorization failed. Check the `message` field for the detailed fault information.', example='SysomOpenAPI.ServerError'),
  data?: [ 
    {
      images?: [ string ](name='images', description='The list of container image names in the Pod.'),
      instance?: string(name='instance', description='The instance ID.', example='i-wz9d00ut2ska3mlyhn6j'),
      namespace?: string(name='namespace', description='The namespace of the Pod.', example='default'),
      pod?: string(name='pod', description='The Pod name.', example='test-pod'),
      regionId?: string(name='region_id', description='The region ID.', example='cn-shanghai'),
      score?: float(name='score', description='The health score.', example='100'),
      status?: string(name='status', description='The running status of the instance. Valid values:
- **Running**: running.
- **Offline**: offline.



> An instance in the Offline status indicates that the heartbeat between the node and the SysOM server is lost. It does not mean that the corresponding ECS instance is not running.', example='Running'),
    }
  ](name='data', description='The returned data.'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error information.', example='Query no data'),
  requestId?: string(name='request_id', description='The request ID.', example='35F91AAB-5FDF-5A22-B211-C7C6B00817D0'),
  total?: int32(name='total', description='The total number of query results.', example='42'),
}

model ListInstanceHealthResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListInstanceHealthResponseBody(name='body'),
}

/**
 * @summary Retrieves the health status list of cluster nodes or Pods within a specified time range.
 *
 * @param request ListInstanceHealthRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListInstanceHealthResponse
 */
@context("listInstanceHealthWithContext")
async function listInstanceHealthWithOptions(request: ListInstanceHealthRequest, headers: map[string]string, runtime: $RuntimeOptions): ListInstanceHealthResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.cluster)) {
    query['cluster'] = request.cluster;
  }
  if (!$isNull(request.current)) {
    query['current'] = request.current;
  }
  if (!$isNull(request.end)) {
    query['end'] = request.end;
  }
  if (!$isNull(request.instance)) {
    query['instance'] = request.instance;
  }
  if (!$isNull(request.pageSize)) {
    query['pageSize'] = request.pageSize;
  }
  if (!$isNull(request.start)) {
    query['start'] = request.start;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListInstanceHealth',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/openapi/cluster_health/range/instance_health_list`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves the health status list of cluster nodes or Pods within a specified time range.
 *
 * @param request ListInstanceHealthRequest
 * @return ListInstanceHealthResponse
 */
async function listInstanceHealth(request: ListInstanceHealthRequest): ListInstanceHealthResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listInstanceHealthWithOptions(request, headers, runtime);
}

model ListInstanceStatusRequest {
  current?: long(name='current', description='The current page number (starting from 1). This field is present when pagination is used.', example='1'),
  instance?: string(name='instance', description='The instance ID.', example='i-wz9b9vucz1iubsz355rh'),
  pageSize?: long(name='pageSize', description='The number of entries per page. Default value: 10.', example='10'),
  region?: string(name='region', description='The region ID.', example='cn-shenzhen'),
  status?: string(name='status', description='Filters the instance list by status. If this field is specified, only instances with the corresponding status are returned.', example='Running'),
}

model ListInstanceStatusResponseBody = {
  requestId?: string(name='RequestId', description='The request ID, which can be used for end-to-end diagnostics.', example='5BC79EF5-798B-5499-9683-4E14EBBBF712'),
  code?: string(name='code', description='The status code.
- `code == Success` indicates that the authorization is successful.
- Other status codes indicate that the authorization failed. Check the `message` field for the detailed error message.', example='Success'),
  data?: [ 
    {
      instance?: string(name='instance', description='The instance ID.', example='i-wz9d00ut2ska3mlyhn6j'),
      region?: string(name='region', description='The region ID.', example='cn-hangzhou'),
      status?: string(name='status', description='The running status of the instance. Valid values:
- **Running**: running.
- **Offline**: offline.



> An instance in the Offline status indicates that the heartbeat between the node and the SysOM server is lost. It does not mean that the corresponding ECS instance is not running.', example='Running'),
    }
  ](name='data', description='The returned data.'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error information.', example='SysomOpenAPIAssumeRoleException: EntityNotExist.Role The role not exists: acs:ram::xxxxx:role/aliyunserviceroleforsysom'),
  total?: long(name='total', description='The total number of records.', example='218'),
}

model ListInstanceStatusResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListInstanceStatusResponseBody(name='body'),
}

/**
 * @summary Retrieves instance statuses.
 *
 * @description Retrieves the list of machines managed by SysOM.
 *
 * @param request ListInstanceStatusRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListInstanceStatusResponse
 */
@context("listInstanceStatusWithContext")
async function listInstanceStatusWithOptions(request: ListInstanceStatusRequest, headers: map[string]string, runtime: $RuntimeOptions): ListInstanceStatusResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.current)) {
    query['current'] = request.current;
  }
  if (!$isNull(request.instance)) {
    query['instance'] = request.instance;
  }
  if (!$isNull(request.pageSize)) {
    query['pageSize'] = request.pageSize;
  }
  if (!$isNull(request.region)) {
    query['region'] = request.region;
  }
  if (!$isNull(request.status)) {
    query['status'] = request.status;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListInstanceStatus',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/am/instance/list_instance_status`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves instance statuses.
 *
 * @description Retrieves the list of machines managed by SysOM.
 *
 * @param request ListInstanceStatusRequest
 * @return ListInstanceStatusResponse
 */
async function listInstanceStatus(request: ListInstanceStatusRequest): ListInstanceStatusResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listInstanceStatusWithOptions(request, headers, runtime);
}

model ListInstancesRequest {
  clusterId?: string(name='cluster_id', description='The cluster ID.', example='xxxxx'),
  current?: long(name='current', description='The current page number. This field exists when pagination is used.', example='1'),
  instance?: string(name='instance', description='The ECS instance ID used to filter results.', example='i-8vbfd3g9vs32sfuvv38h'),
  pageSize?: long(name='pageSize', description='The number of entries per page. Default value: 20. Valid values: 1 to 100.', example='10'),
  region?: string(name='region', description='Filters instances by region.', example='cn-shenzhen'),
  status?: string(name='status', description='Filters instances by status.', example='Running'),
}

model ListInstancesResponseBody = {
  code?: string(name='code', description='The status code.
- `code == Success` indicates that the authorization is successful.
- Other status codes indicate that the authorization failed. Check the `message` field for the detailed fault information.', example='SysomOpenAPI.ServerError'),
  data?: [ 
    {
      clusterId?: string(name='cluster_id', description='The cluster ID.', example='c2218ca2b76ec45e7b7ee1693f6fcd374'),
      instance?: string(name='instance', description='The ECS instance ID.', example='i-wz9d00ut2ska3mlyhn6j'),
      kernelVersion?: string(name='kernel_version', description='The current kernel version of the instance.', example='5.10.134-16.1.an8.x86_64'),
      meta?: any(name='meta', description='The metadata of the instance.', example='{
    "uname": "Linux",
    "oncpu": "off",
    "release": "5.10.134-16.3.al8.aarch64",
    "monitor": "on",
    "version_id": "3",
    "version": "3 (Soaring Falcon)",
    "podNs": [

    ],
    "machine": "aarch64",
    "name": "Alibaba Cloud Linux",
    "sysak": "3.4.0-1",
    "id": "alinux",
    "region": "cn-hangzhou",
    "centos-release": "Alibaba Cloud Linux release 3 (Soaring Falcon)"
}'),
      osArch?: string(name='os_arch', description='The architecture of the ECS instance.', example='x86'),
      osHealthScore?: string(name='os_health_score', description='The health score of the instance.', example='100'),
      osName?: string(name='os_name', description='The operating system name of the instance (obtained from /etc/os-release).', example='Anolis OS'),
      osNameId?: string(name='os_name_id', description='The operating system name ID of the instance (obtained from /etc/os-release).', example='anolis'),
      osVersion?: string(name='os_version', description='The operating system version of the instance (obtained from /etc/os-release).', example='8.9'),
      osVersionId?: string(name='os_version_id', description='The operating system version ID of the instance (obtained from /etc/os-release).', example='rhel fedora centos'),
      region?: string(name='region', description='The region where the instance resides.', example='cn-hangzhou'),
      status?: string(name='status', description='The status of the instance.', example='Running'),
    }
  ](name='data', description='The returned result.'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error information.', example='Requests for llm service failed'),
  requestId?: string(name='request_id', description='The request ID.', example='9515E5A0-8905-59B0-9BBF-5F0BE568C3A0'),
  total?: long(name='total', description='The total number of records.', example='623'),
}

model ListInstancesResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListInstancesResponseBody(name='body'),
}

/**
 * @summary Retrieves a list of instances.
 *
 * @description This operation retrieves the list of instances that are already managed by SysOM. If an ECS instance exists but is not managed by SysOM, it does not appear in the list.
 *
 * @param request ListInstancesRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListInstancesResponse
 */
@context("listInstancesWithContext")
async function listInstancesWithOptions(request: ListInstancesRequest, headers: map[string]string, runtime: $RuntimeOptions): ListInstancesResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.clusterId)) {
    query['cluster_id'] = request.clusterId;
  }
  if (!$isNull(request.current)) {
    query['current'] = request.current;
  }
  if (!$isNull(request.instance)) {
    query['instance'] = request.instance;
  }
  if (!$isNull(request.pageSize)) {
    query['pageSize'] = request.pageSize;
  }
  if (!$isNull(request.region)) {
    query['region'] = request.region;
  }
  if (!$isNull(request.status)) {
    query['status'] = request.status;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListInstances',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/am/instance/list_instances`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves a list of instances.
 *
 * @description This operation retrieves the list of instances that are already managed by SysOM. If an ECS instance exists but is not managed by SysOM, it does not appear in the list.
 *
 * @param request ListInstancesRequest
 * @return ListInstancesResponse
 */
async function listInstances(request: ListInstancesRequest): ListInstancesResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listInstancesWithOptions(request, headers, runtime);
}

model ListInstancesEcsInfoListRequest {
  infoType?: string(name='info_type', description='The type of information to retrieve.

This parameter is required.', example='ip'),
  instanceId?: string(name='instance_id', description='Specifies the instance ID to filter and return the Agent installation status of the specified instance.', example='i-bp118piqcio9tiwgh84b'),
  managedType?: string(name='managed_type', description='The management status of the instance.', example='managed'),
  pluginId?: string(name='plugin_id', description='Specifies the component ID to filter and return the instance information list for the corresponding component.', example='74a86327-3170-412c-8e67-da3389ec56a9'),
  region?: string(name='region', description='The region used to filter instances.

This parameter is required.', example='cn-shenzhen'),
}

model ListInstancesEcsInfoListResponseBody = {
  code?: string(name='code', description='The error code.', example='SysomOpenAPI.InvalidParameter'),
  data?: [ 
    {
      ip?: string(name='ip', description='The IP address of the machine.', example='11.193.52.91'),
      tagKey?: string(name='tag_key', description='The tag name.', example='sysom'),
      tagValue?: string(name='tag_value', description='The tag value.', example='diagnosis'),
      type?: string(name='type', description='The IP address type.', example='public'),
    }
  ](name='data', description='The returned data.'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error message.', example='""'),
}

model ListInstancesEcsInfoListResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListInstancesEcsInfoListResponseBody(name='body'),
}

/**
 * @summary Retrieves lists of ECS information for instances, such as tag lists and public IP address lists.
 *
 * @description The instance list retrieved by this operation contains only machines that are managed by SysOM. If an ECS instance exists but is not managed by SysOM, it does not appear in the list.
 *
 * @param request ListInstancesEcsInfoListRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListInstancesEcsInfoListResponse
 */
@context("listInstancesEcsInfoListWithContext")
async function listInstancesEcsInfoListWithOptions(request: ListInstancesEcsInfoListRequest, headers: map[string]string, runtime: $RuntimeOptions): ListInstancesEcsInfoListResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.infoType)) {
    query['info_type'] = request.infoType;
  }
  if (!$isNull(request.instanceId)) {
    query['instance_id'] = request.instanceId;
  }
  if (!$isNull(request.managedType)) {
    query['managed_type'] = request.managedType;
  }
  if (!$isNull(request.pluginId)) {
    query['plugin_id'] = request.pluginId;
  }
  if (!$isNull(request.region)) {
    query['region'] = request.region;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListInstancesEcsInfoList',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/am/instance/listInstancesEcsInfoList`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves lists of ECS information for instances, such as tag lists and public IP address lists.
 *
 * @description The instance list retrieved by this operation contains only machines that are managed by SysOM. If an ECS instance exists but is not managed by SysOM, it does not appear in the list.
 *
 * @param request ListInstancesEcsInfoListRequest
 * @return ListInstancesEcsInfoListResponse
 */
async function listInstancesEcsInfoList(request: ListInstancesEcsInfoListRequest): ListInstancesEcsInfoListResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listInstancesEcsInfoListWithOptions(request, headers, runtime);
}

model ListInstancesWithEcsInfoRequest {
  current?: int32(name='current', description='The page number for pagination. This parameter specifies the current page.', example='1'),
  healthStatus?: string(name='health_status', description='Filters instances by health status.', example='healthy'),
  instanceId?: string(name='instance_id', description='Specifies an instance ID to filter the Agent installation status of the specified instance.', example='i-bp118piqcio9tiwgh84b'),
  instanceIdName?: string(name='instance_id_name', description='Filters instances by instance name or instance ID. Fuzzy match is supported.', example='84b'),
  instanceName?: string(name='instance_name', description='The name of the component instance.', example='block-load-balancer-hjdm9'),
  instanceTag?: {
    key?: string(name='key', description='The tag key.', example='feature_dim_radar_chart'),
    value?: string(name='value', description='The tag value.', example='test'),
  }(name='instance_tag', description='Filters instances by instance tag.'),
  isManaged?: int32(name='is_managed', description='Filters instances by management status.', example='1'),
  osName?: string(name='os_name', description='Filters instances by operating system name.', example='Alibaba Cloud Linux  3.2104 LTS 64bit'),
  pageSize?: int32(name='pageSize', description='The number of entries per page. Default value: 10.', example='10'),
  privateIp?: string(name='private_ip', description='Filters instances by private IP address.', example='1.1.1.1'),
  publicIp?: string(name='public_ip', description='Filters instances by public IP address.', example='1.1.1.1'),
  region?: string(name='region', description='Filters instances by region.

This parameter is required.', example='cn-shenzhen'),
  resourceGroupId?: string(name='resource_group_id', description='Filters instances by resource group ID.', example='rg-xxxxxxx'),
  resourceGroupIdName?: string(name='resource_group_id_name', description='Filters instances by resource group name or resource group ID. Fuzzy match is supported.', example='default'),
  resourceGroupName?: string(name='resource_group_name', description='Filters instances by resource group name.', example='default resource group'),
}

model ListInstancesWithEcsInfoShrinkRequest {
  current?: int32(name='current', description='The page number for pagination. This parameter specifies the current page.', example='1'),
  healthStatus?: string(name='health_status', description='Filters instances by health status.', example='healthy'),
  instanceId?: string(name='instance_id', description='Specifies an instance ID to filter the Agent installation status of the specified instance.', example='i-bp118piqcio9tiwgh84b'),
  instanceIdName?: string(name='instance_id_name', description='Filters instances by instance name or instance ID. Fuzzy match is supported.', example='84b'),
  instanceName?: string(name='instance_name', description='The name of the component instance.', example='block-load-balancer-hjdm9'),
  instanceTagShrink?: string(name='instance_tag', description='Filters instances by instance tag.'),
  isManaged?: int32(name='is_managed', description='Filters instances by management status.', example='1'),
  osName?: string(name='os_name', description='Filters instances by operating system name.', example='Alibaba Cloud Linux  3.2104 LTS 64bit'),
  pageSize?: int32(name='pageSize', description='The number of entries per page. Default value: 10.', example='10'),
  privateIp?: string(name='private_ip', description='Filters instances by private IP address.', example='1.1.1.1'),
  publicIp?: string(name='public_ip', description='Filters instances by public IP address.', example='1.1.1.1'),
  region?: string(name='region', description='Filters instances by region.

This parameter is required.', example='cn-shenzhen'),
  resourceGroupId?: string(name='resource_group_id', description='Filters instances by resource group ID.', example='rg-xxxxxxx'),
  resourceGroupIdName?: string(name='resource_group_id_name', description='Filters instances by resource group name or resource group ID. Fuzzy match is supported.', example='default'),
  resourceGroupName?: string(name='resource_group_name', description='Filters instances by resource group name.', example='default resource group'),
}

model ListInstancesWithEcsInfoResponseBody = {
  code?: string(name='code', description='The status code.', example='Success'),
  data?: [ 
    {
      clusterId?: string(name='cluster_id', description='The cluster ID.', example='cbf7a37bc905d4682a3338b3744810269'),
      instanceId?: string(name='instance_id', description='The instance ID.', example='i-bp118piqcio9tiwgh84b'),
      instanceName?: string(name='instance_name', description='The instance name.', example='allowed-repos-r2tzl'),
      instanceTag?: [ 
        {
          tagKey?: string(name='tag_key', description='The tag key.', example='test_tag_key'),
          tagValue?: string(name='tag_value', description='The tag value.', example='test_tag_value'),
        }
      ](name='instance_tag', description='The instance tags.'),
      kernelVersion?: string(name='kernel_version', description='The kernel version.', example='5.10.134-14.an8.x86_64'),
      osArch?: string(name='os_arch', description='The architecture of the ECS instance.', example='x86'),
      osHealthScore?: string(name='os_health_score', description='The health score of the instance.', example='100'),
      osName?: string(name='os_name', description='The operating system name of the instance.', example='Alibaba Cloud Linux  3.2104 LTS 64bit'),
      privateIp?: string(name='private_ip', description='The private IP address of the instance.', example='1.1.1.1'),
      publicIp?: string(name='public_ip', description='The public IP address of the instance.', example='1.1.1.1'),
      resourceGroupId?: string(name='resource_group_id', description='The resource group ID.', example='rg-xxxxxx'),
      resourceGroupName?: string(name='resource_group_name', description='The resource group name.', example='default resource group'),
      status?: string(name='status', description='The running status of the instance. Valid values:
- **Running**: The instance is running.
- **Offline**: The instance is offline.



> An instance in the Offline state indicates that the heartbeat between the node and the SysOM server is lost. It does not mean that the corresponding ECS instance has stopped running.', example='Running'),
    }
  ](name='data', description='The returned data.'),
  message?: string(name='message', description='The error message. An empty value indicates that all data has been read.', example='result: code=1 msg=(Request failed, status_code != 200)'),
  requestId?: string(name='request_id', description='The request ID.', example='35F91AAB-5FDF-5A22-B211-C7C6B00817D0'),
  total?: long(name='total', description='The total number of records.', example='319'),
}

model ListInstancesWithEcsInfoResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListInstancesWithEcsInfoResponseBody(name='body'),
}

/**
 * @summary Retrieves information about managed and unmanaged instances, including ECS information.
 *
 * @description The instance list returned by this operation contains only machines that are managed by SysOM. If an ECS instance exists but is not managed by SysOM, it does not appear in the list.
 *
 * @param tmpReq ListInstancesWithEcsInfoRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListInstancesWithEcsInfoResponse
 */
@context("listInstancesWithEcsInfoWithContext")
async function listInstancesWithEcsInfoWithOptions(tmpReq: ListInstancesWithEcsInfoRequest, headers: map[string]string, runtime: $RuntimeOptions): ListInstancesWithEcsInfoResponse {
  tmpReq.validate();
  var request = new ListInstancesWithEcsInfoShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.instanceTag)) {
    request.instanceTagShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.instanceTag, 'instance_tag', 'json');
  }
  var query : map[string]any = {};
  if (!$isNull(request.current)) {
    query['current'] = request.current;
  }
  if (!$isNull(request.healthStatus)) {
    query['health_status'] = request.healthStatus;
  }
  if (!$isNull(request.instanceId)) {
    query['instance_id'] = request.instanceId;
  }
  if (!$isNull(request.instanceIdName)) {
    query['instance_id_name'] = request.instanceIdName;
  }
  if (!$isNull(request.instanceName)) {
    query['instance_name'] = request.instanceName;
  }
  if (!$isNull(request.instanceTagShrink)) {
    query['instance_tag'] = request.instanceTagShrink;
  }
  if (!$isNull(request.isManaged)) {
    query['is_managed'] = request.isManaged;
  }
  if (!$isNull(request.osName)) {
    query['os_name'] = request.osName;
  }
  if (!$isNull(request.pageSize)) {
    query['pageSize'] = request.pageSize;
  }
  if (!$isNull(request.privateIp)) {
    query['private_ip'] = request.privateIp;
  }
  if (!$isNull(request.publicIp)) {
    query['public_ip'] = request.publicIp;
  }
  if (!$isNull(request.region)) {
    query['region'] = request.region;
  }
  if (!$isNull(request.resourceGroupId)) {
    query['resource_group_id'] = request.resourceGroupId;
  }
  if (!$isNull(request.resourceGroupIdName)) {
    query['resource_group_id_name'] = request.resourceGroupIdName;
  }
  if (!$isNull(request.resourceGroupName)) {
    query['resource_group_name'] = request.resourceGroupName;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListInstancesWithEcsInfo',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/am/instance/listInstancesWithEcsInfo`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves information about managed and unmanaged instances, including ECS information.
 *
 * @description The instance list returned by this operation contains only machines that are managed by SysOM. If an ECS instance exists but is not managed by SysOM, it does not appear in the list.
 *
 * @param request ListInstancesWithEcsInfoRequest
 * @return ListInstancesWithEcsInfoResponse
 */
async function listInstancesWithEcsInfo(request: ListInstancesWithEcsInfoRequest): ListInstancesWithEcsInfoResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listInstancesWithEcsInfoWithOptions(request, headers, runtime);
}

model ListPluginsInstancesRequest {
  current?: long(name='current', description='The current page number. This field is present when pagination is used.', example='5'),
  instanceIdName?: string(name='instance_id_name', description='Filters instances by instance ID or instance name. Fuzzy match is supported.', example='i-bpxx'),
  instanceTag?: string(name='instance_tag', description='Filters instances by instance tag.', example='{"key":"app","value":"sysom-aliyun-com"}'),
  operationType?: string(name='operation_type', description='Filters instances by plug-in installation status.

This parameter is required.', example='install'),
  pageSize?: long(name='pageSize', description='The number of entries per page. Default value: 10.', example='20'),
  pluginId?: string(name='plugin_id', description='Filters the instance list by the specified agent. If this parameter is specified, only instances associated with the specified agent are returned.

This parameter is required.', example='74a86327-3170-412c-8e67-da3389ec56a9'),
  region?: string(name='region', description='Filters instances by region.

This parameter is required.', example='cn-shenzhen'),
}

model ListPluginsInstancesResponseBody = {
  code?: string(name='code', description='The status code.
- `code == Success` indicates that the authorization is successful.
- Other status codes indicate that the authorization failed. Check the `message` field for the detailed fault information.', example='Success'),
  data?: [ 
    {
      instanceId?: string(name='instance_id', description='The instance ID.', example='i-bp118piqcio9tiwgh84b'),
      instanceName?: string(name='instance_name', description='The component instance name.', example='allowed-repos-qmf8w'),
      instanceTag?: [ 
        {
          tagKey?: string(name='tag_key', description='The tag key.', example='test_tag_key'),
          tagValue?: string(name='tag_value', description='The tag value.', example='test_tag_value'),
        }
      ](name='instance_tag', description='The instance tag.'),
      osName?: string(name='os_name', description='The operating system name of the instance.', example='Alibaba Cloud Linux  3.2104 LTS 64 bit'),
      privateIp?: string(name='private_ip', description='The private IP address.', example='1.1.1.1'),
      publicIp?: string(name='public_ip', description='The public IP address.', example='1.1.1.1'),
      region?: string(name='region', description='The region where the instance resides.', example='cn-hangzhou'),
      resourceGroupId?: string(name='resource_group_id', description='The resource group ID.', example='rg-xxxx'),
      resourceGroupName?: string(name='resource_group_name', description='The resource group name.', example='default resource group'),
    }
  ](name='data', description='The returned result.'),
  message?: string(name='message', description='The description.', example='SysomOpenAPIException: SysomOpenAPI.InvalidParameter Invalid params, should be json string or dict'),
  requestId?: string(name='request_id', description='The request ID.', example='35F91AAB-5FDF-5A22-B211-C7C6B00817D0'),
  total?: long(name='total', description='The total number of entries returned.', example='42'),
}

model ListPluginsInstancesResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListPluginsInstancesResponseBody(name='body'),
}

/**
 * @summary Retrieves the list of instances for plug-in installation, update, or uninstallation.
 *
 * @description The instance list retrieved by this operation contains only machines that are managed by SysOM. If an ECS instance exists but is not managed by SysOM, it does not appear in the list.
 *
 * @param request ListPluginsInstancesRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListPluginsInstancesResponse
 */
@context("listPluginsInstancesWithContext")
async function listPluginsInstancesWithOptions(request: ListPluginsInstancesRequest, headers: map[string]string, runtime: $RuntimeOptions): ListPluginsInstancesResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.current)) {
    query['current'] = request.current;
  }
  if (!$isNull(request.instanceIdName)) {
    query['instance_id_name'] = request.instanceIdName;
  }
  if (!$isNull(request.instanceTag)) {
    query['instance_tag'] = request.instanceTag;
  }
  if (!$isNull(request.operationType)) {
    query['operation_type'] = request.operationType;
  }
  if (!$isNull(request.pageSize)) {
    query['pageSize'] = request.pageSize;
  }
  if (!$isNull(request.pluginId)) {
    query['plugin_id'] = request.pluginId;
  }
  if (!$isNull(request.region)) {
    query['region'] = request.region;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListPluginsInstances',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/am/agent/listPluginsInstances`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves the list of instances for plug-in installation, update, or uninstallation.
 *
 * @description The instance list retrieved by this operation contains only machines that are managed by SysOM. If an ECS instance exists but is not managed by SysOM, it does not appear in the list.
 *
 * @param request ListPluginsInstancesRequest
 * @return ListPluginsInstancesResponse
 */
async function listPluginsInstances(request: ListPluginsInstancesRequest): ListPluginsInstancesResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listPluginsInstancesWithOptions(request, headers, runtime);
}

model ListPodsOfInstanceRequest {
  clusterId?: string(name='cluster_id', description='The cluster ID.', example='c96e34d74eb6748f3b2a46552d5d653f6'),
  current?: long(name='current', description='The current page number. Pages start from page 1.', example='1'),
  instance?: string(name='instance', description='The instance ID.', example='i-wz9d00ut2ska3mlyhn6j'),
  pageSize?: long(name='pageSize', description='The number of entries per page. Default value: 10.', example='10'),
}

model ListPodsOfInstanceResponseBody = {
  requestId?: string(name='RequestId', description='Id of the request', example='B149FD9C-ED5C-5765-B3AD-05AA4A4D64D7'),
  code?: string(name='code', description='The error code.', example='Success'),
  data?: [ 
    {
      namespace?: string(name='namespace', description='The namespace in which the pod resides.', example='default'),
      pod?: string(name='pod', description='The pod name.', example='test-pod'),
    }
  ](name='data', description='The returned data.'),
  message?: string(name='message', description='The error message. An empty value indicates that all data has been read.

This parameter is required.', example='instance not exists'),
  total?: long(name='total', description='The maximum number of entries returned for this request.', example='42'),
}

model ListPodsOfInstanceResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListPodsOfInstanceResponseBody(name='body'),
}

/**
 * @summary Retrieves the list of pods in a cluster or instance.
 *
 * @param request ListPodsOfInstanceRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListPodsOfInstanceResponse
 */
@context("listPodsOfInstanceWithContext")
async function listPodsOfInstanceWithOptions(request: ListPodsOfInstanceRequest, headers: map[string]string, runtime: $RuntimeOptions): ListPodsOfInstanceResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.clusterId)) {
    query['cluster_id'] = request.clusterId;
  }
  if (!$isNull(request.current)) {
    query['current'] = request.current;
  }
  if (!$isNull(request.instance)) {
    query['instance'] = request.instance;
  }
  if (!$isNull(request.pageSize)) {
    query['pageSize'] = request.pageSize;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListPodsOfInstance',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/am/instance/list_pod_of_instance`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Retrieves the list of pods in a cluster or instance.
 *
 * @param request ListPodsOfInstanceRequest
 * @return ListPodsOfInstanceResponse
 */
async function listPodsOfInstance(request: ListPodsOfInstanceRequest): ListPodsOfInstanceResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listPodsOfInstanceWithOptions(request, headers, runtime);
}

model ListRegionsResponseBody = {
  requestId?: string(name='RequestId', description='The request ID, which can be used for end-to-end diagnostics.', example='B149FD9C-ED5C-5765-B3AD-05AA4A4D64D7'),
  code?: string(name='code', description='The error code.', example='Success'),
  data?: [ string ](name='data', description='The list of regions.', example='["cn-hangzhou", "cn-shengzhen"]'),
  message?: string(name='message', description='The error code description. This value is empty if no error occurs.', example='SysomOpenAPIAssumeRoleException: EntityNotExist.Role The role not exists: acs:ram::xxxxx:role/aliyunserviceroleforsysom'),
}

model ListRegionsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListRegionsResponseBody(name='body'),
}

/**
 * @summary Lists all regions that contain managed instances.
 *
 * @description This operation retrieves the list of regions where the current user has instances managed by SysOM. If a user has ECS instances in a region but none of them are managed by SysOM, that region is not included in the response.
 *
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListRegionsResponse
 */
@context("listRegionsWithContext")
async function listRegionsWithOptions(headers: map[string]string, runtime: $RuntimeOptions): ListRegionsResponse {
  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
  };
  var params = new OpenApiUtil.Params{
    action = 'ListRegions',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/am/instance/list_regions`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Lists all regions that contain managed instances.
 *
 * @description This operation retrieves the list of regions where the current user has instances managed by SysOM. If a user has ECS instances in a region but none of them are managed by SysOM, that region is not included in the response.
 *
 * @return ListRegionsResponse
 */
async function listRegions(): ListRegionsResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listRegionsWithOptions(headers, runtime);
}

model ListVmcoreDiagnosisTaskRequest {
  days?: long(name='days', description='Specifies the tasks from the past number of days, up to a maximum of 30 days

This parameter is required.', example='3'),
}

model ListVmcoreDiagnosisTaskResponseBody = {
  code?: string(name='code', description='Status code.
- `code == Success` indicates the authorization was successful.
- Other status codes indicate authorization failure. When authorization fails, check the `message` field for detailed error information.', example='Success'),
  data?: [ 
    {
      createdAt?: string(name='createdAt', description='Task creation time', example='2025-12-02T17:36:12'),
      errorMsg?: string(name='errorMsg', description='Error message', example='error message'),
      taskId?: string(name='taskId', description='Task ID', example='bbe94a98-4192-4172-b856-95777e0a55d7'),
      taskStatus?: string(name='taskStatus', description='Task status', example='running'),
      taskType?: string(name='taskType', description='Task type', example='vmcore'),
    }
  ](name='data', description='Response data.'),
  message?: string(name='message', description='Error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error message.', example='SysomOpenAPIException: SysomOpenAPI.InvalidParameter Invalid params, should be json string or dict'),
  requestId?: string(name='requestId', description='Id of the request', example='2D693121-C925-5154-8DF6-C09A8B369822'),
  total?: long(name='total', description='Total number of tasks', example='2'),
}

model ListVmcoreDiagnosisTaskResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListVmcoreDiagnosisTaskResponseBody(name='body'),
}

/**
 * @summary Query the historical crash diagnosis task list.
 *
 * @param request ListVmcoreDiagnosisTaskRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListVmcoreDiagnosisTaskResponse
 */
@context("listVmcoreDiagnosisTaskWithContext")
async function listVmcoreDiagnosisTaskWithOptions(request: ListVmcoreDiagnosisTaskRequest, headers: map[string]string, runtime: $RuntimeOptions): ListVmcoreDiagnosisTaskResponse {
  request.validate();
  var query : map[string]any = {};
  if (!$isNull(request.days)) {
    query['days'] = request.days;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListVmcoreDiagnosisTask',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/crashAgent/diagnosis/queryTaskList`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Query the historical crash diagnosis task list.
 *
 * @param request ListVmcoreDiagnosisTaskRequest
 * @return ListVmcoreDiagnosisTaskResponse
 */
async function listVmcoreDiagnosisTask(request: ListVmcoreDiagnosisTaskRequest): ListVmcoreDiagnosisTaskResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return listVmcoreDiagnosisTaskWithOptions(request, headers, runtime);
}

model StartAIAnalysisRequest {
  analysisTool?: string(name='analysisTool', description='Analysis tool. Not required for OpenAPI access.', example='gp/pyki/analysis'),
  analysisParams?: [ string ](name='analysis_params', description='Data richness'),
  channel?: string(name='channel', description='Channel name', example='ecs_sysom'),
  comms?: string(name='comms', description='Process name, optional parameter', example='python_test'),
  createdBy?: string(name='created_by', description='Creator. Not required for OpenAPI access.', example='445333'),
  instance?: string(name='instance', description='Instance ID', example='i-wz9dej066kii4goqxxxx'),
  instanceType?: string(name='instance_type', description='Instance type. Not required for OpenAPI access.', example='ip/sn/hostname'),
  iterationFunc?: string(name='iteration_func', description='Iteration entry function. Required only in iteration mode. Can be left blank.', example='Class.function'),
  iterationMod?: string(name='iteration_mod', description='Iteration entry module. Required only in iteration mode. Can be left blank.', example='a.b.module'),
  iterationRange?: [ int32 ](name='iteration_range', description='Iteration range (iteration count: refers to the number of iterations when the data collection module is activated, independent of the AI job\\\\"s iteration count)'),
  pids?: string(name='pids', description='AI job process PIDs. Supports batch input, separated by commas.', example='2421,36547,10043'),
  region?: string(name='region', description='Region ID', example='cn-shenzhen'),
  timeout?: int32(name='timeout', description='AI Infra analysis time;
Unit: milliseconds;
Default value: 2000;', example='2000'),
  uid?: string(name='uid', description='Alibaba Cloud user ID. Not required for OpenAPI access.', example='123423414'),
}

model StartAIAnalysisResponseBody = {
  code?: string(name='code', description='Response code.', example='Success'),
  data?: {
    analysisId?: string(name='analysis_id', description='AI analysis ID', example='16896fa8-37f6-4c70-bb32-67fa9817d426'),
  }(name='data', description='Response data'),
  message?: string(name='message', description='Error message.', example='""'),
  requestId?: string(name='requestId', description='Id of the request', example='2D693121-C925-5154-8DF6-C09A8B369822'),
}

model StartAIAnalysisResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: StartAIAnalysisResponseBody(name='body'),
}

/**
 * @summary Start AI job analysis.
 *
 * @param request StartAIAnalysisRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return StartAIAnalysisResponse
 */
@context("startAIAnalysisWithContext")
async function startAIAnalysisWithOptions(request: StartAIAnalysisRequest, headers: map[string]string, runtime: $RuntimeOptions): StartAIAnalysisResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.analysisTool)) {
    body['analysisTool'] = request.analysisTool;
  }
  if (!$isNull(request.analysisParams)) {
    body['analysis_params'] = request.analysisParams;
  }
  if (!$isNull(request.channel)) {
    body['channel'] = request.channel;
  }
  if (!$isNull(request.comms)) {
    body['comms'] = request.comms;
  }
  if (!$isNull(request.createdBy)) {
    body['created_by'] = request.createdBy;
  }
  if (!$isNull(request.instance)) {
    body['instance'] = request.instance;
  }
  if (!$isNull(request.instanceType)) {
    body['instance_type'] = request.instanceType;
  }
  if (!$isNull(request.iterationFunc)) {
    body['iteration_func'] = request.iterationFunc;
  }
  if (!$isNull(request.iterationMod)) {
    body['iteration_mod'] = request.iterationMod;
  }
  if (!$isNull(request.iterationRange)) {
    body['iteration_range'] = request.iterationRange;
  }
  if (!$isNull(request.pids)) {
    body['pids'] = request.pids;
  }
  if (!$isNull(request.region)) {
    body['region'] = request.region;
  }
  if (!$isNull(request.timeout)) {
    body['timeout'] = request.timeout;
  }
  if (!$isNull(request.uid)) {
    body['uid'] = request.uid;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'StartAIAnalysis',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/openapi/proxy/post/start_ai_analysis`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Start AI job analysis.
 *
 * @param request StartAIAnalysisRequest
 * @return StartAIAnalysisResponse
 */
async function startAIAnalysis(request: StartAIAnalysisRequest): StartAIAnalysisResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return startAIAnalysisWithOptions(request, headers, runtime);
}

model StartAIDiffAnalysisRequest {
  task1?: {
    analysisId?: string(name='analysisId', description='The AI analysis ID.', example='16896fa8-37f6-4c70-bb32-67fa9817d426'),
    pids?: [ string ](name='pids', description='The pids of AI job processes. Batch input is supported. Separate multiple pids with commas.'),
    stepEnd?: float(name='step_end', description='The step end time, calculated based on the selected step number.', example='4660551334179.955'),
    stepStart?: float(name='step_start', description='The step start time, calculated based on the selected step number.', example='4660550379415.497'),
  }(name='task1', description='The task1 parameters.

This parameter is required.', example='task1参数'),
  task2?: {
    analysisId?: string(name='analysisId', description='The AI analysis ID.

This parameter is required.', example='16896fa8-37f6-4c70-bb32-67fa9817d426'),
    pids?: [ string ](name='pids', description='The pids of AI job processes. Batch input is supported. Separate multiple pids with commas.

This parameter is required.', example='452651:python3 ./test.py'),
    stepEnd?: float(name='step_end', description='The step end time, calculated based on the selected step number.

This parameter is required.', example='4660551334179.955'),
    stepStart?: float(name='step_start', description='The step start time, calculated based on the selected step number.

This parameter is required.', example='4660550379415.497'),
  }(name='task2', description='The task2 parameters.

This parameter is required.', example='task2参数，目前只支持相同analysisId和pid的对比'),
}

model StartAIDiffAnalysisResponseBody = {
  code?: string(name='code', description='The status code.
- `code == Success` indicates that the authorization is successful.
- Other status codes indicate that the authorization failed. Check the `message` field for the detailed fault information.', example='Success'),
  data?: string(name='data', description='The AI differential analysis result data.', example='"[
  {
      name: "xxx", #算子名称
      before_time: 2, # 前者总耗时
      after_time: 4, # 后者总耗时
      time_diff: 2,  # 耗时差异
      before_time_perc: "80%", # 前者总耗时百分比
      after_time_perc: "23%", # 后者总耗时百分比
      time_perc_diff: "-54%",  # 耗时差异
      before_count: 1, # 前者总调用差异
      after_count: 2,  # 后者总调用差异
      count_diff: 1,   # 调用差异
      before_count_perc: "56%", # 前者总调用差异
      after_count_perc: "32%",  # 后者总调用差异
      count_perc_diff: "44%",   # 调用差异
  },
  {...}
]"'),
  message?: string(name='message', description='The error code description. This field is empty if no error occurs.', example='""'),
  requestId?: string(name='requestId', description='Id of the request', example='2D693121-C925-5154-8DF6-C09A8B369822'),
}

model StartAIDiffAnalysisResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: StartAIDiffAnalysisResponseBody(name='body'),
}

/**
 * @summary Starts an AI Infra differential analysis.
 *
 * @description Currently, only comparative analysis of the same pid across different steps within the same AI Infra analysis record is supported.
 *
 * @param request StartAIDiffAnalysisRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return StartAIDiffAnalysisResponse
 */
@context("startAIDiffAnalysisWithContext")
async function startAIDiffAnalysisWithOptions(request: StartAIDiffAnalysisRequest, headers: map[string]string, runtime: $RuntimeOptions): StartAIDiffAnalysisResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.task1)) {
    body['task1'] = request.task1;
  }
  if (!$isNull(request.task2)) {
    body['task2'] = request.task2;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'StartAIDiffAnalysis',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/appObserv/aiAnalysis/diffAnalysis`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Starts an AI Infra differential analysis.
 *
 * @description Currently, only comparative analysis of the same pid across different steps within the same AI Infra analysis record is supported.
 *
 * @param request StartAIDiffAnalysisRequest
 * @return StartAIDiffAnalysisResponse
 */
async function startAIDiffAnalysis(request: StartAIDiffAnalysisRequest): StartAIDiffAnalysisResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return startAIDiffAnalysisWithOptions(request, headers, runtime);
}

model UninstallAgentRequest {
  agentId?: string(name='agent_id', description='The ID of the component to uninstall.

This parameter is required.', example='74a86327-3170-412c-8e67-da3389ec56a9'),
  agentVersion?: string(name='agent_version', description='The version of the component to uninstall.

This parameter is required.', example='3.4.0-1'),
  instances?: [ 
    {
      instance?: string(name='instance', description='The instance ID.

This parameter is required.', example='i-wz9b9vucz1iubsz8sjqo'),
      region?: string(name='region', description='The region ID.

This parameter is required.', example='cn-hangzhou'),
    }
  ](name='instances', description='The list of instances from which to uninstall the component.

This parameter is required.'),
}

model UninstallAgentResponseBody = {
  requestId?: string(name='RequestId', description='The request ID, which can be used for end-to-end diagnostics.', example='13772206-1162-5A0F-81F0-79A10C249A5E'),
  code?: string(name='code', description='The status code.
- If code is Success, the authorization is successful.
- Other status codes indicate that the authorization has failed. Check the message field for the detailed fault information.', example='Success'),
  data?: {
    taskId?: string(name='task_id', description='The task ID.', example='63fc5acb99e642d793f42912612e8001'),
  }(name='data', description='The response data.'),
  message?: string(name='message', description='The error message.
- If code is Success, this field is empty.
- Otherwise, this field contains the request error information.', example='SysomOpenAPIAssumeRoleException: EntityNotExist.Role The role not exists: acs:ram::xxxxx:role/aliyunserviceroleforsysom'),
}

model UninstallAgentResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UninstallAgentResponseBody(name='body'),
}

/**
 * @summary Uninstalls a specified version of a component.
 *
 * @description Calling this operation to uninstall an Agent is asynchronous. After the call, a task_id is returned. Use this ID to call the GetAgentTask operation to retrieve the execution status of the task.
 *
 * @param request UninstallAgentRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UninstallAgentResponse
 */
@context("uninstallAgentWithContext")
async function uninstallAgentWithOptions(request: UninstallAgentRequest, headers: map[string]string, runtime: $RuntimeOptions): UninstallAgentResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.agentId)) {
    body['agent_id'] = request.agentId;
  }
  if (!$isNull(request.agentVersion)) {
    body['agent_version'] = request.agentVersion;
  }
  if (!$isNull(request.instances)) {
    body['instances'] = request.instances;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'UninstallAgent',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/am/agent/uninstall_agent`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Uninstalls a specified version of a component.
 *
 * @description Calling this operation to uninstall an Agent is asynchronous. After the call, a task_id is returned. Use this ID to call the GetAgentTask operation to retrieve the execution status of the task.
 *
 * @param request UninstallAgentRequest
 * @return UninstallAgentResponse
 */
async function uninstallAgent(request: UninstallAgentRequest): UninstallAgentResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return uninstallAgentWithOptions(request, headers, runtime);
}

model UninstallAgentForClusterRequest {
  agentId?: string(name='agent_id', description='The component ID.', example='74a86327-3170-412c-8e67-da3389ec56a9'),
  agentVersion?: string(name='agent_version', description='The component version.', example='3.4.0-1'),
  clusterId?: string(name='cluster_id', description='The cluster ID.

> This parameter must be set to the ID of an ACK cluster.', example='c822f83bb45994ddbac9326b4c2f04f35'),
}

model UninstallAgentForClusterResponseBody = {
  requestId?: string(name='RequestId', description='The request ID, which can be used for end-to-end diagnostics.', example='44841312-7227-55C9-AE03-D59729BFAE38'),
  code?: string(name='code', description='The status code.
- `code == Success` indicates that the authorization is successful.
- Other status codes indicate that the authorization failed. Check the `message` field for the detailed fault information.', example='Success'),
  data?: {
    taskId?: string(name='task_id', description='The task ID. You can use this task ID to call GetAgentTasK to query the execution status of the task.', example='049ea0609515414b9e19c3389d7ba638'),
  }(name='data', description='The response data.'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the error information of the request.', example='SysomOpenAPIException: SysomOpenAPI.NotAuthorizedInstance Instance 21 is not authorized'),
}

model UninstallAgentForClusterResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UninstallAgentForClusterResponseBody(name='body'),
}

/**
 * @summary Uninstalls a component from a cluster.
 *
 * @param request UninstallAgentForClusterRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UninstallAgentForClusterResponse
 */
@context("uninstallAgentForClusterWithContext")
async function uninstallAgentForClusterWithOptions(request: UninstallAgentForClusterRequest, headers: map[string]string, runtime: $RuntimeOptions): UninstallAgentForClusterResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.agentId)) {
    body['agent_id'] = request.agentId;
  }
  if (!$isNull(request.agentVersion)) {
    body['agent_version'] = request.agentVersion;
  }
  if (!$isNull(request.clusterId)) {
    body['cluster_id'] = request.clusterId;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'UninstallAgentForCluster',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/am/agent/uninstall_agent_by_cluster`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Uninstalls a component from a cluster.
 *
 * @param request UninstallAgentForClusterRequest
 * @return UninstallAgentForClusterResponse
 */
async function uninstallAgentForCluster(request: UninstallAgentForClusterRequest): UninstallAgentForClusterResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return uninstallAgentForClusterWithOptions(request, headers, runtime);
}

model UninstallAgentWithTypeRequest {
  agentId?: string(name='agentId', description='This parameter is required.', example='74a86327-3170-412c-8e67-da3389ec56a9'),
  agentVersion?: string(name='agentVersion', description='This parameter is required.', example='3.4.0-1'),
  instanceType?: string(name='instanceType', example='ecs'),
  instances?: [ 
    {
      instance?: string(name='instance', description='This parameter is required.', example='i-wz9b9vucz1iubsz8sjqo'),
      region?: string(name='region', description='This parameter is required.', example='cn-hangzhou'),
    }
  ](name='instances', description='This parameter is required.'),
}

model UninstallAgentWithTypeResponseBody = {
  code?: string(name='code', example='Success'),
  data?: {
    taskId?: string(name='taskId', example='63fc5acb99e642d793f42912612e8001'),
  }(name='data'),
  message?: string(name='message', example='“”'),
  requestId?: string(name='requestId', example='13772206-1162-5A0F-81F0-79A10C249A5E'),
}

model UninstallAgentWithTypeResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UninstallAgentWithTypeResponseBody(name='body'),
}

/**
 * @summary 卸载 SysOM Agent
 *
 * @description 调用本接口卸载 Agent 是异步的，调用接口后会返回一个 task_id，可以凭借该 ID 调用 GetAgentTask 接口获取任务的执行情况。
 *
 * @param request UninstallAgentWithTypeRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UninstallAgentWithTypeResponse
 */
@context("uninstallAgentWithTypeWithContext")
async function uninstallAgentWithTypeWithOptions(request: UninstallAgentWithTypeRequest, headers: map[string]string, runtime: $RuntimeOptions): UninstallAgentWithTypeResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.agentId)) {
    body['agentId'] = request.agentId;
  }
  if (!$isNull(request.agentVersion)) {
    body['agentVersion'] = request.agentVersion;
  }
  if (!$isNull(request.instanceType)) {
    body['instanceType'] = request.instanceType;
  }
  if (!$isNull(request.instances)) {
    body['instances'] = request.instances;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'UninstallAgentWithType',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/am/agent/uninstallAgent`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 卸载 SysOM Agent
 *
 * @description 调用本接口卸载 Agent 是异步的，调用接口后会返回一个 task_id，可以凭借该 ID 调用 GetAgentTask 接口获取任务的执行情况。
 *
 * @param request UninstallAgentWithTypeRequest
 * @return UninstallAgentWithTypeResponse
 */
async function uninstallAgentWithType(request: UninstallAgentWithTypeRequest): UninstallAgentWithTypeResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return uninstallAgentWithTypeWithOptions(request, headers, runtime);
}

model UpdateAlertDestinationRequest {
  id?: string(name='id', description='The ID of the alert contact.', example='1'),
  name?: string(name='name', description='The name of the alert contact.', example='name1'),
  params?: {
    email?: string(name='email', description='The email address.', example='xxx@email.com'),
    phone?: string(name='phone', description='The phone number.', example='1xxx'),
    sec?: string(name='sec', description='The secret key of the chatbot.', example='SECxxx'),
    webhook?: string(name='webhook', description='The webhook URL of the chatbot.', example='https://oapi.dingtalk.com/robot/send?access_token=xxx'),
  }(name='params', description='The configuration parameters.'),
  source?: string(name='source', description='The configuration source.', example='console'),
  target?: string(name='target', description='The alert notification target. Currently, only DingTalk contacts are supported.', example='dingtalk'),
}

model UpdateAlertDestinationResponseBody = {
  code?: string(name='code', description='The status code.
- If `code == Success`, the authorization is successful.
- Other status codes indicate authorization has failed. Check the `message` field for the detailed error message.', example='SysomOpenAPI.InvalidParameter'),
  data?: any(name='data', description='The response data.', example='{
"webhook":"",
"sec":""
}'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error information.', example='Success'),
  requestId?: string(name='requestId', description='The request ID.', example='2D693121-C925-5154-8DF6-C09A8B369822'),
}

model UpdateAlertDestinationResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateAlertDestinationResponseBody(name='body'),
}

/**
 * @summary Updates an alert contact.
 *
 * @description .
 *
 * @param request UpdateAlertDestinationRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateAlertDestinationResponse
 */
@context("updateAlertDestinationWithContext")
async function updateAlertDestinationWithOptions(request: UpdateAlertDestinationRequest, headers: map[string]string, runtime: $RuntimeOptions): UpdateAlertDestinationResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.id)) {
    body['id'] = request.id;
  }
  if (!$isNull(request.name)) {
    body['name'] = request.name;
  }
  if (!$isNull(request.params)) {
    body['params'] = request.params;
  }
  if (!$isNull(request.source)) {
    body['source'] = request.source;
  }
  if (!$isNull(request.target)) {
    body['target'] = request.target;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpdateAlertDestination',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/alertPusher/alert/updateDestination`,
    method = 'PUT',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Updates an alert contact.
 *
 * @description .
 *
 * @param request UpdateAlertDestinationRequest
 * @return UpdateAlertDestinationResponse
 */
async function updateAlertDestination(request: UpdateAlertDestinationRequest): UpdateAlertDestinationResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return updateAlertDestinationWithOptions(request, headers, runtime);
}

model UpdateAlertEnabledRequest {
  enabled?: boolean(name='enabled', description='Specifies whether the alert policy is enabled.', example='true'),
  id?: long(name='id', description='The ID of the alert policy.', example='1'),
}

model UpdateAlertEnabledResponseBody = {
  code?: string(name='code', description='The status code.
- If `code == Success`, the authorization is successful.
- Other status codes indicate authorization failed. Check the `message` field for the detailed fault information.', example='Success'),
  data?: any(name='data', description='The returned data.', example='{
    "uid": "1808078950770264",
    "name": "test-name",
    "strategy": {
      "items": [
        "节点CPU使用率检测"
      ],
      "clusters": [
        "cluster1"
      ]
    },
    "enabled": true,
    "id": 1,
    "created_at": 1753172727,
    "updated_at": 1753172727
  }'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the error information of the request.', example='success'),
  requestId?: string(name='requestId', description='Id of the request', example='2D693121-C925-5154-8DF6-C09A8B369822'),
}

model UpdateAlertEnabledResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateAlertEnabledResponseBody(name='body'),
}

/**
 * @summary Updates the status of a push alert policy.
 *
 * @param request UpdateAlertEnabledRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateAlertEnabledResponse
 */
@context("updateAlertEnabledWithContext")
async function updateAlertEnabledWithOptions(request: UpdateAlertEnabledRequest, headers: map[string]string, runtime: $RuntimeOptions): UpdateAlertEnabledResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.enabled)) {
    body['enabled'] = request.enabled;
  }
  if (!$isNull(request.id)) {
    body['id'] = request.id;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpdateAlertEnabled',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/alertPusher/alert/updateEnabled`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Updates the status of a push alert policy.
 *
 * @param request UpdateAlertEnabledRequest
 * @return UpdateAlertEnabledResponse
 */
async function updateAlertEnabled(request: UpdateAlertEnabledRequest): UpdateAlertEnabledResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return updateAlertEnabledWithOptions(request, headers, runtime);
}

model UpdateAlertStrategyRequest {
  enabled?: boolean(name='enabled', description='Specifies whether the alert policy is enabled.

This parameter is required.', example='true'),
  id?: long(name='id', description='The ID of the alert policy.

This parameter is required.', example='1'),
  k8sLabel?: boolean(name='k8sLabel'),
  name?: string(name='name', description='The Policy Name of the alerting policy.

This parameter is required.', example='strategy1'),
  strategy?: {
    clusters?: [ string ](name='clusters', description='The collection of clusters for which alerts are received.'),
    destinations?: [ int32 ](name='destinations'),
    items?: [ string ](name='items', description='接收告警的异常项合计'),
  }(name='strategy', description='The details of the alert policy.

This parameter is required.'),
}

model UpdateAlertStrategyResponseBody = {
  code?: string(name='code', description='The status code.
- If `code == Success`, the authorization is successful.
- Other status codes indicate authorization is failed. Check the `message` field for the detailed fault information.', example='Success'),
  data?: any(name='data', description='The response data.', example='{
            "uid": "uid-1",
            "name": "test-name",
            "strategy": {
                "clusters": ["test-cluster-1","test-cluster-2"],
                "items": ["test-item1","test-item2"]
            },
            "enabled": True
}'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error information.', example='更新告警策略失败'),
  requestId?: string(name='requestId', description='Id of the request', example='2D693121-C925-5154-8DF6-C09A8B369822'),
}

model UpdateAlertStrategyResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateAlertStrategyResponseBody(name='body'),
}

/**
 * @summary Updates a push alert policy.
 *
 * @param request UpdateAlertStrategyRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateAlertStrategyResponse
 */
@context("updateAlertStrategyWithContext")
async function updateAlertStrategyWithOptions(request: UpdateAlertStrategyRequest, headers: map[string]string, runtime: $RuntimeOptions): UpdateAlertStrategyResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.enabled)) {
    body['enabled'] = request.enabled;
  }
  if (!$isNull(request.id)) {
    body['id'] = request.id;
  }
  if (!$isNull(request.k8sLabel)) {
    body['k8sLabel'] = request.k8sLabel;
  }
  if (!$isNull(request.name)) {
    body['name'] = request.name;
  }
  if (!$isNull(request.strategy)) {
    body['strategy'] = request.strategy;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpdateAlertStrategy',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/alertPusher/alert/updateStrategy`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Updates a push alert policy.
 *
 * @param request UpdateAlertStrategyRequest
 * @return UpdateAlertStrategyResponse
 */
async function updateAlertStrategy(request: UpdateAlertStrategyRequest): UpdateAlertStrategyResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return updateAlertStrategyWithOptions(request, headers, runtime);
}

model UpdateEventsAttentionRequest {
  mode?: int32(name='mode', description='The sensitivity of the anomaly event. Valid values: -1 to 3. A value of -1 indicates that the sensitivity is decreased by 1. A value of 0 indicates that the sensitivity is increased by 1.', example='-1'),
  range?: string(name='range', description='The scope in which the update takes effect. Valid values: cluster and node.', example='cluster'),
  uuid?: string(name='uuid', description='The UUID of the anomaly event.

This parameter is required.', example='03de78af-f49f-433d-b5b1-0f6a70c493ba'),
}

model UpdateEventsAttentionResponseBody = {
  requestId?: string(name='RequestId', description='The request ID, which can be used for end-to-end diagnostics.', example='44841312-7227-55C9-AE03-D59729BFAE38'),
  code?: string(name='code', description='The status code. Valid values:
- Success: The authorization is successful.
- Other values: The authorization failed. Check the message field for the detailed fault information.', example='Success'),
  data?: {
    mode?: int32(name='mode', description='The updated attention level value.', example='1'),
  }(name='data', description='The response data.'),
  message?: string(name='message', description='The error message.', example='Instance not belong to current user'),
}

model UpdateEventsAttentionResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateEventsAttentionResponseBody(name='body'),
}

/**
 * @summary Updates the attention level of an anomaly item. Adjusting the attention level affects the sensitivity of the anomaly detection algorithm.
 *
 * @param request UpdateEventsAttentionRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateEventsAttentionResponse
 */
@context("updateEventsAttentionWithContext")
async function updateEventsAttentionWithOptions(request: UpdateEventsAttentionRequest, headers: map[string]string, runtime: $RuntimeOptions): UpdateEventsAttentionResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.mode)) {
    body['mode'] = request.mode;
  }
  if (!$isNull(request.range)) {
    body['range'] = request.range;
  }
  if (!$isNull(request.uuid)) {
    body['uuid'] = request.uuid;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpdateEventsAttention',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/openapi/proxy/post/cluster_update_events_attention`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Updates the attention level of an anomaly item. Adjusting the attention level affects the sensitivity of the anomaly detection algorithm.
 *
 * @param request UpdateEventsAttentionRequest
 * @return UpdateEventsAttentionResponse
 */
async function updateEventsAttention(request: UpdateEventsAttentionRequest): UpdateEventsAttentionResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return updateEventsAttentionWithOptions(request, headers, runtime);
}

model UpdateFuncSwitchRecordRequest {
  channel?: string(name='channel', description='The diagnostic channel. Currently, this parameter is fixed to the ECS channel.

This parameter is required.', example='ecs'),
  params?: {
    args?: {
      addCmd?: string(name='add_cmd', description='The configuration process.', example='java'),
      cpu?: string(name='cpu', description='The ONCPU tracing switch.', example='true'),
      duration?: int32(name='duration', description='The start duration, in seconds (s).', example='3600'),
      javaStorePath?: string(name='java_store_path', description='The storage path.', example='/tmp/sysom/java-profiler'),
      locks?: string(name='locks', description='The lock tracing switch.', example='true'),
      loop?: int32(name='loop', description='The tracing cycle.', example='-1'),
      mem?: string(name='mem', description='The memory tracing switch.', example='true'),
      pid?: int32(name='pid', description='The process PID.', example='1243'),
      systemProfiling?: string(name='system_profiling', description='The system tracing switch.', example='true'),
    }(name='args', description='The configuration parameters.'),
    functionName?: string(name='function_name', description='The feature name.

This parameter is required.', example='mullprof'),
    instance?: string(name='instance', description='The instance ID.', example='i-2zei55fwj8nnu31h3z46'),
    op?: string(name='op', description='The operation type.', example='restart'),
    region?: string(name='region', description='The region to which the instance belongs. All instance IDs passed in instances must belong to the same region.', example='cn-beijing'),
    uid?: string(name='uid', description='The user ID.', example='1664516888213680'),
  }(name='params', description='The diagnostic parameters. Different types of diagnostics require different diagnostic parameters. You can use this field to filter records whose parameters match the specified values.

This parameter is required.'),
  serviceName?: string(name='service_name', description='The service name.

This parameter is required.', example='livetrace'),
}

model UpdateFuncSwitchRecordShrinkRequest {
  channel?: string(name='channel', description='The diagnostic channel. Currently, this parameter is fixed to the ECS channel.

This parameter is required.', example='ecs'),
  paramsShrink?: string(name='params', description='The diagnostic parameters. Different types of diagnostics require different diagnostic parameters. You can use this field to filter records whose parameters match the specified values.

This parameter is required.'),
  serviceName?: string(name='service_name', description='The service name.

This parameter is required.', example='livetrace'),
}

model UpdateFuncSwitchRecordResponseBody = {
  code?: string(name='code', description='The status code.
- `code == Success` indicates that the authorization is successful.
- Other status codes indicate that the authorization failed. Check the `message` field for the detailed fault information.', example='Success'),
  data?: {
    taskId?: string(name='task_id', description='The task ID.', example='63fc5acb99e642d793f42912612e8001'),
  }(name='data', description='The response data.'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error information.', example='result: code=1 msg=(Request failed, status_code != 200)'),
  requestId?: string(name='requestId', description='Id of the request', example='2D693121-C925-5154-8DF6-C09A8B369822'),
}

model UpdateFuncSwitchRecordResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateFuncSwitchRecordResponseBody(name='body'),
}

/**
 * @summary Updates the configuration of a service feature module.
 *
 * @description - Populate parameters according to the general LLM service input parameters, convert them to a string, and assign the string to llmParamString.
 * - Convert the returned data from a string to a dict before use. Refer to the general LLM service response format.
 *
 * @param tmpReq UpdateFuncSwitchRecordRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateFuncSwitchRecordResponse
 */
@context("updateFuncSwitchRecordWithContext")
async function updateFuncSwitchRecordWithOptions(tmpReq: UpdateFuncSwitchRecordRequest, headers: map[string]string, runtime: $RuntimeOptions): UpdateFuncSwitchRecordResponse {
  tmpReq.validate();
  var request = new UpdateFuncSwitchRecordShrinkRequest{};
  OpenApiUtil.convert(tmpReq, request);
  if (!$isNull(tmpReq.params)) {
    request.paramsShrink = OpenApiUtil.arrayToStringWithSpecifiedStyle(tmpReq.params, 'params', 'json');
  }
  var query : map[string]any = {};
  if (!$isNull(request.channel)) {
    query['channel'] = request.channel;
  }
  if (!$isNull(request.paramsShrink)) {
    query['params'] = request.paramsShrink;
  }
  if (!$isNull(request.serviceName)) {
    query['service_name'] = request.serviceName;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpdateFuncSwitchRecord',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/func-switch/update-service-func-switch`,
    method = 'GET',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Updates the configuration of a service feature module.
 *
 * @description - Populate parameters according to the general LLM service input parameters, convert them to a string, and assign the string to llmParamString.
 * - Convert the returned data from a string to a dict before use. Refer to the general LLM service response format.
 *
 * @param request UpdateFuncSwitchRecordRequest
 * @return UpdateFuncSwitchRecordResponse
 */
async function updateFuncSwitchRecord(request: UpdateFuncSwitchRecordRequest): UpdateFuncSwitchRecordResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return updateFuncSwitchRecordWithOptions(request, headers, runtime);
}

model UpgradeAgentRequest {
  agentId?: string(name='agent_id', description='The ID of the component to update.

This parameter is required.', example='74a86327-3170-412c-8e67-da3389ec56a9'),
  agentVersion?: string(name='agent_version', description='The version to which the component is updated.

This parameter is required.', example='3.4.0-1'),
  instances?: [ 
    {
      instance?: string(name='instance', description='The instance ID.

This parameter is required.', example='i-wz9b9vucz1iubsz8sjqo'),
      region?: string(name='region', description='The region ID.

This parameter is required.', example='cn-hangzhou'),
    }
  ](name='instances', description='The list of instances for which you want to update the component.

This parameter is required.'),
}

model UpgradeAgentResponseBody = {
  requestId?: string(name='RequestId', description='The request ID, which can be used for end-to-end diagnostics.', example='B149FD9C-ED5C-5765-B3AD-05AA4A4D64D7'),
  code?: string(name='code', description='The status code.
- If `code == Success`, the authorization is successful.
- Other status codes indicate that the authorization has failed. Check the `message` field for the detailed fault information.', example='Success'),
  data?: {
    taskId?: string(name='task_id', description='The task ID.', example='391f5aeba2054f66b3aaef0136142fe2'),
  }(name='data', description='The returned data.'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error information.', example='SysomOpenAPIException: SysomOpenAPI.InvalidParameter Invalid params, should be json string or dict'),
}

model UpgradeAgentResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpgradeAgentResponseBody(name='body'),
}

/**
 * @summary Updates an installed component to a specified version.
 *
 * @description Updating the Agent by calling this operation is asynchronous. After you call this operation, a task_id is returned. You can use this ID to call the GetAgentTask operation to query the execution status of the task.
 *
 * @param request UpgradeAgentRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpgradeAgentResponse
 */
@context("upgradeAgentWithContext")
async function upgradeAgentWithOptions(request: UpgradeAgentRequest, headers: map[string]string, runtime: $RuntimeOptions): UpgradeAgentResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.agentId)) {
    body['agent_id'] = request.agentId;
  }
  if (!$isNull(request.agentVersion)) {
    body['agent_version'] = request.agentVersion;
  }
  if (!$isNull(request.instances)) {
    body['instances'] = request.instances;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpgradeAgent',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/am/agent/upgrade_agent`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Updates an installed component to a specified version.
 *
 * @description Updating the Agent by calling this operation is asynchronous. After you call this operation, a task_id is returned. You can use this ID to call the GetAgentTask operation to query the execution status of the task.
 *
 * @param request UpgradeAgentRequest
 * @return UpgradeAgentResponse
 */
async function upgradeAgent(request: UpgradeAgentRequest): UpgradeAgentResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return upgradeAgentWithOptions(request, headers, runtime);
}

model UpgradeAgentForClusterRequest {
  agentId?: string(name='agent_id', description='The component ID.', example='74a86327-3170-412c-8e67-da3389ec56a9'),
  agentVersion?: string(name='agent_version', description='The component version.', example='3.4.0-1'),
  clusterId?: string(name='cluster_id', description='The cluster ID.

> The cluster ID must be the ID of an ACK cluster.', example='c1c187fd513cb41a19876bac0e6b05212'),
}

model UpgradeAgentForClusterResponseBody = {
  requestId?: string(name='RequestId', description='The request ID, which can be used for end-to-end diagnostics.', example='B149FD9C-ED5C-5765-B3AD-05AA4A4D64D7'),
  code?: string(name='code', description='The status code.
- If `code == Success`, the authorization is successful.
- Other status codes indicate authorization failure. In this case, check the `message` field for detailed error information.', example='Success'),
  data?: {
    taskId?: string(name='task_id', description='The task ID.

You can use this task ID to call the GetAgentTask operation to query the task execution status.', example='7523e9e0ddc74d99a5236f4f4d5056e6'),
  }(name='data', description='The response data.'),
  message?: string(name='message', description='The error message.
- If `code == Success`, this field is empty.
- Otherwise, this field contains the request error message.', example='success'),
}

model UpgradeAgentForClusterResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpgradeAgentForClusterResponseBody(name='body'),
}

/**
 * @summary Updates components for a cluster.
 *
 * @param request UpgradeAgentForClusterRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpgradeAgentForClusterResponse
 */
@context("upgradeAgentForClusterWithContext")
async function upgradeAgentForClusterWithOptions(request: UpgradeAgentForClusterRequest, headers: map[string]string, runtime: $RuntimeOptions): UpgradeAgentForClusterResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.agentId)) {
    body['agent_id'] = request.agentId;
  }
  if (!$isNull(request.agentVersion)) {
    body['agent_version'] = request.agentVersion;
  }
  if (!$isNull(request.clusterId)) {
    body['cluster_id'] = request.clusterId;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpgradeAgentForCluster',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/am/agent/upgrade_agent_by_cluster`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Updates components for a cluster.
 *
 * @param request UpgradeAgentForClusterRequest
 * @return UpgradeAgentForClusterResponse
 */
async function upgradeAgentForCluster(request: UpgradeAgentForClusterRequest): UpgradeAgentForClusterResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return upgradeAgentForClusterWithOptions(request, headers, runtime);
}

model UpgradeAgentWithTypeRequest {
  agentId?: string(name='agentId', description='This parameter is required.', example='74a86327-3170-412c-8e67-da3389ec56a9'),
  agentVersion?: string(name='agentVersion', description='This parameter is required.', example='3.4.0-1'),
  instanceType?: string(name='instanceType', example='ecs'),
  instances?: [ 
    {
      instance?: string(name='instance', description='This parameter is required.', example='i-wz9b9vucz1iubsz8sjqo'),
      region?: string(name='region', description='This parameter is required.', example='cn-hangzhou'),
    }
  ](name='instances', description='This parameter is required.'),
}

model UpgradeAgentWithTypeResponseBody = {
  code?: string(name='code', example='Success'),
  data?: {
    taskId?: string(name='taskId', example='391f5aeba2054f66b3aaef0136142fe2'),
  }(name='data'),
  message?: string(name='message', example='“”'),
  requestId?: string(name='requestId', example='B149FD9C-ED5C-5765-B3AD-05AA4A4D64D7'),
}

model UpgradeAgentWithTypeResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpgradeAgentWithTypeResponseBody(name='body'),
}

/**
 * @summary 更新 SysOM Agent
 *
 * @description 调用本接口更新 Agent 是异步的，调用接口后会返回一个 task_id，可以凭借该 ID 调用 GetAgentTask 接口获取任务的执行情况。
 *
 * @param request UpgradeAgentWithTypeRequest
 * @param headers map
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpgradeAgentWithTypeResponse
 */
@context("upgradeAgentWithTypeWithContext")
async function upgradeAgentWithTypeWithOptions(request: UpgradeAgentWithTypeRequest, headers: map[string]string, runtime: $RuntimeOptions): UpgradeAgentWithTypeResponse {
  request.validate();
  var body : map[string]any = {};
  if (!$isNull(request.agentId)) {
    body['agentId'] = request.agentId;
  }
  if (!$isNull(request.agentVersion)) {
    body['agentVersion'] = request.agentVersion;
  }
  if (!$isNull(request.instanceType)) {
    body['instanceType'] = request.instanceType;
  }
  if (!$isNull(request.instances)) {
    body['instances'] = request.instances;
  }

  var req = new OpenApiUtil.OpenApiRequest{ 
    headers = headers,
    body = OpenApiUtil.parseToMap(body),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpgradeAgentWithType',
    version = '2023-12-30',
    protocol = 'HTTPS',
    pathname = `/api/v1/am/agent/upgradeAgent`,
    method = 'POST',
    authType = 'AK',
    style = 'ROA',
    reqBodyType = 'json',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 更新 SysOM Agent
 *
 * @description 调用本接口更新 Agent 是异步的，调用接口后会返回一个 task_id，可以凭借该 ID 调用 GetAgentTask 接口获取任务的执行情况。
 *
 * @param request UpgradeAgentWithTypeRequest
 * @return UpgradeAgentWithTypeResponse
 */
async function upgradeAgentWithType(request: UpgradeAgentWithTypeRequest): UpgradeAgentWithTypeResponse {
  var runtime = new $RuntimeOptions{};
  var headers : map[string]string = {};
  return upgradeAgentWithTypeWithOptions(request, headers, runtime);
}

