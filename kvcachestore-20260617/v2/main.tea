/**
 *
 */
import OpenApi;
import OpenApi.OpenApiUtil;

extends OpenApi;


init(config: OpenApiUtil.Config){
  super(config);
  @endpointRule = 'regional';
  @endpointMap = {
    'cn-beijing' = 'kvcachestore.cn-beijing.aliyuncs.com',
    'cn-shanghai' = 'kvcachestore.cn-shanghai.aliyuncs.com',
    'ap-southeast-1' = 'kvcachestore.ap-southeast-1.aliyuncs.com',
  };

  checkConfig(config);
  @endpoint = getEndpoint('kvcachestore', @regionId, @endpointRule, @network, @suffix, @endpointMap, @endpoint);
}

function getEndpoint(productId: string, regionId: string, endpointRule: string, network: string, suffix: string, endpointMap: map[string]string, endpoint: string) throws: string{
  if (!$isNull(endpoint)) {
    return endpoint;
  }
  
  if (!$isNull(endpointMap) && !$isNull(endpointMap[regionId])) {
    return endpointMap[regionId];
  }
  return OpenApiUtil.getEndpointRules(productId, regionId, endpointRule, network, suffix);
}

model AttachKVCacheStoreRequest {
  arns?: [ 
    {
      assumeRoleFor?: string(name='AssumeRoleFor', description='The UID of the target account for role assumption.', example='1193522024229156'),
      roleArn?: string(name='RoleArn', description='The Alibaba Cloud Resource Name (ARN) of the RAM role.

This parameter is required.', example='acs:ram::1028257687084022:role/zeus-locationservicerole'),
      roleType?: string(name='RoleType', description='The role type. Valid values:
- service
- user

This parameter is required.', example='service'),
    }
  ](name='Arns', description='The cross-account authorization role chain used for cross-account VSC resource verification.'),
  clientToken?: string(name='ClientToken', description='A string generated by the server that serves as a unique identifier for the client request.', example='YOUR_CLIENT_TOKEN'),
  regionId?: string(name='RegionId', description='The region ID, such as cn-hangzhou.

This parameter is required.', example='cn-hangzhou'),
  resourceIds?: [ 
    {
      kvcsId?: string(name='KvcsId', description='KVCacheStore KvcsId

This parameter is required.', example='kvcs-your-resource-id'),
      vscId?: string(name='VscId', description='The VSC ID pre-created on the compute side.

This parameter is required.', example='vsc-uf6acaf6k6mt1ts95to6al'),
    }
  ](name='ResourceIds', description='The list of resources to mount. A maximum of 100 resources can be specified.

This parameter is required.'),
}

model AttachKVCacheStoreResponseBody = {
  attachResults?: [ 
    {
      errorCode?: string(name='ErrorCode', description='The error code returned when the operation fails. This value is null when the operation succeeds.', example='InvalidStatus.OperationDenied'),
      errorMessage?: string(name='ErrorMessage', description='The error message returned when the operation fails. This value is null when the operation succeeds.', example='The operation is not allowed for instance in ATTACHED status.'),
      kvcsId?: string(name='KvcsId', description='KVCacheStore KvcsId', example='kvcs-xxxxx'),
      status?: string(name='Status', description='The operation result. Valid values:
- ATTACHING: The request has been accepted and the asynchronous mount is in progress.
- ATTACHED: The resource is already mounted (idempotent).
- Success: The synchronous verification passed and the asynchronous operation is complete.
- Failed: The operation failed.', example='ATTACHING'),
      vscId?: string(name='VscId', description='The VSC ID on the compute side.', example='vsc-001'),
    }
  ](name='AttachResults', description='The list of mount results.'),
  requestId?: string(name='RequestId', description='The request ID. A request ID is returned regardless of whether the operation is successful.', example='F7BEF1E2-7E56-5BF1-8E36-77A51C5812F1'),
}

model AttachKVCacheStoreResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: AttachKVCacheStoreResponseBody(name='body'),
}

/**
 * @summary Mounts KVCacheInstance resources to the virtualization side in batches.
 *
 * @description * This is an asynchronous operation. A return status of Attaching indicates that the request has been accepted. Call ListKVCacheStoreAttachInfo to query mount records. A record status of Attached indicates that the mount is complete.
 * * The KVCacheStore must be in the Available or InUse state before it can be mounted.
 *
 * @param request AttachKVCacheStoreRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return AttachKVCacheStoreResponse
 */
@context("attachKVCacheStoreWithContext")
async function attachKVCacheStoreWithOptions(request: AttachKVCacheStoreRequest, runtime: $RuntimeOptions): AttachKVCacheStoreResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.arns)) {
    query['Arns'] = request.arns;
  }
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  if (!$isNull(request.resourceIds)) {
    query['ResourceIds'] = request.resourceIds;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'AttachKVCacheStore',
    version = '2026-06-17',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Mounts KVCacheInstance resources to the virtualization side in batches.
 *
 * @description * This is an asynchronous operation. A return status of Attaching indicates that the request has been accepted. Call ListKVCacheStoreAttachInfo to query mount records. A record status of Attached indicates that the mount is complete.
 * * The KVCacheStore must be in the Available or InUse state before it can be mounted.
 *
 * @param request AttachKVCacheStoreRequest
 * @return AttachKVCacheStoreResponse
 */
async function attachKVCacheStore(request: AttachKVCacheStoreRequest): AttachKVCacheStoreResponse {
  var runtime = new $RuntimeOptions{};
  return attachKVCacheStoreWithOptions(request, runtime);
}

model CreateKVCacheStoreRequest {
  capacity?: long(name='Capacity', description='The storage capacity in GiB. The minimum capacity is 300 TiB (307200 GiB), and the capacity is scaled in increments of 300 TiB.

This parameter is required.', example='2395'),
  clientToken?: string(name='ClientToken', description='The client token used to ensure idempotence of the request. The token can be up to 64 characters in length. Use a UUID.', example='YOUR_CLIENT_TOKEN'),
  description?: string(name='Description', description='The KVCacheStore description. The description must be 2 to 256 characters in length and cannot start with http:// or https://. Default value: empty.', example='No description'),
  hpnZone?: string(name='HpnZone', description='The HPN cluster ID, which is used to create an affinity scheduling relationship between the KVCacheStore and the specified HPN cluster. After creation, the KVCacheStore may have affinity relationships with multiple HPN clusters based on network topology. You can call GetKVCacheStore to query the available HPN clusters.

This parameter is required.', example='B6'),
  name?: string(name='Name', description='The KVCacheStore name. The name must be 2 to 128 characters in length and can contain characters from the Unicode letter category (including English and Chinese characters) and digits. The name can contain colons (:), underscores (_), periods (.), and hyphens (-). If this parameter is not specified, the default value is the KVCacheStore ID.', example='sc-data-warehouse-server'),
  paymentType?: string(name='PaymentType', description='The billing method. Valid values: POSTPAY (pay-as-you-go). Default value: POSTPAY.', example='POSTPAY'),
  regionId?: string(name='RegionId', description='The region ID in which to create the KVCacheStore. You can call DescribeRegions to query the list of available regions.

This parameter is required.', example='cn-hangzhou'),
  resourceGroupId?: string(name='ResourceGroupId', description='The resource group ID.', example='rg-aekzafsjd7i4qaq'),
  tag?: [ 
    {
      tagKey?: string(name='TagKey', description='The tag key of the resource.', example='000098da1005a3df'),
      tagValue?: string(name='TagValue', description='The tag value of the resource.', example='000088aabb0023f7'),
    }
  ](name='Tag', description='The list of resource tag key-value pairs. A maximum of 20 tags are supported. This overrides the parent TagDTO type and uses the same Tag type as the Get/List response.'),
  zoneId?: string(name='ZoneId', description='The zone ID. You can call DescribeZones to query the list of zones in the specified region.

This parameter is required.', example='cn-hangzhou-b'),
}

model CreateKVCacheStoreResponseBody = {
  kvcsId?: string(name='KvcsId', description='The ID of the newly created KVCacheStore.', example='kvs-xxxxx'),
  orderId?: string(name='OrderId', description='The order ID generated by this creation request.', example='order-xxxxx'),
  requestId?: string(name='RequestId', description='The request ID.', example='API-a6e81d15-1b36-4d37-ad61-593531e7fa58'),
}

model CreateKVCacheStoreResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: CreateKVCacheStoreResponseBody(name='body'),
}

/**
 * @summary Creates a KVCacheInstance instance.
 *
 * @description * This is an asynchronous operation. After the request is successful, the KVCacheStore enters the Creating state. You can call GetKVCacheStore or ListKVCacheStores to query the KVCacheStore status. The KVCacheStore becomes available when its status changes to Available.
 * * To ensure idempotence in case of network exceptions, use ClientToken. If you call the operation with the same ClientToken and the same request parameters, the same KVCacheStore ID and order ID are returned. If the request parameters change, an IdempotentParameterMismatch error is returned.
 * * Creating a KVCacheStore generates an order. The response includes OrderId, which you can view in the Expenses and Costs console. For more information, refer to View and manage orders.
 *
 * @param request CreateKVCacheStoreRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return CreateKVCacheStoreResponse
 */
@context("createKVCacheStoreWithContext")
async function createKVCacheStoreWithOptions(request: CreateKVCacheStoreRequest, runtime: $RuntimeOptions): CreateKVCacheStoreResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.capacity)) {
    query['Capacity'] = request.capacity;
  }
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.description)) {
    query['Description'] = request.description;
  }
  if (!$isNull(request.hpnZone)) {
    query['HpnZone'] = request.hpnZone;
  }
  if (!$isNull(request.name)) {
    query['Name'] = request.name;
  }
  if (!$isNull(request.paymentType)) {
    query['PaymentType'] = request.paymentType;
  }
  if (!$isNull(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  if (!$isNull(request.resourceGroupId)) {
    query['ResourceGroupId'] = request.resourceGroupId;
  }
  if (!$isNull(request.tag)) {
    query['Tag'] = request.tag;
  }
  if (!$isNull(request.zoneId)) {
    query['ZoneId'] = request.zoneId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'CreateKVCacheStore',
    version = '2026-06-17',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Creates a KVCacheInstance instance.
 *
 * @description * This is an asynchronous operation. After the request is successful, the KVCacheStore enters the Creating state. You can call GetKVCacheStore or ListKVCacheStores to query the KVCacheStore status. The KVCacheStore becomes available when its status changes to Available.
 * * To ensure idempotence in case of network exceptions, use ClientToken. If you call the operation with the same ClientToken and the same request parameters, the same KVCacheStore ID and order ID are returned. If the request parameters change, an IdempotentParameterMismatch error is returned.
 * * Creating a KVCacheStore generates an order. The response includes OrderId, which you can view in the Expenses and Costs console. For more information, refer to View and manage orders.
 *
 * @param request CreateKVCacheStoreRequest
 * @return CreateKVCacheStoreResponse
 */
async function createKVCacheStore(request: CreateKVCacheStoreRequest): CreateKVCacheStoreResponse {
  var runtime = new $RuntimeOptions{};
  return createKVCacheStoreWithOptions(request, runtime);
}

model DeleteKVCacheStoreRequest {
  kvcsId?: string(name='KvcsId', description='The KvCacheStore instance ID. Only instances in the following states can be deleted: Available and Stopped. Instances in other states cannot be deleted.

This parameter is required.', example='kvcs-your-id'),
  regionId?: string(name='RegionId', description='The region ID, such as cn-hangzhou.

This parameter is required.', example='cn-hangzhou'),
}

model DeleteKVCacheStoreResponseBody = {
  kvcsId?: string(name='KvcsId', description='KVCacheStore KvcsId', example='kvcs-your-id'),
  requestId?: string(name='RequestId', description='The request ID.', example='B127704C-ECB1-5B0A-AA9C-8F394A6F179F'),
}

model DeleteKVCacheStoreResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DeleteKVCacheStoreResponseBody(name='body'),
}

/**
 * @summary Deletes a KVCacheInstance instance.
 *
 * @description * The delete operation is irreversible. Data in the KVCacheStore will be erased and cannot be recovered. Proceed with caution.
 * * Billing stops after the deletion begins.
 * * The KVCacheStore to be deleted cannot be in the Creating state.
 * * The KVCacheStore to be deleted cannot have mount records in the Attaching or Attached state.
 *
 * @param request DeleteKVCacheStoreRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DeleteKVCacheStoreResponse
 */
@context("deleteKVCacheStoreWithContext")
async function deleteKVCacheStoreWithOptions(request: DeleteKVCacheStoreRequest, runtime: $RuntimeOptions): DeleteKVCacheStoreResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.kvcsId)) {
    query['KvcsId'] = request.kvcsId;
  }
  if (!$isNull(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DeleteKVCacheStore',
    version = '2026-06-17',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Deletes a KVCacheInstance instance.
 *
 * @description * The delete operation is irreversible. Data in the KVCacheStore will be erased and cannot be recovered. Proceed with caution.
 * * Billing stops after the deletion begins.
 * * The KVCacheStore to be deleted cannot be in the Creating state.
 * * The KVCacheStore to be deleted cannot have mount records in the Attaching or Attached state.
 *
 * @param request DeleteKVCacheStoreRequest
 * @return DeleteKVCacheStoreResponse
 */
async function deleteKVCacheStore(request: DeleteKVCacheStoreRequest): DeleteKVCacheStoreResponse {
  var runtime = new $RuntimeOptions{};
  return deleteKVCacheStoreWithOptions(request, runtime);
}

model DescribeRegionsRequest {
  regionId?: string(name='RegionId', description='The region ID, such as cn-hangzhou.', example='cn-hangzhou'),
}

model DescribeRegionsResponseBody = {
  regions?: [ 
    {
      localName?: string(name='LocalName', description='The localized name of the region.', example='cn-hangzhou'),
      regionEndpoint?: string(name='RegionEndpoint', description='The endpoint of the region.', example='kvcachestore.cn-hangzhou.aliyuncs.com'),
      regionId?: string(name='RegionId', description='The region ID.', example='cn-hangzhou'),
    }
  ](name='Regions', description='The list of regions.'),
  requestId?: string(name='RequestId', description='The request ID. A request ID is returned regardless of whether the API call is successful.', example='019F6DDC-3845-542D-8E2C-60C4CC733BBD'),
}

model DescribeRegionsResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeRegionsResponseBody(name='body'),
}

/**
 * @summary Queries the list of regions where KVCacheStore is activated.
 *
 * @param request DescribeRegionsRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeRegionsResponse
 */
@context("describeRegionsWithContext")
async function describeRegionsWithOptions(request: DescribeRegionsRequest, runtime: $RuntimeOptions): DescribeRegionsResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeRegions',
    version = '2026-06-17',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the list of regions where KVCacheStore is activated.
 *
 * @param request DescribeRegionsRequest
 * @return DescribeRegionsResponse
 */
async function describeRegions(request: DescribeRegionsRequest): DescribeRegionsResponse {
  var runtime = new $RuntimeOptions{};
  return describeRegionsWithOptions(request, runtime);
}

model DescribeZonesRequest {
  regionId?: string(name='RegionId', description='The region ID, such as cn-hangzhou.

This parameter is required.', example='cn-hangzhou'),
}

model DescribeZonesResponseBody = {
  requestId?: string(name='RequestId', description='The request ID. The request ID is returned regardless of whether the API call is successful.', example='6AA27F1A-A62C-59C3-BCC7-D1DFA4E7EEA0'),
  zones?: [ 
    {
      localName?: string(name='LocalName', description='The localized name of the zone.', example='Hangzhou Zone A'),
      zoneId?: string(name='ZoneId', description='The zone ID.', example='cn-hangzhou-a'),
    }
  ](name='Zones', description='The list of zones.'),
}

model DescribeZonesResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DescribeZonesResponseBody(name='body'),
}

/**
 * @summary Queries the list of available zones for KVCacheStore in a specified region.
 *
 * @param request DescribeZonesRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DescribeZonesResponse
 */
@context("describeZonesWithContext")
async function describeZonesWithOptions(request: DescribeZonesRequest, runtime: $RuntimeOptions): DescribeZonesResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DescribeZones',
    version = '2026-06-17',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the list of available zones for KVCacheStore in a specified region.
 *
 * @param request DescribeZonesRequest
 * @return DescribeZonesResponse
 */
async function describeZones(request: DescribeZonesRequest): DescribeZonesResponse {
  var runtime = new $RuntimeOptions{};
  return describeZonesWithOptions(request, runtime);
}

model DetachKVCacheStoreRequest {
  clientToken?: string(name='ClientToken', description='A string generated by the server that serves as a unique identifier for the client request.', example='YOUR_CLIENT_TOKEN'),
  regionId?: string(name='RegionId', description='The region ID, such as cn-hangzhou.

This parameter is required.', example='cn-hangzhou'),
  resourceIds?: [ 
    {
      kvcsId?: string(name='KvcsId', description='KVCacheStore KvcsId

This parameter is required.', example='kvcs-your-resource-id'),
      vscId?: string(name='VscId', description='The VSC ID on the compute side.

This parameter is required.', example='vsc-uf6cpove22nrjnpenwkyal'),
    }
  ](name='ResourceIds', description='The list of resources to unmount. A maximum of 100 resources can be specified.

This parameter is required.'),
}

model DetachKVCacheStoreResponseBody = {
  detachResults?: [ 
    {
      errorCode?: string(name='ErrorCode', description='The error code when the operation fails. This value is null when the operation succeeds.', example='KVCacheInstance.NotAttached'),
      errorMessage?: string(name='ErrorMessage', description='The error message when the operation fails. This value is null when the operation succeeds.', example='The KVCacheInstance is not in ATTACHED status.'),
      kvcsId?: string(name='KvcsId', description='KVCacheStore KvcsId', example='kvcs-xxxxx'),
      status?: string(name='Status', description='The operation result. Valid values:

- DETACHING: The request has been accepted and the asynchronous unmount is in progress. This value is also returned for idempotent calls.
- Success: The synchronous validation passed and the asynchronous operation completed.
- Failed: The operation failed.', example='DETACHING'),
      vscId?: string(name='VscId', description='The VSC ID on the compute side.', example='vsc-001'),
    }
  ](name='DetachResults', description='The list of unmount results.'),
  requestId?: string(name='RequestId', description='The request ID. A request ID is returned regardless of whether the API call succeeds.', example='B127704C-ECB1-5B0A-AA9C-8F394A6F179F'),
}

model DetachKVCacheStoreResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: DetachKVCacheStoreResponseBody(name='body'),
}

/**
 * @summary Unmounts KVCacheInstance resources from the virtualization side in batches.
 *
 * @description * This is an asynchronous operation. A return status of Detaching indicates that the request has been accepted. Call ListKVCacheStoreAttachInfo to query mount records. When no record corresponding to the specified KvcsId and VscId is found, the unmount operation is complete.
 *
 * @param request DetachKVCacheStoreRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return DetachKVCacheStoreResponse
 */
@context("detachKVCacheStoreWithContext")
async function detachKVCacheStoreWithOptions(request: DetachKVCacheStoreRequest, runtime: $RuntimeOptions): DetachKVCacheStoreResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  if (!$isNull(request.resourceIds)) {
    query['ResourceIds'] = request.resourceIds;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'DetachKVCacheStore',
    version = '2026-06-17',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Unmounts KVCacheInstance resources from the virtualization side in batches.
 *
 * @description * This is an asynchronous operation. A return status of Detaching indicates that the request has been accepted. Call ListKVCacheStoreAttachInfo to query mount records. When no record corresponding to the specified KvcsId and VscId is found, the unmount operation is complete.
 *
 * @param request DetachKVCacheStoreRequest
 * @return DetachKVCacheStoreResponse
 */
async function detachKVCacheStore(request: DetachKVCacheStoreRequest): DetachKVCacheStoreResponse {
  var runtime = new $RuntimeOptions{};
  return detachKVCacheStoreWithOptions(request, runtime);
}

model GetKVCacheStoreRequest {
  kvcsId?: string(name='KvcsId', description='This parameter is required.', example='kvcs-your-id'),
  regionId?: string(name='RegionId', description='This parameter is required.', example='cn-hangzhou'),
}

model GetKVCacheStoreResponseBody = {
  kvCacheStore?: {
    capacity?: long(name='Capacity', example='100'),
    createTime?: string(name='CreateTime', example='2026-06-18T10:00:00Z'),
    description?: string(name='Description', example='production kvcachestore'),
    extraStatus?: string(name='ExtraStatus', example='CapacityExpanding'),
    hpnZone?: string(name='HpnZone', example='default'),
    kvcsId?: string(name='KvcsId', example='kvcs-xxxxx'),
    mountPointId?: string(name='MountPointId', example='mp-xxxxx'),
    name?: string(name='Name', example='production-instance'),
    paymentType?: string(name='PaymentType', example='POSTPAY'),
    regionId?: string(name='RegionId', example='cn-hangzhou'),
    resourceGroupId?: string(name='ResourceGroupId', example='rg-aekzrwkxbdvkctq'),
    status?: string(name='Status', example='AVAILABLE'),
    tags?: [ 
      {
        tagKey?: string(name='TagKey', example='ac-cus-tag-6'),
        tagValue?: string(name='TagValue', example='advanced'),
      }
    ](name='Tags'),
    type?: string(name='Type', example='kvcs'),
    zoneId?: string(name='ZoneId', example='cn-hangzhou-a'),
  }(name='KvCacheStore'),
  requestId?: string(name='RequestId', example='019FB5E9-F9E8-52F5-9C56-2CDF479CBEB2'),
}

model GetKVCacheStoreResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: GetKVCacheStoreResponseBody(name='body'),
}

/**
 * @summary 查询 KvCacheStore 实例详情
 *
 * @param request GetKVCacheStoreRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return GetKVCacheStoreResponse
 */
@context("getKVCacheStoreWithContext")
async function getKVCacheStoreWithOptions(request: GetKVCacheStoreRequest, runtime: $RuntimeOptions): GetKVCacheStoreResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.kvcsId)) {
    query['KvcsId'] = request.kvcsId;
  }
  if (!$isNull(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'GetKVCacheStore',
    version = '2026-06-17',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 查询 KvCacheStore 实例详情
 *
 * @param request GetKVCacheStoreRequest
 * @return GetKVCacheStoreResponse
 */
async function getKVCacheStore(request: GetKVCacheStoreRequest): GetKVCacheStoreResponse {
  var runtime = new $RuntimeOptions{};
  return getKVCacheStoreWithOptions(request, runtime);
}

model ListKVCacheStoreAttachInfoRequest {
  kvcsIds?: [ string ](name='KvcsIds', description='The list of KVCacheStore KvcsId values to query. A maximum of 100 values can be specified.

This parameter is required.'),
  maxResults?: long(name='MaxResults', description='The maximum number of entries to return in a single request. Valid values: 1 to 500.

Default value: 10.', example='20'),
  nextToken?: string(name='NextToken', description='The pagination token. Set this parameter to the NextToken value returned in the previous call. You do not need to set this parameter for the first request. If you set NextToken, the PageSize and PageNumber request parameters become ineffective, and the TotalCount value in the response is invalid.', example='your-client-token'),
  pageNumber?: int32(name='PageNumber', description='The page number for a paged query. Used together with PageSize. If the value exceeds the total number of pages, the last page of data is returned.', example='6'),
  pageSize?: int32(name='PageSize', description='The number of entries per page for a paged query.', example='20'),
  regionId?: string(name='RegionId', description='The region ID, such as cn-hangzhou.

This parameter is required.', example='cn-hangzhou'),
}

model ListKVCacheStoreAttachInfoResponseBody = {
  attachInfos?: [ 
    {
      attachedAt?: string(name='AttachedAt', description='The time of the most recent attach operation, in ISO 8601 format. The value is null if the instance has not been attached.', example='2026-06-20T08:30:00Z'),
      capacity?: long(name='Capacity', description='The file system capacity, in GiB.', example='100'),
      kvcsId?: string(name='KvcsId', description='KVCacheStore KvcsId', example='kvcs-xxxxx'),
      mountPointId?: string(name='MountPointId', description='The mount point ID at the file system level.', example='mp-xxxxx'),
      regionId?: string(name='RegionId', description='The region where the instance is deployed.', example='cn-hangzhou'),
      status?: string(name='Status', description='The attach status. Valid values:

- Attaching: The instance is being mounted.
- Attached: The instance is mounted.
- Detaching: The instance is being unmounted.

After unmounting is complete, the record is deleted and not returned.', example='ATTACHED'),
      type?: string(name='Type', description='The instance type. Valid values:

- kvcs: KVCacheStore (CPFS).', example='preview'),
      vscId?: string(name='VscId', description='The VSC ID on the compute side.', example='vsc-001'),
      zoneId?: string(name='ZoneId', description='The zone where the instance is deployed.', example='cn-hangzhou-a'),
    }
  ](name='AttachInfos', description='The list of mount information.'),
  nextToken?: string(name='NextToken', description='The pagination token used to query the next batch of data.', example='AAAAARbaCuN6hiD08qrLdwJ9Fh3NUkN7qf+fcWj7joK8M6tU'),
  pageNumber?: int32(name='PageNumber', description='The page number.', example='1'),
  pageSize?: int32(name='PageSize', description='The number of entries per page.', example='30'),
  requestId?: string(name='RequestId', description='The request ID. A request ID is returned regardless of whether the call is successful.', example='B127704C-ECB1-5B0A-AA9C-8F394A6F179F'),
  totalCount?: long(name='TotalCount', description='The total number of entries returned for the paged query.', example='2'),
}

model ListKVCacheStoreAttachInfoResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListKVCacheStoreAttachInfoResponseBody(name='body'),
}

/**
 * @summary Queries the mount information of KVCacheInstance resources in batches.
 *
 * @param request ListKVCacheStoreAttachInfoRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListKVCacheStoreAttachInfoResponse
 */
@context("listKVCacheStoreAttachInfoWithContext")
async function listKVCacheStoreAttachInfoWithOptions(request: ListKVCacheStoreAttachInfoRequest, runtime: $RuntimeOptions): ListKVCacheStoreAttachInfoResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.kvcsIds)) {
    query['KvcsIds'] = request.kvcsIds;
  }
  if (!$isNull(request.maxResults)) {
    query['MaxResults'] = request.maxResults;
  }
  if (!$isNull(request.nextToken)) {
    query['NextToken'] = request.nextToken;
  }
  if (!$isNull(request.pageNumber)) {
    query['PageNumber'] = request.pageNumber;
  }
  if (!$isNull(request.pageSize)) {
    query['PageSize'] = request.pageSize;
  }
  if (!$isNull(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListKVCacheStoreAttachInfo',
    version = '2026-06-17',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the mount information of KVCacheInstance resources in batches.
 *
 * @param request ListKVCacheStoreAttachInfoRequest
 * @return ListKVCacheStoreAttachInfoResponse
 */
async function listKVCacheStoreAttachInfo(request: ListKVCacheStoreAttachInfoRequest): ListKVCacheStoreAttachInfoResponse {
  var runtime = new $RuntimeOptions{};
  return listKVCacheStoreAttachInfoWithOptions(request, runtime);
}

model ListKVCacheStoreAvailableHpnZonesRequest {
  kvcsIds?: [ string ](name='KvcsIds', description='This parameter is required.'),
  regionId?: string(name='RegionId', description='This parameter is required.', example='cn-hangzhou'),
}

model ListKVCacheStoreAvailableHpnZonesResponseBody = {
  instanceHpnZones?: [ 
    {
      availableHpnZones?: [ 
        {
          hpnZone?: string(name='HpnZone', example='default'),
        }
      ](name='AvailableHpnZones'),
      kvcsId?: string(name='KvcsId', example='kvcs-xxxxx'),
      zoneId?: string(name='ZoneId', example='cn-hangzhou-a'),
    }
  ](name='InstanceHpnZones'),
  requestId?: string(name='RequestId', example='019FB5E9-F9E8-52F5-9C56-2CDF479CBEB2'),
}

model ListKVCacheStoreAvailableHpnZonesResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListKVCacheStoreAvailableHpnZonesResponseBody(name='body'),
}

/**
 * @summary 查询指定 KVCacheStore 实例可用的 HpnZone 列表
 *
 * @param request ListKVCacheStoreAvailableHpnZonesRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListKVCacheStoreAvailableHpnZonesResponse
 */
@context("listKVCacheStoreAvailableHpnZonesWithContext")
async function listKVCacheStoreAvailableHpnZonesWithOptions(request: ListKVCacheStoreAvailableHpnZonesRequest, runtime: $RuntimeOptions): ListKVCacheStoreAvailableHpnZonesResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.kvcsIds)) {
    query['KvcsIds'] = request.kvcsIds;
  }
  if (!$isNull(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListKVCacheStoreAvailableHpnZones',
    version = '2026-06-17',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary 查询指定 KVCacheStore 实例可用的 HpnZone 列表
 *
 * @param request ListKVCacheStoreAvailableHpnZonesRequest
 * @return ListKVCacheStoreAvailableHpnZonesResponse
 */
async function listKVCacheStoreAvailableHpnZones(request: ListKVCacheStoreAvailableHpnZonesRequest): ListKVCacheStoreAvailableHpnZonesResponse {
  var runtime = new $RuntimeOptions{};
  return listKVCacheStoreAvailableHpnZonesWithOptions(request, runtime);
}

model ListKVCacheStoresRequest {
  kvcsIds?: string(name='KvcsIds', description='The list of KvcsId values. Separate multiple IDs with commas. A maximum of 100 IDs are supported.', example='kvcs-87djda131'),
  maxResults?: int32(name='MaxResults', description='The maximum number of entries per page for cursor-based pagination. Default value: 10. Maximum value: 100. This parameter is used together with NextToken.', example='10'),
  name?: string(name='Name', description='The instance name filter. Prefix matching is used.', example='obj-detect'),
  nextToken?: string(name='NextToken', description='The pagination token. Do not specify this parameter for the first request. For subsequent requests, use the NextToken value returned in the previous response. This parameter is mutually exclusive with PageNumber.', example='a24c3a9cc8e6da77b10cffc4c93c7922e0b5e8707e68181f'),
  pageNumber?: int32(name='PageNumber', description='The page number. Default value: 1. This parameter takes precedence over NextToken if both are specified.', example='1'),
  pageSize?: int32(name='PageSize', description='The number of entries per page. Default value: 10. Maximum value: 100. This parameter is used together with PageNumber.', example='10'),
  regionId?: string(name='RegionId', description='The region ID, such as cn-hangzhou.

This parameter is required.', example='cn-hangzhou'),
  status?: string(name='Status', description='The instance status filter. Valid values: Creating, Available, InUse, Stopping, Stopped, and Deleting.', example='Available'),
  zoneId?: string(name='ZoneId', description='The zone ID, such as cn-hangzhou-a.', example='cn-hangzhou-k'),
}

model ListKVCacheStoresResponseBody = {
  KVCacheStores?: [ 
    {
      capacity?: long(name='Capacity', description='The storage capacity. Unit: GiB.', example='100'),
      createTime?: string(name='CreateTime', description='The creation time in ISO 8601 format.', example='2026-06-18T10:00:00Z'),
      description?: string(name='Description', description='The instance description.', example='production kvcachestore'),
      extraStatus?: string(name='ExtraStatus', description='The extra status information. Valid values: CapacityExpanding, CapacityExpandSuccess, and CapacityExpandFail.', example='CapacityExpanding'),
      hpnZone?: string(name='HpnZone', description='The cluster ID.', example='default'),
      kvcsId?: string(name='KvcsId', description='The instance ID.', example='kvcs-xxxxx'),
      mountPointId?: string(name='MountPointId', description='The file system-level mount point ID. Instances under the same file system share this mount point. For more information, call ListKVCacheInstanceAttachInfo.', example='mp-xxxxx'),
      name?: string(name='Name', description='The instance name.', example='production-instance'),
      paymentType?: string(name='PaymentType', description='The payment type. Valid values: PREPAY and POSTPAY.', example='POSTPAY'),
      regionId?: string(name='RegionId', description='The region ID.', example='cn-hangzhou'),
      resourceGroupId?: string(name='ResourceGroupId', description='The resource group ID.', example='rg-aek3dnrvdxj2dvq'),
      status?: string(name='Status', description='The instance status. Valid values: Creating, Available, InUse, Stopping, Stopped, and Deleting.', example='AVAILABLE'),
      tags?: [ 
        {
          tagKey?: string(name='TagKey', description='The tag key of the resource.', example='chapter'),
          tagValue?: string(name='TagValue', description='The tag value of the resource.', example='test-value-1766542011'),
        }
      ](name='Tags', description='The list of resource tags.'),
      type?: string(name='Type', description='The instance type. Valid values: kvcs (KVCacheStore, CPFS).', example='preview'),
      zoneId?: string(name='ZoneId', description='The zone ID.', example='cn-hangzhou-a'),
    }
  ](name='KVCacheStores', description='The list of KVCacheStore instances. Each element contains the following fields: KvcsId, Name, Status, ExtraStatus, RegionId, ZoneId, HpnZone, Type, Capacity, PaymentType, MountPointId, CreateTime, and Description.'),
  maxResults?: int32(name='MaxResults', description='The maximum number of entries returned per pagination request.', example='10'),
  nextToken?: string(name='NextToken', description='The pagination token for the next page. This value is empty when no more data is available. This parameter is valid only for cursor-based pagination.', example='caeba0bbb2be03f84eb48b699f0a4883'),
  pageNumber?: int32(name='PageNumber', description='The current page number. This parameter is valid only for page number-based pagination.', example='1'),
  pageSize?: int32(name='PageSize', description='The number of entries per page. This parameter is valid only for page number-based pagination.', example='10'),
  pageTotal?: int32(name='PageTotal', description='The total number of pages. This value is returned only for page number-based pagination.', example='1'),
  requestId?: string(name='RequestId', description='The request ID. A request ID is returned regardless of whether the API call succeeds.', example='56AC37CD-388E-5D21-951B-C50D16D8E812'),
  totalCount?: int32(name='TotalCount', description='The total number of instances. This value is returned only for page number-based pagination. For cursor-based pagination, the value is -1.', example='10'),
}

model ListKVCacheStoresResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: ListKVCacheStoresResponseBody(name='body'),
}

/**
 * @summary Queries the list of KVCacheInstance instances.
 *
 * @description * Two pagination methods are supported: page number-based pagination (PageNumber + PageSize) and cursor-based pagination (NextToken + MaxResults). If both are specified, page number-based pagination takes precedence.
 *
 * @param request ListKVCacheStoresRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return ListKVCacheStoresResponse
 */
@context("listKVCacheStoresWithContext")
async function listKVCacheStoresWithOptions(request: ListKVCacheStoresRequest, runtime: $RuntimeOptions): ListKVCacheStoresResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.kvcsIds)) {
    query['KvcsIds'] = request.kvcsIds;
  }
  if (!$isNull(request.maxResults)) {
    query['MaxResults'] = request.maxResults;
  }
  if (!$isNull(request.name)) {
    query['Name'] = request.name;
  }
  if (!$isNull(request.nextToken)) {
    query['NextToken'] = request.nextToken;
  }
  if (!$isNull(request.pageNumber)) {
    query['PageNumber'] = request.pageNumber;
  }
  if (!$isNull(request.pageSize)) {
    query['PageSize'] = request.pageSize;
  }
  if (!$isNull(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  if (!$isNull(request.status)) {
    query['Status'] = request.status;
  }
  if (!$isNull(request.zoneId)) {
    query['ZoneId'] = request.zoneId;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'ListKVCacheStores',
    version = '2026-06-17',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Queries the list of KVCacheInstance instances.
 *
 * @description * Two pagination methods are supported: page number-based pagination (PageNumber + PageSize) and cursor-based pagination (NextToken + MaxResults). If both are specified, page number-based pagination takes precedence.
 *
 * @param request ListKVCacheStoresRequest
 * @return ListKVCacheStoresResponse
 */
async function listKVCacheStores(request: ListKVCacheStoresRequest): ListKVCacheStoresResponse {
  var runtime = new $RuntimeOptions{};
  return listKVCacheStoresWithOptions(request, runtime);
}

model UpdateKVCacheStoreRequest {
  capacity?: long(name='Capacity', description='The new storage capacity in GiB. The value must be a multiple of 300 TiB and greater than the current capacity.', example='4096'),
  clientToken?: string(name='ClientToken', description='The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can be up to 64 ASCII characters in length.', example='YOUR_CLIENT_TOKEN'),
  description?: string(name='Description', description='The new KVCacheStore description. The description must be 2 to 256 characters in length and can contain English and Chinese characters. The description cannot start with http:// or https://. Default value: empty.', example='project name pass the check'),
  kvcsId?: string(name='KvcsId', description='The KVCacheStore instance ID.

This parameter is required.', example='kvcs-your-id'),
  name?: string(name='Name', description='The new KVCacheStore name. The name must be 2 to 128 characters in length and can contain characters that are categorized as letter in Unicode (including English and Chinese characters) and digits. The name can contain colons (:), underscores (_), periods (.), and hyphens (-). If this parameter is not specified, the default value is the KVCacheStore ID.', example='1633730290118313-HD-m3u8'),
  regionId?: string(name='RegionId', description='The region ID, such as cn-hangzhou.

This parameter is required.', example='cn-hangzhou'),
  tag?: [ 
    {
      tagKey?: string(name='TagKey', description='The tag key of the resource.', example='projectId'),
      tagValue?: string(name='TagValue', description='The tag value of the resource.', example='projectName'),
    }
  ](name='Tag', description='The list of resource tag key-value pairs. A maximum of 20 tags are supported.'),
}

model UpdateKVCacheStoreResponseBody = {
  kvcsId?: string(name='KvcsId', description='The ID of the modified KVCacheStore.', example='kvs-xxxxx'),
  orderId?: string(name='OrderId', description='The specification change order ID. Returned only when Capacity is modified.', example='order-xxxxx'),
  requestId?: string(name='RequestId', description='The request ID. The request ID is returned regardless of whether the operation is successful.', example='6AA27F1A-A62C-59C3-BCC7-D1DFA4E7EEA0'),
}

model UpdateKVCacheStoreResponse = {
  headers?: map[string]string(name='headers'),
  statusCode?: int32(name='statusCode'),
  body?: UpdateKVCacheStoreResponseBody(name='body'),
}

/**
 * @summary Modifies the attributes of a KVCacheStore.
 *
 * @description * Supports modifying Name, Description, and Capacity. The cluster number (HpnZone) cannot be modified after creation.
 * * Modifying Capacity triggers a specification change order, and the operation returns an OrderId. Modifying only Name or Description does not generate an order.
 *
 * @param request UpdateKVCacheStoreRequest
 * @param runtime runtime options for this request RuntimeOptions
 * @return UpdateKVCacheStoreResponse
 */
@context("updateKVCacheStoreWithContext")
async function updateKVCacheStoreWithOptions(request: UpdateKVCacheStoreRequest, runtime: $RuntimeOptions): UpdateKVCacheStoreResponse {
  request.validate();
  var query = {};
  if (!$isNull(request.capacity)) {
    query['Capacity'] = request.capacity;
  }
  if (!$isNull(request.clientToken)) {
    query['ClientToken'] = request.clientToken;
  }
  if (!$isNull(request.description)) {
    query['Description'] = request.description;
  }
  if (!$isNull(request.kvcsId)) {
    query['KvcsId'] = request.kvcsId;
  }
  if (!$isNull(request.name)) {
    query['Name'] = request.name;
  }
  if (!$isNull(request.regionId)) {
    query['RegionId'] = request.regionId;
  }
  if (!$isNull(request.tag)) {
    query['Tag'] = request.tag;
  }
  var req = new OpenApiUtil.OpenApiRequest{ 
    query = OpenApiUtil.query(query),
  };
  var params = new OpenApiUtil.Params{
    action = 'UpdateKVCacheStore',
    version = '2026-06-17',
    protocol = 'HTTPS',
    pathname = '/',
    method = 'POST',
    authType = 'AK',
    style = 'RPC',
    reqBodyType = 'formData',
    bodyType = 'json',
  };
  return callApi(params, req, runtime);
}

/**
 * @summary Modifies the attributes of a KVCacheStore.
 *
 * @description * Supports modifying Name, Description, and Capacity. The cluster number (HpnZone) cannot be modified after creation.
 * * Modifying Capacity triggers a specification change order, and the operation returns an OrderId. Modifying only Name or Description does not generate an order.
 *
 * @param request UpdateKVCacheStoreRequest
 * @return UpdateKVCacheStoreResponse
 */
async function updateKVCacheStore(request: UpdateKVCacheStoreRequest): UpdateKVCacheStoreResponse {
  var runtime = new $RuntimeOptions{};
  return updateKVCacheStoreWithOptions(request, runtime);
}

